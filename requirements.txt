# Requirements for AquaCal
#
# This file mirrors the runtime dependencies from pyproject.toml for pip-only
# users. For development, prefer: pip install -e ".[dev]"
#
# Authoritative source: pyproject.toml

numpy
scipy>=1.16
# Pinned to one minor: corner detection changes across OpenCV minors and the
# published real-rig numbers were produced under 4.13.0. See pyproject.toml.
opencv-python==4.13.*
pyyaml
matplotlib
pandas
requests
tqdm
natsort>=8.4.0
