---
phase: 28-full-suite-production-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/
  - /home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/
  - /home/tlancaster/freeze02-install-command.txt
  - /home/tlancaster/freeze02-env.txt
  - /home/tlancaster/freeze02-pip-freeze.txt
  - /home/tlancaster/freeze02-pytest-prelaunch.txt
autonomous: true
requirements: [RUN-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A clone of tag rerun-freeze-02 exists at /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod, its HEAD is 7005a2771aa115e4f4c1284cec7e145739586a4a, and that path has never been used by a rehearsal (C1, D5)."
    - "The clone holds no real driver state file for this sha, no experiments/results, and no experiments/results_e2_band -- the three absences the pre-flight and the completeness gate depend on (D5)."
    - "A NEW conda environment aquacal-freeze02-prod exists, and inside it aquacal.__file__ resolves to a path under the production clone -- not under aquacal-frozen-rerun-freeze-01 and not under the rehearsal clone."
    - "cv2.__version__ reports 4.13.x, and pytest and psutil both import -- the two extras whose absence killed e3 and nulled two required manifest fields on attempt 1."
    - "The driver's dry run walked all 20 stages at exit 0, writing only to the .dryrun.tsv path, so the real state file is still absent afterwards."
    - "pytest tests/ -q in the new environment reports exactly 3 failures and their node ids match the three registered in 29.1-PREPUSH-AUDIT.md section 1 (D4)."
  prohibitions:
    - "Results produced by code imported from any checkout other than the production clone must never be presented as results of rerun-freeze-02."
    - "No artifact of this phase may state or imply that the test suite is clean; the three known failures must be reported as they are, never suppressed, re-baselined or renormalised (D4)."
  artifacts:
    - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/
    - /home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python
    - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/run_experiment_suite_state.7005a27.dryrun.tsv
    - /home/tlancaster/freeze02-install-command.txt
    - /home/tlancaster/freeze02-env.txt
    - /home/tlancaster/freeze02-pip-freeze.txt
    - /home/tlancaster/freeze02-pytest-prelaunch.txt
  key_links:
    - "Three of the four conda environments on this machine carry an editable aquacal pointing at the WRONG checkout -- including aquacal-freeze02-cleanenv, which points at the freeze-01 tree despite its name. No gate catches this: a run under one of them imports other code while stamping 7005a27 into every artifact. The import assertion in task 2 is the only control that exists, and it must run before the six-hour burn, not after."
    - "STATE_FILE is derived from RUN_EXPERIMENT_SUITE_DRY_RUN and the short sha only (run_experiment_suite.sh:287-292); --smoke does not enter it. A tree that already carries 20 complete-at-exit-0 lines for 7005a27 makes a real run skip all 20 stages and produce nothing while looking finished. The fresh clone at a NEW path is the mitigation, and the absence assertion is the proof."
    - "The dry run writes only to the .dryrun.tsv path, so it cannot poison the real state file -- that separation is why it is safe to walk the whole queue before committing hours to it."
---

<objective>
Build the production venue for the re-run and prove it end to end before anything expensive
happens: a fresh clone of `rerun-freeze-02` at a path no rehearsal has touched, a NEW conda
environment built by executing the tag's own install command, and a dry run that walks all 20
stages at exit 0 from inside that clone.

Purpose: every one of the three things most likely to destroy this run is decided here.
Attempt 1's history and 29.1's rehearsal both show the failure modes are environmental, not
scientific — a contaminated environment, a contaminated tree, or a misread verdict. This plan
closes the first two and costs minutes.

Output: a verified production clone, a verified environment, and the first three `freeze02-*`
evidence files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-full-suite-production-run/28-CONTEXT.md
@.planning/phases/28-full-suite-production-run/28-RESEARCH.md
@.planning/phases/28-full-suite-production-run/28-VALIDATION.md
</context>

## Phase constants (all plans)

    CLONE   = /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod
    ENV     = aquacal-freeze02-prod
    ENVPY   = /home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python
    CONDASH = /home/tlancaster/anaconda3/etc/profile.d/conda.sh
    TAG     = rerun-freeze-02
    SHA     = 7005a2771aa115e4f4c1284cec7e145739586a4a   (short: 7005a27)
    STATE   = $CLONE/experiments/run_experiment_suite_state.7005a27.tsv
    LOG     = /home/tlancaster/suite_run_freeze02.log

**The clone path is `…-freeze-02-prod`, with the suffix, everywhere (C1).**
`~/aquacal-frozen-rerun-freeze-02` is the 29.1-08 rehearsal clone and must be neither reused
nor shadowed. `experiments/HANDOFF.md` §2.1 writes `cd "$HOME/aquacal-frozen-<tag>"`, which
resolves to the *plain* name — do not follow that line literally. This matters mechanically,
not cosmetically: `e7_focal_standoff` and `reconstruction_bootstrap` read cwd-relative paths and
the driver `cd`s to the repo root itself (`run_experiment_suite.sh:261`).

## Hard constraints (all plans)

1. **The clone is frozen. Nothing under it is edited — not `src/`, not `experiments/`, not a
   config, not a typo.** A stage that ran at a different commit than the rest makes the whole
   run unreportable (`HANDOFF.md:419-421`). If something is wrong, it is a finding, filed and
   returned — not repaired here.
2. **No pre-flight override flags.** Not `--allow-nonempty-out`, not `--skip-e2`, not
   `--allow-frameset-mismatch`, in any form. A refusal is a signal. If pre-flight refuses over a
   non-empty tree, move the offending tree aside — **delete nothing** — and re-assert.
3. **`SUITE_E2_RELEASE_CONFIG` stays unset (D-12).** It is the Windows escape hatch; on Linux
   the in-repo default `experiments/configs/e2_release_linux.yaml` resolves correctly.
4. **`SUITE_DISPATCH_LOG` stays unset (B1).** Match attempt 1's invocation exactly. The cost is
   that success criterion 3 is *derived* rather than directly observed; the derivation is
   written down in `28-03-PLAN.md` and carried into `28-RUN-RECORD.md`.
5. **`SUITE_WORKERS` and `SUITE_THREAD_CAP` keep their defaults (4 and 2).** RAM is the binding
   resource and the `serial_alone` timing stages are BLAS-unpinned on purpose. Do not re-tune.
6. **A healthy run exits NON-ZERO.** Acceptance is never `$?`. See `28-03-PLAN.md`.
7. **This phase commits and pushes no results.** No `results/rerun-freeze-02` branch, no tag,
   no Zenodo upload — those are Phase 29 (A1, RUN-03/04/05).
8. **Delete nothing, and do not overwrite attempt 1's evidence.** `~/rerun-freeze-01-output.tar.gz`
   and `~/suite_run_freeze01.log.preserved` are read-only and are the only copy of attempt 1's
   bulk.

## Artifacts this phase produces

| Artifact | Kind | Plan |
|---|---|---|
| `/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/` | fresh clone at the tag (off-repo) | 01 |
| `/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/` | new conda environment | 01 |
| `$CLONE/experiments/results/` | run output (62-artifact tree) | 03 |
| `$CLONE/experiments/results_e2_invocations/` | run output | 03 |
| `$CLONE/experiments/results_e2_timing/` | run output | 03 |
| `$CLONE/experiments/results_e2_memory/` | run output | 03 |
| `$CLONE/experiments/results_e2_band/` | run output | 03 |
| `$CLONE/experiments/results_e4_repeat/` | run output | 03 |
| `$CLONE/experiments/run_experiment_suite_state.7005a27.tsv` | driver state — the only per-stage timing record that exists | 03 |
| `$CLONE/experiments/run_experiment_suite_state.7005a27.failures.txt` | driver findings | 03 |
| `$CLONE/experiments/run_experiment_suite_state.7005a27.stagelogs/` | per-stage logs | 03 |
| `/home/tlancaster/suite_run_freeze02.log` | run log, outside the clone and outside the output tree | 03 |
| `/home/tlancaster/suite_run_freeze02.log.preserved` | read-only copy of the run log | 04 |
| `/home/tlancaster/rerun-freeze-02-output.tar.gz` | nine-path archive, read-only | 04 |
| `.planning/phases/28-full-suite-production-run/freeze02-install-command.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-env.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-pip-freeze.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-pytest-prelaunch.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-prelaunch-assertions.txt` | evidence | 02 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-rollup.txt` | evidence | 03 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-stage-timing.txt` | evidence | 03 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-tree-state-at-handoff.txt` | evidence | 04 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-archive-manifest.txt` | archive completeness record (per-tree file counts) | 04 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-gates-full.txt` | evidence | 05 |
| `.planning/phases/28-full-suite-production-run/rerun-freeze-02-output.sha256` | checksum manifest | 04 → 05 |
| `.planning/phases/28-full-suite-production-run/28-RUN-RECORD.md` | returned-artifact index and criterion derivations | 05 |

**Explicitly NOT produced by this phase:** the branch `results/rerun-freeze-02`, any git tag, any
push from the production clone, any Zenodo upload, and any grading verdict over the returned run.
All of those are Phase 29 (A1).

## Flagged planner assumptions

The spec-less edge probe over this phase's single requirement returned exactly one row, and it
came back **`unclassified`**. It is surfaced here rather than resolved, because an unclassified
row must never be auto-resolved with a backstop and must never be silently dropped.

| # | Requirement | Probe status | Assumption the plan set makes | If it is wrong |
|---|---|---|---|---|
| FA-1 | RUN-02 — *"the full experiment suite — E1 through E7, the band runs, and E2 — executes once end to end at that single sha"* | `unclassified` / `unresolved` | The planner reads "once end to end" as **one uninterrupted invocation of `experiments/run_experiment_suite.sh` covering all 20 driver stages at exit 0**, and treats an automatic resume after an *infrastructure* failure (reboot, OOM kill, full disk) as still satisfying it, provided every stage's completion line records exit 0 in the one state file. A restart caused by a `src/` defect would NOT satisfy it — that is a restart from stage 1 at a new sha (`run_experiment_suite.sh:183-192`). | If the author intends "once" to exclude even an infrastructure resume, the run must be relaunched from a clean state file rather than resumed, and the returned record must say which happened. The state TSV makes either case visible, so the evidence supports both readings; only the wording of the verdict changes. |

## Package legitimacy — why no install checkpoint appears here

`28-RESEARCH.md` § *Package Legitimacy Audit* ran the registry seam this session over the frozen
dependency set. **Zero `SLOP` verdicts.** Every package returned `SUS`/`OK` with the reason
`unknown-downloads`, `no-repository` or `too-new` — all three properties of PyPI's metadata
surface and of a frequent release cadence, not of package identity. Every row's disposition is
**Approved**, with a stated reason.

Decisively: **this phase selects no package.** It installs exactly what the frozen
`pyproject.toml` declares, and that same dependency set has already been installed and executed
twice on this machine (attempt 1's production run and 29.1-08's clone verification). The
load-bearing assertion here is a **version** assertion, not an identity assertion, and it is
gated in task 2: `cv2.__version__` must print `4.13.x`. Same disposition and precedent as
`29.1-08-PLAN.md` T-29.1-SC.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the frozen queue runs in this clone" — clone, environment, install, dry run</name>
  <precondition>The E2 frameset root /home/tlancaster/PycharmProjects/AquaCal/aquacal_data/real-rig/real-rig/extrinsic/ exists and holds 13 subdirectories totalling at least 3,000,000,000 bytes (pre-flight's identity floor, suite_expectations.json → preflight.frameset.cheap_check.min_total_bytes). Halt and report if it does not — the run would be refused or, worse, made synthetic-only.</precondition>
  <files>/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/ (created), /home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/ (created), /home/tlancaster/freeze02-install-command.txt (created)</files>
  <read_first>
    - `28-RESEARCH.md` § *User Constraints* (D5, the fresh-clone rule) and § *Pitfall 3* (the
      state-file collision) — why the path suffix and the absence assertions exist.
    - `28-RESEARCH.md` § *The install command — read it out of the clone, do not retype it* and
      § *Environments already on this machine — none of them is usable as-is*.
    - `28-RESEARCH.md` § *Validation Architecture* → *Sampling rate* — the dry run's safety
      argument (`.dryrun.tsv` separation).
    - Inside the clone, once it exists: `experiments/HANDOFF.md` §1.1 (the interpreter), §1.2
      (the dependency set and the one hard pin — the install command is at line 49), and §2.1
      (the invocation; note its `cd` line assumes the plain path name, which this phase does not
      use).
    - Inside the clone: `experiments/run_experiment_suite.sh:264-292` (the six output trees, the
      state artifacts, and the `STATE_FILE` derivation) and `:541-562` (the 20-stage array).
  </read_first>
  <action>
    Wire one path through every layer this phase touches — remote tag → clone → conda
    environment → editable install → driver → 20-stage queue → state file — and prove it in
    about a second, before any hours are committed.

    **1. Clone, at the new path (C1, D5).** From `$HOME`:

        git clone --branch rerun-freeze-02 https://github.com/McGrathLab/AquaCal.git aquacal-frozen-rerun-freeze-02-prod

    Confirm `git -C $CLONE rev-parse HEAD` is the frozen sha. If the network clone is
    unavailable, clone from the local repository at the tag exactly as 29.1-08 did, and record
    in the SUMMARY that the source was local — the tree is what matters, but the provenance of
    how it was obtained is part of the record.

    **2. Assert the three absences immediately, before anything writes.** In the clone:
    no `experiments/run_experiment_suite_state.7005a27.tsv`; no `experiments/results`; no
    `experiments/results_e2_band`. The third is not pedantry — that directory holds zero tracked
    files at HEAD and git does not track empty directories, so a fresh clone genuinely lacks it,
    and *absent* scores one `N/A` where *present-and-empty* scores two FAILs. If any of the
    three is present, **halt and report**; do not delete, do not override.

    **3. Build a NEW conda environment.** None of the four already on this machine is usable:
    `aquacal` carries OpenCV 4.14.0 (the version the pin exists to reject), and three others
    carry an editable `aquacal` pointing at the wrong checkout — including
    `aquacal-freeze02-cleanenv`, which points at the **freeze-01** tree despite its name.

        /home/tlancaster/anaconda3/bin/conda create -y -n aquacal-freeze02-prod python=3.11

    Python 3.11 matches attempt 1's 3.11.15 and satisfies `requires-python = ">=3.11"`.

    **4. Install by executing the tag's OWN command, read out of the clone.** Do not retype it
    from memory — 29.1's entire criterion 5 was that the remembered command was wrong. Print
    every `pip install -e` hit in `experiments/HANDOFF.md` with line numbers, identify the §1.2
    install line (line 49), write that exact line to `/home/tlancaster/freeze02-install-command.txt`,
    and execute that string verbatim from the clone root in the activated environment:

        source /home/tlancaster/anaconda3/etc/profile.d/conda.sh
        conda activate aquacal-freeze02-prod
        cd /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod
        <the line read from HANDOFF.md line 49>

    Both extras are load-bearing: `dev` supplies `pytest`, without which `e3` exits 1 on both
    `--check` and `--force` (attempt 1's only stage failure); `bench` supplies `psutil`, without
    which `cpu_count_logical` and `ram_total_bytes` go null and `gate3_run_manifest_fields`
    FAILs. Do not install the `docs` extra.

    **5. Walk the whole queue, dry.** From the clone root in the same activated shell:

        RUN_EXPERIMENT_SUITE_DRY_RUN=1 bash experiments/run_experiment_suite.sh

    This exits **0**, walks all 20 stages in about a second, and writes only to the
    `.dryrun.tsv` path — a separation the driver has explicitly and `--smoke` does not, which is
    exactly why this is the safe rehearsal and a `--smoke` pass in this tree would not be.
    Afterwards, re-assert that the real state file is still absent and that
    `experiments/results` still does not exist.

    Record in the SUMMARY: the clone source, the resolved HEAD, the install line as read, and
    the dry run's 20-stage tally.
  </action>
  <verify>
    <automated>CLONE=/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod; D="$CLONE/experiments/run_experiment_suite_state.7005a27.dryrun.tsv"; test "$(git -C "$CLONE" rev-parse HEAD)" = 7005a2771aa115e4f4c1284cec7e145739586a4a && test -x /home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python && grep -q 'dev,bench' /home/tlancaster/freeze02-install-command.txt && test "$(awk -F'\t' '$3=="complete" && $5==0' "$D" | wc -l)" -eq 20 && test ! -e "$CLONE/experiments/run_experiment_suite_state.7005a27.tsv" && test ! -e "$CLONE/experiments/results" && test ! -e "$CLONE/experiments/results_e2_band" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod rev-parse HEAD` prints
      `7005a2771aa115e4f4c1284cec7e145739586a4a`.
    - The clone path ends in `-prod`; `/home/tlancaster/aquacal-frozen-rerun-freeze-02` (the
      rehearsal clone) is untouched — its mtime and its `.dryrun.*` file set are unchanged.
    - `test ! -e $CLONE/experiments/run_experiment_suite_state.7005a27.tsv` succeeds both before
      and after the dry run.
    - `test ! -e $CLONE/experiments/results` and `test ! -e $CLONE/experiments/results_e2_band`
      both succeed after the dry run.
    - `/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python` exists and is
      executable, and `conda env list` shows the environment as newly created (it did not exist
      before this task).
    - `/home/tlancaster/freeze02-install-command.txt` holds one line, it contains `[dev,bench]`,
      and it is byte-identical to line 49 of the clone's `experiments/HANDOFF.md`.
    - The dry run exited **0** and
      `awk -F'\t' '$3=="complete" && $5==0' …7005a27.dryrun.tsv | wc -l` equals **20**.
    - No file under the clone was edited: `git -C $CLONE status --porcelain --untracked-files=no`
      prints nothing.
  </acceptance_criteria>
  <done>The frozen 20-stage queue has been walked end to end inside the production clone, in the environment that will run it, and the tree is still in the state the pre-flight requires.</done>
</task>

<task type="auto">
  <name>Task 2: Assert environment provenance BEFORE the burn, and capture it</name>
  <files>/home/tlancaster/freeze02-env.txt (created), /home/tlancaster/freeze02-pip-freeze.txt (created)</files>
  <read_first>
    - `28-RESEARCH.md` § *Pitfall 2: the editable-install hazard — agreeing shas, wrong code* —
      the whole rationale for this task, including the measured table of which environment on
      this machine points where.
    - `28-RESEARCH.md` § *The environment that produced attempt 1's accepted results* — the four
      version values worth comparing against, and why a divergence is recorded rather than
      prevented (numpy and scipy are deliberately unpinned; assumption A2).
    - Inside the clone: `experiments/HANDOFF.md` §1.2 (the hard OpenCV pin and its measured
      justification) and §2.6 *What a green verification does NOT prove* (`:398-399`).
  </read_first>
  <action>
    This is the single control that stands between the run and its most dangerous failure mode:
    an editable install resolving `aquacal` from a *different checkout* while every artifact is
    stamped `7005a27`. The cross-artifact sha gate stays green in that world, because the
    *recorded* shas agree. **No gate catches it. This assertion is the only thing that does**
    (`HANDOFF.md:399`), and it must run before the burn, not after.

    From a shell with `aquacal-freeze02-prod` activated and cwd at the clone root, run the tag's
    own confirmation commands (`HANDOFF.md:222-223`) and tee them into the evidence file, in the
    same shape attempt 1 used so the two records compare line for line:

        python -c "import aquacal, sys; print(aquacal.__file__); print(sys.executable)" | tee /home/tlancaster/freeze02-env.txt
        python -c "import cv2; print('cv2', cv2.__version__)" | tee -a /home/tlancaster/freeze02-env.txt
        uname -a | tee -a /home/tlancaster/freeze02-env.txt

    Then assert, mechanically rather than by eye:

    - `aquacal.__file__` starts with `/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/`.
      If it points anywhere else — the freeze-01 tree, the rehearsal clone, a site-packages copy
      — **halt**. Rebuild the environment; do not "adjust" the run.
    - `sys.executable` is `/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python`.
    - `cv2.__version__` matches `4.13.` — a hard pin, never relaxed: 4.14.0 detects 1.95% fewer
      ChArUco corners and moves reconstruction RMSE by +7.8%.
    - `import pytest` and `import psutil` both succeed.

    Capture the resolved dependency set with `pip freeze | tee /home/tlancaster/freeze02-pip-freeze.txt`,
    then **record — do not gate on** — how `numpy` and `scipy` resolved against attempt 1's
    numpy 2.4.6 / scipy 1.17.1. Neither is pinned, by design; the run manifest and
    `environment_lock.txt` capture whatever actually ran. A divergence is a fact Phase 29's E2
    sanity control will want, so write it into the SUMMARY explicitly rather than letting it pass
    unnoticed.

    Do not hand-write any environment note beyond these files: `run_manifest.json` and
    `environment_lock.txt` are written automatically at pre-flight and must never be hand-edited
    (`HANDOFF.md:149`).
  </action>
  <verify>
    <automated>ENVPY=/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python; P="$($ENVPY -c 'import aquacal;print(aquacal.__file__)')"; case "$P" in /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/*) ;; *) echo "WRONG CHECKOUT: $P"; exit 1;; esac; test "$($ENVPY -c 'import sys;print(sys.executable)')" = "$ENVPY" && $ENVPY -c 'import cv2,sys;sys.exit(0 if cv2.__version__.startswith("4.13.") else 1)' && $ENVPY -c 'import pytest, psutil' && grep -q '^/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/' /home/tlancaster/freeze02-env.txt && test "$(wc -l < /home/tlancaster/freeze02-pip-freeze.txt)" -gt 20 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import aquacal; print(aquacal.__file__)"` prints a path whose prefix is
      `/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/` — asserted by string comparison,
      not read by eye.
    - `sys.executable` equals `/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python`.
    - `cv2.__version__` starts with `4.13.`.
    - `import pytest` and `import psutil` both exit 0.
    - `/home/tlancaster/freeze02-env.txt` exists and carries 4 lines in attempt 1's order:
      module path, interpreter path, `cv2 <version>`, `uname -a`.
    - `/home/tlancaster/freeze02-pip-freeze.txt` exists and lists more than 20 packages.
    - The SUMMARY states the resolved `numpy` and `scipy` versions beside attempt 1's
      numpy 2.4.6 / scipy 1.17.1, and says explicitly whether they diverged.
    - No file inside the clone was modified: `git -C $CLONE status --porcelain --untracked-files=no`
      prints nothing.
  </acceptance_criteria>
  <done>The environment is proven to be the frozen tree's own code, on the pinned OpenCV, with both load-bearing extras present — and that proof is on disk before any hours are spent.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the D4 three-failure state in the new environment — before launch, never during</name>
  <files>/home/tlancaster/freeze02-pytest-prelaunch.txt (created)</files>
  <read_first>
    - `28-CONTEXT.md` § *Expect exactly three test failures — do not treat them as a regression (D4)*
      — the ruling, the three node ids, and the sentence that governs every artifact this phase
      produces.
    - `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md` §1 — the authoritative
      D4 register; the node ids asserted below must match it.
    - `28-RESEARCH.md` § *Assumptions Log* rows A2 and A5 — why this runs now rather than after
      the burn, and why the *count* is a caveat to report rather than a gate to tune.
    - `28-RESEARCH.md` § *Anti-patterns* — the prohibition on running pytest while the queue is
      in flight.
  </read_first>
  <action>
    Run the full test suite once, in the new environment, from the clone root, and record what it
    says. This is confirmation that the freshly resolved dependency set reproduces the known
    state — catching an A2/A5 surprise for ~28 minutes instead of for six hours.

    **Timing discipline:** ~28 minutes on this machine. It must run **before** the launch or
    **after** the run finishes, never while the queue is in flight — it competes for the same
    cores and RAM the 200-frame stages need. Here it runs before.

    **Harness discipline:** do not hold a single foreground call open for 28 minutes. Launch it
    detached, writing to the evidence file, then poll that file with short, separate reads until
    a summary line appears:

        source /home/tlancaster/anaconda3/etc/profile.d/conda.sh
        conda activate aquacal-freeze02-prod
        cd /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod
        nohup pytest tests/ -q > /home/tlancaster/freeze02-pytest-prelaunch.txt 2>&1 &
        disown

    Then, on later calls, `tail -5 /home/tlancaster/freeze02-pytest-prelaunch.txt` until the
    summary line is present. If the harness cannot keep a detached process alive, hand this one
    command to the operator and resume from the file.

    **Reading it (D4).** Three failures is the **pass condition**. Zero failures and four
    failures are both anomalies:

    - **exactly 3** failures, and their node ids are the three registered in
      `29.1-PREPUSH-AUDIT.md` §1: `tests/unit/test_discard_accounting.py::test_matches_frozen_anchor`;
      `tests/unit/test_optim_common.py::TestPerObservationDetailSinks::test_detail_sink_recomputed_geometry_matches_projector`;
      `tests/unit/test_pipeline.py::TestSolverConfigSeedIsInert::test_matches_pre_change_anchor`.
    - **0 failures → halt and report.** The tag was cut with these three; their disappearance
      means the environment is not the one the ruling was made under.
    - **4 or more, or a different node id → halt and report.** That is a new finding and it is
      not this phase's to fix inside a frozen tag.

    Report the passed and skipped counts as measured; do **not** assert a remembered total, since
    numpy and scipy are unpinned and the collected set can legitimately move.

    Note also, for the record, that `tests/unit/test_experiments_provenance.py` carries 8
    pre-existing failures (D1) which reappear only once `experiments/results/` is repopulated by
    the run. They are absent now — the tree is empty — and their later reappearance is expected
    and not caused by this phase.

    Afterwards, re-assert that `experiments/results` still does not exist. If any test created
    it, move it aside into a dated directory under `/home/tlancaster/AquaCal_prod_aside/` —
    never delete, never override the pre-flight.
  </action>
  <verify>
    <automated>F=/home/tlancaster/freeze02-pytest-prelaunch.txt; test "$(grep -c '^FAILED ' "$F")" -eq 3 && grep -q '^FAILED tests/unit/test_discard_accounting.py::test_matches_frozen_anchor' "$F" && grep -q '^FAILED tests/unit/test_optim_common.py::TestPerObservationDetailSinks::test_detail_sink_recomputed_geometry_matches_projector' "$F" && grep -q '^FAILED tests/unit/test_pipeline.py::TestSolverConfigSeedIsInert::test_matches_pre_change_anchor' "$F" && grep -qE '3 failed' "$F" && test ! -e /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/results && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `/home/tlancaster/freeze02-pytest-prelaunch.txt` exists and `grep -c '^FAILED '` over it
      equals **3**.
    - All three registered node ids appear verbatim as `FAILED` lines; no fourth `FAILED` line
      exists.
    - The summary line reports `3 failed`; the passed and skipped counts are recorded in the
      SUMMARY as measured, not asserted against a remembered number.
    - The SUMMARY states plainly that the suite is **not** clean at this sha and that three
      failures is the expected, ruled-on state (D4).
    - `test ! -e $CLONE/experiments/results` still succeeds after the run.
    - `git -C $CLONE status --porcelain --untracked-files=no` still prints nothing.
  </acceptance_criteria>
  <done>The new environment reproduces the tag's ruled-on three-failure state, recorded in a file that will ship with the returned evidence, and the tree is still launch-ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote tag → local clone | The only point at which the code that will run is chosen. Everything downstream inherits it. |
| conda environment → imported library | An editable install can silently redirect `import aquacal` to a different checkout while artifacts record the frozen sha. |
| PyPI → installed dependency set | `pip install -e ".[dev,bench]"` resolves unpinned `numpy`/`scipy` from the network. |
| clone tree → pre-flight refusals | Residue in the tree (a state file, a non-empty output tree) changes what the driver will do without changing what it reports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Spoofing | `import aquacal` resolution in the run environment | high | mitigate | Task 2 asserts `aquacal.__file__` is prefixed by the production clone path by string comparison, and `sys.executable` is the new env's interpreter. A new environment is built for this run; the four already on the machine are excluded by name, three of them for pointing at the wrong checkout (measured). |
| T-28-02 | Tampering | driver state file `run_experiment_suite_state.7005a27.tsv` | high | mitigate | Task 1 asserts the real state file is absent before and after the dry run, in a clone at a path no rehearsal has used (C1/D5). A pre-existing 20-line state file would make a real run skip every stage and produce nothing while looking finished. |
| T-28-03 | Tampering | `experiments/results` / `experiments/results_e2_band` | medium | mitigate | Tasks 1 and 3 assert both are absent. `results_e2_band` present-and-empty converts one `N/A` into two FAILs; a non-empty `results` triggers a pre-flight refusal that must be resolved by moving aside, never by an override flag. |
| T-28-SC | Tampering | `pip install -e ".[dev,bench]"` (PyPI) | high | mitigate | `28-RESEARCH.md` § *Package Legitimacy Audit* was run this session: zero `SLOP`, every row Approved, every `SUS` reason a PyPI metadata artifact. This phase selects no package — it installs exactly what the frozen `pyproject.toml` declares, already installed and executed twice on this machine. The load-bearing control is the version assertion `cv2.__version__` starts with `4.13.` (task 2 verify), plus a full `pip freeze` capture into the returned evidence. Precedent: `29.1-08-PLAN.md` T-29.1-SC. |
| T-28-04 | Repudiation | the D4 three-failure state | medium | mitigate | Task 3 asserts the count is exactly 3 and pins all three node ids against the 29.1 register; both 0 and ≥4 halt. The prohibition in `must_haves` forbids any artifact claiming the suite is clean. |
| T-28-05 | Information disclosure | `freeze02-env.txt`, `freeze02-pip-freeze.txt` (absolute home paths) | low | accept | Ruled intentional and required by 27-10's pre-push audit: `experiments/configs/e2_release_linux.yaml` embeds 26 absolute paths under `/home/tlancaster/…` because *a sanitised path that does not resolve on the target would be strictly worse*. Do not sanitise; do not report as a new finding. |
</threat_model>

<verification>
- The clone exists at the `-prod` path with HEAD `7005a2771aa115e4f4c1284cec7e145739586a4a`, and
  the rehearsal clone at `/home/tlancaster/aquacal-frozen-rerun-freeze-02` is untouched.
- No real state file, no `experiments/results`, no `experiments/results_e2_band` in the clone.
- The dry run exited 0 with 20 `complete` stage lines at exit 0 in the `.dryrun.tsv` file.
- `aquacal.__file__` is inside the production clone; `cv2` is `4.13.x`; `pytest` and `psutil`
  import.
- `pytest tests/ -q` reports exactly 3 failures with the three registered node ids.
- `git -C $CLONE status --porcelain --untracked-files=no` prints nothing — the frozen tree is
  unedited.
</verification>

<success_criteria>
The production venue exists and is proven: the right code, in the right environment, in a tree
the pre-flight will accept, with the whole 20-stage queue already walked once. Nothing expensive
has happened yet, and every precondition for the launch is on disk as evidence.
</success_criteria>

<output>
Create `.planning/phases/28-full-suite-production-run/28-01-SUMMARY.md` when done.
</output>
