---
phase: 28-full-suite-production-run
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - /home/tlancaster/freeze02-prelaunch-assertions.txt
autonomous: true
requirements: [RUN-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A pre-launch assertion record exists at /home/tlancaster/freeze02-prelaunch-assertions.txt, every item carrying an explicit PASS, and its final line says all assertions passed -- written only if they all did."
    - "The production clone is clean by the definition the run manifest actually uses (git status --porcelain --untracked-files=no is empty, per _run_manifest.py:183), and the untracked residue is enumerated and consists only of dry-run state artifacts and interpreter caches."
    - "PRELAUNCH_GATE_PYTHON is set to /home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python and that path is executable (D-28), so interpreters_agree is true by construction rather than by luck."
    - "No environment variable in the driver's SUITE namespace and no dry-run variable is exported in the launch shell, so nothing silently redirects the run."
    - "The E2 frameset clears pre-flight's identity floor and the filesystem holding the clone has at least 20 GiB free, both measured at launch time rather than trusted from research."
  artifacts:
    - /home/tlancaster/freeze02-prelaunch-assertions.txt
  key_links:
    - "gate3_run_manifest_clean_tree is computed from git_dirty, which _run_manifest.py:183 derives from `git status --porcelain --untracked-files=no` -- untracked files are ignored deliberately (D-47 cut the dirty-tree refusal because experiments/results/ is tracked). Asserting bare `git status --porcelain` is empty would fail on the dry run's own residue and send the operator hunting a defect that does not exist, or worse, deleting files."
    - "The abort authority ends at pre-flight. Nothing aborts once stage 1 has begun (D-03/D-50, enforced at run_experiment_suite.sh:2058-2076). That is what makes the launch a door rather than a step, and why the human decision belongs here rather than after."
    - "A leftover SUITE_OUT_DIR or SUITE_E2_RELEASE_CONFIG from a prior session would silently redirect the run while every gate stayed green about the wrong tree."
---

<!-- planner-discipline-allow: ^SUITE_ -->
<!-- planner-discipline-allow: ^RUN_EXPERIMENT_SUITE_DRY_RUN -->

<objective>
Run the pre-launch assertion checklist against the machine as it is now, and record it as
evidence for the authorisation gate that opens `28-03-PLAN.md`.

Purpose: every item on this checklist is a two-minute check standing in for a six-hour loss, and
the pre-flight is the last place anything can abort — nothing aborts once stage 1 has begun. The
record this plan writes is the artifact the decision checkpoint presents and the launch task's
precondition reads.

Output: `/home/tlancaster/freeze02-prelaunch-assertions.txt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-full-suite-production-run/28-CONTEXT.md
@.planning/phases/28-full-suite-production-run/28-RESEARCH.md
@.planning/phases/28-full-suite-production-run/28-VALIDATION.md
@.planning/phases/28-full-suite-production-run/28-01-SUMMARY.md
</context>

## Phase constants (all plans)

    CLONE   = /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod
    ENV     = aquacal-freeze02-prod
    ENVPY   = /home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python
    CONDASH = /home/tlancaster/anaconda3/etc/profile.d/conda.sh
    TAG     = rerun-freeze-02
    SHA     = 7005a2771aa115e4f4c1284cec7e145739586a4a   (short: 7005a27)
    STATE   = $CLONE/experiments/run_experiment_suite_state.7005a27.tsv
    LOG     = /home/tlancaster/suite_run_freeze02.log

The clone path carries the `-prod` suffix everywhere (C1). `HANDOFF.md` §2.1's `cd` line assumes
the plain name and must not be followed literally.

## Hard constraints (all plans)

1. **The clone is frozen** — no edit to `src/`, `experiments/`, configs or anything else.
2. **No pre-flight override flags**, in any form. A refusal is a signal; move the offending tree
   aside and re-assert. Delete nothing.
3. **`SUITE_E2_RELEASE_CONFIG` stays unset (D-12)**; the Linux default resolves.
4. **`SUITE_DISPATCH_LOG` stays unset (B1)** — invocation parity with attempt 1.
5. **`SUITE_WORKERS` (4) and `SUITE_THREAD_CAP` (2) keep their defaults.** Do not re-tune.
6. **A healthy run exits NON-ZERO.** Acceptance is never `$?`.
7. **This phase commits and pushes no results** (A1).
8. **Delete nothing; do not overwrite attempt 1's evidence.**

## Artifacts this phase produces

| Artifact | Kind | Plan |
|---|---|---|
| `/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/` | fresh clone at the tag (off-repo) | 01 |
| `/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/` | new conda environment | 01 |
| `$CLONE/experiments/results/` | run output (62-artifact tree) | 03 |
| `$CLONE/experiments/results_e2_invocations/` | run output | 03 |
| `$CLONE/experiments/results_e2_timing/` | run output | 03 |
| `$CLONE/experiments/results_e2_memory/` | run output | 03 |
| `$CLONE/experiments/results_e2_band/` | run output | 03 |
| `$CLONE/experiments/results_e4_repeat/` | run output | 03 |
| `$CLONE/experiments/run_experiment_suite_state.7005a27.tsv` | driver state — the only per-stage timing record that exists | 03 |
| `$CLONE/experiments/run_experiment_suite_state.7005a27.failures.txt` | driver findings | 03 |
| `$CLONE/experiments/run_experiment_suite_state.7005a27.stagelogs/` | per-stage logs | 03 |
| `/home/tlancaster/suite_run_freeze02.log` | run log, outside the clone and outside the output tree | 03 |
| `/home/tlancaster/suite_run_freeze02.log.preserved` | read-only copy of the run log | 04 |
| `/home/tlancaster/rerun-freeze-02-output.tar.gz` | nine-path archive, read-only | 04 |
| `.planning/phases/28-full-suite-production-run/freeze02-install-command.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-env.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-pip-freeze.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-pytest-prelaunch.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-prelaunch-assertions.txt` | evidence | 02 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-rollup.txt` | evidence | 03 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-stage-timing.txt` | evidence | 03 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-tree-state-at-handoff.txt` | evidence | 04 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-archive-manifest.txt` | archive completeness record (per-tree file counts) | 04 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-gates-full.txt` | evidence | 05 |
| `.planning/phases/28-full-suite-production-run/rerun-freeze-02-output.sha256` | checksum manifest | 04 → 05 |
| `.planning/phases/28-full-suite-production-run/28-RUN-RECORD.md` | returned-artifact index and criterion derivations | 05 |

**Explicitly NOT produced by this phase:** the branch `results/rerun-freeze-02`, any git tag, any
push from the production clone, any Zenodo upload, and any grading verdict over the returned run.
All of those are Phase 29 (A1).

<tasks>

<task type="auto">
  <name>Task 1: Run the pre-launch assertion checklist and write the record</name>
  <files>/home/tlancaster/freeze02-prelaunch-assertions.txt (created)</files>
  <read_first>
    - `28-RESEARCH.md` § *Pattern: pre-launch assertions, in the order that fails cheapest first*
      — the ten-item list this task implements, with the correction noted below for item 2.
    - `28-RESEARCH.md` § *Pre-flight: five refusals, two overrides that must not be used, two
      with no override* — what each refusal will find on this machine and which have no override.
    - `28-RESEARCH.md` § *Pitfall 7* — dirty-at-launch versus dirty-by-writing.
    - `28-RESEARCH.md` § *Environment variables the run sets, and the ones it must not* — the
      full table, including the three that must never be set by hand.
    - Inside the clone: `experiments/_run_manifest.py:174-185` (how `git_dirty` is actually
      derived) and `experiments/HANDOFF.md` §2.2 (useful environment variables) and §2.3
      (pre-flight and its five overrides).
  </read_first>
  <action>
    Execute every assertion below, in this order — cheapest failure first — and append each one
    to `/home/tlancaster/freeze02-prelaunch-assertions.txt` as a `PASS`/`FAIL` line carrying the
    measured value, not just a verdict. Write the final line
    `ALL PRELAUNCH ASSERTIONS PASSED` **only if every item passed**. If any item fails, stop,
    leave the file without that line, and report — do not proceed to the checkpoint.

    1. **Sha.** `git -C $CLONE rev-parse HEAD` equals
       `7005a2771aa115e4f4c1284cec7e145739586a4a`.
    2. **Clean by the definition that matters.** `git -C $CLONE status --porcelain --untracked-files=no`
       prints nothing. This is exactly what `_run_manifest.py:174-185` measures for `git_dirty`,
       which is what `gate3_run_manifest_clean_tree` judges — untracked files are ignored on
       purpose, because `experiments/results/` is tracked and a dirty-tree refusal would fire on
       every resume. Then run `git -C $CLONE status --porcelain` as well and **enumerate the
       untracked residue into the record**; the only entries permitted are the dry-run state
       artifacts (`…7005a27.dryrun.tsv`, `.dryrun.failures.txt`, `.dryrun.stagelogs/`) and
       interpreter caches (`__pycache__`, `.pytest_cache`). Anything else — above all anything
       matching `experiments/results*` — is a halt, resolved by moving aside, never by deleting
       and never by an override flag.
    3. **No real state file.** `$CLONE/experiments/run_experiment_suite_state.7005a27.tsv` does
       not exist. This is the D5 collision guard: a file here carrying 20 complete-at-exit-0
       lines makes a real run skip all 20 stages and produce nothing while looking finished.
    4. **No output trees.** Neither `$CLONE/experiments/results` nor
       `$CLONE/experiments/results_e2_band` exists. Absent is the state the tag ships;
       present-and-empty turns one `N/A` into two FAILs, and a non-empty `results` triggers
       pre-flight refusal 4, which has an override that must not be used.
    5. **Interpreter.** In a shell with `aquacal-freeze02-prod` activated, `which python`
       resolves under `/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/`. This matters
       because ~25 of the driver's stage invocation lines run bare `python -u -m experiments.<mod>`
       — `STAGE_PYTHON` is the literal string `python` (`run_experiment_suite.sh:498`), so the
       launch shell must have the environment activated, not merely the gate variable set.
    6. **Library provenance, re-asserted at launch time.** `aquacal.__file__` is prefixed by
       `$CLONE/`. Cheap, and the only control over the wrong-code-right-sha failure.
    7. **The pin.** `cv2.__version__` starts with `4.13.`.
    8. **The extras.** `import pytest` and `import psutil` both succeed.
    9. **Gate interpreter (D-28).** `PRELAUNCH_GATE_PYTHON` is exported and equals
       `/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python`, and that path is
       executable. Set it explicitly; do not rely on the PATH fallback that merely warned during
       29.1's rehearsal and happened to resolve correctly.
    10. **A clean variable namespace.** `env | grep -c '^SUITE_'` prints `0` and
        `env | grep -c '^RUN_EXPERIMENT_SUITE_DRY_RUN'` prints `0`. A leftover from a prior
        session would silently redirect the run. Note in the record that this is what enforces
        D-12 (the Windows config escape hatch stays unset), B1 (`SUITE_DISPATCH_LOG` stays
        unset), and the default worker and thread caps.
    11. **The frameset.** The 13 extrinsic directories under
        `/home/tlancaster/PycharmProjects/AquaCal/aquacal_data/real-rig/real-rig/extrinsic/`
        exist and total at least `3000000000` bytes. Record the measured byte total.
    12. **Disk.** At least 20 GiB free on the filesystem holding the clone (pre-flight's
        `free_space_floor_gib`). Record the measured figure.
    13. **Attempt 1 is untouched.** `/home/tlancaster/rerun-freeze-01-output.tar.gz` and
        `/home/tlancaster/suite_run_freeze01.log.preserved` both still exist, are still mode
        `r--r--r--`, and their sizes are unchanged at 31845719 and 425119 bytes.

    Head the file with the date, the clone path, the environment name and the sha, so it reads
    as a standalone record when it is copied into the phase directory by plan 05.
  </action>
  <verify>
    <automated>F=/home/tlancaster/freeze02-prelaunch-assertions.txt; CLONE=/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod; grep -q '^ALL PRELAUNCH ASSERTIONS PASSED$' "$F" && test "$(grep -c '^FAIL' "$F")" -eq 0 && test "$(git -C "$CLONE" rev-parse HEAD)" = 7005a2771aa115e4f4c1284cec7e145739586a4a && test -z "$(git -C "$CLONE" status --porcelain --untracked-files=no)" && test ! -e "$CLONE/experiments/run_experiment_suite_state.7005a27.tsv" && test ! -e "$CLONE/experiments/results" && test ! -e "$CLONE/experiments/results_e2_band" && test -x /home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python && test "$(stat -c %s /home/tlancaster/rerun-freeze-01-output.tar.gz)" -eq 31845719 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `/home/tlancaster/freeze02-prelaunch-assertions.txt` exists, contains one line per item
      above with the **measured value** on it, contains zero lines beginning with `FAIL`, and
      ends with the single line `ALL PRELAUNCH ASSERTIONS PASSED`.
    - `git -C $CLONE rev-parse HEAD` prints `7005a2771aa115e4f4c1284cec7e145739586a4a`.
    - `git -C $CLONE status --porcelain --untracked-files=no` prints nothing, and the record
      separately enumerates the untracked residue, every entry of which is a dry-run state
      artifact or an interpreter cache.
    - `test ! -e $CLONE/experiments/run_experiment_suite_state.7005a27.tsv`,
      `test ! -e $CLONE/experiments/results` and
      `test ! -e $CLONE/experiments/results_e2_band` all succeed.
    - The record shows `PRELAUNCH_GATE_PYTHON` =
      `/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python` and that the path is
      executable.
    - `env | grep -c '^SUITE_'` prints `0` and `env | grep -c '^RUN_EXPERIMENT_SUITE_DRY_RUN'`
      prints `0`, both recorded.
    - The measured frameset byte total is recorded and is ≥ `3000000000`; the measured free
      space is recorded and is ≥ 20 GiB.
    - `/home/tlancaster/rerun-freeze-01-output.tar.gz` is 31845719 bytes, mode `r--r--r--`, and
      `/home/tlancaster/suite_run_freeze01.log.preserved` is 425119 bytes, mode `r--r--r--`.
  </acceptance_criteria>
  <done>Every pre-launch assertion has been executed against the machine as it is now, and the record that proves it is on disk outside the clone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → driver | Every choice the run makes — sha, flags, environment — is read once at driver start (`run_experiment_suite.sh:278`); nothing downstream can change it. |
| ambient environment → run configuration | An exported variable from a previous session silently redirects output trees or config resolution while all gates stay green about the wrong thing. |
| assertion record → launch authorisation | The decision gate in `28-03-PLAN.md` is only as good as the record this plan writes; a record that says PASS without having measured is worse than no record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-06 | Spoofing | ambient `SUITE_*` / dry-run environment variables | high | mitigate | Checklist item 10 asserts both namespaces are empty in the launch shell and records the measurement, which is what mechanically enforces D-12, B1 and the default worker/thread caps. |
| T-28-07 | Tampering | `gate3_run_manifest_clean_tree` | medium | mitigate | Checklist item 2 asserts cleanliness by the definition `_run_manifest.py:174-185` actually uses, and separately enumerates the untracked residue so a dry-run artifact is never mistaken for a defect — nor deleted in response to one. |
| T-28-08 | Repudiation | the assertion record itself | medium | mitigate | Each item is written with its **measured value**, not a bare verdict, and the all-passed line is written only when every item passed — so a record that was never measured is distinguishable from one that was. |
</threat_model>

<verification>
- `/home/tlancaster/freeze02-prelaunch-assertions.txt` exists, has zero `FAIL` lines, and ends
  with `ALL PRELAUNCH ASSERTIONS PASSED`.
- The clone is at the frozen sha, clean by the manifest's own definition, and free of a real
  state file and of both output trees.
- `PRELAUNCH_GATE_PYTHON` is set to the production environment's absolute interpreter.
- Both driver variable namespaces are empty in the launch shell.
- Attempt 1's two preserved artifacts are unchanged in size and mode.
</verification>

<success_criteria>
Every pre-launch assertion has been executed against the machine as it is now, each with its
measured value on the record, and the authorisation gate in `28-03-PLAN.md` has something real to
be presented with.
</success_criteria>

<output>
Create `.planning/phases/28-full-suite-production-run/28-02-SUMMARY.md` when done.
</output>
