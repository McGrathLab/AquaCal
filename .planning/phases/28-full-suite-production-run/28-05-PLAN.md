---
phase: 28-full-suite-production-run
plan: 05
type: execute
wave: 5
depends_on: [28-04]
files_modified:
  - /home/tlancaster/freeze02-gates-full.txt
  - .planning/phases/28-full-suite-production-run/freeze02-install-command.txt
  - .planning/phases/28-full-suite-production-run/freeze02-env.txt
  - .planning/phases/28-full-suite-production-run/freeze02-pip-freeze.txt
  - .planning/phases/28-full-suite-production-run/freeze02-pytest-prelaunch.txt
  - .planning/phases/28-full-suite-production-run/freeze02-prelaunch-assertions.txt
  - .planning/phases/28-full-suite-production-run/freeze02-rollup.txt
  - .planning/phases/28-full-suite-production-run/freeze02-stage-timing.txt
  - .planning/phases/28-full-suite-production-run/freeze02-tree-state-at-handoff.txt
  - .planning/phases/28-full-suite-production-run/freeze02-archive-manifest.txt
  - .planning/phases/28-full-suite-production-run/freeze02-gates-full.txt
  - .planning/phases/28-full-suite-production-run/rerun-freeze-02-output.sha256
  - .planning/phases/28-full-suite-production-run/28-RUN-RECORD.md
autonomous: true
requirements: [RUN-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The completeness gate was re-run over the returned tree and its full output captured to freeze02-gates-full.txt, whose TOTAL line reproduces the run log's roll-up totals."
    - "The environment that finished the run is provably the environment captured before it started: pip freeze taken now is byte-identical to the pre-launch capture."
    - "A freeze02-* evidence set exists in .planning/phases/28-full-suite-production-run/, one counterpart per attempt-1 freeze01-* file plus this attempt's additions, and no freeze01-* file was modified."
    - "28-RUN-RECORD.md exists and states, from measurement: the invocation, the three hard signals, the roll-up totals beside their reference, the 20-stage timing table, the environment, the returned-artifact index with checksums, and the derivation of ROADMAP success criterion 3."
    - "28-RUN-RECORD.md states plainly that the test suite is NOT clean at this sha -- three known failures (D4) -- and that D1's eight provenance failures are expected to reappear now that experiments/results/ is repopulated."
    - "No results branch was created, no tag was cut, and nothing was pushed from the production clone: that is Phase 29's work (A1)."
  artifacts:
    - .planning/phases/28-full-suite-production-run/28-RUN-RECORD.md
    - .planning/phases/28-full-suite-production-run/freeze02-gates-full.txt
    - .planning/phases/28-full-suite-production-run/freeze02-rollup.txt
    - .planning/phases/28-full-suite-production-run/freeze02-env.txt
    - .planning/phases/28-full-suite-production-run/freeze02-pip-freeze.txt
    - .planning/phases/28-full-suite-production-run/freeze02-tree-state-at-handoff.txt
    - .planning/phases/28-full-suite-production-run/rerun-freeze-02-output.sha256
  key_links:
    - "Phase 28 CAPTURES the gate output; Phase 29 GRADES it. Running check_rerun_gates.py here is a read-only capture of an artifact attempt 1 also captured into this directory, and its FAIL lines are transcribed, not classified. Classification, the E2 sanity control and the E7 before/after comparison are Phase 29's RUN-03."
    - "Attempt 1's freeze01-* files are the evidence for why Phase 29.1 existed. They stay in place, unmodified; freeze02-* files sit alongside them so the two attempts compare line for line."
    - "28-RUN-RECORD.md is the document Phase 29 opens first. Anything derived rather than directly observed -- above all success criterion 3 -- has to be written out there, or the next agent reconstructs it from scratch or, worse, assumes it."
---

<objective>
Turn the run into a record: capture the post-run gate output, prove the environment did not move
under the run, assemble the `freeze02-*` evidence set into the phase directory beside attempt 1's,
and write `28-RUN-RECORD.md`.

Purpose: "returning the artifacts with provenance intact" is where this phase ends. Everything a
Phase 29 verifier needs — including the derivations that are *not* directly observable — has to
be on paper here, or it will be reconstructed from memory later.

Output: the committed evidence set and the run record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-full-suite-production-run/28-CONTEXT.md
@.planning/phases/28-full-suite-production-run/28-RESEARCH.md
@.planning/phases/28-full-suite-production-run/28-VALIDATION.md
@.planning/phases/28-full-suite-production-run/28-03-PLAN.md
@.planning/phases/28-full-suite-production-run/28-04-SUMMARY.md
</context>

## Phase constants (all plans)

    CLONE   = /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod
    ENV     = aquacal-freeze02-prod
    ENVPY   = /home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python
    TAG     = rerun-freeze-02
    SHA     = 7005a2771aa115e4f4c1284cec7e145739586a4a   (short: 7005a27)
    PHASEDIR = .planning/phases/28-full-suite-production-run/

## Hard constraints (all plans)

1. **The clone is frozen** — no edit, no `git checkout`, no branch, no push.
2. **A healthy run exits NON-ZERO.** The gate script exits 1 if any gate FAILs; read the
   `TOTAL:` line, never `$?`.
3. **This phase captures; Phase 29 grades.** Transcribe FAIL lines; do not classify them, do not
   repair them, do not edit the gate script — that would be a change to the frozen tree.
4. **This phase commits and pushes no results** (A1). No `results/rerun-freeze-02`, no tag, no
   Zenodo upload.
5. **Do not modify any `freeze01-*` file.** They are attempt 1's record and the evidence for why
   Phase 29.1 existed.

## Artifacts this phase produces

| Artifact | Kind | Plan |
|---|---|---|
| `/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/` | fresh clone at the tag (off-repo) | 01 |
| `/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/` | new conda environment | 01 |
| `$CLONE/experiments/results/` | run output (62-artifact tree) | 03 |
| `$CLONE/experiments/results_e2_invocations/` | run output | 03 |
| `$CLONE/experiments/results_e2_timing/` | run output | 03 |
| `$CLONE/experiments/results_e2_memory/` | run output | 03 |
| `$CLONE/experiments/results_e2_band/` | run output | 03 |
| `$CLONE/experiments/results_e4_repeat/` | run output | 03 |
| `$CLONE/experiments/run_experiment_suite_state.7005a27.tsv` | driver state — the only per-stage timing record that exists | 03 |
| `$CLONE/experiments/run_experiment_suite_state.7005a27.failures.txt` | driver findings | 03 |
| `$CLONE/experiments/run_experiment_suite_state.7005a27.stagelogs/` | per-stage logs | 03 |
| `/home/tlancaster/suite_run_freeze02.log` | run log, outside the clone and outside the output tree | 03 |
| `/home/tlancaster/suite_run_freeze02.log.preserved` | read-only copy of the run log | 04 |
| `/home/tlancaster/rerun-freeze-02-output.tar.gz` | nine-path archive, read-only | 04 |
| `.planning/phases/28-full-suite-production-run/freeze02-install-command.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-env.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-pip-freeze.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-pytest-prelaunch.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-prelaunch-assertions.txt` | evidence | 02 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-rollup.txt` | evidence | 03 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-stage-timing.txt` | evidence | 03 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-tree-state-at-handoff.txt` | evidence | 04 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-archive-manifest.txt` | archive completeness record (per-tree file counts) | 04 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-gates-full.txt` | evidence | 05 |
| `.planning/phases/28-full-suite-production-run/rerun-freeze-02-output.sha256` | checksum manifest | 04 → 05 |
| `.planning/phases/28-full-suite-production-run/28-RUN-RECORD.md` | returned-artifact index and criterion derivations | 05 |

**Explicitly NOT produced by this phase:** the branch `results/rerun-freeze-02`, any git tag, any
push from the production clone, any Zenodo upload, and any grading verdict over the returned run.
All of those are Phase 29 (A1).

<tasks>

<task type="auto">
  <name>Task 1: Capture the post-run gate output and prove the environment never moved</name>
  <files>/home/tlancaster/freeze02-gates-full.txt (created)</files>
  <read_first>
    - `28-RESEARCH.md` § *Acceptance: the three hard signals* — the reference totals and the
      instruction to report actuals rather than tune toward them.
    - `28-RESEARCH.md` § *Pitfall 1* — why the gate script's exit code is not the verdict, and
      the expected magnitude of sticky findings.
    - `28-RESEARCH.md` § *Pitfall 5* — the 29.1-09 conditional-artifact change, which is a
      legitimate reason this capture can differ from attempt 1's.
    - `.planning/phases/28-full-suite-production-run/freeze01-gates-full.txt` (tail only) — the
      shape and the totals attempt 1's capture ended on.
    - `28-CONTEXT.md` § *Deferred Ideas* — the sentence that puts grading in Phase 29, which is
      what makes this a capture task rather than a verdict task.
  </read_first>
  <action>
    **1. Re-run the completeness gate over the returned tree and capture it whole.** From the
    clone root, in the production environment:

        python experiments/check_rerun_gates.py experiments/results --profile full 2>&1 | tee /home/tlancaster/freeze02-gates-full.txt
        tail -5 /home/tlancaster/freeze02-gates-full.txt

    `--profile full` asserts row counts, not merely artifact existence — that is the one that
    matters here. The script exits 1 if any gate FAILs; read the `TOTAL:` line, not `$?`.

    **Capture, do not adjudicate.** Transcribe the `TOTAL:` line and every `FAIL` line verbatim
    into the SUMMARY. Do **not** classify them as expected-by-construction or real, do not repair
    anything, and above all do not edit the gate script — grading the returned run is Phase 29's
    RUN-03, and the script lives inside the frozen tree. If the totals differ from the run log's
    own roll-up, say so and quote both; they should agree, and a disagreement is itself a finding.

    Record the totals beside the reference `176 PASS, 7 N/A, 0 FAIL` — measured over the
    **freeze-01 output tree** with the freeze-02 gate script, not over a freeze-02 run. It is a
    reference, not a target. Two legitimate reasons this run can differ: 29.1-09 replaced
    PASS-by-absence with a machine-evaluated predicate for `degenerate_observations.csv` and
    `all_observation_depths.csv` at `results_e2_invocations/e2_classification`, and this tree is
    new.

    **2. Prove the environment did not move under the run.** Re-take `pip freeze` in the same
    environment and diff it against the pre-launch capture:

        pip freeze > /tmp/freeze02-pip-freeze-postrun.txt
        diff /home/tlancaster/freeze02-pip-freeze.txt /tmp/freeze02-pip-freeze-postrun.txt

    The diff must be empty. That is what makes the pre-launch environment capture a valid record
    of what actually ran. Re-assert the import path and the OpenCV pin at the same time —
    `aquacal.__file__` still under the clone, `cv2.__version__` still `4.13.x`.

    **3. Confirm the gate run changed nothing.** Compare `git -C $CLONE status --porcelain`
    against `/home/tlancaster/freeze02-tree-state-at-handoff.txt`; they must be identical. The
    gate script reads; it does not write into the tree.
  </action>
  <verify>
    <automated>CLONE=/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod; ENVPY=/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python; G=/home/tlancaster/freeze02-gates-full.txt; test -s "$G" && grep -qE 'TOTAL: [0-9]+ PASS, [0-9]+ N/A, [0-9]+ FAIL' "$G" && "$ENVPY" -m pip freeze > /tmp/freeze02-pip-freeze-postrun.txt && diff -q /home/tlancaster/freeze02-pip-freeze.txt /tmp/freeze02-pip-freeze-postrun.txt && "$ENVPY" -c 'import aquacal,sys;sys.exit(0 if aquacal.__file__.startswith("/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/") else 1)' && "$ENVPY" -c 'import cv2,sys;sys.exit(0 if cv2.__version__.startswith("4.13.") else 1)' && diff -q <(git -C "$CLONE" status --porcelain) /home/tlancaster/freeze02-tree-state-at-handoff.txt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `/home/tlancaster/freeze02-gates-full.txt` exists, is non-empty, and contains a line
      matching `TOTAL: <n> PASS, <n> N/A, <n> FAIL`.
    - The SUMMARY quotes that `TOTAL:` line verbatim beside the run log's roll-up total from
      plan 03, and states whether they agree.
    - Every `FAIL` line in the capture is transcribed verbatim into the SUMMARY and **not**
      classified — the SUMMARY says explicitly that classification is Phase 29's RUN-03.
    - `pip freeze` taken now is byte-identical to `/home/tlancaster/freeze02-pip-freeze.txt`
      (`diff` reports no differences).
    - `aquacal.__file__` still resolves under the production clone and `cv2.__version__` still
      starts with `4.13.`.
    - `git -C $CLONE status --porcelain` is identical to
      `/home/tlancaster/freeze02-tree-state-at-handoff.txt` — the gate run wrote nothing into the
      tree.
    - No file inside the clone was edited and the gate script is unmodified.
  </acceptance_criteria>
  <done>The post-run gate capture exists as an artifact, and the environment that produced the run is proven to be the one already on record.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble the freeze02 evidence set and write 28-RUN-RECORD.md</name>
  <files>.planning/phases/28-full-suite-production-run/freeze02-*.txt, .planning/phases/28-full-suite-production-run/rerun-freeze-02-output.sha256, .planning/phases/28-full-suite-production-run/28-RUN-RECORD.md</files>
  <read_first>
    - `28-RESEARCH.md` § *The Return* — attempt 1's six-file evidence set with its sizes and
      producers; the `freeze02-*` set is its counterpart and must not reuse the `freeze01-`
      prefix.
    - `28-RESEARCH.md` § *State of the Art* — the seven-row old/new table, which is the source
      for the "differences from attempt 1" section of the record.
    - `28-03-PLAN.md` § *How ROADMAP success criterion 3 is derived (B1)* — copy this derivation
      into the run record so the record stands alone.
    - `28-CONTEXT.md` § *Expect exactly three test failures (D4)* and § *Expect D1's eight
      provenance failures to return* — both must appear in the record as stated caveats.
    - `.planning/REQUIREMENTS.md:186-200` and `:270-274` — the RUN-02 wording and the phase map
      that puts RUN-03/04/05 in Phase 29.
    - `.planning/ROADMAP.md` § *Phase 28* — the three success criteria the record maps evidence
      onto.
  </read_first>
  <action>
    **1. Copy the evidence set into the phase directory**, beside attempt 1's, using the
    `freeze02-` prefix throughout:

        cp /home/tlancaster/freeze02-install-command.txt /home/tlancaster/freeze02-env.txt \
           /home/tlancaster/freeze02-pip-freeze.txt /home/tlancaster/freeze02-pytest-prelaunch.txt \
           /home/tlancaster/freeze02-prelaunch-assertions.txt /home/tlancaster/freeze02-rollup.txt \
           /home/tlancaster/freeze02-stage-timing.txt /home/tlancaster/freeze02-tree-state-at-handoff.txt \
           /home/tlancaster/freeze02-archive-manifest.txt /home/tlancaster/freeze02-gates-full.txt \
           /home/tlancaster/rerun-freeze-02-output.sha256 \
           .planning/phases/28-full-suite-production-run/

    Do not modify, move or rename any `freeze01-*` file. The two sets sit side by side so the
    attempts compare line for line.

    **2. Write `.planning/phases/28-full-suite-production-run/28-RUN-RECORD.md`.** This is the
    document Phase 29 opens first. Everything in it comes from measurement recorded in this
    phase's summaries; nothing is recalled. It carries, in this order:

    - **Header** — attempt 2, run date, tag `rerun-freeze-02`, sha
      `7005a2771aa115e4f4c1284cec7e145739586a4a`, clone path, environment name, and the machine.
    - **The invocation, verbatim**, including the detached form and the log destination outside
      the clone, plus the explicit list of what was deliberately left unset:
      `SUITE_E2_RELEASE_CONFIG` (D-12), `SUITE_DISPATCH_LOG` (B1), `SUITE_SERIAL`,
      `SUITE_OUT_DIR`, `SUITE_STATE_DIR`, `SUITE_STAGE_PYTHON`, `RUN_EXPERIMENT_SUITE_DRY_RUN`,
      and every pre-flight override flag. `PRELAUNCH_GATE_PYTHON` was set explicitly (D-28).
    - **The three hard signals**, each with its measured value, and an explicit sentence that the
      exit code was not used as the verdict because a healthy run exits non-zero.
    - **The roll-up and gate totals**, quoted, beside the `176 PASS, 7 N/A, 0 FAIL` reference,
      with the note that the reference was measured over the freeze-01 tree and that nothing was
      tuned toward it.
    - **The 20-stage timing table** from `freeze02-stage-timing.txt`, with the total wall clock
      beside attempt 1's measured 6 h 00 m 21 s and `e6_band` beside its 3 h 35 m 24 s. State
      that `HANDOFF.md` §1.6's 15-16 h, `e6_band`'s 8.9 h and E2's *48-87 min per seed* are all
      **Windows-derived upper bounds** and must not be used as timeouts or as evidence that a
      faster finish skipped work.
    - **The derivation of ROADMAP success criterion 3**, copied in full from `28-03-PLAN.md`,
      including the honest statement of the residual gap and the record that `SUITE_DISPATCH_LOG`
      was left unset by decision B1 for invocation parity. Also map criteria 1 and 2 to their
      evidence: criterion 1 → the `0 FAIL` roll-up at `--profile full`; criterion 2 →
      `gate3_git_sha_consistency` PASS plus `run_manifest.json`'s `git_sha`.
    - **The environment record** — interpreter path, `aquacal.__file__`, `cv2` version, the
      resolved `numpy`/`scipy` versions beside attempt 1's numpy 2.4.6 / scipy 1.17.1, and the
      install command as read out of the tag.
    - **The test-suite caveat, stated and not softened (D4):** the suite is **not** clean at this
      sha. `pytest tests/ -q` reported exactly three failures, node ids listed. Three is the
      expected, ruled-on state; zero or four would both be anomalies. Add that D1's eight
      `test_experiments_provenance.py` failures are expected to reappear now that
      `experiments/results/` is repopulated, and are not caused by this run.
    - **The returned-artifact index** — every path from the artifacts table above that actually
      exists, with byte sizes, the two sha256 sums, and the per-tree file counts from
      `freeze02-archive-manifest.txt`. Say explicitly that a large share of the bulk is
      gitignored under DATA-01b and lives only in the archive.
    - **Differences from attempt 1**, from `28-RESEARCH.md` § *State of the Art*: the corrected
      install extras (29.1-04), E4's real-rig guard count (29.1-01), the unclaimed E1 band
      sidecar (29.1-02), the conditional-artifact gate change (29.1-09), the emptied output tree
      in the tag (29.1-06), and the Linux-native invocation (D-12/D-28).
    - **The Phase 28 / Phase 29 boundary (A1)**, stated as a closing section: this phase
      preserved, captured and checksummed. It did **not** create or push
      `results/rerun-freeze-02`, did not commit the output tree, did not grade the run, did not
      run the E2 sanity control or the E7 before/after comparison, and did not touch Zenodo.
      RUN-03, RUN-04 and RUN-05 all map to Phase 29 in REQUIREMENTS.md.
    - **Open items handed forward**, including the note already flagged upward that Phase 29's
      success criterion 6 is unsatisfiable as written because its 2026-08-21 date has passed.

    **3. Commit the documents.** Stage only the phase directory's new files and commit them as
    documentation — for example `docs(28): freeze02 run evidence and run record`. Do not create
    a branch in the production clone, do not commit the output tree, and do not push anything
    from the clone.
  </action>
  <verify>
    <automated>D=.planning/phases/28-full-suite-production-run; R="$D/28-RUN-RECORD.md"; for f in freeze02-install-command.txt freeze02-env.txt freeze02-pip-freeze.txt freeze02-pytest-prelaunch.txt freeze02-prelaunch-assertions.txt freeze02-rollup.txt freeze02-stage-timing.txt freeze02-tree-state-at-handoff.txt freeze02-archive-manifest.txt freeze02-gates-full.txt rerun-freeze-02-output.sha256; do test -s "$D/$f" || { echo "MISSING $f"; exit 1; }; done; test -s "$R" && grep -q '7005a2771aa115e4f4c1284cec7e145739586a4a' "$R" && grep -qi 'criterion 3' "$R" && grep -q 'SUITE_DISPATCH_LOG' "$R" && grep -qi 'not clean' "$R" && grep -q 'test_matches_frozen_anchor' "$R" && grep -qi 'Phase 29' "$R" && test "$(git status --porcelain "$D" | wc -l)" -eq 0 && git log -1 --name-only --pretty=format: | grep -q 'freeze02-rollup.txt' && test "$(git -C /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod branch --list 'results/*' | wc -l)" -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All eleven `freeze02-*` / `rerun-freeze-02-output.sha256` files exist and are non-empty in
      `.planning/phases/28-full-suite-production-run/`.
    - Every `freeze01-*` file in that directory is unchanged: `git status --porcelain` reports no
      modification to any of them.
    - `28-RUN-RECORD.md` exists and contains: the sha `7005a2771aa115e4f4c1284cec7e145739586a4a`;
      the verbatim invocation; the three hard signals with measured values; the roll-up and gate
      totals beside the `176 PASS, 7 N/A, 0 FAIL` reference; a 20-row stage timing table with a
      total compared against 6 h 00 m 21 s; the phrase identifying `HANDOFF.md`'s 15-16 h figure
      as Windows-derived; the full criterion-3 derivation naming `SUITE_DISPATCH_LOG` and B1; the
      environment record; the D4 statement that the suite is **not clean** with all three node
      ids; the D1 note; the returned-artifact index with both sha256 sums; and the Phase 29
      boundary section.
    - `28-RUN-RECORD.md` maps each of the three ROADMAP success criteria to the specific returned
      artifact that evidences it.
    - The production clone has no branch matching `results/*`, is still on a detached HEAD, and
      nothing was pushed from it.
    - The phase directory is committed clean: `git status --porcelain .planning/phases/28-full-suite-production-run/`
      prints nothing, and the most recent commit includes the evidence files.
  </acceptance_criteria>
  <done>The returned artifacts and their provenance are on the record in the repository, and Phase 29 can start from a document rather than from a reconstruction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| off-repo evidence → committed record | The moment measurements become the project's durable account of what happened. |
| attempt 2's record → attempt 1's record | Same directory, prefixes one character apart. |
| Phase 28's capture → Phase 29's grading | A capture that reads as a verdict would let a grading step inherit an unexamined conclusion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-20 | Spoofing | the `freeze02-*` evidence set | high | mitigate | Every file is regenerated from this run's own log, tree and environment inside this phase; the post-run `pip freeze` is diffed against the pre-launch capture; the checksum manifest ties the record to the archive. Nothing is copied from attempt 1's set. |
| T-28-21 | Tampering | the `freeze01-*` evidence set | high | mitigate | Distinct prefix, and an acceptance criterion asserting no `freeze01-*` file shows as modified in `git status`. |
| T-28-22 | Repudiation | a derived criterion presented as observed | medium | mitigate | The criterion-3 derivation, including its residual gap and the B1 trade, is written into the run record in full rather than left implicit. |
| T-28-23 | Elevation of privilege | git operations in the production clone | medium | mitigate | No branch matching `results/*`, no tag, no push from the clone — asserted. `publish.yml` triggers on `v*` tags only, and this phase creates no tag at all, so that door stays shut. |
| T-28-24 | Information disclosure | committed logs and configs (absolute home paths) | low | accept | Ruled intentional by 27-10: `e2_release_linux.yaml` embeds 26 absolute paths under `/home/tlancaster/…` because a sanitised path that does not resolve on the target would be strictly worse. Already public via attempt 1's committed evidence. |
</threat_model>

<verification>
- `freeze02-gates-full.txt` exists with a `TOTAL:` line, and its totals are quoted beside the run
  log's roll-up.
- Post-run `pip freeze` is byte-identical to the pre-launch capture; the import path and OpenCV
  pin still hold.
- All eleven evidence files are in the phase directory; no `freeze01-*` file is modified.
- `28-RUN-RECORD.md` carries the invocation, the three hard signals, the totals, the timing table,
  the criterion-3 derivation, the environment record, the D4 and D1 caveats, the artifact index
  with checksums, and the Phase 29 boundary.
- The production clone has no results branch and nothing was pushed from it.
- The phase directory is committed clean.
</verification>

<success_criteria>
The run is returned with provenance intact: a complete, immutable, checksummed artifact set
outside the repository, a matching evidence set inside it beside attempt 1's, and a run record
that states what was measured, what was derived, and what deliberately remains for Phase 29.
</success_criteria>

<output>
Create `.planning/phases/28-full-suite-production-run/28-05-SUMMARY.md` when done.
</output>
