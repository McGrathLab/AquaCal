---
phase: 28-full-suite-production-run
plan: 03
type: execute
wave: 3
depends_on: [28-02]
files_modified:
  - /home/tlancaster/suite_run_freeze02.log
  - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/results/
  - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/results_e2_invocations/
  - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/results_e2_timing/
  - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/results_e2_memory/
  - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/results_e2_band/
  - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/results_e4_repeat/
  - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/run_experiment_suite_state.7005a27.tsv
  - /home/tlancaster/freeze02-rollup.txt
  - /home/tlancaster/freeze02-stage-timing.txt
autonomous: false
requirements: [RUN-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A human saw the pre-launch assertion record and the measured six-hour schedule and explicitly authorised the burn, and that authorisation is recorded, before stage 1 began."
    - "One invocation of experiments/run_experiment_suite.sh ran to completion in the production clone at sha 7005a27, detached, with its log at /home/tlancaster/suite_run_freeze02.log -- outside the clone and outside the output tree."
    - "The state TSV carries 20 stages with a complete line at exit code 0 -- counted, not eyeballed."
    - "The last END-OF-RUN COMPLETENESS ROLL-UP block in the log reports 0 FAIL at --profile full, and that block is captured verbatim to /home/tlancaster/freeze02-rollup.txt."
    - "The failures file carries no stage-failure line; the roughly seventeen sticky per-stage gate findings that survive a healthy run are counted and reported as expected-by-construction rather than as defects."
    - "The verdict was read from the roll-up and the state file, never from the shell exit code -- a healthy run exits non-zero by design."
    - "gate3_run_manifest_clean_tree, gate3_git_sha_consistency and gate3_run_manifest_fields all report PASS, and run_manifest.json records git_sha 7005a2771aa115e4f4c1284cec7e145739586a4a with cpu_count_logical and ram_total_bytes both non-null."
    - "The returned log is unambiguously a production log: the smoke-mode disclaimer banner the driver prints during rehearsals does not appear in it."
    - "Per-stage wall clocks are derived from the state TSV's ISO stamps into /home/tlancaster/freeze02-stage-timing.txt -- the only per-stage timing record that exists anywhere in this project."
  prohibitions:
    - "A partial run -- any stage skipped, resumed past, or completed at a non-zero exit -- must never be presented as a complete end-to-end run; the stage tally and exit column are reported as measured."
  artifacts:
    - /home/tlancaster/suite_run_freeze02.log
    - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/run_experiment_suite_state.7005a27.tsv
    - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/run_experiment_suite_state.7005a27.failures.txt
    - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/run_experiment_suite_state.7005a27.stagelogs/
    - /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/results/run_manifest.json
    - /home/tlancaster/freeze02-rollup.txt
    - /home/tlancaster/freeze02-stage-timing.txt
  key_links:
    - "Per-stage gates run the full battery over whatever the tree holds at that moment, and the four auxiliary trees get the battery too although they hold one stage's output and no run manifest. Every finding is sticky and any finding forces a non-zero exit. Reading $? as the verdict is the single most likely way to misjudge a good run as a failure."
    - "The end-of-run roll-up is the only check that judges what is ABSENT. That absence-blindness is what produced F-001: a run that exited 0 while a band CSV was never produced."
    - "The state TSV's ISO stamps are the only per-stage timing record in the project; nothing else records how long a stage took."
---

<!-- planner-discipline-allow: STAGE FAILED -->
<!-- planner-discipline-allow: NOTHING THIS RUN PRODUCES IS EVIDENCE -->

<objective>
Launch the production run and read its verdict correctly.

Purpose: this is the phase. A human authorises the burn against the assertion record and the
measured schedule; then one invocation, detached, left alone for roughly six hours; then a
readout that takes acceptance from the three hard signals rather than from the exit code — the
mistake this project has already made and documented.

Output: the six output trees, the three driver state artifacts, the run log, and two derived
evidence files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-full-suite-production-run/28-CONTEXT.md
@.planning/phases/28-full-suite-production-run/28-RESEARCH.md
@.planning/phases/28-full-suite-production-run/28-VALIDATION.md
@.planning/phases/28-full-suite-production-run/28-02-SUMMARY.md
</context>

## Phase constants (all plans)

    CLONE   = /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod
    ENV     = aquacal-freeze02-prod
    ENVPY   = /home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python
    CONDASH = /home/tlancaster/anaconda3/etc/profile.d/conda.sh
    TAG     = rerun-freeze-02
    SHA     = 7005a2771aa115e4f4c1284cec7e145739586a4a   (short: 7005a27)
    STATE   = $CLONE/experiments/run_experiment_suite_state.7005a27.tsv
    LOG     = /home/tlancaster/suite_run_freeze02.log

The clone path carries the `-prod` suffix everywhere (C1).

## Hard constraints (all plans)

1. **The clone is frozen** — no edit to `src/`, `experiments/`, configs or anything else, and
   above all not while the run is in flight. A stage that ran at a different commit than the
   rest makes the whole run unreportable.
2. **No pre-flight override flags**, in any form. Move aside, never override; delete nothing.
3. **`SUITE_E2_RELEASE_CONFIG` stays unset (D-12).**
4. **`SUITE_DISPATCH_LOG` stays unset (B1).**
5. **`SUITE_WORKERS` (4) and `SUITE_THREAD_CAP` (2) keep their defaults.**
6. **A healthy run exits NON-ZERO.** Acceptance is never `$?`.
7. **This phase commits and pushes no results** (A1).
8. **Delete nothing; do not overwrite attempt 1's evidence.**

## The schedule this run is authorised against

**Measured, on this machine, from attempt 1's own state TSV: 6 h 00 m 21 s** for all 20 stages
(2026-08-20, 00:14:10Z → 06:14:31Z, pooled, `SUITE_WORKERS` default). The critical path is
`e6_band` at **3 h 35 m 24 s**; the next largest are `e2_band` at 1 h 12 m and `e5_band` at 53 m.

**The 15-16 h figure in `experiments/HANDOFF.md` §1.6 is a Windows-box upper bound**, as are
`e6_band`'s 8.9 h estimate and E2's *"48-87 min each"* per-seed figure (≈24 min/seed here); both
are labelled Windows-derived in `suite_expectations.json → wall_clock_summary`. Plan a ~6-8 h
window. Do not "correct" this schedule back to 15-16 h, and do not set a timeout against it.
Equally, do not read a ~6 h finish as evidence that something was skipped — the state TSV settles
that question, not the clock.

## Reading the verdict: the three hard signals

A run is accepted when **all three** hold, and no other reading substitutes for them:

1. **`0 FAIL` in the last `END-OF-RUN COMPLETENESS ROLL-UP` block** of the run log.
2. **No stage-failure line** in `run_experiment_suite_state.7005a27.failures.txt`. Per-stage
   `GATE FAIL` lines are a different thing and roughly **17** of them are expected on a healthy
   run — attempt 1 carried 17, and so did 29.1-08's clone verification.
3. **All 20 stages carrying a zero exit code** in `run_experiment_suite_state.7005a27.tsv`.

Plus one signal that is real whenever it fires: **`gate3_run_manifest_clean_tree` FAIL** means
the tree was dirty *at launch*.

**Attempt 1's driver exited 1 on a healthy run.** Per-stage gates run the full E1/E4/E6/E7
battery over whatever the tree holds at that moment, so early stages FAIL on artifacts later
stages have not written yet, and the four auxiliary trees get the whole battery too although
they hold one stage's output and no run manifest. Findings are sticky and any finding forces a
non-zero exit.

**Reference totals, not targets.** Over the freeze-01 output tree, the freeze-02 gate script
scores `TOTAL: 176 PASS, 7 N/A, 0 FAIL`. This run's tree is new, and 29.1-09's change to the two
conditional artifacts (`degenerate_observations.csv` and `all_observation_depths.csv`, now
machine-evaluated at `results_e2_invocations/e2_classification`) is a legitimate reason the
totals can differ from attempt 1's. **Report the actual totals; tune nothing to reach 183 result
lines.**

## How ROADMAP success criterion 3 is derived (B1)

Criterion 3 reads: *"The set of returned invocations matches the driver's coverage from Phase 26
one for one."* With `SUITE_DISPATCH_LOG` deliberately unset (B1 — invocation parity with the run
whose evidence was accepted), **no returned artifact enumerates argv directly**. This is written
down here so Phase 29's verifier grades the criterion on the argument that actually exists rather
than hunting for a dispatch record.

What is *not* available: the run log does not echo stage argv — measured over attempt 1's
preserved 425 KB log, `python -u -m` occurs exactly **twice**, both times inside the INTERPRETERS
banner. The per-stage logs open with the driver's own banner (`>>> STAGE 16/20: e1_band starting`)
and then carry the stage's stdout; they do not echo argv either.

The criterion is established by a **two-part argument**, both parts resting on returned artifacts:

1. **Coverage of the stage set.** `run_experiment_suite.sh:541-562` defines a 20-element `STAGES`
   array. `experiments/suite_expectations.json` carries the same 20 ids, and
   `tests/unit/test_suite_stage_list.py` asserts both directions and proves the array is a
   topological order of the manifest's `depends_on` edges. The returned state TSV carries a
   `start` and a `complete` line for each of those 20 names, with exit column `0`. The driver
   dispatches each stage exactly once from that array, so **20 completions at exit 0 means every
   stage in the driver's coverage ran, once.**
2. **Coverage of what those stages emit.** The 62-artifact completeness roll-up at
   `--profile full` reaches `0 FAIL`. Every artifact entry in `suite_expectations.json` is
   attributed to the stage that writes it, and `full` asserts row counts, not merely existence.
   An invocation that was skipped, truncated or silently redirected cannot leave a complete,
   correctly-sized artifact set behind — and the roll-up is the only check that judges what is
   **absent**, which is exactly what F-001 lacked.

Taken together, (1) proves every stage in Phase 26's driver coverage was dispatched and returned
success, and (2) proves each stage's declared outputs landed with the declared shapes. Both parts
read the same manifest that *defines* Phase 26's coverage, so the "one for one" comparison is
against the right set by construction.

**The residual gap, stated honestly.** Neither part observes the argv of a *sub-invocation within*
a stage — E3's `--check` then `--force`, `e2_band`'s config emit plus its three per-seed
calibrations, `e4_repeat`'s per-cell loop and its splice. Those are evidenced indirectly, by the
artifacts they produce with their asserted row counts (`benchmark_grid_repeat.csv` at 6 rows,
`exp1_band.csv` at 256, `interface_ablation_band.csv` at 480) and by the stage's exit code.
`SUITE_DISPATCH_LOG` is the one mechanism that would close this gap directly; it was left unset
by the locked decision B1, on the reasoning that provenance parity with attempt 1 is worth more
than upgrading one criterion from derived to direct. This paragraph is the record of that trade.

## Artifacts this phase produces

| Artifact | Kind | Plan |
|---|---|---|
| `/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/` | fresh clone at the tag (off-repo) | 01 |
| `/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/` | new conda environment | 01 |
| `$CLONE/experiments/results/` | run output (62-artifact tree) | 03 |
| `$CLONE/experiments/results_e2_invocations/` | run output | 03 |
| `$CLONE/experiments/results_e2_timing/` | run output | 03 |
| `$CLONE/experiments/results_e2_memory/` | run output | 03 |
| `$CLONE/experiments/results_e2_band/` | run output | 03 |
| `$CLONE/experiments/results_e4_repeat/` | run output | 03 |
| `$CLONE/experiments/run_experiment_suite_state.7005a27.tsv` | driver state — the only per-stage timing record that exists | 03 |
| `$CLONE/experiments/run_experiment_suite_state.7005a27.failures.txt` | driver findings | 03 |
| `$CLONE/experiments/run_experiment_suite_state.7005a27.stagelogs/` | per-stage logs | 03 |
| `/home/tlancaster/suite_run_freeze02.log` | run log, outside the clone and outside the output tree | 03 |
| `/home/tlancaster/suite_run_freeze02.log.preserved` | read-only copy of the run log | 04 |
| `/home/tlancaster/rerun-freeze-02-output.tar.gz` | nine-path archive, read-only | 04 |
| `.planning/phases/28-full-suite-production-run/freeze02-install-command.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-env.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-pip-freeze.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-pytest-prelaunch.txt` | evidence | 01 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-prelaunch-assertions.txt` | evidence | 02 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-rollup.txt` | evidence | 03 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-stage-timing.txt` | evidence | 03 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-tree-state-at-handoff.txt` | evidence | 04 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-archive-manifest.txt` | archive completeness record (per-tree file counts) | 04 → 05 |
| `.planning/phases/28-full-suite-production-run/freeze02-gates-full.txt` | evidence | 05 |
| `.planning/phases/28-full-suite-production-run/rerun-freeze-02-output.sha256` | checksum manifest | 04 → 05 |
| `.planning/phases/28-full-suite-production-run/28-RUN-RECORD.md` | returned-artifact index and criterion derivations | 05 |

**Explicitly NOT produced by this phase:** the branch `results/rerun-freeze-02`, any git tag, any
push from the production clone, any Zenodo upload, and any grading verdict over the returned run.
All of those are Phase 29 (A1).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Authorise the six-hour burn</name>
  <decision>Launch the full 20-stage production suite now, from the production clone, under the invocation this phase has locked — or defer.</decision>
  <context>
    This is the phase's expensive, effectively one-way step. **The abort authority ends at
    pre-flight**: nothing aborts once stage 1 has begun (D-03/D-50, enforced at
    `run_experiment_suite.sh:2058-2076`). After that point the queue runs to completion
    regardless of findings, by design, because every stage's measurements are still wanted. The
    machine is occupied for the duration.

    Present to the human, from measurement rather than memory:

    - **The assertion record in full** — `/home/tlancaster/freeze02-prelaunch-assertions.txt`,
      every item with its measured value, and the untracked-residue enumeration.
    - **The schedule.** Measured on this machine on 2026-08-20: **6 h 00 m 21 s** for all 20
      stages, critical path `e6_band` at 3 h 35 m. The 15-16 h in `HANDOFF.md` §1.6 is a
      **Windows-derived upper bound** and is not the number to plan against.
    - **The exact invocation** that will be run, as a single block, including that the log lands
      at `/home/tlancaster/suite_run_freeze02.log` — outside the clone and outside the output
      tree, because writing it into `experiments/results/` would trip the driver's own non-empty
      tree refusal.
    - **What is deliberately NOT set:** `SUITE_E2_RELEASE_CONFIG` (D-12), `SUITE_DISPATCH_LOG`
      (B1), `SUITE_SERIAL`, `SUITE_OUT_DIR`, `SUITE_STATE_DIR`, `SUITE_STAGE_PYTHON`,
      `RUN_EXPERIMENT_SUITE_DRY_RUN`, and every pre-flight override flag.
    - **The B1 consequence, stated plainly:** with `SUITE_DISPATCH_LOG` unset, ROADMAP success
      criterion 3 becomes a *derived* argument rather than a direct observation. The derivation
      is written out in this plan's § *How ROADMAP success criterion 3 is derived (B1)* and
      carried into `28-RUN-RECORD.md`, so Phase 29's verifier is not left to reconstruct it.
    - **What acceptance will look like:** a **non-zero** exit and roughly 17 sticky `GATE FAIL`
      findings on a *healthy* run. Acceptance is the three hard signals, never `$?`.
    - **Who runs it.** The launch is an operator action in an interactive shell with the
      environment activated. It must not be backgrounded from an agent subprocess: that harness
      kills background commands at 35-50 minutes, and this project has already lost multiple
      sweeps and multiple hours to exactly that (`run_experiment_suite.sh:229-233`).
  </context>
  <options>
    <option id="launch-now">
      <name>Launch now, as specified</name>
      <pros>Every pre-launch assertion is green and measured minutes ago; the environment and tree are in the exact state the pre-flight wants; a ~6-8 h window returns the artifacts Phase 29 is blocked on.</pros>
      <cons>The machine is committed for ~6-8 h and nothing can abort the queue after stage 1 begins.</cons>
    </option>
    <option id="defer">
      <name>Defer to a better window</name>
      <pros>The clone, environment and assertions all persist; re-running the checklist immediately before a later launch is minutes of work.</pros>
      <cons>Phase 29 stays blocked, and with it the Zenodo results package that is waiting on clean output.</cons>
    </option>
    <option id="change-invocation">
      <name>Change something about the invocation first</name>
      <pros>Available if the human wants a different decision than the locked set — for example setting `SUITE_DISPATCH_LOG` to make criterion 3 directly observable.</pros>
      <cons>Any change breaks invocation parity with attempt 1, which is the stated reason B1 chose the conservative default; and the pre-flight override flags are ruled out entirely by CONTEXT.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The human has seen the full contents of `/home/tlancaster/freeze02-prelaunch-assertions.txt`,
      including its final all-passed line and the untracked-residue enumeration.
    - The human has seen the measured 6 h 00 m 21 s schedule with `e6_band` named as the
      3 h 35 m critical path, and has been told explicitly that the 15-16 h figure is a
      Windows-derived upper bound.
    - The human has seen the exact launch block, the log destination outside the clone, and the
      list of variables and flags deliberately left unset.
    - The human has been told that a healthy run exits non-zero with roughly 17 sticky gate
      findings, and that acceptance is the three hard signals.
    - The selected option is recorded verbatim in the SUMMARY before any launch happens.
  </acceptance_criteria>
  <resume-signal>Select: launch-now, defer, or change-invocation</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator launches the production run and returns when the driver has finished</name>
  <precondition>/home/tlancaster/freeze02-prelaunch-assertions.txt exists and its final line is `ALL PRELAUNCH ASSERTIONS PASSED`, and task 1 of this plan returned `launch-now`. Halt if either is unmet — an unmet precondition here costs six hours, not minutes.</precondition>
  <reversibility rating="one-way">The abort authority ends at pre-flight: nothing aborts once stage 1 has begun (D-03/D-50, `run_experiment_suite.sh:2058-2076`), so the machine is committed for the full ~6 h once the command is entered.</reversibility>
  <read_first>
    - `28-RESEARCH.md` § *The launch, verbatim from the tag* and § *Pattern: the shell must have
      the env activated, not merely the gate variable set*.
    - `28-RESEARCH.md` § *Anti-patterns* — in particular the prohibition on backgrounding this
      run from a subagent, and on running pytest while the queue is in flight.
    - `28-RESEARCH.md` § *Measured Baseline* — the per-stage table this run is monitored against.
    - Inside the clone: `experiments/HANDOFF.md` §2.1 (`:218-247`), and §2.8 (`:423`) before
      concluding anything from the exit status.
  </read_first>
  <action>Enter the launch command in an interactive shell with the run environment activated, leave it alone for roughly six hours, and return when the driver's final line appears.</action>
  <instructions>
    **Everything automatable is already done.** The clone exists at the frozen sha, the
    environment is built and proven, the dry run walked all 20 stages, and every pre-launch
    assertion is green and recorded in `/home/tlancaster/freeze02-prelaunch-assertions.txt`.

    **Why this one step is yours and not the agent's.** The driver's own documentation is
    explicit: this run must never be backgrounded from a subagent —
    *"that harness kills background commands at 35-50 min, and a subagent that backgrounds a long
    run and returns has stalled permanently — this project has already lost multiple sweeps and
    multiple hours to both"* (`run_experiment_suite.sh:229-233`). It also has to run from a shell
    where the environment is **activated**, because roughly 25 of the driver's stage invocation
    lines call bare `python -u -m experiments.<module>`; `PRELAUNCH_GATE_PYTHON` covers only the
    gate interpreter.

    **Paste this, in an interactive terminal on the run machine:**

        source /home/tlancaster/anaconda3/etc/profile.d/conda.sh
        conda activate aquacal-freeze02-prod
        cd /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod

        # Last three volatile facts, immediately before launch:
        git status --porcelain --untracked-files=no          # must print nothing
        ls experiments/run_experiment_suite_state.7005a27.tsv  # must say: No such file
        env | grep -c '^SUITE_'                              # must print 0

        export PRELAUNCH_GATE_PYTHON="/home/tlancaster/anaconda3/envs/aquacal-freeze02-prod/bin/python"

        nohup bash experiments/run_experiment_suite.sh \
          > "/home/tlancaster/suite_run_freeze02.log" 2>&1 &
        disown

    Three properties of that command are load-bearing and none is decoration: `nohup … & disown`
    lets the run survive the session ending; the log lands **outside** the clone and outside the
    output tree, because writing it into `experiments/results/` before launch would trip the
    driver's own non-empty-tree refusal; and unbuffered output is already handled by the driver's
    own `python -u` invocations.

    **Then leave it alone.** Expect roughly **6 hours** — measured 6 h 00 m 21 s for all 20
    stages on this machine on 2026-08-20. The 15-16 h figure in `HANDOFF.md` §1.6 is a
    Windows-box upper bound. The only stage worth a specific watch is `e6_band` (measured
    3 h 35 m, the critical path), and nothing can be done about it if it is slow.

    To check progress without disturbing anything: `tail -20 /home/tlancaster/suite_run_freeze02.log`
    and confirm the stage index is advancing. **Do not run `pytest`, and do not start any other
    heavy job, while the queue is in flight** — it competes for the RAM the 200-frame stages
    need. **Do not edit anything in the clone**, not even a typo: a stage that ran at a different
    commit than the rest makes the whole run unreportable.

    **Do not judge the run by its exit code when it ends.** A healthy run exits non-zero, by
    design, with roughly 17 sticky per-stage gate findings. The next task reads the verdict
    properly.

    If the run dies part-way: report it, do not restart blind. Resume (relaunching the same
    command) is for **infrastructure** failures only — a reboot, a process kill, a full disk. It
    is never the recovery path for a `src/` defect. `e7_focal_standoff` and `e4_repeat` are the
    two invocation lines no rehearsal at any scale exercises, so they are the first place to
    look — though both ran to exit 0 in attempt 1's production run.
  </instructions>
  <verification>
    The state file `/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/run_experiment_suite_state.7005a27.tsv`
    exists and carries 20 `complete` lines; the run log's last line reports that the suite driver
    finished all 20 stages; `experiments/results/run_manifest.json` exists.
  </verification>
  <acceptance_criteria>
    - `/home/tlancaster/suite_run_freeze02.log` exists, is non-empty, and its final line reports
      the driver finishing all 20 stages.
    - `/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/run_experiment_suite_state.7005a27.tsv`
      exists and `awk -F'\t' '$3=="complete"' … | wc -l` equals **20**.
    - `$CLONE/experiments/results/run_manifest.json` and
      `$CLONE/experiments/results/environment_lock.txt` both exist and neither has been
      hand-edited.
    - The log lives outside the clone and outside every output tree.
    - No file under the clone was edited during the run:
      `git -C $CLONE diff --stat HEAD -- src/ experiments/*.py experiments/*.sh` prints nothing.
    - The operator reports the wall clock actually observed, beside the measured 6 h 00 m 21 s
      baseline.
  </acceptance_criteria>
  <resume-signal>Type "run finished" and paste the last line of /home/tlancaster/suite_run_freeze02.log, or describe what went wrong</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Read the verdict from the three hard signals and capture the roll-up</name>
  <files>/home/tlancaster/freeze02-rollup.txt (created), /home/tlancaster/freeze02-stage-timing.txt (created)</files>
  <read_first>
    - `28-RESEARCH.md` § *Acceptance: the three hard signals* — including the verbatim extraction
      commands and the note about the space before the filename in the `awk` calls.
    - `28-RESEARCH.md` § *Pitfall 1: reading `$?` as the verdict* and § *Pitfall 5* (the
      conditional-artifact gate changed under 29.1-09, so this roll-up can legitimately differ
      from attempt 1's).
    - `28-RESEARCH.md` § *Measured Baseline* — the per-stage table to compare the derived timings
      against.
    - `28-VALIDATION.md` § *Acceptance: the three hard signals* — the same contract, stated as
      this phase's validation sign-off.
    - Inside the clone: `experiments/HANDOFF.md` §2.8 (`:423-464`) — the exit-code section and
      the clean-tree gate's dirty-at-launch versus dirty-by-writing distinction.
  </read_first>
  <action>
    Read the verdict. **Never from `$?`** — a healthy run exits non-zero by construction, and
    reading the exit code as pass/fail is the single most likely way to misjudge a good run as a
    failure.

    **1. Capture the roll-up.** From the clone root:

        awk '/END-OF-RUN COMPLETENESS ROLL-UP/,0' /home/tlancaster/suite_run_freeze02.log | tee /home/tlancaster/freeze02-rollup.txt

    Read its last `TOTAL:` line and record the three numbers as measured. `0 FAIL` is signal one.
    Every `NOT FOUND` line, if any, names an artifact this run was expected to produce and did
    not — quote them in full rather than summarising.

    **2. Check the failures file** for the driver's stage-failure marker:

        grep -E "STAGE FAILED" experiments/run_experiment_suite_state.7005a27.failures.txt || echo "NO STAGE FAILED LINES"

    Zero occurrences is signal two. Separately count the per-stage `GATE FAIL` lines and report
    the count: roughly 17 is the expected magnitude on a healthy run, and they are
    expected-by-construction, not defects. Do not "fix" one by editing the gate script — that is
    a change to the frozen tree.

    **3. Check the state file.** Note the space before each filename; omitting it has bitten
    before:

        awk -F'\t' '$3=="complete" && $5!=0 {print "NONZERO:",$1,$5}' experiments/run_experiment_suite_state.7005a27.tsv
        awk -F'\t' '$3=="complete"' experiments/run_experiment_suite_state.7005a27.tsv | wc -l

    Nothing printed by the first and `20` from the second is signal three.

    **4. Confirm the manifest gates and the manifest itself.** From the captured roll-up, record
    the verdict lines for `gate3_run_manifest_clean_tree`, `gate3_git_sha_consistency` and
    `gate3_run_manifest_fields`. All three must be PASS. A clean-tree FAIL is **real** and means
    the tree was dirty *at launch* — it is not the same thing as the tree being dirty now, which
    is expected, because `experiments/results/` is a tracked path and goes dirty as soon as stage
    1 writes. Then read `experiments/results/run_manifest.json` and record `git_sha`,
    `aquacal_version`, `cpu_count_logical` and `ram_total_bytes`; the sha must be the frozen one
    and the last two must be non-null (they are the fields the `bench` extra exists to populate).

    **5. Prove the log is a production log, not a rehearsal.** The driver prints a smoke-mode
    disclaimer banner during `--smoke` runs. Assert that banner does not occur in
    `/home/tlancaster/suite_run_freeze02.log`, and record the assertion — CONTEXT asks
    explicitly that the distinction be unambiguous in the log that gets returned.

    **6. Derive the per-stage wall clocks** from the state TSV's ISO stamps into
    `/home/tlancaster/freeze02-stage-timing.txt`: one line per stage with start, end, duration
    and exit code, plus a total. These stamps are the only per-stage timing record that exists
    anywhere in this project. Compare the total and `e6_band` against the measured baseline
    (6 h 00 m 21 s total; `e6_band` 3 h 35 m 24 s) and note any material divergence — as an
    observation, not a gate.

    **7. Write the verdict into the SUMMARY** as three explicit signal lines plus the measured
    totals. If any signal is red, say so plainly and stop: this phase does not repair, re-run or
    grade — it reports. Grading is Phase 29.
  </action>
  <verify>
    <automated>CLONE=/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod; S="$CLONE/experiments/run_experiment_suite_state.7005a27.tsv"; F="$CLONE/experiments/run_experiment_suite_state.7005a27.failures.txt"; grep -E 'TOTAL: [0-9]+ PASS, [0-9]+ N/A, [0-9]+ FAIL' /home/tlancaster/freeze02-rollup.txt | tail -1 | grep -q ', 0 FAIL' && test "$(grep -c 'STAGE FAILED' "$F")" -eq 0 && test "$(awk -F'\t' '$3=="complete" && $5==0' "$S" | wc -l)" -eq 20 && test -z "$(awk -F'\t' '$3=="complete" && $5!=0 {print $1}' "$S")" && test "$(grep -c 'NOTHING THIS RUN PRODUCES IS EVIDENCE' /home/tlancaster/suite_run_freeze02.log)" -eq 0 && grep -q 'gate3_run_manifest_clean_tree' /home/tlancaster/freeze02-rollup.txt && grep -q '"git_sha": "7005a2771aa115e4f4c1284cec7e145739586a4a"' "$CLONE/experiments/results/run_manifest.json" && test -s /home/tlancaster/freeze02-stage-timing.txt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `/home/tlancaster/freeze02-rollup.txt` exists, begins at the `END-OF-RUN COMPLETENESS
      ROLL-UP` marker, and its last `TOTAL:` line matches `, 0 FAIL`. The three totals are quoted
      verbatim in the SUMMARY.
    - `grep -c 'STAGE FAILED'` over
      `$CLONE/experiments/run_experiment_suite_state.7005a27.failures.txt` equals **0**; the
      per-stage `GATE FAIL` count is reported as a number and labelled expected-by-construction.
    - `awk -F'\t' '$3=="complete" && $5==0' …7005a27.tsv | wc -l` equals **20**, and the
      non-zero-exit filter prints nothing.
    - The roll-up's `gate3_run_manifest_clean_tree`, `gate3_git_sha_consistency` and
      `gate3_run_manifest_fields` lines all read PASS and are quoted in the SUMMARY.
    - `experiments/results/run_manifest.json` records
      `"git_sha": "7005a2771aa115e4f4c1284cec7e145739586a4a"`, and neither `cpu_count_logical`
      nor `ram_total_bytes` is null.
    - `grep -c 'NOTHING THIS RUN PRODUCES IS EVIDENCE' /home/tlancaster/suite_run_freeze02.log`
      equals **0**.
    - `/home/tlancaster/freeze02-stage-timing.txt` carries 20 stage rows with start, end,
      duration and exit code, plus a total line; the total and `e6_band` are compared in the
      SUMMARY against 6 h 00 m 21 s and 3 h 35 m 24 s.
    - The SUMMARY states the verdict as three named signals and records that the exit code was
      **not** used as the verdict.
    - The SUMMARY records the roll-up totals beside the reference `176 PASS, 7 N/A, 0 FAIL`, and
      says explicitly that the reference is a reference and nothing was tuned to reach it.
  </acceptance_criteria>
  <done>The run's verdict is established from the roll-up, the failures file and the state file, and both derived evidence files exist outside the clone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver process → operator's judgement | The exit code and the roll-up disagree by design; the operator's reading is the boundary at which a good run can be declared bad or a bad run good. |
| run log → returned evidence | The log is the only narrative record of what happened; a rehearsal log and a production log are distinguishable only by content. |
| in-flight run → the rest of the machine | A competing heavy job or an edit inside the clone corrupts the run silently. |
| pre-flight → stage 1 | The last point at which anything can abort; after it the queue runs to completion regardless. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-10 | Spoofing | the acceptance verdict | high | mitigate | Acceptance is defined as three named signals read from the roll-up, the failures file and the state TSV. The exit code is explicitly excluded, and the ~17 sticky gate findings are pre-labelled expected-by-construction so they are not mistaken for defects. |
| T-28-11 | Repudiation | a partially-completed run | high | mitigate | The stage tally is counted (`complete` at exit 0 == 20) and the non-zero-exit filter must print nothing. A skipped or failed stage cannot pass both. The `must_haves` prohibition forbids presenting a partial run as complete. |
| T-28-12 | Spoofing | rehearsal output presented as production output | medium | mitigate | The returned log is asserted to be free of the smoke-mode disclaimer banner, and the manifest's `git_sha` is pinned to the frozen sha. |
| T-28-08 | Repudiation | the launch decision | medium | mitigate | The blocking `checkpoint:decision` in task 1 records the human's authorisation, the schedule they were shown and the assertion record they saw, before stage 1 begins. |
| T-28-09 | Denial of service (self-inflicted) | the run machine | high | accept | The burn occupies the machine for ~6-8 h and cannot be aborted after stage 1 (D-03/D-50). Accepted deliberately and gated by task 1; the alternative (`SUITE_SERIAL=1`) is 22-31 h for no benefit. |
| T-28-13 | Denial of service (self-inflicted) | the in-flight queue | medium | mitigate | The launch instructions forbid `pytest` and any other heavy job while the queue runs; the D4 confirmation was taken in plan 01, before launch. |
| T-28-14 | Tampering | the frozen clone during the run | high | mitigate | No edit of any kind inside the clone, asserted after the run by `git diff --stat HEAD` over `src/` and the driver scripts. A stage that ran at a different commit than the rest makes the run unreportable. |
</threat_model>

<verification>
- The launch was authorised at a blocking decision checkpoint and the selection is recorded.
- One invocation ran to completion; the log's final line reports all 20 stages finished.
- Signal 1: the last roll-up `TOTAL:` line reports `0 FAIL`.
- Signal 2: zero stage-failure lines in the failures file; the `GATE FAIL` count is reported.
- Signal 3: 20 stages `complete` at exit 0, none non-zero.
- `gate3_run_manifest_clean_tree`, `gate3_git_sha_consistency`, `gate3_run_manifest_fields` all
  PASS; the manifest records the frozen sha with non-null `cpu_count_logical` and
  `ram_total_bytes`.
- The returned log carries no smoke-mode disclaimer banner.
- Per-stage timings are derived and compared against the measured baseline.
</verification>

<success_criteria>
RUN-02 is satisfied in substance: the full suite — E1 through E7, the band runs, and E2 —
executed once end to end at sha `7005a27`, and the evidence that proves it has been read
correctly and captured, with success criterion 3's derivation written down rather than assumed.
</success_criteria>

<output>
Create `.planning/phases/28-full-suite-production-run/28-03-SUMMARY.md` when done.
</output>
