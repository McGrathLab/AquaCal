---
phase: 19.3-scenario-geometry-and-convergence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aquacal/config/schema.py
  - src/aquacal/calibration/interface_estimation.py
  - src/aquacal/calibration/refinement.py
  - src/aquacal/datasets/pipelines.py
  - src/aquacal/calibration/pipeline.py
  - tests/unit/test_schema.py
  - tests/unit/test_discard_accounting.py
  - tests/synthetic/test_guard_inertness.py
autonomous: true
requirements: [GEOM-04]
must_haves:
  truths:
    - "The number of observations the refractive model could not project at the FINAL solution is recorded in discard_stats and reaches diagnostics.json (D-19.3-11)"
    - "The recording change is provably inert -- calibration outputs are bit-identical with and without the sink, by exact equality (D-19.3-11 reasoning 2, Sequencing Constraint 12)"
    - "DegenerateObservationWarning states that first-order optimality is UNRELIABLE when it fires, not that convergence should be judged on it (D-19.3-12)"
    - "The library RECORDS and never raises on a non-zero count -- gating is the harnesses' job (D-19.3-11)"
  artifacts:
    - path: "src/aquacal/calibration/refinement.py"
      provides: "discard_stats_out parameter on joint_refinement + guard-count bump"
      contains: "discard_stats_out"
    - path: "tests/synthetic/test_guard_inertness.py"
      provides: "exact-equality inertness proof following the 19.2-27 pattern"
  key_links:
    - from: "src/aquacal/calibration/interface_estimation.py"
      to: "discard_stats_out['degenerate_observations_at_solution']"
      via: "_bump after the post-solve compute_residuals call"
      pattern: "degenerate_observations_at_solution"
    - from: "src/aquacal/datasets/pipelines.py::calibrate_synthetic"
      to: "joint_refinement"
      via: "discard_stats_out forwarding"
      pattern: "discard_stats_out=discard_stats_out"
---

<objective>
Demote the pinhole continuation from a silent numerical rescue to a recorded diagnostic: count the
observations the refractive model could not project at the final solution, write that count into
the run's existing `discard_stats` channel, correct the warning text that currently gives exactly
backwards advice, and prove by exact-equality test that none of this moves a single number.

Purpose: harnesses need a machine-checkable signal that a cell's optimality is unreadable
(D-19.3-11). And the inertness proof is the single highest-leverage criterion in the phase --
if this change cannot be shown inert, E2 (48-87 min, real data, §3 manuscript numbers re-verified
at 0.000% delta) re-enters scope (Sequencing Constraint 12).

Output: one new `discard_stats` key, corrected warning text in three places, and an exact-equality
inertness suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-RESEARCH.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-27-SUMMARY.md
@src/aquacal/calibration/interface_estimation.py
@src/aquacal/calibration/refinement.py

<interfaces>
Contracts the executor needs. Do NOT go exploring for these.

From src/aquacal/calibration/_observability.py:
    def _bump(stats: dict[str, int] | None, key: str, n: int = 1) -> None
    def check_discard_invariants(stats: dict[str, int]) -> list[str]
        # keys it inspects are enumerated explicitly; a new key is additive and cannot
        # break either invariant

Already present (do NOT re-invent):
    compute_residuals(..., invalid_count_out: list[int] | None = None)
    optimize_interface(..., discard_stats_out: dict[str, int] | None = None)   # HAS the param
    joint_refinement(...)                                                       # LACKS the param
    calibrate_synthetic(..., discard_stats_out: dict[str, int] | None = None)
        # already forwards to optimize_interface (pipelines.py:159); does NOT forward to
        # joint_refinement (pipelines.py:177)
    pipeline.py:821 `discard_stats: dict[str, int] = {}`
    pipeline.py:1677 `discard_stats=dict(discard_stats)` -> diagnostics.json

The guard blocks already evaluate ONLY the final solution:
    interface_estimation.py:410-424 and refinement.py:307-321 each call
    `compute_residuals(result.x, *cost_args, invalid_count_out=invalid_counts)` exactly once,
    after least_squares returns. There is no running/iteration count to disambiguate --
    D-19.3-11's "at convergence" requirement is already satisfied by the existing call site.
</interfaces>

**Locked:** the library RECORDS, it does not raise (D-19.3-11 rejects raise-at-solve-end and an
opt-in strict flag, with reasons that must not be re-litigated). Do NOT touch
`_extend_invalid_projections`, the pinhole continuation, or the hinge. Do NOT add a blend constant.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct the DegenerateObservationWarning text in all three places</name>
  <read_first>
    - src/aquacal/config/schema.py lines 600-625 (the class and its docstring)
    - src/aquacal/calibration/interface_estimation.py lines 404-426 (literal message)
    - src/aquacal/calibration/refinement.py lines 306-322 (literal message)
    - tests/unit/test_schema.py (check whether a warning-text test already exists before creating one)
  </read_first>
  <files>src/aquacal/config/schema.py, src/aquacal/calibration/interface_estimation.py, src/aquacal/calibration/refinement.py, tests/unit/test_schema.py</files>
  <behavior>
    - `DegenerateObservationWarning.__doc__` contains a statement that first-order optimality is
      UNRELIABLE / not trustworthy when the warning fires.
    - `DegenerateObservationWarning.__doc__` does NOT contain "judge convergence on optimality"
      or "Check first-order optimality, not RMS".
    - Both emitted message strings carry the same corrected substance.
  </behavior>
  <action>
    All three sites currently tell the reader to judge convergence on first-order optimality rather
    than RMS. That advice is wrong in precisely the situation that emits the warning: the pinhole
    continuation puts the residual on a C0-but-not-C1 kink, which is what inflates optimality.
    Replace it with the corrected substance (D-19.3-12): when this warning fires, NEITHER the
    reprojection RMS NOR first-order optimality can be read as a convergence measure -- optimality
    is unreliable because the residual is not differentiable at the refractive/pinhole boundary,
    and the fix is to correct the scenario geometry so no corner sits at or above the interface,
    not to re-tune the solver. Exact wording is yours; the substance is locked.

    Keep the existing numeric interpolation (`n_invalid`, `result.optimality`, `result.status`) in
    both messages. Never quote optimality beyond one significant figure in any NEW prose you add --
    it varies ~2x run-to-run; the existing `:.4g` interpolation of a measured value is not prose
    and stays.

    Add or extend a test in `tests/unit/test_schema.py` asserting both the presence of the
    corrected substance and the ABSENCE of the old backwards phrasing in the class docstring.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_schema.py -k degenerate -q` passes.
    - Source assertion: `grep -v '^#' src/aquacal/config/schema.py | grep -c "judge convergence on optimality"` returns 0.
    - Source assertion: `grep -rn "not RMS, when this fires" src/aquacal/` returns nothing.
    - The word "unreliable" (or an equivalent explicit negation) appears in all three sites.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_schema.py -q</automated>
  </verify>
  <done>All three sites state optimality is unreliable when the warning fires; a test asserts both the new substance and the absence of the old advice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record the final-solution guard count into discard_stats</name>
  <read_first>
    - src/aquacal/calibration/interface_estimation.py lines 135-160 (signature) and 404-426 (guard)
    - src/aquacal/calibration/refinement.py lines 40-70 (signature) and 306-322 (guard)
    - src/aquacal/datasets/pipelines.py lines 28-40, 120-185
    - src/aquacal/calibration/pipeline.py lines 815-870, 1080-1170, 1670-1690
    - src/aquacal/calibration/_observability.py lines 90-160 (`_bump`, `check_discard_invariants`)
    - tests/unit/test_discard_accounting.py
  </read_first>
  <files>src/aquacal/calibration/interface_estimation.py, src/aquacal/calibration/refinement.py, src/aquacal/datasets/pipelines.py, src/aquacal/calibration/pipeline.py, tests/unit/test_discard_accounting.py</files>
  <behavior>
    - After a clean synthetic calibration with `discard_stats_out={}` passed in, the dict contains
      key `degenerate_observations_at_solution` with integer value 0 -- present-and-zero, not absent.
    - With `discard_stats_out=None` nothing is recorded and nothing raises.
    - On a deliberately shallow scenario built to force the guard (construct the poses directly,
      bypassing the generators, since plan 01 now forbids shallow depth_range), the key is > 0.
    - `check_discard_invariants` returns no new violations when the key is present.
  </behavior>
  <action>
    Add key `degenerate_observations_at_solution` to the existing `discard_stats` channel. Use a
    SINGLE flat summed key across all solver stages, matching `discard_stats`' own unsuffixed
    naming convention and the whole-run reasoning already established for that dict -- "did
    degeneracy occur anywhere in this run" is a whole-run question, and any non-zero count
    invalidates the optimality diagnostic for the whole run.

    In `interface_estimation.py`'s guard block, after `n_invalid` is computed, call
    `_bump(discard_stats_out, "degenerate_observations_at_solution", n_invalid)` -- unconditionally,
    so a clean run records an explicit 0 rather than an absent key. The `None` vs present-with-0
    distinction is load-bearing and was established in 19.2-27: `None` means "accounting was never
    requested", present-with-0 means "requested, and zero fired". Do NOT put the bump inside the
    `if n_invalid > 0:` branch.

    `joint_refinement` has no `discard_stats_out` parameter. Add it as a trailing keyword
    defaulting to `None` (this IS the codebase's trailing-kwarg-with-safe-default pattern, and it
    is correct here -- unlike plan 01's `board`, nothing is bypassed by omitting an observational
    sink). Bump the same key from its guard block.

    Forward the sink at every call site that already threads `discard_stats`:
    `datasets/pipelines.py::calibrate_synthetic`'s `joint_refinement` call (it already forwards to
    `optimize_interface`), and `calibration/pipeline.py`'s `joint_refinement` call sites. Do not
    add the sink anywhere it is not already threaded.

    Do not raise. Do not change control flow. Do not touch `compute_residuals`,
    `_extend_invalid_projections`, or `cost_args`.

    Extend `tests/unit/test_discard_accounting.py` with the four behaviors above.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_discard_accounting.py -q` passes.
    - Source assertion: `grep -c "degenerate_observations_at_solution" src/aquacal/calibration/interface_estimation.py src/aquacal/calibration/refinement.py` returns 1 for each file.
    - Source assertion: in both files the `_bump` call is at the guard block's outer indentation
      level, NOT nested under `if n_invalid > 0:`.
    - `joint_refinement`'s signature ends with `discard_stats_out: dict[str, int] | None = None`.
    - Behavior assertion: a synthetic run with `discard_stats_out={}` yields
      `stats["degenerate_observations_at_solution"] == 0` (key present).
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_discard_accounting.py tests/unit/test_observability.py -q</automated>
  </verify>
  <done>The count is recorded once per solver stage into one flat key, reaches diagnostics.json through the existing channel, and is present-and-zero on a clean run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the recording change inert by exact equality</name>
  <read_first>
    - .planning/phases/19.2-experiment-execution-and-provenance/19.2-27-SUMMARY.md (the proof pattern, in full -- reproduce it, do not invent a new one)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-RESEARCH.md (section "Inertness proof pattern")
    - tests/synthetic/ (existing test module conventions and markers)
  </read_first>
  <files>tests/synthetic/test_guard_inertness.py</files>
  <behavior>
    - Run the package's cheapest legal synthetic scenario twice with the same seed: once with
      `discard_stats_out=None`, once with `discard_stats_out={}`.
    - Every returned calibration value is bit-identical between the two runs: extrinsics R and t,
      per-camera water_z, every board pose rvec/tvec, and the final RMS -- asserted with
      `np.testing.assert_array_equal` / `==`, NEVER `allclose` and never a tolerance.
    - The sink was actually populated, not vacuously ignored: the run with `{}` has the key present
      and integer-typed; the run with `None` recorded nothing.
    - The call shape matches the real experiment call sites exactly: same `normal_fixed`,
      `refine_intrinsics`, `shared_interface` values the harnesses use, and the intrinsic pass is
      exercised in at least one parametrisation so `joint_refinement`'s new parameter is covered.
  </behavior>
  <action>
    Create `tests/synthetic/test_guard_inertness.py` implementing the behaviors above, following
    19.2-27's proof pattern exactly. Mark it `@pytest.mark.slow` if runtime warrants -- and record
    in the SUMMARY that it is slow-marked, because `-m "not slow"` would then deselect this
    phase's single most load-bearing test and a green fast run would be worthless evidence.

    Include a test-name substring `inert` so `pytest -k inert` selects it (the validation contract
    names this selector).

    If exact equality FAILS, stop. Do not loosen to `allclose`, do not add a tolerance, do not
    mark it xfail. A failure here means the change is not inert, which per Sequencing Constraint 12
    pulls E2 back into the phase's re-run set -- surface that to the user as a blocking finding
    rather than absorbing it.
  </action>
  <acceptance_criteria>
    - `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/synthetic/test_guard_inertness.py -q` passes UNFILTERED (no `-m` selector).
    - Source assertion: the file contains `assert_array_equal` and contains zero occurrences of
      `allclose`, `approx`, `rtol` or `atol`.
    - Source assertion: the file asserts both the populated case and the `None` case.
    - The SUMMARY states whether the module is slow-marked.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/synthetic/test_guard_inertness.py -q</automated>
  </verify>
  <done>Exact-equality inertness is proven for both solver stages, the sink is shown populated, and E2 stays out of this phase's scope.</done>
</task>

</tasks>

<verification>
- `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_schema.py tests/unit/test_discard_accounting.py tests/unit/test_observability.py tests/unit/test_pipeline.py tests/synthetic/test_guard_inertness.py -q` -> 0 failures, run UNFILTERED.
- `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/ -m "not slow" -q` -> no NEW failures beyond the `create_scenario` / `experiments/` breakage owned by plans 01/04/05.
- Confirm `diagnostics.json` from a synthetic pipeline run carries the new key under `discard_stats`.
- Security: no packages installed; `security_enforcement` N/A per `19.3-RESEARCH.md` Security Domain.
</verification>

<success_criteria>
- One flat `discard_stats` key records the final-solution guard count from both solver stages.
- The library never raises on a non-zero count.
- Exact-equality inertness is proven for both `optimize_interface` and `joint_refinement`.
- All three `DegenerateObservationWarning` sites say optimality is unreliable when it fires.
- No change to `compute_residuals`, `_extend_invalid_projections`, or the hinge.
</success_criteria>

<output>
Create `.planning/phases/19.3-scenario-geometry-and-convergence/19.3-02-SUMMARY.md` when done.
Record: the exact key name, whether the inertness module is slow-marked, and an explicit
statement that exact equality held (this is the sentence Sequencing Constraint 12 depends on).
</output>
