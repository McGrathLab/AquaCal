---
phase: 19.3-scenario-geometry-and-convergence
plan: 07
type: execute
wave: 4
depends_on: ["19.3-02", "19.3-05", "19.3-06"]
files_modified:
  - experiments/e4_benchmark_grid.py
  - experiments/e6_generalization_sweep.py
  - experiments/e1_refractive_comparison.py
  - experiments/e5_index_sensitivity.py
  - experiments/e7_interface_ablation.py
  - tests/unit/test_experiments_e4.py
  - tests/unit/test_experiments_e6.py
  - tests/unit/test_experiments_e1.py
  - tests/unit/test_experiments_e5.py
autonomous: true
requirements: [GEOM-04]
must_haves:
  truths:
    - "A cell whose final solution carries a non-zero guard count can never be published with status='ok' (D-19.3-11)"
    - "Every one of the five experiments records the guard count in its own output, so the re-run gate is machine-checkable"
    - "The library still never raises on a non-zero count -- the gate lives entirely in the harnesses"
    - "Gates are written on the recorded guard count and on optimality, never on reprojection RMS (19.2 anti-pattern #2)"
  artifacts:
    - path: "experiments/e6_generalization_sweep.py"
      provides: "guard-count gate on per-configuration status"
      contains: "degenerate_observations_at_solution"
    - path: "experiments/e4_benchmark_grid.py"
      provides: "guard-count gate on per-cell status"
      contains: "degenerate_observations_at_solution"
  key_links:
    - from: "experiments/*.py"
      to: "discard_stats['degenerate_observations_at_solution']"
      via: "discard_stats_out={} passed into the calibration call"
      pattern: "discard_stats_out"
---

<objective>
Give the five experiment harnesses the gate D-19.3-11 assigns them: read the final-solution guard
count that plan 02 records, and make a non-zero count something that cannot be published as
converged.

Purpose: the library records and never raises; the harnesses gate. Without this half, a degenerate
cell still ships as `status="ok"` — which is exactly what happened to three of fourteen E6
configurations at optimality 27-140.

Output: per-cell gating in E4 and E6, guard-count recording in E1/E5/E7, and E1's smoke geometry
repaired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-RESEARCH.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-02-SUMMARY.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-04-SUMMARY.md

<interfaces>
From plan 19.3-02:
    discard_stats["degenerate_observations_at_solution"]  # int, present-and-zero on a clean run
    calibrate_synthetic(..., discard_stats_out: dict[str, int] | None = None)
    optimize_interface(..., discard_stats_out=...)     # E7 calls this directly
    joint_refinement(..., discard_stats_out=...)       # E7 calls this directly

Calibration call sites to thread the sink into:
    experiments/e1_refractive_comparison.py:225   calibrate_synthetic(...)
    experiments/e4_benchmark_grid.py:698          calibrate_synthetic(...)
    experiments/e5_index_sensitivity.py:457       calibrate_synthetic(...)
    experiments/e6_generalization_sweep.py:715    calibrate_synthetic(...)
    experiments/e7_interface_ablation.py:258,287  optimize_interface(...) / joint_refinement(...)

E6 status machinery:
    experiments/e6_generalization_sweep.py:168  STATUS_VALUES = frozenset({"ok","failed","skipped_existing"})
    experiments/e6_generalization_sweep.py:213  "Columns whose value is null for any row whose status is not 'ok'"
    experiments/e6_generalization_sweep.py:492  use_metrics = status == "ok" and metrics is not None
    experiments/e6_generalization_sweep.py:754  outcome = {"status": "ok", "status_reason": "", "metrics": metrics}
</interfaces>

**Locked:** the gate is written on the recorded guard count and on optimality, never on
reprojection RMS — RMS agrees with a broken solve and is a secondary sanity number only (19.2
anti-pattern #2). Never quote optimality beyond one significant figure in any prose or log line
you add; it varies ~2x run-to-run.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate E6 and E4 per-cell status on the guard count</name>
  <read_first>
    - experiments/e6_generalization_sweep.py lines 160-230 (STATUS_VALUES, E6_COLUMNS, null rules) and 680-780 (run_configuration)
    - experiments/e4_benchmark_grid.py lines 660-760 (run_grid_cell and its status assignment)
    - tests/unit/test_experiments_e6.py, tests/unit/test_experiments_e4.py (status-handling tests)
  </read_first>
  <files>experiments/e6_generalization_sweep.py, experiments/e4_benchmark_grid.py, tests/unit/test_experiments_e6.py, tests/unit/test_experiments_e4.py</files>
  <behavior>
    - A configuration whose run records `degenerate_observations_at_solution > 0` gets
      `status == "degenerate"`, never `"ok"`, with `status_reason` naming the count.
    - Its metrics are still recorded (so the determinism statistic in plan 09 can be computed) but
      no aggregation, LaTeX table or published summary treats it as converged -- every such
      consumer filters on `status == "ok"`.
    - A clean run still gets `status == "ok"` and the CSV column set is unchanged in width and order
      except for any new column you add, which must be appended, never inserted.
    - `"degenerate"` is a member of `STATUS_VALUES`.
  </behavior>
  <action>
    Pass `discard_stats_out={}` into each harness's `calibrate_synthetic` call and read
    `degenerate_observations_at_solution` from it. Add `"degenerate"` to E6's `STATUS_VALUES` and
    set it in `run_configuration` when the count is non-zero, with `status_reason` naming the count.

    E6 currently nulls every metric column whenever `status != "ok"`. Keep degenerate cells' metrics
    populated -- plan 09's cell-reproduction statistic needs them -- by widening the metrics-present
    rule to `status in {"ok", "degenerate"}` (or equivalently keying it on `metrics is not None`)
    while leaving every "is this converged" consumer keyed on `status == "ok"` exactly as it is.
    Two rules that were accidentally the same rule now differ; make that explicit in a comment so
    the next reader does not "simplify" them back together.

    Add a `degenerate_observations_at_solution` column to the E6 and E4 CSV schemas, appended at
    the end so existing column positions are untouched. If either harness's `benchmark.json` schema
    version guards its column set, bump it and record the bump in the SUMMARY; if it does not,
    record that you checked.

    Apply the equivalent gate to E4's `run_grid_cell` (it has its own per-cell status and subprocess
    exit-code mapping -- follow whatever shape that already uses rather than importing E6's).

    Do not make the library raise. Do not gate on reprojection RMS.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_experiments_e6.py tests/unit/test_experiments_e4.py -q` passes UNFILTERED.
    - Behavior assertion: a test injects a non-zero `degenerate_observations_at_solution` and proves
      the resulting row has `status == "degenerate"`, non-empty `status_reason`, and populated metrics.
    - Behavior assertion: a test proves every aggregation/LaTeX path excludes non-`"ok"` rows.
    - Source assertion: `grep -c '"degenerate"' experiments/e6_generalization_sweep.py` is at least 2
      (the STATUS_VALUES membership and the assignment).
    - Source assertion: the new CSV column is appended last in the column list, verified by index.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_e6.py tests/unit/test_experiments_e4.py -q</automated>
  </verify>
  <done>A degenerate cell in E4 or E6 records its count, keeps its metrics, and can never be published as converged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record the guard count in E1, E5 and E7</name>
  <read_first>
    - experiments/e1_refractive_comparison.py lines 210-240 and 460-500
    - experiments/e5_index_sensitivity.py lines 440-480 and its provenance writer
    - experiments/e7_interface_ablation.py lines 240-300 (the direct optimize_interface / joint_refinement calls)
    - experiments/_io.py (the shared provenance/CSV writing helpers)
  </read_first>
  <files>experiments/e1_refractive_comparison.py, experiments/e5_index_sensitivity.py, experiments/e7_interface_ablation.py, tests/unit/test_experiments_e1.py, tests/unit/test_experiments_e5.py</files>
  <behavior>
    - Each of E1, E5 and E7 writes `degenerate_observations_at_solution` into its own per-row CSV
      output (or its provenance sidecar where the harness has no per-row shape), as an integer,
      including when it is 0.
    - When the count is non-zero the harness emits one prominent WARNING naming the count and
      stating that first-order optimality is unreadable for that arm.
    - Neither harness raises on a non-zero count -- the run completes and the number is recorded.
  </behavior>
  <action>
    These three have no per-cell `status` column, so they RECORD rather than gate; plan 08's queue
    script owns the actual pass/fail decision, which keeps the gate machine-checkable without
    inventing three different status vocabularies. State that division explicitly in each harness's
    module docstring so the next reader knows where the gate lives.

    E1 and E5 thread `discard_stats_out={}` into their `calibrate_synthetic` calls. E7 calls
    `optimize_interface` and `joint_refinement` directly (lines ~258 and ~287) and threads the sink
    into both, summing across its four arms per-arm rather than globally.

    Also repair E1's `smoke_depths = [0.30]` (line ~478), which is tied to the old shallow `ideal`
    geometry that plan 04 moved. Use the replacement value plan 04 handed over in its SUMMARY. This
    is smoke-only and publication-irrelevant, but a `--smoke` run that raises makes the CI check
    useless.

    Extend the E1 and E5 unit tests to assert the new field is present and integer-typed on a clean
    run. E7 has no dedicated unit-test module in `tests/unit/`; if that is still true, assert E7's
    recording through whichever existing test module covers it and say so in the SUMMARY rather than
    creating a new module for one assertion.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_experiments_e1.py tests/unit/test_experiments_e5.py -q` passes.
    - Source assertion: `grep -c "discard_stats_out" experiments/e1_refractive_comparison.py experiments/e5_index_sensitivity.py experiments/e7_interface_ablation.py` is at least 1 for each file.
    - Source assertion: `grep -rn "smoke_depths = \[0.30\]" experiments/` returns nothing.
    - `python -m experiments.e1_refractive_comparison --smoke` and
      `python -m experiments.e7_interface_ablation --smoke` both complete with exit code 0.
    - Behavior assertion: a test proves the recorded field is present and integer-typed on a clean run.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_e1.py tests/unit/test_experiments_e5.py -q</automated>
  </verify>
  <done>All five experiments surface the guard count in their own output; E1's smoke geometry runs again; no harness raises on a non-zero count.</done>
</task>

<task type="auto">
  <name>Task 3: Full unfiltered suite at the code-wave boundary</name>
  <read_first>
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-VALIDATION.md (the unfiltered-suite rule)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-02-SUMMARY.md (whether the inertness module is slow-marked)
  </read_first>
  <files>(no source changes -- this task is the gate)</files>
  <action>
    Run the full test suite UNFILTERED. `-m "not slow"` deselects exactly the bit-identity,
    frozen-anchor and inertness suites that are this phase's evidence, so a green fast run is not
    evidence of anything. This is established project precedent, not a preference: in Phase 19.2,
    plan 06's launch gate had to be rewritten for this same reason.

    Record the exact pass/skip/deselect/fail counts against the pre-phase baseline of
    `1168 passed, 6 skipped, 42 deselected, 0 failed` (measured on `-m "not slow"`; the unfiltered
    number will be higher and that is expected -- record both).

    This is the last code wave before the experiment re-runs. Every `src/` change for this phase is
    now landed, and D-26 / Sequencing Constraint 12 requires that state to hold before any run that
    yields a publishable result. Confirm and record: `git status --porcelain` is empty, and
    `git diff --stat HEAD -- src/` is empty.

    If anything fails, fix it here. Do not proceed to plan 08 with a red suite and do not
    marker-filter your way to green.
  </action>
  <acceptance_criteria>
    - `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/ -q` (NO `-m` selector) reports 0 failures.
    - Both the unfiltered and the `-m "not slow"` counts are recorded in the SUMMARY.
    - `git status --porcelain` is empty at task end.
    - The SUMMARY states explicitly that all `src/` changes for this phase have landed (D-26).
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/ -q</automated>
  </verify>
  <done>The full unfiltered suite is green, the tree is clean, and the D-26 precondition for the experiment re-runs is recorded as satisfied.</done>
</task>

</tasks>

<verification>
- `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/ -q` UNFILTERED -> 0 failures.
- All five harnesses run `--smoke` to completion.
- Security: no packages installed; `security_enforcement` N/A per `19.3-RESEARCH.md` Security Domain.
</verification>

<success_criteria>
- E4 and E6 cannot publish a degenerate cell as `status="ok"`.
- E1, E5 and E7 record the guard count in their own output.
- The library still never raises on a non-zero count.
- No gate anywhere in this plan is written on reprojection RMS.
- Full unfiltered suite green; tree clean; D-26 precondition recorded as satisfied.
</success_criteria>

<output>
Create `.planning/phases/19.3-scenario-geometry-and-convergence/19.3-07-SUMMARY.md` when done.
Record: the unfiltered and filtered suite counts, any schema-version bump, and the explicit D-26
statement that every `src/` change has landed and is proven inert.
</output>
