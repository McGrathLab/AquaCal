---
phase: 19.3-scenario-geometry-and-convergence
plan: 08
type: execute
wave: 5
depends_on: ["19.3-03", "19.3-04", "19.3-07"]
files_modified:
  - experiments/rerun_19_3.sh
  - experiments/check_rerun_gates.py
  - tests/unit/test_rerun_gates.py
autonomous: true
requirements: [GEOM-05]
must_haves:
  truths:
    - "The five re-runs are chained in ONE detached serial queue with machine-checkable gates (D-19.3-18)"
    - "The paired determinism sweep's second E6 repeat writes to its OWN output directory and provably re-solves rather than resuming checkpoints (D-19.3-20)"
    - "The gate script fails a cell on a non-zero guard count, never on reprojection RMS"
    - "Nothing in the queue commits, tags, or otherwise mutates git while a run is in flight"
    - "The queue writes a stage-level state file that distinguishes a completed stage from one that started and died, so a 3 a.m. failure is recoverable without reconstructing progress from the log"
  artifacts:
    - path: "experiments/rerun_19_3.sh"
      provides: "the serial detached queue: E1 -> E4 -> E5 -> E6(repeat 1) -> E6(repeat 2) -> E7"
    - path: "experiments/check_rerun_gates.py"
      provides: "machine-checkable post-run gates over the produced artifacts"
  key_links:
    - from: "experiments/rerun_19_3.sh"
      to: "experiments/check_rerun_gates.py"
      via: "post-stage invocation writing a per-stage gate verdict to the log"
      pattern: "check_rerun_gates"
---

<objective>
Build the machinery for the ~9 h overnight re-run before anything launches: a single detached
serial queue, a gate script that reads the produced artifacts, and a verified answer to the one
question that can silently invalidate the determinism measurement — how E6 decides to resume.

Purpose: D-19.3-18 collapses five sessions into one night while preserving review H4 (one
production run at a time on the box). D-19.3-20 warns that E6's resumability, which exists for
interrupted sweeps, will silently turn the determinism measurement into a file-copy check if the
second repeat can see the first's checkpoints.

Output: `rerun_19_3.sh`, `check_rerun_gates.py`, and a recorded, verified statement of E6's
resume mechanism.

**Note on the phase's unattended window:** the automated portion of Phase 19.3 ends at plan 09
Task 2 (queue complete). Plan 09 Task 3 is a blocking human-verify checkpoint and is the expected
stopping point — design this queue so that everything up to that boundary needs no human.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-RESEARCH.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-07-SUMMARY.md
@experiments/README.md

<interfaces>
Shared CLI contract (experiments/_io.py::build_experiment_arg_parser), all five experiments:
    --seed (default 42) --out (default experiments/results/) --force --smoke --check
    --check and --force are mutually exclusive (hard error). --check never writes.

Measured runtimes (experiments/README.md, measured not estimated):
    E1 ~20-25 min | E4 ~3.15-3.5 h (nine cells, per-cell subprocess, CELL_TIMEOUT_SECONDS)
    E5 ~21.1 min  | E6 ~107 min     | E7 ~7 min
    Total for one pass of all five plus ONE extra E6 repeat ~= 9 h (D-19.3-13: +1.8 h, not +3.6 --
    the production E6 re-run IS repeat 1 of the paired sweep).

E6 checkpointing: results write to `--out/e6_configs/{config_key}.json` before the CSV is
assembled, so an interrupted sweep resumes per configuration; the resumed path returns
`status="skipped_existing"`.

From plan 19.3-07: `degenerate_observations_at_solution` is present in every experiment's output,
and E4/E6 map a non-zero value onto `status="degenerate"`.
</interfaces>

**Locked operational constraints:**
- Launch detached with `nohup ... & disown`, NEVER `run_in_background`. The harness kills
  background commands at ~35-50 min; this has already cost this project three sweeps.
- One production run at a time on the box (review H4). Serial, not parallel.
- Commit NOTHING while a run is in flight — per-cell `git_sha` capture would split an artifact's
  recorded provenance across two commits.
- All runs at production scale. A `--smoke` pass may not stand in as proof (anti-pattern #4).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Determine and document E6's actual resume mechanism</name>
  <read_first>
    - experiments/e6_generalization_sweep.py lines 660-710 (the checkpoint read path and the skipped_existing branch)
    - experiments/e6_generalization_sweep.py (the --force handling and where it is consulted)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md (D-19.3-20)
  </read_first>
  <files>(investigation; findings recorded in the SUMMARY)</files>
  <action>
    Read the checkpoint-read path directly and determine whether resume is gated on `--force` or
    purely on checkpoint file presence. Do not assume either; RESEARCH flagged this as an open
    question specifically because the two behave differently for the determinism measurement.

    Prove the answer with a cheap live experiment rather than by reading alone: run a `--smoke` E6
    twice against the same `--out` and confirm whether the second invocation reports
    `skipped_existing`; then run it twice against two different `--out` directories and confirm the
    second genuinely re-solves. Record both observations, the exact commands, and the resulting
    status values.

    Also settle the E6 `--check` identity question with a live invocation. CONTEXT.md's Deferred
    section lists the three `is_baseline` rows sharing one `config_key` as an open defect (24 NaN
    cells); 19.2 plan 27's `_SCENARIO_IDENTITY_KEYS` narrowing plausibly fixed it but was validated
    by unit test only. Run `--check` and record whether it is actually resolved. If it is NOT
    resolved, that becomes a prerequisite fix and belongs in this plan; say so and fix it here
    rather than discovering it at 3 a.m. mid-queue.

    Whatever you find, the second repeat WILL use its own isolated `--out` directory (D-19.3-20 is
    locked, and `--force` on a shared directory is explicitly rejected as the mechanism). The point
    of this task is to know why, and to have a positive check that the second repeat really
    re-solved.
  </action>
  <acceptance_criteria>
    - The SUMMARY states, as a verified claim with the commands that produced it, whether E6's
      resume is keyed on `--force` or on checkpoint presence.
    - The SUMMARY records a live `--check` run's outcome and whether the identity collision is resolved.
    - A positive re-solve signal is identified and named (e.g. per-config wall-clock, or the absence
      of `skipped_existing` in the status column) that plan 09 can assert on repeat 2.
    - No smoke result is used as evidence about geometry or convergence -- only about control flow.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_e6.py -q</automated>
  </verify>
  <done>E6's resume mechanism and --check status are verified facts with recorded commands, and a positive re-solve signal is named for plan 09.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the machine-checkable gate script</name>
  <read_first>
    - experiments/results/ (the current artifact shapes: generalization_sweep.csv, benchmark_grid.csv, index_sensitivity.csv, interface_ablation.csv, e*_provenance.json)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md (D-19.3-11, D-19.3-14, D-19.3-17, and the anti-pattern table)
    - experiments/_io.py (provenance field names)
  </read_first>
  <files>experiments/check_rerun_gates.py, tests/unit/test_rerun_gates.py</files>
  <behavior>
    - Given an output directory, the script reports PASS/FAIL per experiment and exits non-zero if
      any gate fails.
    - Gate 1: every row/cell has `degenerate_observations_at_solution == 0`. A non-zero value fails
      that cell and names it.
    - Gate 2: no row is published with `status == "degenerate"`.
    - Gate 3: every artifact carries complete provenance -- `git_sha`, `seed`, `n_water` and the
      timestamp -- and every `git_sha` in the whole run is IDENTICAL (a split sha means something
      was committed mid-run).
    - Gate 4: first-order optimality is reported per stage and is present, not null.
    - The script contains NO tolerance on any recovered parameter and NO reprojection-RMS threshold.
  </behavior>
  <action>
    Write `experiments/check_rerun_gates.py` implementing the gates above, with Google-style
    docstrings and `NDArray` shapes where arrays appear.

    Three things it must NOT do, each for a recorded reason:
    - It must not gate on reprojection RMS. RMS agrees with a broken solve -- an E4 cell shipped a
      publishable 0.79 px RMS at optimality 6e9 (19.2 anti-pattern #2).
    - It must not compare recovered parameters against a tolerance. Bit-identity is not a valid
      pass/fail gate on marginally-conditioned configurations; it is valid on cost, on small
      problems, and on well-conditioned ones. Movement is expected here and is not a defect.
    - It must not print or compare optimality beyond ONE significant figure. Optimality varies ~2x
      between runs of identical code; it supports "1e-3 healthy vs 1e1-1e9 broken" and nothing finer.

    Gate 3's identical-`git_sha` check is the machine-checkable form of "commit nothing while a run
    is in flight". Make its failure message say exactly that.

    Add `tests/unit/test_rerun_gates.py` with synthetic fixture artifacts exercising each gate's
    pass and fail path.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_rerun_gates.py -q` passes with both a pass-path and a fail-path test per gate.
    - Source assertion: `grep -v '^ *#' experiments/check_rerun_gates.py | grep -ci "rms"` returns 0.
    - Source assertion: the file contains no `rtol`, `atol`, or numeric tolerance constant applied
      to a recovered parameter.
    - Behavior assertion: a fixture with two differing `git_sha` values fails gate 3 with a message
      naming the mid-run-commit cause.
    - `python experiments/check_rerun_gates.py experiments/results/` runs against the CURRENT
      (pre-re-run) artifacts and reports FAIL on the guard-count gate -- proving the gate has teeth
      before it is trusted on fresh output.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_rerun_gates.py -q</automated>
  </verify>
  <done>A gate script exists, is unit-tested on both paths, gates only on the guard count / status / provenance / optimality presence, and demonstrably fails on the pre-fix artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Write the chained detached re-run queue with stage-level recovery state</name>
  <read_first>
    - experiments/README.md (per-experiment entry points and measured runtimes)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-08-... Task 1 findings (this plan's own SUMMARY-in-progress)
    - CLAUDE.md (the `python -u` unbuffered-output requirement)
  </read_first>
  <files>experiments/rerun_19_3.sh</files>
  <action>
    Write `experiments/rerun_19_3.sh` as a single serial queue: E7 (7 min) -> E1 (25 min) ->
    E5 (21 min) -> E6 repeat 1 (107 min) -> E6 repeat 2 (107 min, ISOLATED `--out`) -> E4 (3.5 h).
    Order shortest-first so a systematic failure surfaces in minutes rather than after the 3.5 h
    stage. One stage at a time; never two calibrations concurrently (review H4).

    Every python invocation uses `python -u`. Without it, stdout block-buffers into the log and a
    stalled run is indistinguishable from a healthy one for the entire night.

    The script itself is invoked detached by plan 09 as `nohup bash experiments/rerun_19_3.sh > <log> 2>&1 & disown`
    -- write it so that works: no interactive prompts, no reliance on a tty, absolute or
    repo-root-relative paths only.

    **Stage-level recovery state (required).** The queue is a ~9 h serial script with no
    intermediate state today; if the process dies at 3 a.m., recovery currently means reconstructing
    progress from the log by hand. Give it an append-only state file (e.g.
    `experiments/rerun_19_3_state.tsv`) with one line per stage transition recording: stage name,
    stage index, ISO start time, and — on completion — end time and exit code. Requirements:
    - A stage that STARTED but never wrote a completion line must be distinguishable from one that
      completed. Write the start line before launching the stage and the completion line after it
      returns, so an absent completion line unambiguously means "started and died". A resume MUST
      re-run such a stage from scratch and must never treat it as done.
    - Do NOT reuse E6's per-config `e6_configs/*.json` checkpoints as the stage-level mechanism.
      Per D-19.3-20 the determinism sweep's two repeats use separate output directories, and a
      resume must not reuse a stale one — a resumed E6 stage gets a fresh output directory or its
      previous partial directory is removed before re-running, never silently reused.
    - Support resuming from a given stage index (an argument or env var naming the first stage to
      run), skipping only stages with a recorded successful completion line.

    After each stage, invoke `check_rerun_gates.py` on that stage's output and write a clearly
    delimited PASS/FAIL verdict block into the log. A FAIL does not abort the queue -- the remaining
    measurements are still wanted, and a stage that fails a gate is a finding, not a reason to lose
    the night. Record the verdicts; do not act on them automatically.

    E6 repeat 2 gets its own `--out` (e.g. `experiments/results_e6_repeat2/`). Do NOT use `--force`
    against the shared directory as the isolation mechanism (D-19.3-20). After repeat 2, assert the
    positive re-solve signal Task 1 identified and log it.

    The script must not run `git commit`, `git tag`, `git checkout` or any other tree-mutating git
    command — this holds for the resume path too. It may run `git rev-parse HEAD` once at the very
    start and log it, so the whole night's artifacts are attributable to one sha.

    Add a header comment stating the total expected wall clock, the one-run-at-a-time rule and its
    reason, the commit-freeze rule and its reason, and the resume-from-stage-N usage.
  </action>
  <acceptance_criteria>
    - `bash -n experiments/rerun_19_3.sh` exits 0 (syntax check).
    - Source assertion: `grep -c "python -u" experiments/rerun_19_3.sh` equals the number of experiment invocations.
    - Source assertion: `grep -cE "git (commit|tag|checkout|add|push)" experiments/rerun_19_3.sh` returns 0.
    - Source assertion: `grep -c "\-\-smoke" experiments/rerun_19_3.sh` returns 0 -- every stage is production scale.
    - Source assertion: the two E6 invocations have different `--out` values, and neither uses `--force` on the shared directory.
    - Behavior assertion: kill the script mid-stage in a dry-run/smoke-substituted mode and confirm
      the state file shows a start line with no completion line for that stage, and that a resume
      re-runs it rather than skipping it.
    - Behavior assertion: a resume invocation skips only stages carrying a successful completion
      line, and a resumed E6 stage does not reuse a stale output directory.
    - A dry-run mode (or an explicit `--smoke`-substituting env var used only for this check)
      demonstrates the stage sequencing and the per-stage gate invocation without launching a
      production run.
  </acceptance_criteria>
  <verify>
    <automated>bash -n experiments/rerun_19_3.sh &amp;&amp; ~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_rerun_gates.py -q</automated>
  </verify>
  <done>A single serial detached-safe queue exists, runs every stage unbuffered at production scale, gates each stage, isolates E6's second repeat, writes recoverable stage-level state, and touches nothing in git.</done>
</task>

</tasks>

<verification>
- `bash -n experiments/rerun_19_3.sh` -> exit 0.
- `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/ -q` UNFILTERED -> 0 failures.
- `python experiments/check_rerun_gates.py experiments/results/` -> FAILs on the pre-fix artifacts (the gate has teeth).
- The stage-state file distinguishes completed from started-and-died, demonstrated by an induced kill.
- Security: no packages installed; `security_enforcement` N/A per `19.3-RESEARCH.md` Security Domain.
</verification>

<success_criteria>
- One serial queue covering E7, E1, E5, E6 x2, E4 at production scale, all unbuffered.
- E6 repeat 2 isolated by output directory, with a positive re-solve signal asserted.
- A unit-tested gate script that gates on the guard count, status, provenance and optimality
  presence — never on RMS, never on a recovered-parameter tolerance, never beyond 1 sig fig.
- A stage-level state file and recorded resume-from-stage-N commands that make a 3 a.m. death
  recoverable without hand-reading the log, and that never silently skip incomplete work.
- The queue performs no tree-mutating git operation, including on the resume path.
</success_criteria>

<output>
Create `.planning/phases/19.3-scenario-geometry-and-convergence/19.3-08-SUMMARY.md` when done.
Record: E6's verified resume mechanism, the live `--check` outcome, the named re-solve signal, the
stage order with expected wall clocks, the stage-state file's location and format, the EXACT
resume-from-stage-N commands (detached form, preserving the no-commits rule), and the exact
detached launch command plan 09 must use.
</output>
