---
phase: 19.3-scenario-geometry-and-convergence
plan: 10
type: execute
wave: 7
depends_on: ["19.3-09"]
files_modified:
  - .planning/MANUSCRIPT-FINDINGS.md
  - experiments/results/
  - experiments/archive/
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [GEOM-05, GEOM-06]
must_haves:
  truths:
    - "The cell reproduction count out of 308 is reported against the 63/308 pre-fix baseline, whatever it shows, including no improvement (D-19.3-14)"
    - "MF-08 records the before/after and resolves MF-07, which recorded the question without answering it (D-19.3-15)"
    - "'Accuracy unaffected' appears only for E1, E5 and E7 -- the three with a measured seed band. E4 and E6 report the optimality and degeneracy improvement with NO accuracy claim (D-19.3-17)"
    - "The correction is foregrounded as a finding for the reviewer response, not buried in a methods note (D-19.3-16)"
  artifacts:
    - path: ".planning/MANUSCRIPT-FINDINGS.md"
      provides: "MF-08, cross-referencing MF-07"
      contains: "MF-08"
  key_links:
    - from: ".planning/MANUSCRIPT-FINDINGS.md MF-08"
      to: "experiments/archive/*-2026-08-02-pre-depth-fix/"
      via: "before/after artifact pointers"
      pattern: "pre-depth-fix"
---

<objective>
Report the correction: compute the declared determinism statistic, write MF-08, and produce the
reviewer-response prose that foregrounds a benchmark-construction flaw found, fixed, and shown not
to have affected accuracy.

Purpose: five experiments moved at once. D-19.3-16 chose to present that as evidence of rigour to
an audience the seed describes as knowledgeable about and interested in the optimization
complexities — the alternative reads worse to any reviewer who diffs the versions.

Output: MF-08, the determinism statistic, the reviewer-response prose, and updated trackers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-09-SUMMARY.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-03-SUMMARY.md
@.planning/MANUSCRIPT-FINDINGS.md

<interfaces>
Pre-fix baselines to compare against (already on disk, archived by plan 03):
    E6 paired determinism, PRE-FIX: 63 of 308 cells moved between two repeats.
    E6 PRE-FIX optimality: three of fourteen configurations at 27-140, the rest ~1e-3.
    Correlation going in is weak: corr(degenerate count, movement) = 0.19. Every config with <=2
    degenerate observations reproduced bit-exactly and every one with >=6 moved, but the baseline
    with 12 also reproduced exactly -- so no threshold rule holds.

Measured seed bands that DO exist (and therefore license an accuracy claim):
    E1: 5 seeds (19.2 plan 24)    E7: 10 seeds (D-36)    E5: 5 seeds (N_ASSUMED_BAND sweep)
Experiments with NO seed band: E4, E6.

MF-07 is already the E6 non-convergence finding. Its own text says "This entry records the
question; it does not answer it" and names phase 19.3 as fix owner. MF-08 is that answer.

The manuscript tree is READ-ONLY from this repo. This plan produces prose and tables for the user
to place; it does not edit the manuscript.
</interfaces>

**Locked:** the determinism measure is a REPORTED STATISTIC, not a gate. There is no tolerance
anywhere in it, so there is nothing to loosen. It was declared before launch and is not revisited
after. Never quote optimality beyond one significant figure.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute and report the paired determinism statistic</name>
  <read_first>
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-09-SUMMARY.md (the two E6 sweeps' locations and the re-solve evidence)
    - .planning/phases/19.3-scenario-geometry-and-convergence/determinism_probe.py (already written and re-runnable -- extend it rather than starting fresh)
    - .planning/phases/19.3-scenario-geometry-and-convergence/evidence/README.md (what each preserved log proves, and why one "it reproduced" observation is weak evidence)
  </read_first>
  <files>experiments/results/, .planning/phases/19.3-scenario-geometry-and-convergence/</files>
  <action>
    Compute the post-fix cell reproduction count using the SAME definition that produced the
    63/308 pre-fix baseline -- read that definition out of the existing probe rather than
    re-inventing it, or the two numbers are not comparable and the whole measurement is void.
    Extend `determinism_probe.py`; do not start a new script.

    Report the number in the form "N of 308 cells moved between repeats, before 63 of 308". Report
    it whatever it shows. If it does not improve, that IS the finding, and the criterion is not
    revisited (D-19.3-14). There is no tolerance in this measurement and nothing to loosen.

    Include the supporting cross-tabulation the pre-fix analysis carried: movement against the
    per-config degenerate-observation count. Going in, the correlation was 0.19 and no threshold
    rule held. Report the post-fix version of that table with the same honesty -- if the
    relationship is still weak, say so.

    Do not present bit-identity as a pass/fail judgement on any marginally-conditioned
    configuration. It is valid on cost, on small problems and on well-conditioned ones; here it is
    a descriptive statistic about a population of cells.
  </action>
  <acceptance_criteria>
    - The post-fix count is computed by the same code path that produced 63/308, verified by
      re-running the probe against the archived pre-fix pair and reproducing 63.
    - The reported form names both numbers and the denominator 308.
    - The movement-vs-degenerate-count cross-tabulation is included.
    - No sentence in the report frames the statistic as a pass or fail.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe .planning/phases/19.3-scenario-geometry-and-convergence/determinism_probe.py --report</automated>
  </verify>
  <done>The post-fix reproduction count is computed by the pre-fix code path, reproduces 63/308 on the archived pair, and is reported as a statistic with its supporting cross-tabulation.</done>
</task>

<task type="auto">
  <name>Task 2: Write MF-08 and the reviewer-response prose</name>
  <read_first>
    - .planning/MANUSCRIPT-FINDINGS.md (MF-07 in full -- its structure is the closest precedent and MF-08 answers it; also MF-01/MF-02 for the RESOLVED-status convention)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-09-SUMMARY.md (the moved headline numbers)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md (D-19.3-15, D-19.3-16, D-19.3-17)
  </read_first>
  <files>.planning/MANUSCRIPT-FINDINGS.md</files>
  <action>
    Append MF-08 following the established `## MF-NN` header convention with its `**Status:**`,
    `**Found:**`, `**Source of truth:**` and `**Where the prose is:**` fields, then the data tables,
    then a "What may and may not be said" section.

    MF-08 answers MF-07. Cross-reference it explicitly and mark MF-07's status as resolved-by-MF-08,
    following the MF-01/MF-02 precedent for a resolved entry that still owes a prose edit.

    Per experiment, the table carries: the moved headline numbers, the optimality change (ONE
    significant figure), the degenerate-count change, and the delta expressed against THAT
    experiment's own measured seed band.

    D-19.3-17 is non-negotiable and this project has paid for it twice. "Accuracy unaffected" may
    appear ONLY for E1 (5 seeds), E5 (5 seeds) and E7 (10 seeds). E4 and E6 have no seed band, so
    they report the optimality and degeneracy improvement with NO accuracy claim at all. A
    single-seed before/after cannot support an accuracy claim: 19.2's "fixed code is worse on 5 of
    6 metrics" table was overturned by measuring the noise floor, and Phase 19.1 recorded the same
    lesson independently -- a plausible delta with a plausible mechanism is not evidence anything
    changed.

    Before writing E5's row, confirm from `e5_provenance.json` that its band actually varies `seed`
    rather than only `n_assumed`. RESEARCH flagged this as an inherited-not-verified assumption. If
    it varies only `n_assumed`, E5 moves into the no-accuracy-claim group with E4 and E6, and you
    say so.

    Then write the reviewer-response prose as a separate section: a benchmark-construction flaw was
    found via the optimality instrumentation the reviewers' own questions prompted, corrected, and
    shown not to have affected accuracy where a seed band supports that claim. Foreground it
    (D-19.3-16). State the defect precisely -- synthetic board corners protruded through the water
    surface, putting the residual on a C0-but-not-C1 refractive/pinhole hinge and making
    first-order optimality unreadable as a convergence measure -- and state equally precisely what
    it did NOT do: it did not affect accuracy (the high-optimality group's 0.5938 mm mean
    reconstruction RMSE is indistinguishable from the healthy group's 0.5622 mm, and `half_scale`
    at optimality 27-140 had the BEST accuracy in the table at 0.4935 mm).

    Do not edit the manuscript. The manuscript tree is read-only from this repo; this produces
    prose and tables for the user to place.
  </action>
  <acceptance_criteria>
    - `grep -c "^## MF-08" .planning/MANUSCRIPT-FINDINGS.md` returns 1.
    - MF-08 references MF-07 and MF-07's status is updated to reflect the resolution.
    - Source assertion: no accuracy claim appears in MF-08's E4 or E6 rows -- verified by reading
      those two rows and confirming they carry only optimality and degenerate-count deltas.
    - Every experiment with an accuracy claim names its seed count and its measured band.
    - No optimality figure in MF-08 is quoted beyond one significant figure.
    - No file under the manuscript tree is modified.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -c "import sys,pathlib; t=pathlib.Path('.planning/MANUSCRIPT-FINDINGS.md').read_text(encoding='utf-8'); sys.exit(0 if t.count('## MF-08')==1 and 'MF-07' in t.split('## MF-08')[1] else 1)"</automated>
  </verify>
  <done>MF-08 exists, answers MF-07, carries per-experiment before/after with seed-band-gated accuracy claims, and the reviewer-response prose is drafted for the user to place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review MF-08's honesty, then close out the trackers</name>
  <action>Walk the user through MF-08's five honesty checks, then, after approval, tick GEOM-01..06 in REQUIREMENTS.md and update ROADMAP.md's Phase 19.3 entry, completion-table row, and the v2.0.0 handoff to Phases 21 and 22.</action>
  <what-built>
    MF-08, the determinism statistic, the reviewer-response prose, and the pre-fix archives from
    plan 03 now paired with post-fix artifacts.
  </what-built>
  <how-to-verify>
    This one is manual because it is an editorial judgement no test can make.

    1. Read MF-08. Confirm no accuracy claim appears for E4 or E6. They have no measured seed band,
       so any "accuracy unaffected" statement about them is unsupported no matter how plausible it
       looks (D-19.3-17).
    2. Confirm every accuracy claim that IS made names its seed count and its measured band, and
       that the delta is expressed against that band rather than against zero.
    3. Confirm the determinism statistic is reported as a statistic -- "N of 308, before 63 of 308"
       -- with no pass/fail framing anywhere near it.
    4. Confirm no optimality number is quoted beyond one significant figure.
    5. Compare the archive directories against
       `experiments/archive/e2-2026-07-30-pre-pnp-guard/README.md` and confirm each carries the same
       level of provenance detail.

    After approval, update the trackers: tick GEOM-01..06 in `.planning/REQUIREMENTS.md`, update
    `.planning/ROADMAP.md`'s Phase 19.3 entry and completion-table row, and note the breaking
    change for Phases 21 and 22 -- `generate_board_trajectory` is a public export that gained a
    required parameter, so the milestone cuts **v2.0.0**, not a v1.9.x, and both of those phases
    resolve version strings (D-19.3-06, Sequencing Constraint 13).
  </how-to-verify>
  <acceptance_criteria>
    - All five review points confirmed and recorded.
    - GEOM-01 through GEOM-06 are ticked in `.planning/REQUIREMENTS.md`.
    - `.planning/ROADMAP.md`'s Phase 19.3 completion-table row is updated with the plan count.
    - The v2.0.0 consequence is written into the roadmap where Phases 21 and 22 will read it.
    - Commit is `feat!:` scoped so python-semantic-release cuts the major bump.
  </acceptance_criteria>
  <resume-signal>Type "approved" to close the phase, or name what needs revising in MF-08.</resume-signal>
</task>

</tasks>

<verification>
- `grep -c "^## MF-08" .planning/MANUSCRIPT-FINDINGS.md` -> 1.
- `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/ -q` UNFILTERED -> 0 failures.
- All six GEOM requirements ticked in `.planning/REQUIREMENTS.md`.
- Security: no packages installed; `security_enforcement` N/A per `19.3-RESEARCH.md` Security Domain.
</verification>

<success_criteria>
- The cell reproduction count is reported against 63/308, whatever it shows.
- MF-08 exists, resolves MF-07, and gates every accuracy claim on a measured seed band.
- E4 and E6 carry no accuracy claim.
- Reviewer-response prose foregrounds the correction; the manuscript tree is untouched.
- Trackers updated and the v2.0.0 consequence recorded for Phases 21 and 22.
</success_criteria>

<output>
Create `.planning/phases/19.3-scenario-geometry-and-convergence/19.3-10-SUMMARY.md` when done.
Record: the post-fix reproduction count, which experiments carry an accuracy claim and why, E5's
seed-band verification outcome, and the v2.0.0 handoff to Phases 21 and 22.
</output>
