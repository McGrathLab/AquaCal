---
phase: 19.3-scenario-geometry-and-convergence
verified: 2026-08-04T00:00:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 19.3: Scenario Geometry and Convergence Verification Report

**Phase Goal:** The synthetic scenarios are physically valid — board corners stay below the
water surface at every frame — so first-order optimality is a trustworthy convergence
diagnostic again; the five affected experiments are re-measured and the correction is reported.
**Verified:** 2026-08-04
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria 1–7)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Board poses re-centred on centre; both generators require `BoardConfig`; `ValueError` on a floor violation; floor derived, not hardcoded | ✓ VERIFIED | `src/aquacal/datasets/synthetic.py:360` (`worst_upward_corner_excursion`, deterministic sweep over `BoardGeometry` corner cloud, memoised on board fields), `:447` (`board_clearance_floor`); tests `tests/unit/test_datasets.py:59-227` (`test_board_clearance_floor_reference_15deg/20deg`, `test_derived_floor_moves_with_square_size/rotation_range/squares_y`, `test_board_trajectory_poses_are_centred_on_board_centre`, `test_board_trajectory_no_corner_submerged_over_500_frames`, `test_real_rig_trajectory_no_corner_submerged_over_500_frames`) |
| 2 | `generate_camera_array` default and both `create_scenario` presets move off 0.15 m; `default_board` unchanged | ✓ VERIFIED | Plan 19.3-04 (wave 2) claims and ROADMAP checkbox; corroborated by `tests/unit/test_datasets.py:506` `test_create_scenario_presets_no_corner_submerged` and D-19.3-09's site table in `19.3-CONTEXT.md` |
| 3 | E6 scale axis anchors at derived floor, not water surface; docstring matches | ✓ VERIFIED | `experiments/e6_generalization_sweep.py::_scaled_depth_range` per plan 19.3-06/SUMMARY; determinism probe's post-fix run shows `half_scale` present and legal (no raise) |
| 4 | Pinhole continuation demoted to a recorded numerical guard, gated by harnesses, proven inert by exact-equality test, E2 stays out of scope | ✓ VERIFIED | `tests/synthetic/test_guard_inertness.py` (read in full — follows the 19.2-27 exact-equality pattern, asserts bit-identical R/t/water_z/rvec/tvec/RMS with/without `discard_stats_out`, and that the sink is actually populated); full unfiltered suite (`pytest tests/ -q`, run immediately before this verification per operating constraints) reported 1283 passed / 6 skipped / 0 failed, which includes this file |
| 5 | `DegenerateObservationWarning` no longer advises judging convergence on optimality | ✓ VERIFIED | Plan 19.3-02 must-haves + `tests/unit/test_schema.py` per plan frontmatter; text change independently corroborated by MF-08's own framing, which is written on the corrected advice |
| 6 | E1/E3/E4/E5/E6/E7 re-measured on corrected geometry, one detached serial run; determinism sweep reports cell-reproduction count against 63/308 baseline as a pre-declared statistic | ✓ VERIFIED | `experiments.results/` + `experiments/results_e6_repeat2/` at git sha `22e75ef` per ROADMAP wave 6/7 notes; `git log --oneline` shows `ae91625 feat(19.3-09): re-measure all six experiments on the corrected geometry`; determinism probe re-run live in this verification: self-test reproduces `63 of 308` from the archived pre-fix pair, then reports `8 of 308` post-fix with **no pass/fail framing** in either the probe's own output or MF-08's prose |
| 7 | MF-08 records before/after with pre-fix artifacts archived; "accuracy unaffected" claimed only where a measured seed band supports it (E7 only, stricter than the requirement's own text anticipated) | ✓ VERIFIED | `.planning/MANUSCRIPT-FINDINGS.md` § MF-08 read in full: per-experiment table gives accuracy-claim = NONE for E1, E3, E4, E5, E6 and "supported" only for E7, each with a stated reason; `experiments/archive/{e1,e3,e4,e5,e6,e7}-2026-08-02-pre-depth-fix/` all present with README + provenance sha |

**Score:** 7/7 truths verified

### Specific Contentious Points (from task brief)

**(a) "Five" vs six re-measured experiments.** The ROADMAP goal text and this phase's original
title still say "the five affected experiments," but E3 was added to scope on 2026-08-02 (after
planning) because `e3_derived_quantities.py:331` calls `generate_real_rig_trajectory`. Confirmed
E3 really was re-measured: `experiments/archive/e3-2026-08-02-pre-depth-fix/README.md` documents
134 mismatched cells in the pre-fix `newton_iterations.csv`; `git log` shows commit `ae91625
feat(19.3-09): re-measure all six experiments on the corrected geometry`; the regenerated
`experiments/results/newton_iterations.csv` was spot-checked directly (see (e) below) and matches
MF-08's reported post-fix values. **Six were re-measured, and the goal's "five" is stale wording,
not a missed deliverable** — ROADMAP's own Success Criterion 6 for this phase already lists all
six by name and calls out the E3 addition explicitly. This is a documentation nit, not a gap.

**(b) D-19.3-17 gating (highest-value check).** Read MF-08's per-experiment table row by row:
E1 → NONE, E3 → NONE, E4 → NONE, E5 → NONE, E6 → NONE, E7 → supported. Confirmed no accuracy
claim is made for E1, E3, E4, E5, or E6 anywhere in the entry, including the "What may and may
not be said" framing carried over from MF-07 and the explicit corrections dated 2026-08-03
retracting an earlier PARTIAL claim for E1. **Gate held.**

**(c) GEOM-06 requirement text vs delivery.** REQUIREMENTS.md's GEOM-06 text names E1, E5, E7 as
the experiments expected to carry accuracy claims; only E7 qualified after applying D-19.3-17 to
the actual measured data. Both REQUIREMENTS.md and ROADMAP.md's Success Criterion 7 are ticked
with an explicit note ("Delivered stricter than written... satisfied *a fortiori*: the gate held
and admitted fewer claims than planning assumed, which is the direction that matters"). This is
honest bookkeeping — the requirement's intent (gate accuracy claims on a real seed band) was
satisfied more strictly than its literal text predicted, and the discrepancy is disclosed rather
than silently resolved by ticking. No issue.

**(d) Determinism statistic framing.** Re-ran `determinism_probe.py --report` live in this
session: it re-derives 63/308 from the archived pre-fix pair before reporting anything (printed
"PASS: definition reproduces the pre-fix baseline exactly"), then reports "8 of 308 cells moved
between repeats, before 63 of 308" with no pass/fail language attached to the 8/308 figure itself.
MF-08 and the plan-10 SUMMARY use the same "reported statistic, not a gate" framing. No issue.

**(e) MF-01 re-resolution.** Spot-checked two values directly against the live
`experiments/results/newton_iterations.csv`: `ALL,batch` row shows `iter_max=6`,
`incidence_deg_min/max = 0.0800/57.5038`, `n_points=105600` — all three match MF-08's Group-2
table (`6`, `0.08 - 57.50 deg`, `105,600`) and the plan-10 SUMMARY's MF-01 table exactly. The
pre-fix table is preserved verbatim beneath the current one in MF-01, and the archived file is
named (`experiments/archive/e3-2026-08-02-pre-depth-fix/newton_iterations.csv`), confirmed present
on disk. No issue.

**(f) GEOM-01's core claim, enforcement in `src/`.** Read `board_clearance_floor` and
`worst_upward_corner_excursion` in full (`src/aquacal/datasets/synthetic.py:360-447`). The floor
is genuinely derived — computed from `BoardGeometry(board).corner_positions` and a deterministic
sweep over the rotation-sampling box (41×41×180 grid, not random sampling, matching D-19.3-01's
explicit rejection of both the closed form and random sampling) — not a hardcoded constant, and
it is memoised per-`(board, rotation_range_deg)` rather than restated. Both generators call it at
construction time and raise `ValueError` before emitting any pose (confirmed via
`generate_board_trajectory:590` and `generate_real_rig_trajectory:706`, both calling
`board_clearance_floor(...)` then checking `depth_range` against it). Covered by dedicated tests,
not merely asserted in a SUMMARY: `test_board_clearance_floor_reference_15deg/20deg`,
`test_board_trajectory_no_corner_submerged_over_500_frames`,
`test_real_rig_trajectory_no_corner_submerged_over_500_frames`,
`test_create_scenario_presets_no_corner_submerged`. **No issue.**

**(g) Scope discipline — E4/E6 clearance-floor defect deliberately unfixed.** Confirmed
`experiments/e4_benchmark_grid.py:258` (`GRID_DEPTH_RANGE = (board_clearance_floor(GRID_BOARD_CONFIG, _grid_baseline_water_zs, 15.0), 2.0)`) still evaluates the floor once at import time from a
seed-42 camera array, exactly the defect diagnosed in `.planning/debug/e6-seed-locked-clearance-floor.md` and MF-08's E4/E6 subsection. No `depth_range=None`-style dynamic-recompute fix, and no
other change to this call site's freezing behavior, is present. **Absence confirmed correct — no
partial fix leaked into 19.3's commits.**

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/aquacal/datasets/synthetic.py::board_clearance_floor` | derived clearance floor | ✓ VERIFIED | Present, substantive, called from both generators (see (f)) |
| `src/aquacal/datasets/synthetic.py::worst_upward_corner_excursion` | deterministic rotation-sweep excursion | ✓ VERIFIED | Present, substantive, memoised, matches D-19.3-01's method requirement |
| `tests/unit/test_datasets.py` | centre/clearance/derived-floor/board-required/submerged-corner tests | ✓ VERIFIED | 17 matching test functions found, read several in full |
| `tests/synthetic/test_guard_inertness.py` | exact-equality inertness proof | ✓ VERIFIED | Full file read; follows 19.2-27 pattern exactly, asserts bit-identity, checked sink population |
| `experiments/archive/{e1,e3,e4,e5,e6,e7}-2026-08-02-pre-depth-fix/` | pre-fix artifact archive | ✓ VERIFIED | All six directories present plus `README.md` index; E3's README read in full, has provenance sha and rationale |
| `.planning/MANUSCRIPT-FINDINGS.md` MF-08 | before/after report, six experiments | ✓ VERIFIED | Read in full; matches D-19.3-14/17 requirements exactly |
| `experiments/results/newton_iterations.csv` | regenerated tier-2 CSV | ✓ VERIFIED | Spot-checked against MF-08 and plan-10 SUMMARY values, exact match |
| `experiments/e4_benchmark_grid.py:258` | frozen-at-import `GRID_DEPTH_RANGE` (fix deliberately NOT applied) | ✓ VERIFIED (absence confirmed correct) | Still evaluates once at import from seed-42 array |
| `determinism_probe.py` | paired determinism statistic, self-testing | ✓ VERIFIED | Re-run live in this session; self-test passes, reports 8/308 vs 63/308 baseline with no gate framing |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| GEOM-01 | 19.3-01, 19.3-05 | Re-centred poses + derived clearance floor + required `board` param | ✓ SATISFIED | See (f); `board_clearance_floor` threaded into `experiments/e4_benchmark_grid.py::build_grid_scenario` |
| GEOM-02 | 19.3-04 | Real-rig standoff finished into library defaults/presets | ✓ SATISFIED | ROADMAP checkbox + `default_board` unchanged claim corroborated by D-19.3-09's own before/after site table |
| GEOM-03 | 19.3-06 | E6 scale axis anchors at derived floor | ✓ SATISFIED | Determinism probe's post-fix run shows all 14 configurations (including `half_scale`) legal with zero guard hits |
| GEOM-04 | 19.3-02, 19.3-07 | Guard demoted to recorded numerical guard, gated by harnesses, proven inert | ✓ SATISFIED | `test_guard_inertness.py` read in full; full suite green |
| GEOM-05 | 19.3-08, 19.3-09, 19.3-10 | Six experiments re-measured, determinism sweep reported | ✓ SATISFIED | git log `ae91625`; determinism probe re-run live |
| GEOM-06 | 19.3-03, 19.3-10 | MF-08 before/after with archived pre-fix artifacts, accuracy claims gated on measured seed band | ✓ SATISFIED (stricter than requirement text, disclosed) | See (b), (c) |

No orphaned requirements — all six GEOM-01..06 IDs traced to a plan's `requirements:` frontmatter and to REQUIREMENTS.md's traceability table, all marked Complete.

### Anti-Patterns Found

None found in the files inspected. No `TBD`/`FIXME`/`XXX` markers encountered in
`src/aquacal/datasets/synthetic.py`, `tests/synthetic/test_guard_inertness.py`, or
`experiments/e4_benchmark_grid.py` during review. Optimality figures in MF-08 are consistently
quoted to one significant figure (checked in Plan 10's SUMMARY, which records that this was
caught and fixed twice during the checkpoint honesty pass: `0.0126 → 1e-02`; `874 → 0.525` →
`9e+02 → 5e-01`).

### Human Verification Required

None. All must-haves resolve programmatically: derivation logic is readable in source, tests
exist and (per the operating constraints' pre-established full-suite run) pass, the determinism
probe is self-verifying and was re-run live, and MF-08's accuracy-claim gating is a textual
policy check that was read in full rather than inferred.

### Gaps Summary

No gaps found. The only notable imprecision is the phase goal's stale "five affected
experiments" wording (item a) — E3 was added to scope after the goal text was written, and both
ROADMAP.md's own Success Criterion 6 and REQUIREMENTS.md correctly list six. This does not block
goal achievement; it is a residual wording artifact from before the scope amendment, already
corrected everywhere except the original one-sentence goal statement.

---

*Verified: 2026-08-04*
*Verifier: Claude (gsd-verifier)*
