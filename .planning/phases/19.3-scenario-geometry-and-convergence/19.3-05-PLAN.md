---
phase: 19.3-scenario-geometry-and-convergence
plan: 05
type: execute
wave: 2
depends_on: ["19.3-01"]
files_modified:
  - experiments/e4_benchmark_grid.py
  - experiments/e3_derived_quantities.py
  - experiments/e5_index_sensitivity.py
  - tests/unit/test_experiments_e4.py
  - tests/unit/test_experiments_e3.py
  - tests/unit/test_experiments_e5.py
autonomous: true
requirements: [GEOM-01]
must_haves:
  truths:
    - "build_grid_scenario threads a BoardConfig into the trajectory generator and produces a legal scenario at every grid cell"
    - "GRID_DEPTH_RANGE is computed from the derived clearance floor, not restated as a literal (D-19.3-01 derive-don't-hardcode)"
    - "E3's and E5's three hardcoded depth_range=(1.1, 2.0) call sites are deliberately updated rather than bypassed (D-19.3-04)"
    - "E5's calibration trajectory and its held-out trajectory move in lockstep -- they never use inconsistent geometry"
  artifacts:
    - path: "experiments/e4_benchmark_grid.py"
      provides: "derived GRID_DEPTH_RANGE + board threading in build_grid_scenario"
      contains: "board_clearance_floor"
  key_links:
    - from: "experiments/e4_benchmark_grid.py::build_grid_scenario"
      to: "generate_board_trajectory"
      via: "board=GRID_BOARD_CONFIG"
      pattern: "board=GRID_BOARD_CONFIG"
---

<objective>
Update every `experiments/` call site that plan 01's required `board` parameter and derived floor
now break: E4's `build_grid_scenario` and `GRID_DEPTH_RANGE`, E3's Newton sweep, and E5's two
trajectories.

Purpose: D-19.3-04 chose a hard `ValueError` over warn-and-continue precisely so these three sites
would force a deliberate update rather than silently continuing with shallow geometry. This plan
is that deliberate update.

Output: a grid family whose depth range is derived rather than restated, and three harnesses that
run again.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-RESEARCH.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-01-SUMMARY.md
@experiments/e4_benchmark_grid.py

<interfaces>
Call sites that now raise (verified line numbers):

  experiments/e4_benchmark_grid.py:547  build_grid_scenario -> generate_board_trajectory(...)
                                        passes NO board today; depth_range defaults to GRID_DEPTH_RANGE
  experiments/e4_benchmark_grid.py:215  GRID_DEPTH_RANGE = (1.1, 2.0)      # literal, must become derived
  experiments/e4_benchmark_grid.py:212  GRID_HEIGHT_ABOVE_WATER = 1.031    # FROZEN, do not touch
  experiments/e4_benchmark_grid.py:153  GRID_BOARD_CONFIG = BoardConfig(12, 9, 0.060, 0.045, ...)
  experiments/e3_derived_quantities.py:331  generate_real_rig_trajectory(depth_range=(1.1, 2.0), ...)
  experiments/e5_index_sensitivity.py:187   generate_real_rig_trajectory(depth_range=(1.1, 2.0), ...)  # calibration set
  experiments/e5_index_sensitivity.py:467   generate_real_rig_trajectory(depth_range=(1.1, 2.0), ...)  # held-out set

From plan 19.3-01:
  board_clearance_floor(board, water_zs, rotation_range_deg, margin_factor=0.1) -> float
  generate_board_trajectory(..., *, board, depth_range=None, ...)      # rotation_range_deg=15.0 default
  generate_real_rig_trajectory(..., *, board, depth_range=None, water_zs=None, ...)  # 20.0 internally

Established pattern in this family (e6 already does it, lines 129-135): import and DERIVE from
e4's constants rather than restating absolute numbers.
</interfaces>

**Not in scope here:** E6's scale axis (plan 19.3-06) and the harness `status` gating on the guard
count (plan 19.3-07). E3 is NOT one of GEOM-05's five re-measured experiments — it needs the
mechanical call-site fix so it does not raise, nothing more. Do not conflate the two.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive GRID_DEPTH_RANGE and thread the board through build_grid_scenario</name>
  <read_first>
    - experiments/e4_benchmark_grid.py lines 145-235 (the constants block and its comments, in full)
    - experiments/e4_benchmark_grid.py lines 470-570 (build_grid_scenario)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-01-SUMMARY.md (the actual derived floor)
  </read_first>
  <files>experiments/e4_benchmark_grid.py, tests/unit/test_experiments_e4.py</files>
  <behavior>
    - `GRID_DEPTH_RANGE[0]` equals the floor `board_clearance_floor` derives for `GRID_BOARD_CONFIG`
      at `rotation_range_deg=15.0` against the grid family's own `water_zs`; `GRID_DEPTH_RANGE[1]`
      stays 2.0 (D-19.3-03).
    - `build_grid_scenario` constructs without raising for every cell in the committed nine-cell
      grid (camera counts and frame counts as configured).
    - No board corner in any frame of a `build_grid_scenario` output is at or above `max(water_zs)`.
    - Changing `GRID_BOARD_CONFIG.square_size` moves `GRID_DEPTH_RANGE[0]` -- the constant is
      derived, not a literal that happens to be right.
  </behavior>
  <action>
    Make `GRID_DEPTH_RANGE` a computed-at-import value rather than a literal. Compute it as
    `(board_clearance_floor(GRID_BOARD_CONFIG, <grid water_zs>, 15.0), 2.0)`. Note the anchoring
    subtlety D-19.3-07 flags explicitly: the constraint binds on the deepest PER-CAMERA `water_z`
    (~1.0367 with `height_variation`'s interface tilt), not on the nominal
    `GRID_HEIGHT_ABOVE_WATER = 1.031`, and that 5.7 mm does not scale. Anchor on `max(water_zs)`.
    Obtain those `water_zs` the same way `build_grid_scenario` does -- from
    `generate_camera_array(..., height_above_water=GRID_HEIGHT_ABOVE_WATER, spacing=GRID_SPACING)`
    at the baseline camera count -- so the constant cannot drift from what the cells actually build.

    Extend the constants block's comment in the existing D-29 voice: state that this value is now
    DERIVED, name the inputs it derives from, and state that it moves if the board or the tilt
    range moves. That property is the entire point of D-19.3-01.

    `GRID_HEIGHT_ABOVE_WATER` stays 1.031 and `GRID_BOARD_CONFIG` stays unchanged. `WATER_Z` is frozen.

    In `build_grid_scenario`, pass `board=GRID_BOARD_CONFIG` to `generate_board_trajectory`. Check
    whether `run_grid_cell`'s held-out trajectory goes through the same builder (19.2 plan 18 made
    it do so specifically to avoid a bare-defaults call); if it does, it inherits the fix -- confirm
    that in the SUMMARY rather than assuming it.

    Add the tests named in `<behavior>` to `tests/unit/test_experiments_e4.py`. Verify the existing
    monkeypatch spy (~lines 924-935) that asserts `build_grid_scenario` never calls the generator
    with bare defaults still captures the `board=` kwarg correctly.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_experiments_e4.py -q` passes.
    - Source assertion: `grep -v '^#' experiments/e4_benchmark_grid.py | grep -c "GRID_DEPTH_RANGE = (1.1, 2.0)"` returns 0.
    - Source assertion: `grep -c "board=GRID_BOARD_CONFIG" experiments/e4_benchmark_grid.py` is at least 1.
    - Behavior assertion: a test proves `GRID_DEPTH_RANGE[0]` changes when `GRID_BOARD_CONFIG`'s
      `square_size` is varied (patched), and that `GRID_DEPTH_RANGE[1] == 2.0`.
    - Behavior assertion: a test proves zero corners at or above `max(water_zs)` for a
      `build_grid_scenario` output at production frame count.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_e4.py -q</automated>
  </verify>
  <done>GRID_DEPTH_RANGE is derived from the clearance floor, build_grid_scenario threads the board, and every grid cell builds legally with no protruding corners.</done>
</task>

<task type="auto">
  <name>Task 2: Update E3's and E5's three hardcoded depth_range call sites</name>
  <read_first>
    - experiments/e3_derived_quantities.py lines 300-350
    - experiments/e5_index_sensitivity.py lines 170-200 and 440-480
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-RESEARCH.md ("Pitfall 4")
  </read_first>
  <files>experiments/e3_derived_quantities.py, experiments/e5_index_sensitivity.py, tests/unit/test_experiments_e3.py, tests/unit/test_experiments_e5.py</files>
  <action>
    At all three sites, replace the hardcoded `depth_range=(1.1, 2.0)` with the derived form: pass
    `board=` (the same `BoardConfig` each harness already uses for its scenario) and either
    `depth_range=None` so the generator derives its own floor, or an explicitly derived
    `(board_clearance_floor(...), 2.0)` if that harness needs the value in scope for logging. Do not
    hardcode a new literal, and do not introduce a bypass, an optional-board wrapper, or a local
    shallow default.

    E5's two sites are its calibration trajectory (line ~187) and its held-out/validation
    trajectory (line ~467). They must move together and end up with identical geometry -- a
    calibration set and a held-out set built at different depths would silently make E5's
    generalization number measure the wrong thing. Add an assertion or a shared helper so the two
    cannot drift.

    E3 is NOT one of GEOM-05's five re-measured experiments. This is a mechanical fix so it does not
    raise. Whether E3's own numbers move meaningfully is a question for its own `--check`, not a
    reason to add it to the re-run set. Record in the SUMMARY whether a quick E3 `--check` still
    passes at its committed tolerance, and if it does not, hand that observation to plan 19.3-10
    rather than expanding scope here.

    Add or extend unit tests asserting each harness's scenario builder produces a legal
    (non-raising) trajectory and that E5's two trajectories share one depth range.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_experiments_e3.py tests/unit/test_experiments_e5.py -q` passes.
    - Source assertion: `grep -rn "depth_range=(1.1, 2.0)" experiments/` returns nothing.
    - Behavior assertion: a test proves E5's calibration and held-out trajectories are generated
      with the same resolved `depth_range`.
    - `python -m experiments.e5_index_sensitivity --smoke` completes without a `ValueError`
      (smoke is acceptable HERE because this is a mechanical import/construction check, not the
      GEOM-05 acceptance evidence -- anti-pattern #4 forbids smoke as proof of a fix, and this is
      not that claim).
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_e3.py tests/unit/test_experiments_e5.py -q</automated>
  </verify>
  <done>All three hardcoded call sites are derived, E5's two trajectories provably share one geometry, and neither harness raises.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the grid family runs end to end at production frame counts</name>
  <read_first>
    - experiments/e4_benchmark_grid.py (CELL_TIMEOUT_SECONDS, the --cell subprocess contract)
    - experiments/README.md (E4's measured runtime and CLI contract)
  </read_first>
  <files>tests/unit/test_experiments_e4.py</files>
  <action>
    Add a construction-only integration test that builds -- but does not calibrate -- every cell in
    the committed grid at its PRODUCTION frame count, asserting each scenario is legal and that no
    corner protrudes. Construction is cheap; only the calibration is not, so this gives production-
    scale geometry evidence without a 3-hour run. Mark it `slow` only if it actually is.

    This is deliberately NOT a claim that the fix works -- anti-pattern #4 (every geometry variant
    converges at 6 frames with the bug present; the failure needs ~50) means only GEOM-05's real
    re-runs can make that claim. State that boundary in the test's docstring so a later reader does
    not mistake this for the acceptance evidence.

    Do not launch any calibration run from this plan. Production runs are box-exclusive and belong
    to plan 19.3-09 (D-19.3-18, review H4).
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_experiments_e4.py -q` passes UNFILTERED.
    - The new test iterates every configured grid cell at production `n_frames`, not a reduced count.
    - The test docstring states it is geometry evidence only, not convergence evidence.
    - No calibration is executed by this plan -- `grep -c "calibrate_synthetic" ` in the new test returns 0.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_e4.py -q</automated>
  </verify>
  <done>Every production-scale grid cell constructs legally with no protruding corners, proven by test, with no calibration run.</done>
</task>

</tasks>

<verification>
- `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_e3.py tests/unit/test_experiments_e4.py tests/unit/test_experiments_e5.py -q` -> 0 failures, UNFILTERED.
- `grep -rn "depth_range=(1.1, 2.0)" experiments/` -> nothing.
- Security: no packages installed; `security_enforcement` N/A per `19.3-RESEARCH.md` Security Domain.
</verification>

<success_criteria>
- `GRID_DEPTH_RANGE` is computed from the derived floor and anchored on `max(water_zs)`.
- `build_grid_scenario` threads `board=GRID_BOARD_CONFIG`.
- All three E3/E5 hardcoded call sites are derived; E5's two trajectories cannot drift apart.
- Every production-scale grid cell constructs legally with no protruding corners.
- `GRID_HEIGHT_ABOVE_WATER`, `GRID_BOARD_CONFIG`, `WATER_Z` and `margin_factor` are all unchanged.
</success_criteria>

<output>
Create `.planning/phases/19.3-scenario-geometry-and-convergence/19.3-05-SUMMARY.md` when done.
Record: the computed `GRID_DEPTH_RANGE`, whether `run_grid_cell`'s held-out path inherited the fix,
and the E3 `--check` observation.
</output>
