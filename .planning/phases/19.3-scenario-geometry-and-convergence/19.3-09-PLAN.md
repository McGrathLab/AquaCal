---
phase: 19.3-scenario-geometry-and-convergence
plan: 09
type: execute
wave: 6
depends_on: ["19.3-08"]
files_modified:
  - experiments/prelaunch_gate.sh
  - experiments/results/
  - experiments/results_e6_repeat2/
autonomous: false
requirements: [GEOM-05]
must_haves:
  truths:
    - "E1, E4, E5, E6 and E7 are all re-measured on the corrected geometry at production scale in one detached serial run (D-19.3-18)"
    - "Every artifact from the night carries the same git_sha -- nothing was committed while a run was in flight"
    - "E6's second repeat genuinely re-solved rather than resuming the first repeat's checkpoints (D-19.3-20)"
    - "The declared measure -- cell reproduction count out of 308 -- is recorded whatever it shows, including no improvement (D-19.3-14)"
    - "The pre-launch freeze conditions are checked by a script that ABORTS on failure, not by a human gate that a sleeping user cannot answer"
  artifacts:
    - path: "experiments/prelaunch_gate.sh"
      provides: "the scripted five-check abort gate that must exit 0 before the queue launches"
    - path: "experiments/results/generalization_sweep.csv"
      provides: "E6 repeat 1 on corrected geometry"
    - path: "experiments/results_e6_repeat2/generalization_sweep.csv"
      provides: "E6 repeat 2, isolated output directory"
    - path: "experiments/results/benchmark_grid.csv"
      provides: "E4 nine-cell grid on corrected geometry"
  key_links:
    - from: "experiments/rerun_19_3.sh"
      to: "experiments/results/"
      via: "detached nohup launch, one stage at a time"
      pattern: "nohup"
---

<objective>
Execute the ~9 h chained overnight re-run of E1, E4, E5, E6 (twice) and E7 on the corrected
geometry, detached and serial, with the tree frozen for the duration.

Purpose: this is GEOM-05's actual measurement. Everything before it existed to make this run
trustworthy; everything after it reads its output.

Output: a full set of corrected-geometry artifacts, a second isolated E6 sweep, and per-stage gate
verdicts.
</objective>

<unattended_execution_boundary>
**Task 3 of this plan is the expected end of unattended execution. Reaching it is SUCCESS, not
failure.**

Tasks 1 and 2 are fully autonomous: a scripted abort gate, then a detached ~9 h queue monitored to
completion. `workflow.auto_advance` is `false`, so the `checkpoint:human-verify` at Task 3 genuinely
HALTS — it is not rubber-stamped. That halt is intentional and correct: Task 3 is an editorial
judgement about what the artifacts mean, which no script can make.

**State of the phase when it pauses at Task 3:**
- The queue has finished. All six stages (E7, E1, E5, E6 x2, E4) have reached a terminal state.
- Corrected-geometry artifacts are on disk in `experiments/results/` and
  `experiments/results_e6_repeat2/`.
- Per-stage gate verdicts are in the run log; `check_rerun_gates.py` output is captured.
- **Nothing from the night is committed.** HEAD is still the sha recorded by Task 1 — that is a
  requirement, not an oversight. Committing during a run splits an artifact's recorded provenance.
- Plan 10 has not started. No accuracy claim has been made anywhere.

**What to look at first on returning:**
1. The tail of `experiments/rerun_19_3_<date>.log` — did all six stages exit cleanly?
2. The captured `check_rerun_gates.py` output — is
   `degenerate_observations_at_solution == 0` on every row? That zero is the phase's headline result.
3. Whether `skipped_existing` appears anywhere in E6 repeat 2 — if it does, checkpoint isolation
   failed and the determinism measurement is void for those configs (the D-19.3-20 trap).
4. `git log --oneline -1` — must still be Task 1's recorded sha.
</unattended_execution_boundary>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-08-SUMMARY.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-07-SUMMARY.md
@experiments/rerun_19_3.sh

<interfaces>
Launch form (from plan 08's SUMMARY — use the exact command recorded there):
    nohup bash experiments/rerun_19_3.sh > experiments/rerun_19_3_<date>.log 2>&1 & disown

Stage order and expected wall clock:
    E7 ~7 min -> E1 ~25 min -> E5 ~21 min -> E6 repeat 1 ~107 min
    -> E6 repeat 2 ~107 min (isolated --out) -> E4 ~3.5 h.  Total ~= 9 h.

Stage-level state file (written by the queue, per plan 08 Task 3): one line per stage recording
name, start time and exit code, distinguishing "stage completed" from "stage started but died".
Plan 08's SUMMARY records the exact resume-from-stage-N commands.

Peak memory: Stage 3 peaks at ~9.8 GiB on the 13-camera real rig; the synthetic grid's largest
cells are smaller but a 16-camera cell is a known OOM. An OOM on one cell is a recorded outcome,
not a reason to restart the night.
</interfaces>

**Non-negotiable operational rules for this plan:**
- Launch DETACHED (`nohup ... & disown`). NEVER `run_in_background` — the harness kills background
  commands at ~35-50 min and this has already cost three sweeps.
- Commit NOTHING, tag nothing, and do not touch the working tree between launch and completion.
  Per-cell `git_sha` capture would split an artifact's provenance across two commits.
- One production run at a time on the box (review H4). Do not start anything else, including tests.
- A quiet transcript is not a dead run. Check the process listing and the log's mtime before
  concluding anything is wrong — relaunching a live queue starts a duplicate calibration.
- All stages at production scale. `--smoke` is not evidence (anti-pattern #4).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-launch freeze gate (scripted abort gate)</name>
  <read_first>
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-07-SUMMARY.md (the D-26 statement and the unfiltered suite counts)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-08-SUMMARY.md (the exact launch command)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-03-SUMMARY.md (confirm the pre-fix archive already exists)
  </read_first>
  <files>experiments/prelaunch_gate.sh</files>
  <action>
    This is a PRE-FLIGHT / ABORT GATE, not a human-verify gate. Every one of its five checks is a
    file-existence check or a command exit code, so a script can actually run them and a sleeping
    human cannot. Per this project's own gate taxonomy, a precondition verifiable by a command exit
    code is scripted and aborts on failure; it does not wait for a typed approval.

    Write `experiments/prelaunch_gate.sh` implementing the five checks below as hard assertions,
    then run it. If it exits non-zero, ABORT here: do not launch the queue, do not proceed to
    Task 2, and record the named failing check in the SUMMARY. Falling through to Task 2 after a
    failed check is the specific failure mode this task exists to prevent — the abort must be loud
    and must name which check failed.

    Each check prints its own `PASS <NAME>` / `FAIL <NAME>` line so the abort message identifies
    itself:

    1. TREE_CLEAN — `git status --porcelain` produces no output. Any output fails.
    2. SUITE_GREEN — `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/ -q` with NO `-m`
       selector exits 0. The run stays UNFILTERED. `-m "not slow"` deselects exactly the
       bit-identity, frozen-anchor and inertness suites that are this phase's evidence, so a
       filtered run is not a valid gate — this is the same rewrite Phase 19.2's plan 06 launch gate
       needed. The script must not accept a marker selector from the environment or an argument,
       and the check fails if the pytest summary line reports any marker-caused deselection.
    3. HEAD_RECORDED — `git rev-parse HEAD` is captured, echoed, and written to a file that both
       the queue and the SUMMARY read. Every artifact produced tonight must carry this sha.
    4. ARCHIVES_PRESENT — `experiments/archive/*-2026-08-02-pre-depth-fix` resolves to all five
       pre-fix archive directories. Fewer than five fails, and the message names which experiments
       are missing.
    5. WORKTREES_CLEAN — `git worktree list` shows no stray executor worktrees. The superseded
       19.2-21 branch at `.claude/worktrees/worktree-agent-a1a99b5a5289e9e05` must be either absent
       or present-and-unmerged; it is evidence only and must never be merged. If it appears merged,
       fail.

    On success the script echoes the recorded sha and restates the freeze — from here until the
    queue finishes (~9 h) nothing is committed, no tests are run, and no other work happens on this
    box — then exits 0. The freeze is enforced by this plan's own rules rather than by a typed
    acknowledgement; the user has explicitly authorised unattended execution for this window.
  </action>
  <acceptance_criteria>
    - `bash -n experiments/prelaunch_gate.sh` exits 0.
    - Each of the five checks emits a self-naming PASS/FAIL line; the SUMMARY pastes the full output.
    - Source assertion: `grep -v '^ *#' experiments/prelaunch_gate.sh | grep -c 'not slow'` returns 0 —
      the suite check is unfiltered and cannot be filtered.
    - Non-zero exit on any failure is demonstrated, not assumed: induce one failure (e.g. run the
      script against a deliberately dirty scratch copy) and confirm both the non-zero exit and the
      named failing check in the output.
    - The plan aborts before Task 2 on non-zero exit — the SUMMARY states this explicitly as the
      controlling rule.
    - The recorded HEAD sha is written to disk before launch and quoted in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>bash -n experiments/prelaunch_gate.sh &amp;&amp; bash experiments/prelaunch_gate.sh</automated>
  </verify>
  <done>A scripted gate ran all five freeze checks unfiltered, exited 0, recorded the frozen sha to disk, and is demonstrated to abort loudly with a named check on any failure.</done>
</task>

<task type="auto">
  <name>Task 2: Launch the detached queue and monitor to completion</name>
  <read_first>
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-08-SUMMARY.md (exact launch command, stage order, per-stage expected wall clock, the E6 re-solve signal, the stage-state file location and the resume-from-stage-N commands)
    - experiments/rerun_19_3.sh
  </read_first>
  <files>experiments/results/, experiments/results_e6_repeat2/</files>
  <action>
    Only run this task if Task 1's gate exited 0. If it did not, stop.

    Launch the queue exactly as plan 08's SUMMARY records it, detached with `nohup ... & disown`.
    Confirm within the first two minutes that the log is being written and that a python process is
    alive; that is the only early check that distinguishes a launch failure from a slow first stage.

    Monitor by polling the log for the per-stage delimiters and the gate verdict blocks. Expected
    per-stage wall clocks are in plan 08's SUMMARY; a stage running past ~1.5x its expectation
    warrants checking the process listing, NOT relaunching. A quiet transcript is not a dead run,
    and relaunching a live queue starts a duplicate calibration on a box that can only host one.

    If the queue process dies mid-night, recover from the stage-level state file rather than
    reconstructing progress from the log by hand: it distinguishes a completed stage from one that
    started and died, and plan 08's SUMMARY carries the exact resume-from-stage-N command. A stage
    recorded as started-but-not-completed is re-run from scratch, never resumed. Any resume relaunch
    is itself detached with `nohup ... & disown`, and the no-commits rule holds across the recovery.

    Failure signatures worth watching for in the log: `MemoryError` / OOM kill (expected on the
    largest cells; record it, do not restart), `ValueError` naming a clearance floor (means a
    scenario is still illegal — a real defect, record the cell and let the queue continue),
    `ConvergenceError`, and `skipped_existing` appearing during E6 repeat 2 (means checkpoint
    isolation failed and the determinism measurement is void for those configs — record it
    explicitly; that is the trap D-19.3-20 exists to catch).

    Commit nothing. Run no tests. Touch no files under `src/` or `experiments/` while the queue runs.

    When the queue exits, record for each stage: wall clock, exit status, the gate verdict block,
    and whether any cell reported a non-zero `degenerate_observations_at_solution`.
  </action>
  <acceptance_criteria>
    - The launch used `nohup ... & disown`; the SUMMARY records the exact command and the PID.
    - `git log --oneline -1` at queue exit is the SAME sha recorded in Task 1 -- nothing was committed.
    - All six stages (E7, E1, E5, E6x2, E4) reached a terminal state; each one's wall clock and exit
      status is recorded, and the stage-state file is pasted into the SUMMARY.
    - Any mid-queue death and its recovery are recorded: which stage, which resume command, and
      confirmation that a started-but-incomplete stage was re-run rather than resumed.
    - `python experiments/check_rerun_gates.py experiments/results/` output is captured verbatim.
    - E6 repeat 2's output directory is distinct from repeat 1's, and the positive re-solve signal
      named in plan 08 is asserted and recorded. No `skipped_existing` rows in repeat 2.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe experiments/check_rerun_gates.py experiments/results/</automated>
  </verify>
  <done>All six stages completed, artifacts exist for all five experiments plus the isolated second E6 sweep, every artifact carries the frozen sha, and repeat 2 provably re-solved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Post-run artifact review</name>
  <action>Present the captured gate output and provenance checks to the user for review. Make no accuracy claim; that is plan 10's gate. THIS IS THE EXPECTED END OF UNATTENDED EXECUTION — halting here is the designed outcome, not a failure.</action>
  <what-built>
    A complete set of corrected-geometry artifacts for E1, E4, E5, E6 (two independent sweeps) and
    E7, plus per-stage gate verdicts. Nothing has been committed; HEAD is still the sha frozen
    before launch.
  </what-built>
  <how-to-verify>
    1. Read the captured `check_rerun_gates.py` output. Every experiment should show
       `degenerate_observations_at_solution == 0` on every row. That zero is the phase's headline
       result: it is what makes first-order optimality readable again.
    2. If ANY row is non-zero, that is a finding, not a failure to hide. It means an ESTIMATED pose
       or `water_z` put a corner above the ESTIMATED interface despite a legal ground-truth scenario
       — "the solver wandered". Record the cells and their counts and bring them to the user.
    3. Confirm every artifact's `git_sha` is the sha recorded before launch. A split sha means
       something was committed mid-run and the provenance is broken.
    4. Sanity-check E6's optimality column against the pre-fix values, quoting to ONE significant
       figure only. The pre-fix picture was three configurations at optimality 27-140 against
       ~1e-3 for the rest. Do not quote a finer comparison; optimality varies ~2x run to run.
    5. Do NOT draw an accuracy conclusion here. That is D-19.3-17's gate and belongs to plan 10.
  </how-to-verify>
  <acceptance_criteria>
    - The gate output and the per-artifact `git_sha` check are pasted into the SUMMARY.
    - Any non-zero guard count is named cell-by-cell rather than summarised away.
    - No optimality figure anywhere in the review is quoted beyond one significant figure.
    - No accuracy claim is made in this plan.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to reporting, or describe what needs re-running.</resume-signal>
</task>

</tasks>

<verification>
- `bash experiments/prelaunch_gate.sh` -> exit 0 before any launch; non-zero aborts the plan.
- `python experiments/check_rerun_gates.py experiments/results/` and the same against
  `experiments/results_e6_repeat2/` -- both captured verbatim.
- `git status --porcelain` empty and HEAD unchanged across the entire run.
- All five experiments' artifacts present at production scale; no `--smoke` output anywhere.
- Security: no packages installed; `security_enforcement` N/A per `19.3-RESEARCH.md` Security Domain.
</verification>

<success_criteria>
- The pre-launch freeze conditions were checked by a script that aborts, with no human in the loop.
- Five experiments re-measured at production scale in one detached serial night.
- A second, isolated E6 sweep that provably re-solved.
- One git sha across every artifact produced.
- Per-stage gate verdicts recorded, including any non-zero guard count, named cell-by-cell.
- No commits, no parallel runs, no smoke substitutions.
- Execution halts cleanly at Task 3 for human editorial review.
</success_criteria>

<output>
Create `.planning/phases/19.3-scenario-geometry-and-convergence/19.3-09-SUMMARY.md` when done.
Record: the pre-launch gate's full output, the frozen sha, per-stage wall clocks and exit statuses,
the stage-state file contents, any mid-queue recovery, the full gate output, the E6 repeat-2
re-solve evidence, and every non-zero guard count.
</output>
