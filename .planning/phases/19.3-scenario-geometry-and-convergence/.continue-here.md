> # ⚠ STALE — Phase 19.3 CLOSED 2026-08-04 (`d406001`, 10/10 plans)
>
> **Do not resume from this file.** It describes the pre-close state, when plan 10 sat at its
> Task 3 checkpoint with the trackers unticked. That checkpoint was walked and approved; GEOM-01..06
> are ticked and the `feat!:` commit is made. The authoritative record is `19.3-10-SUMMARY.md`.
>
> **Two things below are now wrong:** § "Where the phase actually is" (the phase is complete, and
> `/gsd:execute-phase 19.3 --wave 7` has nothing left to run), and § "START HERE" step 1 (19.3 is
> already closed — only step 2, the new phase for the E6 fix, remains).
>
> **What is still live and must carry forward:** the § "Critical anti-patterns still binding" table
> at the bottom, including anti-pattern 7. Copy it into the next phase's CONTEXT.md before this file
> is deleted — that is the 19.2 precedent.
>
> Next step: create the phase for the E4/E6 clearance-floor fix. Planning input is
> `.planning/debug/e6-seed-locked-clearance-floor.md`.

# Continue here — Phase 19.3

**Rewritten 2026-08-03 (second revision), after the E6 defect was fully diagnosed, E6 was
measured at two non-42 seeds, and the manuscript was read against the findings file.**
The previous revision's "START HERE: the E6 seed failures" section is **CLOSED** — both of
its open questions are answered. See § "What changed in this revision".

**On `main`. `git log -1` is authoritative — do not trust a SHA written here.** The
substance of this revision landed in `416ffa8`
(`docs(19.3): diagnose the E6 clearance-floor defect and map findings to the manuscript`);
anything after that is this handoff correcting itself. *Two earlier commits in this phase
exist solely to fix a stale SHA in this file — hence the wording.*

Unfiltered suite **1283 passed, 6 skipped, 0 failed** as of the last run. Nothing is
running. The 19.2 evidence worktree (`worktree-agent-a1a99b5a5289e9e05`) is still present
and **must never be merged**.

## Where the phase actually is

Plans 01–09 are COMPLETE with SUMMARYs. **Plan 10 is at its Task 3 checkpoint:
Tasks 1 and 2 are done and committed; the trackers are deliberately UNTICKED and the
closing `feat!:` commit is unmade, both awaiting approval.**

```
/gsd:execute-phase 19.3 --wave 7     # resumes at plan 10 Task 3
```

Do NOT pass `--auto` — it would rubber-stamp the human gate.

## ► START HERE: the user's decided next step

**Decision taken 2026-08-03 (user's words): "close this phase and then make a new phase for
this fix."** The E6 clearance-floor fix is diagnosed and specified but deliberately NOT
applied — it must not land inside 19.3.

So the sequence is:

1. **Close 19.3** — resume at wave 7, plan 10 Task 3 (the human gate above).
2. **Create a new phase for the E6/E4 clearance-floor fix.** Its planning input is
   ready: `.planning/debug/e6-seed-locked-clearance-floor.md` contains the root cause,
   all six call sites, the consequential changes, and a costed verification plan.

Nothing is blocked on further investigation. Both open questions from the previous
revision are answered.

## What changed in this revision

### 1. The E6 defect is fully diagnosed (both open questions answered)

Full report: **`.planning/debug/e6-seed-locked-clearance-floor.md`** (status `diagnosed`).
It supersedes F12 in `19.3-CODE-TRACE-FINDINGS.md`.

**Q1 — why 43–46 failed in ~1 s but 47/50 burned 74–84 min.** `run_configuration` builds
**two** scenarios inside one `try` with a blanket `except`: calibration at `seed`
(`e6:759`) and holdout at `seed + 1_000_000` (`e6:787`). Both are checked against the same
frozen constant and **both must clear it**. 43–46 fail the calibration draw → raise before
any solve. 47/50 pass calibration, run Stage 2 + Stage 3 **to convergence**, then fail the
holdout draw and the finished solve is discarded. Verified: the printed floor matches one
of the two arrays at 4 dp for **72/72** seed×config cases.

**Q2 — why "predicted-legal" seeds weren't.** The prediction's method was right — a
standalone `generate_camera_array` IS the call `build_grid_scenario` makes. It was
**incomplete**: it modelled only the calibration array. *The previous revision of this
handoff claimed the standalone call "does not reproduce E6's construction". That was wrong
and is retracted.*

**Three claims in the previous revision were wrong** and are corrected in the report:
- Not "every configuration builds its own array with its own floor" — the floor is
  **invariant to layout and spacing** (180 arrays, 20 seeds). It varies with `n_cameras`
  (11/23 seeds), which is why E4 has up to six legality draws per seed.
- Not "the fix is verified inert at seed 42" — `depth_range=None` is inert for the
  calibration scene but shifts the **holdout** scene by up to **0.469 mm**. The form that
  is bit-inert for both is
  `max(GRID_DEPTH_RANGE[0], board_clearance_floor(GRID_BOARD_CONFIG, water_zs, 15.0))`.
- Not "there cannot be a seed band without a code change" — see below.

### 2. E6 is NOT seed-locked to 42 — and it converges off 42

**29 of seeds 0–499 (5.8%) clear both draws and run today with no fix.** Below 100:
**28, 42, 52, 62, 72, 75, 94.**

Seeds **62** (85 min) and **28** (83 min) were run: both `{'ok': 14}`, guard count **0** on
all 14 configurations. Accuracy is reproducible — `reconstruction_rmse_mm` within ~5%
everywhere. **Two seed-fragile spots** surfaced, both previously invisible:
`scale/double_scale` optimality (diagnostic only, no accuracy cost) and `layout/line`
parameter recovery (`xy` 1.6→6.2 mm, `water_z` 3.5→11.8 mm, while reconstruction stays
clean — the weak-observability signature).

Durable record: **`evidence/e6_seed_band_42_62_28.md`** and the `.csv` beside it. The raw
output in `seed_sweep_19_3/e6/seed_{62,28}/` is **gitignored** and will not survive a
cleanup; regenerating costs ~85 min/seed.

**Consequence: MF-08 still says E6's accuracy silence is *structural*. That is now wrong
and needs amending.** It is a measurement question, not an impossibility. n = 3 is not a
band — do not quote one.

### 3. The manuscript has been read against the findings file — MF-09

**`.planning/MANUSCRIPT-FINDINGS.md` § MF-09** is the edit map: every MF-01..08 finding
located in the actual submitted `main.tex` / `supplement.tex`, with a verdict on each.

Headline: **most of §3's headline numbers must NOT be edited** — the noise floor is wide
enough that several apparent corrections are noise. Four things genuinely need changing;
six must be left alone. MF-09 also found three **location errors** in earlier entries
(e.g. MF-01 sends you to the supplement, but the Newton claim is in `main.tex:204` too)
and one edit that is already unnecessary.

## Uncommitted / recently committed work

If the session was lost, check `git log -1` first. Committed in this session's final
commit: MF-09, the debug session file, the E6 seed-band evidence, the probe harness, and
this handoff. **`.planning/config.json` carries an unstaged gsd-tools schema rename
(`depth` → `granularity`) that is NOT mine — leave or commit as you see fit.**

## What was measured

The ~9 h production queue ran in **6 h 02 min**, all seven stages exit 0, one git sha
(`22e75ef`) across every artifact. Gate: **81 PASS / 7 N/A / 8 FAIL**, against 52/7/37
pre-fix. Guard counts are zero everywhere except E1's non-refractive arm (explained
below). Determinism improved **63/308 → 8/308**. E6's optimality outliers collapsed
(5e+01 → 1e-02, 3e+01 → 8e-03), verified on **both** optimality columns.

Then three seed sweeps: **E1 ×10** and **E7 ×10** (both integrity-audited and sound),
and **E6 at three seeds** — which exposed a defect rather than a band.

## The three things that matter most

**1. The `135x → 128x` change is NOT real.** The deepest-point ratio bands
**97.3x – 178.0x** (mean 139.5, sd 25.1, n=10). The change is 7x — 0.09x of the band.
An earlier revision of MF-08 reported it as a corrected §3 number *and* decomposed it
to the geometry fix. **Both are retracted in MF-08.** Reassuringly, the published
`~135x` sits 0.18 sd from the ten-seed mean, and the abstract's 1.9 mm is conservative
against a 1.69 mm mean. **Recommendation: change nothing here.** "Correcting" it would
be publishing noise.

**2. E6 is NOT seed-locked to 42 — this replaces the previous revision's claim.** 29 of
seeds 0–499 clear both draws and run today with no fix; seeds 62 and 28 both returned
`{'ok': 14}` with guard count 0. **MF-08's "E6's silence is structural" is therefore wrong
and needs amending** — it is a measurement question. n = 3 is not a band; do not quote one.
Full detail in `.planning/debug/e6-seed-locked-clearance-floor.md` and
`evidence/e6_seed_band_42_62_28.md`.

**Still true, and still the reason E6 has no band:** E6 records a per-configuration failure
as a `status=failed` row rather than raising, so a run in which all 14 configurations died
still **exits 0**. Four dead runs were once logged as "complete, exit 0" and caught only by
the wall clock. **Check `status` counts, never the exit code.**

**3. The abstract leads with its most seed-fragile number.** Depth ratio: 80% spread.
Inter-corner distance RMSE: **0.5% spread** (0.4194–0.4216 mm), sub-mm on every seed.
Reordering costs nothing and retracts nothing — the single best low-risk improvement
available to the revision.

## Accuracy claims: only E7 survives

D-19.3-17 applied strictly is **more restrictive than planning assumed**:

- **E7 — supported**, and now on a *corrected-geometry* 10-seed band. The primary
  `fixed` pairing STRENGTHENED to **10/10, no zero crossing, p = 0.00098**. The
  secondary `refined` pairing WEAKENED to 8/10 (p = 0.055). Both recorded in MF-05.
- **E1 — NONE** (was PARTIAL). Its `xy` metric read "within noise" only against the
  *pre-fix* band; the corrected band is 4.5x narrower and **both** metrics fall outside.
- **E5 — NONE.** `e5_provenance.json` shows `seed: 42` with an 11-point `n_assumed_band`
  — it varies the assumed index, not the seed. The inherited "E5: 5 seeds" was wrong.
- **E3, E4, E6 — NONE.** For E4 this is still "not measurable as shipped" (its `n_cameras`
  axis gives up to six legality draws per seed). **For E6 the reason has changed**: a band
  is obtainable from the naturally-legal seeds, it just has not been measured to band
  size. n = 3 as of 2026-08-03.

## Other manuscript findings (MF-08, and the edit map in MF-09)

**Read MF-09 before editing anything.** It locates every MF-01..08 finding in the actual
`main.tex` / `supplement.tex`, rules on each, and corrects three location errors in earlier
entries — including MF-01, which sends you to the supplement when the Newton claim is in
`main.tex:204` as well.

- §3's calibration depth range is stale: 0.10–0.84 m → **0.15–0.93 m** (corners).
- "Sub-2 mm at every depth" **fails on 2 of 10 seeds** (44 → 2.111, 50 → 2.254), and the
  paper never states its seed; E1's records carry no `seed` field.
- §2 **understates its own FD reduction 4x**: 42x measured on the exact 12-camera,
  100-frame case it names, against "roughly an order of magnitude".
- `benchmark_grid.csv`'s `real_rig` row is E2-derived at `6c7f930b`; its 11 moved fields
  are `faa05b3`'s guard fix (**MF-04**), not a geometry effect.

## Verified sound — do not re-litigate

- **§3's real-rig numbers reproduce exactly from `release_calibration`** (0.268 / 0.674 /
  7762 / 0.879 / 15.134 / 1.0306). They are NOT reproducible from `experiments/results/`,
  which is a different E2 run — that divergence is MF-04's, fully owned and correct.
- **Ground-truth vs assumed refractive index are correctly separated** — detections are
  generated at the true 1.333 while the calibration assumes 1.0 or 1.333. This was the
  highest-risk design question in the paper's headline comparison; it is implemented
  correctly.
- E1's test placements are genuinely independent; frame count (30) unchanged across the
  fix; no NaN reaches the optimizer; `interface_distance` is not misused; E4's
  representativeness was fixed by D-29; E6's scale axis matches its revised intent.

## If this handoff is not enough

It was written from working memory, not by re-reading the session, so it captures what
was judged load-bearing at the time. The full transcript of the 2026-08-02/03 session
that produced everything here is:

```
~/.claude/projects/C--Users-tucke-PycharmProjects-AquaCal/ae411039-a90a-43de-9e1c-dbf0fe18ea5c.jsonl
```

~6.0 MB. **Do not load it wholesale** — it is roughly 600k tokens. Read the tail, or grep
it for a specific thing, and only when this handoff plus `19.3-CODE-TRACE-FINDINGS.md`
plus MF-08 have failed to answer the question. Sibling sessions in the same directory:
`48288322-…` (2026-08-02, the pre-launch planning) and `c8a889b5-…` (background task
output for this session).

## Where the detail lives

- `.planning/MANUSCRIPT-FINDINGS.md` — MF-08 (six experiments, seed bands, retraction),
  MF-01 (regenerated Newton table), MF-05 (E7 re-measurement), MF-04 (real-rig).
- `19.3-CODE-TRACE-FINDINGS.md` — all twelve trace findings with evidence.
- `evidence/water_z_null_direction_probe.py`, `evidence/e1_pinned_water_z.py` — the
  proofs behind E1's 14,949 being bookkeeping.
- `seed_sweep_19_3/` — gitignored raw sweep output (E1 ×10, E7 ×10, E6 ×3).

## Critical anti-patterns still binding

| # | Anti-pattern | severity |
|---|---|---|
| 1 | Merging 19.2's `worktree-agent-a1a99b5a5289e9e05` branch | **blocking** — superseded evidence only |
| 2 | Judging convergence by reprojection RMS | **blocking** — optimality and guard count are the discriminators |
| 3 | Relaxing a determinism/byte-identity gate to make it pass | **blocking** — amend the plan, never the tolerance |
| 4 | Concluding a fix works from a low frame count | **blocking** — the failure needs ~50 frames |
| 5 | Tuning `k` to make a failing case pass | **blocking** — if a scenario fails, the scenario moves |
| 6 | Letting a subagent background a long run and return | warning — it has stalled |
| 7 | **Treating a single-seed difference as a real effect** | **blocking** — see below |

**Anti-pattern 7 is new, and it is the lesson of 2026-08-03.** Seven errors were made
that day, all one class: generalising from a sample that did not represent the
population. Two were caught loudly by the D-19.3-01 clearance guard. The rest were
caught only by re-measurement — and one produced a confidently-stated *decomposition of
pure noise* that would have reached the manuscript. **Before attributing any movement to
any cause, measure the noise floor first.** This is the third independent time this
project has recorded this lesson (19.1, 19.2, 19.3).
