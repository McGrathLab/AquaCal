---
phase: 19.3-scenario-geometry-and-convergence
plan: 06
type: execute
wave: 3
depends_on: ["19.3-05"]
files_modified:
  - experiments/e6_generalization_sweep.py
  - tests/unit/test_experiments_e6.py
autonomous: true
requirements: [GEOM-03]
must_haves:
  truths:
    - "Every SCALE_AXIS_VALUES entry builds a legal scenario by construction -- half_scale no longer violates the clearance floor (D-19.3-07)"
    - "The scale axis anchors at the derived clearance floor, not at the water surface"
    - "The axis's documented claim matches what it now measures -- no documented-but-false claim survives"
  artifacts:
    - path: "experiments/e6_generalization_sweep.py"
      provides: "floor-anchored _scaled_depth_range plus corrected axis prose"
      contains: "_scaled_depth_range"
  key_links:
    - from: "experiments/e6_generalization_sweep.py::_scaled_depth_range"
      to: "experiments/e4_benchmark_grid.py::GRID_DEPTH_RANGE"
      via: "import and derive, never restate"
      pattern: "GRID_DEPTH_RANGE"
---

<objective>
Re-define E6's scale axis to scale the working-volume depth extent ABOVE the derived clearance
floor, keeping the floor fixed, so every scale value is legal by construction — and correct the
axis prose that stops being true when the anchor moves.

Purpose: the scale axis and the clearance rule are in direct conflict as currently defined.
`half_scale` halves the board-to-surface standoff while the clearance requirement stays fixed, so
it violates the floor by 133 mm today and by 84 mm even after the floor moves. D-19.3-07 resolves
that in favour of the clearance rule: the axis is what changes.

Output: a floor-anchored `_scaled_depth_range`, corrected docstring/comment prose, and a legality
test across all three scale values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-RESEARCH.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-05-SUMMARY.md
@experiments/e6_generalization_sweep.py

<interfaces>
Current code (experiments/e6_generalization_sweep.py:105-166):

  _BASELINE_DEPTH_BELOW_WATER = (GRID_DEPTH_RANGE[0] - GRID_HEIGHT_ABOVE_WATER,
                                 GRID_DEPTH_RANGE[1] - GRID_HEIGHT_ABOVE_WATER)
  _BASELINE_XY_EXTENT = default_xy_extent_for_layout(...)
  def _scaled_depth_range(factor) -> (GRID_HEIGHT_ABOVE_WATER + factor*lo,
                                      GRID_HEIGHT_ABOVE_WATER + factor*hi)
  SCALE_AXIS_VALUES = [("half_scale", _scaled_depth_range(0.5), 0.5*_BASELINE_XY_EXTENT, 0.5*GRID_SPACING),
                       ("default", None, None, None),
                       ("double_scale", _scaled_depth_range(2.0), 2.0*_BASELINE_XY_EXTENT, 2.0*GRID_SPACING)]

After plan 19.3-05, `GRID_DEPTH_RANGE[0]` IS the derived clearance floor and
`GRID_DEPTH_RANGE[1] == 2.0`.

D-19.3-07's arithmetic (do not re-derive it, use it):
  baseline today   standoff 0.069  min depth 1.100  floor 1.199  violates by  99 mm
  half_scale today standoff 0.0345 min depth 1.0655 floor 1.199  violates by 133 mm
  half_scale after the floor moves  min depth 1.115 floor 1.199  STILL violates by 84 mm
</interfaces>

**Locked:** the board's `square_size` is NOT scaled (D-19.3-08). The board-to-surface standoff
stops scaling, which is defensible — standoff is set by the tank and the target, not by rig size.
`xy_extent` and `spacing` keep scaling (review M2's ratio argument is untouched).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Anchor the scale axis at the derived clearance floor</name>
  <read_first>
    - experiments/e6_generalization_sweep.py lines 100-215 (the axis block, SCALE_AXIS_VALUES, STATUS_VALUES, E6_COLUMNS)
    - experiments/e4_benchmark_grid.py (the derived GRID_DEPTH_RANGE from plan 05)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md (D-19.3-07 in full)
  </read_first>
  <files>experiments/e6_generalization_sweep.py</files>
  <behavior>
    - `_scaled_depth_range(f)[0]` equals the derived floor for every `f` in {0.5, 1.0, 2.0} --
      the floor is FIXED, only the extent above it scales.
    - `_scaled_depth_range(0.5)[1] - floor` equals half of `_scaled_depth_range(1.0)[1] - floor`.
    - `_scaled_depth_range(1.0)` equals `GRID_DEPTH_RANGE` exactly (the baseline is unchanged).
    - `_scaled_depth_range(f)[0] >= floor` for every `f >= 0`, by construction.
  </behavior>
  <action>
    Replace `_BASELINE_DEPTH_BELOW_WATER` with a floor-relative baseline extent, computed by
    importing and deriving from `e4_benchmark_grid`'s constants rather than restating absolute
    numbers — the same import-and-derive pattern this module already uses. The floor is
    `GRID_DEPTH_RANGE[0]` after plan 05, but derive it through `board_clearance_floor` (or through
    an explicitly named constant e4 exports) so the coupling is visible and cannot silently break
    if e4's constant moves.

    Anchor on `max(water_zs)`, not on the nominal `GRID_HEIGHT_ABOVE_WATER = 1.031`. D-19.3-07
    calls this out specifically: interface tilt puts the binding constraint 5.7 mm deeper, and that
    5.7 mm does not scale, so anchoring on the nominal constant leaves `half_scale` about 3 mm short
    even under a corrected definition. Plan 05 already resolved the same question for
    `GRID_DEPTH_RANGE`; reuse its answer rather than re-solving it.

    Rewrite `_scaled_depth_range` to `(floor, floor + factor * baseline_extent_above_floor)` and
    keep `xy_extent` and `spacing` scaling exactly as they are today.

    Do NOT change `SCALE_AXIS_VALUES`' three labels or their factors. Do NOT scale `square_size`.
    Do NOT raise the floor to make `half_scale` fit -- the axis changes, not the floor, and
    certainly not `margin_factor`.
  </action>
  <acceptance_criteria>
    - Behavior assertion: `_scaled_depth_range(0.5)[0] == _scaled_depth_range(2.0)[0] == GRID_DEPTH_RANGE[0]`.
    - Behavior assertion: `_scaled_depth_range(1.0) == GRID_DEPTH_RANGE`.
    - Source assertion: `grep -c "GRID_HEIGHT_ABOVE_WATER + factor" experiments/e6_generalization_sweep.py` returns 0.
    - Source assertion: the module contains no new absolute depth literal -- every number in the
      axis block derives from an imported e4 constant.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_e6.py -q</automated>
  </verify>
  <done>The scale axis anchors at the derived floor with a fixed floor and a scaled extent; the baseline is bit-identical to GRID_DEPTH_RANGE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove every scale value builds a legal scenario, and correct the axis prose</name>
  <read_first>
    - experiments/e6_generalization_sweep.py lines 105-130 (the axis comment block whose claim changes)
    - tests/unit/test_experiments_e6.py (existing axis tests and naming conventions)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-VALIDATION.md (the `scale_axis_legal` selector)
  </read_first>
  <files>experiments/e6_generalization_sweep.py, tests/unit/test_experiments_e6.py</files>
  <behavior>
    - For every entry in `SCALE_AXIS_VALUES`, `build_grid_scenario` constructs without raising at
      PRODUCTION frame count (100 baseline), and no board corner in any frame is at or above
      `max(water_zs)`.
    - The same holds for every entry in `LAYOUT_AXIS_VALUES` and every other configured axis, so a
      legality regression on any axis is caught, not only on the one that changed.
  </behavior>
  <action>
    Add a test whose name contains `scale_axis_legal` implementing the behaviors above, at
    production frame counts (anti-pattern #4: every geometry variant converges at 6 frames with the
    bug present; the failure needs ~50). Construction only -- do not calibrate.

    Correct the axis comment block (lines ~105-130). Its current claim, that this axis measures
    "does accuracy hold as the whole rig/tank scales up or down together", stops being exactly true
    once the board-to-surface standoff no longer scales. Rewrite it to describe what the axis now
    measures: the working-volume depth EXTENT above a fixed standoff scales together with
    `xy_extent` and `spacing`, while the standoff and the board stay fixed. State why -- standoff is
    set by the tank and the target, not by rig size; the board does not shrink with the tank -- and
    reference D-19.3-07. Leaving the old prose in place would be a documented-but-false claim,
    which CONTEXT.md names explicitly as unacceptable.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_experiments_e6.py -k scale_axis_legal -q` passes.
    - Source assertion: `grep -c "scales up or down together" experiments/e6_generalization_sweep.py` returns 0.
    - Source assertion: the rewritten comment block cites `D-19.3-07`.
    - The legality test uses production `n_frames`, not a smoke value -- assert the literal in the
      test matches the sweep's configured baseline frame count rather than hardcoding a small number.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_e6.py -q</automated>
  </verify>
  <done>All three scale values (and every other axis value) build legal, non-protruding scenarios at production frame count, and the axis prose describes what the axis actually measures.</done>
</task>

</tasks>

<verification>
- `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_e4.py tests/unit/test_experiments_e6.py -q` -> 0 failures, UNFILTERED.
- Confirm by direct evaluation that `_scaled_depth_range(0.5)[0]`, `(1.0)[0]` and `(2.0)[0]` are all
  equal to the derived floor.
- Security: no packages installed; `security_enforcement` N/A per `19.3-RESEARCH.md` Security Domain.
</verification>

<success_criteria>
- The scale axis anchors at the derived floor; every scale value is legal by construction.
- The baseline scale value reproduces `GRID_DEPTH_RANGE` exactly.
- The axis prose matches the axis's new meaning and cites D-19.3-07.
- `square_size`, `margin_factor` and the clearance floor are all unchanged by this plan.
</success_criteria>

<output>
Create `.planning/phases/19.3-scenario-geometry-and-convergence/19.3-06-SUMMARY.md` when done.
Record: the three resolved `SCALE_AXIS_VALUES` depth ranges and the before/after axis prose.
</output>
