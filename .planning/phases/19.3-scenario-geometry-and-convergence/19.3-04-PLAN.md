---
phase: 19.3-scenario-geometry-and-convergence
plan: 04
type: execute
wave: 2
depends_on: ["19.3-01"]
files_modified:
  - src/aquacal/datasets/synthetic.py
  - tests/unit/test_datasets.py
  - tests/unit/test_datasets_pipelines.py
  - docs/tutorials/02_synthetic_validation.ipynb
autonomous: true
requirements: [GEOM-02]
must_haves:
  truths:
    - "generate_camera_array's default height_above_water is the real-rig standoff, not 0.15 m (D-19.3-09)"
    - "create_scenario('ideal') and create_scenario('minimal') both construct without raising and place their cameras at the real-rig standoff"
    - "All three presets share one unchanged default_board -- board angular size is never a cross-scenario confound (D-19.3-08)"
    - "The library cannot construct a mis-framed rig by accident"
  artifacts:
    - path: "src/aquacal/datasets/synthetic.py"
      provides: "real-rig standoff as the library default and in both presets"
      contains: "height_above_water: float = WATER_Z"
  key_links:
    - from: "src/aquacal/datasets/synthetic.py::create_scenario"
      to: "board_clearance_floor"
      via: "depth_range=None on every preset resolves to the derived floor"
      pattern: "depth_range=None"
---

<objective>
Finish the real-rig standoff into the library: `generate_camera_array`'s default and both
`create_scenario` presets move off 0.15 m, and every preset's `depth_range` becomes derived rather
than a shallow literal.

Purpose: D-29 moved the standoff in the experiment layer only. The library still defaults to a
0.15 m rig framed for a board that overfills its frame ~4x, so the library can still construct a
mis-framed scenario by accident (D-19.3-09).

Output: one library default and two presets migrated, frozen anchors deliberately regenerated,
and a verified statement of exactly which numbers moved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-RESEARCH.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-01-SUMMARY.md
@src/aquacal/datasets/synthetic.py

<interfaces>
Sites to change (verified line numbers, pre-plan-01):

  synthetic.py:136  generate_camera_array(..., height_above_water: float = 0.15, ...)
  synthetic.py:759  create_scenario("ideal")     height_above_water=0.15, depth_range=(0.25, 0.45), xy_extent=0.08
  synthetic.py:791  create_scenario("minimal")   height_above_water=0.15, depth_range=(0.25, 0.40), xy_extent=0.06
  synthetic.py:818  create_scenario("realistic") -> generate_real_rig_array() (already 1.031),
                    generate_real_rig_trajectory(depth_range=(1.1, 2.0))
  synthetic.py      WATER_Z = 1.031   # FROZEN, do not re-derive

From plan 19.3-01 (already landed in this wave's base):
  board_clearance_floor(board, water_zs, rotation_range_deg, margin_factor=0.1) -> float
  generate_board_trajectory(..., *, board, depth_range=None, ...)
  generate_real_rig_trajectory(..., *, board, depth_range=None, water_zs=None, ...)
</interfaces>

**Verified blast radius (D-19.3-10) — the preset change itself moves ZERO publishable numbers:**
`ideal` is used only by `e1_refractive_comparison.py:477` under `--smoke` (temp dir); `minimal`
only by `e7_interface_ablation.py:192` under `--smoke`. `realistic` (E1 and E7 production) keeps
its 1.031 standoff untouched — but its numbers DO move, via plan 01's `depth_range` floor, and
that is expected and accounted for.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the default standoff and both presets onto the real-rig geometry</name>
  <read_first>
    - src/aquacal/datasets/synthetic.py lines 130-180 (generate_camera_array) and 735-840 (create_scenario)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md (D-19.3-08, D-19.3-09, D-19.3-10)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-01-SUMMARY.md (the derived floors actually computed)
  </read_first>
  <files>src/aquacal/datasets/synthetic.py, tests/unit/test_datasets.py</files>
  <behavior>
    - inspect.signature(generate_camera_array).parameters["height_above_water"].default equals WATER_Z.
    - create_scenario("ideal"), create_scenario("minimal") and create_scenario("realistic") each
      construct without raising.
    - For all three presets, every water_zs value is greater than 1.0 -- no 0.15 m rig survives.
    - For all three presets, no board corner in any frame is at or above max(water_zs).
    - All three presets return equal board_config field values (D-19.3-08: one shared board).
  </behavior>
  <action>
    Change `generate_camera_array`'s `height_above_water` default from `0.15` to the module's frozen
    `WATER_Z`, referencing it by name rather than restating the literal, and update its docstring.
    Do not touch `height_variation`.

    In `create_scenario("ideal")` and `create_scenario("minimal")`, drop the explicit
    `height_above_water=0.15` so both fall through to the corrected default, and pass
    `depth_range=None` so each preset derives its own floor. Do the same for
    `create_scenario("realistic")`'s `generate_real_rig_trajectory` call. Thread
    `board=default_board` into all three trajectory calls -- plan 01 made it required.

    Keep `xy_extent` at its current per-preset values (0.08 ideal, 0.06 minimal). CONTEXT.md raises
    D-28-style footprint derivation for these two as an open question; deriving it here would
    smuggle an observability change in alongside a geometry fix, which is the same reasoning
    D-19.3-03 used to reject unifying the tilt ranges. Add a one-line comment at each site saying
    exactly that, and record it in the SUMMARY as a deliberate non-change with the question still open.

    `default_board` stays shared and byte-identical across all three presets. Do not vary
    `square_size` per preset or per anything (D-19.3-08 is locked and its reasoning is not to be
    re-litigated: rig spacing, camera count and layout do not enter `f * board_extent / standoff`).

    Update the preset descriptions so they no longer imply a shallow tank.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_datasets.py -k "preset or scenario" -q` passes.
    - Source assertion: `grep -v '^ *#' src/aquacal/datasets/synthetic.py | grep -c "height_above_water=0.15"` returns 0.
    - Behavior assertion: for each of the three presets, min(scenario.water_zs.values()) is greater than 1.0.
    - Behavior assertion: a test whose name contains `presets_share_board` proves all three
      board_config values are equal field-for-field.
    - Behavior assertion: a test proves no corner is at or above max(water_zs) for any frame of any preset.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_datasets.py -q</automated>
  </verify>
  <done>The library default and both presets sit at the real-rig standoff, every preset derives its own legal depth range, and one shared board is proven across all three.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the affected frozen anchors and fix the dependent tests</name>
  <read_first>
    - tests/unit/test_datasets.py (the `ideal` frozen anchor; note line ~256 passes height_above_water=0.15 explicitly)
    - tests/unit/test_datasets_pipelines.py
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-RESEARCH.md (section "Preset/default migration" fallout table)
  </read_first>
  <files>tests/unit/test_datasets.py, tests/unit/test_datasets_pipelines.py</files>
  <action>
    Regenerate every frozen anchor that legitimately moves: the `ideal` preset anchor (this is its
    SECOND deliberate regeneration -- the first was under D-27 at `4ed8259`, so expect it and say
    so, rather than treating it as alarming), plus any `minimal` or `realistic` anchors.

    The test at ~line 256 passes `height_above_water=0.15` explicitly. Determine whether it asserts
    the PARAMETER's behavior -- in which case keep the explicit 0.15, since passing a shallow
    standoff must still work and only the DEFAULT moved -- or asserts a preset's default, in which
    case update it. State which, and why, in the SUMMARY.

    Record every regenerated anchor in the SUMMARY: what it was, what it is now, and the one-line
    cause. Frozen anchors exist to catch unintended movement; this is one of the rare cases where
    movement is intended, and the only thing distinguishing the two is that the regeneration is
    written down.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_datasets.py tests/unit/test_datasets_pipelines.py -q` passes with 0 failures, run UNFILTERED.
    - Every regenerated anchor appears in the SUMMARY with before/after values and a cause.
    - Source assertion: the diff introduces no new `xfail`, `skip` or deleted anchor assertion --
      no test was weakened to make the suite pass.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_datasets.py tests/unit/test_datasets_pipelines.py -q</automated>
  </verify>
  <done>All affected anchors are regenerated deliberately with the regeneration recorded; no test was weakened to pass.</done>
</task>

<task type="auto">
  <name>Task 3: Repair the tutorial notebook's latent break and hand E1's smoke geometry to plan 07</name>
  <read_first>
    - docs/tutorials/02_synthetic_validation.ipynb (the generator import near line 129 and every depth_range literal)
    - experiments/e1_refractive_comparison.py lines 470-500 (`smoke_depths = [0.30]`)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-RESEARCH.md (the docs/ table)
  </read_first>
  <files>docs/tutorials/02_synthetic_validation.ipynb</files>
  <action>
    The notebook imports `generate_real_rig_trajectory`. It is not re-executed by the docs build
    (`nbsphinx_execute = "never"`), so a broken call is a silent latent break rather than a CI
    failure. Find every generator call in the notebook's SOURCE cells and update it to pass `board=`
    and either `None` or a legal `depth_range`. Do NOT re-execute the notebook and do NOT regenerate
    its output cells -- notebook re-execution belongs to Phase 21's DATA-03.

    `experiments/e1_refractive_comparison.py`'s `smoke_depths = [0.30]` is tied to the old shallow
    `ideal` geometry and must move with it. That file belongs to plan 19.3-07, so do NOT edit it
    here. Confirm the break, then hand plan 07 the exact line number and the replacement depth,
    derived from the new `ideal` preset's legal range, via the SUMMARY.
  </action>
  <acceptance_criteria>
    - Every generator call in the notebook's code cells passes `board=`.
    - No `depth_range` literal remaining in the notebook has a minimum below the derived floor.
    - `git diff --stat docs/tutorials/02_synthetic_validation.ipynb` shows source-cell edits only;
      no output cell changed.
    - The SUMMARY names E1's `smoke_depths` line number and the replacement value for plan 07.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/ -m "not slow" -q -k "datasets or scenario"</automated>
  </verify>
  <done>The notebook's source cells construct legal scenarios; E1's smoke-depth fix is specified and handed to plan 07.</done>
</task>

</tasks>

<verification>
- `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_datasets.py tests/unit/test_datasets_pipelines.py -q` -> 0 failures, run UNFILTERED.
- `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/ -m "not slow" -q` -> the only remaining failures are `experiments/` call sites owned by plans 05 and 06.
- Confirm by direct construction that all three presets build and no corner protrudes.
- Security: no packages installed; `security_enforcement` N/A per `19.3-RESEARCH.md` Security Domain.
</verification>

<success_criteria>
- `generate_camera_array` defaults to the real-rig standoff; neither preset passes 0.15 m.
- Every preset derives its own `depth_range` floor.
- One shared, unchanged `default_board` across all three presets, proven by test.
- Regenerated anchors are recorded with before/after values and causes.
- `WATER_Z` and every `square_size` are unchanged.
</success_criteria>

<output>
Create `.planning/phases/19.3-scenario-geometry-and-convergence/19.3-04-SUMMARY.md` when done.
Record: each preset's newly derived depth range, every regenerated anchor's before/after, the
deliberate `xy_extent` non-change, and E1's `smoke_depths` handoff for plan 07.
</output>
