---
phase: 19.3-scenario-geometry-and-convergence
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/archive/
autonomous: true
requirements: [GEOM-06]
must_haves:
  truths:
    - "Every pre-fix artifact for E1/E4/E5/E6/E7 is either copied into experiments/archive/ or reachable by a recorded `git show <sha>:<path>` pointer (D-19.3-15)"
    - "Each archive directory records the exact commit that produced its artifacts, taken from that experiment's own provenance sidecar and not from current HEAD"
    - "No archive lands under experiments/results/, which is globbed by the live-provenance test at collection time"
  artifacts:
    - path: "experiments/archive/e6-2026-08-02-pre-depth-fix/README.md"
      provides: "provenance, what is copied vs retrievable, how to diff"
    - path: "experiments/archive/README.md"
      provides: "index of every archive generation and why each exists"
  key_links:
    - from: "experiments/archive/*-pre-depth-fix/README.md"
      to: "the producing commit sha"
      via: "git show pointer"
      pattern: "git show [0-9a-f]{7,40}:"
---

<objective>
Archive the pre-depth-fix artifacts of all five experiments before anything overwrites them, using
the convention already established by `experiments/archive/e2-2026-07-30-pre-pnp-guard/`.

Purpose: five experiments move at once in this phase. That is precisely the point at which git
archaeology stops being a sufficient audit trail, because a reader diffing the manuscript's
numbers has no single artifact to diff against (D-19.3-15).

Output: five archive directories plus an index, each carrying its own provenance and rationale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-CONTEXT.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-RESEARCH.md
@experiments/archive/e2-2026-07-30-pre-pnp-guard/README.md

<interfaces>
Live pre-fix artifacts currently in `experiments/results/` (all git-tracked, none ignored):

  E1: exp1_parameter_errors.csv, exp2_depth_generalization.csv, exp3_xy_vs_z_anisotropy.csv,
      e1_benchmark_refractive.json, e1_benchmark_nonrefractive.json
  E4: benchmark_grid.csv, benchmark_grid.tex, e4_cells/
  E5: index_sensitivity.csv, e5_provenance.json
  E6: generalization_sweep.csv, e6_configs/, e6_provenance.json
  E7: interface_ablation.csv, interface_ablation_conditioning.json,
      e7_benchmark_{shared,percamera}_{fixed,refined}.json, e7_trace_*.csv

Pre-commit hooks that force a partial copy: `check-added-large-files`, `detect-secrets`.
The e2 precedent copied only the small manuscript-facing files and pointed at the rest with
`git show <sha>:<path>`.
</interfaces>

**Do not** place any archive under `experiments/results/` --
`tests/unit/test_experiments_provenance.py` globs that directory at collection time and would
sweep an archived copy into the live-provenance gate.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive the five experiments' pre-fix artifacts</name>
  <read_first>
    - experiments/archive/e2-2026-07-30-pre-pnp-guard/README.md (the template, in full)
    - experiments/results/e5_provenance.json, e6_provenance.json, e3_provenance.json (the git_sha fields)
    - tests/unit/test_experiments_provenance.py (confirm what it globs)
  </read_first>
  <files>experiments/archive/e1-2026-08-02-pre-depth-fix/, experiments/archive/e4-2026-08-02-pre-depth-fix/, experiments/archive/e5-2026-08-02-pre-depth-fix/, experiments/archive/e6-2026-08-02-pre-depth-fix/, experiments/archive/e7-2026-08-02-pre-depth-fix/</files>
  <action>
    Create one directory per experiment named `e{N}-2026-08-02-pre-depth-fix`. Into each, copy the
    small manuscript-facing artifacts byte-for-byte. For anything the pre-commit hooks reject
    (large CSVs, `e4_cells/`, `e6_configs/`), record a `git show <sha>:<path>` pointer in that
    directory's README instead of copying.

    The recorded `<sha>` is the commit that PRODUCED the artifacts, read from that experiment's own
    provenance sidecar (`e5_provenance.json`, `e6_provenance.json`, the `git_sha` field in each
    `benchmark*.json`), NOT from current HEAD. Where an experiment has no sidecar, use
    `git log -1 --format=%H -- <artifact path>` and say so explicitly in the README.

    Each README follows the e2 precedent's five sections: why it exists (the depth-clearance defect
    and what it invalidated), provenance (exact producing commit, run date), what is copied vs what
    is retrievable, why it is not under `experiments/results/`, and how to use it (diff a
    regenerated artifact against the archived copy; only the expected deltas are legitimate).

    In the "why it exists" section, state the defect once and precisely: board corners protruded
    through the water surface in every synthetic scenario, which put the residual on the
    refractive/pinhole hinge and made first-order optimality unreadable as a convergence measure.
    Do NOT claim accuracy was wrong -- the evidence says it was not.
  </action>
  <acceptance_criteria>
    - `ls experiments/archive/` shows five new `*-2026-08-02-pre-depth-fix` directories plus the existing e2 one.
    - Each new directory contains a `README.md` with all five sections.
    - `grep -lE "git show [0-9a-f]{7,40}:" experiments/archive/*-pre-depth-fix/README.md` lists every directory that skipped a large file.
    - `git status --porcelain experiments/results/` shows no modification -- the originals are untouched.
    - `pre-commit run --files experiments/archive/**/*` (or the commit itself) passes without a large-file or secrets rejection.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_provenance.py -q</automated>
  </verify>
  <done>Five archive directories exist with e2-shaped READMEs; every uncopied artifact has a recorded git pointer; the live-provenance test still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the archive index</name>
  <read_first>
    - experiments/archive/ (the directory listing after Task 1)
    - experiments/README.md (heading style and cross-reference conventions)
  </read_first>
  <files>experiments/archive/README.md</files>
  <action>
    Create `experiments/archive/README.md` as an index: one table row per archive generation
    (directory, experiments covered, date, the defect it precedes, the producing commit), plus a
    short statement of the archive convention itself so the next person does not have to reverse
    engineer it from the e2 directory. Link it from `experiments/README.md` if that file has a
    natural place for it; if not, say so in the SUMMARY rather than forcing an edit.
  </action>
  <acceptance_criteria>
    - `experiments/archive/README.md` exists and its table lists six rows (e2 plus the five new ones).
    - Every directory under `experiments/archive/` appears in the index -- verified by comparing
      `ls -d experiments/archive/*/` against the table.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -c "import pathlib,sys; idx=pathlib.Path('experiments/archive/README.md').read_text(encoding='utf-8'); missing=[d.name for d in pathlib.Path('experiments/archive').iterdir() if d.is_dir() and d.name not in idx]; sys.exit(1 if missing else 0)"</automated>
  </verify>
  <done>Every archive directory is indexed and the convention is documented in one place.</done>
</task>

</tasks>

<verification>
- `~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_provenance.py -q` -> 0 failures.
- `git status --porcelain experiments/results/` -> empty (originals untouched).
- Security: no packages installed; `security_enforcement` N/A per `19.3-RESEARCH.md` Security Domain.
</verification>

<success_criteria>
- Five pre-depth-fix archives exist, each with its own provenance README, before any re-run starts.
- No archive sits under `experiments/results/`.
- The index lists every archive generation.
</success_criteria>

<output>
Create `.planning/phases/19.3-scenario-geometry-and-convergence/19.3-03-SUMMARY.md` when done.
Record: which artifacts were copied vs pointed at, and each experiment's producing commit sha.
</output>
