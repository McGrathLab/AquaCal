---
phase: 19.2-experiment-execution-and-provenance
plan: 24
type: execute
wave: 6
gap_closure: true
depends_on: ["19.2-21", "19.2-22", "19.2-23"]
files_modified:
  - tests/unit/test_experiments_provenance.py
  - tests/unit/test_experiments_e3.py
autonomous: true
requirements: [EXP-11]

must_haves:
  truths:
    - "The mechanical EXP-11 gate asserts all FOUR fields -- seed, AquaCal version, git SHA, environment -- for E5 and E6, so a CSV that carries only a seed cannot pass. Today `test_new_phase_csvs_carry_a_seed_column` checks ONE of four while its docstring reframes the requirement as if it enforced all of it"
    - "The twelve `e6_configs/*.json` checkpoints and E5's and E6's new sidecars are inside the gate, not invisible to it -- they now carry `schema_version`, so the existing environment and seed checks apply to them by construction"
    - "WR-11: a missing file skips only its own case. Two tests currently call `pytest.skip()` from inside a `for` loop, so one absent file silently skips the remaining two or five -- the exact 'silently weakened gate' pattern the module docstring says it exists to prevent"
    - "The `SELF_DESCRIBING_JSON` comment's claim to list 'the full set of exceptions in one place' is made true or corrected: `calibration.json` also lacks `schema_version`, escapes every check, and is absent from the set"
    - "[D-35] E1 and E7 are REGENERATED on the final tree, not `--check`ed against stale baselines: 7e0cb90's PnP guard moved E1 across three CSVs (159/44/48 cells) and E7 in 321 cells, so `--check` would fail on a non-defect. Regeneration is the stronger evidence this plan already accepts for E3/E5. DONE 2026-07-31/08-01: E1 at seeds 42-46, E7 at seeds 42-51 (TEN, not five)"
    - "[D-36 — AMENDED, see the banner] E7's cross-seed spread is reported AND its criterion is the PAIRED per-seed difference, not the marginal spread. ADJUDICATED at ten seeds: shared beats per-camera 9/10 in both pairings, p = 0.0107, mean +1.0 mm, and the difference DOES cross zero. The manuscript may state a qualified tendency with its exception count; it may NOT state an unqualified directional claim, and it may NOT quote absolute refined-arm values (0.450-11.550 mm). Recorded in MF-05"
    - "The one red test on `main` is fixed: `test_experiments_e3.py::TestBuildNewtonIterationsDfBothLoops::test_scalar_rows_unchanged_columns_and_semantics_vs_committed_baseline` anchors to the LIVE committed `newton_iterations.csv` as a 'pre-D-32 baseline', which plan 23 was written to regenerate. The determinism claim it guards is INTACT (verified: 13 -> 26 rows, exactly one added `loop` column, scalar rows reproduce committed values exactly) -- so the fix is to stop anchoring a test to a mutable artifact, NOT to relax the assertion"
    - "The `SEEDLESS_LEGACY_RECORDS` carve-out is NOT part of the problem and must not be widened: six members, each asserted to exist AND to still genuinely lack a seed so the exemption self-destructs on regeneration, cardinality pinned, E2 excluded, all six still held to the environment check"
  artifacts:
    - path: "tests/unit/test_experiments_provenance.py"
      provides: "an EXP-11 gate that enforces four fields for every committed result"
      contains: "REQUIRED_ENVIRONMENT_KEYS"
  key_links:
    - from: "tests/unit/test_experiments_provenance.py"
      to: "experiments/results/e5_provenance.json, e6_provenance.json, e6_configs/*.json"
      via: "schema-versioned discovery plus explicit per-CSV record mapping"
      pattern: "CSV_TO_RECORD"
---

<objective>
> ## ⚠ AMENDED 2026-07-31 under D-35 — Task 2's premise no longer holds
>
> This plan's Task 2 rests on E1 and E7 being *"the only two experiments this phase neither re-runs
> nor regenerates"*, and therefore on `--check` reproduction being the only available evidence for
> them. **Both halves of that are now false.**
>
> `7e0cb90`'s degenerate-PnP guard is deliberately **not inert**, and the recorded blast radius
> measured **E1 moving across three CSVs (159 / 44 / 48 cells)** and **E7 moving in 321 cells**. So
> `--check` would not exit 0, and the plan would halt on a non-defect. Worse, halting there invites
> exactly the response the phase has already been bitten by: regenerating a baseline or widening a
> tolerance to make a red gate green.
>
> **They are now REGENERATED, not checked.** The combined plan 19.2-24 + D-36 sweep launched
> 2026-07-31 (`98b17cb`) re-ran E7 and E1 at seeds 42–46, which is the stronger evidence this plan
> itself says regeneration provides ("E3 and E5 need no re-assertion — wave 5 regenerated them,
> which is a stronger check than `--check`"). The same argument now applies to E1 and E7.
>
> **Task 2 is replaced, not deleted** — see its own `[D-35]` banner. Task 1, the provenance-gate
> widening, is unaffected and remains this plan's substance.

> ## ⚠ AMENDED AGAIN 2026-08-01 — the D-36 adjudication is DONE, and its criterion CHANGED
>
> **Do not re-adjudicate D-36 from this plan's original wording. Read MF-05 and use its result.**
>
> **Three corrections to the text below:**
>
> 1. **"five seeds" is wrong for E7.** E7 ran **ten** (42–51); seeds 47–51 were added 2026-08-01.
>    E1 remains five (42–46).
>
> 2. **D-36's criterion was AMENDED by the user on 2026-07-31, from the marginal spread to the
>    PAIRED per-seed difference.** The original wording — *"if the gap is smaller than the spread,
>    E7 does not support a directional conclusion"* — is superseded. Applied literally it would
>    now produce the WRONG answer: the gap (~1.0 mm) IS smaller than the refined marginal spread
>    (11 mm), so an executor following it verbatim would conclude E7 supports nothing, contradicting
>    the adjudicated result. The seeds are paired — one seed builds one scenario and both arms are
>    evaluated on it — so the marginal spread carries scenario difficulty common to both arms.
>
> 3. **The adjudication is COMPLETE and unanimity BROKE.** At ten seeds, shared beats per-camera
>    **9/10** (not 10/10) in both pairings; sign test p = 0.0107; mean paired difference ~+1.0 mm;
>    **the difference does cross zero.** `fixed`'s exception (seed 49, −0.020 mm) is a tie;
>    **`refined`'s (seed 48, −1.000 mm) is a full-effect-size reversal.** Two earlier claims are
>    WITHDRAWN: that shared is better *always*, and that this retires the Phase 19.1 worry about
>    per-camera beating shared — seed 48 shows exactly that.
>
> **Task 2's remaining job is therefore to RECORD this, not to recompute it.** MF-05 already
> carries the numbers, the withdrawal, and the may/may-not list.
>
> **One task is ADDED: fix the red test.** `test_experiments_e3.py::
> TestBuildNewtonIterationsDfBothLoops::test_scalar_rows_unchanged_columns_and_semantics_vs_committed_baseline`
> fails on `main` (suite: 1140 passed, 1 failed). It was orphaned — no wave-6 plan owned its file —
> so this plan adopts it. **The determinism claim it guards is intact and must stay asserted**;
> the defect is that it anchors to a *mutable* artifact. Do not relax the assertion to make it pass.

Widen the mechanical EXP-11 gate so the failure it missed cannot recur, and **[D-35]** adjudicate
the regenerated E1 and E7 results — including E7's cross-seed spread (D-36) — against the code that
ships.

Purpose: EXP-11. The gap that failed verification was not subtle in the artifacts — neither E5 nor
E6 imported any provenance helper — but it *was* invisible to the gate, because the one test covering
those two files checks a `seed` column and nothing else, and both its docstring and the README narrate
that narrow check in language that reads like full coverage. **This is exactly the failure mode the
phase was meant to eliminate: a provenance claim that is true of most artifacts, asserted as if true
of all.**

Waves 3–5 regenerated E4's, E6's, E5's and E3's artifacts with real provenance. This plan makes the
gate enforce what those artifacts now carry, so the next artifact committed without provenance fails
a test rather than a verification pass six weeks later.

Output: a provenance suite that enforces four fields per artifact and cannot be weakened by an absent
file; **[D-35]** the adjudicated regenerations of E1 (five seeds) and E7 (ten seeds) recorded with
E7's spread and its 9/10 outcome; and a green suite, with the orphaned e3 anchor test fixed at its
cause rather than by relaxing it.

**Do not widen `SEEDLESS_LEGACY_RECORDS`.** It was audited and holds up: exactly six members, all
Phase-19.1 records that genuinely predate `solver_config["seed"]`, each asserted to exist *and* to
still lack a seed so the exemption self-destructs the moment one is regenerated, cardinality pinned,
E2 explicitly excluded, and all six still held to the environment check. The carve-out is one field
wide, not blanket. Adding a member to make a new test pass would convert a defended exemption into
the blanket one it was designed not to be.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** fork from the merged result of wave 5, with every regenerated artifact committed.
Verify `git status --porcelain` is empty and
`python -m pytest tests/unit/test_experiments_provenance.py -q` exits 0 before starting.
**PYTHONPATH:** in a worktree, `export PYTHONPATH="$(pwd)/src"`.
**[AMENDED 2026-08-01] Task 2 runs NO `--check` invocations.** The earlier text here promised
"two `--check` invocations totalling roughly half an hour"; D-35 removed them (they would fail on
a non-defect) and the regenerations replaced them. Task 2 is now desk work against artifacts that
already exist, so this plan needs no box time at all and may run alongside plan 19.2-25.

**Suite is RED at the start of this plan** — 1140 passed, 1 failed — and Task 3 fixes it. Do not
treat the pre-existing failure as something your changes caused; do confirm it is the only one.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-GAP-CONTEXT.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-VERIFICATION.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-REVIEW.md

<interfaces>
`tests/unit/test_experiments_provenance.py` (line numbers at EXPECTED_BASE):
- `RESULTS_DIR = pathlib.Path("experiments/results")` (27) — **cwd-relative**, and the module's
  `pytestmark` skips the ENTIRE EXP-11 gate when it is not resolvable, so the mechanical check can
  vanish from a run without failing it (WR-06).
- `REQUIRED_ENVIRONMENT_KEYS` (34): `aquacal_version`, `git_sha`, `python_version`, `numpy_version`,
  `scipy_version`.
- `SELF_DESCRIBING_JSON` (51): `real_rig_metrics.json`, `interface_ablation_conditioning.json` —
  with a comment claiming it lists the full set of exceptions "in one place". `calibration.json` and
  (before wave 4) the twelve `e6_configs/*.json` also lacked `schema_version` and were absent.
- `SEEDLESS_LEGACY_RECORDS` (75): E1's two and E7's four. Do not modify.
- `CSV_TO_RECORD` (90–143): maps every committed CSV to its provenance record.
  `generalization_sweep.csv` reads "self (own seed column); no benchmark.json backs it";
  `index_sensitivity.csv` reads "self (own seed column); no benchmark.json backs it (E5 has no
  per-row JSON sidecar)". Both are now false — plans 19.2-16 and 19.2-19 gave each a sidecar.
- `_discover_json_files` (146), `_discover_csv_files` (152), `_schema_versioned_json_files` (162),
  `_record_seed` (172), `_pytest_id` (188).
- `TestEnvironmentPresence` (205), `TestE4CellProvenance` (226), `TestE3SidecarProvenance` (239),
  `TestSeedProvenance` (253), `TestCsvProvenanceMap` (334), `TestOneMachineConsistency` (355),
  `TestSelfDescribingJson` (381).
- **WR-11 sites:** `test_new_phase_csvs_carry_a_seed_column` (306–320) and
  `test_e1_and_e7_records_already_comply_on_environment` (322–331) both call `pytest.skip()` from
  inside a `for` loop.
- **Tautology worth fixing:** `test_environment_presence_check_rejects_a_mutated_record` (217–223)
  writes `{"schema_version": 1}` then asserts `"environment" not in record` — a tautology over
  `json.loads` that exercises no project code and cannot fail. Its sibling seed guard (291–304) is
  genuine: it mutates a real E4 cell record and exercises `_record_seed`.
- `TestOneMachineConsistency` (355) covers E4's cells and E3's sidecar only; E5's and E6's new
  sidecars should join it now that they exist.

Plan summaries to read for the exact new artifact names and shapes: `19.2-16-SUMMARY.md` (E6
sidecar + checkpoint key set), `19.2-19-SUMMARY.md` (E5 sidecar key set **and the exact
`CSV_TO_RECORD` narration change it asks for**), `19.2-20-SUMMARY.md` (the new
`newton_iterations.csv` schema).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the gate enforce four fields per artifact, per file, and anchor it to the repo</name>
  <files>tests/unit/test_experiments_provenance.py</files>
  <read_first>
    - `tests/unit/test_experiments_provenance.py` in full (all 395 lines) — every constant, every
      helper, and every test class, so the widening is designed against the whole gate rather than
      bolted onto one test.
    - `19.2-VERIFICATION.md` gap 1 — the four `missing` items and the artifact list, especially the
      note that the gate "enforces one quarter of EXP-11 while reading as if it enforced all of it".
    - `19.2-REVIEW.md` §§ WR-06, WR-11, and the verification report's Anti-Patterns table entries for
      lines 217–223 and 44–63.
    - `19.2-GAP-CONTEXT.md` § D-31, including the paragraph defending `SEEDLESS_LEGACY_RECORDS`.
    - Plans `19.2-16-SUMMARY.md`, `19.2-19-SUMMARY.md`, `19.2-20-SUMMARY.md` — the new artifacts'
      names and key sets.
  </read_first>
  <behavior>
    - Every committed CSV under `experiments/results/` is covered by an assertion that its named
      provenance record exists and carries a seed, an AquaCal version, a git SHA, and an environment
      block — so a seed-only artifact fails.
    - Each file is its own parametrized case: an absent file skips only that case, never its
      siblings.
    - `RESULTS_DIR` resolves from the repository regardless of the process working directory, so the
      gate cannot silently vanish from a run.
    - The `SEEDLESS_LEGACY_RECORDS` carve-out behaves exactly as before: still six members, still
      exempt on seed only, still held to the environment check, still excluding E2.
  </behavior>
  <action>
Replace `test_new_phase_csvs_carry_a_seed_column` with a check that enforces the whole of EXP-11 for
the CSVs it covers: for each, resolve the provenance record named in `CSV_TO_RECORD` and assert seed,
version, git SHA and environment. Rewrite its docstring so it states what it checks and no more — the
present docstring's reframing ("their own `seed` column IS their seed provenance") is half of why the
gap survived review.

Update `CSV_TO_RECORD`'s entries for `generalization_sweep.csv` and `index_sensitivity.csv`, which
now have real sidecars. Plan 19.2-19's summary states the exact narration it asked for; use it.

Parametrize both WR-11 sites over their file lists so a missing file skips only its own case. Prefer
`@pytest.mark.parametrize` with `_pytest_id`-style ids, matching the idiom already used elsewhere in
this module.

Anchor `RESULTS_DIR` to the repository via `Path(__file__).resolve().parents[N]`, keeping the
fresh-clone skip but basing it on the resolved path — so the gate skips only when the directory
genuinely does not exist, not when the process happened to start elsewhere (WR-06).

Bring the twelve `e6_configs/*.json` checkpoints and the two new sidecars into explicit coverage.
They now carry `schema_version`, so `_schema_versioned_json_files()` discovers them and the
environment and seed checks apply automatically — assert that this is actually happening rather than
assuming it: add a test that the discovered set *contains* them by name, so a future write that drops
`schema_version` fails loudly instead of quietly leaving the gate.

Make `SELF_DESCRIBING_JSON`'s comment true. `calibration.json` also carries no `schema_version` and
escapes every check; either add it with a one-line note that its sibling `benchmark.json` covers it
(as the README's provenance table already says), or narrow the comment's claim. Add a test that the
set of `schema_version`-less committed JSON files equals `SELF_DESCRIBING_JSON` exactly, so the claim
is enforced rather than narrated.

Replace the tautological negative control at 217–223 with one that exercises project code: mutate a
real record's `environment` and assert the presence check rejects it, mirroring how the genuine seed
guard at 291–304 mutates a real E4 cell record.

Add `experiments/results/e5_provenance.json` and `e6_provenance.json` to
`TestOneMachineConsistency`'s record set, so every record this phase produced is asserted to report
the same machine (constraint 11).

Do NOT modify `SEEDLESS_LEGACY_RECORDS` or any test that defends it.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_provenance.py -q</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts, for `index_sensitivity.csv` and `generalization_sweep.csv`, that a provenance
      record exists carrying a seed AND an `aquacal_version` AND a `git_sha` AND an environment block
      with every key in `REQUIRED_ENVIRONMENT_KEYS`. Confirm by construction that this test would
      FAIL against the pre-wave-5 artifacts — e.g. run it against a temp copy with the sidecar
      removed — and record that demonstration in the summary.
    - Both WR-11 sites are parametrized: `python -m pytest tests/unit/test_experiments_provenance.py -q --collect-only | grep -c "carry"` shows more than one case for the widened CSV test, and no `pytest.skip()` call remains inside a `for` loop
      (`grep -c "for .*:" -A 5 ...` is not required; assert by reading, and state the confirmation).
    - `python -c "import pathlib, importlib.util, sys; sys.path.insert(0,'tests/unit'); import test_experiments_provenance as t; print(t.RESULTS_DIR); assert t.RESULTS_DIR.is_absolute()"` exits 0 when run from a directory other than the repository root; the directory used is recorded.
    - A test asserts the schema-versioned discovery set contains `e5_provenance.json`,
      `e6_provenance.json` and all twelve `e6_configs/*.json` by name.
    - A test asserts the set of committed JSON files lacking `schema_version` equals
      `SELF_DESCRIBING_JSON` exactly.
    - The replaced negative control exercises project code and fails when the check is disabled;
      the summary states how that was demonstrated.
    - `python -c "import sys; sys.path.insert(0,'tests/unit'); import test_experiments_provenance as t; assert len(t.SEEDLESS_LEGACY_RECORDS)==6 and 'benchmark.json' not in t.SEEDLESS_LEGACY_RECORDS"` exits 0 — the carve-out is unchanged.
    - `python -m pytest tests/unit/test_experiments_provenance.py -q` exits 0; the summary records
      passed/skipped counts and confirms the skips are exactly the six carve-out members.
    - `python -m pytest tests/ -m "not slow" -q` exits 0; new counts recorded.
  </acceptance_criteria>
  <done>A committed artifact carrying only a seed can no longer pass the gate, a missing file weakens only its own case, the gate cannot vanish because of a working directory, and the exceptions list is enforced rather than narrated.</done>
</task>

<task type="auto">
  <name>Task 2: Re-assert E1's and E7's reproduction on the final merged tree</name>
  <files>experiments/results/</files>
  <read_first>
    - Plan `19.2-18-SUMMARY.md` § Task 3 — the four `--check` exit codes recorded there, and the
      tree they were run against.
    - `19.2-GAP-CONTEXT.md` § D-27 "Required gate" item 3, and § "Out of scope" (E1, E2 and E7 are
      not re-run for geometry reasons).
    - `experiments/README.md` § 2 — measured runtimes: E1 ~20–25 min, E7 ~7 min.
  </read_first>
  <action>
Run `python -u -m experiments.e1_refractive_comparison --check` and
`python -u -m experiments.e7_interface_ablation --check` from the repository root, in the background,
with logs outside the repository. Record each exit code and each printed report message verbatim.

> **[D-35] DO NOT RUN THE TWO `--check` COMMANDS ABOVE.** They are retained so the reasoning is
> auditable, and they would now fail on a non-defect: `7e0cb90`'s PnP guard moved E1 across three
> CSVs (159/44/48 cells) and E7 in 321 cells. Running them produces a red gate whose only honest
> resolutions are the two this phase forbids — regenerate a baseline, or widen a tolerance.

**[D-35] What to do instead: adjudicate the regenerated results.** The plan 19.2-24 + D-36 five-seed
sweep (launched 2026-07-31 at `98b17cb`, artifacts under
`Desktop/Aqua/AquaCal/seed_sweep_19_2/<exp>/seed_<N>/`) regenerated E7 and E1 at seeds 42–46 on the
final tree. That is the same class of evidence this plan already accepts for E3 and E5 — regeneration
rather than reproduction — and it is strictly stronger than `--check` against a stale baseline.

For **E1**: compare seed 42 against the committed CSVs and attribute every moved column to the PnP
guard. **D-19 still stands**: a divergence in a *headline* number HALTS and needs a recorded user
response; a within-tolerance move stays autonomous. E1's noise floor is measured (three seeds; fix
delta 12–105x smaller than seed range), so unlike E5 there IS a band to judge magnitude against —
use it, and say which seeds it came from.

For **E7 (D-36)**: report a **spread across the five seeds**, not a point estimate. Compute, per arm,
the mean and full range of `camera_height_drift_mm`, then answer the question the experiment exists
to answer:

  - If the shared-vs-per-camera gap **exceeds** the spread, E7 supports a directional conclusion.
    State the gap, the spread, and the seeds.
  - If the gap is **smaller than** the spread, **E7 does not support a directional conclusion and the
    manuscript must not state one.** Say that plainly. This is a legitimate, publishable outcome, not
    a failure of the run — the prior single-seed evidence swung 0.483 → 11.550 mm on `shared_refined`
    across three seeds, which is why D-36 exists.
  - Either way, record the per-arm spread in `.planning/MANUSCRIPT-FINDINGS.md`. A directional claim
    that the data cannot support is a manuscript defect whether or not anyone notices it.

**Do not average the spread away.** Reporting only a mean across seeds would reproduce exactly the
single-seed error D-36 was written to stop, with extra steps.

E3 and E5 need no re-assertion — wave 5 regenerated them. E4's and E6's baselines were deliberately
superseded. **E2 must not be run at all** — it was re-run under D-34 and committed at `faa05b3`.

Neither adjudication writes to `experiments/results/` until the results are accepted; confirm with
`git status --porcelain experiments/results/`.
  </action>
  <verify>
    <automated>git status --porcelain experiments/results/</automated>
  </verify>
  <acceptance_criteria>
    - **[D-35]** The two `--check` commands were NOT run; the summary states why (they would fail on a non-defect, and the only resolutions available would be the two this phase forbids).
    - **[D-35]** E1 seed 42 is compared against the committed CSVs, every moved column attributed to the PnP guard, and its magnitude judged against E1's MEASURED noise band with the contributing seeds named. A headline-number divergence HALTED and was escalated with a recorded user response (D-19).
    - **[D-36 — AMENDED 2026-08-01]** E7's per-arm mean and full range of `camera_height_drift_mm` across **all TEN seeds (42–51)** are tabulated in the summary, with the paired per-seed difference stated beside the marginal spread.
    - **[D-36 — AMENDED 2026-08-01]** The summary applies the **paired** criterion, not the marginal one, and records the adjudicated outcome from MF-05 rather than recomputing it: **9/10 in both pairings, p = 0.0107, mean ~+1.0 mm, difference crosses zero.** It states that the manuscript may claim a qualified tendency with its exception count, and may NOT claim an unqualified direction. **Do not conclude "E7 supports nothing" from the gap being smaller than the marginal spread — that is the superseded criterion and it gives the wrong answer here.**
    - **[D-36 — AMENDED 2026-08-01]** The summary records that unanimity BROKE at ten seeds and names the two withdrawn claims: that shared is better always, and that E7 no longer shows per-camera beating shared (seed 48 does).
    - **[D-36]** The per-arm spread is recorded in `.planning/MANUSCRIPT-FINDINGS.md` regardless of which way the answer falls.
    - **[D-36]** No seed's result is averaged away: the summary reports a range, not only a mean.
    - Any seed that failed is recorded with its exit code and log tail; a partial sweep is reported as partial rather than presented as five-seed evidence.
    - `git status --porcelain experiments/results/` is empty unless the results were accepted and committed, in which case the summary says so.
    - The summary states which experiments' evidence now rests on what: E1 and E7 on five-seed regeneration; E3 and E5 on wave 5's regeneration; E4 and E6 on deliberately superseded baselines; E2 on its D-34 re-run at `faa05b3`.
  </acceptance_criteria>
  <done>Every committed experiment result is either regenerated by this phase or mechanically reproduced against the tree that ships, and which is which is written down.</done>
</task>

<task type="auto">
  <name>Task 3: [ADDED 2026-08-01] Fix the orphaned e3 anchor test at its cause</name>
  <files>tests/unit/test_experiments_e3.py</files>
  <read_first>
    - `tests/unit/test_experiments_e3.py` — `TestBuildNewtonIterationsDfBothLoops::test_scalar_rows_unchanged_columns_and_semantics_vs_committed_baseline`.
    - `19.2-23-SUMMARY.md` § the red-test record, and `.continue-here.md`'s red-test section.
    - `19.2-20-SUMMARY.md` — D-32's `NEWTON_COLUMNS` and the scalar/batch row design.
  </read_first>
  <action>
This test fails on `main` and no plan owned its file until this amendment; the phase must not close
red.

**The cause.** The test reads the **live committed** `experiments/results/newton_iterations.csv` as
its "pre-D-32 baseline" and asserts `set(scalar_df.columns) - set(baseline_header) == {"loop"}`.
Plan 19.2-23 was written to regenerate that file — D-32 deliberately changed its schema — so the
committed copy now already contains `loop`, the set difference is empty, and the assertion fails.
The defect is **anchoring a test to a mutable artifact**: the live CSV stopped being "pre-D-32" the
moment a plan regenerated it, and will never be pre-D-32 again.

**What must NOT change.** The determinism claim the test guards is intact and was verified against
the pre-regeneration copy captured before `--force` ran: 13 → 26 rows, exactly one added column
(`loop`), and **the scalar rows reproduce the committed values exactly**. Keep that assertion.
Relaxing or deleting it to reach green is forbidden — it is the determinism check *inside* a
legitimate schema change, which is the only thing standing between "the schema changed" and "the
numbers changed too."

Fix the anchoring, not the assertion. Freeze the expected pre-D-32 header as an explicit literal in
the test (so the anchor cannot drift again), or point it at an archived pre-D-32 copy. State in the
summary which was chosen and why the other was not.

Then confirm the whole fast suite is green — this plan is the phase's closing gate.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e3.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -m "not slow" -q` exits 0, with the count recorded against the 1140-passed/1-failed starting state.
    - The scalar-rows-reproduce-committed-values assertion still exists and still asserts equality; the summary quotes the assertion before and after.
    - The test no longer derives its baseline from a file any plan regenerates; the summary names the chosen anchoring and rejects the alternative explicitly.
    - The summary states that the failure predated this plan and was caused by plan 23's sanctioned regeneration, not by a defect in the data.
  </acceptance_criteria>
  <done>The suite is green, and the test that guarded D-32's determinism still guards it — anchored to something that cannot drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test discovery -> gate coverage | Globs and skip conditions decide what is checked; a file outside them is silently unchecked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.2-94 | Repudiation | The gate is widened in name but a new artifact still escapes it via a glob it does not match | mitigate | Task 1 asserts the discovery set contains the new files BY NAME and that the `schema_version`-less set equals `SELF_DESCRIBING_JSON` exactly. |
| T-19.2-95 | Repudiation | A missing file silently skips a whole test class's remaining coverage and the run reports a skip rather than partial coverage | mitigate | Both WR-11 sites are parametrized per file. |
| T-19.2-96 | Repudiation | The whole EXP-11 gate vanishes from a run because `experiments/results` was not resolvable from the cwd | mitigate | `RESULTS_DIR` is anchored to the repository and asserted absolute from a foreign cwd. |
| T-19.2-97 | Tampering | `SEEDLESS_LEGACY_RECORDS` is widened to make a new assertion pass, converting a defended exemption into a blanket one | mitigate | The objective forbids it; an acceptance criterion pins the membership and the E2 exclusion. |
| T-19.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
1. `python -m pytest tests/unit/test_experiments_provenance.py -q` — exits 0; skips are exactly the six carve-out members.
2. `python -m pytest tests/ -m "not slow" -q` — exits 0. **This is the phase-closing gate: it is RED (1 failed) when this plan starts and MUST be green when it ends.**
3. **[AMENDED 2026-08-01]** ~~Both `--check` invocations in Task 2 exit 0.~~ Withdrawn with the `--check` commands themselves; Task 2 records the completed adjudication instead.
4. `git status --porcelain experiments/results/` — empty. **No experiment is re-run by this plan**; a non-empty result here means something regenerated an artifact that should not have.
</verification>

<success_criteria>
- EXP-11 is enforced mechanically at four fields per artifact, for every committed CSV and record.
- No test in the module can be weakened by one absent file or by a working directory.
- E1's and E7's reproduction is recorded against the shipping tree.
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-24-SUMMARY.md` when done.
Record: how the widened test was demonstrated to fail against a seed-only artifact; the new
provenance-suite passed/skipped counts with confirmation that the skips are exactly the six carve-out
members; the foreign working directory used to prove the anchoring; the two `--check` exit codes and
messages with the tree SHA; and the evidence map naming which experiment's reproduction rests on what.
</output>
