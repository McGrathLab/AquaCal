---
phase: 19.2
slug: experiment-execution-and-provenance
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-28
---

> **`wave_0_complete: false` is accurate, not stale.** Every ❌ W0 item below is a test file
> that plans 19.2-01 through 19.2-05 create during execution. It flips to `true` once wave 1
> merges.

# Phase 19.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (existing — `tests/unit/` one file per source module, `tests/synthetic/` integration) |
| **Config file** | `pyproject.toml` (pytest config; `slow` marker already registered) |
| **Quick run command** | `python -m pytest tests/unit/test_experiments_*.py tests/unit/test_optim_common.py -x` |
| **Full suite command** | `python -m pytest tests/ -m "not slow"` |
| **Estimated runtime** | ~60 s quick · ~5 min full (874 tests green on `main` as of Phase 19.1) |

**Standing hazard — see `[[worktree-editable-install-resolves-to-main]]`.** The editable install
resolves to the main checkout, so any executor working in a git worktree MUST
`export PYTHONPATH="$(pwd)/src"` or its passing tests are meaningless. This bit during both
Phase 18 and Phase 19.

---

## Sampling Rate

- **After every task commit:** the specific new unit-test file for that script.
- **After every plan wave:** `python -m pytest tests/unit/test_experiments_*.py tests/unit/test_optim_common.py -x`
- **Before `/gsd:verify-work`:** `python -m pytest tests/ -m "not slow"` green, **plus** all four
  new `--smoke` invocations exit 0.
- **Max feedback latency:** ~60 s.

**Long-run carve-out.** E2's re-run (~50 min), E4's grid, E5's band, and E6's sweep are NOT part
of any sampling loop. They are one-shot production runs launched with `python -u` in the
background per `CLAUDE.md`. Their correctness is sampled by the schema/CLI unit tests plus
`--smoke`; their *results* are reviewed as artifacts, not as test outcomes.

**Production runs are serialized, one per wave (review H4).** E4's grid (plan 19.2-09, wave 4),
E5's band (plan 19.2-13, wave 5) and E6's sweep (plan 19.2-11, wave 6) each own the machine for
their wave, and 19.2-09 additionally depends on every code plan whose `--smoke` could otherwise
overlap it. E4 publishes wall-clock and peak-memory columns, so a concurrent experiment would make
those columns measure contention. Each production plan carries an explicit quiescence check
recorded at launch and at completion. Do NOT run these in parallel to save calendar time.

---

## Per-Task Verification Map

| Req | Behavior | Test Type | Automated Command | File Exists |
|-----|----------|-----------|-------------------|-------------|
| EXP-07 | Tier-1 declared constants match live source values (9 rows incl. `dense_threshold` and all three Huber `f_scale` sites — D-15/D-18, review M10) | unit | `pytest tests/unit/test_experiments_e3_constants.py -x` | ❌ W0 |
| EXP-07 | Declared table is the single source (D-18): the E3 script's rendered CSV keys equal the test module's declared keys | unit | `pytest tests/unit/test_experiments_e3_constants.py -k declaration -x` | ❌ W0 |
| EXP-07 | Tier-3 covers all six `tab:cpr` configurations, all at `normal_fixed=False` | unit | `pytest tests/unit/test_experiments_e3.py -k cpr_configs -x` | ❌ W0 |
| EXP-07 | Tier-3 emits 12 rows (6 configs x 2 interface modes) with exactly one `copied_from_e2_benchmark` row, sourced from `stage3_intrinsic_pass` | unit + artifact | `pytest tests/unit/test_experiments_e3.py -k cpr -x` | ❌ W0 |
| EXP-07 | `docs/guide/optimizer.md`'s three documented 13x100 P/group configurations (DOCS-01 / D-20) — **not** the `tab:cpr` rows | unit | `pytest tests/unit/test_optim_common.py -k Documented -x` | ✅ existing (`test_optim_common.py`, `_DOCUMENTED_CONFIGS`) |
| EXP-07 | Tier-3 per-camera rows: group count invariant while P grows (D-21) | unit | `pytest tests/unit/test_optim_common.py -k per_camera -x` | ✅ existing |
| EXP-07 | P formula validated against live `pack_params` length (D-22) | unit | `pytest tests/unit/test_experiments_e3.py -k pack_params -x` | ❌ W0 |
| EXP-07 | Newton diagnostic returns iteration count + residual, and the projection path is bit-unchanged (D-19) | unit | `pytest tests/unit/test_refractive_geometry.py -k newton_diagnostic -x` | ❌ W0 |
| EXP-08 | E4 row schema: `status`, `status_reason`, `timing_scope`, `record_source`, accuracy + CPR + element-count columns present (D-02/D-04/D-14/D-15/D-16) | unit (hand-built record fixture, no calibration) | `pytest tests/unit/test_experiments_e4.py -x` | ❌ W0 |
| EXP-08 | All nine declared cells emit a row even when one fails (D-04) | unit (injected failure) | `pytest tests/unit/test_experiments_e4.py -k declared_cells -x` | ❌ W0 |
| EXP-08 | E4 `--smoke` runs the full code path at trivial scale | smoke (CI) | `python -m experiments.e4_benchmark_grid --smoke` | ❌ W0 |
| EXP-08 | E4's cells run tilt-ENABLED and the record says so: `solver_config.normal_fixed` is `false` and the CSV carries `normal_fixed`/`shared_interface`/`seed` (review H1, H5, L6) | unit (hand-built record fixture) | `pytest tests/unit/test_experiments_e4.py -k solver_config -x` | ❌ W0 |
| EXP-08 | E4's per-cell subprocess exit codes map onto the three-value status vocabulary, so an OOM lands as `status=failed` with an `exit_code` (review H2, H3) | unit (monkeypatched `subprocess.run`, no real spawn) | `pytest tests/unit/test_experiments_e4.py -k subprocess -x` | ❌ W0 |
| EXP-08 | `build_grid_scenario` refuses the reserved scenario name `"calibration"` (review L1) | unit | `pytest tests/unit/test_experiments_e4.py -k scenario_shape -x` | ❌ W0 |
| EXP-08 | Grid actually runs on one machine, alone on the box, 9 synthetic + E2's real tenth point | **manual** — production run | — | — |
| EXP-08 | One representative 16x200 cell is measured before the nine-cell commitment (review M8) | **manual** — production probe; see Manual-Only below | — | — |
| EXP-09 | E5's Δn = 0 control columns (`scale_bias_pct_control`, `bias_over_control`) derive from the band's own zero-delta row (review M6) | unit | `pytest tests/unit/test_experiments_e5.py -k control -x` | ❌ W0 |
| EXP-10 | E6's tilt configuration is E4's imported `GRID_NORMAL_FIXED`, and `water_z_error_mm_mean` has one tested origin (review M4, M5) | unit | `pytest tests/unit/test_experiments_e6.py -k "tilt or water_z" -x` | ❌ W0 |
| EXP-10 | E6's baseline row agrees with `benchmark_grid.csv`'s 12/100 cell (review M4) | **manual** — cross-artifact check in plan 19.2-11 Task 2 | — | — |
| EXP-09 | E5 depth/scale-bias + held-out RMSE computation on hand-built fixtures | unit | `pytest tests/unit/test_experiments_e5.py -x` | ❌ W0 |
| EXP-09 | E5 `--smoke` | smoke (CI) | `python -m experiments.e5_index_sensitivity --smoke` | ❌ W0 |
| EXP-09 | Bias moves while reprojection residual does not | **manual** — the scientific finding; reviewed as an artifact | — | — |
| EXP-10 | `calibrate_synthetic` honors `scenario.n_air`/`n_water` (D-23) **and** every existing default-1.333 caller is bit-identical | unit (exact-equality guard) | `pytest tests/unit/test_datasets_pipelines.py -k n_true -x` | ❌ W0 |
| EXP-10 | E6 tidy long-format schema: one row per (axis, value, model) with the varied-axis column | unit | `pytest tests/unit/test_experiments_e6.py -x` | ❌ W0 |
| EXP-10 | E6 `--smoke` | smoke (CI) | `python -m experiments.e6_generalization_sweep --smoke` | ❌ W0 |
| EXP-11 | Every committed `*.json` under `experiments/results/` carries aquacal version, git SHA, environment | unit (key-presence on committed artifacts) | `pytest tests/unit/test_experiments_provenance.py -x` | ❌ W0 |
| EXP-11 | Every non-exempt benchmark record carries `solver_config["seed"]`; the six Phase-19.1 records that predate the key are exempted by an EXPLICIT, exactly-verified `SEEDLESS_LEGACY_RECORDS` set rather than by omission (review H5) | unit (parametrized + `tmp_path` mutation test proving the checker rejects a seedless record) | `pytest tests/unit/test_experiments_provenance.py -k seed -x` | ❌ W0 |
| EXP-11 | The three new CSVs (`benchmark_grid`, `index_sensitivity`, `generalization_sweep`) each carry a fully populated `seed` column | unit | `pytest tests/unit/test_experiments_provenance.py -k csv -x` | ❌ W0 |
| EXP-11 | E3 tiers 1–2 minimal sidecar carries library version + git SHA + Python/SciPy versions | unit | `pytest tests/unit/test_experiments_e3.py -k sidecar -x` | ❌ W0 |
| EXP-11 | README provenance table maps every artifact to script + data file + figure generator | **manual** — prose; see Manual-Only below | — | — |
| all | Four new scripts honour the identical five-flag CLI contract (19.1 D-21) | unit (parametrized over all scripts) | `pytest tests/unit/test_experiments_io.py -k contract -x` | ✅ extend existing |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/test_experiments_e3_constants.py` — **owns** D-18's declared-constants table;
      this file is the CI gate tier 1 depends on, so it must exist before the E3 script imports it
- [ ] `tests/unit/test_experiments_e3.py` — P-formula validation and the tier-1/2 sidecar
- [ ] `tests/unit/test_refractive_geometry.py` — extend for the new public Newton diagnostic,
      including a bit-exactness assertion that `refractive_project` is unchanged
- [ ] `tests/unit/test_experiments_e4.py`, `test_experiments_e5.py`, `test_experiments_e6.py` —
      schema/CLI per script, following `tests/unit/test_experiments_e1.py`'s hand-built-fixture shape
- [ ] `tests/unit/test_experiments_provenance.py` — EXP-11's key-presence check over committed results
- [ ] `tests/unit/test_datasets_pipelines.py` — extend with D-23's exact-equality guard, D-06's
      `memory_out` default-`None` bit-identity guard, **and** review H1's `normal_fixed`
      default-`True` bit-identity guard plus its exact `n_params`-differs-by-2 assertion
- [ ] Framework install: **none needed** — pytest and `psutil` 7.2.2 are already present

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| E2 re-run reproduces all nine §3 numbers | EXP-08 (D-07/D-08) | Requires a ~50 min real-rig calibration against the local release frameset; not a test-suite candidate | `python -u -m experiments.e2_real_rig --config <release config> --force` in the background with `internals.benchmark_memory: true`. Diff every §3 number against the committed `real_rig_metrics.json`. **Any difference is a defect and a hard stop** — source is bit-identical to E2's run (`git diff f193d60..HEAD -- src/` empty), so a move means nondeterminism, not drift. Do NOT run against the Zenodo archive. |
| One 16x200 cell is measured before the nine-cell grid is launched | EXP-08 | Calibrates every downstream runtime estimate; the project's own record is 48-87 min for a comparable Stage-3 problem while the plans said "minutes each" (review M8) | `python -u -m experiments.e4_benchmark_grid --cell 16x200 --out <scratch outside experiments/results> --force`. Record wall-clock, per-stage seconds, peak memory as a fraction of the 16.9 GB box, `n_observations`, and the exit code. Escalate to the orchestrator against the five stated conditions in plan 19.2-09 Task 1 rather than shrinking `DECLARED_CELLS`. |
| E4's nine-cell grid produces a monotone-ish cost curve and no cell OOMs | EXP-08 | Production run; the 16-camera/200-frame cell is the memory risk on a 16.9 GB box | `python -u -m experiments.e4_benchmark_grid --force` in the background, **with nothing else running on the box** — E4 is a wall-clock and peak-memory benchmark and a concurrent experiment contaminates it (review H4). Each cell runs in its own child process, so an OOM is a recorded non-zero `exit_code`, not a death (review H2, H3). Confirm nine rows, `status=ok` on each; a `failed` row with a populated `status_reason` and `exit_code` is an acceptable *recorded* outcome, a missing row is not. |
| E5's bias moves while reprojection residual stays flat | EXP-09 | The scientific claim itself — reviewed as an artifact, not asserted | Inspect `index_sensitivity.csv`: bias column varies with Δn, reprojection column does not. Express bias against the 0.674 mm / 60 mm ≈ 1.1% held-out floor (D-13). |
| E6's accuracy holds (or degrades in a characterized way) across all three axes | EXP-10 | Outcome is genuinely uncertain and D-12 forbids a baked-in pass/fail | Inspect `generalization_sweep.csv`. Degradation is a **result**, not a failure — report it, do not narrow the range. |
| README provenance table completeness | EXP-11 | Prose table; a machine parser would duplicate the table's own content | Read `experiments/README.md`. Every artifact in Part 3's provenance map has a row naming script + data file + figure generator. E5/E6 figure-generator cells name the intended `DissertationFigures` module and mark it a downstream handoff — **not** left blank. The `tab:cpr` subsection must name `cpr_grouping.csv` as the SOLE origin of all six rows and must NOT describe a cross-file split (withdrawn by review H1). |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all ❌ references above
- [ ] No watch-mode flags
- [ ] Feedback latency < 60 s
- [x] **All SIX zero-numerical-change guards** are exact-equality, not tolerance-based — the
      Phases 16/17/19 pattern. Corrected 2026-07-28: this originally named only two, then four.
      1. D-06 — `memory_out` on `calibrate_synthetic` (plan 19.2-01)
      2. D-23 — `n_true` index threading (plan 19.2-01)
      3. D-19 — the public Newton diagnostic leaves the projection path untouched (plan 19.2-03)
      4. `SolverDiagnostics.n_residuals` — added during planning, not enumerated in CONTEXT.md
         (plan 19.2-02). Note its grep gate uses the **"does not increase from a stated
         baseline of 3"** form, not `== 0`: `tests/unit/test_observability.py` already carries
         three tolerance tokens at lines 114 and 454-455, so a bare `== 0` gate would fail on
         contact.
      5. `normal_fixed` passthrough on `calibrate_synthetic` (plan 19.2-01, review H1) — guarded by
         `test_normal_fixed_default_unchanged` (`assert_array_equal` between the omitted-argument
         and explicit-`True` calls) plus `test_normal_fixed_false_changes_problem_size`, which
         asserts an **exact** `n_params` difference of 2 rather than an inequality, so a flag that
         is accepted and silently dropped fails.
      6. `seed` / `memory_readings` on `write_direct_call_benchmark` (plan 19.2-02, review H5) —
         guarded by a re-read-from-disk assertion that a record written without the argument has
         `"seed" not in solver_config`, keeping E1's and E7's committed records byte-shape-identical.
- [x] `nyquist_compliant: true` — every task in the 12-plan set carries an automated verify;
      no watch-mode flags; no run of 3 consecutive tasks without an automated check.

**Approval:** approved 2026-07-28 (post plan-checker; sign-off corrected for the two guards
originally omitted and for the `n_residuals` guard added during planning).

**Revised 2026-07-28 after the cross-AI review (`19.2-REVIEWS.md`).** Corrections in this pass:
- **L4 — a coverage claim that was false.** The EXP-07 row previously asserted that the six
  `tab:cpr` rows were already covered by `tests/unit/test_optim_common.py` with a ✅. They are not:
  `_DOCUMENTED_CONFIGS` is three configurations, all at 13 cameras x 100 frames, pinning
  `docs/guide/optimizer.md`'s "Sparse Jacobian Strategy" numbers (DOCS-01 / D-20). It has never
  covered `tab:cpr`. The row has been split into two: the existing test keeps its ✅ against its
  actual subject, and the six `tab:cpr` configurations are a new ❌ W0 obligation on E3
  (plan 19.2-05), which now owns every row of that table. This was the same gap as review H1,
  surfacing as a green checkmark — the map asserted coverage that did not exist, which is strictly
  worse than asserting none.
- Tier-1 constants: 5 rows -> 9 (review M10 added the two missing Huber `f_scale` sites).
- New W0 rows for E4's solver-config self-description, its subprocess status mapping, its pinned
  scenario name, E5's control columns, E6's tilt anchor and `water_z` helper, and EXP-11's seed
  assertions with their explicit legacy carve-out.
- Production runs documented as serialized one-per-wave with recorded quiescence checks.
