---
phase: 19.2-experiment-execution-and-provenance
plan: 25
type: execute
wave: 6
gap_closure: true
depends_on: ["19.2-21", "19.2-22", "19.2-23"]
files_modified:
  - experiments/README.md
  - .planning/REQUIREMENTS.md
  - .planning/MANUSCRIPT-FINDINGS.md
autonomous: true
requirements: [EXP-07, EXP-09, EXP-10, EXP-11]

must_haves:
  truths:
    - "`experiments/README.md`'s universal provenance claim is TRUE rather than contradicted two sentences later: it opens with 'Every committed result has a benchmark.json-shaped provenance record next to it ... carrying its seed, AquaCal version, git SHA, and the Python/NumPy/SciPy/OS environment', then narrows to seed-only for E5/E6 without ever stating the other three fields are absent"
    - "The README records which committed artifacts are PRE- and which are POST-D-27, since the phase deliberately leaves some experiments on either side of a non-inert library change (D-27 gate item 5)"
    - "`REQUIREMENTS.md` marks EXP-07, EXP-09, EXP-10 and EXP-11 Complete. The requirements ledger is itself a traceability artifact for a phase whose stated purpose is traceability, and it currently says Pending for four of five while the phase records every plan complete"
    - "MF-01's 'resolve before citing' caveat is RESOLVED with the batch-loop measurement, and its median/max framing is restated against the batch loop's semantics -- under all-points termination per-point cost behaves like the tail, not the median"
    - "MF-02 is re-measured against the new grid: E4's old 16x200 cell peaked at 3.31 GiB while E2's 13-camera real rig peaked at 9.78 GiB, and the redesigned geometry is precisely the change that should narrow that gap"
    - "Every runtime in the README's provenance table is a MEASURED value from this phase's execution, not a pre-run estimate -- the table says so, so the re-run's measurements must replace the superseded ones"
  artifacts:
    - path: "experiments/README.md"
      provides: "a provenance table whose universal claim is true and whose runtimes are current"
    - path: ".planning/REQUIREMENTS.md"
      provides: "a ledger that matches the delivered state"
    - path: ".planning/MANUSCRIPT-FINDINGS.md"
      provides: "MF-01 resolved against the production loop; MF-02 re-measured"
  key_links:
    - from: "experiments/README.md provenance table"
      to: "experiments/results/*"
      via: "one row per committed artifact naming script, output file, figure generator, runtime"
      pattern: "e5_provenance.json"
---

<objective>
Make the documentation match what this phase actually delivered: a true provenance claim, a current
runtime table, a ledger that is not four-fifths wrong, and two manuscript findings resolved against
fresh measurements.

Purpose: EXP-07, EXP-09, EXP-10, EXP-11, and the phase goal's traceability clause. The verification
report found the README's provenance section opening with a universal claim that the same paragraph
then quietly contradicts, and `REQUIREMENTS.md` marking four of five requirements `Pending` while the
phase recorded 14/14 plans complete — in a phase whose entire subject is traceability.

Output: an accurate `experiments/README.md`, a corrected `REQUIREMENTS.md`, and `MF-01`/`MF-02`
updated from waves 3–5's measurements.

**This plan writes prose from measurements, not from expectations.** Every number it records comes
from a summary produced by waves 3, 4 or 5. If a number you need is not in a summary, say so rather
than estimating it — an estimate in a table whose header promises measured values is the same class
of defect this phase exists to eliminate.

**The manuscript itself is out of scope.** It lives in OneDrive, outside this repository, and is
read-only from here. `MANUSCRIPT-FINDINGS.md` exists precisely because a finding surfaced by a script
has no natural place to land; entries there need a human editing pass on the paper before submission.
Record the finding; do not attempt to edit the paper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** fork from the merged result of wave 5, with every regenerated artifact committed.
Verify `git status --porcelain` is empty before starting.
This plan runs no experiment and modifies no code, so it may share the box with plan 19.2-24.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MANUSCRIPT-FINDINGS.md
@experiments/README.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-GAP-CONTEXT.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-VERIFICATION.md

<interfaces>
Sources of every number this plan records — read the summaries, not the artifacts, where the summary
carries the comparison:
- `19.2-21-SUMMARY.md` — E4's re-run: per-cell OLD-vs-NEW `n_observations`, residuals, runtime and
  peak memory; the 16x200 outcome and which mechanism caught it; the measured grid wall-clock;
  the N > 16 caveat; any cell that ran under memory pressure.
- `19.2-22-SUMMARY.md` — E6's re-run: wall-clock, the baseline-vs-E4 anchor, `layout_line`'s outcome,
  the unconfounded layout table, the `double_scale` divergence finding.
- `19.2-23-SUMMARY.md` — E5's and E3's re-runs: E5's wall-clock and the byte-identical diff results;
  the full batch-vs-scalar Newton comparison.
- `19.2-18-SUMMARY.md` — the finalised geometry constants and the four containment `--check` results.
- `19.2-16-SUMMARY.md`, `19.2-19-SUMMARY.md` — the new sidecars' names and key sets.

`experiments/README.md` section 2 "Provenance table": one row per committed artifact with Command /
Output file(s) / Figure generator / Runtime, whose preamble states "**Every runtime below is a
measured value from this phase's wave-3/4 execution, not a pre-run estimate.**" Superseded values
include E4 "~27 min for the nine-cell grid (measured, 19.2-09-SUMMARY.md)", E5 "~22.5 min (measured,
19.2-13-SUMMARY.md)", E6 "~33.5 min (measured, 19.2-11-SUMMARY.md)". The E4 paragraph below the table
also asserts "all cells `status=ok`", which the re-run may falsify.

`.planning/REQUIREMENTS.md`: EXP-07/09/10/11 are unchecked in the Phase 19.2 list (around lines
112–116) and `Pending` in the status table (around lines 225–229). EXP-08 is already `Complete`.

`.planning/MANUSCRIPT-FINDINGS.md`:
- **MF-01** — Newton iteration count, status "open — needs a prose edit, **and its source is under
  review**", carrying a provenance caveat block (CR-05) which says the caveat is resolved either by
  migrating the batch loop onto `_solve_newton_r_p` **or** by correcting the docstring and restating
  the CSV's scope. This phase took a third route — option (c), instrument the batch path — so both of
  the caveat's stated resolutions are now out of date. Its committed measurement: 104,052 points,
  12 cameras, min 2, median 4.0, max 7, not converged 0, incidence 0.13-62.92 deg, max residual ~1e-9 m,
  against a supplement claim of "two to four steps".
- **MF-02** — "E4's memory curve does not bound a real deployment of the same camera count":
  16x200 at 3.31 GiB against E2's 13-camera real rig at 9.78 GiB, "fewer cameras, ~3x the memory",
  warning a reader they would under-provision by roughly 3x.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the README's provenance claim true and its runtimes current</name>
  <files>experiments/README.md</files>
  <read_first>
    - `experiments/README.md` in full — the provenance section's opening claim, the whole table, and
      the E4 paragraph beneath it.
    - `19.2-VERIFICATION.md` gap 1, artifact entry for `experiments/README.md` — the exact sentence
      that opens with a universal claim and then narrows.
    - `19.2-GAP-CONTEXT.md` § D-27 "Required gate" item 5 — the pre/post-D-27 artifact record.
    - The five wave summaries named in the interfaces block above.
  </read_first>
  <action>
Rewrite the provenance section's opening claim so it is true of every row, now that E5's and E6's
sidecars exist. If any artifact still falls short of all four fields, the sentence must say so
explicitly and name it — the defect being corrected is not the narrowness of the coverage but the gap
between the claim and the coverage.

Add the two new sidecars (`e5_provenance.json`, `e6_provenance.json`) as rows, and update the
`e6_configs/*.json` row: those twelve files are no longer "checkpoint only" — after plan 19.2-16 they
carry `schema_version`, an environment block and a seed, so they are provenance records that also
serve as checkpoints. Say which.

Replace every superseded runtime with the measured value from this phase's re-run summaries, keeping
the "measured, not estimated" discipline and citing the summary each came from. Where a runtime did
not change (E1, E2, E7), leave it and its citation alone.

Update the E4 paragraph beneath the table: the grid was re-run on redesigned geometry, and its
"all cells `status=ok`" claim must match `benchmark_grid.csv` as it now stands. If a cell failed,
state which and why in one sentence, with the row as the citable source.

**Add a short subsection recording which committed artifacts are pre- and which are post-D-27.** This
is D-27's gate item 5, and it is the piece a future reader will most need: the phase deliberately
changed a library default and deliberately left some experiments on either side of it. State that
E1, E2, E3, E5 and E7 build their scenes through `generate_real_rig_trajectory` or the real frameset
and are structurally unaffected; that E4's and E6's artifacts were regenerated after the change; and
name the mechanical evidence for each claim — plan 19.2-18's frozen-anchor tests and grep-gate, its
four `--check` reproductions, plan 19.2-24's two final-tree `--check` runs, and wave 5's regeneration
of E3 and E5.

Do not restate a number you cannot cite to a summary or an artifact.
  </action>
  <verify>
    <automated>python -c "import pathlib; t=pathlib.Path('experiments/README.md').read_text(encoding='utf-8'); [print(k, k in t) for k in ('e5_provenance.json','e6_provenance.json','D-27')]; assert all(k in t for k in ('e5_provenance.json','e6_provenance.json','D-27'))"</automated>
  </verify>
  <acceptance_criteria>
    - The provenance section's opening sentence is true of every row, or names every artifact it is
      not true of. The summary quotes the old sentence and the new one side by side.
    - `experiments/README.md` contains rows for `e5_provenance.json` and `e6_provenance.json`, and the
      `e6_configs/*.json` row describes them as provenance records rather than "checkpoint only".
    - Every runtime that this phase re-measured is replaced with the new measured value and cites the
      summary it came from; `grep -c "19.2-09-SUMMARY\|19.2-11-SUMMARY\|19.2-13-SUMMARY" experiments/README.md`
      returns 0 for any runtime cell that has been superseded (a historical mention elsewhere in
      prose is fine — state which, if any, you kept and why).
    - The E4 paragraph's status claim matches `benchmark_grid.csv`; if a cell failed, it is named with
      a one-sentence reason.
    - A pre/post-D-27 subsection exists naming, per experiment, which side of the change its committed
      artifacts sit on and what the mechanical evidence is.
    - `python -m pytest tests/unit/test_experiments_provenance.py -q` still exits 0 (the README is not
      under test, but the provenance map it narrates is).
  </acceptance_criteria>
  <done>The README's universal claim is one a reader can rely on, its runtimes are this phase's measurements, and the pre/post-D-27 boundary is written down where a future reader will look for it.</done>
</task>

<task type="auto">
  <name>Task 2: Close the requirements ledger and resolve MF-01 and MF-02 against fresh measurements</name>
  <files>.planning/REQUIREMENTS.md, .planning/MANUSCRIPT-FINDINGS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the Phase 19.2 requirement list (around lines 108–120) and the
      status table (around lines 218–232), plus how a completed requirement is written for phases 19
      and 19.1, so the style matches.
    - `.planning/MANUSCRIPT-FINDINGS.md` in full — the file's stated purpose, MF-01 with its
      provenance caveat block, and MF-02.
    - `19.2-23-SUMMARY.md` — the batch-vs-scalar Newton comparison table, which is MF-01's new
      evidence.
    - `19.2-21-SUMMARY.md` — the new grid's per-cell peak memory, which is MF-02's new evidence.
    - `19.2-VERIFICATION.md` § Anti-Patterns, the `.planning/REQUIREMENTS.md` row.
  </read_first>
  <action>
**REQUIREMENTS.md.** Mark EXP-07, EXP-09, EXP-10 and EXP-11 complete in both places — the checkbox
list and the status table — matching the style used for EXP-08 and for the phase-19.1 entries. Change
only the status markers; do not reword any requirement text, since the requirement is the contract
and rewording it after delivery is how a ledger stops being evidence.

**MF-01.** The caveat block offers two resolutions, and this phase took a third: option (c),
instrumenting the batch path. Rewrite the entry so it reflects what was actually done and what was
actually measured:
  - state that `newton_iterations.csv` now reports both loops, and that the batch rows describe the
    loop `_optim_common.py:635` runs through `refractive_project_batch`;
  - replace the caveat's "resolve before citing" status with the resolution, naming option (c) and
    noting that options (a) and (b) were considered and recorded as rejected — (a) because the shared
    helper is scalar and per-point termination shifts results at ~1e-16 through the bundle
    adjustment, risking a section-3 move;
  - record the batch-loop measurement beside the scalar one, from plan 19.2-23's table;
  - restate the median/max framing against the batch loop's semantics: under all-points termination
    every point iterates until the slowest converges, so production per-point cost behaves like the
    tail rather than the median. Note that this **sharpens** the entry's original "understates the
    tail" conclusion while resting on different evidence than the committed scalar table did;
  - keep the entry's existing discipline: do not silently change the number to the maximum, and do
    not turn a provenance correction into a claim that the positions were wrong. They almost
    certainly were not.

**MF-02.** Re-measure the entry against the new grid. Its original point — E4's 16x200 cell at
3.31 GiB against E2's 13-camera real rig at 9.78 GiB, fewer cameras and roughly 3x the memory — was
itself a symptom of the unrealistic geometry, and D-29 was adopted partly because of it. Update with
the new grid's figures and state plainly whether the gap narrowed, closed, or inverted, and whether
the "under-provision by roughly 3x" warning still applies. If 16x200 failed in the re-run, that is
part of the finding: record it with the mechanism that caught it, and note the irony worth having on
the record — the cell only fit at 3.31 GiB *because* the geometry was unrealistic, so the original
~14.4 GiB projection was closer to correct than the measurement that appeared to refute it.

If either finding is now fully resolved, change its status accordingly rather than leaving an open
entry that no longer needs a prose edit. If a prose edit is still required, say exactly what it is —
the file exists so a human editing pass on the paper has something specific to act on.
  </action>
  <verify>
    <automated>python -c "import pathlib; t=pathlib.Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); import re; rows=[l for l in t.splitlines() if l.startswith('| EXP-')]; print('\n'.join(rows)); assert not any('Pending' in l for l in rows), 'an EXP requirement is still Pending'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^| EXP-\(07\|09\|10\|11\) | Phase 19.2 | Pending" .planning/REQUIREMENTS.md` returns 0,
      and the corresponding checkbox entries are checked.
    - No requirement's descriptive text was reworded; `git diff .planning/REQUIREMENTS.md` shows only
      status-marker changes.
    - MF-01 names option (c) as what was done, records the batch measurement beside the scalar one
      with both cited to `19.2-23-SUMMARY.md`, restates the median/max framing against all-points
      termination, and no longer carries a "resolve before citing" status.
    - MF-01 still states that the computed positions are not known to be wrong and that this was a
      provenance correction.
    - MF-02 records the new grid's 16x200 outcome and peak, states whether the gap to E2's 9.78 GiB
      narrowed, closed or inverted, and says whether the under-provisioning warning still applies.
    - Every number added to either entry is cited to a wave-3/4/5 summary or to a committed artifact;
      the summary lists each number with its source.
    - `git status --porcelain src/ experiments/` is empty — this task changes only planning documents.
  </acceptance_criteria>
  <done>The requirements ledger matches the delivered state, MF-01 cites the loop production runs and says so, and MF-02 reports what the redesigned geometry did to the memory gap it was partly adopted to explain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| summary prose -> published documentation | Numbers move from run summaries into a table whose header promises they are measured. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.2-98 | Repudiation | A runtime or memory figure is estimated rather than measured, inside a table that states every value is measured | mitigate | Both tasks require each number cited to a wave summary or artifact, and the summary must list each number with its source. |
| T-19.2-99 | Repudiation | The README's universal provenance claim is left true-of-most-and-asserted-of-all, which is the exact defect that failed verification | mitigate | Task 1 requires the claim to be true of every row or to name its exceptions, with old and new sentences quoted in the summary. |
| T-19.2-100 | Repudiation | A requirement's text is reworded after delivery so the ledger describes what was built rather than what was agreed | mitigate | An acceptance criterion requires `git diff` to show only status-marker changes. |
| T-19.2-101 | Repudiation | MF-01 is closed by asserting the old numbers were wrong, weakening an accuracy claim the evidence does not support | mitigate | Task 2 requires the entry to keep its statement that the positions are not known to be wrong and that this is a provenance correction. |
| T-19.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
1. The automated blocks in Tasks 1 and 2 exit 0.
2. `python -m pytest tests/unit/test_experiments_provenance.py -q` — exits 0.
3. `git status --porcelain src/ experiments/results/` — empty.
4. `git diff --stat` — touches only `experiments/README.md`, `.planning/REQUIREMENTS.md` and
   `.planning/MANUSCRIPT-FINDINGS.md`.
</verification>

<success_criteria>
- The README's provenance claim is true, its runtimes are this phase's measurements, and the
  pre/post-D-27 boundary is recorded.
- EXP-07, EXP-09, EXP-10 and EXP-11 are marked Complete in both places, with no requirement reworded.
- MF-01 cites the production loop and resolves its caveat; MF-02 reports the redesigned grid's memory
  figures against E2's real rig.
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-25-SUMMARY.md` when done.
Record: the README's old and new opening provenance sentence side by side; every runtime replaced,
with its old value, new value and source summary; the pre/post-D-27 subsection's content in brief;
and, for MF-01 and MF-02, each number added with the summary or artifact it came from.
</output>
