---
phase: 19.2-experiment-execution-and-provenance
plan: 17
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/aquacal/io/benchmark.py
  - tests/unit/test_benchmark.py
  - experiments/e4_benchmark_grid.py
  - tests/unit/test_experiments_e4.py
autonomous: true
requirements: [EXP-08]

must_haves:
  truths:
    - "CR-01: a resumed E4 grid publishes the metrics its per-cell records already hold -- a `skipped_existing` cell is no longer nulled by the same aggregation that just read it successfully"
    - "CR-03: a missing or unreadable E2 `benchmark.json` degrades to a null real-rig row with a named `record_source`, instead of raising after all nine cells have solved and losing both the CSV and the LaTeX fragment"
    - "D-33 gap 2: every cell is spawned with an explicit timeout, and a `TimeoutExpired` lands as a recorded `status=failed` row with a distinct reason -- the same never-raises contract a non-zero exit already has (D-04, WR-09)"
    - "D-33 gap 1: the failed-child path is exercised against a REAL child process that exits non-zero and against one that dies allocating memory, not only against a monkeypatched `subprocess.run` -- the status MAPPING was proven, the behaviour under memory pressure on this OS never was"
    - "D-33 gap 3: a cell cannot report `status=ok` with silently degraded timings. Committed/virtual memory is recorded beside `peak_wset`, a cell near the physical ceiling is flagged as a MEASUREMENT CONDITION (not a verdict, D-12 stands), and a cell projected over a declared fraction of physical RAM is refused deliberately as a recorded row rather than left for the OS to page"
    - "This box has 15.7 GB physical and 30.7 GB virtual and Windows has no OOM killer, so `run_cell_subprocess`'s 'turns an OS-level OOM kill into a recordable exit code' is Linux-shaped reasoning: the likeliest bad outcome is a PAGED SUCCESS with inflated wall-clock and understated `peak_wset`, which bends the scaling curve in a flattering direction"
    - "The memory-reading extension is ADDITIVE: `peak_bytes` and `mode` keep their exact meanings and values, so every committed record and every existing consumer is unaffected"
  artifacts:
    - path: "src/aquacal/io/benchmark.py"
      provides: "commit/virtual memory alongside the resident peak"
      contains: "capture_peak_memory"
    - path: "experiments/e4_benchmark_grid.py"
      provides: "lossless resume, guarded aggregation, per-cell timeout, memory ceiling"
    - path: "tests/unit/test_experiments_e4.py"
      provides: "real-child failure coverage and a resume test with populated metrics"
  key_links:
    - from: "experiments/e4_benchmark_grid.py run_cell_subprocess"
      to: "subprocess.run(..., timeout=...)"
      via: "explicit per-cell timeout mapped onto a recorded row"
      pattern: "TimeoutExpired"
    - from: "experiments/e4_benchmark_grid.py build_grid_dataframe"
      to: "experiments/results/benchmark.json"
      via: "existence-guarded read anchored to __file__"
      pattern: "missing_e2_benchmark"
---

<objective>
Make E4's failure, resume and memory-pressure paths real before the run that will exercise them.

Purpose: EXP-08, and the ordering constraint **D-30**. D-29 (wave 2) deliberately pushes the
16×200 cell to the edge of this box, so the failed-cell, resume and paging paths stop being
theoretical and become load-bearing. CR-01, CR-03 and D-33's three gaps must all be closed **before**
the wave-3 re-run, not after it.

Output: an additively-extended memory reading in the library; an E4 module that resumes losslessly,
aggregates defensively, bounds every child, and cannot publish a paged success as a clean `ok`.

**The subtle requirement is D-33 gap 3, and it must not be reduced to "add a memory field".** A crash
is self-announcing; a paged success is not. Publishing "16 cameras × 200 frames: N GiB, M minutes"
when M measures pagefile thrash is the same class of defect as MF-01 and CR-05 — a plausible number
whose stated meaning is wrong. The Windows metric is `psutil`'s `peak_wset`, a *resident* working
set that paging caps near the physical limit while true commit runs higher, so a thrashing cell can
report a peak **lower** than a healthy one while taking several times the wall clock.

**Correct a stale claim while you work.** CR-01 states "every E4 fixture uses `status='ok'`". That is
overstated — `tests/unit/test_experiments_e4.py` has failed and `skipped_existing` fixtures, and
`test_subprocess_status_mapping` already proves exit 1 → `status="failed"` with a populated reason.
CR-01's *substance* (metrics discarded on the resume path) may still hold. Verify each finding in
source before fixing it and record the correction rather than inheriting the bad evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** `e46a37060c9a108af683c268cf214ac64e347ab0` (EXPECTED_BASE). Verify with
`git rev-parse HEAD`; if it differs, re-fork.
**PYTHONPATH:** this plan modifies `src/`. The editable install resolves to the MAIN checkout, so in
a worktree you MUST `export PYTHONPATH="$(pwd)/src"` and confirm
`python -c "import aquacal; print(aquacal.__file__)"` resolves inside your tree — otherwise your
tests exercise `main`'s library and prove nothing about your change.
**pytest needs the AquaCal conda env** — bare `python` in Git Bash is Anaconda base with no numpy.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-GAP-CONTEXT.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-REVIEW.md
@CLAUDE.md

<interfaces>
`src/aquacal/io/benchmark.py`:
- `capture_peak_memory() -> dict` at 172. Today returns exactly `{"peak_bytes", "mode"}`. Windows
  branch (214–219) reads `psutil.Process().memory_full_info().peak_wset`. Never raises; degrades to
  `{"peak_bytes": None, "mode": "unavailable"}`.
- `assemble_benchmark_record(..., memory_readings=None)` at 310; the per-boundary block is built at
  **404–408** and selects exactly three keys: `cumulative_peak_bytes_as_of_stage_end`,
  `delta_bytes_since_previous_boundary`, `mode`. A new key on the reading does NOT appear in the
  record unless it is threaded here.
- `capture_environment()` already records `ram_total_bytes` (E2's committed value:
  16,857,190,400 bytes).

`src/aquacal/datasets/pipelines.py` `calibrate_synthetic(..., memory_out=...)` populates
`memory_out["_baseline"]`, `["stage3_interface_optimization"]`, `["stage3_intrinsic_pass"]` by
calling `capture_peak_memory()` at 117/161/194.

`experiments/e4_benchmark_grid.py` (line numbers at EXPECTED_BASE):
- `E2_BENCHMARK_PATH = Path("experiments/results/benchmark.json")` at **162** — cwd-relative.
  E3's correct pattern is `_E2_BENCHMARK_JSON_PATH` at `e3_derived_quantities.py:153-155`,
  anchored to `__file__`, and E3's `_build_copied_cpr_row` (425–443) degrades to nulls with
  `record_source="missing_e2_benchmark"` and a WARNING.
- `GRID_COLUMNS` at 171 — 34 columns. `_NULL_METRICS` at 211.
- `run_cell_subprocess(..., timeout=None)` at 548; `subprocess.run(..., timeout=timeout)` at 602 is
  NOT wrapped, so `TimeoutExpired` escapes (WR-09). Called at **1010** (`_run_smoke_cells`) and
  **1048** (`_run_full`), both without a timeout.
- `build_grid_dataframe` at 774; the CR-01 nulling is at **836–839**; the unguarded E2 read is at
  **843–844**. `_run_check`'s correct rule ("cell file exists → ok") is at 986–996.
- `SKIPPED_EXIT_CODE = 3` at 149.

Measured anchor for the memory model (plan 19.2-06): the real rig at 12 cameras × 200 frames peaked
at **9.78 GiB whole-run, +8.54 GiB inside `stage3_interface_optimization`** — roughly **7×** the
1.40 GiB dense Jacobian, because `scipy/optimize/_lsq/trf.py:481-482` holds `J`, `J_h = J * d`,
`svd`'s input copy, `U`, and the LAPACK `gesdd` workspace simultaneously.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Record commit/virtual memory beside the resident peak, additively</name>
  <files>src/aquacal/io/benchmark.py, tests/unit/test_benchmark.py</files>
  <read_first>
    - `src/aquacal/io/benchmark.py` lines 140–245 (`_linux_vmhwm_bytes`, `capture_peak_memory` and
      its full docstring, including the monotonicity property per-stage attribution depends on) and
      380–440 (the `memory_readings` attribution block that selects three keys).
    - `tests/unit/test_benchmark.py` lines 105–140 (`TestCapturePeakMemory`, including
      `test_returns_dict_with_exactly_two_keys`, which this task must update) and 395–430
      (`test_memory_attribution_exact_field_names_and_deltas`, which pins the per-stage memory
      block's key set).
    - `19.2-GAP-CONTEXT.md` § D-33 gap 3 in full — especially why `peak_wset` alone cannot
      distinguish a clean measurement from a paged one.
  </read_first>
  <behavior>
    - `capture_peak_memory()` returns the same `peak_bytes` and the same `mode` string it returns
      today on every platform branch — bit-identical values, no renames.
    - On Windows it additionally returns the process's current and peak commit/pagefile charge and
      the machine's total physical RAM, so a reader can compute how close the run came to the
      physical ceiling and whether commit exceeded residency.
    - On any platform where those fields cannot be obtained, they are present and `None` rather than
      absent, so a consumer never has to branch on key existence.
    - The function still never raises: a failure inside the new reads degrades the new fields only.
  </behavior>
  <action>
Extend `capture_peak_memory()` additively with commit/virtual fields plus the physical total. On
Windows, `psutil.Process().memory_full_info()` already carries `pagefile` and `peak_pagefile` beside
`peak_wset`, and `psutil.virtual_memory().total` gives the machine total; use those rather than
inventing an estimate. Name the keys for what they are — a resident high-water mark and a commit
charge are different quantities and the record must not let a reader conflate them, which is the
whole point of the field.

Preserve `peak_bytes` and `mode` exactly. `mode`'s existing vocabulary (`psutil_peak_wset`,
`proc_status_vmhwm`, `tracemalloc_python_heap`, `psutil_rss_sampled`, `unavailable`) must not gain
or lose members: `mode` describes how `peak_bytes` was obtained, and it already distinguishes a true
high-water mark from the weaker instantaneous fallback. Do not overload it to also describe the new
fields.

Thread the new fields through `assemble_benchmark_record`'s per-boundary memory block at 404–408 so
they actually reach `benchmark.json`; a reading key that never leaves the function is not provenance.
Keep the existing three keys' names and semantics unchanged.

Update the two tests that pin key sets (`test_returns_dict_with_exactly_two_keys` and
`test_memory_attribution_exact_field_names_and_deltas`) to the new expected sets, and state the new
sets in the summary. Update `capture_peak_memory`'s docstring, including the "Returns dict with
exactly two keys" sentence, which becomes false.

Do NOT add polling, a background thread, or a `start()`/`stop()` pairing. Every mode stays a single
synchronous read — that property is what makes the function safe to call at every stage boundary.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_benchmark.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from aquacal.io import capture_peak_memory as f; r=f(); print(r); assert r['mode']=='psutil_peak_wset'; assert r['peak_bytes']>0"` exits 0 on this Windows box, and the summary records the printed dict.
    - The returned dict contains, in addition to `peak_bytes` and `mode`, a current commit/pagefile
      figure, a peak commit/pagefile figure, and the machine's total physical RAM — each named in the
      summary with its observed value.
    - A test asserts the new fields are present and `None` (not absent) when `psutil` is
      unavailable, and that `peak_bytes`/`mode` are unaffected.
    - A test asserts `assemble_benchmark_record` propagates the new fields into
      `record["stages"][<stage>]["memory"]` while the three pre-existing keys keep their exact names.
    - `python -m pytest tests/unit/test_benchmark.py -q` exits 0; the summary states the new
      expected key sets for both updated tests.
    - `grep -c "exactly two keys" src/aquacal/io/benchmark.py` returns 0.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 with at least 1033 passed; new counts recorded.
  </acceptance_criteria>
  <done>A memory reading can distinguish a clean measurement from one that ran against the physical ceiling, the addition is provably additive on `peak_bytes`/`mode`, and the new fields reach `benchmark.json` rather than stopping at the function boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lossless resume and defensive aggregation (CR-01, CR-03)</name>
  <files>experiments/e4_benchmark_grid.py, tests/unit/test_experiments_e4.py</files>
  <read_first>
    - `experiments/e4_benchmark_grid.py` lines 774–860 (`build_grid_dataframe` in full, including
      the nulling rule at 836–839 and the unguarded E2 read at 843–844), 958–1005 (`_run_check`,
      whose "cell file exists → ok" rule at 986–996 is the proof the nulling is unintended), and
      **162** (`E2_BENCHMARK_PATH`, cwd-relative).
    - `experiments/e3_derived_quantities.py` lines 150–160 (`_E2_BENCHMARK_JSON_PATH` anchored to
      `__file__`) and 425–443 (`_build_copied_cpr_row`'s graceful degradation with
      `record_source="missing_e2_benchmark"`). E4 should not be the one module that dies.
    - `tests/unit/test_experiments_e4.py` — the `full_grid_dir` fixture (~184–193) and every
      existing fixture's `status` value. **Check for yourself** whether failed and
      `skipped_existing` fixtures exist before repeating CR-01's stated evidence.
    - `19.2-REVIEW.md` §§ CR-01, CR-03, WR-06.
    - `19.2-GAP-CONTEXT.md` § D-33 "What is already covered (do not re-do)".
  </read_first>
  <behavior>
    - `build_grid_dataframe` with a `skipped_existing` cell whose record is on disk yields populated
      metric columns for that cell.
    - `build_grid_dataframe` with a `failed` cell, or with a cell whose record is absent, still
      yields fully null metrics plus the preserved `status_reason` and `exit_code`.
    - `build_grid_dataframe` with a missing E2 `benchmark.json` returns a ten-row frame whose
      real-rig row is null with `record_source="missing_e2_benchmark"`, and logs a warning — it
      does not raise.
    - Running the module from a working directory other than the repository root resolves the same
      E2 record.
  </behavior>
  <action>
Verify both findings in source first and record what you actually observed, including whether
CR-01's fixture claim holds. Then:

**CR-01.** Null the metric columns only when the status is `failed` or when no record was read for
that cell, so a `skipped_existing` cell keeps the metrics `aggregate()` just read successfully. State
the rule in the docstring in terms of *why* — the skip exists to make the documented D-24 resume
path work, and nulling defeats the only reason it exists.

**CR-03.** Anchor `E2_BENCHMARK_PATH` to `__file__` the way E3 does, and guard the read: a missing or
unparseable E2 record must emit a null real-rig row carrying `record_source="missing_e2_benchmark"`
and a WARNING, never an exception after all nine cells have solved. Also guard `aggregate(cells_dir)`
against a record it refuses — losing the whole run's CSV and LaTeX at the last step is the failure
mode, whatever triggers it.

Do not change the nine-row × declared-cell left join, `GRID_COLUMNS`' order, the status vocabulary,
or the `record_source` values used on the success paths. `missing_e2_benchmark` is a new value for a
new condition, matching the string E3 already publishes for the same condition.

Write the tests first. Assert on the frame, not on log text.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e4.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The summary states, from your own reading of `tests/unit/test_experiments_e4.py`, whether
      failed and `skipped_existing` fixtures exist, and explicitly corrects or confirms CR-01's
      "every E4 fixture uses `status='ok'`" claim.
    - A test builds the frame with a `skipped_existing` cell backed by an on-disk record and asserts
      the metric columns are populated from that record. It FAILS on EXPECTED_BASE — record the
      observed failure.
    - A test builds the frame with a `failed` cell and asserts every metric column is null while
      `status_reason` and `exit_code` survive.
    - A test points `e2_benchmark_path` at a nonexistent file and asserts a ten-row frame is returned
      whose real-rig row has `record_source == "missing_e2_benchmark"` and null metrics, with no
      exception.
    - `python -c "import experiments.e4_benchmark_grid as m; assert m.E2_BENCHMARK_PATH.is_absolute()"` exits 0.
    - `python -m experiments.e4_benchmark_grid --smoke` exits 0 when run from a directory other than
      the repository root (record the directory used).
    - `python -m pytest tests/unit/test_experiments_e4.py -q` exits 0.
  </acceptance_criteria>
  <done>A resumed grid publishes the cells it already computed, and a missing E2 record costs a null row and a warning rather than the entire run's output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bound every cell, observe a real death, and make a paged success unreportable as ok (D-33)</name>
  <files>experiments/e4_benchmark_grid.py, tests/unit/test_experiments_e4.py</files>
  <read_first>
    - `experiments/e4_benchmark_grid.py` lines 548–630 (`run_cell_subprocess` in full, including the
      docstring's "Never raises on a non-zero child exit" promise and the unwrapped
      `subprocess.run(..., timeout=timeout)` at 602), 369–546 (`run_grid_cell`, where the per-cell
      calibration and its `memory` dict are assembled), 1006–1060 (`_run_smoke_cells` and
      `_run_full`, the two `timeout=None` call sites at 1010 and 1048), and `GRID_COLUMNS` at 171.
    - `tests/unit/test_experiments_e4.py::test_subprocess_status_mapping` — what is ALREADY proven
      (exit 1 → `status="failed"` with a populated reason, via a monkeypatched `subprocess.run`) and
      the module's own statement that no real child is ever spawned.
    - `19.2-GAP-CONTEXT.md` § D-33 in full — all three gaps and the outcomes table.
    - `19.2-REVIEW.md` § WR-09.
    - Task 1's summary — the new memory fields' exact names.
    - `CLAUDE.md` § "Always run calibration unbuffered".
  </read_first>
  <behavior>
    - A real child process that exits non-zero produces a recorded row with `status="failed"`, a
      populated `status_reason`, and the child's exit code — no exception, no hang.
    - A real child that dies while allocating produces the same shape of recorded row, whatever exit
      code this OS reports.
    - A child that exceeds its timeout produces `status="failed"` with a reason distinguishable from
      a non-zero exit, and `run_cell_subprocess` still never raises.
    - A cell whose projected Stage-3 memory exceeds the declared fraction of physical RAM is refused
      before the solve and recorded as a failed row with a reason naming the projection and the
      ceiling — not left for the OS to page.
    - A cell that completed but whose observed peak approached the physical limit carries a
      measurement-condition flag; a comfortably-sized cell does not.
  </behavior>
  <action>
**Gap 2 — timeout.** Wrap `subprocess.run` and map `subprocess.TimeoutExpired` onto a recorded row
(`status="failed"`, a reason naming the timeout and its value, an exit code of `None`), honouring the
never-raises contract the docstring already promises. Pass an explicit per-cell timeout at both call
sites. Size it from measurement, not from a guess: the wave-3 plan runs a probe cell first, and the
committed grid took ~27 minutes for nine cells at the old geometry, while a real-rig calibration
takes 48–87 minutes. Declare the timeout as a module constant with a comment stating what it is
derived from and that it exists to bound a thrashing cell, not to enforce a schedule.

**Gap 1 — a real child.** Add tests that spawn a genuine child process rather than monkeypatching
`subprocess.run`: one that exits non-zero, and one that allocates until it dies. Assert the row is
*recorded* — not raised, not hung, not dropped. Keep the allocating test bounded and skippable in CI
if it would destabilise the shared runner, but it must run here: the whole point is that this
machine's behaviour under memory pressure has never been observed. Record what actually happened —
the exit code, whether Windows reported a `MemoryError`, an OS kill, or a slow paged survival — as
a finding in the summary. That observation is the evidence D-29's pre-authorised 16×200 failure
rests on.

**Gap 3 — the paged success.** Make a degraded measurement impossible to report as a clean `ok`:

  - Record the commit/virtual figures from Task 1 alongside `peak_wset` in each cell's record and as
    columns on the grid, so paging is visible in the record rather than inferred.
  - Add a memory-pressure field with a small, explicit vocabulary — e.g. a clean measurement, a
    completed run whose peak approached the physical ceiling, and a deliberate pre-flight refusal.
    This is a **measurement condition, not a verdict**: D-12 stands and no pass/fail column is added
    to any results CSV. Say so in the constant's comment so a later reader does not mistake it for
    one.
  - Add a pre-flight ceiling: before Stage 3, project the dense Jacobian's memory from the problem's
    own shape (`n_residuals × n_params × 8 bytes`) scaled by a declared multiplier, and refuse the
    cell as a recorded row if the projection exceeds a declared fraction of physical RAM. Take the
    multiplier from plan 19.2-06's measurement (9.78 GiB whole-run against a 1.40 GiB dense
    Jacobian, ≈7×, driven by `trf.py:481-482` holding `J`, `J_h`, the SVD input copy, `U` and the
    `gesdd` workspace at once) and declare BOTH the multiplier and the fraction as named constants
    whose comments cite that measurement. A deliberate recorded failure is a better datum than a
    paged success.

Add the new columns to `GRID_COLUMNS` and `_NULL_METRICS` and state the new column count in the
summary. Do NOT reduce `DECLARED_CELLS` or change the declared grid. Do NOT add a pass/fail verdict
column.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e4.py -q</automated>
  </verify>
  <acceptance_criteria>
    - A test spawns a REAL child process (no `monkeypatch` of `subprocess.run`) that exits non-zero
      and asserts `run_cell_subprocess` returns `status="failed"` with a populated `status_reason`
      and the child's exit code.
    - A test spawns a real child that allocates until it dies and asserts a recorded row is returned.
      The summary records the observed exit code and how this OS terminated it (`MemoryError`, OS
      kill, or paged survival) — the first direct observation of this path on this machine.
    - A test forces a timeout (a child that sleeps past a short timeout) and asserts
      `status="failed"` with a reason distinguishable from a non-zero exit, with nothing raised.
    - `python -c "import experiments.e4_benchmark_grid as m, inspect; s=inspect.getsource(m._run_full); assert 'timeout' in s"` exits 0 — the production call site passes a timeout.
    - `grep -c "TimeoutExpired" experiments/e4_benchmark_grid.py` returns at least 1.
    - The memory-ceiling fraction and the Jacobian memory multiplier are module constants whose
      comments cite plan 19.2-06's 9.78 GiB / 1.40 GiB measurement; `grep -v '^#' experiments/e4_benchmark_grid.py | grep -c "9.78\|MEMORY_CEILING\|JACOBIAN_MEMORY"` returns a non-zero count.
    - A test drives the pre-flight projection over a fabricated problem shape that exceeds the
      ceiling and asserts a `status="failed"` row whose reason names both the projection and the
      ceiling, and that the calibration was NOT attempted.
    - A test asserts the memory-pressure field is a measurement condition with a fixed vocabulary and
      that a comfortably-sized smoke cell reports the clean value.
    - `python -c "import experiments.e4_benchmark_grid as m; print(len(m.GRID_COLUMNS)); assert set(m._NULL_METRICS) <= set(m.GRID_COLUMNS)"` exits 0; the new column count is recorded in the summary.
    - `python -m experiments.e4_benchmark_grid --smoke` exits 0.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 with at least 1033 passed; new counts recorded.
  </acceptance_criteria>
  <done>Every cell is bounded by a timeout mapped onto a recorded row, the real failed-child path has been observed on this machine rather than assumed from a Linux-shaped docstring, and a cell can no longer report `status=ok` while its timings measure pagefile thrash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child process -> parent | Exit codes, stderr tails and on-disk records produced by a process the parent cannot introspect. |
| OS memory subsystem -> record | Windows pages rather than killing, so the OS silently changes what a timing means. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.2-65 | Repudiation | A paged 16×200 cell publishes `status=ok` with inflated wall-clock and understated `peak_wset`, bending the published scaling curve in a flattering direction | mitigate | Task 1 records commit/virtual beside the resident peak; Task 3 adds a memory-pressure measurement condition and a pre-flight ceiling that refuses the cell as a recorded row. |
| T-19.2-66 | Denial of Service | A thrashing cell runs unbounded because both production call sites pass `timeout=None` | mitigate | Task 3 passes an explicit per-cell timeout derived from measurement and maps `TimeoutExpired` onto a recorded row. |
| T-19.2-67 | Repudiation | The failure path is trusted on the strength of a monkeypatched status-mapping test that never spawned a process | mitigate | Task 3 requires two real-child tests and a recorded observation of how this OS actually terminates an allocating child. |
| T-19.2-68 | Repudiation | A resumed grid or a missing E2 record silently destroys the run's output after all nine cells have solved | mitigate | Task 2's guarded aggregation and `missing_e2_benchmark` row, with tests asserting on the frame. |
| T-19.2-69 | Tampering | The additive memory extension changes `peak_bytes` semantics, invalidating every committed record's memory column | mitigate | Task 1's acceptance requires `peak_bytes`/`mode` unchanged, asserted by test, with the new fields threaded separately. |
| T-19.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; `psutil` is already a dependency used by `capture_peak_memory`. |
</threat_model>

<verification>
1. `python -m pytest tests/unit/test_benchmark.py tests/unit/test_experiments_e4.py -q` — exits 0.
2. `python -m experiments.e4_benchmark_grid --smoke` — exits 0.
3. `python -m pytest tests/ -m "not slow" -q` — exits 0, at least 1033 passed.
4. `git diff --stat experiments/results/` — empty; this plan regenerates no artifact.
</verification>

<success_criteria>
- CR-01, CR-03, WR-09 and all three D-33 gaps are closed, each covered by a test that fails on
  EXPECTED_BASE where a defect existed.
- The library's memory reading distinguishes residency from commit, additively.
- The wave-3 re-run can rely on the failed-cell, resume, timeout and ceiling paths because they have
  been exercised, not assumed (D-30).
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-17-SUMMARY.md` when done.
Record: the correction or confirmation of CR-01's fixture claim; the new `capture_peak_memory()` key
set and this box's observed values; the new per-stage memory block key set; the new `GRID_COLUMNS`
count; the declared timeout, memory multiplier and ceiling fraction with their derivations; and —
most importantly — what actually happened when a real child allocated until it died on this machine.
</output>
