---
phase: 19.2-experiment-execution-and-provenance
plan: 12
type: execute
wave: 5
depends_on: ["19.2-05", "19.2-08", "19.2-09", "19.2-11"]
files_modified:
  - tests/unit/test_experiments_provenance.py
  - experiments/README.md
  - .github/workflows/test.yml
  - experiments/results/cpr_derived_values.tex
  - experiments/results/cpr_grouping.tex
autonomous: true
requirements: [EXP-11]

must_haves:
  truths:
    - "Every committed result under experiments/results/ carries its seed, AquaCal version, git SHA, and environment - asserted by a test over the real committed artifacts, not by inspection"
    - "E1's and E7's already-committed records are VERIFIED, not re-run - they were written by the same assemble_benchmark_record path in 19.1 and a re-run costs ~90 minutes for no new information"
    - "experiments/README.md's provenance table gains rows for E3, E4, E5 and E6 in the shape the existing E1/E2/E7 rows already use - extended, not redesigned"
    - "E5's and E6's figure-generator cells name the intended DissertationFigures module and mark it a downstream handoff rather than being left blank"
    - "The README documents which tab:cpr rows come from cpr_grouping.csv and which from benchmark_grid.csv, so the split has no invisible gap"
    - "CI's experiments-smoke job runs all four new scripts, so the suite cannot rot"
  artifacts:
    - path: "tests/unit/test_experiments_provenance.py"
      provides: "key-presence assertion over every committed provenance record"
      min_lines: 80
    - path: "experiments/README.md"
      provides: "the completed provenance table plus the tab:cpr split note"
      contains: "e3_derived_quantities"
    - path: ".github/workflows/test.yml"
      provides: "experiments-smoke covering all seven scripts"
      contains: "e6_generalization_sweep"
  key_links:
    - from: "tests/unit/test_experiments_provenance.py"
      to: "experiments/results/*.json"
      via: "glob + key-presence assertion"
      pattern: "experiments/results"
    - from: "experiments/README.md"
      to: "every committed artifact"
      via: "one provenance row per artifact naming script, data file, and figure generator"
      pattern: "benchmark_grid.csv"
---

<objective>
Close EXP-11: assert mechanically that every committed result carries its provenance, extend
`experiments/README.md`'s provenance table to cover E3/E4/E5/E6, wire the four new scripts into
CI's smoke job, and regenerate E3's derived-values fragment now that `benchmark_grid.csv` exists.

Purpose: provenance is the deliverable of this phase, not a by-product. The paper has already been
bitten twice by hand-carried numbers — the dissertation appendix's `P → log(P)` and the docs'
"~12x fewer evaluations". A reviewer following the SoftwareX reproducibility path should never have
to guess which script made which number.
Output: one new test file, a completed README table, an extended CI job, and a regenerated LaTeX
fragment with no remaining TODO placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** fork from the merged result of plans 19.2-05, 19.2-08, 19.2-09 and 19.2-11 — every
artifact this plan documents must already exist. Verify with
`ls experiments/results/{code_constants,newton_iterations,cpr_grouping,benchmark_grid,index_sensitivity,generalization_sweep}.csv`
before starting; if any is missing, your base is stale.
**PYTHONPATH:** the editable install resolves to the main checkout, not a worktree. If working in
a worktree, `export PYTHONPATH="$(pwd)/src"` before running pytest or its passing tests are
meaningless.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-CONTEXT.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-SOURCE-BRIEF.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-PATTERNS.md
@experiments/README.md

<interfaces>
<!-- Contracts the executor needs. Extracted live from the codebase 2026-07-28. -->

Committed provenance records under `experiments/results/` at the start of this phase:
  benchmark.json                       (E2, pipeline-written, refreshed by plan 19.2-06)
  e1_benchmark_refractive.json, e1_benchmark_nonrefractive.json      (E1, direct-call)
  e7_benchmark_shared_fixed.json, e7_benchmark_shared_refined.json,
  e7_benchmark_percamera_fixed.json, e7_benchmark_percamera_refined.json   (E7, direct-call)
  real_rig_metrics.json                (E2, its own schema with a `provenance` block)
  interface_ablation_conditioning.json (E7, conditioning report)
plus, added by this phase:
  e3_provenance.json                   (E3 tiers 1-2, environment-only sidecar)
  experiments/results/e4_cells/cameras_*_frames_*/benchmark.json    (E4, one per cell)

`capture_environment(repo_hint_path=None) -> dict`   # src/aquacal/io/benchmark.py:66
  Read its body to learn the EXACT key names it emits (library version, git SHA, Python/NumPy/
  SciPy versions, CPU, cores, RAM, OS). The test must assert against those live key names, not
  against names copied from prose.

`assemble_benchmark_record(...)` emits top-level `schema_version`, `problem_shape`, `stages`,
`solver_config`, `accuracy`, `environment`, and `memory` (only when memory_readings was supplied).

CI job to extend (`.github/workflows/test.yml`, the `experiments-smoke` job) currently runs:
  python -m experiments.e1_refractive_comparison --smoke
  python -m experiments.e2_real_rig --smoke
  python -m experiments.e7_interface_ablation --smoke

README's existing provenance table columns (section 2):
  | Paper artifact | Experiment | Command | Output file(s) | Figure generator | Runtime |
It also carries a paragraph stating `e4_benchmark_grid.py` is "a relocated skeleton only" and
"deliberately not run in this phase" — that paragraph is now false and must be replaced.

Downstream figure modules (a different repository — X5, deferred; cells NAME them and mark them a
handoff rather than being left blank):
  DissertationFigures/src/dissertationfigures/figures/aquacal/
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mechanically assert provenance over every committed record</name>
  <files>tests/unit/test_experiments_provenance.py</files>
  <behavior>
    - `test_every_benchmark_record_has_environment`: every `*.json` under `experiments/results/`
      that carries a `schema_version` key also carries an `environment` block containing the exact
      keys `capture_environment()` emits for library version, git SHA, and Python/NumPy/SciPy
      versions. Parametrized over the discovered files so a failure names the offending file.
    - `test_e4_cell_records_have_provenance`: every
      `experiments/results/e4_cells/*/benchmark.json` carries `schema_version`, `environment`, and
      `memory`.
    - `test_e3_sidecar_has_minimal_provenance`: `e3_provenance.json` carries `experiment`,
      `schema_version`, `seed`, and an `environment` block with the same version/SHA keys — the
      Part 4 Rule 2 carve-out for tiers that never run a calibration.
    - `test_e1_and_e7_records_already_comply`: E1's two and E7's four committed records each pass
      the same environment key-presence check. These are VERIFIED, not re-run.
    - `test_all_committed_csvs_have_a_named_record`: every `*.csv` under `experiments/results/` is
      listed in a module-level `CSV_TO_RECORD` mapping naming the provenance record that covers it;
      a CSV present on disk but absent from the mapping fails the test. This is the tripwire that
      catches a future artifact committed without provenance.
    - `test_environment_blocks_report_one_machine`: every record produced by THIS phase reports the
      same CPU and RAM figures as `benchmark.json` — constraint 11, asserted rather than assumed.
  </behavior>
  <read_first>
    - `src/aquacal/io/benchmark.py` lines 44-150 — `_find_git_root` and `capture_environment`'s
      full body, so the asserted key names are the live ones.
    - `experiments/results/benchmark.json` and `experiments/results/e1_benchmark_refractive.json` —
      two real committed records, one pipeline-written and one direct-call, so the test is written
      against the actual shapes.
    - `experiments/results/real_rig_metrics.json` — note it has its OWN schema with a `provenance`
      block and NO `schema_version`; the test must handle it explicitly rather than failing it.
    - `tests/unit/test_experiments_io.py` — the established convention of mirroring a real
      committed artifact's shape in module-level constants so a contract change breaks the test
      intentionally.
    - `19.2-CONTEXT.md` § Claude's Discretion item 1 — E1's and E7's records are verified, not
      re-run.
  </read_first>
  <action>
Create `tests/unit/test_experiments_provenance.py`. Module docstring: this file is EXP-11's
mechanical gate — every committed result under `experiments/results/` must carry its seed, AquaCal
version, git SHA, and environment, and a new artifact committed without provenance must fail CI
rather than be noticed in review.

Define module-level constants:
  - `RESULTS_DIR = Path("experiments/results")`
  - `REQUIRED_ENVIRONMENT_KEYS` — the subset of `capture_environment()`'s live key names covering
    library version, git SHA, and the Python/NumPy/SciPy versions. Derive these by reading
    `capture_environment`'s body, not by copying names from prose.
  - `CSV_TO_RECORD` — an explicit mapping from every committed CSV filename to the provenance
    record that covers it (e.g. `benchmark_grid.csv` to the per-cell records under `e4_cells/`,
    `index_sensitivity.csv` to E5's record, `code_constants.csv` to `e3_provenance.json`).
  - `SELF_DESCRIBING_JSON` — the explicit set of committed JSON files that carry their own schema
    rather than `assemble_benchmark_record`'s (currently `real_rig_metrics.json` and
    `interface_ablation_conditioning.json`), each with a one-line comment naming what covers its
    provenance instead.

Implement the six behaviors above. Discover files by globbing `RESULTS_DIR`, and parametrize so a
failure names the offending file rather than reporting a bare boolean.

Skip cleanly (`pytest.skip`) when `RESULTS_DIR` is absent or empty, so a fresh clone without the
committed artifacts does not fail — but do NOT skip when the directory exists and merely lacks a
particular file; that is the gap the test exists to catch.

Do not re-run E1 or E7 to produce fresh records. Their committed records were written by the same
`assemble_benchmark_record` path in Phase 19.1; a re-run costs roughly 90 minutes for no new
information. Verify them where they sit.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_provenance.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_experiments_provenance.py -x -q` exits 0 and collects at least 6 test functions (more once parametrized over discovered files).
    - `python -m pytest tests/unit/test_experiments_provenance.py -m slow -q` collects 0 tests.
    - `python -c "from tests.unit.test_experiments_provenance import CSV_TO_RECORD; import pathlib; found={p.name for p in pathlib.Path('experiments/results').glob('*.csv')}; missing=found-set(CSV_TO_RECORD); assert not missing, missing"` exits 0 — every committed CSV is mapped.
    - `grep -c "calibrate_synthetic\|run_calibration" tests/unit/test_experiments_provenance.py` returns 0 — nothing is re-run.
    - The test fails as intended when a provenance key is removed: verified by temporarily deleting the `environment` block from a copy of a record in a `tmp_path` fixture and asserting the checker rejects it.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 with at least 874 passed.
    - `ruff check tests/unit/test_experiments_provenance.py && ruff format --check tests/unit/test_experiments_provenance.py` exits 0.
  </acceptance_criteria>
  <done>Every committed record's provenance is asserted mechanically, every committed CSV is mapped to a covering record, one-machine consistency is asserted rather than assumed, and E1's and E7's records were verified without a re-run.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the README provenance table and wire the four scripts into CI</name>
  <files>experiments/README.md, .github/workflows/test.yml</files>
  <read_first>
    - `experiments/README.md` — the FULL file. Section 2's provenance table (its six columns and
      the exact row shape used for E1/E2/E7), and the paragraph after the table declaring
      `e4_benchmark_grid.py` a "relocated skeleton only" that is "deliberately not run in this
      phase" — now false.
    - `.github/workflows/test.yml` lines 59-79 — the `experiments-smoke` job's current three
      invocations and its setup steps.
    - `19.2-SOURCE-BRIEF.md` § Part 3 — the provenance map naming every paper artifact, its
      experiment, its data file, and its figure generator. This is the table the README should
      carry.
    - `19.2-CONTEXT.md` § Claude's Discretion item 1 — extend the table's shape, do not redesign
      it; name the intended `DissertationFigures` module in E5's and E6's figure-generator cells
      and mark them downstream handoffs rather than leaving them blank.
    - The summaries of plans 19.2-05, 19.2-08, 19.2-09 and 19.2-11 — the MEASURED runtimes to put
      in the Runtime column. Every runtime in this table is measured, not estimated.
  </read_first>
  <action>
**README.** Extend section 2's provenance table with rows for every artifact this phase committed,
using the existing six-column shape verbatim — do not redesign the table, do not add or remove
columns, do not reorder the existing rows:
  - E3: `code_constants.csv`, `newton_iterations.csv`, `cpr_grouping.csv`, `cpr_grouping.tex`,
    `cpr_derived_values.tex`, `e3_provenance.json`, each with the command
    `python -m experiments.e3_derived_quantities` and its measured runtime.
  - E4: `benchmark_grid.csv` and `benchmark_grid.tex`, plus the per-cell `e4_cells/*/benchmark.json`
    provenance records, with the command `python -m experiments.e4_benchmark_grid` and the
    measured grid runtime.
  - E5: `index_sensitivity.csv` with `python -m experiments.e5_index_sensitivity`.
  - E6: `generalization_sweep.csv` with `python -m experiments.e6_generalization_sweep`.

For E5's and E6's Figure generator cells, name the intended
`DissertationFigures/src/dissertationfigures/figures/aquacal/` module and mark it explicitly as a
downstream handoff (X5, a different repository, so no AquaCal phase can satisfy it). Do not leave
either cell blank. E4's and E3's LaTeX-fragment artifacts are their own "figure generator" — say so
rather than leaving a dash that reads as "unknown".

DELETE the paragraph declaring `e4_benchmark_grid.py` a relocated skeleton that is deliberately not
run, and replace it with a short paragraph stating that E4 is now a direct-call synthetic grid, why
(a real 13-camera run is 48–87 minutes and 16 cameras is unreachable from a 13-camera rig), and
that its nine cells are `timing_scope=optimization_only` / `record_source=assembled` while E2's
tenth row is `end_to_end` / `pipeline` — with one sentence saying why those two columns exist, so a
reader cannot compare the two kinds of row by accident.

Add a short subsection documenting the `tab:cpr` split: which rows come from `cpr_grouping.csv`
(the 3-camera/3-frame and 13-camera/200-frame configurations, the latter copied from E2's
`benchmark.json`) and which come from `benchmark_grid.csv` (the configurations inside E4's grid).
State the rule that produced the split — one number, one origin — so the omission from
`cpr_grouping.csv` cannot be mistaken for an oversight.

Add the four new commands to section 7's "Reproducing a number" block, following the existing
`--check` / `--force` idiom.

**CI.** Append four invocations to `.github/workflows/test.yml`'s `experiments-smoke` job, one per
new or rewritten script, in the same style as the existing three:
`python -m experiments.e3_derived_quantities --smoke`,
`python -m experiments.e4_benchmark_grid --smoke`,
`python -m experiments.e5_index_sensitivity --smoke`,
`python -m experiments.e6_generalization_sweep --smoke`.
Make no structural change to the job — same setup steps, same runner, same install extras.
  </action>
  <verify>
    <automated>python -c "
import pathlib, re
r = pathlib.Path('experiments/README.md').read_text(encoding='utf-8')
for name in ['code_constants.csv','newton_iterations.csv','cpr_grouping.csv','benchmark_grid.csv','index_sensitivity.csv','generalization_sweep.csv','e3_provenance.json']:
    assert name in r, name
for cmd in ['e3_derived_quantities','e4_benchmark_grid','e5_index_sensitivity','e6_generalization_sweep']:
    assert cmd in r, cmd
assert 'relocated skeleton' not in r
assert 'DissertationFigures' in r or 'figures/aquacal' in r
w = pathlib.Path('.github/workflows/test.yml').read_text(encoding='utf-8')
for cmd in ['e1_refractive_comparison','e2_real_rig','e7_interface_ablation','e3_derived_quantities','e4_benchmark_grid','e5_index_sensitivity','e6_generalization_sweep']:
    assert f'python -m experiments.{cmd} --smoke' in w, cmd
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `experiments/README.md` names all seven new data artifacts (`code_constants.csv`, `newton_iterations.csv`, `cpr_grouping.csv`, `benchmark_grid.csv`, `index_sensitivity.csv`, `generalization_sweep.csv`, `e3_provenance.json`) in section 2's table.
    - Section 2's table still has exactly its original six columns, in their original order, and the pre-existing E1/E2/E7 rows are unchanged.
    - `grep -c "relocated skeleton" experiments/README.md` returns 0.
    - The README states why `timing_scope` and `record_source` exist and names which value each kind of row carries.
    - The README's `tab:cpr` subsection names which rows come from `cpr_grouping.csv` and which from `benchmark_grid.csv`.
    - E5's and E6's Figure generator cells each name a `DissertationFigures` path and the word "handoff"; neither is blank or a bare dash.
    - Every Runtime cell added is a measured value taken from a plan summary, not an estimate — the summary records which plan each came from.
    - `.github/workflows/test.yml` contains exactly seven `python -m experiments.<name> --smoke` lines.
    - `git diff .github/workflows/test.yml` shows only added invocation lines — no change to the job's setup steps, runner, or install extras.
    - `ruff check . && pre-commit run --all-files` exits 0.
  </acceptance_criteria>
  <done>The provenance table covers every artifact this phase committed in its original six-column shape, the false skeleton paragraph is gone, the `tab:cpr` split is documented, downstream figure handoffs are named rather than blank, and CI smoke-tests all seven scripts.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate E3's derived-values fragment now that benchmark_grid.csv exists</name>
  <files>experiments/results/cpr_derived_values.tex, experiments/results/cpr_grouping.tex</files>
  <read_first>
    - `experiments/e3_derived_quantities.py` — `write_derived_values_latex`, especially the branch
      that emits the `\TODO{awaiting benchmark\_grid.csv from E4}` placeholder when its input is
      missing.
    - `experiments/results/cpr_derived_values.tex` — the fragment as first written by plan 19.2-05,
      to confirm the placeholder is actually present before regenerating.
    - `experiments/results/benchmark_grid.csv` — the input that was missing then and exists now.
    - `19.2-CONTEXT.md` § D-22 — the two derived prose asides that must be regenerated from measured
      rows rather than trusted from the supplement's arithmetic.
  </read_first>
  <action>
Re-run `python -m experiments.e3_derived_quantities --force` so the derived-values fragment is
regenerated with `benchmark_grid.csv` present. E3's tiers are seconds-to-minutes, so this is not a
long run.

Confirm that `cpr_derived_values.tex` now carries a real value where the `\TODO{` placeholder was,
and that the value was computed from `benchmark_grid.csv`'s measured rows — not from the
supplement's arithmetic and not hand-entered.

Confirm `code_constants.csv`, `newton_iterations.csv`, and `cpr_grouping.csv` are unchanged by this
re-run apart from any legitimately re-measured value. If `newton_iterations.csv` moved, that is a
finding worth reporting — those tiers are deterministic given the seed — so record the diff rather
than accepting it silently.

Record in the summary both regenerated derived asides verbatim, together with the
`benchmark_grid.csv` rows they were computed from, so the manuscript's prose can cite a measured
origin for each.
  </action>
  <verify>
    <automated>python -c "
import pathlib
t = pathlib.Path('experiments/results/cpr_derived_values.tex').read_text(encoding='utf-8')
assert 'TODO' not in t, t
assert t.count('\\\\newcommand') >= 2 or t.count('def') >= 2
print(t)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m experiments.e3_derived_quantities --force` exits 0.
    - `grep -c "TODO" experiments/results/cpr_derived_values.tex` returns 0.
    - `cpr_derived_values.tex` contains at least two LaTeX macro definitions, both carrying numeric values.
    - Each regenerated value is traceable to named rows of `benchmark_grid.csv` or `cpr_grouping.csv`, recorded in the summary alongside the source rows.
    - `git diff experiments/results/code_constants.csv experiments/results/cpr_grouping.csv` is empty, or any difference is recorded and explained in the summary.
    - `git diff experiments/results/newton_iterations.csv` is empty; if it is not, the diff is recorded and reported as a finding, since these tiers are deterministic given the seed.
    - `python -m pytest tests/unit/test_experiments_provenance.py tests/unit/test_experiments_e3.py tests/unit/test_experiments_e3_constants.py -q` exits 0.
  </acceptance_criteria>
  <done>`cpr_derived_values.tex` carries measured values with no TODO placeholder, both asides are traceable to named source rows, and the deterministic tiers reproduced unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow definition | Four invocations added to a GitHub Actions job. No new secrets, no new permissions, no new action, no structural change. |
| local filesystem write | README, workflow file, and two regenerated LaTeX fragments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.2-50 | Repudiation | A future artifact is committed without a provenance record and nobody notices, reintroducing exactly the hand-carried-number failure this phase exists to close | mitigate | `test_all_committed_csvs_have_a_named_record` fails when a CSV appears on disk without an entry in `CSV_TO_RECORD`; the mapping must be updated deliberately. |
| T-19.2-51 | Repudiation | E1's and E7's records are re-run "to be safe", burning ~90 minutes and producing records that differ from the ones the manuscript already cites | mitigate | The task verifies them where they sit; a grep gate asserts nothing in the provenance test re-runs a calibration. |
| T-19.2-52 | Repudiation | A figure-generator cell is left blank, so a reader cannot tell whether the generator is missing or merely elsewhere | mitigate | Acceptance criterion requires E5's and E6's cells to name a `DissertationFigures` path and the word "handoff"; neither may be blank or a bare dash. |
| T-19.2-53 | Repudiation | The README keeps claiming E4 is a skeleton that is deliberately not run, contradicting the committed grid | mitigate | Grep gate asserts "relocated skeleton" no longer appears, and a replacement paragraph explaining the two comparability columns is required. |
| T-19.2-54 | Repudiation | The `tab:cpr` rows split across two CSVs with no documentation, so the gap in `cpr_grouping.csv` reads as an oversight | mitigate | A README subsection must name which rows come from which file and state the one-number-one-origin rule that produced the split. |
| T-19.2-55 | Tampering | The CI job's setup steps or runner are altered while adding invocations | mitigate | Acceptance criterion requires `git diff .github/workflows/test.yml` to show only added invocation lines. |
| T-19.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. `19.2-RESEARCH.md` § Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
1. `python -m pytest tests/unit/test_experiments_provenance.py -q` — exits 0.
2. Both automated blocks in Tasks 2 and 3 exit 0.
3. `grep -c "python -m experiments\..* --smoke" .github/workflows/test.yml` — returns 7.
4. `grep -c "TODO" experiments/results/cpr_derived_values.tex` — returns 0.
5. `python -m pytest tests/ -m "not slow" -q` — exits 0, at least 874 passed.
6. `ruff check . && pre-commit run --all-files` — exits 0.
</verification>

<success_criteria>
- ROADMAP SC5 is TRUE: every committed result across E1–E7 carries its seed, AquaCal version, git
  SHA, and environment, reusing `benchmark.json` rather than a hand-rolled sidecar, with a minimal
  sidecar for E3's tiers 1–2 — and it is asserted by a test, not by inspection.
- ROADMAP SC6 is TRUE: `experiments/README.md`'s provenance table maps every paper artifact to its
  producing script, its data file, and its figure generator, with downstream handoffs named rather
  than blank.
- CI's `experiments-smoke` job covers all seven scripts, so the suite cannot rot.
- No number in the manuscript's supplement traces to a TODO placeholder.
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-12-SUMMARY.md` when done.
Record: the `REQUIRED_ENVIRONMENT_KEYS` list as derived from `capture_environment`'s body; the
final `CSV_TO_RECORD` mapping; the measured runtime placed in each new README row and which plan
summary it came from; both regenerated derived asides verbatim with the source rows they were
computed from; and confirmation that E1's and E7's records were verified rather than re-run.
</output>
