---
phase: 19.2-experiment-execution-and-provenance
plan: 25
subsystem: docs
tags: [provenance, requirements-ledger, manuscript-findings, newton-diagnostic, memory-benchmark]

requires:
  - phase: 19.2 (waves 3-5: plans 19.2-21/22/23, plus 19.2-16/19/20 for the sidecars and D-32 instrumentation)
    provides: the measured runtimes, the E5/E6 provenance sidecars, and the batch-loop Newton measurement this plan writes prose from
provides:
  - "experiments/README.md's provenance-section claim is true of every committed artifact, with current runtimes and a pre/post-D-27 boundary subsection"
  - ".planning/REQUIREMENTS.md EXP-07/09/10/11 marked Complete in both the checkbox list and the status table"
  - "MANUSCRIPT-FINDINGS.md MF-01 resolved against the production Newton loop (D-32 option (c)); MF-02 re-measured against the redesigned E4 grid"
affects: []

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - experiments/README.md
    - .planning/REQUIREMENTS.md
    - .planning/MANUSCRIPT-FINDINGS.md

key-decisions:
  - "Cited plan 19.2-24's original two --check runs was withdrawn: 19.2-24-PLAN.md's own D-35 amendment removed those two --check invocations before this plan ran (19.2-24-SUMMARY.md does not yet exist), so the pre/post-D-27 subsection cites the full E1/E7 regeneration (MF-05/MF-06) instead, per D-35's own reasoning that regeneration is the stronger evidence"
  - "MF-02's already-present staleness notice (added 2026-07-31, before this plan) supplied the exact numbers the plan asked for; this plan's job was to formalize its Status line as RESOLVED and state explicitly that the gap INVERTED rather than narrowed or closed"

requirements-completed: [EXP-07, EXP-09, EXP-10, EXP-11]

duration: ~45min
completed: 2026-08-01
---

# Phase 19.2 Plan 25: Documentation and Provenance Close-Out Summary

**Made `experiments/README.md`'s provenance claim true of every committed artifact (E5 and E6
now have real four-field sidecars from wave 5), replaced every superseded runtime with this
phase's re-measurements, recorded the pre/post-D-27 boundary, closed the requirements ledger
(EXP-07/09/10/11 → Complete), and resolved MF-01 (Newton batch-loop measurement, D-32 option
(c)) and MF-02 (E4-vs-E2 memory gap inverted, not narrowed) in `MANUSCRIPT-FINDINGS.md`.**

No code was touched and no experiment was run. `git status --porcelain experiments/results/`
is empty throughout; `git diff --stat HEAD~2 HEAD` touches exactly the three files this plan is
scoped to. MF-05 was not touched.

## Task 1 — `experiments/README.md`

### Opening claim, old vs. new

**Old** (provenance-table intro, and again in the closing paragraph):
> "One row per artifact committed under `experiments/results/`. Every runtime below is a
> **measured** value from this phase's wave-3/4 execution, not a pre-run estimate."
>
> ...and further down: "`index_sensitivity.csv` and `generalization_sweep.csv` have no
> benchmark.json sidecar behind them; their own `seed` column is their seed provenance" — this
> was the sentence that narrowed the universal claim two paragraphs after asserting it, without
> saying so.

**New:**
> "One row per artifact committed under `experiments/results/`. Every artifact below now has a
> genuine four-field provenance record behind it — seed, AquaCal version, git SHA, and the
> Python/NumPy/SciPy/OS environment — whether that record is a `benchmark.json`-shaped file
> (E1, E2, E4, E7), a minimal calibration-free sidecar (E3's `e3_provenance.json`), or the
> provenance sidecars wave 5 added for E5 and E6 (`e5_provenance.json`, `e6_provenance.json`).
> This is a change from earlier in the phase: before wave 5 (plans 19.2-16/19), E5 and E6
> carried only a `seed` column with no version/SHA/environment fields, and this section's
> opening claim narrowed for them a few paragraphs down without saying so."

Verified true on disk before writing this, not assumed: `experiments/results/e5_provenance.json`
and `e6_provenance.json` both carry `schema_version`, `seed`, `solver_config.seed`, and a full
`environment` block (aquacal_version, git_sha, python/numpy/scipy versions, os); each of the
twelve `e6_configs/*.json` checkpoints carries the same three fields individually.

The closing paragraph (bottom of the README) was rewritten from the same "no sidecar" framing
into a three-shape taxonomy (`benchmark.json`-shaped / E3's minimal sidecar / E5's and E6's
minimal sidecars), explicitly stating the wave-5 change closed the gap.

### Runtimes replaced

| Row | Old value + citation | New value + citation |
|---|---|---|
| E4 grid | ~27 min (19.2-09-SUMMARY.md) | ~3.15 h summed per-cell wall-clock (~3.5 h with one killed/resumed launch); 19.2-21-SUMMARY.md |
| E5 band | ~22.5 min (19.2-13-SUMMARY.md) | ~21.1 min (1264 s); 19.2-23-SUMMARY.md |
| E6 sweep | ~33.5 min (19.2-11-SUMMARY.md) | ~107 min (a 3.2x increase, expected — D-29 geometry puts the board at realistic distance); 19.2-22-SUMMARY.md |

`grep -c "19.2-09-SUMMARY\|19.2-11-SUMMARY\|19.2-13-SUMMARY" experiments/README.md` → `0`
(verified). No superseded citation was kept anywhere in the file — the `--check` structural-
property paragraph, which previously cited `19.2-09-SUMMARY.md`, was reworded to cite
`19.2-21-SUMMARY.md` (the re-measured grid re-confirmed the same structural exit-1 behavior)
rather than retaining both.

E1, E2, E7 runtimes were left untouched — their numbers did not change this phase.

### E4 status paragraph

Old: "The nine-cell grid was run to completion in Phase 19.2 (all cells `status=ok`; see
`19.2-09-SUMMARY.md`)."

New: names the two library defects fixed before the re-run (flat NaN clamp, unvalidated
`cv2.solvePnP`), states all nine cells now converge with the measured optimality range
(9.1e-4 to 3.5e-2, from `benchmark_grid.csv`), and cites `19.2-21-SUMMARY.md`. Verified against
`experiments/results/benchmark_grid.csv` directly: all nine synthetic rows read `status=ok`,
none `status=failed` — the claim needed no softening, only re-citation and the optimality range.

### New provenance-table rows

Added `e5_provenance.json` and `e6_provenance.json` as their own rows (provenance-only,
"same run" as their respective CSVs). Updated the `e6_configs/*.json` row's description from
"checkpoint only" to "provenance records that double as resumability checkpoints" — verified
against one file on disk (`experiments/results/e6_configs/baseline.json`) that each carries
`schema_version`, `environment`, and `seed` alongside its `config`/`metrics` blocks.

### Pre/post-D-27 subsection (new)

Added directly after the E4 status paragraph. Content in brief:

- **Structurally unaffected (proven, not asserted):** E1, E2, E3, E5, E7. Cites three
  mechanical proofs from `19.2-18-SUMMARY.md` § Task 3 — two frozen-anchor exact-equality tests
  (`generate_real_rig_trajectory`, `create_scenario("realistic")` bit-identical pre/post D-27),
  a source-scanning grep-gate (no realistic-path caller passes `center`), and four passing
  `--check` reproductions (E1, E3, E5, E7). Adds wave 5's regeneration of E3/E5
  (`19.2-23-SUMMARY.md`) as a second, independent confirmation. Notes E1's and E7's CSVs did
  move this phase, but for an unrelated, documented reason (the PnP guard, `7e0cb90` — see
  MF-05/MF-06), regenerated across multiple seeds rather than `--check`ed.
- **Regenerated after D-27:** E4 (`19.2-21-SUMMARY.md`) and E6 (`19.2-22-SUMMARY.md`) — both
  postdate D-27 by construction since D-27 is bundled into the same commits (`d5d9dde`,
  `a2b244d`) that this wave's grid/sweep runs are built on.

**Deviation from the plan's own citation.** The plan's `<action>` text names "plan 19.2-24's two
final-tree `--check` runs" as part of this evidence. Checked `19.2-24-PLAN.md` directly: those
two `--check` invocations (`e1_refractive_comparison --check`, `e7_interface_ablation --check`)
were explicitly withdrawn under D-35 before this wave, replaced with full regeneration at
multiple seeds (E1: 5 seeds, E7: 10 seeds — MF-06/MF-05's own source). No `19.2-24-SUMMARY.md`
exists yet to confirm the two `--check` runs were ever executed, and the plan text that would
have run them was itself struck through. Rather than cite evidence that was withdrawn before it
could be produced, the subsection cites the regeneration that replaced it and states the
substitution explicitly, matching D-35's own stated reasoning (regeneration is the stronger
evidence). Not a Rule 4 architectural question — it is choosing the correct citable artifact
over a stale plan instruction, consistent with "do not restate a number you cannot cite to a
summary or an artifact."

### Verification

```
python -c "... assert all(k in t for k in ('e5_provenance.json','e6_provenance.json','D-27'))"
```
→ passes.

```
python -m pytest tests/unit/test_experiments_provenance.py -q
```
→ `92 passed, 6 skipped`.

## Task 2 — REQUIREMENTS.md and MANUSCRIPT-FINDINGS.md

### REQUIREMENTS.md

Marked EXP-07, EXP-09, EXP-10, EXP-11 `[x]` in the Phase 19.2 checkbox list and `Complete` in
the status table. `git diff .planning/REQUIREMENTS.md` (inspected before commit) shows only
status-marker changes — no requirement text was reworded.

```
python -c "... rows=[l for l in t.splitlines() if l.startswith('| EXP-')]; assert not any('Pending' in l for l in rows)"
```
→ passes; all eleven EXP rows read `Complete`.

### MF-01 — resolved

**Status** changed from "open — needs a prose edit, and its source is under review" to
"RESOLVED — the provenance caveat is closed; a prose edit is still needed, and the required
framing has changed."

Numbers added, each cited:

| Number | Source |
|---|---|
| Batch loop `ALL`: min 2, median 4.0, max 7, not converged 0, incidence 0.13–62.92 deg, max residual ~1.0e-9 m | `experiments/results/newton_iterations.csv`, `loop=batch`, `camera=ALL` row |
| Scalar loop `ALL`: identical min/median/max/not-converged | same CSV, `loop=scalar`, `camera=ALL` row (the pre-existing committed values, confirmed unchanged) |
| D-32 was decided 2026-07-29, option (c) chosen, options (a)/(b) rejected with the stated reasoning (per-point termination shifts results ~1e-16 through the bundle adjustment; docstring-only fix documents rather than fixes) | `.planning/phases/19.2-experiment-execution-and-provenance/19.2-GAP-CONTEXT.md` § D-32 |
| Option (c) implemented via an opt-in `return_diagnostics` flag on `_refractive_project_newton_batch`, off by default, proven bit-identical to pre-D-32 production output | `19.2-20-SUMMARY.md` |
| Regenerated with provenance by | `19.2-23-SUMMARY.md` ("tier-2 Newton numbers this run produced are in `newton_iterations.csv` for plan 25 to consume") |

**Restated framing:** under all-points termination, every point in a batch iterates until the
slowest point in that batch converges, so the production per-point cost is the batch's max (up
to 7 steps), not its median (4) — sharpening rather than replacing the original "understates the
tail" conclusion. The entry keeps its original discipline unchanged: the computed positions are
not known to be wrong (convergence is never in question in either loop; every residual sits at
tolerance), and this is a provenance correction, not an accuracy correction. Also rewrote the
entry's closing "Do not silently change the number to 7" sentence, which read as contradicting
the new framing — it now says report both quantities and state which is which, rather than
picking one number over the other.

### MF-02 — resolved

**Status** changed from "⚠ SUPERSEDED as to its numbers" to "RESOLVED — re-measured against the
current grid; still needs a prose edit, and the edit's direction has reversed."

The entry already carried a staleness notice (added 2026-07-31, before this plan ran) with the
exact numbers the plan's interfaces block asked for. Verified those numbers directly against
`experiments/results/benchmark_grid.csv`'s `cameras_16_frames_200` row rather than trusting the
prose: `peak_bytes_stage3_interface_optimization = 10731393024` (9.994 GiB) and
`peak_bytes_stage3_intrinsic_pass = 11215290368` (10.446 GiB, the overall peak) — both match the
entry's "9.99 GiB" / "10.45 GiB" figures exactly. E2's real-rig row in the same CSV:
`peak_bytes_stage3_intrinsic_pass = 10499325952` (9.777 GiB), matching the entry's 9.78 GiB.

This plan's addition: a new "Resolution" paragraph stating explicitly that the gap **inverted**,
not narrowed or closed — E4's 16×200 cell (10.45 GiB) now exceeds E2's real rig (9.78 GiB), so
the original "under-provision by ~3×" warning no longer applies in its original direction; if
anything a reader sizing off the grid today would slightly over-provision. The suggested framing
(present the grid as a scaling curve in problem size, not a per-camera capacity table) is noted
as unaffected by the inversion — it was already about not treating camera count as the sizing
variable.

### Verification

`git status --porcelain src/ experiments/` — empty throughout Task 2 (checked before and after
edits).

## Files not touched, deliberately

- `.planning/MANUSCRIPT-FINDINGS.md` MF-03 through MF-06 — untouched, per this plan's explicit
  constraint. Read them fully; found no factual error to report.
- `tests/unit/test_experiments_provenance.py`'s `CSV_TO_RECORD` dict still describes
  `index_sensitivity.csv`/`generalization_sweep.csv` as having "no benchmark.json backing" via
  comment strings that predate the wave-5 sidecars — these are human-readable error-message
  strings, not assertions the test enforces against file existence, and the suite passes with
  them as-is (92 passed, 6 skipped). Not in this plan's `files_modified`; left alone and noted
  here as a minor staleness for a future plan, not fixed.

## Self-Check: PASSED

- `experiments/README.md`, `.planning/REQUIREMENTS.md`, `.planning/MANUSCRIPT-FINDINGS.md` —
  all three exist and contain the edits described above (re-read after editing).
- Commit `183a65d` (`docs(19.2-25): make README's provenance claim true and its runtimes
  current`) — present in `git log`.
- Commit `a090a05` (`docs(19.2-25): close the requirements ledger and resolve MF-01/MF-02`) —
  present in `git log`.
