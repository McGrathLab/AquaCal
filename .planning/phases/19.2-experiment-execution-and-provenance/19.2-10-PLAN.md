---
phase: 19.2-experiment-execution-and-provenance
plan: 10
type: execute
wave: 3
depends_on: ["19.2-01", "19.2-07"]
files_modified:
  - experiments/e6_generalization_sweep.py
  - tests/unit/test_experiments_e6.py
autonomous: true
requirements: [EXP-10]

must_haves:
  truths:
    - "E6 sweeps refractive index, layout, and scale ONE AXIS AT A TIME through a common baseline, never a full factorial (D-11)"
    - "The baseline is the grid-family 12-camera scene built by E4's build_grid_scenario AND run at E4's own GRID_NORMAL_FIXED - identical construction AND identical solver configuration to E4's 12-camera cell - so the cross-experiment anchor is real rather than a lookalike (amended D-11; review H1, M4)"
    - "The scale axis scales the rig baseline with the working volume, so it measures scale rather than the ratio of working volume to a pinned 0.1 m camera spacing (review M2)"
    - "Every configuration writes its own small JSON before the CSV is assembled, so an interrupted sweep resumes per configuration instead of restarting the longest sweep in the phase from zero (review M7)"
    - "Every row carries its seed, so EXP-11 holds for this CSV too (review H5)"
    - "Every axis's value list contains the baseline value, so each axis genuinely passes through the baseline"
    - "Camera count is E4's axis and is NOT re-swept here (D-11)"
    - "The index axis varies the TRUE index, which only works because D-23 threads scenario.n_air/n_water into detection generation - without it every scenario is silently generated at 1.333 and the axis measures nothing"
    - "Degradation is reported as measured; no pass/fail verdict is baked into the CSV or implied by a column name (D-12)"
  artifacts:
    - path: "experiments/e6_generalization_sweep.py"
      provides: "three one-dimensional axes, tidy long-format rows, CLI"
      contains: "def build_axis_configurations"
      min_lines: 300
    - path: "tests/unit/test_experiments_e6.py"
      provides: "fast schema, baseline-membership and no-verdict tests"
  key_links:
    - from: "experiments/e6_generalization_sweep.py"
      to: "experiments.e4_benchmark_grid.build_grid_scenario"
      via: "shared grid-family scene builder; E6's baseline IS E4's 12-camera cell"
      pattern: "build_grid_scenario"
    - from: "experiments/e6_generalization_sweep.py"
      to: "aquacal.datasets.pipelines.compute_per_camera_errors"
      via: "parameter-recovery metrics computed by the library, not the script"
      pattern: "compute_per_camera_errors"
---

<objective>
Write `experiments/e6_generalization_sweep.py`: three independent one-dimensional axes — refractive
index, layout, and scale — each passing through a common grid-family 12-camera baseline, emitting
tidy long-format rows that carry which axis varied.

Purpose: EXP-10 answers R1.4, which asked for physical experiments across multiple tanks, media,
and layouts. Physical multi-medium rigs are out of scope; controlled synthetic sweeps isolate each
variable more cleanly, and the single physical rig (E2) anchors them to reality.
Output: one script and fast unit tests. The production sweep is plan 19.2-11.

**Three constraints that are settled, not open.** (0) E6 runs at E4's `GRID_NORMAL_FIXED`
(`False`, tilt-enabled), imported from `experiments.e4_benchmark_grid`, not at
`calibrate_synthetic`'s `True` default. The baseline is supposed to BE E4's 12-camera cell; if the
two run different solver configurations, `benchmark_grid.csv` and `generalization_sweep.csv` publish
two different `reprojection_rms` values for what the text calls one configuration — the D-16
violation applied to accuracy (review H1, M4). (1) The baseline is
`build_grid_scenario(n_cameras=12, ...)` — NOT `create_scenario("realistic")`, which calls a
different generator (`generate_real_rig_array`, a fixed real-rig layout with no `layout`
parameter). A `realistic` baseline would sit off the layout axis entirely, and no axis value would
equal the baseline. (2) E5 and E6 are fully independent scripts; the "run them together" idea was
evaluated with numbers and dropped — their bands coincide at exactly one configuration, on
different geometries, so sharing saves under 5% of wall-clock in exchange for coupling two scripts
and a cache-key correctness risk. Do not re-raise either.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** fork from the merged result of plans 19.2-01 and 19.2-07. Verify
`python -c "from experiments.e4_benchmark_grid import build_grid_scenario, GRID_BOARD_CONFIG"` and
`python -c "import inspect; from aquacal.datasets.pipelines import calibrate_synthetic as f; assert 'n_water=scenario.n_water' in inspect.getsource(f)"`
both exit 0 before starting. If the second fails, STOP — without D-23 the index axis is a silent
null result that looks like data.
**PYTHONPATH:** the editable install resolves to the main checkout, not a worktree. If working in
a worktree, `export PYTHONPATH="$(pwd)/src"` before running pytest or its passing tests are
meaningless.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-CONTEXT.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-PATTERNS.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-RESEARCH.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-SOURCE-BRIEF.md
@experiments/README.md

<interfaces>
<!-- Contracts the executor needs. Extracted live from the codebase 2026-07-28. -->

Shared scene builder (plan 19.2-07's output — import it, do not reimplement):
  build_grid_scenario(n_cameras, n_frames, seed, *, layout="grid", depth_range=None,
      xy_extent=None, spacing=None, height_above_water=None, n_water=1.333,
      name=GRID_SCENARIO_NAME) -> SyntheticScenario
      # its keyword axes exist precisely so E6 can vary exactly one at a time.
      # `spacing` was added for E6's scale axis (review M2): without it the axis varies the
      # working volume against a PINNED 0.1 m camera baseline, which changes a ratio, not a scale.
      # `name` is pinned away from "calibration" because pipelines.py branches on that string.
  GRID_BOARD_CONFIG, GRID_NOISE_STD, GRID_N_WATER,
  GRID_NORMAL_FIXED (False), GRID_SHARED_INTERFACE (True), GRID_SCENARIO_NAME   # same module

Underlying generator controls:
  generate_camera_array(..., layout: str = "grid", ...)   # accepts "grid", "line", "ring"
  generate_board_trajectory(..., depth_range=(0.3, 0.6), xy_extent=0.15, ...)
      # depth_range = working distance; xy_extent = tank footprint. This IS the scale axis's
      # control surface. Its `camera_positions` and `min_cameras_per_frame` arguments are
      # accepted but UNUSED by the body — a ring or line layout at wide baseline may therefore
      # produce a disconnected pose graph and fail Stage 2. Record it, do not chase it.

Calibration + metrics (all library-side; the script orchestrates only, P2):
  calibrate_synthetic(scenario, n_water, refine_intrinsics=True, seed=42,
      diagnostics_out=None, timings_out=None, memory_out=None)
      # after D-23, scenario.n_water is the index ground truth is GENERATED at.
      # For E6's index axis the calibration assumption MATCHES the truth: pass
      # n_water=scenario.n_water, because the claim under test is that an arbitrary index ratio
      # works, not that a mismatch is tolerable (that is E5's question).
  compute_per_camera_errors(result, ground_truth, *, gauge_correct_z=False)
      -> dict[str, PerCameraErrors]
      # keys per camera: focal_length_error_pct, z_position_error_mm, xy_position_error_mm,
      # k1_error, k2_error, gt_x_m, gt_y_m, gt_z_m, est_x_m, est_y_m, est_z_m,
      # reprojection_rms_px
      # NOTE (review M5): there is NO water_z error among these keys. `water_z_error_mm_mean` is
      # NOT available from this function and must be computed in the script — see Task 2.
  evaluate_calibration(calibration, detections, board, ...) -> HeldOutEvaluation
      # .reprojection, .reconstruction (a DistanceErrors with mean/rmse/signed_mean/
      # num_comparisons/percent_error), .board_poses, .num_frames
  generate_synthetic_detections(intrinsics, extrinsics, water_zs, board, board_poses,
      noise_std, n_air, n_water, seed)     # for the held-out set

E7's column-naming discipline to follow: `reprojection_rms_px_control` — never imply a verdict in
a column name.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The three axes, the shared baseline, and the per-configuration runner</name>
  <files>experiments/e6_generalization_sweep.py</files>
  <read_first>
    - `experiments/e4_benchmark_grid.py` — the FULL module as written by plan 19.2-07:
      `build_grid_scenario`'s signature and body, `GRID_BOARD_CONFIG`, `run_grid_cell`'s
      try/except status pattern, and its `main` control flow.
    - `experiments/e7_interface_ablation.py` lines 145-330 — the multi-arm direct-call structure,
      the `ArmResult` container shape, and the one-run-per-configuration discipline.
    - `src/aquacal/datasets/synthetic.py` — `generate_camera_array`'s three layouts and
      `generate_board_trajectory`'s `depth_range` / `xy_extent` defaults, which define what the
      scale axis's baseline value is.
    - `src/aquacal/datasets/pipelines.py` — `calibrate_synthetic` (post-D-23) and
      `compute_per_camera_errors`' full returned key list.
    - `19.2-CONTEXT.md` §§ D-10, amended D-11, D-12, D-23.
    - `19.2-SOURCE-BRIEF.md` § E6 — the three axes and the tidy long-format requirement.
  </read_first>
  <action>
Create `experiments/e6_generalization_sweep.py`. Module docstring: "E6: generalization sweep
(EXP-10)", the five-flag CLI contract, the emitted artifact `generalization_sweep.csv`, and an
explicit statement that camera count is E4's axis and is deliberately not re-swept here (D-11).
Define `CHECK_RTOL = 1e-6`.

Import `build_grid_scenario`, `GRID_BOARD_CONFIG`, `GRID_NORMAL_FIXED` and
`GRID_SHARED_INTERFACE` from `experiments.e4_benchmark_grid`. Do not reimplement the scene
construction and do not restate the tilt configuration as a local literal — E6's baseline IS E4's
12-camera cell, and that anchor only holds if it is literally the same function AND literally the
same solver configuration (review M4).

Declare the baseline and the three axes as module-level literals (P4 — the sweep is data, not
control flow):
  - `BASELINE_N_CAMERAS = 12`, `BASELINE_N_FRAMES = 100`, `BASELINE_LAYOUT = "grid"`,
    `BASELINE_N_WATER = 1.333`, `BASELINE_SCALE = "default"`.
  - `INDEX_AXIS_VALUES` — an explicit list spanning 1.33 to 1.55, roughly 0.03 steps, whose FIRST
    element is `BASELINE_N_WATER` so the axis passes through the baseline. Water through oil to
    resin; this axis demonstrates the arbitrary-index-ratio claim.
  - `LAYOUT_AXIS_VALUES = ["grid", "ring", "line"]` at a fixed 12 cameras — `grid` IS the
    baseline value, so the axis passes through it.
  - `SCALE_AXIS_VALUES` — an explicit list of three named
    `(label, depth_range, xy_extent, spacing)` settings, one of which is
    `("default", None, None, None)`, i.e. the generator's own defaults, which is the baseline. The
    other two vary tank footprint, working distance, AND camera baseline together in a documented
    direction (a smaller tank at a shorter working distance with proportionally closer cameras, and
    a larger tank at a longer distance with proportionally wider cameras).

    **`spacing` must vary with the other two, and the docstring must say why (review M2).**
    `generate_camera_array` pins `spacing` at 0.1 m and `generate_board_trajectory` defaults
    `xy_extent` to 0.15 m. Varying only `depth_range` and `xy_extent` changes the RATIO of working
    volume to camera baseline, not the scale — a reviewer asking "does this generalize to a larger
    tank" would reasonably expect the rig to grow with it, and the axis as originally written did
    not. Scale all three by the same factor per setting and state the factor in the label.
    Deliberately do NOT scale the board's `square_size`: a real calibration target does not shrink
    with the tank. Say so in the docstring, in those words, so the axis's meaning —
    "working volume and rig baseline scale together at fixed board size" — is on the record rather
    than inferred.

Write `build_axis_configurations() -> list[dict]`: expand the three axes into one flat list of
configuration dicts, each carrying `axis`, `axis_value`, `is_baseline`, and the four scene
keywords. The baseline configuration appears once per axis (so a figure module can facet by axis
and every facet has its anchor), but the underlying SCENE is identical across those three entries.
Give the baseline configuration a stable `config_key` so the runner can compute it ONCE and reuse
the result for all three baseline rows rather than running it three times.

Result: three distinct baseline rows over one shared run, plus the non-baseline values of each
axis. State the expected counts in the docstring so a drift is visible: with the axis lists above,
`len(build_axis_configurations())` is `len(INDEX_AXIS_VALUES) + len(LAYOUT_AXIS_VALUES) +
len(SCALE_AXIS_VALUES)`, and exactly 3 entries have `is_baseline` True.

Write `run_configuration(config: dict, seed: int, n_frames: int) -> dict`:
  - build the scenario via `build_grid_scenario(n_cameras=BASELINE_N_CAMERAS, n_frames=n_frames,
    seed=seed, layout=..., depth_range=..., xy_extent=..., spacing=..., n_water=...)`;
  - call `calibrate_synthetic(scenario, n_water=scenario.n_water, refine_intrinsics=True,
    seed=seed, normal_fixed=GRID_NORMAL_FIXED)` — the assumed index MATCHES the true index on every
    axis (a mismatch is E5's question, not E6's), and the tilt configuration MATCHES E4's. Passing
    `normal_fixed` is mandatory: `calibrate_synthetic` defaults it to `True`, so omitting it would
    make E6's "baseline" a different solver configuration from E4's 12-camera cell while both files
    publish a `reprojection_rms` for what the text calls one configuration (review M4);
  - after computing the row, write it as a small JSON under
    `out_dir / "e6_configs" / f"{config_key}.json"` BEFORE moving to the next configuration, and
    skip a configuration whose file already exists unless `force` is set. This mirrors E4's per-cell
    mechanism. E6 is both the largest sweep in the phase and the designated calendar-fallback
    casualty, so it is the one that most needs to survive an interruption; file-level CSV
    resumability alone would mean a full restart (review M7). The CSV is assembled from these files
    at the end, so a resumed run reuses completed configurations verbatim rather than recomputing
    them;
  - compute parameter recovery with `compute_per_camera_errors(result, scenario)` and aggregate to
    means across cameras — the library computes the per-camera errors, the script only aggregates;
  - generate a separate held-out detection set at the scenario's own indices and call
    `evaluate_calibration` for held-out reconstruction accuracy;
  - wrap in try/except and record `status` from the same three-value vocabulary E4 uses
    (`ok`, `failed`, `skipped_existing`), with a populated `status_reason` on failure. A ring or
    line layout may produce a disconnected pose graph; that is a recorded outcome.

**Assert nothing about the science and gate on no threshold.** E6's stated finding — that
parameter recovery and held-out accuracy hold across the sweep — can genuinely fail; a line layout
or n = 1.55 may degrade. A bounded, characterized degradation ("holds to n = 1.5, degrades beyond")
is a better answer to R1.4 than a claim of universal invariance a reviewer can poke. Interpretation
is manuscript work (D-12). Do not add a threshold, a pass/fail column, or a verdict-implying
column name.
  </action>
  <verify>
    <automated>python -c "
import experiments.e6_generalization_sweep as m
cfgs = m.build_axis_configurations()
assert m.BASELINE_N_WATER == m.INDEX_AXIS_VALUES[0]
assert m.BASELINE_LAYOUT in m.LAYOUT_AXIS_VALUES
assert any(s[0]=='default' for s in m.SCALE_AXIS_VALUES)
assert all(len(s)==4 for s in m.SCALE_AXIS_VALUES)
assert any(s[3] is not None for s in m.SCALE_AXIS_VALUES)
from experiments.e4_benchmark_grid import GRID_NORMAL_FIXED
assert m.GRID_NORMAL_FIXED is GRID_NORMAL_FIXED is False
assert sum(1 for c in cfgs if c['is_baseline']) == 3
assert {c['axis'] for c in cfgs} == {'index','layout','scale'}
assert len(cfgs) == len(m.INDEX_AXIS_VALUES)+len(m.LAYOUT_AXIS_VALUES)+len(m.SCALE_AXIS_VALUES)
assert len({c['config_key'] for c in cfgs if c['is_baseline']}) == 1
print(len(cfgs), 'configurations')"</automated>
  </verify>
  <acceptance_criteria>
    - `experiments/e6_generalization_sweep.py` contains `def build_axis_configurations(` and `def run_configuration(`.
    - `grep -c "from experiments.e4_benchmark_grid import" experiments/e6_generalization_sweep.py` returns exactly 1, and the imported names include `build_grid_scenario`.
    - `grep -c "generate_camera_array\|generate_board_trajectory" experiments/e6_generalization_sweep.py` returns 0 — the scene is built only through the shared builder.
    - `grep -c "create_scenario\|generate_real_rig_array" experiments/e6_generalization_sweep.py` returns 0 — the baseline is the grid-family scene, not `realistic` (amended D-11).
    - `python -c "import experiments.e6_generalization_sweep as m; assert m.BASELINE_N_WATER in m.INDEX_AXIS_VALUES and m.BASELINE_LAYOUT in m.LAYOUT_AXIS_VALUES"` exits 0 — every axis passes through the baseline.
    - `python -c "import experiments.e6_generalization_sweep as m; assert min(m.INDEX_AXIS_VALUES) <= 1.333 and max(m.INDEX_AXIS_VALUES) >= 1.55"` exits 0.
    - The three baseline entries share one `config_key`, so the baseline scene is computed once.
    - `grep -ci "n_cameras.*axis\|camera_count_axis" experiments/e6_generalization_sweep.py` returns 0 — camera count is not re-swept.
    - `grep -c "n_water=scenario.n_water" experiments/e6_generalization_sweep.py` returns at least 1 — the assumed index matches the true index.
    - `grep -v '^\s*#' experiments/e6_generalization_sweep.py | grep -c 'normal_fixed=GRID_NORMAL_FIXED'` returns at least 1, and `grep -c 'normal_fixed=True\|normal_fixed=False' experiments/e6_generalization_sweep.py` returns 0 — the tilt configuration is imported from E4, never restated (review M4).
    - `python -c "import experiments.e6_generalization_sweep as m; assert all(len(s)==4 for s in m.SCALE_AXIS_VALUES); assert sum(1 for s in m.SCALE_AXIS_VALUES if s[3] is not None) == 2"` exits 0 — two of the three scale settings vary camera spacing; the baseline leaves it at the generator default (review M2).
    - `grep -c "e6_configs" experiments/e6_generalization_sweep.py` returns at least 1 — per-configuration resumability exists (review M7).
    - `python -c "import experiments.e6_generalization_sweep as m; d=m.build_grid_scenario.__doc__ or ''; import experiments.e4_benchmark_grid as e4; assert 'square_size' in (e4.build_grid_scenario.__doc__ or '')"` exits 0 — the fixed-board-size rationale is documented at the shared builder.
    - `ruff check experiments/e6_generalization_sweep.py && ruff format --check experiments/e6_generalization_sweep.py` exits 0.
  </acceptance_criteria>
  <done>Three one-dimensional axes are declared as literals, each containing the baseline value; the baseline is E4's 12-camera grid-family scene via the shared builder AND at E4's imported tilt configuration, computed once; the scale axis scales the rig baseline with the working volume at fixed board size; every configuration checkpoints to its own JSON so the sweep resumes per configuration; camera count is not re-swept; and no threshold or verdict exists anywhere in the module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tidy long-format schema, CLI, smoke mode, and unit tests</name>
  <files>experiments/e6_generalization_sweep.py, tests/unit/test_experiments_e6.py</files>
  <behavior>
    - `test_e6_row_schema`: the row builder over a hand-built result fixture returns exactly
      `E6_COLUMNS`, in order, and `axis` is one of `index`, `layout`, `scale`.
    - `test_every_axis_passes_through_baseline`: for each axis, the emitted configuration list
      contains exactly one `is_baseline=True` entry whose `axis_value` equals that axis's baseline
      value.
    - `test_baseline_scene_is_shared`: the three baseline configurations share one `config_key`, so
      the runner computes the baseline scene once rather than three times.
    - `test_no_verdict_column`: no name in `E6_COLUMNS` matches `pass`, `fail`, `verdict`,
      `acceptable`, `holds`, or `degraded`.
    - `test_water_z_error_helper`: the water_z-error helper, given a hand-built estimate/truth pair
      with a known 3 mm discrepancy on one camera and 0 on the rest, returns the expected
      millimetre mean — so the column has a tested origin rather than an inline expression
      (review M5).
    - `test_tilt_configuration_matches_e4`: the module's imported `GRID_NORMAL_FIXED` is
      `experiments.e4_benchmark_grid.GRID_NORMAL_FIXED` and is `False`, and every emitted row
      carries that value in its `normal_fixed` column (review M4).
    - `test_seed_column_populated`: every row of a hand-built frame carries a non-null `seed`.
    - `test_camera_count_not_swept`: every configuration's `n_cameras` equals
      `BASELINE_N_CAMERAS` — camera count is E4's axis (D-11).
    - `test_status_vocabulary`: the row builder's `status` values come from
      `{ok, failed, skipped_existing}`.
  </behavior>
  <read_first>
    - `tests/unit/test_experiments_e1.py` — the FULL file; the hand-built-fixture, no-calibration,
      nothing-slow discipline for this family.
    - `tests/unit/test_experiments_e4.py` (plan 19.2-07's output) — the sibling schema-test shape
      to mirror, especially how a hand-built record fixture is fed to the row builder.
    - `experiments/e7_interface_ablation.py` lines 559-617 — `_run_check`, `build_arg_parser`,
      `main`, and the smoke-mode `tempfile.TemporaryDirectory` branch.
    - `experiments/_io.py` — `write_experiment_csv`'s `key_columns` requirement.
    - `19.2-CONTEXT.md` § D-12.
  </read_first>
  <action>
Declare `E6_COLUMNS` as an explicit module-level list in exactly this order:

`axis`, `axis_value`, `model`, `config_key`, `is_baseline`, `seed`,
`n_cameras`, `n_frames`, `layout`, `n_true`, `n_assumed`, `normal_fixed`,
`depth_range_min`, `depth_range_max`, `xy_extent`, `spacing`,
`status`, `status_reason`,
`reprojection_rms_px`, `reconstruction_mae_mm`, `reconstruction_rmse_mm`, `signed_mean_mm`,
`focal_error_pct_mean`, `xy_position_error_mm_mean`, `z_position_error_mm_mean`,
`water_z_error_mm_mean`, `num_comparisons`, `num_frames`

— 28 columns — and `E6_KEY_COLUMNS = ["axis", "axis_value"]`.

`seed` is on every row because EXP-11 and ROADMAP SC5 require every committed result to carry its
seed and this CSV has no `benchmark.json` behind it (review H5). `normal_fixed` is on every row so
the committed file self-describes the solver configuration, which is what makes the "E6's baseline
IS E4's 12-camera cell" claim checkable from the artifacts rather than from prose (review H1, M4).
`spacing` records the scale axis's third scaled quantity (review M2).

**`water_z_error_mm_mean` has no library source and must be computed here — this was previously
unspecified (review M5).** `compute_per_camera_errors` returns
`focal_length_error_pct`, `z_position_error_mm`, `xy_position_error_mm`, `k1_error`, `k2_error`,
the gt/est positions, and `reprojection_rms_px`; there is no water_z error among them. Compute it in
the script as the mean over cameras of `abs(estimated_water_z - scenario.water_zs[cam]) * 1000`,
taking the estimate from the `CalibrationResult`'s own interface field and the truth from the
scenario. Write a short module-level helper with a docstring stating the units (millimetres) and
naming both inputs, so the quantity has one origin and is not hand-rolled twice. Index error couples
directly into recovered interface depth, so this is the single most diagnostic column on the index
axis; leaving it under-specified risks it landing null or inconsistent.

`model` carries the literal `refractive` on every row — the column exists so the long format
matches the requirement's "one row per (axis, value, model)" shape and so a future non-refractive
arm can be added without reshaping the committed CSV. `axis_value` is a string so the three axes'
heterogeneous values (a float index, a layout name, a scale label) share one column — this is what
makes the format tidy and facetable by a single figure module.

Metric columns are null for any row whose `status` is not `ok`.

Add `build_arg_parser()` (five shared flags, no script-local flags) and `main(argv=None) -> int`
copying E7's control flow: `--check` never writes and compares against the committed
`generalization_sweep.csv` at `CHECK_RTOL`; `--smoke` uses a throwaway
`tempfile.TemporaryDirectory` when `--out` is at its parser default, runs a reduced set (the
baseline plus one non-baseline value from each axis) at a small `n_frames` with
`refine_intrinsics=False`, and always writes with `force=True`; the normal branch runs the full
configuration list. `--smoke` must exercise the `e6_configs/` per-configuration write and its
skip-if-exists path, so CI proves the resumability mechanism rather than only the happy path. Log each configuration's `axis`, `axis_value`, `status`, and
`reprojection_rms_px` at INFO so a background run's log is followable.

Create `tests/unit/test_experiments_e6.py` with the six behaviors above. Hand-built fixtures only;
nothing marked slow; no test may call `calibrate_synthetic` or `optimize_interface`.

Do NOT run the full sweep in this plan. That is plan 19.2-11.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e6.py -x -q && python -m experiments.e6_generalization_sweep --smoke</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_experiments_e6.py -x -q` exits 0 and collects at least 9 tests.
    - `python -m pytest tests/unit/test_experiments_e6.py -m slow -q` collects 0 tests.
    - `python -c "import experiments.e6_generalization_sweep as m; assert len(m.E6_COLUMNS)==28 and len(set(m.E6_COLUMNS))==28 and m.E6_COLUMNS[:2]==['axis','axis_value'] and m.E6_KEY_COLUMNS==['axis','axis_value']; assert {'seed','normal_fixed','spacing'} <= set(m.E6_COLUMNS)"` exits 0.
    - `python -c "import experiments.e6_generalization_sweep as m, re; assert not any(re.search(r'pass|fail|verdict|acceptable|holds|degraded', c) for c in m.E6_COLUMNS)"` exits 0.
    - `python -c "import experiments.e6_generalization_sweep as m; assert all(c['n_cameras']==m.BASELINE_N_CAMERAS for c in m.build_axis_configurations())"` exits 0.
    - `grep -c "calibrate_synthetic\|optimize_interface" tests/unit/test_experiments_e6.py` returns 0.
    - `python -m experiments.e6_generalization_sweep --smoke` exits 0 in under 300 seconds and leaves `git status --porcelain experiments/results/` empty.
    - `python -m experiments.e6_generalization_sweep --check --force` exits non-zero with a usage error.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 with at least 874 passed.
    - `ruff check experiments/ tests/ && ruff format --check experiments/ tests/` exits 0.
  </acceptance_criteria>
  <done>E6 emits a 28-column tidy long format keyed on (axis, axis_value) with `model`, `seed`, `normal_fixed` and `spacing` columns and no verdict column, computes `water_z_error_mm_mean` through one tested helper, honours the five-flag contract, and passes nine fast unit tests including the baseline-membership, tilt-configuration-matches-E4, and camera-count-not-swept guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem write | `generalization_sweep.csv` under a user-supplied `--out`. Controls inherited from `_io.py`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.2-41 | Repudiation | The index axis silently measures nothing because ground truth is generated at 1.333 for every value, producing a null result that looks like data | mitigate | `execution_context` requires verifying D-23's fix landed before starting and says STOP if it has not; a grep gate asserts `n_water=scenario.n_water` appears at the calibration call. |
| T-19.2-42 | Repudiation | The baseline is `create_scenario("realistic")`, which sits off the layout axis, so no axis value equals the baseline and the "one axis at a time through a common baseline" design is broken | mitigate | Grep gate asserts `create_scenario` and `generate_real_rig_array` do not appear; a unit test asserts each axis's value list contains the baseline value. |
| T-19.2-43 | Repudiation | The scene is reimplemented locally, so E6's baseline and E4's 12-camera cell drift apart and the cross-experiment anchor becomes a coincidence | mitigate | Grep gates assert the generators are not called directly and that `build_grid_scenario` is imported from E4's module. |
| T-19.2-43b | Repudiation | E6's baseline shares E4's SCENE but not E4's SOLVER CONFIGURATION, so `benchmark_grid.csv` and `generalization_sweep.csv` publish two different accuracy numbers for what the text calls one configuration | mitigate | `GRID_NORMAL_FIXED` is imported from E4 and passed explicitly; a grep gate rejects any local `normal_fixed=True/False` literal; `test_tilt_configuration_matches_e4` asserts the import identity; and plan 19.2-11 cross-checks the baseline row against `benchmark_grid.csv`'s 12/100 cell (review M4). |
| T-19.2-43c | Repudiation | The "scale" axis varies working volume against a pinned 0.1 m camera baseline, so it measures a ratio while the manuscript calls it scale | mitigate | `spacing` scales with `depth_range`/`xy_extent`; a criterion asserts two of three settings vary it; the docstring states that board `square_size` is deliberately fixed and names what the axis therefore measures (review M2). |
| T-19.2-43d | Denial of Service | The phase's longest sweep has no per-configuration resume, so any interruption costs a full restart of the one run the calendar can least afford to repeat | mitigate | Each configuration checkpoints to `results/e6_configs/{config_key}.json` before the next begins, with skip-unless-`force`, mirroring E4's per-cell mechanism; `--smoke` exercises the skip path (review M7). |
| T-19.2-44 | Repudiation | A pass/fail threshold is baked into the CSV, so the swept range or the verdict is effectively chosen after seeing the data | mitigate | D-12 forbids it; a unit test asserts no `E6_COLUMNS` name matches a verdict-implying pattern, and the task text forbids any threshold or assertion about the science. |
| T-19.2-45 | Repudiation | Camera count is re-swept here, re-treading E4's grid under a different label and giving those numbers two origins | mitigate | A unit test asserts every configuration's `n_cameras` equals the baseline; a grep gate rejects a camera-count axis. |
| T-19.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. `19.2-RESEARCH.md` § Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
1. `python -m pytest tests/unit/test_experiments_e6.py -q` — exits 0.
2. `python -m experiments.e6_generalization_sweep --smoke` — exits 0, `experiments/results/` untouched.
3. The automated block in Task 1 exits 0 and prints the configuration count.
4. `grep -c "create_scenario\|generate_camera_array" experiments/e6_generalization_sweep.py` — returns 0.
5. `python -m pytest tests/ -m "not slow" -q` — exits 0, at least 874 passed.
6. `ruff check . && ruff format --check experiments/ tests/` — exits 0.
</verification>

<success_criteria>
- Three one-dimensional axes (index 1.33→1.55, layout grid/ring/line at fixed 12 cameras, and a
  scale axis that scales working volume and rig baseline together at fixed board size) each pass
  through a common baseline that is literally E4's 12-camera grid-family cell, at E4's own tilt
  configuration.
- The sweep resumes per configuration, and `water_z_error_mm_mean` has one tested origin.
- Tidy long-format rows carry `axis`, `axis_value`, and `model`, keyed on (axis, axis_value).
- The index axis varies true ground truth, which requires and verifies D-23's fix.
- Nothing in the script asserts, thresholds, or names a verdict (D-12).
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-10-SUMMARY.md` when done.
Record: the verbatim `INDEX_AXIS_VALUES`, `LAYOUT_AXIS_VALUES`, and `SCALE_AXIS_VALUES` including
each scale setting's `spacing`; the total configuration count and how many distinct scenes it
implies once the shared baseline is deduped; the final 28-entry `E6_COLUMNS`; the exact expression
used for `water_z_error_mm_mean` and its units; the `e6_configs/` path shape and skip semantics; and
the smoke-mode configuration subset with its measured runtime.
</output>
