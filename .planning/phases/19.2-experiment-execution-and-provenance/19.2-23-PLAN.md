---
phase: 19.2-experiment-execution-and-provenance
plan: 23
type: execute
wave: 5
gap_closure: true
depends_on: ["19.2-19", "19.2-20", "19.2-22"]
files_modified:
  - experiments/results/index_sensitivity.csv
  - experiments/results/e5_provenance.json
  - experiments/results/code_constants.csv
  - experiments/results/cpr_grouping.csv
  - experiments/results/cpr_derived_values.tex
  - experiments/results/newton_iterations.csv
  - experiments/results/e3_provenance.json
autonomous: true
requirements: [EXP-07, EXP-09, EXP-11]

must_haves:
  truths:
    - "E5's band is re-run FOR PROVENANCE ONLY -- to make `index_sensitivity.csv` and its sidecar describe the same run, which is the only way provenance can be added to an artifact"
    - "E5's numbers are deterministic and seeded and its `--check` exits 0 today, so a moved science column in this re-run is a DETERMINISM DEFECT AND A HARD STOP, not expected churn. `E5_COLUMNS` was deliberately left unchanged by plan 19.2-19 so this comparison is unambiguous"
    - "The same hard stop applies to E3's `code_constants.csv` and `cpr_grouping.csv`: both must reproduce byte-for-byte"
    - "`newton_iterations.csv` is the ONE artifact whose schema legitimately changes, because D-32 rewired tier 2 to measure the batch loop the optimizer actually runs. Its scalar-labelled rows must still reproduce the committed values -- that is the determinism check inside the schema change"
    - "The regenerated `newton_iterations.csv` states in its provenance which loop it measured, closing the traceability clause that failed verification: a number citing the wrong implementation is worse than an absent one"
    - "Nothing else ran on the box during these runs (review H4)"
    - "E2 is NOT re-run. E1 and E7 are NOT re-run"
  artifacts:
    - path: "experiments/results/index_sensitivity.csv"
      provides: "the index band, byte-identical, now with a provenance sidecar describing its run"
    - path: "experiments/results/e5_provenance.json"
      provides: "E5's seed, version, git SHA, environment and run configuration"
    - path: "experiments/results/newton_iterations.csv"
      provides: "Newton convergence measured on the production batch loop"
    - path: "experiments/results/e3_provenance.json"
      provides: "E3's environment-only sidecar, regenerated with this run"
  key_links:
    - from: "experiments/results/index_sensitivity.csv"
      to: "experiments/results/e5_provenance.json"
      via: "written by the same _run_full invocation"
      pattern: "e5_provenance"
    - from: "experiments/results/newton_iterations.csv"
      to: "the batch Newton loop"
      via: "E3 tier 2 rewired by plan 19.2-20"
      pattern: "batch"
---

<objective>
Regenerate E5's band and E3's fast tiers so every committed artifact carries provenance that
describes the run that produced it, and so `newton_iterations.csv` finally measures the loop the
optimizer runs.

Purpose: EXP-07, EXP-09, EXP-11 — and both truths that failed phase verification. E5's committed CSV
carried a `seed` column and nothing else: no AquaCal version, no git SHA, no environment. Provenance
can only be added to an artifact by **regenerating it**, which is why E5 is re-run (~22 min) despite
its numbers being untouched by every geometry decision in this phase. `newton_iterations.csv`
published `n_not_converged = 0` as a property of a loop the optimizer never runs; plan 19.2-20 fixed
the measurement and this plan publishes it.

**This plan runs ALONE on the machine**, in its own wave. Nothing else — no experiment, no test
suite — may run while it does (review H4).

**Two hard stops, stated up front so they are not discovered as surprises:**

1. **E5.** Its numbers are deterministic and seeded, its `--check` exits 0 today, and plan 19.2-19
   deliberately left `E5_COLUMNS` unchanged so this comparison is a clean determinism proof. If any
   science column moves, that is a determinism defect — stop and report. Do not rationalise it as a
   consequence of the phase's geometry work: E5 builds its scenario from
   `generate_real_rig_trajectory`, which plan 19.2-18 did not modify, and its immunity was asserted
   mechanically by that plan's `--check`.
2. **E3.** `code_constants.csv` and `cpr_grouping.csv` must reproduce byte-for-byte. Only
   `newton_iterations.csv` may change, and only in the way D-32 specifies.

**E2 must not be re-run at all.** E1 and E7 are not re-run either — their reproduction was proven
mechanically in plan 19.2-18's containment gate.

Output: `index_sensitivity.csv` + `e5_provenance.json`; `code_constants.csv`, `cpr_grouping.csv`,
`cpr_derived_values.tex`, `newton_iterations.csv` + `e3_provenance.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** fork from the merged result of wave 4. Before launching, verify:
- `git status --porcelain` is empty;
- `python -m pytest tests/ -m "not slow" -q` exits 0 (record the counts) — **before the runs, never
  during them**;
- `python -c "import experiments.e5_index_sensitivity as m; import pandas as pd; assert list(pd.read_csv('experiments/results/index_sensitivity.csv').columns) == m.E5_COLUMNS"` exits 0;
- `python -c "import experiments.e5_index_sensitivity as m; p=m._default_metrics_path(); assert p.is_absolute() and p.exists()"` exits 0 — the anchored noise-floor path from plan 19.2-19, without which two yardstick columns publish null;
- `python -m experiments.e5_index_sensitivity --smoke` and
  `python -m experiments.e3_derived_quantities --smoke` both exit 0.
If any fails, re-fork.
**PYTHONPATH:** in a worktree, `export PYTHONPATH="$(pwd)/src"` and confirm
`python -c "import aquacal; print(aquacal.__file__)"` resolves inside your tree BEFORE launching.
**Run from the repository root**, always `python -u`, in the background, log OUTSIDE the repository.
E5's committed CSV was produced from the repository root; running from elsewhere is the one way to
make a determinism comparison meaningless for reasons that have nothing to do with determinism.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-GAP-CONTEXT.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-VERIFICATION.md
@.planning/MANUSCRIPT-FINDINGS.md
@experiments/README.md
@CLAUDE.md

<interfaces>
E5: `python -u -m experiments.e5_index_sensitivity --force`. Sweeps `N_ASSUMED_BAND` (11 points, a
fine band around `N_TRUE = 1.333`) at `n_frames=30` on real-rig geometry, writing
`index_sensitivity.csv` (11 rows, `E5_KEY_COLUMNS = ["n_assumed"]`) and — after plan 19.2-19 —
`e5_provenance.json`. Measured runtime ~22.5 min. `--check` re-runs the whole band and compares;
after plan 19.2-19 it also checks the baseline exists first.

E3: `python -u -m experiments.e3_derived_quantities --force`. Three tiers, ~10 s total, writing
`code_constants.csv` (9 rows, all `pass_fail=PASS`), `newton_iterations.csv`,
`cpr_grouping.csv` (12 rows = 6 configs × 2 interface modes), `cpr_derived_values.tex` and
`e3_provenance.json`. Tier 2 was rewired by plan 19.2-20 to measure the batch loop with the scalar
loop retained for comparison — read that plan's summary for the new `NEWTON_COLUMNS` and the new
expected row count.

Committed tier-2 measurement to compare the scalar rows against (13 rows, 104,052 points): min 2,
median 4.0 on every camera, max 7 (6–7 per camera), `n_not_converged = 0`, incidence 0.13°–62.92°,
max residual ~1e-9 m.

`MANUSCRIPT-FINDINGS.md` MF-01 carries a "resolve before citing" caveat that this run's measurement
resolves. **Do not edit MF-01 here** — plan 19.2-25 owns that file in wave 6. Record the numbers it
needs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run E5's band for provenance, and prove the science did not move</name>
  <files>experiments/results/index_sensitivity.csv, experiments/results/e5_provenance.json</files>
  <read_first>
    - `experiments/e5_index_sensitivity.py` — `_run_full`, `run_band`, `run_index_point`,
      `load_holdout_floor_pct`, and the provenance sidecar plan 19.2-19 added.
    - Plan `19.2-19-SUMMARY.md` — the sidecar's key set, the captured configuration values
      (especially `refine_intrinsics`, which the production band ran as `False`), and the
      confirmation that nothing changes when run from the repository root.
    - `experiments/results/index_sensitivity.csv` — the committed baseline, and
      `git show HEAD:experiments/results/index_sensitivity.csv` for the comparison.
    - `19.2-GAP-CONTEXT.md` § "Out of scope", the 2026-07-29 CORRECTION paragraph — why E5 IS re-run
      and why a moved column is a hard stop.
    - `CLAUDE.md` § "Always run calibration unbuffered".
  </read_first>
  <action>
Confirm quiescence and record the process listing verbatim. Capture the committed CSV first —
`git show HEAD:experiments/results/index_sensitivity.csv > <scratch>/index_sensitivity.committed.csv`
— so you have an off-tree copy to diff against after the regeneration overwrites it.

Launch from the repository root, unbuffered, in the background:
`python -u -m experiments.e5_index_sensitivity --force`, log outside the repository. Record the
command verbatim and the measured wall-clock against the previous ~22.5 minutes.

When it completes, diff the fresh CSV against the captured copy. **Every science column must be
identical**, and `E5_COLUMNS` must be unchanged. Report the diff result explicitly, whichever way it
goes:

  - Byte-identical: state that, and state what it proves — an 11-point real-rig band reproduced
    exactly across a phase that changed the library's board-trajectory default, which is the
    strongest available evidence that D-27's containment held on E5's path.
  - Any moved value: **stop and report to the orchestrator** with the column, the row, both values
    and the relative difference. Do not widen a tolerance, do not re-run hoping for a different
    result, and do not attribute it to the geometry work without evidence — E5 uses
    `generate_real_rig_trajectory`, which was not modified.

Pay particular attention to `holdout_floor_pct` and `scale_bias_over_floor`. If they came back null,
the anchored path from plan 19.2-19 is not in effect or `real_rig_metrics.json` is missing — that is a
configuration failure, not a determinism defect, and it must not be committed as a result.

Confirm `e5_provenance.json` was written beside the CSV and carries all four EXP-11 fields plus the
run configuration, and that its `environment` reports the same machine as E2's committed
`benchmark.json`.
  </action>
  <verify>
    <automated>python -c "
import pandas as pd, json, pathlib
import experiments.e5_index_sensitivity as m
df = pd.read_csv('experiments/results/index_sensitivity.csv')
assert list(df.columns) == m.E5_COLUMNS, list(df.columns)
assert len(df) == 11, len(df)
assert df.notna().all().all(), df.isna().sum()
rec = json.loads(pathlib.Path('experiments/results/e5_provenance.json').read_text())
assert rec['experiment'] == 'e5'
assert rec['solver_config']['seed'] is not None
for k in ('aquacal_version','git_sha','python_version','numpy_version','scipy_version'):
    assert k in rec['environment'], k
ref = json.loads(pathlib.Path('experiments/results/benchmark.json').read_text())['environment']
assert rec['environment']['cpu_model'] == ref['cpu_model']
assert rec['environment']['ram_total_bytes'] == ref['ram_total_bytes']
print(df.to_string())"</automated>
  </verify>
  <acceptance_criteria>
    - The recorded launch command contains `python -u`, `-m experiments.e5_index_sensitivity` and
      `--force`, run from the repository root in the background with output redirected outside it.
    - The summary records the quiescence check and the measured wall-clock against ~22.5 minutes.
    - The fresh CSV was diffed against the pre-run committed copy and the result is stated
      explicitly. If any science column moved, the orchestrator was notified with column, row, both
      values and the relative difference, and nothing was rationalised away.
    - `index_sensitivity.csv` has 11 rows, columns exactly `E5_COLUMNS`, and zero nulls — in
      particular `holdout_floor_pct` and `scale_bias_over_floor` are populated.
    - `e5_provenance.json` exists, carries `experiment == "e5"`, `schema_version`, `seed`,
      `solver_config["seed"]`, all five required environment keys, and the run configuration
      including `refine_intrinsics`, `normal_fixed` and the calibration frame count.
    - The sidecar's `environment` reports the same `cpu_model` and `ram_total_bytes` as
      `experiments/results/benchmark.json` (constraint 11).
  </acceptance_criteria>
  <done>`index_sensitivity.csv` and its sidecar describe the same run on the same machine, every EXP-11 field is present, and the band's reproduction across a phase that changed a library default has been checked rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run E3's tiers, hold two CSVs byte-identical, and publish the batch-loop Newton measurement</name>
  <files>experiments/results/code_constants.csv, experiments/results/cpr_grouping.csv, experiments/results/cpr_derived_values.tex, experiments/results/newton_iterations.csv, experiments/results/e3_provenance.json</files>
  <read_first>
    - `experiments/e3_derived_quantities.py` — `build_newton_iterations_df` and
      `_summarize_newton_records` as rewired by plan 19.2-20, `build_provenance_sidecar`, and
      `_run_full`.
    - Plan `19.2-20-SUMMARY.md` — the batch diagnostic's name and return shape, the new
      `NEWTON_COLUMNS`, the new expected row count, and the observed scalar-vs-batch iteration
      difference on a shared fixture.
    - `experiments/results/newton_iterations.csv`, `code_constants.csv`, `cpr_grouping.csv` — the
      committed baselines, captured off-tree before the run.
    - `.planning/MANUSCRIPT-FINDINGS.md` MF-01 in full — the "resolve before citing" caveat this
      measurement resolves, and the framing note that under all-points termination per-point cost
      behaves like the tail, not the median.
    - `19.2-GAP-CONTEXT.md` § D-32 "Acceptance".
  </read_first>
  <action>
Capture all three committed CSVs off-tree first
(`git show HEAD:experiments/results/<name> > <scratch>/<name>.committed`), then run from the
repository root, unbuffered: `python -u -m experiments.e3_derived_quantities --force`. It takes
roughly ten seconds; still record the wall-clock and keep the box quiet, since it shares a wave with
E5's twenty-two minutes.

Then compare, with different expectations per file:

  - **`code_constants.csv` and `cpr_grouping.csv` must be byte-identical.** Both are derived from
    source constants and from `pack_params`/`build_structural_column_groups` over a structural
    detection pattern; nothing in this phase touched either. A difference here is a hard stop —
    report it with the differing rows rather than committing the new file. `code_constants.csv` must
    still be 9 rows, all `pass_fail=PASS`; `cpr_grouping.csv` must still be 12 rows.
  - **`newton_iterations.csv` legitimately changes schema** — this is the one artifact in this wave
    whose header may move, because D-32 rewired tier 2 onto the batch loop. Its **scalar-labelled
    rows must still reproduce the committed values**: min 2, median 4.0 per camera, max 7,
    `n_not_converged = 0`, incidence 0.13°–62.92°, max residual ~1e-9 m, over 104,052 points. That
    is the determinism check hiding inside the schema change; a scalar row that moved is a hard stop
    exactly as E5's would be.
  - **`cpr_derived_values.tex`** should follow `cpr_grouping.csv`; if it changed while the CSV did
    not, report that.

Record the batch-loop measurement in full, since it is the number the supplement will cite and the
number `MANUSCRIPT-FINDINGS.md` MF-01 needs (plan 19.2-25 does the write-up; you supply the data):
the per-point iteration distribution on the batch path, the batch `n_not_converged`, how many loop
iterations the batch actually executed before its all-points break, and the difference from the
scalar rows. State plainly what the comparison shows about the two loops' semantics rather than
asserting the old number was wrong — the computed positions are almost certainly correct, and this is
a provenance correction, not a numerical one.

Confirm `e3_provenance.json` was rewritten by this run and carries the environment of the tree the
artifacts came from.
  </action>
  <verify>
    <automated>python -c "
import pandas as pd, json, pathlib
import experiments.e3_derived_quantities as m
cc = pd.read_csv('experiments/results/code_constants.csv')
assert len(cc) == 9 and set(cc['pass_fail']) == {'PASS'}, cc.to_string()
cpr = pd.read_csv('experiments/results/cpr_grouping.csv')
assert len(cpr) == 12, len(cpr)
nw = pd.read_csv('experiments/results/newton_iterations.csv')
assert list(nw.columns) == m.NEWTON_COLUMNS, list(nw.columns)
print(nw.to_string())
rec = json.loads(pathlib.Path('experiments/results/e3_provenance.json').read_text())
assert rec['schema_version'] and rec['solver_config']['seed'] is not None
for k in ('aquacal_version','git_sha','python_version','numpy_version','scipy_version'):
    assert k in rec['environment'], k"</automated>
  </verify>
  <acceptance_criteria>
    - `code_constants.csv` and `cpr_grouping.csv` are byte-identical to their pre-run committed
      copies; the summary states the comparison method and its result. Any difference was escalated
      and neither file was committed changed.
    - `code_constants.csv` is 9 rows all `PASS`; `cpr_grouping.csv` is 12 rows.
    - `newton_iterations.csv`'s columns equal `experiments.e3_derived_quantities.NEWTON_COLUMNS`, and
      its scalar-labelled rows reproduce the committed values (min 2, median 4.0, max 7,
      `n_not_converged = 0`, 104,052 pooled points). Any moved scalar value was escalated.
    - The summary tabulates the batch-loop measurement in full — per-point iteration distribution,
      batch `n_not_converged`, loop iterations actually executed — beside the scalar rows, with a
      plain statement of what the difference means and an explicit note that this is a provenance
      correction rather than evidence the positions were wrong.
    - `e3_provenance.json` was rewritten by this run and its `environment` matches E2's committed
      record on `cpu_model` and `ram_total_bytes`.
    - `python -m experiments.e3_derived_quantities --check` exit code is recorded (it re-runs the
      tiers, so a green result is a genuine reproduction claim).
    - `git status --porcelain src/ experiments/*.py` is empty — no code changed during wave 5.
  </acceptance_criteria>
  <done>E3's two source-derived tables reproduced byte-for-byte, the Newton table now measures the loop the optimizer runs and says so, and the data MF-01's rewrite needs is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed artifact -> regenerated artifact | The pre-run file is the only evidence of what the previous run produced once it is overwritten. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.2-90 | Repudiation | A determinism defect is absorbed as "expected churn from the phase's geometry work" | mitigate | Both tasks capture the committed file off-tree before running and require an explicit stated diff result; E5's and E3's immunity paths are named so a plausible-sounding attribution can be checked rather than accepted. |
| T-19.2-91 | Repudiation | The Newton table's schema change is used to cover a moved scalar value | mitigate | Task 2 requires the scalar-labelled rows to reproduce the committed numbers, which are quoted in the plan. |
| T-19.2-92 | Repudiation | E5's yardstick columns publish null because the run started outside the repository root | mitigate | The execution context requires the repository root and pre-verifies the anchored path; the automated check asserts zero nulls. |
| T-19.2-93 | Denial of Service | E5's 22-minute band shares the box with something else, though only its provenance matters | accept | E5 publishes no timing column, so contention would not corrupt a published number — but the wave is exclusive anyway and quiescence is recorded, since E3 shares it. |
| T-19.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
1. The automated blocks in Tasks 1 and 2 exit 0.
2. `python -c "import pandas as pd; print(len(pd.read_csv('experiments/results/index_sensitivity.csv')))"` — prints `11`.
3. `git diff --stat experiments/results/code_constants.csv experiments/results/cpr_grouping.csv` — empty.
4. `git status --porcelain src/ experiments/*.py` — empty.
5. `python -m pytest tests/ -m "not slow" -q` — exits 0. **After the runs, never during them.**
</verification>

<success_criteria>
- E5's band and its sidecar describe one run on one machine, with all four EXP-11 fields.
- E5's science columns and E3's two source-derived tables reproduced exactly.
- `newton_iterations.csv` measures the production batch loop and reports per-point convergence for it.
- E2, E1 and E7 were not re-run.
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-23-SUMMARY.md` when done.
Record: both launch commands verbatim; the quiescence checks; E5's wall-clock against ~22.5 minutes;
the stated diff result for `index_sensitivity.csv`, `code_constants.csv` and `cpr_grouping.csv`; the
full batch-vs-scalar Newton comparison table that plan 19.2-25 needs for MF-01; and the `--check`
exit codes.
</output>
