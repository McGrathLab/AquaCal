---
phase: 19.2-experiment-execution-and-provenance
plan: 06
subsystem: experiments
tags: [benchmark-json, memory-instrumentation, provenance, e2-real-rig, psutil, determinism]

# Dependency graph
requires:
  - phase: 19.2-01
    provides: "calibrate_synthetic memory_out / normal_fixed (zero-numerical-change guard)"
  - phase: 19.2-02
    provides: "SolverDiagnostics.n_residuals, wired through assemble_benchmark_record"
  - phase: 19.2-03
    provides: "public Newton diagnostic in core/refractive_geometry.py (additive, uncalled by pipeline)"
  - phase: 19.2-14
    provides: "solver_config.seed threaded into run_calibration_from_config's pipeline path"
provides:
  - "experiments/results/benchmark.json carrying a top-level memory block, per-stage memory sub-blocks, solver_config.seed, and stages.*.n_residuals"
  - "experiments/results/calibration.json refreshed (metadata.calibration_date only change)"
  - "Confirmation that every section-3 (manuscript) real-rig number reproduces at 0.000% delta on a src that changed under D-26 but is covered by passing bit-identity tests"
  - "Stage-attributed peak memory for the 13-camera/262-frame real rig: growth concentrated in stage3_interface_optimization (+8.54 GiB), a smaller +837 MiB in stage3_intrinsic_pass, zero further growth through auxiliary_registration/validation"
affects: ["19.2-05", "19.2-09 (E4 grid's real-rig tenth row reuses this benchmark.json)", "19.2-12 (provenance test_every_benchmark_record_carries_a_seed)"]

tech-stack:
  added: []
  patterns: ["D-26 bit-identity gate replacing git-diff-is-empty as the pre-production-run inertness check"]

key-files:
  created: []
  modified:
    - experiments/results/benchmark.json
    - experiments/results/calibration.json
    - tests/unit/test_benchmark.py

key-decisions:
  - "D-26 bit-identity gate (six suites, no marker filter, plus the full suite) run and passed immediately before launch, replacing the withdrawn git diff f193d60..HEAD -- src/ check"
  - "Isolated the run's writes to an --out outside experiments/results/ (C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/e2_results) so the committed artifacts were only overwritten after the section-3 comparison passed"
  - "Updated tests/unit/test_benchmark.py's TestCommittedBenchmarkRecordsUnaffectedByNResiduals to exclude experiments/results/benchmark.json -- the one record this plan deliberately refreshes -- while every other committed benchmark file still asserts no n_residuals key (Rule 1/3 fix, in-scope of this plan's own artifact)"

patterns-established: []

requirements-completed: [EXP-08]

# Metrics
duration: ~67min
completed: 2026-07-28
---

# Phase 19.2 Plan 06: E2 Real-Rig Re-run with Memory Instrumentation Summary

**Re-ran E2 against the local release frameset with `internals.benchmark_memory: true`; every section-3 number reproduced at 0.000% delta, and `experiments/results/benchmark.json` now carries a stage-attributed peak-memory block (peak growth concentrated in `stage3_interface_optimization`, +8.54 GiB) plus `solver_config.seed` and `stages.*.n_residuals`.**

## Performance

- **Duration:** ~67 min total (D-26 gates ~11.2 min + ~45 min calibration + ~11 min verification/tripwire/commit)
- **Started:** 2026-07-28T23:05:00Z (D-26 gate 1 launch)
- **Calibration launched:** 2026-07-28T23:17:51Z (local 7:17:51 PM EDT)
- **Calibration completed:** ~2026-07-29T00:02:43Z (local ~8:02:43 PM EDT), wall clock ≈44.9 min
- **Completed:** 2026-07-29T00:12:35Z
- **Tasks:** 2/2
- **Files modified:** 3 (benchmark.json, calibration.json, tests/unit/test_benchmark.py)

## Accomplishments

- E2's committed `benchmark.json` now carries a `memory` block (`whole_run_peak_bytes=10499325952`, `mode=psutil_peak_wset`) and per-stage `memory` sub-blocks on `stage3_interface_optimization`, `stage3_intrinsic_pass`, `auxiliary_registration`, and `validation` — satisfying D-07 so E4's real-rig tenth row (plan 19.2-09) can report stage-attributed memory like the nine synthetic cells beside it.
- Every section-3 quantity reproduced exactly (0.000% delta on all 12 keys of `real_rig_metrics.json`), confirming D-26's restated D-08 premise holds: source changed since the original `f193d60` baseline, but every `src` change is covered by a passing exact-equality bit-identity test, and the real-rig calibration is still fully deterministic.
- `solver_config.seed=42` (plan 19.2-14) and `stages.stage3_interface_optimization.n_residuals=147950` / `stages.stage3_intrinsic_pass.n_residuals=147950` (plan 19.2-02) now appear in the committed record — the two fields D-26 identified as missing from a wave-1-only run, closing the gap plan 19.2-09's grid-completeness and plan 19.2-12's `test_every_benchmark_record_carries_a_seed` both depend on.
- `release_calibration/diagnostics.json` (the release calibration's own comparison baseline) is untouched — mtime confirmed identical before and after the run (`2026-02-19 18:15:25.600176600 -0500`).

## Task Commits

1. **Task 1: Prepare an isolated re-run config and launch the run in the background** — no repo files modified (writes confined to `C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/`, outside the repository); no commit.
2. **Task 2: Verify every section-3 number is unchanged, then commit the refreshed artifacts** — `427738f` (feat)

**Plan metadata:** (this commit, made after this summary is written)

## D-26 Bit-Identity Gate (run before launch, per the plan's replacement for the withdrawn `git diff` check)

Both commands run from the repository root at HEAD `77a1026adae2e7e3f6857f424c829ed762628975` (confirmed matching `EXPECTED_BASE`), with the `AquaCal` conda interpreter (`~/anaconda3/envs/AquaCal/python.exe`; `aquacal.__file__` resolved to `C:\Users\tucke\PycharmProjects\AquaCal\src\aquacal\__init__.py`):

```
python -m pytest tests/unit/test_datasets_pipelines.py tests/unit/test_observability.py tests/unit/test_experiments_io.py tests/unit/test_refractive_geometry.py tests/unit/test_pipeline.py tests/synthetic/test_full_pipeline.py -q
-> 248 passed in 261.34s (0:04:21)
```

```
python -m pytest tests/ -q
-> 977 passed, 13 warnings in 410.55s (0:06:50)
```

Both exit 0. Copies stored beside the run log:
- `C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/d26_gate1_bit_identity_suites.log`
- `C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/d26_gate2_full_suite.log`

All four wave-1 summaries confirmed present before the gate: `19.2-01-SUMMARY.md`, `19.2-02-SUMMARY.md`, `19.2-03-SUMMARY.md`, `19.2-14-SUMMARY.md`. `git diff --stat HEAD -- src/` was empty at launch.

## Quiescence and Duplicate-Process Checks (review H4)

**Pre-launch** (`C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/quiescence_check_pre_launch.txt`): `Get-CimInstance Win32_Process -Filter "Name like '%python%'"` returned **no rows** — no python process of any kind running, and in particular no existing `experiments.e2_real_rig` command line. Launch proceeded.

**Post-completion** (`C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/quiescence_check_post_completion.txt`): same query, again **no rows**. No other AquaCal experiment process appeared during the run; `seconds` and `memory` fields are uncontaminated.

## Config Diff (the two permitted edits only)

```
71c71
<   output_dir: "C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration"
---
>   output_dir: "C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/output"
107a108,110
>
> internals:
>   benchmark_memory: true  # Opt-in: per-stage-boundary peak-RSS reading in benchmark.json (D-07)
```
Stored at `C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/config_diff.txt`. No other line differs — `frame_step`, `max_calibration_frames`, `holdout_fraction`, `n_water`, `initial_water_z`, camera list, and every video path are untouched.

## Launch Command (verbatim)

```
python -u -m experiments.e2_real_rig --config "C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/config.yaml" --out "C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/e2_results" --force > "C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/run.log" 2>&1 &
```

`--out` was pointed outside `experiments/results/` deliberately (not required by the plan text, but consistent with T-19.2-21/T-19.2-23's spirit): the run's own artifacts were isolated until Task 2's comparison passed, and only then copied into the repository. `benchmark.json`/`calibration.json` were, as designed, copied by the script from the pipeline's own `output_dir` (`C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/output`) — never reconstructed.

## Section-3 Key-by-Key Comparison (D-08/D-26 gate)

All 12 keys/sub-keys of `real_rig_metrics.json`, committed vs. fresh:

| Key | Committed | Fresh | Delta | Delta % |
|---|---|---|---|---|
| `auxiliary_reprojection_px.e3v8250` | 15.134314706895728 | 15.134314706895728 | 0.0 | 0.000% |
| `camera_height_range_m[0]` | 1.0082839060819992 | 1.0082839060819992 | 0.0 | 0.000% |
| `camera_height_range_m[1]` | 1.0815275632132504 | 1.0815275632132504 | 0.0 | 0.000% |
| `inter_corner_mae_mm` | 0.26815491460133334 | 0.26815491460133334 | 0.0 | 0.000% |
| `inter_corner_rmse_mm` | 0.6740741599086391 | 0.6740741599086391 | 0.0 | 0.000% |
| `mean_per_camera_reprojection_px` | 0.8786339665321746 | 0.8786339665321746 | 0.0 | 0.000% |
| `mean_relative_error_pct` | 0.44692485766888895 | 0.44692485766888895 | 0.0 | 0.000% |
| `mean_reprojection_px` | 1.019136036829683 | 1.019136036829683 | 0.0 | 0.000% |
| `n_comparisons` | 7762 | 7762 | 0 | 0.000% |
| `reprojection_range_px[0]` | 0.5393762736907708 | 0.5393762736907708 | 0.0 | 0.000% |
| `reprojection_range_px[1]` | 2.4078753984550487 | 2.4078753984550487 | 0.0 | 0.000% |
| `water_z_m` | 1.030554775 | 1.030554775 | 0.0 | 0.000% |
| `provenance` | identical | identical | — | — |

The `real_rig_metrics.json`, `camera_parameters.csv`, `reprojection_residuals.csv`, and `reconstruction_errors.csv` files were byte-identical between the committed and fresh runs (verified with `diff`) and were left untouched on disk — no commit was needed for them.

`calibration.json` differed only in `metadata.calibration_date` (a timestamp field). `benchmark.json`'s `problem_shape` and `accuracy` blocks were identical; every per-stage `n_params`, `n_groups`, `fd_reduction`, `nfev`, `njev`, `cost`, `optimality`, `status`, and `message` value was identical (verified field-by-field for `stage3_interface_optimization`, `stage3_intrinsic_pass`, `auxiliary_registration_e3v8250`). `environment` differed only in `git_sha` (as expected — the working tree advanced from `f193d60` to `77a1026`).

**No halt was required — D-08's hard stop did not trigger.**

## Memory Block and Stage Attribution (D-07)

```
"memory": {
  "mode": "psutil_peak_wset",
  "whole_run_peak_bytes": 10499325952
}
```

Per-stage `delta_bytes_since_previous_boundary` (consecutive deltas from `_baseline`, which is captured after Stage 1 + Stage 2 and correctly appears as no key at all):

| Stage | `cumulative_peak_bytes_as_of_stage_end` | `delta_bytes_since_previous_boundary` |
|---|---|---|
| `stage3_interface_optimization` | 9,621,364,736 | **+9,167,876,096** (~8.54 GiB) |
| `stage3_intrinsic_pass` | 10,499,325,952 | +877,961,216 (~837 MiB) |
| `auxiliary_registration` | 10,499,325,952 | 0 |
| `validation` | 10,499,325,952 | 0 |

**The peak grew almost entirely during `stage3_interface_optimization`** (the first joint refractive bundle-adjustment pass), with a much smaller further increase in `stage3_intrinsic_pass` (the second pass that unlocks intrinsics). Detection, Stage 1, and Stage 2 all fold into `_baseline` and are not separately attributable — this granularity limit was called out in D-07 finding 2 and is out of scope for this plan.

**Note for MEMORY.md / future sizing:** the measured whole-run peak (~9.78 GiB) is higher than the project's prior provisional estimate of ~7 GB for this 13-camera rig (see `[[stage3-peak-memory-may-exceed-documented-3-6gb]]`). Recording this measurement here since it is a direct, load-bearing output of this plan; not editing MEMORY.md itself (outside this plan's scope).

## Provenance Fields Added

- `solver_config.seed`: **42** (plan 19.2-14, threaded from `config.seed` into `run_calibration_from_config`'s `solver_config`)
- `stages.stage3_interface_optimization.n_residuals`: **147950**
- `stages.stage3_intrinsic_pass.n_residuals`: **147950**
- `stages.auxiliary_registration_e3v8250.n_residuals`: `null` (reason: "register_auxiliary_camera uses dense 2-point FD; no column-grouping structure exists at this site" — same absent-metric convention as `n_params`/`n_groups` at that site)

## Files Created/Modified

- `experiments/results/benchmark.json` — added `memory` block, per-stage `memory` sub-blocks, `solver_config.seed`, `stages.*.n_residuals`; `git_sha` and per-stage `seconds` updated to this run's values; every other field byte-identical.
- `experiments/results/calibration.json` — `metadata.calibration_date` updated to this run's timestamp; every other field byte-identical.
- `tests/unit/test_benchmark.py` — updated `TestCommittedBenchmarkRecordsUnaffectedByNResiduals.COMMITTED_BENCHMARK_FILES` to exclude `experiments/results/benchmark.json` (the record this plan deliberately refreshes), keeping the guard active for the six E1/E7 records that were not re-run.

Not modified (byte-identical, so left untouched on disk): `experiments/results/real_rig_metrics.json`, `experiments/results/camera_parameters.csv`, `experiments/results/reprojection_residuals.csv`, `experiments/results/reconstruction_errors.csv`.

## Decisions Made

- Ran the calibration with `--out` pointed at an external scratch directory rather than directly into `experiments/results/`, so the committed artifacts were only touched after Task 2's section-3 comparison passed. Not required verbatim by the plan text (which anticipates `--force` writing straight into `experiments/results/` and comparing before "refreshing"), but strictly stronger for T-19.2-23 (no possibility of a partially-written or premature overwrite if the comparison had failed).
- Fixed a stale guard test (`tests/unit/test_benchmark.py::TestCommittedBenchmarkRecordsUnaffectedByNResiduals`) that asserted `experiments/results/benchmark.json` must never carry `n_residuals` — correct through wave 1 (plan 19.2-02's own summary), but this plan is the one specifically tasked with adding `n_residuals` to that exact file. Excluded only that one file from the parametrized list; the other six committed benchmark files (E1 x2, E7 x4) still assert no `n_residuals` key, so the guard's original intent (no *retroactive* rewrite as a side effect of the library change alone) is preserved.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated a test guard made stale by this plan's own intended action**
- **Found during:** Task 2 tripwire run (`pytest tests/ -m "not slow" -q`)
- **Issue:** `test_committed_record_has_no_n_residuals_key[experiments/results/benchmark.json]` failed after the refreshed `benchmark.json` was copied in — the test (added by plan 19.2-02) asserted no committed benchmark file carries `n_residuals`, which was correct at the time it was written but is now the outcome this plan is explicitly designed to produce for exactly this one file.
- **Fix:** Removed `experiments/results/benchmark.json` from `TestCommittedBenchmarkRecordsUnaffectedByNResiduals.COMMITTED_BENCHMARK_FILES`, with a docstring update explaining why, leaving the assertion intact for the six files this plan did not touch.
- **Files modified:** `tests/unit/test_benchmark.py`
- **Verification:** `pytest tests/ -m "not slow" -q` re-run, 937 passed, 39 deselected (previously 1 failed, 937 passed).
- **Committed in:** `427738f` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 Rule 1 - bug/stale guard)
**Impact on plan:** Necessary and directly caused by this plan's own intended write to `experiments/results/benchmark.json`. No scope creep — no other test file or assertion was touched.

## Issues Encountered

- The D-26 bit-identity gate's two pytest invocations each exceed the Bash tool's default 2-minute timeout; both were launched in the background (`nohup ... &`, `disown`) and polled via a Windows process-liveness check (`Get-Process -Id <pid>`) rather than relying on a quiet log, per the executor prompt's guidance that a quiet log is not evidence of a dead run.
- None otherwise — the run completed on the first attempt with no OOM, no crash, and no need to relaunch.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `experiments/results/benchmark.json` is now as complete as the nine synthetic cells it will sit beside in `benchmark_grid.csv` (D-26): it carries `seed`, `n_residuals`, and a stage-attributed `memory` block.
- Plan 19.2-09 (E4 grid) can reuse this `benchmark.json` as its real-rig tenth row without a comparability gap.
- Plan 19.2-05 (which depends on this plan per its `depends_on` edge for its 13/200 `tab:cpr` row) can proceed against the refreshed committed record.
- No blockers. `src/` was never touched during the run; the D-26 ordering invariant (all `src` changes land and are proven inert before any publishable-result experiment) holds for this plan.

## Self-Check: PASSED

- FOUND: `experiments/results/benchmark.json`
- FOUND: `experiments/results/calibration.json`
- FOUND: `tests/unit/test_benchmark.py`
- FOUND: commit `427738f`

---
*Phase: 19.2-experiment-execution-and-provenance*
*Completed: 2026-07-28*
