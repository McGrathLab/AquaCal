---
phase: 19.2-experiment-execution-and-provenance
plan: 28
type: execute
wave: 8
gap_closure: true
depends_on: ["19.2-27"]
files_modified:
  - experiments/results/generalization_sweep.csv
  - experiments/results/e6_configs/
  - experiments/results/e6_provenance.json
autonomous: true
requirements: [EXP-10, EXP-11]

must_haves:
  truths:
    - "E6's committed artifacts are regenerated by the code that ships, so the optimality columns plan 27 added are PRESENT in the published CSV rather than merely supported by the code"
    - "The re-run reproduces every existing metric value EXACTLY. Plan 27 proved its sinks inert by unit test; this run proves it on the real sweep. A moved metric column means the instrumentation is NOT passive and is a HARD STOP, not a number to accept"
    - "All three `is_baseline` rows still equal E4's `cameras_12_frames_100` `reprojection_rms` = 0.7755597414084439 to the last digit. This is the shared `build_grid_scenario` contract and was already satisfied on 2026-07-31; losing it now would mean plan 27 changed scene construction"
    - "`python -m experiments.e6_generalization_sweep --check` exits 0 -- the first time E6 has had a working reproduction path for its baseline rows. This is the end-to-end proof of plan 27 Task 2, which could only be unit-tested there because the schema changed"
    - "All twelve checkpoints report ONE `git_sha` (plan 27 Task 3), and NOTHING is committed while the sweep runs"
    - "Every configuration's optimality is reported in the summary beside its RMS, so the sweep's convergence is on the record rather than resting on manual spot-checks"
  artifacts:
    - path: "experiments/results/generalization_sweep.csv"
      provides: "14 rows carrying optimality alongside the existing metrics"
      contains: "optimality"
    - path: "experiments/results/e6_configs/"
      provides: "twelve checkpoints with optimality and a single consistent git_sha"
  key_links:
    - from: "experiments/results/generalization_sweep.csv"
      to: "experiments/results/benchmark_grid.csv"
      via: "the baseline rows still equal E4's 12x100 cell exactly"
      pattern: "is_baseline"
---

<objective>
Re-run E6's three-axis sweep on plan 27's code so the committed artifacts carry solver optimality, a
single consistent `git_sha`, and a `--check` path that works.

Purpose: EXP-10, EXP-11, and the two `human_verification` items from the 2026-08-02 verification
report. Plan 27 fixed the code; until this runs, the shipped code and the published artifacts
disagree — which is the precise "true of the code, asserted of the artifacts" failure this phase
exists to eliminate.

**Runtime ~107 minutes** (measured 2026-07-31; the previous figure of ~33.5 min predates the D-29
geometry). Box exclusive.

**This run is expected to reproduce, and that expectation is the gate.** Plan 27 proved the new
sinks inert by unit test; every existing metric column must come back identical. Movement is a
defect in plan 27's instrumentation, not a new measurement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** fork from the merged result of wave 7 with plan 27 committed. Before launching:
- `git status --porcelain` empty;
- `pytest tests/ -m "not slow" -q` exits 0 — **before the sweep, never during it**;
- `python -c "import experiments.e6_generalization_sweep as m; print(len(m.E6_COLUMNS)); print(len(m.build_axis_configurations()))"` shows the NEW column count and 14 configurations;
- `python -m experiments.e6_generalization_sweep --smoke` exits 0;
- `experiments/results/benchmark_grid.csv` still has `status=ok` for `cameras_12_frames_100` at `0.7755597414084439`.

**LAUNCH DETACHED.** `nohup … & disown`, `python -u`, log OUTSIDE the repository. A
`run_in_background` command is killed by the harness at ~35-50 min and this run takes ~107; that
failure has already cost this phase three sweeps. Watch the log with a Monitor.

**COMMIT NOTHING WHILE IT RUNS** — not even docs. Plan 27 Task 3 makes the environment capture
once-per-sweep, but do not test that fix by violating the discipline it encodes.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-22-PLAN.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-22-SUMMARY.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-27-SUMMARY.md
@CLAUDE.md

<interfaces>
Entry point: `python -u -m experiments.e6_generalization_sweep --force`. `--force` is REQUIRED: the
committed checkpoints predate plan 27's optimality field and reusing them would mix two record
shapes in one table.

**The values this run must reproduce** (committed 2026-07-31, `242cb30`):

| axis | axis_value | reprojection_rms_px | reconstruction_rmse_mm |
|---|---|---|---|
| index | 1.333 | 0.775560 | 0.538454 |
| index | 1.36 | 0.780863 | 0.535721 |
| index | 1.39 | 0.767919 | 0.552780 |
| index | 1.42 | 0.788808 | 0.570385 |
| index | 1.45 | 0.705617 | 0.554975 |
| index | 1.48 | 0.704651 | 0.548331 |
| index | 1.51 | 0.705504 | 0.540604 |
| index | 1.55 | 0.705214 | 0.544063 |
| layout | grid | 0.775560 | 0.538454 |
| layout | line | 0.789925 | 0.541051 |
| layout | ring | 0.704959 | 0.717436 |
| scale | default | 0.775560 | 0.538454 |
| scale | double_scale | 0.705195 | 0.751640 |
| scale | half_scale | 1.007156 | 0.493464 |

`--check` reconstitutes rows from `e6_configs/*.json` and NEVER runs a configuration (~1.1 s). Plan
22 describes it as re-running the sweep; that description is wrong and this plan must not repeat it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the sweep on a quiet box</name>
  <files>experiments/results/e6_configs/, experiments/results/generalization_sweep.csv, experiments/results/e6_provenance.json</files>
  <read_first>
    - `19.2-27-SUMMARY.md` — what changed and the recorded inertness proof.
    - `19.2-22-SUMMARY.md` — the launch discipline that worked, and the `git_sha` split that did not.
    - `CLAUDE.md` § "Always run calibration unbuffered".
  </read_first>
  <action>
Record the process listing verbatim before launching and again at completion — no other AquaCal
process, no pytest, nothing else.

Launch detached and unbuffered from the repository root:
`nohup python -u -m experiments.e6_generalization_sweep --force > <log outside repo> 2>&1 & disown`.
Record the command verbatim and the measured wall-clock against the previous 107 minutes.

Watch for a configuration that stops making progress rather than assuming it is slow; there is no
timeout on this path. An abandoned configuration is a recorded outcome, not a deletion.

If the run dies partway, relaunch WITHOUT `--force` to resume from the checkpoints already written,
and record the interruption. Do not delete checkpoints by hand and do not reduce any axis.
  </action>
  <verify>
    <automated>python -c "
import json, pathlib, collections
cfgs = sorted(pathlib.Path('experiments/results/e6_configs').glob('*.json'))
print(f'{len(cfgs)} checkpoints')
shas = collections.Counter()
for p in cfgs:
    r = json.loads(p.read_text())
    env = r.get('environment') or {}
    shas[env.get('git_sha')] += 1
    print(p.name, r.get('status'), 'optimality' in json.dumps(r), env.get('aquacal_version'))
assert cfgs, 'no checkpoints written'
print('DISTINCT git_sha:', dict(shas))
assert len(shas) == 1, f'git_sha split across checkpoints: {dict(shas)}'"</automated>
  </verify>
  <acceptance_criteria>
    - The launch command contains `python -u`, `--force`, `nohup … & disown`, and a log path outside the repository.
    - Quiescence recorded verbatim at launch and completion; wall-clock recorded against ~107 min.
    - Twelve checkpoints, each with optimality, all four EXP-11 fields, and **exactly one distinct `git_sha`**.
    - `git log` shows NO commit with a timestamp inside the run window.
  </acceptance_criteria>
  <done>The sweep ran alone on plan 27's code, and its provenance names one commit.</done>
</task>

<task type="auto">
  <name>Task 2: Prove reproduction, then prove --check works</name>
  <files>experiments/results/generalization_sweep.csv</files>
  <read_first>
    - The committed CSV via `git show HEAD:experiments/results/generalization_sweep.csv` — capture it OFF-TREE before the run overwrites it.
    - `experiments/results/benchmark_grid.csv` — the `cameras_12_frames_100` anchor.
  </read_first>
  <action>
**Reproduction is the gate.** Compare every pre-existing metric column against the captured
baseline. They must be EXACTLY equal — plan 27's sinks were proven inert by unit test, so a moved
column means that proof was wrong.

**A moved metric column is a HARD STOP.** Report it with the column, the row, both values and the
relative difference; do not accept it, do not adjust a tolerance, and do not rationalise it as seed
noise — the seed is fixed at 42 and the previous run is four days old on the same geometry.
Relaxing a determinism gate to make it pass is a recorded BLOCKING anti-pattern here.

Confirm the three `is_baseline` rows still equal E4's `cameras_12_frames_100`
`reprojection_rms = 0.7755597414084439` exactly, quoting both at full precision.

Then run `python -m experiments.e6_generalization_sweep --check` and record the exit code. **It must
now exit 0.** This is the end-to-end proof of plan 27 Task 2 — the first working reproduction path
E6's baseline rows have ever had. If it still fails, quote the mismatch report verbatim; a partial
fix is a finding, not something to paper over.

Finally, tabulate every configuration's optimality beside its RMS and state plainly whether any
configuration converged poorly. This is the WR-02 answer: the sweep's convergence is now recorded
rather than spot-checked. Report the numbers; do not add a verdict column or threshold them (D-12).
  </action>
  <verify>
    <automated>python -m experiments.e6_generalization_sweep --check</automated>
  </verify>
  <acceptance_criteria>
    - Every pre-existing metric column reproduces EXACTLY; any movement was escalated as a hard stop rather than absorbed.
    - The three baseline rows equal `0.7755597414084439`, quoted at full precision.
    - `--check` exit code is recorded and is **0**; if not, the mismatch is quoted verbatim.
    - Optimality is tabulated for all 14 rows beside RMS, with a plain statement about convergence and no verdict column.
    - `python -c "import pandas as pd; d=pd.read_csv('experiments/results/generalization_sweep.csv'); assert d['seed'].notna().all()"` exits 0.
  </acceptance_criteria>
  <done>E6's published artifacts carry their own convergence evidence, reproduce their predecessors exactly, and can be checked mechanically.</done>
</task>

</tasks>

<verification>
1. Task 1 and Task 2 automated blocks exit 0.
2. `python -m experiments.e6_generalization_sweep --check` — exits 0.
3. `python -m pytest tests/ -m "not slow" -q` — exits 0. **After the sweep, never during it.**
4. `git status --porcelain src/ experiments/*.py` — empty.
</verification>

<success_criteria>
- E6's committed artifacts carry optimality, one consistent `git_sha`, and all four EXP-11 fields.
- Every pre-existing metric value reproduced exactly.
- `--check` exits 0 for the first time.
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-28-SUMMARY.md`.
Record: the verbatim launch command; both quiescence checks; wall-clock vs ~107 min; the
column-by-column reproduction result; the baseline-vs-E4 anchor at full precision; the `--check`
exit code; the optimality table; and confirmation that nothing was committed during the run.
</output>
