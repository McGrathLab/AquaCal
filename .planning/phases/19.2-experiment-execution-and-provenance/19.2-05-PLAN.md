---
phase: 19.2-experiment-execution-and-provenance
plan: 05
type: execute
wave: 2
depends_on: ["19.2-03", "19.2-04"]
files_modified:
  - experiments/e3_derived_quantities.py
  - tests/unit/test_experiments_e3.py
  - experiments/results/code_constants.csv
  - experiments/results/newton_iterations.csv
  - experiments/results/cpr_grouping.csv
  - experiments/results/cpr_grouping.tex
  - experiments/results/cpr_derived_values.tex
  - experiments/results/e3_provenance.json
autonomous: true
requirements: [EXP-07, EXP-11]

must_haves:
  truths:
    - "code_constants.csv renders the declared table imported from tests/, with a pass_fail column and no FAIL row, and re-declares nothing (D-18)"
    - "newton_iterations.csv reports min/median/max/count of Newton iteration counts measured over the real rig's working volume through the shipped root-find, not a re-implementation (D-19, D-20)"
    - "cpr_grouping.csv carries only the tab:cpr configurations E4's grid does NOT run, each with shared-interface and per-camera-mode rows, so no configuration has two origins (D-16, D-21)"
    - "The default LaTeX fragment renders shared-interface rows only; per-camera rows are behind a flag (D-21)"
    - "E3 tiers 1-2 carry a minimal provenance sidecar built from capture_environment() alone, since they never run a calibration (Part 4 Rule 2 carve-out)"
    - "The script honours the identical five-flag CLI contract every other experiment shares (19.1 D-21)"
  artifacts:
    - path: "experiments/e3_derived_quantities.py"
      provides: "E3's three tiers, CLI, sidecar, LaTeX fragments"
      contains: "DECLARED_CONSTANTS"
      min_lines: 300
    - path: "experiments/results/code_constants.csv"
      provides: "declared vs source value with pass/fail"
      contains: "pass_fail"
    - path: "experiments/results/newton_iterations.csv"
      provides: "iteration-count distribution over the rig's working volume"
    - path: "experiments/results/cpr_grouping.csv"
      provides: "P / groups / fd_reduction for the E3-owned tab:cpr rows"
    - path: "experiments/results/e3_provenance.json"
      provides: "minimal environment-only sidecar for tiers 1-2"
      contains: "environment"
  key_links:
    - from: "experiments/e3_derived_quantities.py"
      to: "tests/unit/test_experiments_e3_constants.py"
      via: "import of DECLARED_CONSTANTS (deliberate inverted direction, D-18)"
      pattern: "from tests.unit.test_experiments_e3_constants import"
    - from: "experiments/e3_derived_quantities.py"
      to: "aquacal.core.refractive_project_newton_diagnostic"
      via: "tier 2 measurement"
      pattern: "refractive_project_newton_diagnostic"
    - from: "experiments/e3_derived_quantities.py"
      to: "aquacal.calibration._optim_common structural grouping"
      via: "tier 3 builds patterns and calls the library's own grouper"
      pattern: "build_structural_column_groups"
---

<objective>
Write `experiments/e3_derived_quantities.py`: the three-tier derived-quantities script that turns
every supplement claim about *how the library behaves* into a committed, regenerable artifact.

Purpose: EXP-07. Tier 1 renders the declared constants table CI already owns; tier 2 measures the
Newton iteration distribution the supplement asserts as "two to four steps" but never measured;
tier 3 emits `P` / group counts for the `tab:cpr` rows E4's grid does not cover.
Output: one script, four data artifacts, two LaTeX fragments, one provenance sidecar, and unit
tests over its novel helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** fork from the merged result of plans 19.2-03 and 19.2-04. Verify
`python -c "from aquacal.core import refractive_project_newton_diagnostic"` and
`python -c "from tests.unit.test_experiments_e3_constants import DECLARED_CONSTANTS"` both exit 0
before starting; if either fails, your base is stale — re-fork.
**PYTHONPATH:** the editable install resolves to the main checkout, not a worktree. If working in
a worktree, `export PYTHONPATH="$(pwd)/src"` before running pytest or its passing tests are
meaningless.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-CONTEXT.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-PATTERNS.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-RESEARCH.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-SOURCE-BRIEF.md
@experiments/README.md

<interfaces>
<!-- Contracts the executor needs. Extracted live from the codebase 2026-07-28. -->

Shared CLI/IO contract (`experiments/_io.py`):
  build_experiment_arg_parser() -> ArgumentParser   # add_help=False; --seed --out --force --smoke --check
  validate_args(parser, args) -> None               # errors if --check and --force both set
  resolve_out_dir(out: Path) -> Path
  write_experiment_csv(df, path, *, key_columns: list[str], force: bool) -> bool
  compare_experiment_csv(fresh, committed_path, *, key_columns, rtol) -> ComparisonReport
  exit_code_for(report) -> int

Provenance:
  capture_environment(repo_hint_path: Path | None = None) -> dict   # aquacal.io

Tier 1 source (owned by plan 04, imported here, never re-declared):
  tests.unit.test_experiments_e3_constants.DECLARED_CONSTANTS
      # sequence of DeclaredConstant(key, claim, source, declared_value, read_via, live)

Tier 2 source (added by plan 03):
  aquacal.core.refractive_project_newton_diagnostic(camera, interface, point_3d,
      max_iterations=10, tolerance=1e-9) -> dict | None
      # keys: n_iterations, converged, final_residual, r_p, incidence_angle_deg

Real-rig geometry for tier 2 (same substrate E5 uses, D-09/D-20):
  generate_real_rig_array() -> (intrinsics, extrinsics, water_zs)     # no parameters; 12 cameras
  generate_real_rig_trajectory(n_frames=100, depth_range=(1.1, 2.0), seed=42) -> list[BoardPose]
      # both importable from aquacal.datasets

Tier 3 structure:
  build_jacobian_sparsity(detections, reference_camera, camera_order, frame_order, min_corners,
                          refine_intrinsics, normal_fixed, shared_interface=True) -> NDArray[np.int8]
  build_structural_column_groups(jac_sparsity, n_cams, n_frames, refine_intrinsics=False,
                                 normal_fixed=True, shared_interface=True) -> NDArray[np.intp]
      # both in aquacal.calibration._optim_common
  Fixture builder to ADAPT (not import) — tests/unit/test_optim_common.py lines 33-88:
      _make_detections(n_cams, n_frames, visibility, corners_per_view=4, seed=0)

E2's committed record to COPY the 13-camera/200-frame row from (never re-derive, D-16):
  experiments/results/benchmark.json
      stages.stage3_interface_optimization.{n_params, n_groups, fd_reduction}
      stages.stage3_intrinsic_pass.{n_params, n_groups, fd_reduction}

Canonical main() control flow to copy — experiments/e7_interface_ablation.py lines 584-617
LaTeX layer — experiments/_render.py::write_latex_fragment(df, path, columns), _latex_escape
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E3 skeleton, five-flag CLI, tier 1 rendering, and the minimal sidecar</name>
  <files>experiments/e3_derived_quantities.py</files>
  <read_first>
    - `experiments/e7_interface_ablation.py` — the FULL file, especially the module docstring
      (lines 1-59), `CHECK_RTOL` at line 113, `_run_check` (559-574), `build_arg_parser` (577-582),
      and `main` (584-617). This is the control flow to copy, including the `--smoke`
      `tempfile.TemporaryDirectory` branch that keeps CI from polluting `experiments/results/`.
    - `experiments/_io.py` — the FULL file: the five-flag parser, `validate_args`,
      `resolve_out_dir`, `write_experiment_csv`'s `key_columns` requirement and skip-if-exists
      semantics.
    - `tests/unit/test_experiments_e3_constants.py` (plan 04's output) — the `DeclaredConstant`
      field order this task renders.
    - `src/aquacal/io/benchmark.py` lines 66-150 — `capture_environment`'s returned key shape.
    - `19.2-CONTEXT.md` § Claude's Discretion item 2 — the sidecar carve-out.
    - `.claude/rules/code-style.md`.
  </read_first>
  <action>
Create `experiments/e3_derived_quantities.py`.

Module docstring: "E3: derived quantities and code constants (EXP-07)." State that it is invoked
as `python -m experiments.e3_derived_quantities`, inherits the shared five-flag CLI contract from
`experiments._io.build_experiment_arg_parser`, and emits `code_constants.csv`,
`newton_iterations.csv`, `cpr_grouping.csv`, `cpr_grouping.tex`, `cpr_derived_values.tex`, and
`e3_provenance.json`. State explicitly that tier 1's table is DECLARED in
`tests/unit/test_experiments_e3_constants.py` and only rendered here (D-18) — the inverted import
direction is deliberate, and `experiments/` never ships because `pyproject.toml` scopes package
discovery to `where = ["src"]`.

Define `CHECK_RTOL = 1e-6` as a module constant, matching every other script.

Define `CODE_CONSTANTS_COLUMNS = ["key", "claim", "source", "read_via", "declared_value",
"source_value", "pass_fail"]` and `CODE_CONSTANTS_KEY_COLUMNS = ["key"]`.

Write `build_code_constants_df() -> pd.DataFrame`: import `DECLARED_CONSTANTS` from
`tests.unit.test_experiments_e3_constants`, call each entry's `live()`, and emit one row per
entry with `declared_value` and `source_value` rendered via `repr()` so tuple-valued rows survive
the CSV round-trip readably, and `pass_fail` set to the literal string `PASS` or `FAIL`. Do NOT
raise on a mismatch — the CSV is the artifact and CI is the gate; a FAIL row must be visible in
the committed file rather than aborting the run. Do NOT re-declare any claim, source, or value in
this module.

Write `build_provenance_sidecar(seed: int) -> dict` returning
`{"experiment": "e3", "schema_version": 1, "seed": seed, "environment": capture_environment()}`.
This is the Part 4 Rule 2 carve-out — tiers 1 and 2 never run a calibration, so there is no
`benchmark.json` to reuse. Do NOT invent a second sidecar format and do NOT hand-roll any
environment field `capture_environment()` already supplies.

Write `build_arg_parser()` extending `build_experiment_arg_parser()` via `parents=[...]`, adding
exactly one script-local flag: `--include-per-camera-latex` with `action="store_true"`, default
off, documented as D-21's switch — the supplement's sparsity table describes the production
(shared-interface) configuration; per-camera mode belongs where E7 frames it as a deliberate
ablation.

Write `main(argv=None) -> int` copying E7's control flow exactly: parse, `validate_args`,
`logging.basicConfig(level=logging.INFO, format="%(message)s")`, then the `--check` branch (never
writes), the `--smoke` branch (throwaway `tempfile.TemporaryDirectory` when `--out` is at its
parser default, always `force=True`), then the normal branch using `resolve_out_dir(args.out)` and
`force=args.force`. End with `if __name__ == "__main__": sys.exit(main())`.

In this task wire only tier 1 and the sidecar into `main`; leave clearly-marked call sites for
tiers 2 and 3 that Tasks 2 and 3 fill in. Write `code_constants.csv` via `write_experiment_csv`
with `key_columns=CODE_CONSTANTS_KEY_COLUMNS`. Write the sidecar with
`json.dumps(..., indent=2, sort_keys=True)`, honouring the same skip-unless-`force` discipline
`write_experiment_csv` uses.
  </action>
  <verify>
    <automated>python -m experiments.e3_derived_quantities --smoke</automated>
  </verify>
  <acceptance_criteria>
    - `python -m experiments.e3_derived_quantities --smoke` exits 0.
    - `python -m experiments.e3_derived_quantities --check --force` exits non-zero with a usage error (mutual exclusion enforced by `validate_args`).
    - Running with `--smoke --out <tmp>` produces `code_constants.csv` with exactly 8 data rows, columns exactly `key, claim, source, read_via, declared_value, source_value, pass_fail`, and no `FAIL` value in `pass_fail`.
    - Running with `--smoke --out <tmp>` produces `e3_provenance.json` that parses as JSON and has top-level keys `experiment`, `schema_version`, `seed`, `environment`; `environment` carries the git SHA and Python/NumPy/SciPy version fields `capture_environment()` returns.
    - `grep -c "from tests.unit.test_experiments_e3_constants import DECLARED_CONSTANTS" experiments/e3_derived_quantities.py` returns exactly 1.
    - `grep -v '^\s*#' experiments/e3_derived_quantities.py | grep -c "500_000_000\|1e-9\|0\.01, 2\.0"` returns 0 — no declared constant value is restated in this module.
    - `python -c "import experiments.e3_derived_quantities as m; a=m.build_arg_parser().parse_args([]); assert all(hasattr(a,f) for f in ['seed','out','force','smoke','check','include_per_camera_latex'])"` exits 0.
    - `ruff check experiments/e3_derived_quantities.py && ruff format --check experiments/e3_derived_quantities.py` exits 0.
  </acceptance_criteria>
  <done>The script runs under all five shared flags plus its one local flag, renders the imported constants table to an 8-row all-PASS CSV, writes an environment-only sidecar, and restates no declared constant value.</done>
</task>

<task type="auto">
  <name>Task 2: Tier 2 - Newton iteration distribution over the real rig's working volume</name>
  <files>experiments/e3_derived_quantities.py</files>
  <read_first>
    - `src/aquacal/core/refractive_geometry.py` — `refractive_project_newton_diagnostic` as added
      by plan 03, including its returned key list and its three `None` cases.
    - `src/aquacal/datasets/synthetic.py` — `generate_real_rig_array` (fixed 12-camera layout,
      common `water_z`, all cameras at world Z=0) and `generate_real_rig_trajectory`, including the
      `X_CENTER`, `Y_CENTER`, `XY_EXTENT`, `ROTATION_RANGE_DEG` constants inside its body, so the
      sweep covers the volume the rig actually observes including its most oblique corners.
    - `src/aquacal/core/camera.py` and `src/aquacal/core/interface_model.py` — how to construct a
      `Camera` and an `Interface` from the generator's `(intrinsics, extrinsics, water_zs)` triple.
    - `src/aquacal/core/board.py` — `BoardGeometry` and how a `BoardPose` maps board corners into
      world coordinates, since tier 2's sweep points are the board corners at each pose.
    - `.planning/geometry.md` — the Z-down world frame and the `h_c = water_z - C_z` convention.
    - `19.2-CONTEXT.md` § D-20 — report min/median/max/count, not just a range; a maximum above
      four is a finding, not a failure.
  </read_first>
  <action>
Add tier 2 to `experiments/e3_derived_quantities.py`.

Define `NEWTON_COLUMNS = ["camera", "n_points", "iter_min", "iter_median", "iter_max",
"n_not_converged", "incidence_deg_min", "incidence_deg_max", "residual_max_m"]` and
`NEWTON_KEY_COLUMNS = ["camera"]`.

Write `build_newton_iterations_df(n_frames: int, seed: int) -> pd.DataFrame`:
  - Call `generate_real_rig_array()` for the 12-camera geometry and
    `generate_real_rig_trajectory(n_frames=n_frames, depth_range=(1.1, 2.0), seed=seed)` for board
    poses spanning the working volume.
  - For each camera, construct its `Camera` and the shared `Interface` (normal `[0, 0, -1]`,
    `n_air=1.0`, `n_water=1.333`) and call `refractive_project_newton_diagnostic` once per board
    corner per pose. Skip `None` returns (the degenerate cases) and count them separately — they
    must not enter the iteration statistics.
  - Aggregate per camera: point count, min/median/max of `n_iterations`, count of entries whose
    `converged` is False, min/max of `incidence_angle_deg`, max of `final_residual`.
  - Emit one row per camera plus one pooled row with `camera` set to the literal `ALL`, computed
    from the same per-point records — not by re-averaging the per-camera summaries.

Never re-implement the Newton loop, never call `_refractive_project_newton`, and never infer an
iteration count from a residual. The public diagnostic is the only source (P2, D-19).

Do NOT assert or gate on the observed maximum. If it exceeds four, that is a finding for the
manuscript, recorded in the CSV; the script's exit code must not depend on it (D-20). Log the
pooled min/median/max at INFO so a background run's log carries the headline without opening the
CSV.

Under `--smoke` use a small `n_frames` (3) so the tier stays sub-second; under a normal run use at
least 30 frames so every camera gets a meaningful sample.

Wire the write into `main`'s normal and smoke branches via
`write_experiment_csv(..., key_columns=NEWTON_KEY_COLUMNS, force=...)`, and add
`newton_iterations.csv` to the `--check` branch's comparison set at `CHECK_RTOL`.
  </action>
  <verify>
    <automated>python -m experiments.e3_derived_quantities --smoke</automated>
  </verify>
  <acceptance_criteria>
    - Running with `--smoke --out <tmp>` produces `newton_iterations.csv` with columns exactly `camera, n_points, iter_min, iter_median, iter_max, n_not_converged, incidence_deg_min, incidence_deg_max, residual_max_m`.
    - That CSV has exactly 13 data rows: 12 cameras plus one row whose `camera` value is `ALL`.
    - Every row has `n_points` greater than 0 and `iter_min` at least 1.
    - `grep -c "_refractive_project_newton" experiments/e3_derived_quantities.py` returns 0.
    - `grep -c "f_prime\|d_air_sq\|sin_water" experiments/e3_derived_quantities.py` returns 0 — the Newton loop is not reproduced in the script.
    - `grep -c "refractive_project_newton_diagnostic" experiments/e3_derived_quantities.py` returns at least 2 (import plus call site).
    - The script's exit code is 0 even when `iter_max` exceeds 4 — verified by reading the code path, which must contain no `assert`, no `raise`, and no `return 1` keyed on an iteration count.
    - `ruff check experiments/e3_derived_quantities.py` exits 0.
  </acceptance_criteria>
  <done>Tier 2 emits a 13-row distribution CSV measured through the shipped root-find over the real rig's working volume, reports min/median/max/count rather than a bare range, and gates on nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tier 3 CPR grouping, the two LaTeX fragments, and unit tests</name>
  <files>experiments/e3_derived_quantities.py, tests/unit/test_experiments_e3.py</files>
  <behavior>
    - `test_build_code_constants_df_shape`: the tier-1 DataFrame has the seven declared columns and
      one row per `DECLARED_CONSTANTS` entry, with `pass_fail` in `{"PASS", "FAIL"}`.
    - `test_cpr_rows_exclude_e4_grid`: the declared tier-3 configuration list contains no
      `(n_cameras, n_frames)` pair from E4's grid `{8,12,16} x {50,100,200}` — D-16's
      one-number-one-origin rule, asserted structurally rather than trusted.
    - `test_cpr_row_builder_shape`: the tier-3 row builder returns the declared columns for a tiny
      hand-built configuration, with `fd_reduction` equal to `n_params / n_groups`.
    - `test_per_camera_rows_present`: for every shared-interface configuration there is a matching
      `shared_interface=False` row with the same `(n_cameras, n_frames, refine_intrinsics,
      normal_fixed)` key.
    - `test_latex_fragment_default_excludes_per_camera`: the default fragment builder is given a
      DataFrame containing both modes and emits only the shared rows; with the flag set it emits
      both.
    - `test_sidecar_carries_environment`: `build_provenance_sidecar(42)` has an `environment` block
      containing the keys `capture_environment()` returns.
  </behavior>
  <read_first>
    - `tests/unit/test_optim_common.py` lines 33-88 (`_make_detections` / `_make_pattern`) and
      lines 189-276 (`_DOCUMENTED_CONFIGS` and the six `tab:cpr` configurations, which name the
      exact `(n_cams, n_frames, normal_fixed, refine_intrinsics)` tuples).
    - `src/aquacal/calibration/_optim_common.py` — `build_jacobian_sparsity` and
      `build_structural_column_groups` signatures, including `shared_interface`.
    - `experiments/results/benchmark.json` — the committed E2 record. Read
      `stages.stage3_interface_optimization` and `stages.stage3_intrinsic_pass` to find the
      `n_params` / `n_groups` / `fd_reduction` values the 13-camera/200-frame row COPIES.
    - `experiments/_render.py` — `write_latex_fragment`'s signature and `_latex_escape`'s
      behavior; do not write a second LaTeX layer.
    - `tests/unit/test_experiments_e1.py` — the hand-built-fixture, no-calibration, nothing-slow
      unit-test shape to follow.
    - `19.2-CONTEXT.md` §§ D-16, D-21, D-22.
    - `19.2-SOURCE-BRIEF.md` § E3 Tier 3 — the six `tab:cpr` rows and the two derived prose asides.
  </read_first>
  <action>
**Tier 3 in the script.** Define
`CPR_COLUMNS = ["config_key", "n_cameras", "n_frames", "normal_fixed", "refine_intrinsics",
"shared_interface", "n_params", "n_groups", "fd_reduction", "record_source"]` and
`CPR_KEY_COLUMNS = ["config_key"]`. `record_source` takes the literal value `computed` for
configurations this script builds a sparsity pattern for, and `copied_from_e2_benchmark` for the
13-camera/200-frame row.

Declare `CPR_CONFIGS` as an explicit module-level list of the `tab:cpr` configurations E4 does NOT
run, per D-16: the 3-camera/3-frame configuration, and the 13-camera/200-frame real-rig
configuration. Each declared configuration is emitted twice — once with `shared_interface=True`
and once with `shared_interface=False` (D-21) — so a coverage gap is countable. Add a module-level
comment naming `benchmark_grid.csv` as the origin for the remaining `tab:cpr` rows, so a reader
cannot mistake the omission for an oversight.

Adapt `_make_detections` from `tests/unit/test_optim_common.py` into a module-private helper in
this script (per P3, `experiments/` imports from `src/aquacal`, not from `tests/` — tier 1's
constants table is the single deliberate exception, and it is not this). Build each computed
configuration's pattern with `build_jacobian_sparsity`, group it with
`build_structural_column_groups`, and read `n_params` as `jac_sparsity.shape[1]` and `n_groups` as
`int(groups.max()) + 1`. Never hand-roll a grouping and never use scipy's `group_columns`.

For the 13-camera/200-frame row, read the values out of `experiments/results/benchmark.json` —
copy, do not re-derive (D-16). If that file is absent, emit the row with null metrics and
`record_source` set to `missing_e2_benchmark`, and log a WARNING naming the file; do not raise.

**The two LaTeX fragments.** Write `write_cpr_latex(df, out_dir, include_per_camera)` using
`experiments/_render.py::write_latex_fragment` — do not write a second LaTeX layer, and do not
recompute any value at render time (P1). Default renders only rows with `shared_interface` True
into `cpr_grouping.tex`; when `include_per_camera` is set, both modes are rendered.

Write `write_derived_values_latex(out_dir, cpr_df, grid_csv_path)` emitting
`cpr_derived_values.tex` with the two derived prose asides D-22 requires regenerated: the
"N of M parameters" aside computed from the 13-camera/200-frame row's `n_params` and the
corresponding pose-parameter count, and the fd_reduction ratio across the 8-camera and 12-camera
configurations. The second aside needs rows that live in `benchmark_grid.csv` (E4 owns them). If
`grid_csv_path` does not exist, emit that macro with the literal placeholder value
`\TODO{awaiting benchmark\_grid.csv from E4}` and log a WARNING naming the missing file — the
close-out plan 19.2-12 (the phase's final wave) re-runs this script with `--force` once E4's
grid has landed. Never invent the
number and never silently omit the macro.

**Tests.** Extend `tests/unit/test_experiments_e3.py` in place with the six behaviors above. Keep
them fast: hand-built fixtures only, no `create_scenario`, no calibration, nothing marked slow.
Wire the tier-3 writes into `main`, add `cpr_grouping.csv` to the `--check` comparison set, and
extend the module docstring's emitted-artifact list if it drifted.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e3.py -x -q && python -m experiments.e3_derived_quantities --smoke</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_experiments_e3.py -x -q` exits 0 and collects at least 6 new tests beyond plan 04's `pack_params` tests.
    - `python -m pytest tests/unit/test_experiments_e3.py -m slow -q` collects 0 tests.
    - Running with `--smoke --out <tmp>` produces `cpr_grouping.csv` with columns exactly `config_key, n_cameras, n_frames, normal_fixed, refine_intrinsics, shared_interface, n_params, n_groups, fd_reduction, record_source`.
    - That CSV has exactly 4 data rows (2 declared configurations x 2 interface modes), with exactly 2 rows having `shared_interface` True.
    - No row of `cpr_grouping.csv` has an `(n_cameras, n_frames)` pair drawn from `{8,12,16} x {50,100,200}`.
    - Exactly 2 rows have `record_source` equal to `copied_from_e2_benchmark`, and their `n_params`/`n_groups` values equal the corresponding values read from `experiments/results/benchmark.json`.
    - `cpr_grouping.tex` exists and contains no `shared_interface=False` row by default; running with `--include-per-camera-latex --force` produces a fragment containing both modes.
    - `cpr_derived_values.tex` exists and contains two LaTeX macro definitions; when `benchmark_grid.csv` is absent one of them carries the `\TODO{` placeholder and the run logged a WARNING naming the file.
    - `grep -c "group_columns" experiments/e3_derived_quantities.py` returns 0.
    - `grep -c "build_structural_column_groups" experiments/e3_derived_quantities.py` returns at least 2.
    - `ruff check experiments/ tests/ && ruff format --check experiments/ tests/` exits 0.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 with at least 874 passed.
  </acceptance_criteria>
  <done>Tier 3 emits a 4-row CSV covering only the non-E4 `tab:cpr` configurations in both interface modes, the default LaTeX fragment is shared-only, the derived-values fragment either carries a regenerated number or an explicit TODO naming its missing input, and six unit tests cover the script's novel helpers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem write | The script writes CSV/TeX/JSON under a user-supplied `--out`. Controls are `resolve_out_dir`, `warn_if_overwriting`, and the skip-unless-`force` guard, all inherited unchanged from `_io.py`. |
| test-module import from `experiments/` | `e3_derived_quantities.py` imports `DECLARED_CONSTANTS` from `tests/`. Knowingly accepted (D-18): `experiments/` never ships, so nothing leaks into the wheel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.2-15 | Repudiation | The script re-declares a constant instead of importing it, so an experiment-side edit could make `code_constants.csv` disagree with what CI asserts | mitigate | Grep gate asserts the declared values do not appear as literals in the script and that the `DECLARED_CONSTANTS` import appears exactly once. |
| T-19.2-16 | Repudiation | Tier 3 re-derives P/groups for a configuration `benchmark_grid.csv` also reports, creating a second origin for a published number | mitigate | `test_cpr_rows_exclude_e4_grid` asserts structurally that no declared configuration falls inside E4's grid; the 13/200 row carries `record_source=copied_from_e2_benchmark` and its values are asserted equal to the committed record's. |
| T-19.2-17 | Tampering | Tier 2 measures a re-implemented Newton loop rather than the shipped one, so the supplement's sentence describes a lookalike | mitigate | Grep gates assert the loop's distinctive tokens (`f_prime`, `d_air_sq`, `sin_water`) do not appear in the script and that `_refractive_project_newton` is never called. |
| T-19.2-18 | Repudiation | The derived-values fragment silently omits a macro whose input has not been produced yet, and the omission is never noticed | mitigate | The macro is always emitted; when its input is missing it carries an explicit `\TODO{...}` placeholder and the run logs a WARNING naming the file; close-out plan 19.2-12 re-runs with `--force`. |
| T-19.2-19 | Information Disclosure | `e3_provenance.json` embeds git SHA, CPU model, and OS in a committed artifact | accept | Deliberate and required by EXP-11. Public research artifact; no credentials or PII. |
| T-19.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. `19.2-RESEARCH.md` § Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
1. `python -m experiments.e3_derived_quantities --smoke` — exits 0.
2. `python -m experiments.e3_derived_quantities --force` — exits 0 and writes all six artifacts into `experiments/results/`.
3. `python -m experiments.e3_derived_quantities --check` — exits 0 against the just-written baseline.
4. `python -m pytest tests/unit/test_experiments_e3.py tests/unit/test_experiments_e3_constants.py -q` — exits 0.
5. `python -m pytest tests/ -m "not slow" -q` — exits 0, at least 874 passed.
6. `ruff check . && ruff format --check experiments/ tests/` — exits 0.
</verification>

<success_criteria>
- ROADMAP SC1 is TRUE: `code_constants.csv` (declared vs source with pass/fail),
  `newton_iterations.csv`, and `cpr_grouping.csv` all exist and are committed, with tier 1's
  constants also asserted in the test suite so CI breaks when a default changes.
- Every number in the three CSVs has exactly one origin: tier 1 from `DECLARED_CONSTANTS`, tier 2
  from the shipped Newton root-find, tier 3 either computed here or copied from E2's record and
  labelled as such.
- The default `tab:cpr` LaTeX fragment describes the production shared-interface configuration
  (D-21).
- E3 carries an environment-only provenance sidecar (EXP-11's carve-out).
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-05-SUMMARY.md` when done.
Record: the pooled Newton iteration min/median/max and whether the maximum exceeded four (a
finding for the manuscript if so); the exact `CPR_CONFIGS` list; the `n_params`/`n_groups` values
copied from E2's `benchmark.json`; and whether `cpr_derived_values.tex` carries a `\TODO`
placeholder pending E4's grid.
</output>
