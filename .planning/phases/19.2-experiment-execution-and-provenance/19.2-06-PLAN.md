---
phase: 19.2-experiment-execution-and-provenance
plan: 06
type: execute
wave: 2
depends_on: ["19.2-01", "19.2-02", "19.2-03", "19.2-14", "19.2-26"]
files_modified:
  - experiments/results/benchmark.json
  - experiments/results/real_rig_metrics.json
  - experiments/results/calibration.json
  - experiments/results/camera_parameters.csv
  - experiments/results/reprojection_residuals.csv
  - experiments/results/reconstruction_errors.csv
autonomous: true
requirements: [EXP-08]

must_haves:
  truths:
    - "E2's committed benchmark.json carries a memory block with per-stage peak readings and a named measurement mode, so E4's real-rig tenth row reports stage-attributed memory rather than nothing (D-07)"
    - "Section-3 numbers that moved are ACCEPTED when the movement is attributable to the degenerate-PnP guard and the guard rejected exactly 10 of 3538 poses; a changed rejection count, or movement no mechanism explains, still halts (D-34, overriding D-08)"
    - "The re-run used the LOCAL RELEASE FRAMESET via --config, not the published Zenodo archive, so the numbers being compared are the manuscript's own"
    - "The original release_calibration outputs are preserved as an untouched comparison baseline - the re-run writes to a fresh output directory"
    - "The comparison baseline is experiments/archive/e2-2026-07-30-pre-pnp-guard/, not the working tree - the --force run overwrites experiments/results/ BEFORE the comparison can be made (D-34)"
    - "The before/after of every moved section-3 value is recorded in .planning/MANUSCRIPT-FINDINGS.md, because the manuscript's section-3 prose must now be edited to match (D-34 downstream obligation)"
    - "The bit-identity test suites from plans 19.2-01, 19.2-02, 19.2-03 and 19.2-14 were run immediately before launch, passed, and their output is recorded in the run's artifacts - this is the gate, replacing the withdrawn git-diff-is-empty check (D-26)"
  artifacts:
    - path: "experiments/results/benchmark.json"
      provides: "pipeline-written record now carrying the memory block"
      contains: "memory"
    - path: "experiments/results/real_rig_metrics.json"
      provides: "the section-3 numbers, each moved value attributed to the PnP guard (D-34 - NOT 'unchanged'; movement is expected here)"
  key_links:
    - from: "the re-run config copy"
      to: "src/aquacal/config/schema.py benchmark_memory"
      via: "internals.benchmark_memory: true (defaults False)"
      pattern: "benchmark_memory"
    - from: "experiments/results/benchmark.json"
      to: "pipeline memory capture boundaries"
      via: "capture_peak_memory at _baseline / stage3_* / auxiliary_registration / validation"
      pattern: "psutil_peak_wset"
---

<objective>
> ## ⚠ AMENDED 2026-07-31 under D-34 — read this before anything else
>
> **This plan's central gate has been replaced.** As originally written it required every
> section-3 number to reproduce *exactly* and halted on any movement (D-08). **That premise is
> now false and the criterion would fail as written.**
>
> Between the plan's authoring and now, plan 19.2-21 surfaced two real library defects, fixed in
> `7e0cb90`. One of them — a **degenerate-`cv2.solvePnP` guard** — is deliberately **NOT inert**.
> On this frameset it rejects **10 of 3538 poses**, so E2's section-3 numbers **WILL move**, by
> design. Every experiment except E3 was re-checked with `--check` and moved.
>
> **D-34 (user, 2026-07-31) overrides D-08 for this run** and accepts the moved numbers within a
> stated scope. The replacement gate is not a wider tolerance — it is a **different question**:
> *"is this movement consistent with the PnP guard?"* rather than *"is this movement zero?"*
>
> **Do not 'fix' the old criterion by loosening a tolerance.** That is anti-pattern #3 in
> `.continue-here.md` and is recorded as blocking. Amending the question is the sanctioned move;
> widening the threshold until it passes is the failure mode this phase has already hit once.
>
> Three consequences ripple through the plan and are marked **[D-34]** at each site:
> Task 2's comparison logic, the comparison **baseline** (now the archive, not the working tree),
> and a new **downstream obligation** to record before/after in `MANUSCRIPT-FINDINGS.md`.
>
> Unchanged: everything about *how the run is launched* — the frameset, the two-line config edit,
> the quiescence checks, the `src`-frozen temporal constraint, and the D-26 bit-identity gate.

Re-run E2 against the local release frameset with `internals.benchmark_memory: true` so the
committed real-rig record carries stage-attributed peak memory (D-07), and adjudicate the moved
section-3 numbers against D-34's attribution test.

Purpose: E4's tenth grid point reuses E2's `benchmark.json` (sequencing constraint 9, EXP-04
precedes EXP-08). Without the memory block, the real row would carry no memory while the nine
synthetic rows do, reintroducing in a second column the comparability problem `record_source` and
`timing_scope` exist to fix.
Output: a refreshed set of committed E2 artifacts whose moved numbers are each **attributed to the
degenerate-PnP guard** (D-34 — not "unchanged"; movement is expected), plus a memory block
attributing peak growth to a stage.

**This is a production run, not a test step.** ~50 minutes. Launch with `python -u` in the
background per `CLAUDE.md`; without `-u` the log stays empty for the entire run and a real stall is
indistinguishable from normal progress.

**Why this plan is in wave 2, after every `src`-modifying plan — D-26.** An earlier revision of
this plan set placed it in wave 1 on the argument that D-08's determinism premise required an
unmodified `src/`. **That placement was withdrawn by the user's ordering principle D-26:** *all `src`
changes land, and are proven inert, before any experiment that yields a publishable result.* Three
things forced it:

1. **Wave 1 was self-contradictory.** It held plans 19.2-01, 19.2-02 and 19.2-03, all of which modify
   `src/`, alongside this plan, whose own acceptance criterion requires `git status --porcelain src/
   experiments/` to be empty at both launch and completion. `files_modified` disjointness held — which
   is why the wave model could not see the conflict — because this plan's constraint is **temporal**
   (`src/` frozen for the duration of the run), not file-based.
2. **This record needs two fields that only `src` changes supply.** `n_residuals` comes from plan
   19.2-02's addition to `SolverDiagnostics`, which IS captured on the pipeline path — and plan
   19.2-09 reuses this record as the grid's tenth row, where D-15's element count is
   `n_residuals × n_params`. `solver_config["seed"]` comes from plan **19.2-14**; without it this
   fresh record would fail plan 19.2-12's own `test_every_benchmark_record_carries_a_seed`.
   Running first and carving the two fields out as null-with-reason was offered to the user and
   **rejected**: it would publish a §3 record less complete than the nine synthetic cells beside it.
3. **The determinism argument survives in a stronger form.** See the gate below.

**[D-34] D-08's hard stop is now SCOPED, not unconditional.** The text below records the reasoning
as it stood before 2026-07-31; it is superseded and retained so the change is auditable.

~~D-08's hard stop is NOT weakened — only its justification changes. The premise is no longer
"source is bit-identical to `f193d60`". It is now: source changed, and every change is covered by a
passing exact-equality bit-identity test. A moved §3 number therefore still halts.~~

**What actually holds now.** That premise died with `7e0cb90`. The four wave-1 changes are still
each covered by an exact-equality guard — but the **PnP guard is a fifth `src` change that is
deliberately not inert**, and no bit-identity test could cover it without asserting the bug it
fixes. So "every change is proven inert" is false, and a moved number is no longer an unambiguous
defect signal.

The gate is therefore **attribution, not equality**:

| | halts? | why |
|---|---|---|
| numbers moved, rejection count is exactly 10 of 3538, movement plausibly follows from 10 dropped poses | **no — accept** | this is the fix working as intended (D-34) |
| rejection count is anything other than 10 of 3538 | **HALT** | the guard's behaviour on this frameset changed; something else moved |
| a number moved that 10 dropped poses cannot explain (e.g. a quantity computed on retained poses only) | **HALT** | unexplained movement, still a defect |
| `nfev`, `njev`, `optimality`, `cost` moved *without* any accuracy number moving | **HALT** | that is nondeterminism, not the guard |

**A caveat the executor must not paper over.** D-34 also asks that magnitude be "comparable to the
measured seed-noise band". **That test is not mechanically available for E2.** Verified live at
`experiments/e2_real_rig.py:550-581`: the `--config` branch returns before the seed log at line 587
and never reads `args.seed` at all — E2's seed comes from the config file's own key, and Task 1
forbids editing it. Measuring E2's own band would need a second 48-87 min run at a different config
seed, which **D-34 did not authorise**. So:

- E1's and E7's measured bands (E1: fix delta 12-105x smaller than seed range; E7 `shared_fixed`:
  bit-identical) are an **order-of-magnitude reference only**. Do not present them as E2's band.
- The **load-bearing** check is the mechanistic one: exactly 10 of 3538 rejected, and movement that
  10 dropped poses can account for.
- If movement looks larger than 10 of 3538 poses could plausibly produce, **HALT and report** —
  and name the second-seed run as the escalation the orchestrator may authorise. Do not run it
  unasked; do not accept the number instead.

**The mechanical launch gate is unchanged:** `git diff 5762d6d..HEAD -- src/` is legitimately
non-empty and MUST NOT be used; Task 1 requires the four bit-identity suites to pass immediately
before launch, with their output recorded. That gate proves the *wave-1* changes inert. It says
nothing about the PnP guard, and must not be cited as if it did.

Its `files_modified` are all under `experiments/results/` and collide with no other plan in wave 2 or
earlier, so parallelism is safe. It is the phase's first production run; plans 19.2-09, 19.2-13 and
19.2-11 are the other three, and each sits in its own wave so no two ever overlap on the box (review
H4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** this plan forks from the phase integration HEAD **after wave 1 has merged** — i.e.
a base that already contains plans 19.2-01, 19.2-02, 19.2-03 and 19.2-14. Do NOT pin the old
`5762d6d` base; under D-26 that base is now wrong. Verify your base by the two checks Task 1 requires:
the four wave-1 summary files exist, and the four bit-identity suites pass. If the orchestrator
interpolated an `EXPECTED_BASE` sha, confirm `git merge-base HEAD $EXPECTED_BASE` matches it before
doing anything else — executor worktrees have twice forked from a stale base in this milestone.
**PYTHONPATH:** the editable install resolves to the main checkout, not a worktree. If working in
a worktree, `export PYTHONPATH="$(pwd)/src"` before running pytest or its passing tests are
meaningless. **This matters more here than anywhere else in the phase** — a re-run executed
against the wrong checkout's library code invalidates the entire D-08 determinism argument.

**Do NOT run this plan in a worktree unless PYTHONPATH is exported first and verified with
`python -c "import aquacal, pathlib; print(aquacal.__file__)"` pointing inside your working tree.**
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-CONTEXT.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-FRAMESET-PROVENANCE.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md
@experiments/README.md

<interfaces>
<!-- Contracts the executor needs. Extracted live from the codebase and disk 2026-07-28. -->

E2's CLI (`experiments/e2_real_rig.py`):
  build_arg_parser()  = the shared five flags + `--config <path/to/config.yaml>`
  Without --config it runs the PUBLISHED ZENODO ARCHIVE (a ~4.3x frame-subsampled extraction,
  1,817 comparisons). With --config it runs the full local frameset and reproduces section 3.
  _run_real_calibration chdirs to `config_path.parent` for the duration of run_calibration and
  restores cwd afterwards; it then reads `cfg.output_dir` (absolute here) and copies
  `benchmark.json` and `calibration.json` out of it verbatim — never reconstructing them.

The release config of record:
  C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/config.yaml
  - every video path is ABSOLUTE, so the file can be copied elsewhere and still resolve
  - `paths.output_dir: "C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration"` — i.e. the
    config writes back into the directory that also holds `diagnostics.json`, the release
    calibration's own record
  - `detection.frame_step: 30`, `optimization.max_calibration_frames: 200`,
    `validation.holdout_fraction: 0.2`, `interface.n_water: 1.333`
  - it has NO `internals:` block today

The flag to add (`src/aquacal/config/schema.py`, `benchmark_memory` defaults to False):
  internals:
    benchmark_memory: true

Pipeline memory capture boundaries (`src/aquacal/calibration/pipeline.py`), in temporal order:
  _baseline, stage3_interface_optimization, stage3_rerun, stage3_intrinsic_pass,
  auxiliary_registration, validation
  `_baseline` is captured AFTER Stage 1 and Stage 2, so detection + Stage 1 + Stage 2 all fold
  into the baseline figure. The available split is pre-optimization vs per-optimization-stage.
  Separating detection from Stage 1/2 is explicitly OUT of scope (D-07 finding 2).
  Readings are documented monotonic high-water marks, so CONSECUTIVE DELTAS attribute growth to
  a stage — a stage's reading is not its own allocation.

Committed comparison baseline: `experiments/results/real_rig_metrics.json` (the 19.1 run of
record, full local frameset, 7,762 comparisons).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare an isolated re-run config and launch the run in the background</name>
  <files>(no repo files modified; writes only under the scratch re-run directory)</files>
  <read_first>
    - `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-FRAMESET-PROVENANCE.md` — the
      FULL file. It is the authority on which input the run of record used and why the Zenodo
      archive must not be substituted.
    - `experiments/e2_real_rig.py` lines 526-620 — `_run_real_calibration`'s `--config` branch,
      including the `os.chdir(config_path.parent)` behavior and how `output_dir` is resolved.
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/config.yaml` — the FULL file,
      especially the `paths` block (all absolute), `interface`, `optimization`, `detection`, and
      `validation` sections, and confirm for yourself that no `internals:` block exists.
    - `src/aquacal/config/schema.py` — the `internals` YAML surface and `benchmark_memory`'s
      default, so the added key is spelled exactly as `load_config` parses it.
    - `CLAUDE.md` § "Always run calibration unbuffered".
    - `19.2-CONTEXT.md` § **D-26** — the ordering principle that put this plan in wave 2, and the
      exact wording of the bit-identity gate that replaces the withdrawn `git diff .. -- src/` check.
    - The four wave-1 summaries this plan depends on:
      `19.2-01-SUMMARY.md`, `19.2-02-SUMMARY.md`, `19.2-03-SUMMARY.md`, `19.2-14-SUMMARY.md` —
      specifically each one's recorded bit-identity/zero-numerical-change test result. If any of the
      four is missing, wave 1 has not merged and this plan must not run.
  </read_first>
  <action>
**FIRST, before anything else: run the D-26 bit-identity gate and record its output.** This replaces
the withdrawn `git diff 5762d6d..HEAD -- src/` is-empty check, which under D-26 is now legitimately
false. Every `src` change this phase makes must have landed AND be covered by a passing
exact-equality test before a publishable result is produced:

0. **[D-34] Confirm the base actually contains the PnP guard and its instrumentation.** Run
   `git merge-base --is-ancestor 7e0cb90 HEAD` and confirm it exits 0, and confirm
   `19.2-26-SUMMARY.md` exists. **This is the single most dangerous false pass in the plan.** A
   worktree forked from a stale base runs *without* the guard, rejects 0 poses, and reproduces the
   archive exactly — `0.000%` on every row, which is precisely what this plan's PRE-amendment
   criterion called success. It reads as a triumphant reproduction and is in fact a run of the
   wrong library. Executor worktrees have forked from a stale base **twice** in this milestone.
   Record the resolved `HEAD` sha verbatim in the summary. If either check fails, STOP.
1. Confirm all four wave-1 summaries exist:
   `19.2-01-SUMMARY.md`, `19.2-02-SUMMARY.md`, `19.2-03-SUMMARY.md`, `19.2-14-SUMMARY.md`.
2. Run, from the repository root and with `PYTHONPATH` verified:
   `python -m pytest tests/unit/test_datasets_pipelines.py tests/unit/test_observability.py
   tests/unit/test_experiments_io.py tests/unit/test_refractive_geometry.py
   tests/unit/test_pipeline.py tests/synthetic/test_full_pipeline.py -q`
   with **no marker filter** — the slow bit-exactness tests are the ones that actually exercise the
   optimization, so `-m "not slow"` would skip the evidence the gate exists to gather.
3. Run the full suite: `python -m pytest tests/ -q`.
4. Record both commands verbatim with their pass/fail counts, into the plan summary AND into a text
   file placed beside the run log inside the re-run directory, so the gate result travels with the
   run's artifacts rather than only with the planning documents.

If any of those fails, or any of the four summaries is absent, **STOP and report to the orchestrator.**
Do not launch the calibration. Do not run it "anyway to save time" and reconcile later — a production
run against a `src` whose inertness is unproven is precisely what D-26 forbids, and its output could
not be distinguished from a determinism defect.

Then create a re-run directory OUTSIDE the repository, e.g.
`C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/`. Copy
`C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/config.yaml` into it and make exactly two
edits to the copy:

1. Add an `internals:` block containing `benchmark_memory: true`. This is the one line D-07
   requires; `benchmark_memory` defaults to `False`, so without it the run produces no memory
   readings at all.
2. Change `paths.output_dir` to point at a fresh subdirectory of the re-run directory, e.g.
   `C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/output`.

**Change nothing else.** Not `frame_step`, not `max_calibration_frames`, not `holdout_fraction`,
not `n_water`, not `initial_water_z`, not the camera list, not a video path. **[D-34]** The reason
is now *stronger*, not weaker: source is no longer identical (the PnP guard changed it), so the
input is the ONLY remaining control. Task 2's attribution test asks whether movement follows from
10 dropped poses — a question that becomes unanswerable if the input moved too. An incidental
config edit would make the run uninterpretable rather than merely imprecise.

**[D-34] The config's own `seed` key is covered by this prohibition.** Varying it to measure E2's
noise band is a legitimate experiment, but it is a *different* run that D-34 did not authorise, and
doing it here would destroy this run's comparability with the archive.

The `output_dir` change is not cosmetic: the shipped release config writes back into
`release_calibration/`, which also holds `diagnostics.json` — the release calibration's own record
and a comparison baseline. Overwriting it would destroy the artifact the comparison depends on.
Confirm after the run that `release_calibration/diagnostics.json`'s modification time is unchanged.

Record a diff of the two config files (original vs copy) in the plan summary so the two edits are
auditable.

Launch the run in the background, unbuffered, from the repository root:
`python -u -m experiments.e2_real_rig --config <path to the copied config> --force`
redirecting stdout and stderr to a log file inside the re-run directory. Do not run it in the
foreground; do not omit `-u`.

While it runs, do not modify any file under `src/` or `experiments/` — a mid-run edit to the
library makes the run's provenance unverifiable, and it would land after the bit-identity gate
already passed, so nothing would have proven it inert. This is the temporal constraint D-26 exists to
protect and the reason this plan sits in its own wave.

Poll the log until the process exits. The expected wall-clock is roughly 50 minutes. Treat a
completely empty log after several minutes as a signal to check the process is alive, not as a
reason to relaunch.

**Establish quiescence BEFORE launching, and confirm it again at completion (review H4).** This
plan's `benchmark.json` carries per-stage wall-clock `seconds` and a `memory` block, and plan
19.2-09 reuses this record as the E4 grid's real-rig tenth point — so a concurrent process makes
those fields measure contention rather than the calibration, and the contamination propagates
into the published grid. Before launch, verify that no other AquaCal experiment process is
running — no second E2 re-run, no E4 grid, no E5 band, no E6 sweep, no `--smoke` invocation from
another session — and record the process listing you inspected and its result verbatim in the
summary. Repeat the check at completion and record it again. If another AquaCal process appears
mid-run, record when it appeared, report to the orchestrator, and do not silently publish
contaminated timings.

**Duplicate prevention is part of that check, not a separate nicety.** Enumerate running
processes for an existing `experiments.e2_real_rig` command line specifically, and **abort rather
than launch** if one is found. This is not hypothetical: on 2026-07-28 two E2 re-runs were
started against each other because a quiet subagent transcript was misread as a dead run. They
consumed ~7 GB combined on a 15.7 GB box and contaminated the survivor's wall-clock. A live
process listing is a liveness signal that buffering cannot fake; an empty log is not.

Note this check governs *timing and memory only*. The section-3 comparison in Task 2 is
load-independent — identical frameset, seeded holdout, and a deterministic solve — so contention
can never move those numbers. **[D-34]** Machine load is therefore never an admissible explanation
for a moved section-3 value: the only sanctioned explanation is the PnP guard, and anything else
halts. Do not weaken or skip Task 2's attribution test on the grounds that the box was busy.
  </action>
  <verify>
    <automated>test -f "C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/output/benchmark.json" && python -c "
import json,pathlib
r=json.loads(pathlib.Path('C:/Users/tucke/Desktop/Aqua/AquaCal/rerun_19_2/output/benchmark.json').read_text())
assert 'memory' in r, 'no memory block - benchmark_memory was not enabled'
assert r['memory']['whole_run_peak_bytes'] > 0, r['memory']
assert r['solver_config'].get('seed') is not None, 'no seed - plan 19.2-14 did not land'
staged = {k for k, v in r['stages'].items() if 'memory' in v}
assert 'stage3_interface_optimization' in staged and 'validation' in staged, staged
print('mode', r['memory']['mode'], '| stages with memory', sorted(staged))"</automated>
  </verify>
  <acceptance_criteria>
    - The copied config differs from the release config in exactly two respects: a new `internals.benchmark_memory: true` and a changed `paths.output_dir`. Demonstrated by a recorded `diff` with no other differing lines.
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/diagnostics.json` has an unchanged modification time after the run.
    - **The D-26 bit-identity gate passed at launch time, and its output is recorded.** `python -m pytest tests/unit/test_datasets_pipelines.py tests/unit/test_observability.py tests/unit/test_experiments_io.py tests/unit/test_refractive_geometry.py tests/unit/test_pipeline.py tests/synthetic/test_full_pipeline.py -q` (no marker filter) exits 0 with 0 failed and 0 errors, AND `python -m pytest tests/ -q` exits 0. Both command lines and their summary lines are recorded verbatim in the plan summary and in a text file stored beside the run log. **This is the replacement for the withdrawn `git diff 5762d6d..HEAD -- src/` check, which under D-26 is legitimately non-empty and must NOT be used as a gate.** If either command fails, the plan STOPS without launching and reports to the orchestrator; it does not run the calibration anyway and does not weaken Task 2's comparison to compensate.
    - **All four wave-1 summaries exist**: `ls .planning/phases/19.2-experiment-execution-and-provenance/19.2-0{1,2,3}-SUMMARY.md .planning/phases/19.2-experiment-execution-and-provenance/19.2-14-SUMMARY.md` exits 0. If any is missing, wave 1 has not merged and the plan STOPS.
    - **`git diff --stat HEAD -- src/` is empty at launch** (working tree clean with respect to `src/`) — the merged wave-1 changes are committed, and no uncommitted local edit is in play.
    - The run was launched with `python -u` and `--config`, in the background, with output redirected to a log file. The command line is recorded verbatim in the summary.
    - The run's `output_dir` contains `benchmark.json`, `calibration.json`, and `spatial_measurements.csv`.
    - The run's `benchmark.json` contains a top-level `memory` key.
    - The top-level `memory` block names a measurement mode (`memory["mode"]`; on this machine `psutil_peak_wset`) and a positive `whole_run_peak_bytes`.
    - Per-stage readings are present as `stages["stage3_interface_optimization"]["memory"]` and `stages["validation"]["memory"]`, each carrying `cumulative_peak_bytes_as_of_stage_end`, `delta_bytes_since_previous_boundary`, and `mode`. NOTE the record's actual shape, verified live in `io/benchmark.py:394-448`: `_baseline` is consumed as the delta origin and never appears as a key, and the top-level `memory` block holds only `whole_run_peak_bytes` and `mode` — stage attribution comes from the per-stage `delta_bytes_since_previous_boundary` fields.
    - The run's `benchmark.json` carries `solver_config["seed"]` (supplied by plan 19.2-14) and `stages.*.n_residuals` (supplied by plan 19.2-02) — the two fields D-26 identified as missing and the reason this plan waits for wave 1.
    - No file under `src/` or `experiments/` was modified between launch and completion — `git status --porcelain src/ experiments/` is empty at both points.
    - **No pre-existing `experiments.e2_real_rig` process was running at launch.** The process listing inspected is recorded verbatim in the summary. If one was found, the plan aborted without launching rather than running a second copy alongside it (review H4).
    - **The summary records the quiescence check verbatim at BOTH launch and completion**: the process listing inspected and the confirmation that no other AquaCal experiment process was running (review H4). If another appeared mid-run, the summary names it, states when it appeared, and flags the `seconds` and `memory` fields as contaminated — because plan 19.2-09 reuses this record as the grid's tenth point.
  </acceptance_criteria>
  <done>A full local-frameset E2 run completed against an isolated config differing from the release config only by the memory flag and the output directory, producing a `benchmark.json` with a populated `memory` block, and the release calibration's own outputs are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Verify every section-3 number is unchanged, then commit the refreshed artifacts</name>
  <files>experiments/results/benchmark.json, experiments/results/real_rig_metrics.json, experiments/results/calibration.json, experiments/results/camera_parameters.csv, experiments/results/reprojection_residuals.csv, experiments/results/reconstruction_errors.csv</files>
  <read_first>
    - **[D-34] `.planning/phases/19.2-experiment-execution-and-provenance/.continue-here.md`
      § "D-34" and § "Critical Anti-Patterns"** — the decision that overrides D-08 for this run,
      its scope, and the blocking anti-pattern against loosening a gate to make it pass. **Read
      this before the two items below**, both of which predate it.
    - **[D-34] `experiments/archive/e2-2026-07-30-pre-pnp-guard/README.md`** — the comparison
      baseline of record and how to retrieve the four artifacts stored only in git.
    - `experiments/results/real_rig_metrics.json` — **[D-34] NOT the baseline any more.** Task 1's
      `--force` run has already overwritten it. Read it only to enumerate the key set; compare
      against the archive.
    - `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md` — the nine
      named section-3 quantities. **[D-34] Its 0.000% result is the shape of the *table*, no longer
      the expected *outcome*** — those nine rows are now expected to move.
    - `19.2-CONTEXT.md` § D-08 AND § D-26 — the original hard-stop rule and its D-26 restatement.
      **[D-34] Both are superseded for this run** by the attribution test in the action below.
      Retained because the reasoning chain matters; do not apply D-08 as written.
    - `experiments/e2_real_rig.py` lines 610-674 — `_run_full`'s artifact writes and the
      `benchmark.json` / `calibration.json` copy step, so you know which files the run refreshed.
  </read_first>
  <action>
**[D-34] Establish the baseline BEFORE you compare — the working tree is already gone.** The
`--force` run in Task 1 overwrites `experiments/results/real_rig_metrics.json` and the other five
artifacts *before* this task runs. Comparing "fresh vs committed working tree" would therefore
compare the fresh run against itself and report a spurious 0.000% on every row.

The baseline is `experiments/archive/e2-2026-07-30-pre-pnp-guard/`, which exists for exactly this
purpose:
- `real_rig_metrics.json` and `camera_parameters.csv` — present as files.
- The four larger artifacts — retrieve with `git show 35d76a6:experiments/results/<name>`. They are
  not stored as files because they tripped `check-added-large-files` and `detect-secrets`; see that
  directory's README. **Do not bypass repo hooks to make a redundant copy.**

Compare key by key, for every key present in either file, and report a table of
(key, baseline, fresh, delta, delta_pct).

**[D-34] The gate is attribution, not equality.** D-08's unconditional stop is overridden for this
run: the degenerate-PnP guard in `7e0cb90` is a deliberate, non-inert correctness fix, so movement
is EXPECTED. Adjudicate as follows.

**Step 1 — establish the PRIMARY mechanism from `diagnostics.json`, not from the log.** Plan
**19.2-26** instruments the guard; before it merged, the guard was completely silent
(`return None` at `extrinsics.py:203` and `:214`, four bare `continue` consumers, zero `logger.`
calls in the module) and this count was **not recoverable from any artifact**. Read
`discard_stats.pnp_guard_rejected` and `discard_stats.pnp_attempts_total` from the re-run's
`diagnostics.json`. The expected pair is **10 and 3538**.

- Pair is **10 and 3538** → primary mechanism confirmed; continue to step 2.
- **Numerator** differs → **HALT**. The guard behaves differently on this frameset than when the
  blast radius was measured; this is not the movement D-34 accepted.
- **Denominator** differs → **HALT, and diagnose it as an INPUT error first, not a guard error.**
  `pnp_attempts_total` is a property of the frameset. A denominator near 3538 but unequal means the
  input changed; a denominator far below it means the Zenodo archive was used despite `--config`
  (T-19.2-20). Check the input before blaming the guard.
- `discard_stats` **absent** → **HALT.** Plan 19.2-26 has not merged, so the run was executed on a
  library where the mechanism is unobservable. Do not fall back to the handoff's remembered figure
  and do not assume 10 — an unverifiable mechanism is not an established one.

**Step 2 — establish the SECONDARY mechanism, the frame-rejection cascade.** `reject_outlier_frames`
defaults to **true** (`cli.py:604`, guardrail `frame_rejection_max_fraction: 0.25`). It is an
independent, post-Stage-3 dropper: 10 rejected poses shift Stage 3's per-frame RMS distribution, so
the rejecter may then drop a **different set of frames**. Movement can therefore be legitimately far
larger than "10 of 3538" alone suggests, and treating 10 dropped poses as the whole story would
produce a false HALT — which is the pressure that leads someone to loosen the gate.

Compare frame rejection on both sides. It is already recorded, so **no instrumentation is needed**:
- baseline: `C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/diagnostics.json` (the
  pre-guard run of record, which T-19.2-21 requires be left untouched — read it, never write it)
- fresh: the re-run's `output_dir/diagnostics.json`
Both carry the `frame_rejection` block from `save_diagnostic_report` (`pipeline.py:1659`).

Record both frame-rejection sets and their difference. **The attribution chain is two-stage:**
10 dropped poses → N changed frame rejections → the moved numbers.

**Step 3 — adjudicate each moved value against that two-stage chain:**

- **ACCEPT** — accuracy and comparison-count quantities that shift consistently with the combined
  effect of 10 dropped poses AND the observed frame-rejection difference. A changed comparison count
  is now *expected*, not a halt condition.
- **ACCEPT** — movement larger than 10 poses alone would suggest, **when the frame-rejection delta
  accounts for the excess** and that arithmetic is shown in the summary.
- **HALT** — any value that neither mechanism explains. Clearest case: a quantity computed only over
  retained poses that moves anyway.
- **HALT** — `nfev`, `njev`, `cost` or `optimality` moving while no accuracy number moves. That is
  solver nondeterminism, not the guard. D-34 did not accept it.
- **HALT** — the frame-rejection guardrail fired (`frame_rejection_max_fraction` suppressed a
  rejection, or the dropped fraction approaches 0.25). That means the cascade is no longer a small
  correction and the run needs adjudication, not acceptance.
- **HALT** — movement neither mechanism explains *quantitatively*. Report, and name a second run at
  a different config seed as the escalation the orchestrator may authorise. **Do not run it unasked,
  and do not accept the number instead.** Per the objective's caveat, E2's own seed-noise band is
  unmeasured and cannot be measured without that second run; E1's and E7's bands are an
  order-of-magnitude reference only and must not be quoted as E2's.

**[D-34] Do not widen a tolerance to make a row pass.** If a row will not fit the attribution test,
the answer is to halt and report it, never to raise a threshold. Anti-pattern #3 in
`.continue-here.md` is blocking, and this is the plan it was written about.

**[D-34] ON HALT, RESTORE THE WORKING TREE BEFORE REPORTING.** Task 1's `--force` run has already
overwritten all six files under `experiments/results/` on disk — a halt does not undo that, and
leaving post-guard artifacts in a dirty tree invites a later commit to sweep them in as though they
had passed. Run `git checkout -- experiments/results/` and confirm `git status --porcelain
experiments/results/` is empty before reporting the halt. Preserve the re-run's own output
directory untouched — that is the evidence; only the *repository* copies are reverted.

There is still **no `checkpoint:human-verify`** here. D-34 is the user's decision already taken —
it authorises acceptance within the scope above, so an accepted movement needs no further approval,
and a halt is a defect report rather than a request for adjudication.

**[D-34] Downstream obligation — this run creates manuscript work.** Section 3's published values
will no longer match the committed artifacts. Before finishing, append an entry to
`.planning/MANUSCRIPT-FINDINGS.md` recording, for every moved value: the name, the pre-guard
baseline, the post-guard value, the delta, and the attribution (PnP guard, 10 of 3538 poses). Note
the manuscript prose needs a human editing pass. Follow the file's existing MF-NN convention and
name the archive directory as the citable source for the "before" column.

Then refresh the committed artifacts under
`experiments/results/` from the re-run. The script's `--force` run already wrote
`camera_parameters.csv`, `reprojection_residuals.csv`, `reconstruction_errors.csv`, and
`real_rig_metrics.json`, and copied `benchmark.json` and `calibration.json` out of the pipeline's
own output directory. Confirm each of the six files' on-disk content is from this run, then
verify:

- `benchmark.json` now has a `memory` block that the previously committed version lacked;
- **[D-34]** every other block of `benchmark.json` (`problem_shape`, `stages`, `accuracy`,
  `solver_config`) is adjudicated by the same attribution test, not by equality:
  - `accuracy.*` and any observation/comparison counts **may move** — 10 rejected poses change what
    the solve sees. Record them; they feed the MANUSCRIPT-FINDINGS entry.
  - `stages.*.n_params`, `n_groups`, `fd_reduction` moving is **expected only if** the pose count
    changed; confirm the arithmetic ties to 10 dropped poses, and HALT if it does not.
  - `nfev`, `njev`, `cost`, `optimality` moving **while no accuracy number moved** is a HALT — that
    is solver nondeterminism, which D-34 did not accept.
  - wall-clock `seconds` and the `environment` block's timestamp/version fields move freely and are
    never a halt condition. **Note the timing caveat:** a Defender exclusion was added to the
    project tree on 2026-07-31, so this run's `seconds` are not comparable to the archived run's.
    Do not report a timing delta as a finding.

Compute and record, from the `memory` block's consecutive deltas, the pre-optimization versus
per-optimization-stage attribution of the peak. Record which stage the peak grew in. This is the
detection-vs-optimization view D-07 asked for; note in the summary that detection, Stage 1, and
Stage 2 all fold into `_baseline` and that finer attribution is explicitly out of scope.
  </action>
  <verify>
    <automated>python -c "
import json, pathlib, subprocess
# [D-34] The comparison must be PERFORMED, not asserted. Read the archive here so a
# skipped or self-referential comparison cannot pass this block. See T-19.2-29.
ARCH = pathlib.Path('experiments/archive/e2-2026-07-30-pre-pnp-guard/real_rig_metrics.json')
assert ARCH.is_file(), 'archived pre-guard baseline missing - comparison has no reference'
base = json.loads(ARCH.read_text())
fresh = json.loads(pathlib.Path('experiments/results/real_rig_metrics.json').read_text())
assert base is not fresh and base != {} , 'baseline unreadable'
keys = sorted(set(base) | set(fresh))
moved = [k for k in keys if isinstance(base.get(k), (int, float))
         and isinstance(fresh.get(k), (int, float)) and base[k] != fresh[k]]
print('BASELINE :', ARCH)
print('keys compared:', len(keys), '| moved:', len(moved))
for k in moved:
    print(f'  MOVED {k}: {base[k]} -> {fresh[k]}')
# A comparison that finds nothing moved is itself suspicious here: the PnP guard is
# known non-inert on this frameset, so zero movement means the guard did not run.
if not moved:
    print('WARNING: nothing moved. Verify HEAD contains 7e0cb90 and that this is not a stale-base run.')
r = json.loads(pathlib.Path('experiments/results/benchmark.json').read_text())
assert 'memory' in r, 'committed benchmark.json still has no memory block'
assert r['memory']['whole_run_peak_bytes'] > 0 and r['memory']['mode'], r['memory']
staged = {k for k, v in r['stages'].items() if 'memory' in v}
assert 'stage3_interface_optimization' in staged and 'validation' in staged, staged
assert r['solver_config'].get('seed') is not None, 'no solver_config.seed - plan 19.2-14 did not land'
assert 'n_residuals' in r['stages']['stage3_interface_optimization'], 'no n_residuals - plan 19.2-02 did not land'
print('mode', r['memory']['mode'], '| seed', r['solver_config']['seed'], '| stages with memory', sorted(staged))
metrics = json.loads(pathlib.Path('experiments/results/real_rig_metrics.json').read_text())
print('metric keys:', sorted(metrics))"</automated>
  </verify>
  <acceptance_criteria>
    - **[D-34]** A key-by-key comparison table of the re-run's `real_rig_metrics.json` against `experiments/archive/e2-2026-07-30-pre-pnp-guard/` (NOT the working-tree copy, which Task 1 overwrote) is recorded in the summary, covering every key in either file, with delta and delta_pct columns.
    - **[D-34]** `discard_stats.pnp_guard_rejected` and `discard_stats.pnp_attempts_total` are read from the re-run's `diagnostics.json` (NOT from the run log, which does not carry them, and NOT from the handoff's remembered figure) and recorded verbatim. The pair is exactly **10 and 3538**. A differing numerator halts; a differing denominator halts AND is diagnosed as an input error first (T-19.2-20); an absent `discard_stats` halts because plan 19.2-26 has not merged.
    - **[D-34]** The frame-rejection blocks from BOTH `release_calibration/diagnostics.json` (pre-guard baseline, read-only) and the re-run's `diagnostics.json` are recorded, with their difference. The summary shows the two-stage attribution arithmetic: 10 dropped poses → N changed frame rejections → the moved numbers.
    - **[D-34]** If the frame-rejection guardrail fired — a suppressed rejection, or a dropped fraction approaching `frame_rejection_max_fraction: 0.25` — the plan HALTED rather than accepting the movement.
    - **[D-34]** `git merge-base --is-ancestor 7e0cb90 HEAD` exited 0 before launch and the resolved HEAD sha is recorded. **A comparison showing zero movement is a FAILURE, not a success** — the guard is known non-inert on this frameset, so `0.000%` on every row means the run executed against a library without the guard (stale base) and must be investigated, never reported as a clean reproduction.
    - **[D-34]** If the plan halted, `git checkout -- experiments/results/` was run and `git status --porcelain experiments/results/` is empty — Task 1's `--force` run had already overwritten all six files on disk, so a halt without an explicit restore leaves post-guard artifacts staged for accidental commit. The re-run's own output directory was preserved untouched as evidence.
    - **[D-34]** Every moved row is classified in the summary as ACCEPTED (attributable to 10 dropped poses) or HALT, with the reasoning stated per row. A row that moved and was accepted names the mechanism; a row that could not be attributed halted the plan. **No row was accepted by widening a tolerance** — the summary states explicitly that no threshold was changed.
    - **[D-34]** If `nfev`, `njev`, `cost` or `optimality` moved while no `accuracy.*` value moved, the plan HALTED — that combination is solver nondeterminism, outside D-34's scope.
    - **[D-34]** An MF-NN entry was appended to `.planning/MANUSCRIPT-FINDINGS.md` recording every moved section-3 value as (name, pre-guard baseline, post-guard value, delta, attribution), naming the archive directory as the citable source for the "before" column and flagging that the manuscript's section-3 prose needs a human editing pass.
    - **[D-34]** The summary does NOT quote E1's or E7's seed-noise bands as if they were E2's. E2's own band is unmeasured — the `--config` branch never reads `args.seed` (`experiments/e2_real_rig.py:550-581`) — and the summary says so if magnitude is discussed at all.
    - `experiments/results/benchmark.json` contains a top-level `memory` block (`whole_run_peak_bytes` > 0 and a named `mode`) and per-stage `memory` sub-blocks on at least `stage3_interface_optimization` and `validation`. `_baseline` is the delta origin and correctly appears as no key at all.
    - `experiments/results/benchmark.json` carries `solver_config["seed"]` and `stages.stage3_interface_optimization.n_residuals`, so it passes plan 19.2-12's `test_every_benchmark_record_carries_a_seed` WITHOUT being added to `SEEDLESS_LEGACY_RECORDS` — that carve-out stays at exactly six (E1's two + E7's four).
    - `git diff --stat experiments/results/benchmark.json` shows the `memory` block, `solver_config.seed`, and `stages.*.n_residuals` as ADDITIONS; a comparison of the old and new `stages.*.n_params`, `n_groups`, `fd_reduction`, `nfev`, `njev`, `cost`, `optimality`, and every `accuracy.*` field shows no change. Those three additions are the only new fields expected — anything else new is a finding to report.
    - The summary records which stage the peak memory grew in, computed from consecutive `memory` deltas, and states that detection, Stage 1, and Stage 2 fold into `_baseline`.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 with at least 874 passed (this plan makes no library change of its own; this is a tripwire against an accidental edit during the run).
    - `git status --porcelain src/` is empty at completion, matching the launch-time check — no `src` file was touched while the run was in flight (the temporal constraint D-26 protects).
    - No `checkpoint:human-verify` task exists in this plan.
  </acceptance_criteria>
  <done>Every section-3 number reproduced exactly, the committed `benchmark.json` now carries a stage-attributed memory block, and the peak's stage attribution is recorded — or the plan halted with a full comparison table and touched nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local video files + release config (outside the repo) | The run reads absolute paths under `Desktop/Aqua/AquaCal/`. Trusted local data owned by the researcher; no untrusted input crosses into the pipeline. |
| committed artifact overwrite | The run rewrites six files under `experiments/results/` that the manuscript cites. This is the highest-consequence write in the phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.2-20 | Tampering | Re-running against the published Zenodo archive instead of the local release frameset moves section-3 numbers for reasons unrelated to memory, and the move is misread as nondeterminism | mitigate | The launch command must carry `--config <copied release config>`; acceptance criteria record the command verbatim and require the comparison against the archived pre-guard baseline. **[D-34]** The archived run's 7,762 comparisons are no longer the expected count — 10 rejected poses may change it — so the Zenodo-vs-local discriminator is the frameset scale (~260 frames vs ~60), not an exact comparison count. A count near 1,817 means the wrong input was used; a count near 7,762 but not equal to it is the guard. |
| T-19.2-21 | Tampering | The re-run overwrites `release_calibration/diagnostics.json`, destroying the comparison baseline | mitigate | `paths.output_dir` is repointed to a fresh directory; an acceptance criterion asserts the release directory's `diagnostics.json` modification time is unchanged. |
| T-19.2-22 | Tampering | An incidental config edit (frame_step, holdout, n_water) silently changes the input and invalidates the determinism argument | mitigate | A recorded `diff` must show exactly two changed items; every other key is named in the task text as forbidden to touch. |
| T-19.2-23 | Repudiation | **[D-34, rewritten]** Movement is accepted *wholesale* because D-34 authorised "the numbers will move", so a genuine second defect rides in alongside the PnP guard's expected movement and ships unexamined into the manuscript. This is the mirror image of the original threat: the old risk was explaining away a real defect, the new risk is a real defect hiding inside an authorised change | mitigate | Acceptance is per-row and mechanism-first, never blanket. The rejection count must be exactly 10 of 3538 before any row is accepted; each moved row is individually classified ACCEPTED-with-mechanism or HALT; solver-diagnostic movement without accuracy movement halts regardless; and movement exceeding what 10 dropped poses can produce halts with a named escalation rather than being absorbed. The summary must state that no threshold was widened. |
| T-19.2-28 | Repudiation | **[D-34, new]** The manuscript keeps its pre-guard section-3 values while the repository ships post-guard artifacts, so the paper and its own data silently disagree at submission | mitigate | A MANUSCRIPT-FINDINGS MF-NN entry recording every moved value with its before/after and attribution is a hard acceptance criterion of Task 2, and the manuscript tree is read-only from here so the finding has no other landing place. |
| T-19.2-29 | Tampering | **[D-34, new]** The comparison is run against `experiments/results/real_rig_metrics.json`, which Task 1's `--force` run already overwrote, so the fresh run is compared against itself and reports a spurious 0.000% on every row — the strongest possible false pass | mitigate | The baseline is named as `experiments/archive/e2-2026-07-30-pre-pnp-guard/` in Task 2's `read_first`, its action, and its acceptance criteria, with the working-tree copy explicitly flagged as no longer valid. The four artifacts not stored as files are retrieved via `git show 35d76a6:...`. |
| T-19.2-24 | Tampering | The run executes against the main checkout's library while the executor believes it is testing a worktree | mitigate | `execution_context` requires exporting `PYTHONPATH` and verifying `aquacal.__file__` resolves inside the working tree before launching. |
| T-19.2-25 | Information Disclosure | `benchmark.json` and `calibration.json` embed absolute local paths, CPU model, and git SHA | accept | Deliberate, required by EXP-11, and already the case in the committed records. Public research artifacts; no credentials or PII. |
| T-19.2-26 | Repudiation | **[D-34: scope narrowed — this row now governs the WAVE-1 changes only. The PnP guard in `7e0cb90` is a knowingly non-inert `src` change that no bit-identity test covers; it is not an "unguarded change slipping in" but an authorised one, and T-19.2-23 governs it instead.]** Under D-26 this plan deliberately runs AFTER 19.2-01/02/03/14 modify `src/`, so the hazard is no longer the ordering but **an unguarded `src` change slipping into the base** — a library edit that landed without a passing exact-equality bit-identity test, or one made mid-run after the gate already passed. Either would silently falsify D-26's premise ("source changed, and every change is proven inert"), and a moved section-3 number would degrade from an unambiguous defect signal into a judgement call about which unguarded edit caused it — exactly the adjudication D-08 exists to eliminate | mitigate | Task 1 carries a hard acceptance criterion replacing the withdrawn `git diff 5762d6d..HEAD -- src/` check: all four wave-1 summaries must exist, `pytest` over `test_datasets_pipelines.py`, `test_observability.py`, `test_experiments_io.py`, `test_refractive_geometry.py`, `test_pipeline.py` and `test_full_pipeline.py` (no marker filter, so the slow bit-exactness tests actually run) must exit 0, and the full suite must exit 0 — all immediately before launch, with output recorded beside the run log. `git status --porcelain src/` is asserted empty at BOTH launch and completion, closing the mid-run window. Halting is mandatory on any failure; the run is never launched "anyway". |
| T-19.2-27 | Denial of Service | Another AquaCal experiment — or a second copy of this very run — executes concurrently, so this record's per-stage `seconds` and `memory` block measure contention rather than the calibration. Because plan 19.2-09 reuses this `benchmark.json` as the E4 grid's real-rig tenth point, and `supplement.tex:543`'s WP2 hook awaits this 13-camera wall-clock by name, the contamination propagates into a published number | mitigate | Task 1 requires a recorded quiescence check at launch AND at completion, plus an explicit scan for an existing `experiments.e2_real_rig` command line that ABORTS rather than launching a duplicate. Observed 2026-07-28: two concurrent E2 re-runs consumed ~7 GB on a 15.7 GB box after a quiet subagent transcript was misread as a dead run — a live process listing is the liveness signal, not an empty log. Section-3 accuracy is unaffected either way (deterministic), so Task 2's hard stop is never weakened on contention grounds. |
| T-19.2-35 | Tampering | **[D-34, new]** The executor's worktree forks from a base predating `7e0cb90`, so the run executes a library WITHOUT the PnP guard. It rejects 0 poses and reproduces the archive exactly — `0.000%` on every row, which is what this plan's pre-amendment criterion called success. The most dangerous false pass available: a wrong-library run that reads as a triumphant reproduction. Worktrees have forked from a stale base twice in this milestone | mitigate | Task 1 step 0 requires `git merge-base --is-ancestor 7e0cb90 HEAD` to exit 0 and `19.2-26-SUMMARY.md` to exist, with the resolved HEAD sha recorded. An acceptance criterion inverts the reading of a zero-movement result: with a known non-inert guard, zero movement is a failure signal, not a pass. Task 2's automated block prints an explicit stale-base warning when nothing moved. |
| T-19.2-36 | Repudiation | **[D-34, new]** The frame-rejection cascade is ignored, so movement larger than 10 dropped poses alone predicts triggers a HALT that is actually explainable — and the pressure to "fix" the false halt lands on the tolerance, which is anti-pattern #3 arriving by the back door | mitigate | Task 2 step 2 makes the attribution chain explicitly two-stage and requires the frame-rejection blocks from both sides be compared. No instrumentation is needed: `frame_rejection` already reaches `diagnostics.json` (`pipeline.py:1659`) and the pre-guard baseline survives in `release_calibration/diagnostics.json`, which T-19.2-21 already protects. A fired guardrail halts rather than being absorbed. |
| T-19.2-37 | Tampering | **[D-34, new]** A halt leaves the six overwritten files under `experiments/results/` in the working tree, because Task 1's `--force` run wrote them before Task 2 ever compared. A later commit sweeps post-guard artifacts in as though they had passed the gate | mitigate | Task 2 requires `git checkout -- experiments/results/` on halt and asserts `git status --porcelain experiments/results/` is empty before the halt is reported, while preserving the re-run's own output directory as evidence. |
| T-19.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. `19.2-RESEARCH.md` § Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
0a. **[D-34] Base check (run FIRST):** `git merge-base --is-ancestor 7e0cb90 HEAD` exits 0, and `19.2-26-SUMMARY.md` exists. HEAD sha recorded.
0b. **[D-34] Mechanism check (run AFTER the calibration):** the re-run's `diagnostics.json` carries `discard_stats.pnp_guard_rejected == 10` and `discard_stats.pnp_attempts_total == 3538`; the frame-rejection blocks from both sides are recorded with their difference.
0. **D-26 gate (run BEFORE the calibration):** `python -m pytest tests/unit/test_datasets_pipelines.py tests/unit/test_observability.py tests/unit/test_experiments_io.py tests/unit/test_refractive_geometry.py tests/unit/test_pipeline.py tests/synthetic/test_full_pipeline.py -q` and `python -m pytest tests/ -q` — both exit 0, output recorded beside the run log.
1. `python -c "import json,pathlib; r=json.loads(pathlib.Path('experiments/results/benchmark.json').read_text()); assert 'memory' in r and r['solver_config'].get('seed') is not None"` — exits 0.
2. **[D-34]** The recorded key-by-key comparison of `real_rig_metrics.json` is against `experiments/archive/e2-2026-07-30-pre-pnp-guard/`, the run log's pose-rejection count is exactly 10 of 3538, and every moved row carries a per-row ACCEPTED-with-mechanism or HALT classification. (The former `0.000%`-on-every-row check is superseded — it would now fail by design.)
3. `git diff --stat experiments/results/` shows only the expected six files.
4. `stat` on `C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/diagnostics.json` shows an unchanged modification time.
5. `python -m pytest tests/ -m "not slow" -q` — exits 0, at least 874 passed.
</verification>

<success_criteria>
- E2's committed `benchmark.json` carries a `memory` block with a named measurement mode and
  per-boundary readings, so E4's tenth row can report stage-attributed memory (D-07).
- **[D-34]** Every moved section-3 number was individually attributed to the degenerate-PnP guard
  against a confirmed rejection count of 10 of 3538, or the plan halted and reported a defect
  without writing anything. No threshold was widened to make a row pass.
- **[D-34]** The comparison was made against `experiments/archive/e2-2026-07-30-pre-pnp-guard/`,
  not the working-tree copy that Task 1's `--force` run overwrote.
- **[D-34]** A MANUSCRIPT-FINDINGS MF-NN entry records every moved value's before/after and
  attribution, so the manuscript's section-3 prose has a landing place for its human editing pass.
- The refreshed record carries `solver_config["seed"]` and `stages.*.n_residuals`, so it is as
  complete as the nine synthetic cells it will sit beside in `benchmark_grid.csv` (D-26).
- The four **wave-1** `src` changes had landed and passed their exact-equality bit-identity tests
  before the run started, with the gate output recorded beside the run log (D-26). **[D-34]** That
  gate covers wave 1 only — the PnP guard is deliberately not inert and no bit-identity test covers
  it; do not cite the gate as if it proved otherwise.
- The run used the local release frameset via `--config`, and the release calibration's own
  outputs were not overwritten.
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-06-SUMMARY.md` when done.
Record: the D-26 bit-identity gate's two pytest command lines with their verbatim summary lines and
the path of the copy stored beside the run log; the four wave-1 summary files confirmed present; the
verbatim launch command; the two-line config diff; the full key-by-key section-3 comparison table with
delta_pct, **[D-34] taken against `experiments/archive/e2-2026-07-30-pre-pnp-guard/` and carrying a
per-row ACCEPTED-with-mechanism / HALT classification**; **[D-34] the pose-rejection count extracted
verbatim from the run log, which must read exactly 10 of 3538**; **[D-34] the MF-NN entry number
appended to `.planning/MANUSCRIPT-FINDINGS.md`**; **[D-34] an explicit statement that no tolerance or
threshold was widened**; the per-stage `memory` sub-blocks, the top-level `mode` and
`whole_run_peak_bytes`, and the stage the peak grew in (from `delta_bytes_since_previous_boundary`);
the values of `solver_config.seed` and `stages.*.n_residuals`; and the measured wall-clock **[D-34]
flagged as not comparable to the archived run's, per the 2026-07-31 Defender exclusion**. If the plan
halted, record the halt reason and every diagnostic listed in Task 2 instead.
</output>
