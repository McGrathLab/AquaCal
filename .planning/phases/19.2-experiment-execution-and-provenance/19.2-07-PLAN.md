---
phase: 19.2-experiment-execution-and-provenance
plan: 07
type: execute
wave: 3
depends_on: ["19.2-01", "19.2-02", "19.2-06"]
files_modified:
  - experiments/e4_benchmark_grid.py
  - tests/unit/test_experiments_e4.py
autonomous: true
requirements: [EXP-08]

must_haves:
  truths:
    - "E4 builds its nine cells by direct calls to generate_camera_array + generate_board_trajectory -> calibrate_synthetic -> assemble_benchmark_record, the same path E1 and E7 already run in production (D-01)"
    - "Every declared cell emits a row unconditionally with status and status_reason; a coverage gap is countable rather than invisible (D-04)"
    - "Every row carries timing_scope and record_source, so the nine optimization-only assembled rows are never silently compared against E2's end-to-end pipeline-written row (D-02)"
    - "Each row carries the accuracy block and the CPR columns copied from its own benchmark.json, so no number in the grid has a second origin (D-14, D-16)"
    - "Each row carries the Jacobian element count as n_residuals x n_params, turning the dense-regime claim into a measured column (D-15)"
    - "The 12-camera cell is a grid-family scene on its own terms and E6 reuses the same scene builder, so the cross-experiment anchor is a shared function rather than a coincidence (amended D-03, D-11)"
    - "The LaTeX fragment renders the real-rig row in its own labeled block, not as a tenth point on the scaling curve (D-02)"
  artifacts:
    - path: "experiments/e4_benchmark_grid.py"
      provides: "grid scene builder, per-cell runner, row assembly, aggregation, LaTeX"
      contains: "def build_grid_scenario"
      min_lines: 350
    - path: "tests/unit/test_experiments_e4.py"
      provides: "schema and declared-cell-coverage tests on hand-built fixtures"
  key_links:
    - from: "experiments/e4_benchmark_grid.py"
      to: "aquacal.datasets.pipelines.calibrate_synthetic"
      via: "direct call with diagnostics_out / timings_out / memory_out"
      pattern: "memory_out="
    - from: "experiments/e4_benchmark_grid.py"
      to: "experiments._io.write_direct_call_benchmark"
      via: "per-cell benchmark.json with memory_readings forwarded"
      pattern: "memory_readings="
    - from: "experiments/e4_benchmark_grid.py"
      to: "experiments._render.aggregate"
      via: "glob of per-cell benchmark.json into a flattened DataFrame"
      pattern: "aggregate\\("
---

<objective>
Rewrite `experiments/e4_benchmark_grid.py` from a real-config sweep runner into the direct-call
synthetic grid EXP-08 requires (D-01), with the status/scope/accuracy/CPR/element-count columns
D-02/D-04/D-14/D-15/D-16 specify, per-cell resumability, and a two-view LaTeX fragment.

Purpose: the relocated skeleton subsamples a real 13-camera YAML and calls
`run_calibration_from_config`. That cannot satisfy EXP-08 — a real run is 48–87 minutes, nine of
them is days of wall-clock against a 2026-08-21 deadline, and **16 cameras is unreachable from a
13-camera rig**.
Output: a rewritten module whose production run is plan 09; this plan writes and unit-tests the
code and leaves the nine-cell execution to that plan.

**Ruled out by inspection, not preference — do not reopen it.** Running synthetic data through the
real pipeline so `benchmark.json` would be pipeline-written was checked:
`run_calibration_from_config` starts from videos/framesets and has **no precomputed-detection
entry point**, so it would mean rendering synthetic images.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** fork from the merged result of plans 19.2-01, 19.2-02 and 19.2-06. Verify all
three landed before starting:
`python -c "import inspect; from aquacal.datasets.pipelines import calibrate_synthetic as f; assert 'memory_out' in inspect.signature(f).parameters"`,
`python -c "import inspect; from experiments._io import write_direct_call_benchmark as f; assert 'memory_readings' in inspect.signature(f).parameters"`,
and `python -c "import json,pathlib; assert 'memory' in json.loads(pathlib.Path('experiments/results/benchmark.json').read_text())"`
must all exit 0. If any fails, your base is stale — re-fork.
**PYTHONPATH:** the editable install resolves to the main checkout, not a worktree. If working in
a worktree, `export PYTHONPATH="$(pwd)/src"` before running pytest or its passing tests are
meaningless.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-CONTEXT.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-PATTERNS.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-RESEARCH.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-SOURCE-BRIEF.md
@experiments/README.md

<interfaces>
<!-- Contracts the executor needs. Extracted live from the codebase 2026-07-28. -->

Scene generators (`aquacal.datasets`):
  generate_camera_array(n_cameras: int, layout: str = "grid", spacing: float = 0.1,
      height_above_water: float = 0.15, height_variation: float = 0.005,
      image_size: tuple[int,int] = (1920,1080), fov_deg: float = 60.0, seed: int = 42)
      -> (dict[str,CameraIntrinsics], dict[str,CameraExtrinsics], dict[str,float])
      # layout accepts "grid", "line", "ring". cam0 is always the reference at the origin.
      # Inherently stochastic per camera; a fresh default_rng(seed) is created per call, so
      # reruns at the same seed reproduce, but different n_cameras values do NOT share
      # cameras 0..k-1's exact poses.
  generate_board_trajectory(n_frames: int, camera_positions: dict[str,NDArray],
      water_zs: dict[str,float], depth_range=(0.3,0.6), xy_extent=0.15,
      rotation_range_deg=15.0, min_cameras_per_frame=2, seed=42) -> list[BoardPose]
      # WARNING: the body never reads `camera_positions` and never enforces
      # `min_cameras_per_frame`. Poses are drawn uniformly at random with NO connectivity check.
      # A wide-baseline 16-camera cell may therefore produce a disconnected pose graph and fail
      # Stage 2. That is an expected, recorded outcome (D-04), not a bug to chase.

  `create_scenario("realistic")` calls `generate_real_rig_array()` — a FIXED 12-camera real-rig
  layout with NO n_cameras and NO layout parameter. It is a different generator entirely.
  E4's 12-camera cell is therefore a grid-family cell on its own terms; the "matches realistic
  exactly" clause was withdrawn (amended D-03). Do NOT attempt to make them match.

  The board config `create_scenario` uses is defined inline inside that function and is NOT
  exported: BoardConfig(squares_x=12, squares_y=9, square_size=0.060, marker_size=0.045,
  dictionary="DICT_5X5_100"). E4 declares it as a module constant here; E6 imports it from E4.

Calibration verb (`aquacal.datasets.pipelines`, after plan 19.2-01):
  calibrate_synthetic(scenario, n_water, refine_intrinsics=True, seed=42,
      diagnostics_out=None, timings_out=None, memory_out=None)
      -> (CalibrationResult, DetectionResult)
      # memory_out keys: "_baseline", "stage3_interface_optimization", "stage3_intrinsic_pass"

Held-out accuracy (the accuracy block must be MEASURED, not taken from
`calibrate_synthetic`'s DiagnosticsData, whose validation_3d_error_* fields are hardcoded 0.0):
  evaluate_calibration(calibration, detections, board, min_corners=8, cameras=None,
      board_poses=None, include_reconstruction=True) -> HeldOutEvaluation
      # aquacal.validation.evaluation; fields: reprojection, reconstruction, board_poses, num_frames

Record writing (`experiments/_io.py`, after plan 19.2-02):
  write_direct_call_benchmark(path, *, problem_shape, timings, diagnostics, solver_config,
      accuracy, memory_readings=None, force=False) -> bool
      # timings/diagnostics keys HARD-RESTRICTED to
      # {"stage3_interface_optimization", "stage3_intrinsic_pass"} — raises ValueError otherwise.
      # memory_readings is NOT subject to that allowlist (its "_baseline" key is legitimate).
      # Skips the write and returns False if `path` exists and `force` is False — this IS the
      # per-cell resumability mechanism.

Diagnostics fields available per stage (after plan 19.2-02): nfev, njev, cost, optimality,
status, message, ftol, xtol, gtol, max_nfev_effective, max_nfev_source,
n_params, n_groups, n_residuals (+ *_reason). `assemble_benchmark_record` additionally computes
`fd_reduction = n_params / n_groups` per stage.

Aggregation (`experiments/_render.py`, reuse UNMODIFIED):
  aggregate(root_dir: Path) -> pd.DataFrame
      # rglob's "benchmark.json" under root_dir, refuses on schema_version mismatch, and
      # json_normalize's each record, promoting record["stages"][name] to a dotted top-level
      # key. Yields stage3_interface_optimization.n_params, .n_groups, .fd_reduction,
      # accuracy.reprojection_rms, problem_shape.n_cameras, etc. with ZERO new code.
  write_latex_fragment(df: pd.DataFrame, path: Path, columns: list[str]) -> None
  _latex_escape(text: str) -> str

E2's tenth row source (pipeline-written, end-to-end): experiments/results/benchmark.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grid scene builder and per-cell runner with unconditional status recording</name>
  <files>experiments/e4_benchmark_grid.py</files>
  <read_first>
    - `experiments/e4_benchmark_grid.py` — the FULL current file. It is the relocated
      `benchmarks/sweep_runner.py` skeleton; `run_sweep` and `_build_arg_parser` are both being
      REPLACED, not extended. Read it so nothing of the old contract survives by accident.
    - `experiments/e7_interface_ablation.py` lines 184-320 (`_build_shared_data` / `_run_arm`) —
      the direct-call Stage-2/Stage-3 sequence with per-arm `SolverDiagnostics` and timing, and
      the discipline of constructing one fresh `SolverDiagnostics()` per stage per run.
    - `experiments/e1_refractive_comparison.py` — its CLI shape and how it builds an `accuracy`
      block from a held-out evaluation before calling `write_direct_call_benchmark`.
    - `src/aquacal/datasets/synthetic.py` — `generate_camera_array` in full (including the
      per-camera random roll and height variation), `generate_board_trajectory` in full
      (confirm for yourself that `camera_positions` and `min_cameras_per_frame` are unused),
      `SyntheticScenario`'s field list, and `create_scenario`'s inline `default_board` literal.
    - `src/aquacal/datasets/pipelines.py` — `calibrate_synthetic` as edited by plan 19.2-01.
    - `src/aquacal/validation/evaluation.py` lines 88-151 — `evaluate_calibration`'s signature and
      the `HeldOutEvaluation` fields.
    - `19.2-CONTEXT.md` §§ D-01, amended D-03, D-04, D-05, D-06.
    - `19.2-RESEARCH.md` § Pitfall 4 — the connectivity footgun this task's try/except exists for.
  </read_first>
  <action>
Delete `run_sweep` and `_build_arg_parser` and the `if __name__ == "__main__":` block from
`experiments/e4_benchmark_grid.py` entirely, along with the `yaml`, `copy`, `load_config`, and
`run_calibration_from_config` imports. Replace the module docstring with one describing E4 as the
direct-call synthetic benchmark grid (EXP-08), naming the five-flag CLI contract and the emitted
artifacts `benchmark_grid.csv` and `benchmark_grid.tex`, and stating that the real-rig tenth point
is E2's pipeline-written record rather than a run performed here.

Declare module constants:
  - `CAMERA_COUNTS = [8, 12, 16]`, `FRAME_COUNTS = [50, 100, 200]`
  - `DECLARED_CELLS` — the explicit nine-element list of `(n_cameras, n_frames)` pairs. This is
    P4's "the grid is data, not control flow": the list is the declaration, and every element of
    it produces a row whether or not its run succeeded.
  - `GRID_NOISE_STD = 0.5`, `GRID_LAYOUT = "grid"`, `GRID_N_WATER = 1.333`
  - `GRID_BOARD_CONFIG` — the `BoardConfig(squares_x=12, squares_y=9, square_size=0.060,
    marker_size=0.045, dictionary="DICT_5X5_100")` literal, with a comment stating it mirrors the
    board `create_scenario` builds inline and that E6 imports this constant rather than declaring
    a third copy.
  - `CHECK_RTOL = 1e-6`

Write `build_grid_scenario(n_cameras: int, n_frames: int, seed: int, *, layout: str = GRID_LAYOUT,
depth_range: tuple[float, float] | None = None, xy_extent: float | None = None,
n_water: float = GRID_N_WATER, name: str | None = None) -> SyntheticScenario`. It calls
`generate_camera_array(n_cameras=..., layout=layout, seed=seed)`, then
`generate_board_trajectory(n_frames=..., camera_positions=<the extrinsics' centers>,
water_zs=..., seed=seed, ...)` passing `depth_range`/`xy_extent` only when not `None` so the
generator's own defaults remain the grid family's fixed values, and assembles a
`SyntheticScenario` with `board_config=GRID_BOARD_CONFIG`, `noise_std=GRID_NOISE_STD`,
`n_air=1.0`, `n_water=n_water`, and `seed=seed`.

This function is E6's baseline as well as E4's cell builder (D-11): E6's baseline is
`build_grid_scenario(12, <baseline frames>, seed)` — identical construction to E4's 12-camera
cell. Its `layout`, `depth_range`, `xy_extent`, and `n_water` keywords exist so E6 can vary exactly
one axis at a time through that shared baseline without duplicating the scene code a third time.
Give it a Google docstring saying so, and stating that the grid family is `generate_camera_array`
and is a different generator from `create_scenario("realistic")`'s `generate_real_rig_array` — so
no future reader tries to unify them.

Write `run_grid_cell(n_cameras, n_frames, seed, out_dir, force) -> dict`. For one cell it must:
  - build the scenario via `build_grid_scenario`;
  - construct one fresh `SolverDiagnostics()` per stage, and fresh `timings`/`memory` dicts;
  - call `calibrate_synthetic(scenario, n_water=GRID_N_WATER, refine_intrinsics=True, seed=seed,
    diagnostics_out=..., timings_out=..., memory_out=...)`;
  - generate a SEPARATE held-out pose set at a different seed, generate its detections at the same
    indices, and call `evaluate_calibration` to build a real `accuracy` block. Do NOT read
    `result.diagnostics.validation_3d_error_mean` / `_std` for this — `calibrate_synthetic`
    hardcodes both to `0.0`, so copying them would put two fabricated zeros into a published
    table. The `accuracy` block must carry `reprojection_rms`,
    `validation_3d_error_mean`, and `validation_3d_error_std` measured from the held-out
    evaluation, keyed exactly as E2's pipeline-written record keys them;
  - write one `benchmark.json` per cell into `out_dir / "e4_cells" / f"cameras_{n}_frames_{m}"`
    via `write_direct_call_benchmark(..., memory_readings=<the memory dict>, force=force)`. One
    file per cell is the per-configuration resumability mechanism — the writer's own
    skip-if-exists-unless-force applies naturally per cell. Do NOT aggregate in memory across all
    nine cells before writing;
  - return a dict carrying at minimum `n_cameras`, `n_frames`, `status`, `status_reason`.

Wrap the whole per-cell body in `try/except Exception`. On success set `status="ok"` and
`status_reason=""`; on an exception set `status="failed"` and `status_reason` to the exception
type plus message, log it at WARNING, and continue to the next cell. If the cell's
`benchmark.json` already existed and `force` was False, set `status="skipped_existing"`. These
three values are the complete `status` vocabulary (D-04).

A disconnected pose graph on a wide-baseline 16-camera cell is an EXPECTED failure mode, not a bug
to chase: `generate_board_trajectory` accepts `camera_positions` and `min_cameras_per_frame` but
its body uses neither, so connectivity is not guaranteed. A `status=failed` row with a populated
`status_reason` is the correct recorded outcome. A MISSING row is not.

Do NOT enable `save_conditioning` for any cell. It was considered and rejected (D-17): a dense SVD
per cell is real cost, R4.3 is already answered by E7's four arms, and the 16-camera/200-frame cell
is already the memory risk on a 16.9 GB box.
  </action>
  <verify>
    <automated>python -c "
import inspect, experiments.e4_benchmark_grid as m
assert len(m.DECLARED_CELLS)==9
assert set(m.DECLARED_CELLS)=={(c,f) for c in [8,12,16] for f in [50,100,200]}
s=inspect.signature(m.build_grid_scenario).parameters
assert all(k in s for k in ['n_cameras','n_frames','seed','layout','depth_range','xy_extent','n_water'])
assert 'run_sweep' not in dir(m)
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import experiments.e4_benchmark_grid as m; assert not hasattr(m,'run_sweep')"` exits 0.
    - `grep -c "run_calibration_from_config\|load_config\|import yaml" experiments/e4_benchmark_grid.py` returns 0.
    - `experiments/e4_benchmark_grid.py` contains `def build_grid_scenario(` and `def run_grid_cell(`.
    - `python -c "import experiments.e4_benchmark_grid as m; assert len(m.DECLARED_CELLS)==9 and len(set(m.DECLARED_CELLS))==9"` exits 0.
    - `grep -c "generate_real_rig_array\|create_scenario" experiments/e4_benchmark_grid.py` returns 0 — E4 builds grid-family scenes only.
    - `grep -c "save_conditioning" experiments/e4_benchmark_grid.py` returns 0 (D-17).
    - `grep -c "validation_3d_error_mean" experiments/e4_benchmark_grid.py` returns at least 1, and `grep -c "result.diagnostics.validation_3d_error" experiments/e4_benchmark_grid.py` returns 0 — accuracy is measured, not copied from the hardcoded zeros.
    - `grep -c "memory_readings=" experiments/e4_benchmark_grid.py` returns exactly 1.
    - `python -c "import experiments.e4_benchmark_grid as m; sc=m.build_grid_scenario(3, 4, 42); assert len(sc.intrinsics)==3 and len(sc.board_poses)==4 and sc.n_water==1.333"` exits 0.
    - `ruff check experiments/e4_benchmark_grid.py && ruff format --check experiments/e4_benchmark_grid.py` exits 0.
  </acceptance_criteria>
  <done>The old real-config sweep is gone, `build_grid_scenario` is a reusable grid-family scene builder with the four axis keywords E6 needs, `run_grid_cell` records one of exactly three status values for every cell and writes one resumable `benchmark.json` per cell with memory forwarded, and the accuracy block is measured from a held-out evaluation.</done>
</task>

<task type="auto">
  <name>Task 2: Row assembly, aggregation, and the two-view LaTeX fragment</name>
  <files>experiments/e4_benchmark_grid.py</files>
  <read_first>
    - `experiments/_render.py` — the FULL file: `_flatten_record`'s dotted-key promotion,
      `aggregate`'s rglob and schema-version refusal, `write_latex_fragment`, `_latex_escape`, and
      `_mixed_memory_mode_values`.
    - `experiments/results/benchmark.json` — E2's committed record as refreshed by plan 19.2-06.
      Read its `problem_shape`, `stages`, `accuracy`, `memory`, and `environment` blocks so the
      tenth row's column mapping is written against the real shape, not an assumed one.
    - `src/aquacal/io/benchmark.py` lines 355-420 — how `fd_reduction` and the per-boundary memory
      deltas are computed, so neither is recomputed here (P1).
    - `19.2-CONTEXT.md` §§ D-02, D-14, D-15, D-16.
    - `19.2-SOURCE-BRIEF.md` § E4 — the per-run metric list and the "compact summary table in the
      main text, full grid in the supplement" placement plan.
  </read_first>
  <action>
Declare `GRID_COLUMNS` as an explicit module-level list, in exactly this order:

`cell_key`, `n_cameras`, `n_frames`, `status`, `status_reason`, `timing_scope`, `record_source`,
`seconds_stage3_interface_optimization`, `seconds_stage3_intrinsic_pass`,
`peak_bytes_baseline`, `peak_bytes_stage3_interface_optimization`,
`peak_bytes_stage3_intrinsic_pass`, `memory_mode`,
`n_params_stage3_interface_optimization`, `n_groups_stage3_interface_optimization`,
`fd_reduction_stage3_interface_optimization`, `n_residuals_stage3_interface_optimization`,
`jacobian_elements_stage3_interface_optimization`,
`n_params_stage3_intrinsic_pass`, `n_groups_stage3_intrinsic_pass`,
`fd_reduction_stage3_intrinsic_pass`,
`nfev_stage3_interface_optimization`, `njev_stage3_interface_optimization`,
`optimality_stage3_interface_optimization`,
`reprojection_rms`, `validation_3d_error_mean`, `validation_3d_error_std`

and `GRID_KEY_COLUMNS = ["cell_key"]`.

Column semantics, all mandatory:
  - `timing_scope` takes the literal `optimization_only` for the nine synthetic cells (Stage 2 +
    Stage 3 only) and `end_to_end` for E2's row (detection + Stage 1 + Stage 2 + Stage 3).
  - `record_source` takes `assembled` for the nine synthetic cells and `pipeline` for E2's row.
    These two columns exist precisely so the two kinds of row are never silently compared (D-02).
  - `jacobian_elements_stage3_interface_optimization` is
    `n_residuals * n_params` for that stage — the measured form of the supplement's dense-regime
    claim (D-15). It is `None` when either input is `None`.
  - Metric columns are `None` for any row whose `status` is not `ok` (D-04).
  - The CPR columns (`n_params_*`, `n_groups_*`, `fd_reduction_*`) are COPIED from each cell's own
    `benchmark.json` (D-16). Do not recompute `fd_reduction`; `assemble_benchmark_record` already
    computed it.

Write `build_grid_dataframe(out_dir: Path, cell_statuses: list[dict],
e2_benchmark_path: Path) -> pd.DataFrame`:
  - call `aggregate(out_dir / "e4_cells")` to flatten every cell's `benchmark.json` into the
    dotted-column frame — reuse it unmodified, write no second aggregator;
  - LEFT-JOIN that frame onto the literal `DECLARED_CELLS` list (P4) so a failed cell with no
    `benchmark.json` still produces a row. Join on `(n_cameras, n_frames)` taken from the declared
    list, not from the aggregate's own `problem_shape` columns, so a missing file cannot drop a
    declared cell;
  - merge `cell_statuses` onto the result for `status` / `status_reason`;
  - append E2's tenth row read from `e2_benchmark_path`, with `cell_key="real_rig_13cam_200fr"`,
    `timing_scope="end_to_end"`, `record_source="pipeline"`;
  - return the frame with columns in exactly `GRID_COLUMNS` order.

Write `write_grid_latex(df: pd.DataFrame, path: Path) -> None` using
`experiments/_render.py::write_latex_fragment` — do not write a second LaTeX layer and do not
recompute any value at render time (P1). It emits **two views into one file**, because the word
budget forces a compact main-text table and a full supplement grid:
  - a compact summary block over the nine synthetic rows only, carrying `n_cameras`, `n_frames`,
    total seconds, peak memory, and `reprojection_rms`;
  - a full grid block over the same nine rows with every column;
  - the real-rig row in **its own separately labeled block**, never as a tenth point appended to
    the scaling curve. Nine comparable points draw the curve; the real row anchors it to reality
    and answers the 13-camera wall-clock question separately (D-02).
Add a comment at the render site stating that reason, so a future editor does not "tidy" the three
blocks into one table.
  </action>
  <verify>
    <automated>python -c "
import experiments.e4_benchmark_grid as m
assert len(m.GRID_COLUMNS)==27 and m.GRID_COLUMNS[0]=='cell_key'
assert m.GRID_KEY_COLUMNS==['cell_key']
assert 'jacobian_elements_stage3_interface_optimization' in m.GRID_COLUMNS
assert {'timing_scope','record_source','status','status_reason'} <= set(m.GRID_COLUMNS)
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import experiments.e4_benchmark_grid as m; assert len(m.GRID_COLUMNS)==27"` exits 0.
    - `experiments/e4_benchmark_grid.py` contains `def build_grid_dataframe(` and `def write_grid_latex(`.
    - `grep -c "from experiments._render import" experiments/e4_benchmark_grid.py` returns at least 1, and the imported names include `aggregate` and `write_latex_fragment`.
    - `grep -c "def aggregate\|def write_latex_fragment" experiments/e4_benchmark_grid.py` returns 0 — no second aggregation or LaTeX layer.
    - `grep -c "n_params / n_groups\|n_params/n_groups" experiments/e4_benchmark_grid.py` returns 0 — `fd_reduction` is copied, not recomputed.
    - The `write_grid_latex` call site carries a comment stating why the real-rig row is a separate block.
    - `ruff check experiments/e4_benchmark_grid.py` exits 0.
  </acceptance_criteria>
  <done>`GRID_COLUMNS` is a 27-column declaration carrying scope, source, status, accuracy, CPR and element-count columns; the frame is built by left-joining the aggregator's output onto the literal declared-cell list; and the LaTeX fragment renders three blocks with the real-rig row separated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CLI wiring, smoke mode, and unit tests</name>
  <files>experiments/e4_benchmark_grid.py, tests/unit/test_experiments_e4.py</files>
  <behavior>
    - `test_grid_row_schema`: `build_grid_dataframe` over hand-built per-cell records and a
      hand-built E2 record returns exactly `GRID_COLUMNS`, in order.
    - `test_declared_cells_all_emit_rows`: with one declared cell's `benchmark.json` deliberately
      absent and its status recorded as `failed`, the frame still has 10 rows (9 declared + the
      real-rig row) and the failed row's metric columns are null while `status_reason` is
      non-empty.
    - `test_status_vocabulary`: every `status` value in the frame is one of
      `ok`, `failed`, `skipped_existing`.
    - `test_timing_scope_and_record_source`: exactly 9 rows have
      `timing_scope="optimization_only"` and `record_source="assembled"`; exactly 1 has
      `timing_scope="end_to_end"` and `record_source="pipeline"`.
    - `test_jacobian_elements_is_product`: for a row with both inputs present,
      `jacobian_elements_stage3_interface_optimization ==
      n_residuals_stage3_interface_optimization * n_params_stage3_interface_optimization`; and it
      is null when either input is null.
    - `test_build_grid_scenario_shape`: the scenario has the requested camera and frame counts,
      `noise_std == 0.5`, and the declared board config.
    - `test_latex_fragment_separates_real_rig`: the emitted fragment contains the real-rig row's
      key in a block distinct from the nine-cell block.
  </behavior>
  <read_first>
    - `tests/unit/test_experiments_e1.py` — the FULL file. Its header states the discipline for
      this family: minimal fixtures constructed directly, no `create_scenario`, no calibration,
      none marked slow. Follow it exactly.
    - `tests/unit/test_experiments_io.py` — how a hand-built `benchmark.json` fixture is written to
      a `tmp_path` and read back.
    - `experiments/e7_interface_ablation.py` lines 559-617 — `_run_check`, `build_arg_parser`, and
      `main`, the control flow to copy including the smoke-mode `tempfile.TemporaryDirectory`
      branch.
    - `experiments/e4_benchmark_grid.py` as written in Tasks 1 and 2.
  </read_first>
  <action>
Add `build_arg_parser()` extending `build_experiment_arg_parser()` via `parents=[...]` with no
script-local flags, and `main(argv=None) -> int` copying E7's control flow: parse,
`validate_args`, `logging.basicConfig(level=logging.INFO, format="%(message)s")`, then the
`--check` branch (never writes; compares a freshly built frame against the committed
`benchmark_grid.csv` at `CHECK_RTOL` via `compare_experiment_csv`, prints `report.message`, and
returns `exit_code_for(report)`), the `--smoke` branch, then the normal branch.

`--smoke` must exercise the full code path at trivial scale: a reduced declared-cell list of
2 cells at 3 cameras and 3-4 frames, `refine_intrinsics=False` if that keeps it under a few
seconds, a throwaway `tempfile.TemporaryDirectory` when `--out` is at its parser default, and
`force=True`. It must NOT run the nine-cell grid. Log a one-line summary per smoke cell.

The normal branch iterates `DECLARED_CELLS`, calls `run_grid_cell` for each, collects statuses,
calls `build_grid_dataframe`, writes `benchmark_grid.csv` via `write_experiment_csv(...,
key_columns=GRID_KEY_COLUMNS, force=args.force)`, and calls `write_grid_latex`. Log each cell's
completion at INFO with its status and elapsed seconds, so a background run's log is followable —
this run takes hours and an empty log is indistinguishable from a hang.

End with `if __name__ == "__main__": sys.exit(main())`.

Create `tests/unit/test_experiments_e4.py` covering the seven behaviors above with hand-built
fixtures. No test in this file may call `calibrate_synthetic`, `optimize_interface`, or
`run_grid_cell`'s calibration path — that is `--smoke` and production-run territory. Nothing here
is marked slow.

Do NOT run the nine-cell grid in this plan. That is plan 19.2-09.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e4.py -x -q && python -m experiments.e4_benchmark_grid --smoke</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_experiments_e4.py -x -q` exits 0 and collects at least 7 tests.
    - `python -m pytest tests/unit/test_experiments_e4.py -m slow -q` collects 0 tests.
    - `python -m pytest tests/unit/test_experiments_e4.py -k declared_cells -q` collects at least 1 test and exits 0.
    - `grep -c "calibrate_synthetic\|optimize_interface" tests/unit/test_experiments_e4.py` returns 0.
    - `python -m experiments.e4_benchmark_grid --smoke` exits 0 in under 120 seconds and writes nothing into `experiments/results/` — verified by `git status --porcelain experiments/results/` being empty afterwards.
    - `python -m experiments.e4_benchmark_grid --check --force` exits non-zero with a usage error.
    - `python -c "import experiments.e4_benchmark_grid as m; a=m.build_arg_parser().parse_args([]); assert sorted(vars(a))==['check','force','out','seed','smoke']"` exits 0.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 with at least 874 passed.
    - `ruff check experiments/ tests/ && ruff format --check experiments/ tests/` exits 0.
  </acceptance_criteria>
  <done>E4 honours the five-flag contract, `--smoke` runs the full code path at trivial scale without touching `experiments/results/`, and seven fast unit tests cover the row schema, the declared-cell coverage guarantee, the status vocabulary, the scope/source columns, and the element-count product.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem write | Per-cell `benchmark.json` files plus `benchmark_grid.csv`/`.tex` under a user-supplied `--out`. Controls inherited unchanged from `_io.py`: `resolve_out_dir`, `warn_if_overwriting`, skip-unless-`force`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.2-26 | Repudiation | A failed grid cell produces no row, so a coverage gap is invisible and the published grid silently has eight points instead of nine | mitigate | The frame is built by left-joining the aggregator onto the literal `DECLARED_CELLS` list, never from the aggregator alone; `test_declared_cells_all_emit_rows` injects a missing cell and asserts 10 rows survive. |
| T-19.2-27 | Repudiation | The nine assembled optimization-only rows are compared against E2's end-to-end pipeline row as if they measured the same thing | mitigate | Mandatory `timing_scope` and `record_source` columns on every row, asserted by `test_timing_scope_and_record_source`; the LaTeX fragment renders the real-rig row in its own labeled block with an in-code comment explaining why. |
| T-19.2-28 | Repudiation | The accuracy block is copied from `calibrate_synthetic`'s hardcoded `validation_3d_error_mean = 0.0`, publishing two fabricated zeros | mitigate | Accuracy is measured via `evaluate_calibration` on a separate held-out set; a grep gate asserts `result.diagnostics.validation_3d_error` is never read. |
| T-19.2-29 | Repudiation | E4 recomputes `fd_reduction` or the grouping, creating a second origin for a number `cpr_grouping.csv` also produces | mitigate | D-16 restricts E3 tier 3 to non-grid configurations and E4 to copying from its own record; grep gates assert no `n_params / n_groups` expression and no second aggregator in the module. |
| T-19.2-30 | Denial of Service | The 16-camera/200-frame cell exhausts the 16.9 GB box, or a wide-baseline cell produces a disconnected pose graph and aborts the whole sweep | mitigate | Per-cell try/except records `status=failed` with a reason and continues; per-cell `benchmark.json` files make the sweep resumable; `save_conditioning` is forbidden (D-17) as the most likely way to push a cell over the memory limit. |
| T-19.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. `19.2-RESEARCH.md` § Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
1. `python -m pytest tests/unit/test_experiments_e4.py -q` — exits 0.
2. `python -m experiments.e4_benchmark_grid --smoke` — exits 0, `experiments/results/` untouched.
3. `python -c "import experiments.e4_benchmark_grid as m; assert len(m.DECLARED_CELLS)==9 and len(m.GRID_COLUMNS)==27"` — exits 0.
4. `grep -c "run_calibration_from_config\|import yaml\|create_scenario" experiments/e4_benchmark_grid.py` — returns 0.
5. `python -m pytest tests/ -m "not slow" -q` — exits 0, at least 874 passed.
6. `ruff check . && ruff format --check experiments/ tests/` — exits 0.
</verification>

<success_criteria>
- E4 is a direct-call synthetic grid built on `generate_camera_array` + `generate_board_trajectory`
  -> `calibrate_synthetic` -> `assemble_benchmark_record`, with the real-config sweep runner
  removed entirely (D-01).
- `build_grid_scenario` is the single grid-family scene builder, parameterized so E6's three axes
  pass through E4's 12-camera cell as their baseline (amended D-03, D-11).
- Nine declared cells always yield nine rows with a status vocabulary of exactly three values
  (D-04), each carrying `timing_scope`, `record_source`, measured accuracy, copied CPR columns, and
  the Jacobian element count (D-02, D-14, D-15, D-16).
- No number in the grid has a second origin, and no second aggregation or LaTeX layer was written.
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-07-SUMMARY.md` when done.
Record: `build_grid_scenario`'s final signature (E6 depends on it), the final `GRID_COLUMNS` list,
the exact `status` vocabulary, the smoke-mode cell list and its measured runtime, and how the
`accuracy` block's three keys were measured.
</output>
