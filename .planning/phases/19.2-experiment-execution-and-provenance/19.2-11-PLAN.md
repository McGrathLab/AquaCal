---
phase: 19.2-experiment-execution-and-provenance
plan: 11
type: execute
wave: 4
depends_on: ["19.2-10"]
files_modified:
  - experiments/results/generalization_sweep.csv
autonomous: true
requirements: [EXP-10]

must_haves:
  truths:
    - "generalization_sweep.csv exists with one row per (axis, value, model) and a column recording which axis varied, in a tidy long format a single figure module can facet"
    - "Every axis's baseline row is present, so each axis genuinely passes through the common baseline"
    - "A degrading axis value is reported as measured; the swept range was not narrowed after seeing the results (D-12)"
    - "The index axis produced materially different scenarios across its range, confirming D-23's fix actually reached generation rather than silently generating everything at 1.333"
  artifacts:
    - path: "experiments/results/generalization_sweep.csv"
      provides: "the three-axis generalization sweep"
      contains: "axis"
  key_links:
    - from: "experiments/results/generalization_sweep.csv"
      to: "experiments/e6_generalization_sweep.py"
      via: "the declared axis value lists"
      pattern: "INDEX_AXIS_VALUES"
---

<objective>
Execute E6's three-axis generalization sweep and commit `generalization_sweep.csv`.

Purpose: EXP-10 and ROADMAP SC4. This is the production run for the module plan 19.2-10 wrote and
unit-tested.

**Sequenced last among the production runs, deliberately.** E6 is the largest sweep and the most
compressible: its three axes are independent, so if the 2026-08-21 deadline forces a cut, dropping
the scale axis costs one supplement row rather than invalidating the others. E4's nine cells and
E3's tiers are cheap and are never the cut.

**This is a production run, not a test step.** Launch with `python -u` in the background per
`CLAUDE.md`; without `-u` the log stays empty for the entire run and a stall is indistinguishable
from progress.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** fork from the merged result of plan 19.2-10. Verify
`python -m experiments.e6_generalization_sweep --smoke` exits 0 and
`python -m pytest tests/unit/test_experiments_e6.py -q` exits 0 before launching. If either fails,
your base is stale — re-fork rather than running a multi-hour sweep against the wrong code.
**PYTHONPATH:** the editable install resolves to the main checkout, not a worktree. If working in
a worktree, `export PYTHONPATH="$(pwd)/src"` and verify
`python -c "import aquacal; print(aquacal.__file__)"` resolves inside your working tree BEFORE
launching.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-CONTEXT.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-VALIDATION.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-CONTEXT.md
@experiments/README.md

<interfaces>
<!-- Contracts the executor needs. -->

Entry point: `python -u -m experiments.e6_generalization_sweep --force`
  Runs every entry of `build_axis_configurations()`, computing the shared baseline scene once and
  reusing its result for all three baseline rows, and writes `generalization_sweep.csv` with
  columns `E6_COLUMNS`, keyed on `["axis", "axis_value"]`.

Per-configuration resumability: `write_experiment_csv` skips a write when the file exists and
`force` is False. Note this is FILE-level, not row-level — an interrupted E6 run has no per-row
resume, so a full restart is the recovery path. Budget accordingly.

Expected outcomes, all recorded rather than chased (D-12, RESEARCH Pitfall 4):
  - a `ring` or `line` layout at 12 cameras may produce a disconnected pose graph and fail
    Stage 2, giving `status=failed` with a reason;
  - accuracy may genuinely degrade at n = 1.55 or on a line layout. R1.4 asked whether the method
    generalizes; a bounded, characterized degradation is a better answer than a claim of universal
    invariance a reviewer can poke.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Launch and monitor the three-axis sweep</name>
  <files>experiments/results/generalization_sweep.csv</files>
  <read_first>
    - `experiments/e6_generalization_sweep.py` — the FULL module as written by plan 19.2-10:
      `build_axis_configurations`, `run_configuration`'s try/except, `E6_COLUMNS`, and `main`'s
      normal branch.
    - `CLAUDE.md` § "Always run calibration unbuffered".
    - `19.2-VALIDATION.md` § Manual-Only Verifications — the E6 row: degradation is a RESULT, not
      a failure; report it, do not narrow the range.
    - `19.2-CONTEXT.md` § D-12 and § Claude's Discretion item 3 (the calendar fallback).
  </read_first>
  <action>
Launch from the repository root, in the background, unbuffered:
`python -u -m experiments.e6_generalization_sweep --force` with stdout and stderr redirected to a
log file OUTSIDE the repository. Do not run it in the foreground; do not omit `-u`.

Record the launch command verbatim in the summary.

While it runs, do not modify any file under `src/` or `experiments/`.

Poll the log until the process exits.

**If a configuration fails**, that is the designed behavior: the row is emitted with
`status=failed` and a populated `status_reason`. Record which configuration, its reason, and
whether it was a pose-graph connectivity failure (the anticipated mode for `ring` and `line`
layouts) or something else. Do NOT drop the axis value, do NOT narrow the axis, and do NOT retry it
at a different camera count.

**If the sweep does not finish in the available time**, the calendar fallback is to drop the SCALE
axis only — it is the most compressible, costing one supplement row rather than invalidating the
others. Taking that path means removing the `SCALE_AXIS_VALUES` entries from the committed source
and recording the decision in the summary and in `experiments/README.md`, not silently omitting
rows from a CSV whose axis list still declares them. Report the situation to the orchestrator
before taking this path; do not take it pre-emptively.
  </action>
  <verify>
    <automated>python -c "
import pandas as pd
import experiments.e6_generalization_sweep as m
df = pd.read_csv('experiments/results/generalization_sweep.csv')
assert list(df.columns) == m.E6_COLUMNS, list(df.columns)
assert set(df['axis']) == {'index','layout','scale'}
assert len(df) == len(m.build_axis_configurations()), (len(df), len(m.build_axis_configurations()))
assert int(df['is_baseline'].sum()) == 3
assert set(df['status']) <= {'ok','failed','skipped_existing'}
assert (df['model'] == 'refractive').all()
print(df[['axis','axis_value','status','reprojection_rms_px','reconstruction_rmse_mm','focal_error_pct_mean']].to_string())"</automated>
  </verify>
  <acceptance_criteria>
    - The launch command recorded in the summary contains `python -u`, `-m experiments.e6_generalization_sweep`, and `--force`, and was run in the background with output redirected.
    - `experiments/results/generalization_sweep.csv` exists with columns exactly `experiments.e6_generalization_sweep.E6_COLUMNS`.
    - Its row count equals `len(build_axis_configurations())`, and exactly 3 rows have `is_baseline` true — one per axis.
    - `set(df['axis'])` is exactly `{index, layout, scale}`, and every `status` value is one of `ok`, `failed`, `skipped_existing`.
    - Every value declared in `INDEX_AXIS_VALUES`, `LAYOUT_AXIS_VALUES`, and `SCALE_AXIS_VALUES` appears in the CSV — no declared value is missing.
    - `git status --porcelain src/ experiments/*.py` is empty at both launch and completion.
    - `git diff experiments/e6_generalization_sweep.py` shows no change to any axis value list after the run — the range was not adjusted from the results.
    - Any failed configuration is recorded in the summary with its `status_reason` and a classification, and was not dropped or retried at different scope.
  </acceptance_criteria>
  <done>The full three-axis sweep ran to completion, every declared axis value produced a row, each axis carries its baseline row, and no axis value was dropped or narrowed after seeing the results.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the index axis is real, record the findings, and verify reproducibility</name>
  <files>experiments/results/generalization_sweep.csv</files>
  <read_first>
    - `experiments/results/generalization_sweep.csv` — the file the run just produced.
    - `.planning/phases/19.1-experiment-suite-consolidation/19.1-CONTEXT.md` — its closing lesson:
      *a plausible delta with a plausible mechanism is not evidence anything changed.* Four defects
      were found in 19.1 and none was in the library; one was caught by an optimality of 1.5e11
      rather than by the science numbers.
    - `19.2-CONTEXT.md` §§ D-12, D-23.
    - `experiments/e6_generalization_sweep.py` — `build_grid_scenario`'s import and the axis value
      lists, for the independent re-derivation below.
  </read_first>
  <action>
**Confirm the index axis actually varied ground truth.** Without D-23's fix every scenario would be
generated at 1.333 and the axis would produce a silent null result that looks like data. Verify
positively; do not assume the fix landed:

  - Across the index-axis rows, at least one recovery metric must vary materially between the
    n = 1.333 baseline row and the highest swept index. If every index-axis metric is identical to
    the baseline to many significant figures, that is the null-result signature — STOP and report
    it as a defect rather than publishing a flat curve as evidence of invariance.
  - Independently of the CSV, re-derive the check: build the scenario at 1.333 and at the highest
    swept index via `build_grid_scenario`, call `generate_synthetic_detections` on each with the
    scenario's own indices, and confirm a common (frame, camera) pair's `corners_2d` arrays differ.
    Record the check and its outcome verbatim in the summary. This is the 19.1 lesson applied — a
    plausible flat line with a plausible mechanism is not evidence the method is invariant.

**Record the findings for the manuscript**, without asserting a verdict (D-12):
  - index axis: `reprojection_rms_px`, `reconstruction_rmse_mm`, and `focal_error_pct_mean` at each
    swept index, and the index (if any) beyond which accuracy degrades. Phrase it as a bounded
    characterization, not as a claim of universal invariance;
  - layout axis: the same three metrics for grid, ring, and line, plus any `status=failed` row and
    its reason;
  - scale axis: the same three metrics at each scale setting.

Do NOT narrow the swept range, do NOT add a threshold, and do NOT relabel a degrading value as an
outlier. R1.4 asked whether the method generalizes; a characterized boundary is the better answer.

Then run `python -m experiments.e6_generalization_sweep --check` and record its exit code. If it
fails, record the worst offending cell from the report message rather than suppressing it — the
nonlinear solve's exact convergence trajectory can differ at BLAS last-digit level, which is why
`CHECK_RTOL` is `1e-6` rather than a byte compare.
  </action>
  <verify>
    <automated>python -c "
import pandas as pd
df = pd.read_csv('experiments/results/generalization_sweep.csv')
idx = df[(df['axis']=='index') & (df['status']=='ok')].sort_values('axis_value')
assert len(idx) >= 4, len(idx)
base = idx.iloc[0]; far = idx.iloc[-1]
varied = (abs(far['reconstruction_rmse_mm'] - base['reconstruction_rmse_mm']) > 1e-9
          or abs(far['focal_error_pct_mean'] - base['focal_error_pct_mean']) > 1e-9
          or abs(far['reprojection_rms_px'] - base['reprojection_rms_px']) > 1e-9)
assert varied, 'index axis produced identical metrics end to end - suspect D-23 did not reach generation'
print(idx[['axis_value','reprojection_rms_px','reconstruction_rmse_mm','focal_error_pct_mean']].to_string())"</automated>
  </verify>
  <acceptance_criteria>
    - The index-axis rows show a material difference in at least one recovery metric between the 1.333 baseline and the highest swept index. If they do not, the plan halted and reported a suspected D-23 regression rather than committing the CSV.
    - The independent re-derivation is recorded: two scenarios built at different indices produce differing `corners_2d` for a common (frame, camera) pair.
    - The summary records `reprojection_rms_px`, `reconstruction_rmse_mm`, and `focal_error_pct_mean` for every row of all three axes.
    - The index (if any) beyond which accuracy degrades is stated as a bounded characterization, not as a verdict, and the swept range was not narrowed — `git diff experiments/e6_generalization_sweep.py` is empty.
    - `python -m experiments.e6_generalization_sweep --check` exit code is recorded, with the worst offending cell named if it is nonzero.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 with at least 874 passed.
  </acceptance_criteria>
  <done>The index axis is confirmed to have varied real ground truth by an independent check, all three axes' metrics are recorded as measured with no verdict or threshold applied, and `--check` reproducibility is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem write | `generalization_sweep.csv` under `experiments/results/`. Controls inherited from `_io.py`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.2-46 | Repudiation | The index axis silently generates every scenario at 1.333, so a flat curve is published as evidence of invariance when in fact nothing varied | mitigate | Task 2 requires a positive confirmation that a recovery metric moved end-to-end, plus an independent re-derivation comparing `corners_2d` between two scenarios; a flat result halts the plan as a suspected D-23 regression. |
| T-19.2-47 | Repudiation | A degrading axis value is dropped, relabelled an outlier, or the range is narrowed after seeing the data | mitigate | D-12 forbids it; acceptance criteria require every declared axis value to appear in the CSV and `git diff` on the axis lists to be empty after the run. |
| T-19.2-48 | Repudiation | The calendar fallback is taken silently, so the committed CSV has fewer rows than the source declares | mitigate | Dropping the scale axis requires removing its entries from the committed source, recording the decision in the summary and README, and reporting to the orchestrator first. |
| T-19.2-49 | Tampering | The library is edited mid-sweep, so early and late configurations were produced by different code | mitigate | `git status --porcelain src/ experiments/*.py` must be empty at both launch and completion. |
| T-19.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. `19.2-RESEARCH.md` § Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
1. Both automated blocks above exit 0.
2. `python -c "import pandas as pd; df=pd.read_csv('experiments/results/generalization_sweep.csv'); print(df.groupby('axis').size())"` — shows all three axes.
3. `git diff experiments/e6_generalization_sweep.py` — empty.
4. `python -m experiments.e6_generalization_sweep --check` — exit code recorded.
5. `python -m pytest tests/ -m "not slow" -q` — exits 0, at least 874 passed.
</verification>

<success_criteria>
- ROADMAP SC4 is TRUE: E6 swept refractive index, layout, and scale one axis at a time through a
  common baseline, emitting tidy long-format rows carrying which axis varied.
- The index axis is confirmed to have varied real ground truth, not silently generated everything
  at 1.333.
- Degradation, where it occurred, is reported as a measured, bounded characterization; nothing was
  narrowed, thresholded, or relabelled after the fact.
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-11-SUMMARY.md` when done.
Record: the verbatim launch command and measured wall-clock; the per-row metrics for all three
axes; the independent index-axis confirmation and its outcome; any failed configuration with its
reason and classification; the bounded characterization of where accuracy holds and where it
degrades; and the `--check` exit code.
</output>
