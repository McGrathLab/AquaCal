---
phase: 19.2-experiment-execution-and-provenance
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_experiments_e3_constants.py
  - tests/unit/test_experiments_e3.py
autonomous: true
requirements: [EXP-07]

must_haves:
  truths:
    - "The declared-constants table exists in exactly one place - a test module - so CI breaks first when a library default changes, and the E3 script can only render it, never re-declare it (D-18)"
    - "Every declared row is asserted against a LIVE value read by calling the function or reading its signature default, never against a source line number (RESEARCH Pitfall 1)"
    - "dense_threshold is the table's fifth constant family and the solver tolerances are assertable now that BENCH-06 landed (D-15)"
    - "ALL THREE Huber f_scale sites are pinned, not just optimize_interface's signature default, so a change to the hardcoded auxiliary-registration f_scale breaks CI instead of silently contradicting the supplement (review M10)"
    - "The P formula is validated against live pack_params length rather than against the supplement's arithmetic (D-22)"
  artifacts:
    - path: "tests/unit/test_experiments_e3_constants.py"
      provides: "DECLARED_CONSTANTS - the single declaration, plus the assertions over it"
      contains: "DECLARED_CONSTANTS"
      min_lines: 120
    - path: "tests/unit/test_experiments_e3.py"
      provides: "P-formula validation against live pack_params"
      contains: "pack_params"
  key_links:
    - from: "tests/unit/test_experiments_e3_constants.py"
      to: "aquacal.calibration._optim_common"
      via: "live value accessors (build_bounds call, signature defaults)"
      pattern: "build_bounds|make_sparse_jacobian_func"
    - from: "tests/unit/test_experiments_e3.py"
      to: "pack_params / build_jacobian_sparsity"
      via: "length-vs-column-count equality across configurations"
      pattern: "len\\(pack_params"
---

<objective>
Create the single declared-constants table (D-18) that both CI and E3's `code_constants.csv`
consume, and validate the supplement's `P` formula against live `pack_params` length (D-22).

Purpose: this table is the CI gate E3 tier 1 depends on. It must exist before the E3 script
imports it, and it must live in `tests/` so that an experiment-script edit cannot weaken an
assertion without touching the test suite. Every number in the supplement that describes *how the
library behaves* currently exists only as text someone typed — this closes that for the constants.
Output: `tests/unit/test_experiments_e3_constants.py` owning `DECLARED_CONSTANTS`, and a
P-formula test in `tests/unit/test_experiments_e3.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** this plan forks from `1ef72e95860900359379d44f81d30526c80bfc47` (the replan HEAD). If your worktree's `git merge-base HEAD 1ef72e9` is not that sha, stop and re-fork — a stale executor base has bitten this milestone twice. `git diff 5762d6d..1ef72e9 -- src/` is empty (11 docs/planning commits), so any bit-exactness constant captured at `5762d6d` remains valid at this base.
**PYTHONPATH:** the editable install resolves to the main checkout, not a worktree. If working in
a worktree, `export PYTHONPATH="$(pwd)/src"` before running pytest or its passing tests are
meaningless.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-CONTEXT.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-PATTERNS.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-RESEARCH.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-SOURCE-BRIEF.md

<interfaces>
<!-- Contracts the executor needs. Extracted live from the codebase 2026-07-28. -->

Live-value surfaces (verified this session — values confirmed, line numbers deliberately omitted
per RESEARCH Pitfall 1):

  build_bounds(...)                       # aquacal.calibration._optim_common
      water_z slot bounded to [0.01, 2.0]; with refine_intrinsics=True the fx/fy slots are
      bounded to [0.5*fx, 2.0*fx] and [0.5*fy, 2.0*fy].
  build_parameter_labels(camera_order, frame_order, reference_camera, refine_intrinsics,
                         normal_fixed, shared_interface) -> list[str]
      # aquacal.calibration._observability — mirrors pack_params' layout exactly, so
      # labels.index("water_z") and labels.index(f"{cam}_fx") locate bound slots without
      # re-deriving the packing arithmetic.
  make_sparse_jacobian_func(..., dense_threshold: int = 500_000_000, ...)
      # aquacal.calibration._optim_common — a signature default, readable via inspect
  Huber f_scale — THREE live sites, verified 2026-07-28. D-18 names only two; the third is a
  bare literal and is the one CI would not currently notice:
      interface_estimation.py:140   optimize_interface(..., loss_scale: float = 1.0, ...)
      refinement.py:53              joint_refinement(...,  loss_scale: float = 1.0, ...)
      interface_estimation.py:720   `f_scale=1.0` hardcoded inside `register_auxiliary_camera`
                                    (the function begins at :526) — no callable or signature
                                    surface exposes it, so it needs a source accessor.
  _refractive_project_newton(..., max_iterations: int = 10, tolerance: float = 1e-9)
      # aquacal.core.refractive_geometry — private, but it IS the shipped projector; use its
      # signature defaults here so this plan stays independent of plan 03 (same wave).
  compute_residuals(...)                  # aquacal.calibration._optim_common
      # the 100 px invalid/TIR penalty is a bare literal `diff[invalid] = 100.0` inside this
      # function's body — no callable or signature surface exposes it.
  optimize_interface / joint_refinement pass ftol/xtol/gtol explicitly at 1e-8 (BENCH-06),
      recorded on SolverDiagnostics as .ftol/.xtol/.gtol after a solve.

  pack_params(...)                        # aquacal.calibration._optim_common
  build_jacobian_sparsity(...)            # aquacal.calibration._optim_common
      # S.shape[1] must equal len(pack_params(...)) for the same configuration
  build_structural_column_groups(jac_sparsity, n_cams, n_frames, refine_intrinsics=False,
                                 normal_fixed=True, shared_interface=True) -> NDArray[np.intp]

Existing precedent to copy (`tests/unit/test_optim_common.py`):
  _make_detections(n_cams, n_frames, visibility, corners_per_view=4, seed=0)
  _make_pattern(n_cams, n_frames, visibility, refine_intrinsics, normal_fixed, seed=0)
  _DOCUMENTED_CONFIGS = [(True, False, 673, 13), (False, False, 675, 13), (False, True, 727, 17)]
  class TestDocumentedGroupingNumbers  — the pin-a-table-and-assert-it-live shape
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare and assert the constants table (D-18, D-15)</name>
  <files>tests/unit/test_experiments_e3_constants.py</files>
  <behavior>
    - `DECLARED_CONSTANTS` is a module-level sequence of exactly 9 entries.
    - For every entry, the live value read from the library equals the declared value — asserted by
      a single parametrized test so a failure names the offending row.
    - Every entry's `read_via` label is one of `"call"`, `"signature_default"`, or `"source_regex"`,
      and exactly 2 entries use `"source_regex"` — the two library literals that expose no callable
      or signature surface (`compute_residuals`' 100 px penalty and `register_auxiliary_camera`'s
      hardcoded `f_scale`). Any third would mean an accessor was written lazily where a live read
      was available, so the cap is asserted as an equality, not an inequality.
    - No entry's `source` string contains a digit-only line reference (e.g. `:519`).
    - The declared value and the claim text are both non-empty strings/numbers for every row.
  </behavior>
  <read_first>
    - `tests/unit/test_optim_common.py` lines 33-88 (`_make_detections` / `_make_pattern`) and
      lines 189-276 (`_DOCUMENTED_CONFIGS` + `TestDocumentedGroupingNumbers`) — this is the exact
      pin-and-assert shape to follow, including the class docstring's framing ("a future change
      fails a test rather than silently rotting the prose").
    - `src/aquacal/calibration/_optim_common.py` — `build_bounds` in full (the water_z and
      intrinsic bound blocks), `compute_residuals`'s invalid/TIR penalty assignment, and
      `make_sparse_jacobian_func`'s signature.
    - `src/aquacal/calibration/_observability.py` — `build_parameter_labels` in full, so bound
      slots are located by label rather than by re-deriving offsets.
    - `src/aquacal/calibration/interface_estimation.py` — `optimize_interface`'s `loss_scale`
      default and its explicit `ftol`/`xtol`/`gtol` arguments to `least_squares`; and
      `register_auxiliary_camera` in full (it begins around line 526), whose `least_squares` call
      passes a hardcoded `f_scale=1.0` around line 720. That literal is the third Huber site and the
      one nothing currently pins.
    - `src/aquacal/calibration/refinement.py` — `joint_refinement`'s own `loss_scale` default, the
      second Huber site.
    - `src/aquacal/core/refractive_geometry.py` — `_refractive_project_newton`'s signature.
    - `19.2-SOURCE-BRIEF.md` § E3 Tier 1 — the supplement claim wording for each row, which is what
      the `claim` field must carry.
    - `19.2-CONTEXT.md` § D-18 and § D-15.
    - `19.2-RESEARCH.md` § Pitfall 1 — why line numbers must not appear.
  </read_first>
  <action>
Create `tests/unit/test_experiments_e3_constants.py`. Module docstring: this module OWNS the
declared-constants table for E3 tier 1 (D-18); `experiments/e3_derived_quantities.py` imports
`DECLARED_CONSTANTS` from here and only renders it; the table is declared once so CI breaks first
when a library default changes.

Define a `DeclaredConstant` NamedTuple with fields, in this order:
`key: str`, `claim: str`, `source: str`, `declared_value: object`, `read_via: str`,
`live: Callable[[], object]`.
  - `key` — a stable snake_case identifier used as the CSV's primary key.
  - `claim` — the supplement's own sentence, quoted from `19.2-SOURCE-BRIEF.md` § E3 Tier 1.
  - `source` — a dotted module path plus symbol, e.g.
    `aquacal.calibration._optim_common.build_bounds`. **Never a line number.**
  - `declared_value` — the value the supplement asserts.
  - `read_via` — `"call"`, `"signature_default"`, or `"source_regex"`.
  - `live` — a zero-argument callable returning the current value from the library.

Declare exactly these 9 rows:

| key | declared_value | how `live` reads it |
|---|---|---|
| `water_z_bounds_m` | `(0.01, 2.0)` | `"call"` — build a tiny config, call `build_bounds`, locate the slot via `build_parameter_labels(...).index("water_z")`, return `(lower[i], upper[i])` |
| `refined_intrinsics_bounds_frac` | `(0.5, 2.0)` | `"call"` — same approach with `refine_intrinsics=True`; locate a camera's `fx` slot by label and return `(lower[i]/fx_nominal, upper[i]/fx_nominal)` |
| `huber_f_scale_px` | `(1.0, 1.0)` | `"signature_default"` — a 2-tuple of `inspect.signature(optimize_interface).parameters["loss_scale"].default` and `inspect.signature(joint_refinement).parameters["loss_scale"].default`. D-18 names two Huber sites; both are asserted here, not just the first. |
| `aux_registration_f_scale_px` | `1.0` | `"source_regex"` — `inspect.getsource(register_auxiliary_camera)` matched against an anchored pattern on the `f_scale=` keyword token, extracting the float. This is the THIRD Huber site: a bare literal with no callable or signature surface, and the one a change would currently slip past CI (review M10). |
| `invalid_tir_penalty_px` | `100.0` | `"source_regex"` — `inspect.getsource(compute_residuals)` matched against an anchored pattern on the assignment token `diff[invalid] =`, extracting the float. This is the ONLY row permitted to use source text, because the literal has no callable or signature surface. |
| `newton_tolerance_m` | `1e-9` | `"signature_default"` — `_refractive_project_newton`'s `tolerance` default |
| `newton_max_iterations` | `10` | `"signature_default"` — `_refractive_project_newton`'s `max_iterations` default |
| `dense_threshold_elements` | `500_000_000` | `"signature_default"` — `make_sparse_jacobian_func`'s `dense_threshold` default |
| `solver_tolerances` | `(1e-8, 1e-8, 1e-8)` | `"call"` — run one cheap `optimize_interface` solve with a `SolverDiagnostics` out-parameter and return `(d.ftol, d.xtol, d.gtol)`. Mark the test `@pytest.mark.slow` if this exceeds a second; keep the scenario minimal. |

Write one parametrized test over `DECLARED_CONSTANTS` asserting `entry.live() == entry.declared_value`
for each row, with the parametrization id set to `entry.key` so a failure names the row. Compare
tuples element-wise with exact equality; use `math.isclose` only if a float comparison genuinely
requires it, and if so state why in a comment.

Write three structural tests, named so `pytest -k declaration` selects them:
  - the table has exactly 9 entries and every `key` is unique;
  - no `source` string matches a line-number pattern (`:` followed by digits);
  - every `read_via` is in the allowed set, and `"source_regex"` appears exactly twice.

Do NOT import anything from `experiments/` here — the import direction is deliberately
tests -> library, and experiments -> tests (D-18), never the reverse.
Do NOT hardcode any source line number in a comment, docstring, or assertion.

**Record the import precondition in the module docstring (review L2).** `tests/__init__.py` exists
but `tests/unit/__init__.py` does NOT, so `from tests.unit.test_experiments_e3_constants import
DECLARED_CONSTANTS` resolves only via PEP 420 namespace-portion semantics and only when the repo
root is on `sys.path`. State in this module's docstring that it is imported by
`experiments/e3_derived_quantities.py`, that the repo root must therefore be importable, and that
plan 19.2-05's script bootstraps `sys.path` from `__file__` rather than relying on the working
directory. Do NOT create `tests/unit/__init__.py` to "fix" this — adding a package marker under an
existing rootdir changes pytest's collection semantics for the whole directory, which is a larger
blast radius than the import it would tidy.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e3_constants.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_experiments_e3_constants.py -x -q` exits 0.
    - `python -c "from tests.unit.test_experiments_e3_constants import DECLARED_CONSTANTS as D; assert len(D)==9; ks=[e.key for e in D]; assert len(set(ks))==9; assert set(ks)=={'water_z_bounds_m','refined_intrinsics_bounds_frac','huber_f_scale_px','aux_registration_f_scale_px','invalid_tir_penalty_px','newton_tolerance_m','newton_max_iterations','dense_threshold_elements','solver_tolerances'}; print('ok')"` exits 0.
    - `python -c "from tests.unit.test_experiments_e3_constants import DECLARED_CONSTANTS as D; assert sum(1 for e in D if e.read_via=='source_regex')==2; assert all(e.read_via in {'call','signature_default','source_regex'} for e in D)"` exits 0.
    - `python -c "from tests.unit.test_experiments_e3_constants import DECLARED_CONSTANTS as D; e=[x for x in D if x.key=='huber_f_scale_px'][0]; assert tuple(e.live())==(1.0,1.0)"` exits 0 — both signature-default Huber sites are read, not one.
    - `python -c "from tests.unit.test_experiments_e3_constants import DECLARED_CONSTANTS as D; e=[x for x in D if x.key=='aux_registration_f_scale_px'][0]; assert e.live()==1.0"` exits 0 — the hardcoded auxiliary-registration site is pinned.
    - `python -c "import re; from tests.unit.test_experiments_e3_constants import DECLARED_CONSTANTS as D; assert not any(re.search(r':\d+', e.source) for e in D)"` exits 0.
    - `python -m pytest tests/unit/test_experiments_e3_constants.py -k declaration -q` collects exactly 3 tests and exits 0.
    - `grep -c "from experiments\|import experiments" tests/unit/test_experiments_e3_constants.py` returns 0.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 with at least 874 passed.
    - `ruff check tests/unit/test_experiments_e3_constants.py && ruff format --check tests/unit/test_experiments_e3_constants.py` exits 0.
  </acceptance_criteria>
  <done>`DECLARED_CONSTANTS` holds exactly the 9 named rows, each asserted against a live library read, with no line-number citation anywhere and exactly two source-text accessors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate the P formula against live pack_params (D-22)</name>
  <files>tests/unit/test_experiments_e3.py</files>
  <behavior>
    - `test_pack_params_length_matches_sparsity_columns`: for each of at least 6 configurations
      spanning `(normal_fixed, refine_intrinsics, shared_interface)` combinations at two different
      `(n_cams, n_frames)` sizes, `len(pack_params(...)) == build_jacobian_sparsity(...).shape[1]`.
    - `test_pack_params_length_matches_closed_form`: the same configurations agree with a closed-form
      `expected_P(n_cams, n_frames, refine_intrinsics, normal_fixed, shared_interface)` written in
      the test as a pure arithmetic function of the packing layout — this is the supplement's
      formula, restated in code and validated against the live packer rather than trusted.
    - `test_per_camera_mode_grows_P_without_growing_groups`: for a fixed configuration,
      `shared_interface=False` yields a strictly larger `P` than `shared_interface=True` while
      `build_structural_column_groups(...).max() + 1` is unchanged (D-21's prediction, confirmed
      rather than assumed).
  </behavior>
  <read_first>
    - `tests/unit/test_optim_common.py` lines 33-88 — `_make_detections` / `_make_pattern`, the
      zero-calibration fixture builders to adapt; and lines 380-420, the existing per-camera-mode
      group-count test, so this plan extends rather than duplicates it.
    - `src/aquacal/calibration/_optim_common.py` — `pack_params`'s full signature and the packing
      order of its blocks (tilt, extrinsics, water_z, poses, intrinsics), which the closed form
      must mirror.
    - `src/aquacal/calibration/_observability.py` — `build_parameter_labels`, whose emission order
      documents the same layout in readable form.
    - `19.2-CONTEXT.md` § D-22 and § D-21.
    - `19.2-VALIDATION.md` § Per-Task Verification Map — the EXP-07 row names the `-k pack_params`
      selector these tests must match.
  </read_first>
  <action>
Create `tests/unit/test_experiments_e3.py`. Module docstring following
`tests/unit/test_experiments_e1.py`'s shape: fast unit tests, minimal fixtures constructed
directly, no `create_scenario`, no calibration, none marked slow.

Adapt (do not import) `_make_detections` / `_make_pattern` from `tests/unit/test_optim_common.py`
so this file stands alone; keep the helper names prefixed with `_`.

Write the closed-form `expected_P` as a module-level pure function with a docstring naming each
term and which packing block it counts: `2` tilt params when `not normal_fixed`;
`6 * (n_cams - 1)` extrinsic params; `1` water_z when `shared_interface` else `n_cams`;
`6 * n_frames` board-pose params; `4 * n_cams` intrinsic params when `refine_intrinsics`. Derive
the exact term list by reading `pack_params`'s body — do not copy this list on faith, verify each
block against the source and correct the docstring if the live packer differs.

Name the tests so `pytest -k pack_params` selects the first two.

Do not assert any specific `P` value as a literal here — `tests/unit/test_optim_common.py` already
pins the three documented `(673, 13) / (675, 13) / (727, 17)` configurations, and re-pinning them
would create a second origin for numbers the supplement's `tab:cpr` quotes.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e3.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_experiments_e3.py -x -q` exits 0.
    - `python -m pytest tests/unit/test_experiments_e3.py -k pack_params -q` collects at least 12 parametrized cases (≥6 configurations x 2 tests) and exits 0.
    - `grep -c "def expected_P" tests/unit/test_experiments_e3.py` returns exactly 1.
    - `grep -c "673\|675\|727" tests/unit/test_experiments_e3.py` returns 0 — those numbers keep their single origin in `test_optim_common.py`.
    - `python -m pytest tests/unit/test_experiments_e3.py -m slow -q` collects 0 tests.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 with at least 874 passed.
    - `ruff check tests/unit/test_experiments_e3.py` exits 0.
  </acceptance_criteria>
  <done>The `P` formula is expressed once as a closed form, validated against live `pack_params` length and against `build_jacobian_sparsity`'s column count across ≥6 configurations, and D-21's per-camera prediction is confirmed by measurement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only additions. No production code, no I/O, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.2-12 | Repudiation | A declared constant is asserted against a stale source line number, so the test silently stops describing the shipped default the moment the file grows | mitigate | Every row reads a live value via `call` or `signature_default`; a structural test rejects any `source` string containing `:<digits>`; exactly two `source_regex` rows are permitted (review M10 added the second) and both are anchored on a code token, not a line. |
| T-19.2-13 | Tampering | The constants table is declared in `experiments/`, so an experiment-script edit could weaken a CI assertion without touching `tests/` | mitigate | D-18 places the table in `tests/`; a grep gate asserts this module imports nothing from `experiments/`. |
| T-19.2-14a | Repudiation | Only one of the three Huber `f_scale` sites is pinned, so changing `register_auxiliary_camera`'s hardcoded `f_scale=1.0` leaves CI green while the supplement's "f_scale = 1.0 everywhere" sentence becomes false | mitigate | `huber_f_scale_px` reads BOTH signature defaults as a 2-tuple and a ninth row `aux_registration_f_scale_px` reads the hardcoded literal by anchored source regex; the structural test requires exactly two `source_regex` rows so neither accessor can be quietly dropped. |
| T-19.2-14 | Repudiation | Re-pinning `tab:cpr`'s 673/675/727 numbers here creates a second origin for a published table | mitigate | Grep gate asserts those literals do not appear in the new test file; `test_optim_common.py` keeps sole ownership. |
| T-19.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. `19.2-RESEARCH.md` § Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
1. `python -m pytest tests/unit/test_experiments_e3_constants.py tests/unit/test_experiments_e3.py -q` — exits 0.
2. `python -c "from tests.unit.test_experiments_e3_constants import DECLARED_CONSTANTS; print(len(DECLARED_CONSTANTS))"` — prints `9`, run from the repository root.
3. `python -m pytest tests/ -m "not slow" -q` — exits 0, at least 874 passed.
4. `grep -rn ":[0-9][0-9]*" tests/unit/test_experiments_e3_constants.py | grep -c "source"` — returns 0.
5. `ruff check . && ruff format --check tests/` — exits 0.
</verification>

<success_criteria>
- One declaration, two consumers: `DECLARED_CONSTANTS` lives only in `tests/`, CI asserts it, and
  the E3 script (plan 05) will import and render it without re-declaring anything.
- All 9 rows including `dense_threshold`, the BENCH-06 solver tolerances, and all three Huber
  `f_scale` sites pass against live library values.
- The `P` formula is validated against `pack_params`, not against the supplement's arithmetic.
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-04-SUMMARY.md` when done.
Record: the 9 `key` values, each row's `read_via` mechanism, the anchored regex used for each of
the two `source_regex` rows, the exact `expected_P` term list as
verified against `pack_params`'s body, and whether D-21's group-count invariance held.
</output>
