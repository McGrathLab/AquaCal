---
phase: 19.2-experiment-execution-and-provenance
plan: 27
type: execute
wave: 7
gap_closure: true
depends_on: ["19.2-22", "19.2-23", "19.2-24", "19.2-25"]
files_modified:
  - experiments/e6_generalization_sweep.py
  - experiments/e5_index_sensitivity.py
  - tests/unit/test_experiments_e6.py
  - tests/unit/test_experiments_e5.py
autonomous: true
requirements: [EXP-09, EXP-10, EXP-11]

must_haves:
  truths:
    - "E6 records solver optimality per configuration, so a diverged solve can no longer be published under `status=\"ok\"` with nothing in the record distinguishing it (WR-02). This closes the blind spot that produced MF-04 on E2 and the four unconverged cells in E4's first grid"
    - "The recorded field is a MEASUREMENT, not a verdict. D-12 forbids a verdict column and still stands: record `optimality`, never a `converged: true/false` flag. The reader judges; the artifact reports"
    - "E6's `--check` identity guard compares SCENARIO-determining fields only, so the three `is_baseline` rows that deliberately share one `config_key` all match `baseline.json`. WR-03's actual purpose -- catching a checkpoint that predates a scenario change -- is PRESERVED and must still trip"
    - "E6 captures the environment ONCE at sweep start and stamps every checkpoint from it, so a commit landing mid-sweep can no longer split `git_sha` across an artifact set (measured 2026-07-31: baseline.json 3d5c3e6, the other eleven 3d0aa3e)"
    - "E5 emits `discard_stats` so plan 19.2-23's attribution gate becomes runnable against evidence that exists, instead of naming a field E5 never produced"
    - "THIS PLAN RUNS NO EXPERIMENT AND REGENERATES NO ARTIFACT. `git status --porcelain experiments/results/` is empty at completion. D-26: every code change lands and is proven inert BEFORE the runs that yield publishable results (plans 28 and 29)"
    - "Inertness is proven by TEST, not by argument: the new keyword arguments are output sinks, and a unit test asserts that passing them does not change any returned value"
  artifacts:
    - path: "experiments/e6_generalization_sweep.py"
      provides: "optimality capture, scenario-only identity comparison, capture-once provenance"
      contains: "diagnostics_out"
    - path: "experiments/e5_index_sensitivity.py"
      provides: "discard_stats emission into E5's provenance sidecar"
      contains: "discard_stats_out"
  key_links:
    - from: "experiments/e6_generalization_sweep.py"
      to: "aquacal.datasets.pipelines.calibrate_synthetic"
      via: "diagnostics_out / discard_stats_out keyword sinks that already exist on the signature"
      pattern: "diagnostics_out"
---

<objective>
Close the four defects this phase disclosed but deliberately left unfixed, **in code only**. The two
production re-runs that make the committed artifacts match this code are plans 19.2-28 (E6) and
19.2-29 (E5).

Purpose: EXP-10 and EXP-11, plus the two `human_verification` items the 2026-08-02 verification
report raised. That report scored the phase 7/7 and closed both prior gaps; it did **not** clear
these, it escalated them for a decision. The decision (user, 2026-08-01) is: fix all four.

**Why code-only.** D-26 requires every `src`/`experiments` change to land and be proven inert before
any experiment that yields a publishable result. Splitting the fixes from the re-runs also means ONE
E6 run instead of two, and removes the window in which shipped code and committed artifacts disagree.

**All four fixes are wiring, not new infrastructure.** `calibrate_synthetic`'s signature already
carries `diagnostics_out: dict[str, SolverDiagnostics] | None` and `discard_stats_out: dict[str, int]
| None` — the first from Phase 19's BENCH work, the second added by plan 19.2-26. E4 already uses
`diagnostics_out` and records `optimality_stage3_interface_optimization`. E6 imports from
`e4_benchmark_grid` but has never passed either sink; E5 passes neither.

Output: four fixes, each guarded by a test that would fail if the fix were absent, and a suite that
is green with `experiments/results/` untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** fork from the merged result of wave 6. Verify `git status --porcelain` is empty and
`pytest tests/ -m "not slow" -q` exits 0 (1150 passed) before starting.
**PYTHONPATH:** in a worktree, `export PYTHONPATH="$(pwd)/src"` and confirm
`python -c "import aquacal; print(aquacal.__file__)"` resolves INSIDE your tree. The editable install
otherwise resolves to the main checkout and your passing tests prove nothing.
**Interpreter:** `/c/Users/tucke/anaconda3/envs/AquaCal/python.exe`. Git Bash `python` is Anaconda
base with no numpy (~39 collection errors — an interpreter problem, not a breakage).
**No box exclusivity needed** — this plan runs no calibration.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-VERIFICATION.md
@.planning/phases/19.2-experiment-execution-and-provenance/.continue-here.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-22-SUMMARY.md
@.planning/phases/19.2-experiment-execution-and-provenance/19.2-23-SUMMARY.md
@CLAUDE.md

<interfaces>
`calibrate_synthetic(scenario, n_water, refine_intrinsics=True, seed=42,
diagnostics_out=None, timings_out=None, memory_out=None, normal_fixed=True,
discard_stats_out=None)`.

E6 calls it at `experiments/e6_generalization_sweep.py:625` inside `run_configuration`, passing
neither sink. E5 calls it at `experiments/e5_index_sensitivity.py:439`, likewise.

E4's precedent for optimality capture: `e4_benchmark_grid.py:693-703` builds
`diag_stage3 = SolverDiagnostics()` and `diag_intrinsic_pass = SolverDiagnostics()`, passes them via
`diagnostics_out={...}`, and surfaces `optimality_stage3_interface_optimization`. Follow that naming
so E4 and E6 are readable side by side.

`E6_COLUMNS` is 28 entries with a hard `assert len(E6_COLUMNS) == 28 and len(set(E6_COLUMNS)) == 28`
at `e6_generalization_sweep.py:199`. Adding columns means updating that assert and any test mirroring
the count.

WR-03's identity guard: `_config_identity_matches(config, cached_config)` compares
`_resolve_config_identity(config)` against the checkpoint's recorded `config`, through a JSON
round-trip. Measured 2026-08-01: `index/1.333` matches `baseline.json`; `layout/grid` and
`scale/default` do NOT, because the identity includes `axis` and `axis_value`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record solver optimality per E6 configuration (WR-02)</name>
  <files>experiments/e6_generalization_sweep.py, tests/unit/test_experiments_e6.py</files>
  <read_first>
    - `experiments/e4_benchmark_grid.py:690-800` — the `SolverDiagnostics` pattern to mirror, including how E4 names and null-fills its optimality columns.
    - `experiments/e6_generalization_sweep.py` — `run_configuration` (the call at 625), `build_row`, `E6_COLUMNS`, `_METRIC_COLUMNS`.
    - `.planning/phases/19.2-experiment-execution-and-provenance/19.2-22-SUMMARY.md` § "Scale axis" — the committed `double_scale` row that was a diverged solve published as `status="ok"` (1536 px RMS, 1.33 km reconstruction RMSE), and WR-02.
  </read_first>
  <action>
Build `SolverDiagnostics()` sinks in `run_configuration` and pass them via `diagnostics_out=` to the
existing `calibrate_synthetic` call. Record the resulting optimality into the per-configuration
checkpoint JSON and add the corresponding columns to `E6_COLUMNS`, mirroring E4's names
(`optimality_stage3_interface_optimization` and the intrinsic-pass equivalent). Update the
`assert len(E6_COLUMNS) == 28` and any test that mirrors the count.

Treat the new columns like the other metric columns: null for any row whose `status` is not `"ok"`
(see `_METRIC_COLUMNS`).

**D-12 boundary — do not cross it.** Record the optimality NUMBER. Do NOT add a `converged`,
`diverged`, `verdict`, or pass/fail column, and do NOT threshold the value anywhere in E6. D-12
rejected a verdict column and still stands; this task exists because the *measurement* was missing,
not because a judgment was. The anti-pattern table's wording is the guide: "optimality is the
discriminator" — recorded, for a human to read.

**Do not regenerate any artifact.** The committed `generalization_sweep.csv` will not have these
columns until plan 19.2-28 re-runs the sweep; that is expected and is that plan's job.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e6.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `run_configuration` passes `diagnostics_out=` to `calibrate_synthetic`, and the checkpoint JSON gains the optimality field(s).
    - `E6_COLUMNS` contains the new column(s), its `assert` is updated, and the mirrored count in the test suite matches.
    - A test asserts the optimality value reaches both the checkpoint and the built row, and that it is null when `status != "ok"`.
    - NO verdict/threshold/boolean-convergence column exists anywhere in E6; the summary states this explicitly against D-12.
    - `git status --porcelain experiments/results/` is empty.
  </acceptance_criteria>
  <done>E6 records the number that distinguishes a converged solve from a diverged one, and records nothing that pretends to make the judgment for the reader.</done>
</task>

<task type="auto">
  <name>Task 2: Fix WR-03's identity collision so --check can pass (and still trips on real drift)</name>
  <files>experiments/e6_generalization_sweep.py, tests/unit/test_experiments_e6.py</files>
  <read_first>
    - `experiments/e6_generalization_sweep.py` — `_resolve_config_identity`, `_config_identity_matches`, `_reconstitute_row`, `build_axis_configurations`.
    - `.planning/phases/19.2-experiment-execution-and-provenance/19.2-16-SUMMARY.md` — why WR-03 (T-19.2-63) was added and what it is meant to catch.
    - `19.2-22-SUMMARY.md` § "`--check`" — the measured collision table.
  </read_first>
  <action>
Restrict the compared identity to **scenario-determining** fields — those that change which scene is
built (index, layout, scale, camera count, frame count, geometry parameters) — and exclude the
**presentational** ones (`axis`, `axis_value`) that merely name which axis a row illustrates.

The three `is_baseline` rows deliberately share one `config_key` and one scene; comparing labels
guarantees a mismatch no correct run can avoid, which is why `--check` cannot currently pass for any
baseline row.

**This is a fix at the cause, not a loosened guard, and the plan requires you to PROVE that.** WR-03
exists to catch a checkpoint that predates an axis-value edit — but such an edit changes the
*scenario*, which the restricted identity still compares. Write a test that mutates a
scenario-determining field in a cached checkpoint and asserts the row still degrades to
`status="failed"` with the WR-03 reason. **If you cannot make that test fail without your fix and
pass with it, stop and report — the guard has been weakened rather than corrected.** Relaxing a
determinism gate to make it pass is a recorded BLOCKING anti-pattern in this phase.

**Do not expect `--check` to exit 0 at the end of this plan.** Task 1 adds columns, so `--check`
against the OLD committed CSV will fail on a header mismatch until plan 19.2-28 regenerates it.
End-to-end `--check` green is plan 28's gate. Validate this task by unit test only, and say so.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e6.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All three `is_baseline` configurations (`index/1.333`, `layout/grid`, `scale/default`) match `baseline.json`'s recorded identity in a unit test.
    - A test proves WR-03 still trips: mutating a scenario-determining field degrades the row to `failed` with the WR-03 reason. The summary states this test fails without the fix.
    - The summary states explicitly that `--check` is NOT expected to exit 0 in this plan, and why.
    - `git status --porcelain experiments/results/` is empty.
  </acceptance_criteria>
  <done>The identity guard compares what determines the scene rather than what labels the row, and a test proves it still catches the drift it was built for.</done>
</task>

<task type="auto">
  <name>Task 3: Capture E6's environment once per sweep, not once per configuration</name>
  <files>experiments/e6_generalization_sweep.py, tests/unit/test_experiments_e6.py</files>
  <read_first>
    - `experiments/e6_generalization_sweep.py` — where `capture_environment` is called, `run_sweep`, `_run_full`.
    - `.planning/phases/19.2-experiment-execution-and-provenance/19.2-22-SUMMARY.md` § "Provenance (EXP-11)" — the measured split.
  </read_first>
  <action>
Capture the environment ONCE at sweep start and stamp every checkpoint from that single record,
instead of calling `capture_environment` per configuration.

Measured cause (2026-07-31): `capture_environment` calls `git rev-parse` per configuration, so a
docs-only commit landing mid-sweep left `baseline.json` recording `3d5c3e6` and the other eleven
recording `3d0aa3e`. No individual record was false — each named the tree state when that cell ran —
but the set cannot name one commit as the code that produced the sweep, which is what EXP-11
provenance is for.

Every checkpoint must still CARRY a full environment block (the provenance gate widened in plan
19.2-24 asserts four fields per artifact, and that gate must keep passing) — the change is where the
block comes from, not whether it is there.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e6.py tests/unit/test_experiments_provenance.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `capture_environment` is invoked once per sweep; every checkpoint still carries a complete environment block with all four EXP-11 fields.
    - A test asserts every checkpoint written by one sweep reports an identical `git_sha`.
    - `tests/unit/test_experiments_provenance.py` still passes unchanged.
    - `git status --porcelain experiments/results/` is empty.
  </acceptance_criteria>
  <done>One sweep names one commit, by construction rather than by luck about what was committed while it ran.</done>
</task>

<task type="auto">
  <name>Task 4: Emit discard_stats from E5 so plan 23's attribution gate is runnable</name>
  <files>experiments/e5_index_sensitivity.py, tests/unit/test_experiments_e5.py</files>
  <read_first>
    - `experiments/e5_index_sensitivity.py:439` — the `calibrate_synthetic` call, and the provenance sidecar plan 19.2-19 added.
    - `.planning/phases/19.2-experiment-execution-and-provenance/19.2-26-SUMMARY.md` — what `discard_stats` counts and why plan 26 added it.
    - `19.2-23-SUMMARY.md` § "The attribution gate is unrunnable as specified".
  </read_first>
  <action>
Pass `discard_stats_out=` to E5's `calibrate_synthetic` call and record the result into E5's
provenance sidecar (`e5_provenance.json`), so a future adjudication can confirm the PnP-guard
mechanism from E5's own output.

Plan 19.2-23's gate requires `discard_stats.pnp_guard_rejected > 0` "in the run's own output"; E5
emits no such field, so that gate could not be evaluated and attribution fell back to
magnitude-against-seed-band plus a source trace. This is the same defect class as plan 06's D-34
gate, which was unrunnable until plan 26 added the counters to the calibration path — the counters
exist now; E5 simply never asked for them.

Aggregate across the band's 11 index points in whatever shape is most faithful (per-point or summed);
state which you chose and why in the summary. Do not fabricate a value when the sink is empty —
absent must be distinguishable from zero.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e5.py -q</automated>
  </verify>
  <acceptance_criteria>
    - E5 passes `discard_stats_out=` and records the counters into `e5_provenance.json`.
    - A test asserts the field is present and that an empty sink is recorded distinguishably from a zero count.
    - The summary states the chosen aggregation shape and its rationale.
    - `git status --porcelain experiments/results/` is empty.
  </acceptance_criteria>
  <done>Plan 23's attribution gate now names evidence that E5 actually produces.</done>
</task>

<task type="auto">
  <name>Task 5: Prove the new sinks are numerically inert</name>
  <files>tests/unit/test_experiments_e6.py, tests/unit/test_experiments_e5.py</files>
  <read_first>
    - `.planning/phases/19.2-experiment-execution-and-provenance/19.2-26-SUMMARY.md` — how plan 26 proved its counters inert (exact-equality against the uninstrumented path plus a frozen pre-edit anchor).
  </read_first>
  <action>
`diagnostics_out` and `discard_stats_out` are output sinks and should not perturb any returned value.
**Prove it rather than assert it**, because plans 28 and 29 will re-run both experiments and compare
against committed numbers — if the sinks are not inert, that comparison misattributes the movement.

Write a test that runs the same small scenario twice, once with the sinks and once without, and
asserts exact equality of the returned calibration values. Follow plan 26's pattern.

Keep it small enough for the fast suite; this is a passivity proof, not a convergence study. If you
find the sinks are NOT inert, STOP and report — that is a finding that changes plans 28 and 29, not
something to accommodate.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_e5.py tests/unit/test_experiments_e6.py -q</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts exact equality of returned values with and without the new sinks, for both experiments' call shapes.
    - The test is not marked slow and runs in the fast suite.
    - If non-inertness was found, it is reported rather than accommodated.
  </acceptance_criteria>
  <done>The re-runs in plans 28 and 29 can attribute any movement to something other than this plan's instrumentation, because a test says so.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -m "not slow" -q` — exits 0. Record the count against the 1150-passed starting state.
2. `git status --porcelain experiments/results/` — EMPTY. This plan regenerates nothing.
3. `git status --porcelain` — empty at completion.
4. No verdict/threshold column exists in E6 (D-12).
</verification>

<success_criteria>
- All four disclosed defects are fixed in code, each guarded by a test that fails without the fix.
- Nothing in `experiments/results/` changed.
- The suite is green, so plans 28 and 29 start from a known-good tree.
</success_criteria>

<output>
Create `.planning/phases/19.2-experiment-execution-and-provenance/19.2-27-SUMMARY.md`.
Record: each fix and the test that guards it; the WR-03 proof that the guard still trips; the D-12
statement that no verdict column was added; the chosen E5 aggregation shape; the inertness result;
and an explicit note that `--check` is expected to fail on a header mismatch until plan 28 runs.
</output>
