---
phase: 21-new-feature-documentation-dataset-refresh
plan: 08
type: execute
wave: 4
depends_on: ["21-07", "21-03"]
files_modified:
  - .planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md
  - docs/tutorials/03_cli_walkthrough.md
autonomous: false
requirements: [DATA-01a, DATA-02]

must_haves:
  truths:
    - "A calibration run from the exact bytes that will be uploaded reproduces `reconstruction.num_comparisons = 7762` and the other eight §3 quantities — D-15 gate 1, and DATA-01a's actual acceptance test."
    - "Every command printed in the CLI tutorial executes as written against the staged archive — D-15 gate 3."
    - "If the archive does not reproduce §3, the phase HALTS and escalates; no tolerance is chosen and §3 is not edited — D-16."
    - "All four D-15 gates read PASS before any DOI is minted."
  artifacts:
    - path: ".planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md"
      provides: "Four-gate status table, all PASS, plus the nine-quantity comparison"
      contains: "7762"
  key_links:
    - from: "real-rig-calib.zip"
      to: "release_calibration/diagnostics.json"
      via: "nine-quantity reproduction comparison"
      pattern: "num_comparisons"
---

<objective>
Clear D-15's remaining two pre-publish gates against the exact bytes that will be uploaded.

Purpose: a Zenodo draft is not publicly downloadable, so `load_example`'s end-to-end check can
only run after minting — and a minted DOI cannot be withdrawn. Everything checkable pre-publish
is checked pre-publish, so the post-publish step is a confirmation, not a discovery.

Gate 1 is DATA-01a's real acceptance test and the phase's PUBLICATION BLOCKER: the published
archive must reproduce the published numbers. Gate 3 proves the CLI tutorial is executable as
printed, since it is now the only doc that touches the archive.

Output: an updated four-gate table in `21-ARCHIVE-MANIFEST.md`, all PASS — or a halt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-FRAMESET-PROVENANCE.md
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md
@CLAUDE.md
</context>

<interfaces>
**The nine §3 quantities and their exact `release_calibration/diagnostics.json` values**
(source: `19.1-E2-FRAMESET-PROVENANCE.md` § "The finding"):

| # | §3 quantity | `diagnostics.json` path | value |
|---|---|---|---|
| 1 | Mean reprojection | mean of the 12 primary per-camera `reprojection_rms` values | 0.87863 px |
| 2 | Per-camera range | min `e3v829d`, max `e3v83f0` | 0.53938 - 2.40788 px |
| 3 | Auxiliary fisheye | `auxiliary_cameras.e3v8250.reprojection_rms` | 15.13432 px |
| 4 | Inter-corner MAE | `reconstruction.mean` | 0.00026815 m |
| 5 | Inter-corner RMSE | `reconstruction.rmse` | 0.00067407 m |
| 6 | Mean relative error | `reconstruction.percent_error` | 0.44692 % |
| 7 | Comparisons | `reconstruction.num_comparisons` | **7762** (integer, exact) |
| 8 | Recovered water_z | `camera_heights.water_z` | 1.03055 m |
| 9 | Camera height range | min `e3v83e9`, max `e3v83f1` | 1.00828 - 1.08153 m |

Read the literal JSON key spellings out of the file itself before writing the comparison script —
the table names the quantities, not necessarily the exact nesting.
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: D-15 gates 1 and 3 — run the tutorial's own commands against the staged archive (LONG — orchestrator/user only)</name>
  <files>none (writes to a scratch directory outside the repo)</files>
  <read_first>
    - `docs/tutorials/03_cli_walkthrough.md` — **the source of the commands to run; run them verbatim, do not paraphrase**
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md` (gate 2 and 4 evidence, and the zip's md5 and byte size)
    - `CLAUDE.md` § "Never let a subagent background a long run and return", § "Always run calibration unbuffered", and the "Commit nothing during a production run" rule
  </read_first>
  <action>
**CHECKPOINT — NOT AUTONOMOUS.** The orchestrator or the user performs this task by running the
commands in `<how-to-verify>` below, in order, exactly as written. Do NOT execute this task from
inside an executor subagent: per CLAUDE.md, an executor that backgrounds a long run stalls
permanently and never returns. Record every command, its exit code and its wall-clock runtime in
the SUMMARY, then confirm each item in `<acceptance_criteria>` before signalling resume.
  </action>
  <what-built>
`real-rig-calib.zip` (plan 21-07) with both configs, the 262-frame lossless PNG frameset, and the
paper run's reference outputs; and `docs/tutorials/03_cli_walkthrough.md` (plan 21-03), whose
commands this task executes verbatim.
  </what-built>
  <how-to-verify>
**DO NOT RUN THIS FROM INSIDE AN EXECUTOR SUBAGENT.** The `config_paper.yaml` solve takes ~50
minutes and ~11 GiB peak on a 15.7 GiB machine. Per CLAUDE.md, an executor that backgrounds it
stalls permanently and never returns. The orchestrator or the user runs it directly.

**Run in the AquaCal conda env.** Confirm first:
`python -c "import cv2,sys; print(sys.executable, cv2.__version__)"`

**Commit nothing between launch and completion** — a mid-run `git rev-parse` splits the recorded
sha of the `benchmark.json` this run writes.

1. **Extract the exact bytes that will be uploaded.** Do not reuse plan 21-07's staging tree —
   the gate is on the zip.
   ```
   mkdir -p "C:/Users/tucke/Desktop/Aqua/AquaCal/gate1_scratch"
   python -c "import zipfile; zipfile.ZipFile(r'C:/Users/tucke/Desktop/Aqua/AquaCal/real-rig-calib.zip').extractall(r'C:/Users/tucke/Desktop/Aqua/AquaCal/gate1_scratch')"
   cd "C:/Users/tucke/Desktop/Aqua/AquaCal/gate1_scratch/real-rig"
   ```

2. **Gate 3, part one — the quickstart command exactly as the tutorial prints it** (~8 min):
   ```
   nohup python -u -m aquacal calibrate config_quickstart_not_paper.yaml -v > ../quickstart.log 2>&1 &
   disown
   ```
   Must exit 0. Record its runtime. **Do not record or quote its numbers anywhere** — D-12: they
   do not reproduce §3. Then `mv output output_quickstart` so step 3 starts clean.

3. **Gate 1 — the paper command exactly as the tutorial prints it** (~50 min, alone on the box):
   ```
   nohup python -u -m aquacal calibrate config_paper.yaml -v > ../gate1_paper.log 2>&1 &
   disown
   ```
   Poll with `tail -20 ../gate1_paper.log`. A quiet log is not a dead run — check the process
   listing before relaunching.

4. **Gate 3, part two — every remaining command block on the tutorial page.** Work through
   `docs/tutorials/03_cli_walkthrough.md` top to bottom and execute each fenced `bash` or
   `python -c` block against this directory, in order. The one exception is the
   `load_example('real-rig')` download in §1, which cannot run until the new version is published;
   record it as "deferred to plan 21-10 (post-publish)". Note every block that fails or needs
   adjustment — the edits go in Task 3.
  </how-to-verify>
  <acceptance_criteria>
    - Both `aquacal calibrate` invocations exited 0, with the final log lines recorded.
    - `gate1_scratch/real-rig/output/diagnostics.json` exists and parses as JSON.
    - `gate1_scratch/real-rig/output/benchmark.json` exists and its `problem_shape.n_frames_holdout` equals 52.
    - The commands run were copied verbatim from `docs/tutorials/03_cli_walkthrough.md`: the SUMMARY quotes the tutorial's printed text and the command actually run, and they match character for character.
    - Every fenced command block on the tutorial page has a pass/fail record, except the `load_example` download, explicitly recorded as deferred to plan 21-10.
    - `git log --oneline -1` returns the same sha before and after the run (no commit landed mid-run).
    - Both wall-clock runtimes are recorded, with no runtime attributed to any code change (project rule).
  </acceptance_criteria>
  <verify>
    <automated>python -c "
import json,os
base=r'C:/Users/tucke/Desktop/Aqua/AquaCal/gate1_scratch/real-rig/output'
d=json.load(open(os.path.join(base,'diagnostics.json')))
b=json.load(open(os.path.join(base,'benchmark.json')))
assert b['problem_shape']['n_frames_holdout']==52, b['problem_shape']
print('ok', d['reconstruction']['num_comparisons'])"</automated>
  </verify>
  <done>Both calibrations exited 0 from the zipped bytes, diagnostics.json and benchmark.json exist, and every tutorial command block has a recorded exit status.</done>
  <resume-signal>Type "gate1 run complete" with both exit codes and both runtimes, or paste the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Compare the nine §3 quantities — D-16 halt on any miss</name>
  <files>.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md</files>
  <read_first>
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/gate1_scratch/real-rig/output/diagnostics.json` (the fresh run)
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/gate1_scratch/real-rig/reference_outputs/diagnostics.json` (the archive's shipped copy of the §3 source)
    - `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-FRAMESET-PROVENANCE.md` § "The finding"
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md` D-16 (the halt condition)
  </read_first>
  <action>
Write a throwaway comparison script (do NOT commit it) that loads both `diagnostics.json` files
and reports all nine quantities side by side with a percent delta:
  1. mean of the 12 primary `reprojection_rms` values
  2. min and max of those 12, each with its camera name
  3. `auxiliary_cameras.e3v8250.reprojection_rms`
  4. `reconstruction.mean`
  5. `reconstruction.rmse`
  6. `reconstruction.percent_error`
  7. `reconstruction.num_comparisons`
  8. `camera_heights.water_z`
  9. min and max camera height, each with its camera name

**Pass condition, declared in advance and not renegotiable after the fact:**
  - Quantity 7, `num_comparisons`, must be **exactly 7762**. It is an integer count, so any other
    value means the frameset is wrong, not that the solve drifted.
  - Every other quantity must agree with the reference to **at least 4 significant figures**. The
    19.1 wave-6 gate measured 0.000% on all nine against this frameset, so anything materially
    worse is a real difference, not noise.

**D-16 — if ANY quantity misses, HALT:**
  - Do NOT widen the tolerance.
  - Do NOT edit §3 to match what the archive produces. Option (b) was considered and rejected at
    the 19.1 wave-6 gate; the ENLARGE decision was chosen precisely because the full frameset
    reproduces §3 exactly and therefore needs no manuscript prose edits against a
    ~3,916-of-4,000 word budget.
  - Do NOT proceed to plan 21-09. A minted DOI cannot be withdrawn.
  - Write the full nine-row comparison into `21-ARCHIVE-MANIFEST.md` under a
    `## HALT - gate 1 FAILED` heading, mark gate 1 FAIL in the gates table, stop, and escalate to
    the user with the table and your leading hypotheses, in this order: a wrong `frame_step` in
    one of the two extractions; a camera-order difference in `config_paper.yaml`; a missing or
    extra frame in one camera directory.

On PASS: append the nine-row comparison under `## D-15 Gate 1 - §3 reproduction` in
`21-ARCHIVE-MANIFEST.md` and set gate 1 to PASS in the gates table.

Do not delete `gate1_scratch/` — Task 3 needs it.
  </action>
  <verify>
    <automated>python -c "
import json
t=open('.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md',encoding='utf-8').read()
assert 'D-15 Gate 1' in t, 'gate 1 section missing'
assert 'HALT' not in t, 'gate 1 HALTED - do not proceed to plan 21-09'
assert '7762' in t
d=json.load(open(r'C:/Users/tucke/Desktop/Aqua/AquaCal/gate1_scratch/real-rig/output/diagnostics.json'))
assert d['reconstruction']['num_comparisons']==7762, d['reconstruction']['num_comparisons']
print('gate1 PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `output/diagnostics.json`'s `reconstruction.num_comparisons` is exactly `7762`.
    - All eight remaining quantities agree with `reference_outputs/diagnostics.json` to >= 4 significant figures; the nine-row table with per-row percent deltas is written into `21-ARCHIVE-MANIFEST.md`.
    - The manifest document contains the heading `## D-15 Gate 1 - §3 reproduction` and does **not** contain the string `HALT`.
    - The gates table in `21-ARCHIVE-MANIFEST.md` shows gate 1 PASS.
    - No comparison script is left committed: `git status --porcelain` shows no new untracked `.py` file.
    - On failure, the plan stops here: the SUMMARY records the FAIL table and no work proceeds to plan 21-09.
  </acceptance_criteria>
  <done>Gate 1 is PASS with a recorded nine-row comparison, or the phase has halted and escalated with a FAIL table.</done>
</task>

<task type="auto">
  <name>Task 3: Close gate 3 — reconcile the tutorial with what actually ran</name>
  <files>docs/tutorials/03_cli_walkthrough.md, .planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md</files>
  <read_first>
    - `docs/tutorials/03_cli_walkthrough.md` (full)
    - Task 1's per-block pass/fail record from the SUMMARY
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/gate1_scratch/real-rig/output/` directory listing (what the run actually produced, versus what §4 of the tutorial claims it produces)
  </read_first>
  <action>
For every tutorial command block Task 1 recorded as failing or needing adjustment, fix the
**tutorial** — not the archive — so the printed command is what a reader can run. Typical fixes:
a flag spelled differently by `src/aquacal/cli.py`, a relative path that assumed a different cwd,
or an output filename §4 names that the run does not actually produce.

Then re-verify the two D-06 properties that Task 1's run makes checkable for the first time:
  - Every §3 value quoted in the tutorial matches the corresponding value in
    `gate1_scratch/real-rig/reference_outputs/diagnostics.json`. Assert each of the nine
    numerically, not by eye.
  - Every quoted number still names its source file inline (`reference_outputs/...`).

Update `21-ARCHIVE-MANIFEST.md`: set gate 3 to PASS in the gates table, and add a
`## D-15 Gate 3 - CLI tutorial commands` section listing each command block, its exit status, and
any edit made to the tutorial as a result. Explicitly list the `load_example` block as deferred
to plan 21-10.

Finally confirm all four gates read PASS. Then delete `gate1_scratch/` and
`archive_staging_smoke/` if present — together they are several GB — but **keep**
`archive_staging/` and `real-rig-calib.zip`; plan 21-09 uploads the zip.

Rebuild the docs to confirm the tutorial edits did not break the site:
`sphinx-build -W --keep-going -b html docs docs/_build/html`
  </action>
  <verify>
    <automated>python -c "
import json,re
ref=json.load(open(r'C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/reference_outputs/diagnostics.json'))
t=open('docs/tutorials/03_cli_walkthrough.md',encoding='utf-8').read()
assert str(ref['reconstruction']['num_comparisons']) in t
for v in (ref['reconstruction']['mean'], ref['reconstruction']['rmse'], ref['reconstruction']['percent_error']):
    assert any(s in t for s in (f'{v:.5f}', f'{v:.5g}', f'{v*1000:.5f}', f'{v:.4f}')), v
m=open('.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md',encoding='utf-8').read()
assert m.count('PASS')>=4, m.count('PASS')
assert 'HALT' not in m
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Every command block in `docs/tutorials/03_cli_walkthrough.md` has a recorded exit status in the manifest's gate-3 section, and every non-zero one has a corresponding tutorial edit.
    - Each of the nine §3 values printed in the tutorial is asserted numerically equal to the corresponding value in `reference_outputs/diagnostics.json`.
    - `grep -c 'reference_outputs/' docs/tutorials/03_cli_walkthrough.md` still returns >= 9 (D-06 attribution survived the edits).
    - The gates table in `21-ARCHIVE-MANIFEST.md` shows all four D-15 gates PASS, and the file contains no `HALT`.
    - `sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/gate1_scratch` no longer exists; `real-rig-calib.zip` still does.
  </acceptance_criteria>
  <done>All four D-15 gates read PASS, the tutorial's commands are proven executable as printed, and the scratch trees are cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged zip -> permanent public DOI | This is the last point at which the archive can still be changed |
| documented commands -> reader's shell | Every command printed in the tutorial is executed by readers verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08-01 | Repudiation | published archive does not reproduce published numbers | mitigate | Gate 1 runs the full solve from the exact zip bytes and asserts `num_comparisons == 7762` plus eight quantities to 4 s.f.; D-16 makes any miss a hard halt with no tolerance-widening and no §3 edit |
| T-21-08-02 | Tampering | a documented command that silently does something else | mitigate | Gate 3 executes every fenced block verbatim and records a per-block exit status; failures are fixed in the tutorial, never worked around by hand |
| T-21-08-03 | Information disclosure | scratch trees and logs holding local paths | mitigate | `gate1_scratch/` is deleted in Task 3; the tutorial is re-grepped for `C:[\\/]Users` after edits |
| T-21-08-04 | Denial of service | executor stalling on the ~50 min solve | mitigate | Task 1 is `checkpoint:human-action`, `autonomous: false`, with an explicit do-not-run-from-an-executor banner and `nohup … & disown` |
| T-21-08-05 | Spoofing | quoting the quickstart run's numbers as if they were §3 | mitigate | Task 1 forbids recording the quickstart numbers anywhere; the config's own filename carries the warning (D-12) |
| T-21-08-SC | Tampering | package installs | mitigate | Nothing is installed; the run uses the existing AquaCal env and the already-installed `aquacal` |
</threat_model>

<verification>
- `output/diagnostics.json`'s `reconstruction.num_comparisons == 7762`.
- All nine §3 quantities agree to >= 4 significant figures; the table is in `21-ARCHIVE-MANIFEST.md`.
- `21-ARCHIVE-MANIFEST.md` shows four PASS gates and contains no `HALT`.
- `sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.

**Do not proceed to plan 21-09 unless all four gates read PASS.**
</verification>

<success_criteria>
The archive that will be uploaded has been proven, from its own zipped bytes, to reproduce §3
exactly; the CLI tutorial's commands have been proven executable as printed; all four D-15 gates
are PASS and recorded; or the phase has halted per D-16 with no fallback taken.
</success_criteria>

<output>
Create `.planning/phases/21-new-feature-documentation-dataset-refresh/21-08-SUMMARY.md` when done.
</output>
