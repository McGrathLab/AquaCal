---
phase: 21-new-feature-documentation-dataset-refresh
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/tutorials/01_full_pipeline.ipynb
  - docs/tutorials/02_synthetic_validation.ipynb
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "Notebook 01 contains no Zenodo download path at all — no branch, no option bullet, no narration (D-18)."
    - "Both notebooks run start to finish on synthetic data in minutes, not tens of minutes (D-17, D-19)."
    - "Every committed cell output was produced by the notebook in its current edited form (D-21)."
    - "No narration in either notebook describes a four-stage pipeline or a stale runtime/size estimate (D-20)."
  artifacts:
    - path: "docs/tutorials/01_full_pipeline.ipynb"
      provides: "Synthetic-only end-to-end pipeline tutorial with fresh outputs"
    - path: "docs/tutorials/02_synthetic_validation.ipynb"
      provides: "Refractive-vs-non-refractive validation tutorial at RIG_SIZE='small' with fresh outputs"
  key_links:
    - from: "docs/tutorials/01_full_pipeline.ipynb"
      to: "synthetic scenario generators"
      via: "the surviving DATA_SOURCE branches only"
      pattern: "synthetic-small"
---

<objective>
Make both tutorial notebooks fast, synthetic-only, internally consistent, and freshly executed
(D-17, D-18, D-19, D-20, D-21).

Purpose: `DATA_SOURCE = "zenodo"` is the committed default in notebook 01 today, so notebook 01
*is* currently the full-dataset tutorial — which is why its stored outputs are real-rig outputs
and why its Colab badge is dishonest against a 4.4 GB download. The real-data path moves
entirely to the CLI tutorial (plan 21-03) and the experiment suite. Notebook 02 defaults to a
12-camera / ~60-minute configuration nobody re-runs.

Output: two edited, re-executed notebooks with fresh committed outputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notebook 01 — delete the Zenodo path and do the full editorial pass</name>
  <files>docs/tutorials/01_full_pipeline.ipynb</files>
  <read_first>
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-PATTERNS.md` § "`docs/tutorials/01_full_pipeline.ipynb`" — the cell-by-cell map with verbatim excerpts
    - `docs/tutorials/01_full_pipeline.ipynb` — **read every markdown and code cell in full**; the four cells named below are known items, not the complete list (D-20)
    - `docs/guide/optimizer.md` (the canonical three-stage framing DOCS-06 settled)
  </read_first>
  <action>
Edit the notebook JSON in place (or via `nbformat`), preserving cell ids and metadata. Known
required edits, by cell index:

- **cell 2 (markdown):** delete the entire `**`zenodo`**: Downloads a real hardware dataset from
  Zenodo (~164 MB, 13 cameras) ...` bullet. The "~164 MB" claim dies with it.
- **cell 3 (code):** change `DATA_SOURCE = "zenodo"` to `DATA_SOURCE = "synthetic-small"` and
  rewrite the trailing options comment to `# Options: "synthetic-small", "synthetic-large"` —
  `"zenodo"` must not appear.
- **cell 6 (markdown):** delete the bullet
  `- **Zenodo (real rig)**: download the dataset and run the complete pipeline (Stages 1-4, ~15-20 min)`.
- **cell 7 (code):** delete the **entire** `elif DATA_SOURCE == "zenodo":` branch including its
  `load_example` call, its `run_calibration` call, and its `chdir` logic. Keep the
  `if DATA_SOURCE in ("synthetic-small", "synthetic-large"):` branch and the trailing
  `else: raise ValueError(...)`. Update the `ValueError` message so it lists only the two
  surviving options.
- **cell 0 (markdown):** rewrite `"Run all four calibration stages (or load a previous
  calibration)"` into the three-stage framing (in-air intrinsics; extrinsic initialisation by
  best-first pose-graph traversal; joint refractive bundle adjustment). Drop "or load a previous
  calibration" if the surviving code path no longer offers it.
- **cell 26 (markdown, Summary):** rewrite list items 1 and 2 — `"1. Load calibration data
  (synthetic or real hardware from Zenodo)"` and `"2. Run the calibration pipeline (Stages 2-3
  for synthetic, Stages 1-4 for real data)"` — to describe only the synthetic path and the
  three-stage model.

**Then do the rest of the D-20 editorial pass, which the list above does not cover.** Read every
remaining markdown cell and every code comment and fix, at minimum: any surviving mention of
Zenodo, real hardware, `load_example`, download size, "Stage 4", "four stages", or a runtime
estimate that no longer applies. Where the notebook's prose points a reader at real data, replace
the pointer with a link to the new CLI tutorial:
`../tutorials/03_cli_walkthrough.md` — use the relative form `03_cli_walkthrough.md` since it is
a sibling document. Record every edit you made beyond the six listed cells in the SUMMARY, cell
index by cell index.

Also check the Colab badge cell: if the badge's stated prerequisites or download claims referred
to the Zenodo dataset, correct them. A synthetic-only notebook is genuinely Colab-viable, so keep
the badge.

Do not change the notebook's scientific content or the synthetic scenarios it runs.
  </action>
  <verify>
    <automated>python -c "
import json
nb=json.load(open('docs/tutorials/01_full_pipeline.ipynb',encoding='utf-8'))
src=''.join(''.join(c['source']) for c in nb['cells'])
for bad in ['zenodo','Zenodo','load_example','164 MB','Stages 1-4','Stage 4','four calibration stages']:
    assert bad not in src, bad
print('ok', len(nb['cells']), 'cells')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json;nb=json.load(open('docs/tutorials/01_full_pipeline.ipynb',encoding='utf-8'));s=''.join(''.join(c['source']) for c in nb['cells']);assert 'zenodo' not in s.lower()"` exits 0.
    - The same concatenated source contains none of: `load_example`, `164 MB`, `Stages 1-4`, `Stage 4`, `four calibration stages`, `all four calibration stages`.
    - `DATA_SOURCE = "synthetic-small"` appears exactly once in the notebook source.
    - The notebook remains valid: `python -c "import nbformat;nbformat.validate(nbformat.read('docs/tutorials/01_full_pipeline.ipynb',as_version=4))"` exits 0.
    - The SUMMARY lists every edited cell index and, for edits beyond the six named cells, the old and new text.
  </acceptance_criteria>
  <done>Notebook 01 has no Zenodo path in code or prose, defaults to `synthetic-small`, and uses the three-stage framing throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Notebook 02 — demote RIG_SIZE and do the full editorial pass</name>
  <files>docs/tutorials/02_synthetic_validation.ipynb</files>
  <read_first>
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-PATTERNS.md` § "`docs/tutorials/02_synthetic_validation.ipynb`" — both confirmed `RIG_SIZE` sites with verbatim excerpts
    - `docs/tutorials/02_synthetic_validation.ipynb` — **read every markdown and code cell in full** (D-20)
  </read_first>
  <action>
- **cell 2 (code):** change `RIG_SIZE = "large"` to `RIG_SIZE = "small"` (D-19). Reorder the
  three-line explanatory comment block above it so the now-default `"small"` option is described
  first, matching notebook 01 cell 2's recommended-option-leads convention. Verify the two
  runtime claims in that comment (`"small"` ~2 min, `"large"` ~60 min) against what you actually
  observe in Task 3 and correct them to the measured values.
- **cell 6 (code):** no change required — the `if RIG_SIZE == "small": ... else: ...` branch
  already handles both values. Confirm this by reading it; do not edit it.

**Then the rest of the D-20 pass.** Read every remaining markdown cell and code comment. Fix any
narration that assumes 12 cameras, 30 frames, "realistic noise", or a ~60-minute runtime as the
default experience, since the default is now 4 cameras / 20 frames / `create_scenario("ideal")`.
Where a claim about the *magnitude* of a result depends on the rig size, either restate it
against the small rig's actual output from Task 3 or explicitly attribute it to `RIG_SIZE =
"large"`. Fix any "Stage 4" / "four stages" framing. Record every edit in the SUMMARY by cell
index.

This notebook is two feature releases behind and nothing has re-read it since 2026-02 — treat
the two known items as the start of the pass, not the whole of it.
  </action>
  <verify>
    <automated>python -c "
import json
nb=json.load(open('docs/tutorials/02_synthetic_validation.ipynb',encoding='utf-8'))
src=''.join(''.join(c['source']) for c in nb['cells'])
assert 'RIG_SIZE = \"small\"' in src
assert 'RIG_SIZE = \"large\"' not in src
for bad in ['Stage 4','four stages','four calibration stages']:
    assert bad not in src, bad
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `RIG_SIZE = "small"` appears in the notebook source and `RIG_SIZE = "large"` does not.
    - The concatenated source contains none of: `Stage 4`, `four stages`, `four calibration stages`.
    - `python -c "import nbformat;nbformat.validate(nbformat.read('docs/tutorials/02_synthetic_validation.ipynb',as_version=4))"` exits 0.
    - The comment block above the `RIG_SIZE` assignment describes `"small"` before `"large"`.
    - The SUMMARY lists every edited cell index with old and new text.
  </acceptance_criteria>
  <done>Notebook 02 defaults to the 4-camera small rig and its narration matches that default.</done>
</task>

<task type="auto">
  <name>Task 3: Re-execute both notebooks and commit fresh outputs</name>
  <files>docs/tutorials/01_full_pipeline.ipynb, docs/tutorials/02_synthetic_validation.ipynb</files>
  <read_first>
    - `docs/conf.py` lines 30-40 (`nbsphinx_execute = "never"`, `nbsphinx_allow_errors = False` — committed outputs are what the site renders, and any cell error fails the build)
    - Both notebooks as edited in Tasks 1 and 2
    - `CLAUDE.md` § Environment (the AquaCal conda env requirement)
  </read_first>
  <action>
Re-execute both notebooks **in the AquaCal conda env** so the committed outputs match the edited
code. D-21 keeps this manual — do NOT add a Makefile target, a CI job, or a committed
re-execution script. Run, from the repo root:

```
jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=900 docs/tutorials/01_full_pipeline.ipynb
jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=900 docs/tutorials/02_synthetic_validation.ipynb
```

Expected runtimes: notebook 01 at `synthetic-small` — under 5 minutes; notebook 02 at
`RIG_SIZE = "small"` — roughly 2 minutes. **If either command has not returned after 15 minutes,
stop, do not background it, and escalate** with what the last executed cell was. Do NOT run
these with `run_in_background` and do NOT wait on a notification — per CLAUDE.md a backgrounded
long run inside an executor never returns.

Both must complete with zero cell errors — `nbsphinx_allow_errors = False` means a stored error
output fails the docs build.

After execution:
1. Record both measured wall-clock times and use them to correct notebook 02's runtime comment
   from Task 2 (re-run notebook 02 once more if that comment changed).
2. Re-run the two automated checks from Tasks 1 and 2 — execution must not have reintroduced any
   forbidden string.
3. Record the exact commands and the measured runtimes in the SUMMARY. D-21 requires the manual
   re-execution steps to be written into the phase record, because nothing automates them.
4. If either notebook writes files under `docs/tutorials/output/` or
   `docs/tutorials/aquacal_data/`, check `git status --porcelain` and confirm those paths are
   gitignored or are not staged; do not commit generated run artifacts.
  </action>
  <verify>
    <automated>python -c "
import json
for p in ['docs/tutorials/01_full_pipeline.ipynb','docs/tutorials/02_synthetic_validation.ipynb']:
    nb=json.load(open(p,encoding='utf-8'))
    exec_counts=[c.get('execution_count') for c in nb['cells'] if c['cell_type']=='code']
    assert all(e is not None for e in exec_counts), (p,'unexecuted code cell')
    assert exec_counts==sorted(e for e in exec_counts), (p,'out-of-order execution')
    errs=[o for c in nb['cells'] for o in c.get('outputs',[]) if o.get('output_type')=='error']
    assert not errs, (p,errs[:1])
    print('ok',p,'max exec',max(exec_counts))"</automated>
  </verify>
  <acceptance_criteria>
    - Every code cell in both notebooks has a non-null `execution_count`, and the counts are strictly increasing top to bottom (proving a single clean top-to-bottom run).
    - Neither notebook contains any output of `output_type == "error"`.
    - Re-running Task 1's and Task 2's forbidden-string checks still exits 0 after execution.
    - `sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0 (run this after plan 21-03's index change has merged; if `03_cli_walkthrough` is not yet present, record that the build was deferred to the post-merge gate and say so in the SUMMARY).
    - The SUMMARY records both `jupyter nbconvert` commands verbatim and both measured wall-clock runtimes (D-21).
    - `git status --porcelain docs/tutorials/output docs/tutorials/aquacal_data` produces no staged additions.
  </acceptance_criteria>
  <done>Both notebooks carry fresh, error-free, monotonically-numbered outputs from a single clean run, and the manual re-execution recipe is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notebook outputs -> published docs site | Committed cell outputs are rendered verbatim; `nbsphinx_execute = "never"` |
| local filesystem -> notebook outputs | Executed cells can print absolute paths and environment details into committed JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Information disclosure | committed cell outputs | mitigate | After re-execution, grep the notebook JSON for `C:\\Users` / `C:/Users` and for any hostname or username; both notebooks are synthetic-only so no real-rig path should appear. `detect-secrets` already excludes `docs/tutorials/*.ipynb` from its baseline, so this grep is the only control — run it explicitly |
| T-21-04-02 | Repudiation | stale outputs vs edited code | mitigate | The `execution_count` monotonicity check proves the committed outputs came from one clean run of the current code, not a partial re-run |
| T-21-04-03 | Denial of service | executor stalling on a long notebook run | mitigate | Both notebooks are synthetic and small by construction (D-17/D-19); a 15-minute hard stop with escalation is written into the task |
| T-21-04-SC | Tampering | package installs | mitigate | No package is installed; `jupyter`/`nbconvert` come from the existing AquaCal env |
</threat_model>

<verification>
- Neither notebook's source contains `zenodo`, `load_example`, `Stage 4`, or `four stages`.
- Every code cell has a non-null, strictly increasing `execution_count` and no error output.
- `nbformat.validate` passes on both files.
</verification>

<success_criteria>
Notebook 01 is synthetic-only with the Zenodo branch and all its narration removed; notebook 02
defaults to `RIG_SIZE = "small"`; both carry fresh committed outputs from a single clean run; the
manual re-execution commands and measured runtimes are recorded in the SUMMARY per D-21.
</success_criteria>

<output>
Create `.planning/phases/21-new-feature-documentation-dataset-refresh/21-04-SUMMARY.md` when done.
</output>
