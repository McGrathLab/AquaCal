---
phase: 21-new-feature-documentation-dataset-refresh
plan: 09
type: execute
wave: 5
depends_on: ["21-08"]
files_modified:
  - .planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md
autonomous: false
requirements: [DATA-02]

must_haves:
  truths:
    - "The new archive is published as a NEW VERSION of Zenodo record 18645385, not a fresh record, so the concept DOI and citation lineage are preserved (D-13)."
    - "No Zenodo API token entered the session or the repository (D-14)."
    - "The human step was transcription of pre-computed values, not judgement (D-14)."
    - "The DOI was minted only after all four D-15 gates read PASS."
  artifacts:
    - path: ".planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md"
      provides: "The minted record id and version DOI, filled into the TBD slot"
      contains: "zenodo_record_id"
  key_links:
    - from: "21-ARCHIVE-MANIFEST.md"
      to: "Zenodo record 18645385"
      via: "New version upload of real-rig-calib.zip"
      pattern: "18645385"
---

<objective>
Publish the regenerated archive as a new version of the existing Zenodo record, and capture the
minted record id (D-13, D-14).

Purpose: DATA-02 needs a published, publicly downloadable archive before `manifest.json` can be
updated and before `load_example("real-rig")` can be verified end to end. A Zenodo draft is not
publicly downloadable, which is exactly why D-15 front-loaded four gates.

Output: a published Zenodo version with its own DOI, and the record id written into
`21-ARCHIVE-MANIFEST.md`. No source code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Pre-flight — confirm all four gates PASS and the bytes are final</name>
  <files>.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md</files>
  <read_first>
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md` (the gates table and the pre-computed md5 and size)
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-08-SUMMARY.md` (gate 1 and gate 3 evidence)
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md` D-13, D-14, D-15, D-16
  </read_first>
  <action>
**CHECKPOINT — NOT AUTONOMOUS.** The orchestrator or the user performs this task by running the
commands in `<how-to-verify>` below, in order, exactly as written. Do NOT execute this task from
inside an executor subagent: per CLAUDE.md, an executor that backgrounds a long run stalls
permanently and never returns. Record every command, its exit code and its wall-clock runtime in
the SUMMARY, then confirm each item in `<acceptance_criteria>` before signalling resume.
  </action>
  <what-built>
`real-rig-calib.zip` at `C:/Users/tucke/Desktop/Aqua/AquaCal/`, gated by all four D-15 checks:
gate 1 (§3 reproduction, `num_comparisons = 7762`), gate 2 (md5, size, extraction layout),
gate 3 (CLI tutorial commands run verbatim), gate 4 (both configs load and validate under
v2.0.0).
  </what-built>
  <how-to-verify>
Run these three checks and report their output. **If any fails, STOP — do not upload.** A minted
DOI cannot be withdrawn.

1. All four gates PASS and no halt was recorded:
   ```
   grep -n "PASS\|FAIL\|HALT" .planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md
   ```
   Expect four PASS rows, zero FAIL, zero HALT.

2. The zip on disk is still byte-identical to the one the gates ran against — re-compute its md5
   and compare against the recorded value:
   ```
   python -c "
   import hashlib,os,re
   p=r'C:/Users/tucke/Desktop/Aqua/AquaCal/real-rig-calib.zip'
   h=hashlib.md5()
   with open(p,'rb') as f:
       for b in iter(lambda: f.read(1<<20), b''): h.update(b)
   m=open('.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md',encoding='utf-8').read()
   rec=re.search(r'checksum:\s*md5:([0-9a-f]{32})', m).group(1)
   rsz=int(re.search(r'size_bytes:\s*(\d+)', m).group(1))
   print('recorded', rec, rsz); print('actual  ', h.hexdigest(), os.path.getsize(p))
   assert h.hexdigest()==rec and os.path.getsize(p)==rsz"
   ```

3. **Content spot-check before an irrevocable publication.** Open three or four extracted frames
   at random — one extrinsic, one intrinsic, one from a different camera — and confirm they show
   only the calibration board and the tank, with nothing unintended in frame. This is the last
   opportunity; the archive becomes permanently public in Task 2.

Also confirm the working tree is clean and nothing has been committed since the gate runs, so the
published bytes correspond to a known repository state.
  </how-to-verify>
  <acceptance_criteria>
    - `21-ARCHIVE-MANIFEST.md` shows four `PASS` rows, zero `FAIL`, zero `HALT`.
    - The re-computed md5 and byte size of `real-rig-calib.zip` equal the recorded `checksum` and `size_bytes` exactly.
    - The frame spot-check is recorded in the SUMMARY: which files were opened and the confirmation that nothing unintended appears.
    - `git status --porcelain` is clean.
  </acceptance_criteria>
  <verify>
    <automated>python -c "
import hashlib,os,re
doc='.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md'
m=open(doc,encoding='utf-8').read()
assert 'HALT' not in m and 'FAIL' not in m, 'gates not clean'
assert m.count('PASS')>=4, m.count('PASS')
p=r'C:/Users/tucke/Desktop/Aqua/AquaCal/real-rig-calib.zip'
h=hashlib.md5()
f=open(p,'rb')
for b in iter(lambda: f.read(1<<20), b''): h.update(b)
assert h.hexdigest()==re.search(r'checksum:\s*md5:([0-9a-f]{32})',m).group(1)
assert os.path.getsize(p)==int(re.search(r'size_bytes:\s*(\d+)',m).group(1))
print('pre-flight ok')"</automated>
  </verify>
  <done>All four D-15 gates read PASS, the zip's md5 and size still match the recorded values, and the frame spot-check is recorded.</done>
  <resume-signal>Type "pre-flight ok" with the md5/size comparison output, or "stop" with the failure.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Upload and publish a new version of record 18645385 (MANUAL, user, web UI)</name>
  <files>none</files>
  <read_first>
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md` (the values to transcribe)
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md` D-13 and D-14
  </read_first>
  <action>
**CHECKPOINT — NOT AUTONOMOUS.** The orchestrator or the user performs this task by running the
commands in `<how-to-verify>` below, in order, exactly as written. Do NOT execute this task from
inside an executor subagent: per CLAUDE.md, an executor that backgrounds a long run stalls
permanently and never returns. Record every command, its exit code and its wall-clock runtime in
the SUMMARY, then confirm each item in `<acceptance_criteria>` before signalling resume.
  </action>
  <what-built>The gated `real-rig-calib.zip`, ready to upload.</what-built>
  <how-to-verify>
**This step is performed by the user, by hand, in the Zenodo web UI. D-14: no API token enters
this session or the repository, and no upload script is written.** An agent must not attempt this
step, must not ask for a token, and must not add a `zenodo` dependency or a `ZENODO_TOKEN`
environment variable anywhere.

*Concern raised and overridden by the user, recorded and not re-litigated: 4.4 GB through a
browser is fragile and not reproducible for the next release.*

Numbered checklist:

1. Sign in to https://zenodo.org and open record **18645385** (the existing `real-rig` archive).
2. Click **New version**. This preserves the concept DOI and keeps the 2026-02 archive resolvable
   for anyone citing it (D-13). **Do not create a fresh record.**
3. In the new draft, **delete the old `real-rig-calib.zip` file** from the file list.
4. Upload `C:\Users\tucke\Desktop\Aqua\AquaCal\real-rig-calib.zip` (~4.4 GB). Keep the browser tab
   open and the machine awake; a browser upload of this size is fragile. If it fails partway,
   delete the partial file from the draft and restart the upload — do not publish a partial file.
5. When the upload finishes, confirm Zenodo's reported file size matches `size_bytes` from
   `21-ARCHIVE-MANIFEST.md` **exactly**, digit for digit. Zenodo also displays an md5 checksum for
   the uploaded file — confirm it matches the `checksum` value in the manifest document. If either
   differs, the upload is corrupt: delete and re-upload. **Do not publish on a mismatch.**
6. Update the record's description to state that this version carries the full 262-frame lossless
   frameset that reproduces the paper's §3 numbers, that it ships two configs
   (`config_paper.yaml` reproduces §3; `config_quickstart_not_paper.yaml` does not), and that it
   includes the run's own reference outputs under `reference_outputs/`. Leave authors, license and
   keywords as they are.
7. Click **Publish**. **This mints a DOI and cannot be undone.** Only click it if step 5 matched
   exactly and Task 1's pre-flight passed.
8. From the published page, copy back and report:
   - the **new version's record id** (the integer in the record URL — this is the value
     `manifest.json`'s `zenodo_record_id` takes),
   - the **version DOI**,
   - the **concept DOI** (unchanged),
   - the **filename** as Zenodo lists it (must still be `real-rig-calib.zip`),
   - the **file size** and **md5** as Zenodo displays them.

Then fill those values into `21-ARCHIVE-MANIFEST.md`, replacing the
`zenodo_record_id: TBD` line, and add a `## Published` section recording the version DOI, the
concept DOI, and the publication timestamp.
  </how-to-verify>
  <acceptance_criteria>
    - `21-ARCHIVE-MANIFEST.md` contains a `zenodo_record_id: <integer>` line with no `TBD`, and that integer is **not** `18645385` (a new version gets a new record id).
    - It contains a `## Published` section with the version DOI, the concept DOI, and the publication timestamp.
    - The Zenodo-reported file size equals the recorded `size_bytes` exactly, and the Zenodo-reported md5 equals the recorded `checksum` — both comparisons quoted in the SUMMARY.
    - The published filename is `real-rig-calib.zip`.
    - No token, credential, or secret appears anywhere: `git diff` introduces no `ZENODO`, `TOKEN`, `api_key`, or `Bearer` string, and `pre-commit run detect-secrets --all-files` exits 0.
    - No upload script was created: `git status --porcelain` shows no new `.py` file under `scripts/` or the repo root.
  </acceptance_criteria>
  <verify>
    <automated>python -c "
import re
m=open('.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md',encoding='utf-8').read()
rid=re.search(r'zenodo_record_id:\s*(\d+)', m)
assert rid and int(rid.group(1))!=18645385, 'record id missing or unchanged'
assert '## Published' in m
for bad in ('ZENODO_TOKEN','api_key','Bearer '):
    assert bad not in m, bad
print('published', rid.group(1))"</automated>
  </verify>
  <done>A new version of record 18645385 is published; its record id, version DOI and concept DOI are recorded; no credential entered the session or the repo.</done>
  <resume-signal>Paste the new record id, the version DOI, the concept DOI, and Zenodo's reported size and md5 — or "stop" with what went wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user credentials -> Zenodo | Authentication happens entirely in the user's browser session |
| repository/session -> secret material | D-14 exists specifically to keep a Zenodo API token out of both |
| local zip -> permanent public artifact | Publication is irreversible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09-01 | Information disclosure | Zenodo API credential | mitigate | D-14 makes upload manual and browser-based. No token is requested, stored, or referenced; acceptance asserts `git diff` introduces no `ZENODO`/`TOKEN`/`api_key`/`Bearer` string and that `detect-secrets` passes. No upload script may be created |
| T-21-09-02 | Tampering | corrupt or partial 4.4 GB browser upload | mitigate | Step 5 compares Zenodo's reported size **and** md5 against the pre-computed manifest values before Publish is clicked; a mismatch means delete and re-upload, never publish |
| T-21-09-03 | Repudiation | citation lineage broken by a fresh record | mitigate | D-13: New version of record 18645385, never a new record. Acceptance asserts the captured record id differs from 18645385 while the concept DOI is recorded unchanged |
| T-21-09-04 | Information disclosure | unintended content in published frames | mitigate | Task 1's spot-check is the last pre-publication review; it is a blocking human step and its result is recorded |
| T-21-09-05 | Elevation of privilege | an agent publishing on the user's behalf | mitigate | Both tasks are `gate="blocking-human"` and are never auto-approvable; `workflow.auto_advance` must be ignored here. A minted DOI cannot be withdrawn |
| T-21-09-SC | Tampering | package installs | mitigate | No package is installed and no `zenodo` client library is added |
</threat_model>

<verification>
- Four PASS gates and zero HALT in `21-ARCHIVE-MANIFEST.md` before upload.
- Zenodo's reported size and md5 match the pre-computed values digit for digit.
- `21-ARCHIVE-MANIFEST.md` carries a concrete `zenodo_record_id` and a `## Published` section.
- `pre-commit run detect-secrets --all-files` exits 0.
</verification>

<success_criteria>
A new version of Zenodo record 18645385 is published carrying `real-rig-calib.zip` whose size and
md5 match the pre-computed manifest values; the new record id, version DOI and concept DOI are
recorded; no credential entered the session or the repository.
</success_criteria>

<output>
Create `.planning/phases/21-new-feature-documentation-dataset-refresh/21-09-SUMMARY.md` when done.
</output>
