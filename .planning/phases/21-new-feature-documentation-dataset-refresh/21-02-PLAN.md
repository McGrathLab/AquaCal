---
phase: 21-new-feature-documentation-dataset-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guide/benchmarking.md
  - docs/guide/configuration.md
  - docs/guide/refractive_geometry.md
  - docs/guide/index.md
autonomous: true
requirements: [DOCS-05]

must_haves:
  truths:
    - "A reader holding a benchmark.json can look up every field in it on one docs page (D-02)."
    - "A reader holding a trace_stage3.csv knows what each column means and how to read convergence from it (D-03)."
    - "A reader holding conditioning.json + conditioning.npz knows what each array is and how the correlation matrix is indexed (D-03)."
    - "The two one-line internals rows in configuration.md point forward to that page instead of being the only documentation (D-03)."
    - "shared_interface's existing ablation-option framing is confirmed current, not rewritten (D-04)."
  artifacts:
    - path: "docs/guide/benchmarking.md"
      provides: "benchmark.json / trace CSV / conditioning .npz output-schema reference"
      contains: "schema_version"
      min_lines: 150
    - path: "docs/guide/index.md"
      provides: "Nav entry and toctree stem for benchmarking"
      contains: "benchmarking"
  key_links:
    - from: "docs/guide/configuration.md"
      to: "docs/guide/benchmarking.md"
      via: "forward link on save_optimization_trace / save_conditioning rows"
      pattern: "benchmarking\\.md"
    - from: "docs/guide/benchmarking.md"
      to: "docs/guide/optimizer.md"
      via: "parameter-vector ordering needed to read the correlation matrix"
      pattern: "optimizer\\.md"
---

<objective>
Close the real DOCS-05 gap: `benchmark.json` has zero documentation anywhere today, and the
optimization trace and conditioning outputs have two one-line config rows and nothing else.

Purpose: a flag whose output is undocumented is close to an undocumented feature (D-03). Phases
16 and 19 shipped the observability surface this milestone's reviewer response leans on; this
page is what makes it usable by someone who is not the author.

Output: a new `docs/guide/benchmarking.md`, forward links from `configuration.md`'s `internals`
section, a verified-only pass over the two `shared_interface` passages (D-04), and guide nav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Concrete schemas this page must document — read these from the live artifacts, do not invent:

**`benchmark.json` top-level keys** (live instance: `experiments/results/benchmark.json`):
`schema_version`, `environment`, `solver_config`, `problem_shape`, `stages`, `memory`, `accuracy`.

- `environment`: `aquacal_version`, `git_sha`, `git_sha_source`, `python_version`,
  `numpy_version`, `scipy_version`, `opencv_version`, `os`, `cpu_model`, `cpu_count_logical`,
  `ram_total_bytes`.
- `solver_config`: `robust_loss`, `loss_scale`, `refine_intrinsics`, `interface_normal_fixed`,
  `seed`.
- `problem_shape`: `n_cameras`, `n_frames_calibration`, `n_frames_holdout`.
- `stages.<stage_key>`: `nfev`, `njev`, `cost`, `optimality`, `status`, `message`, `ftol`,
  `xtol`, `gtol`, `max_nfev_effective`, `max_nfev_source`, `n_params`, `n_groups`, `n_residuals`,
  `fd_reduction`, `seconds`, and the paired `*_reason` fields that explain a `null`, plus a
  nested `memory` block (`mode`, `cumulative_peak_bytes_as_of_stage_end`,
  `commit_peak_bytes_as_of_stage_end`, `commit_current_bytes_as_of_stage_end`,
  `delta_bytes_since_previous_boundary`, `ram_total_bytes`).
- `memory`: `mode`, `whole_run_peak_bytes`.
- `accuracy`: `reprojection_rms`, `validation_3d_error_mean`, `validation_3d_error_std`.

**Trace CSV header** — `src/aquacal/calibration/_observability.py:193-202`, `TRACE_CSV_HEADER`:
```
iteration,n_fev,cost,step_norm,optimality,water_z,tilt_rx,tilt_ry
```
Written to `output_dir/internals/trace_stage3.csv`, `trace_stage3_rerun.csv`,
`trace_stage3_intrinsic_pass.csv` (`pipeline.py:1120-1352`).

**Conditioning payload** — `src/aquacal/validation/conditioning.py:220-244`,
`save_conditioning_report`:
- `internals/conditioning.json`: `condition_number` (null when non-finite), `singular_values`,
  `rank`, `rank_tolerance`, `n_params`, `n_residuals`, `parameter_names`, `correlation_npz`,
  `stage`.
- `internals/conditioning.npz`: `correlation` (n x n), `singular_values`, `parameter_names`.

Producers, for the "always up to date" pointer: `assemble_benchmark_record`,
`capture_environment`, `write_benchmark_json` (exported from `aquacal.io`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/guide/benchmarking.md</name>
  <files>docs/guide/benchmarking.md</files>
  <read_first>
    - `docs/guide/configuration.md` lines 1-11 (opening pattern), 87-118 (section+table+fenced-example pattern and the admonition style), 235-252 (the `internals` section this page expands), 311-316 (See Also footer)
    - `docs/guide/optimizer.md` lines 83-100 (parameter-vector ordering — the index of the correlation matrix) and 185-200 (the "numbers asserted live against the code" phrasing)
    - `experiments/results/benchmark.json` (the live schema instance to excerpt from)
    - `src/aquacal/calibration/_observability.py` lines 190-230 and 665-705 (`TRACE_CSV_HEADER`, `TraceRow`, `write_trace_csv`)
    - `src/aquacal/validation/conditioning.py` lines 180-250 (`save_conditioning_report` payload keys)
    - `src/aquacal/io/benchmark.py` (`assemble_benchmark_record`, `capture_environment`, `write_benchmark_json` signatures and docstrings)
  </read_first>
  <action>
Create `docs/guide/benchmarking.md`, titled `# Benchmarking & Diagnostics`. Use MyST throughout
(`:::{admonition}` colon-fence, `{class}`/`{func}`/`{mod}` roles) — the parser is myst-parser,
per `docs/conf.py:17-38`.

Opening (mirror `configuration.md:1-11`'s shape): one paragraph saying every calibration run
writes a `benchmark.json` into `output_dir`, and that two further diagnostic surfaces are opt-in
under `output_dir/internals/`. Then a paragraph naming exactly what this page documents — the
`benchmark.json` fields, the optimization trace CSV columns, and the conditioning JSON/NPZ
contents — and pointing at {func}`aquacal.io.assemble_benchmark_record` and
{func}`aquacal.io.write_benchmark_json` as the exhaustive, always-current source, the way
configuration.md points at `CalibrationConfig`.

Sections, in this order, each a `## ` heading with a `| Key | Type | Meaning |` table paired
with an immediately-following fenced ```json / ```csv excerpt taken *verbatim and trimmed* from
`experiments/results/benchmark.json` or a real trace CSV — never a hand-written example:

1. `## benchmark.json` — a short paragraph on when it is written and where, then
   `### schema_version` (state it is an integer, currently `1`, and that
   `experiments/_render.py` refuses an unrecognised value rather than guessing).
2. `### environment` — table of all eleven fields. Call out `git_sha` + `git_sha_source`
   explicitly as the provenance pair, and note `git_sha_source` records *how* the sha was
   obtained (e.g. `git_rev_parse`) so a packaged install's fallback is visible.
3. `### solver_config` — table of the five fields; note `seed` is the holdout-split seed.
4. `### problem_shape` — `n_cameras`, `n_frames_calibration`, `n_frames_holdout`.
5. `### stages` — the largest section. Explain that keys are stage names matching the
   three-stage model (e.g. `stage3_interface_optimization`, `stage3_intrinsic_pass`,
   `auxiliary_registration`, and per-camera sub-stage keys such as
   `auxiliary_registration_e3v8250`). Table every field listed in `<interfaces>`. Add a
   `:::{admonition} A null field always comes with a reason :class: tip` box explaining the
   `*_reason` convention — `n_params: null` paired with `n_params_reason` naming why the
   quantity does not exist for that stage (e.g. dense 2-point FD has no column-grouping
   structure) — and state plainly that a `null` here means "not applicable", never "not
   measured".
   Add a second `:::{admonition} Quote optimality to one significant figure :class: warning`
   box: `optimality` is a first-order stopping criterion, not an accuracy figure, and it is
   trustworthy only on a well-conditioned problem.
6. `### memory` — `mode` and `whole_run_peak_bytes`, plus the per-stage nested `memory` block.
   State that memory capture is opt-in (`benchmark_memory`) and that `mode` labels the
   measurement technique (e.g. `psutil_peak_wset`) so a number is never comparable across modes.
   Note that `cumulative_peak_bytes_as_of_stage_end` is a running maximum, not a per-stage
   figure, and that the per-stage delta is `delta_bytes_since_previous_boundary`.
7. `### accuracy` — the three fields, with units (`reprojection_rms` in pixels;
   `validation_3d_error_mean`/`_std` in metres).
8. `## Optimization trace (`internals/trace_*.csv`)` — enabled by
   `internals.save_optimization_trace: true`. Name the three files
   (`trace_stage3.csv`, `trace_stage3_rerun.csv`, `trace_stage3_intrinsic_pass.csv`) and give a
   `| Column | Meaning | How to read it |` table for all eight columns:
   `iteration` (accepted `trf` iterations only — rejected steps are not rows),
   `n_fev` (cumulative residual evaluations; the gap between consecutive `n_fev` values is the
   number of trial steps the trust region rejected),
   `cost` (0.5 * sum of squared robust-weighted residuals; must be monotonically non-increasing),
   `step_norm` (norm of the accepted parameter step; a collapse toward 0 with `cost` still
   falling means the trust region is shrinking),
   `optimality` (first-order optimality, the `gtol` criterion),
   `water_z` (the current shared interface height in metres — the single most useful column for
   watching the interface converge),
   `tilt_rx` / `tilt_ry` (interface normal tilt; `nan` when `normal_fixed: true`).
   Follow with the real 3-line excerpt from `experiments/results/e7_trace_shared_fixed.csv`.
9. `## Conditioning (`internals/conditioning.json` + `.npz`)` — enabled by
   `internals.save_conditioning: true`. Two tables: the JSON scalars/spectrum keys, and the NPZ
   arrays. State explicitly that the (n, n) correlation matrix is **never** serialized to JSON
   and lives only in the NPZ, and that `condition_number` is `null` rather than `Infinity` when
   non-finite because JSON has no infinity.
   `### Reading the correlation matrix` — state that row/column *i* is
   `parameter_names[i]`, that the ordering is the packed parameter vector documented in
   [Optimizer Pipeline](optimizer.md), and show a 5-line Python snippet that loads the `.npz`,
   builds a name->index map, and pulls one off-diagonal entry.
   Close with a `:::{admonition} The camera-height / interface-distance block :class: note`
   box (D-03): `water_z` and each camera's `C_z` enter the model almost entirely through their
   difference `h_c = water_z - C_z`, so a high-magnitude off-diagonal between the interface
   parameter and the camera translation-Z parameters is the degeneracy signature; a layout that
   fails to locate the water surface is expected to show a markedly higher off-diagonal in this
   block than a well-conditioned grid layout. Label this as the hypothesis it is and cite
   `.planning/MANUSCRIPT-FINDINGS.md` MF-12 by name **without** asserting the test has been run —
   it has not.
10. `## See Also` — reuse `configuration.md:311-316`'s bullet shape, linking
    `configuration.md` (the flags that produce these files), `optimizer.md` (parameter ordering),
    `cli.md`, and `troubleshooting.md`.

Do not use the word "four" of the stage model anywhere — DOCS-06 settled on three stages.
  </action>
  <verify>
    <automated>python -c "
import re,sys
t=open('docs/guide/benchmarking.md',encoding='utf-8').read()
need=['schema_version','environment','solver_config','problem_shape','stages','memory','accuracy','step_norm','optimality','correlation','singular_values','parameter_names','condition_number','optimizer.md','configuration.md']
missing=[n for n in need if n not in t]
assert not missing, missing
assert len(t.splitlines())>=150, len(t.splitlines())
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/guide/benchmarking.md` exists and is >= 150 lines.
    - It contains every one of these literal strings: `schema_version`, `environment`, `solver_config`, `problem_shape`, `stages`, `memory`, `accuracy`, `git_sha_source`, `max_nfev_source`, `fd_reduction`, `n_residuals`, `whole_run_peak_bytes`, `psutil_peak_wset`, `validation_3d_error_mean`.
    - It contains all eight trace column names: `iteration`, `n_fev`, `cost`, `step_norm`, `optimality`, `water_z`, `tilt_rx`, `tilt_ry`.
    - It contains all nine conditioning JSON keys: `condition_number`, `singular_values`, `rank`, `rank_tolerance`, `n_params`, `n_residuals`, `parameter_names`, `correlation_npz`, `stage`; and the NPZ array name `correlation`.
    - `grep -c 'MF-12' docs/guide/benchmarking.md` returns >= 1 and the surrounding text contains the word `hypothesis`.
    - `grep -ci 'stage 4\|four stages\|four calibration stages' docs/guide/benchmarking.md` returns 0.
    - At least three fenced blocks tagged ```` ```json ```` or ```` ```csv ```` are present, and each contains a value that also appears in `experiments/results/benchmark.json` or `experiments/results/e7_trace_shared_fixed.csv`.
  </acceptance_criteria>
  <done>The page documents benchmark.json field-by-field, all eight trace columns with interpretation, and both conditioning payloads with the correlation-matrix indexing rule.</done>
</task>

<task type="auto">
  <name>Task 2: Forward links from configuration.md, D-04 verification pass</name>
  <files>docs/guide/configuration.md, docs/guide/refractive_geometry.md</files>
  <read_first>
    - `docs/guide/configuration.md` lines 90-120 (the `shared_interface` row and admonition) and 232-252 (the whole `internals` section)
    - `docs/guide/refractive_geometry.md` lines 105-130 (the per-camera `water_z` ablation admonition)
    - `docs/guide/benchmarking.md` (as written in Task 1 — link targets must resolve)
    - `src/aquacal/config/schema.py` (confirm the current default of `shared_interface` is still `True`)
  </read_first>
  <action>
**configuration.md — additive edits only.** In the `## internals` table (currently
`configuration.md:241-245`), append to the `Meaning` cell of the `save_optimization_trace` row:
` — see [Benchmarking & Diagnostics](benchmarking.md) for the CSV column schema` and to the
`save_conditioning` row: ` — see [Benchmarking & Diagnostics](benchmarking.md) for the JSON/NPZ
schema`. Do NOT delete either row (D-03: they stay as the config-flag reference).
Add `- [Benchmarking & Diagnostics](benchmarking.md) — `benchmark.json`, trace CSV, and
conditioning output schemas` to the `## See Also` list at the end of the file.

**D-04 verification, not rewrite.** Read the `shared_interface` row (`configuration.md:97`) and
the admonition (`configuration.md:109-118`), and the ablation admonition in
`refractive_geometry.md:117-127`. Confirm against `src/aquacal/config/schema.py` that (a) the
default is still `True`, (b) the flag is still spelled `interface.shared_interface`, and (c)
the "not a recommended production setting" framing still matches behaviour. **If all three hold,
change nothing in those passages** and record "D-04 verified, no edit required" in the SUMMARY
with the three checks and their observed values. Make an edit ONLY if a stated fact is now
false; if you edit, quote the old and new text in the SUMMARY.

One permitted addition to `refractive_geometry.md:117-127`: its last sentence reads "A full
worked example is deferred to a later documentation pass." Replace only that sentence with a
pointer to the conditioning section of the new page:
`See [Benchmarking & Diagnostics](benchmarking.md) for the conditioning diagnostics used to
quantify how tightly the shared value is constrained.` This is the only content change this
task may make to that file.
  </action>
  <verify>
    <automated>python -c "
c=open('docs/guide/configuration.md',encoding='utf-8').read()
r=open('docs/guide/refractive_geometry.md',encoding='utf-8').read()
assert c.count('benchmarking.md')>=3, c.count('benchmarking.md')
assert 'save_optimization_trace' in c and 'save_conditioning' in c
assert 'shared_interface' in c
assert r.count('benchmarking.md')==1, r.count('benchmarking.md')
assert 'deferred to a later documentation pass' not in r
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'benchmarking.md' docs/guide/configuration.md` returns >= 3 (two internals rows + See Also).
    - `grep -c 'save_optimization_trace' docs/guide/configuration.md` and `grep -c 'save_conditioning' docs/guide/configuration.md` are each >= 2 (table row + YAML example still present — the rows were not deleted).
    - `grep -c 'deferred to a later documentation pass' docs/guide/refractive_geometry.md` returns 0, and `grep -c 'benchmarking.md' docs/guide/refractive_geometry.md` returns 1.
    - `git diff --stat docs/guide/refractive_geometry.md` shows at most 2 changed lines.
    - The SUMMARY records the D-04 verification with the three observed values (default of `shared_interface`, the config key path, and whether the framing still matches).
  </acceptance_criteria>
  <done>Both internals rows and the See Also list link forward to benchmarking.md; the D-04 passages are verified and recorded, with at most a one-sentence pointer change.</done>
</task>

<task type="auto">
  <name>Task 3: Register benchmarking.md in the guide nav and build the docs</name>
  <files>docs/guide/index.md</files>
  <read_first>
    - `docs/guide/index.md` (full file — the "Practical Guides" bullet list and the hidden toctree)
    - `.github/workflows/docs.yml` line 30 (the exact build command CI uses)
  </read_first>
  <action>
In `docs/guide/index.md`, add to the `## Practical Guides` bullet list, after the
`Configuration Reference` bullet:
`- [Benchmarking & Diagnostics](benchmarking.md) — `benchmark.json` schema, per-iteration
optimizer trace columns, and conditioning report contents`
and add the bare stem `benchmarking` to the hidden `:::{toctree}` block, immediately after
`configuration`.

Then build the docs exactly as CI does, from the repo root, in the AquaCal conda env:
`sphinx-build -W --keep-going -b html docs docs/_build/html`
`-W` turns warnings into errors, so an unresolved link or an orphaned page fails the build. Fix
any error this surfaces in the files this plan owns. If the build surfaces a pre-existing error
in a file this plan does not own, record it in the SUMMARY and do not fix it here.

Do NOT commit `docs/_build/`.
  </action>
  <verify>
    <automated>sphinx-build -W --keep-going -b html docs docs/_build/html</automated>
  </verify>
  <acceptance_criteria>
    - `sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
    - `docs/_build/html/guide/benchmarking.html` exists.
    - `grep -c '^benchmarking$' docs/guide/index.md` returns 1 (the toctree stem).
    - `grep -c 'benchmarking.md' docs/guide/index.md` returns 1 (the bullet link).
    - `git status --porcelain docs/_build` produces no output (build artifacts are not staged).
  </acceptance_criteria>
  <done>The page is in the guide nav and the toctree, and the `-W` docs build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> published docs site | Content written here is published by `.github/workflows/docs.yml` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Information disclosure | `benchmark.json` excerpts pasted into public docs | mitigate | Excerpt only from `experiments/results/benchmark.json`, which is already committed and public; do not paste absolute local paths, machine names, or any `paths:` block into the page |
| T-21-02-02 | Repudiation | undocumented diagnostic output | mitigate | This plan is the mitigation — the provenance fields (`git_sha`, `git_sha_source`, `seed`) are documented as the provenance pair a reader must check |
| T-21-02-03 | Tampering | docs build | accept | `sphinx-build -W` is run locally and in CI; no new extension or dependency is introduced |
| T-21-02-SC | Tampering | package installs | mitigate | This plan installs nothing; `sphinx`, `myst-parser`, `nbsphinx` are already pinned dev dependencies |
</threat_model>

<verification>
- `sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
- `docs/guide/benchmarking.md` contains every field name listed in the acceptance criteria.
- `git diff --stat docs/guide/refractive_geometry.md` shows at most 2 changed lines.
</verification>

<success_criteria>
`benchmark.json`, the trace CSV, and the conditioning JSON/NPZ are documented field-by-field on
one discoverable guide page; `configuration.md`'s two internals rows link forward to it; the
`shared_interface` documentation is verified current per D-04; the `-W` docs build is green.
</success_criteria>

<output>
Create `.planning/phases/21-new-feature-documentation-dataset-refresh/21-02-SUMMARY.md` when done.
</output>
