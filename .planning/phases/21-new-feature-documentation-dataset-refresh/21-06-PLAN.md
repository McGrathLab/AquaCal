---
phase: 21-new-feature-documentation-dataset-refresh
plan: 06
type: execute
wave: 2
depends_on: ["21-01"]
files_modified: []
autonomous: false
requirements: [DATA-01a]

must_haves:
  truths:
    - "Every 30th frame of all 13 extrinsic AVIs exists on disk as lossless PNG, 262 per camera (D-07, D-09)."
    - "Every 30th frame of all 13 intrinsic AVIs exists on disk as lossless PNG (D-08)."
    - "The extraction ran under the AquaCal conda env, so no camera silently produced zero frames."
    - "The staged tree lives OUTSIDE the repository and nothing from it is staged for commit."
  artifacts:
    - path: "C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/extrinsic"
      provides: "13 camera dirs x 262 lossless PNG frames"
    - path: "C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/intrinsic"
      provides: "13 camera dirs of lossless PNG frames (ragged counts by design)"
  key_links:
    - from: "scripts/extract_frames.py"
      to: "archive_staging/real-rig/{extrinsic,intrinsic}"
      via: "two invocations at --step 30"
      pattern: "extract_frames"
---

<objective>
Produce the lossless PNG frameset the regenerated Zenodo archive ships: ~4.4 GB extracted from
12 GB of raw AVI (D-07, D-08, D-09).

Purpose: DATA-01a is the phase's PUBLICATION BLOCKER. The published archive currently holds a
~4.3x subsampled 60-frame extraction that yields 1,817 comparisons where §3 reports 7,762. This
extraction is what makes the archive and the manuscript describe the same run.

Output: a staged frame tree outside the repository. No repository file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-FRAMESET-PROVENANCE.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run the extrinsic extraction (LONG — orchestrator/user only)</name>
  <files>none (writes outside the repo)</files>
  <read_first>
    - `scripts/extract_frames.py` (as delivered by plan 21-01 — read its `--help` output and its guards before launching)
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-01-SUMMARY.md`
    - `CLAUDE.md` § "Never let a subagent background a long run and return" and § "Always run calibration unbuffered"
  </read_first>
  <action>
**CHECKPOINT — NOT AUTONOMOUS.** The orchestrator or the user performs this task by running the
commands in `<how-to-verify>` below, in order, exactly as written. Do NOT execute this task from
inside an executor subagent: per CLAUDE.md, an executor that backgrounds a long run stalls
permanently and never returns. Record every command, its exit code and its wall-clock runtime in
the SUMMARY, then confirm each item in `<acceptance_criteria>` before signalling resume.
  </action>
  <what-built>
`scripts/extract_frames.py` (plan 21-01), a deterministic every-Nth-frame AVI -> lossless-PNG
extractor with zero-frame and ragged-count guards.
  </what-built>
  <how-to-verify>
**DO NOT RUN THIS FROM INSIDE AN EXECUTOR SUBAGENT.** This writes ~4.0 GB from 9.9 GB of source
video and will exceed any subagent's tool-call ceiling. Per CLAUDE.md, a subagent that
backgrounds it stalls permanently. The orchestrator or the user runs it directly.

**It must run in the AquaCal conda env.** Git Bash's `python` is Anaconda base and
`VideoCapture.read()` returns `None` on these AVIs — the result would be 13 empty directories.
Confirm the interpreter first:
```
python -c "import cv2,sys; print(sys.executable); print(cv2.__version__)"
```
`sys.executable` must be the AquaCal env, not Anaconda base.

**Smoke first** (under a minute — this is the cheap proof the decoder works):
```
python -u scripts/extract_frames.py \
  --video-dir "C:/Users/tucke/Desktop/Aqua/AquaCal/raw_videos/extrinsics" \
  --out-dir   "C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging_smoke/extrinsic" \
  --step 30 --limit 3
```
Expect exit 0 and 13 directories x 3 PNGs. If any camera reports 0 frames, STOP — you are in the
wrong environment. Delete `archive_staging_smoke/` before the production run.

**Production run**, detached and unbuffered per CLAUDE.md's long-run rules:
```
nohup python -u scripts/extract_frames.py \
  --video-dir "C:/Users/tucke/Desktop/Aqua/AquaCal/raw_videos/extrinsics" \
  --out-dir   "C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/extrinsic" \
  --step 30 > /c/Users/tucke/Desktop/Aqua/AquaCal/extract_extrinsic.log 2>&1 &
disown
```
Poll with `tail -20 .../extract_extrinsic.log`. A quiet log is not a dead run — check the
process listing before relaunching.

**Do not commit anything while this runs.** Nothing here records a git sha, but the same
discipline applies to the §3 run in plan 21-08 and it costs nothing to hold here.

On completion, verify:
```
for d in "C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/extrinsic"/*; do echo "$d $(ls "$d" | wc -l)"; done
du -sh "C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/extrinsic"
```
Expect **13 directories, exactly 262 files each** (7,860 frames / 30 = 262, D-09), total ~4.0 GB,
first file `frame0000.png`, last `frame0261.png`.
  </how-to-verify>
  <acceptance_criteria>
    - The extractor exited 0 (`echo $?` immediately after, or the log's final line is the `TOTAL:` summary).
    - Exactly 13 camera directories exist under `archive_staging/real-rig/extrinsic/`.
    - Every directory contains exactly 262 files, all with a `.png` extension, named `frame0000.png` through `frame0261.png` with no gaps.
    - Total size of `archive_staging/real-rig/extrinsic/` is between 3.0 GB and 5.5 GB (a figure far below this band means JPEG-quality output or a partial run).
    - A spot-checked PNG reads back as a 1200x1600 3-channel `uint8` array: `python -c "import cv2;a=cv2.imread('.../extrinsic/e3v829d/frame0000.png');print(a.shape,a.dtype)"` prints `(1600, 1200, 3) uint8` or `(1200, 1600, 3) uint8`.
    - `git status --porcelain` in the repo shows no new untracked files (the staging tree is outside the repo).
  </acceptance_criteria>
  <verify>
    <automated>python -c "
import glob,os
root=r'C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/extrinsic'
dirs=sorted(glob.glob(root+'/*'))
assert len(dirs)==13, len(dirs)
counts={len(glob.glob(d+'/*.png')) for d in dirs}
assert counts=={262}, counts
total=sum(os.path.getsize(f) for d in dirs for f in glob.glob(d+'/*.png'))
assert 3e9 < total < 5.5e9, total
print('ok', total)"</automated>
  </verify>
  <done>13 extrinsic camera directories hold exactly 262 lossless PNGs each, totalling 3.0-5.5 GB, and the repo is untouched.</done>
  <resume-signal>Type "extrinsic ok" with the observed per-camera frame count and total size, or paste the failure.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run the intrinsic extraction (LONG — orchestrator/user only)</name>
  <files>none (writes outside the repo)</files>
  <read_first>
    - The Task 1 log and its verified output
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/config.yaml` (`detection.frame_step: 30` is a single global setting applied to the intrinsic path too — `src/aquacal/calibration/pipeline.py:408,860,896`)
  </read_first>
  <action>
**CHECKPOINT — NOT AUTONOMOUS.** The orchestrator or the user performs this task by running the
commands in `<how-to-verify>` below, in order, exactly as written. Do NOT execute this task from
inside an executor subagent: per CLAUDE.md, an executor that backgrounds a long run stalls
permanently and never returns. Record every command, its exit code and its wall-clock runtime in
the SUMMARY, then confirm each item in `<acceptance_criteria>` before signalling resume.
  </action>
  <what-built>The same extractor, applied to the intrinsic video set.</what-built>
  <how-to-verify>
**DO NOT RUN THIS FROM INSIDE AN EXECUTOR SUBAGENT.** Same reason as Task 1 (1.7 GB source,
~350 MB output).

D-08: the intrinsic set is re-extracted as PNG too. The gate response's "`intrinsic/` unchanged"
refers to *frame count* — the 60 -> 262 expansion applies only to `extrinsic/` — not to encoding.
Leaving `intrinsic/` as today's JPEG would reintroduce exactly the re-encode variable D-07 was
chosen to remove, and intrinsics feed every downstream stage.

The intrinsic videos have **different lengths per camera**, so the per-camera counts will differ.
This is expected and requires `--allow-ragged`.

```
nohup python -u scripts/extract_frames.py \
  --video-dir "C:/Users/tucke/Desktop/Aqua/AquaCal/raw_videos/intrinsics" \
  --out-dir   "C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/intrinsic" \
  --step 30 --allow-ragged > /c/Users/tucke/Desktop/Aqua/AquaCal/extract_intrinsic.log 2>&1 &
disown
```

On completion:
```
for d in "C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/intrinsic"/*; do echo "$d $(ls "$d" | wc -l)"; done
du -sh "C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/intrinsic"
```
Expect 13 directories, each with a **non-zero** count, ragged by design, total ~250-450 MB.

**Note `--step 30` is deliberate and must not be changed.** The release run applied
`detection.frame_step: 30` globally, so every 30th intrinsic frame is exactly the set that
produced §3's intrinsics. A different step here silently changes the calibration and would
surface only as a D-15 gate 1 failure fifty minutes later.
  </how-to-verify>
  <acceptance_criteria>
    - The extractor exited 0 and its log's final line is the `TOTAL:` summary.
    - Exactly 13 camera directories exist under `archive_staging/real-rig/intrinsic/`, each with at least 1 file and all files `.png`.
    - No camera directory is empty (the zero-frame guard would have exited 1, but re-assert it by listing).
    - Total size of `archive_staging/real-rig/intrinsic/` is between 150 MB and 700 MB.
    - `archive_staging/real-rig/` now contains exactly the two subdirectories `extrinsic/` and `intrinsic/` and nothing else.
    - The SUMMARY records both commands verbatim, both wall-clock runtimes, the per-camera counts for both sets, and the interpreter path confirmed before launch.
  </acceptance_criteria>
  <verify>
    <automated>python -c "
import glob,os
root=r'C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/intrinsic'
dirs=sorted(glob.glob(root+'/*'))
assert len(dirs)==13, len(dirs)
counts=[len(glob.glob(d+'/*.png')) for d in dirs]
assert min(counts)>0, counts
total=sum(os.path.getsize(f) for d in dirs for f in glob.glob(d+'/*.png'))
assert 1.5e8 < total < 7e8, total
print('ok', counts, total)"</automated>
  </verify>
  <done>13 intrinsic camera directories are non-empty lossless PNG sets totalling 150-700 MB, extracted at the same --step 30 as the release run.</done>
  <resume-signal>Type "intrinsic ok" with the per-camera counts and total size, or paste the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw research video -> staged archive content | These bytes become a publicly published 4.4 GB artifact under a permanent DOI |
| local environment -> extraction correctness | The wrong Python interpreter silently produces empty output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06-01 | Information disclosure | frames published publicly under a permanent DOI | mitigate | Frames are ChArUco board captures in a tank; before the publish checkpoint in plan 21-09 the user spot-checks a sample for anything unintended in frame. A minted DOI cannot be withdrawn, so this check is pre-publish, not post |
| T-21-06-02 | Tampering | silent empty or partial extraction | mitigate | Interpreter is asserted before launch; the extractor's zero-frame guard exits 1; acceptance requires exactly 262 files per extrinsic camera and a total size inside a stated band, so a JPEG-sized or truncated run fails the gate |
| T-21-06-03 | Denial of service | executor stalling on a multi-hour run | mitigate | Task is `checkpoint:human-action`, `autonomous: false`, with an explicit "do not run from inside an executor" banner and `nohup … & disown` per CLAUDE.md |
| T-21-06-04 | Tampering | staged bytes accidentally committed | mitigate | Staging root is outside the repository; acceptance requires `git status --porcelain` to be clean |
| T-21-06-SC | Tampering | package installs | mitigate | Nothing is installed; the run uses the existing AquaCal env |
</threat_model>

<verification>
- 13 extrinsic directories x exactly 262 `.png` files, total 3.0-5.5 GB.
- 13 intrinsic directories, all non-empty, total 150-700 MB.
- `git status --porcelain` clean in the repo.
</verification>

<success_criteria>
`archive_staging/real-rig/` holds the complete lossless PNG frameset — 262 extrinsic frames per
camera at `--step 30`, plus the every-30th intrinsic frames — produced under the AquaCal conda
env, verified by count and size, and entirely outside the repository.
</success_criteria>

<output>
Create `.planning/phases/21-new-feature-documentation-dataset-refresh/21-06-SUMMARY.md` when done.
</output>
