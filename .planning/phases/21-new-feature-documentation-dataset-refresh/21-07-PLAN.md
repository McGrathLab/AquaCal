---
phase: 21-new-feature-documentation-dataset-refresh
plan: 07
type: execute
wave: 3
depends_on: ["21-06"]
files_modified:
  - .planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md
autonomous: false
requirements: [DATA-01, DATA-01b, DATA-02]

must_haves:
  truths:
    - "One archive holds both configs, the full frameset, and the run's own reference outputs (D-10)."
    - "The fast config's filename itself says its numbers do not reproduce the paper (D-12)."
    - "Both configs load and validate under v2.0.0 `load_config()` semantics — D-15 gate 4."
    - "The zip extracts to the nested layout `loader.py:60` resolves — D-15 gate 2."
    - "The three manifest values (record id excepted) are computed and written down BEFORE any upload."
    - "The multi-GB zip build and scratch extraction ran outside any executor subagent."
  artifacts:
    - path: ".planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md"
      provides: "Pre-computed md5, size_bytes, zip name, layout listing and the gate-2/gate-4 evidence"
      contains: "size_bytes"
  key_links:
    - from: "real-rig-calib.zip"
      to: "src/aquacal/datasets/loader.py:60"
      via: "nested top-level real-rig/ folder"
      pattern: "real-rig/"
    - from: "config_paper.yaml"
      to: "release_calibration/config.yaml"
      via: "identical optimization/detection/validation settings, image-directory paths"
      pattern: "frame_step"
---

<objective>
Assemble the single ~4.4 GB archive (D-10) and clear two of D-15's four pre-publish gates.

Purpose: a minted DOI cannot be withdrawn, so everything checkable before publication is checked
before publication. Gates 2 (checksum, size, extraction layout) and 4 (both configs load and
validate under v2.0.0) land here; gates 1 and 3 — the ~50 minute §3 reproduction and the CLI
tutorial's commands — are plan 21-08.

Output: `real-rig-calib.zip` staged on disk, plus a committed manifest document holding the
pre-computed values the human transcribes into Zenodo in plan 21-09.

**This plan is `autonomous: false`.** Tasks 2 and 4 move multiple gigabytes — a ~4.4 GB zip build
and a ~4.4 GB extraction/deletion — and would exceed an executor subagent's tool-call ceiling.
Per CLAUDE.md § "Never let a subagent background a long run and return", a subagent that
backgrounds such a run stalls permanently: the auto-background notification can never arrive
because the subagent's turn ending *is* its completion. Those tasks are `checkpoint:human-action`
and are performed by the orchestrator or the user, exactly as in plans 21-06, 21-08 and 21-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-GATE-RESPONSE.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-FRAMESET-PROVENANCE.md
@CLAUDE.md
</context>

<interfaces>
**Target layout — fixed by this phase, not the executor's choice.** Plan 21-03's CLI tutorial is
already written against it:

```
real-rig-calib.zip
└── real-rig/
    ├── README.md
    ├── config_paper.yaml
    ├── config_quickstart_not_paper.yaml
    ├── reference_calibration.json
    ├── extrinsic/<camera>/frame0000.png … frame0261.png
    ├── intrinsic/<camera>/frame0000.png …
    └── reference_outputs/
        ├── calibration.json
        ├── diagnostics.json
        ├── reprojection_residuals.csv
        ├── reconstruction_errors.csv
        ├── exp2_spatial_errors.csv
        └── interface_ablation_conditioning.npz
```

`src/aquacal/datasets/loader.py:56-60` resolves `cache_path / name` when it exists, so the single
top-level `real-rig/` folder inside the zip is required, not optional.

**Settings `config_paper.yaml` must carry**, from
`C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/config.yaml`:
`refine_intrinsics: true`, `refine_auxiliary_intrinsics: true`, `max_calibration_frames: 200`,
`holdout_fraction: 0.2`, `n_water: 1.333`, `n_air: 1.0`, `normal_fixed: false`,
`robust_loss: huber`, `loss_scale: 1.0`, `min_corners: 8`, `min_cameras: 2`,
`save_detailed_residuals: true`, the same 12 `cameras` in the same order, `auxiliary_cameras:
[e3v8250]`, `fisheye_cameras: [e3v8250]`, and both board blocks verbatim.
**`frame_step: 1`** — the frames are already subsampled at 30 on disk (D-09), so 1 over the image
directories is equivalent to 30 over the video (`ImageSet.iterate_frames` honours `step`;
files are `natsort`ed).

**Scratch extraction path used by Tasks 2, 3 and 4** (one value, referred to as `<scratch>`):
`C:/Users/tucke/Desktop/Aqua/AquaCal/archive_scratch`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the two archive configs and the archive README</name>
  <files>C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/config_paper.yaml, C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/config_quickstart_not_paper.yaml, C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/README.md</files>
  <read_first>
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/config.yaml` — the config that produced §3; every optimization/detection/validation/board/camera setting is copied from it
    - `docs/tutorials/aquacal_data/real-rig/real-rig/config.yaml` — the currently published archive config, for the relative image-directory path form (`extrinsic/<camera>/`) and for the camera-order difference to check
    - `src/aquacal/config/schema.py` (`CalibrationConfig` fields and validation; confirm the current spelling of every key before writing)
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md` D-12 (the fast config is repurposed, not dropped, and its name must signal it does not reproduce §3)
  </read_first>
  <action>
**`config_paper.yaml`** — copy `release_calibration/config.yaml` and change only what must change:
  - `paths.intrinsic_videos` / `paths.extrinsic_videos`: replace every absolute `.avi` path with
    the relative image directory `intrinsic/<camera>/` and `extrinsic/<camera>/`, matching the
    published archive's form. All 13 cameras in each block.
  - `paths.output_dir: output`.
  - `detection.frame_step: 1` (the frames on disk are already every-30th — D-09).
  - `interface.initial_water_z`: keep the per-camera mapping form, all 13 cameras at `1.0`,
    exactly as the release config has it. **Do not emit `initial_distances`** — that deprecated
    key is what the folded 2026-02-24 todo exists to remove.
  - **`cameras` list order must match `release_calibration/config.yaml` exactly** — the first
    camera is the reference camera and defines the world origin. Note the currently published
    archive config has `e3v82f9` and `e3v82e0` transposed relative to the release config; use the
    **release config's** order. Record this in the SUMMARY.
Add a header comment block: what this config reproduces (§3 of the AquaCal paper), the expected
runtime (~50 min) and peak memory (~11 GiB), and that it must have the machine to itself.

**`config_quickstart_not_paper.yaml`** — the same file with `detection.frame_step: 5` and
`optimization.max_calibration_frames: 150`. Header comment, first line, in capitals:
`# QUICKSTART CONFIG — the numbers this produces do NOT reproduce the paper's Section 3.`
followed by: use `config_paper.yaml` for reproduction; this exists so a reader is not forced into
a ~50 minute solve on first contact. The filename carries the same warning, which is D-12's
requirement.

**`README.md`** — a short archive-level readme: what the dataset is (13-camera underwater rig,
ChArUco, flat air/water interface), the directory layout with one line per entry, the two configs
and which one reproduces the paper, a pointer to the CLI tutorial in the AquaCal docs, the
citation line for the paper, and the license. Do **not** state a DOI or record id — plan 21-09
mints it. State that `reference_outputs/` holds the paper run's own outputs so a reader can diff
their result directly.

**Do NOT build the zip here and do not extract anything.** The multi-GB steps are Tasks 2 and 4
and are `checkpoint:human-action`; running them from inside an executor subagent stalls it
permanently (CLAUDE.md).
  </action>
  <verify>
    <automated>python -c "
import yaml,sys
base='C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/'
rel=yaml.safe_load(open('C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/config.yaml'))
p=yaml.safe_load(open(base+'config_paper.yaml'))
q=yaml.safe_load(open(base+'config_quickstart_not_paper.yaml'))
assert p['detection']['frame_step']==1
assert q['detection']['frame_step']==5
assert p['cameras']==rel['cameras'], (p['cameras'],rel['cameras'])
assert p['optimization']['max_calibration_frames']==200
assert p['optimization']['refine_intrinsics'] is True
assert p['optimization']['refine_auxiliary_intrinsics'] is True
assert p['optimization']['robust_loss']=='huber' and p['optimization']['loss_scale']==1.0
assert p['validation']['holdout_fraction']==0.2
assert p['interface']['n_water']==1.333 and p['interface']['normal_fixed'] is False
assert 'initial_distances' not in p['interface'] and 'initial_distances' not in q['interface']
assert set(p['paths']['extrinsic_videos'].values())=={'extrinsic/%s/'%c for c in list(p['paths']['extrinsic_videos'])}
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Both configs exist in `archive_staging/real-rig/` and parse as YAML.
    - `config_paper.yaml`: `detection.frame_step == 1`, `optimization.max_calibration_frames == 200`, `refine_intrinsics` and `refine_auxiliary_intrinsics` both `true`, `robust_loss == "huber"`, `loss_scale == 1.0`, `validation.holdout_fraction == 0.2`, `interface.n_water == 1.333`, `interface.normal_fixed == false`.
    - `config_paper.yaml`'s `cameras` list equals `release_calibration/config.yaml`'s `cameras` list element-for-element, in order.
    - `interface.initial_water_z` is a 13-entry mapping with every value `1.0`; the literal string `initial_distances` appears in neither config (`grep -c initial_distances` returns 0 for both).
    - All 26 `paths.*_videos` values are relative directories of the form `extrinsic/<camera>/` or `intrinsic/<camera>/`; no value contains `.avi` or `C:`.
    - `config_quickstart_not_paper.yaml`'s first line contains the literal `do NOT reproduce` (case as written), and its `detection.frame_step == 5`.
    - `README.md` exists, is >= 30 lines, names both configs, and contains no `10.5281` or `zenodo.org/record` literal.
    - No zip was built and no archive was extracted by this task.
  </acceptance_criteria>
  <done>Two configs and a README sit in the staging tree; the paper config matches the release config on every setting that affects the solve.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Place the reference outputs, build the zip, and extract it for gating (LONG — orchestrator/user only)</name>
  <files>none (writes outside the repo)</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` DATA-01b (the exact five files that move into the archive)
    - `experiments/results/` directory listing (`calibration.json`, `reprojection_residuals.csv`, `reconstruction_errors.csv` are tracked; `exp2_spatial_errors.csv` and `interface_ablation_conditioning.npz` are already gitignored — confirm with `git ls-files experiments/results`)
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/diagnostics.json` (the §3 source, which the archive must carry so a reader can diff against it)
    - `src/aquacal/datasets/loader.py` lines 33-63 (the nested-path resolution and the `reference_calibration.json` lookup)
    - `CLAUDE.md` § "Never let a subagent background a long run and return" and § "Always run calibration unbuffered"
  </read_first>
  <action>
**CHECKPOINT — NOT AUTONOMOUS.** The orchestrator or the user performs this task by running the
commands in `<how-to-verify>` below, in order, exactly as written. Do NOT execute this task from
inside an executor subagent: per CLAUDE.md, an executor that backgrounds a long run stalls
permanently and never returns. Record every command, its exit code and its wall-clock runtime in
the SUMMARY, then confirm each item in `<acceptance_criteria>` before signalling resume.
  </action>
  <what-built>
`archive_staging/real-rig/` — the two configs and the README from Task 1, on top of the complete
lossless PNG frameset staged by plan 21-06.
  </what-built>
  <how-to-verify>
**DO NOT RUN THIS FROM INSIDE AN EXECUTOR SUBAGENT.** This writes a ~4.4 GB zip and then extracts
another ~4.4 GB copy. Both exceed any subagent's tool-call ceiling; per CLAUDE.md, a subagent that
backgrounds them stalls permanently. The orchestrator or the user runs them directly.

Run in the AquaCal conda env.

**1. Copy the reference outputs (fast — seconds).** Create
`archive_staging/real-rig/reference_outputs/` and **copy** (do not move — plan 21-11 owns the
repo-side deletion, and it must not happen until after the archive is published) these six files
into it:
  - `experiments/results/calibration.json`
  - `experiments/results/reprojection_residuals.csv`
  - `experiments/results/reconstruction_errors.csv`
  - `experiments/results/exp2_spatial_errors.csv`
  - `experiments/results/interface_ablation_conditioning.npz`
  - `C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/diagnostics.json`

Also copy `experiments/results/calibration.json` to
`archive_staging/real-rig/reference_calibration.json` — `load_example` looks for that exact
filename at the archive root (`loader.py`) and the currently published archive has it.

Record each source file's md5 and byte size in the SUMMARY so a later question about which
`calibration.json` shipped has an answer.

**2. Build the zip — detached and unbuffered per CLAUDE.md's long-run rules.** Zipping ~4.4 GB of
PNG produces essentially no compression (PNG is already deflated), but it is minutes of solid
I/O:
```
nohup python -u -c "import shutil; shutil.make_archive('C:/Users/tucke/Desktop/Aqua/AquaCal/real-rig-calib', 'zip', root_dir='C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging', base_dir='real-rig')" \
  > /c/Users/tucke/Desktop/Aqua/AquaCal/zip_build.log 2>&1 &
disown
```
Poll with `tail -5 /c/Users/tucke/Desktop/Aqua/AquaCal/zip_build.log` and by watching the zip's
size grow. A quiet log is not a dead run — check the process listing before relaunching.

Keep the filename `real-rig-calib.zip` — `manifest.json`'s `zenodo_filename` is already that
value, and not changing it removes a field from plan 21-10's edit.

**3. Compute the two pre-publish manifest values from the final zip bytes and nothing else:**
```
python -u -c "
import hashlib,os
p='C:/Users/tucke/Desktop/Aqua/AquaCal/real-rig-calib.zip'
h=hashlib.md5()
with open(p,'rb') as f:
    for b in iter(lambda: f.read(1<<20), b''): h.update(b)
print('md5', h.hexdigest()); print('size_bytes', os.path.getsize(p))"
```
Paste both values verbatim into the resume signal — Task 3 transcribes them into the manifest
document and plan 21-09's human step transcribes them into Zenodo.

**4. Extract the zip to the scratch directory — detached, same reason as step 2:**
```
nohup python -u -c "import zipfile; zipfile.ZipFile(r'C:/Users/tucke/Desktop/Aqua/AquaCal/real-rig-calib.zip').extractall(r'C:/Users/tucke/Desktop/Aqua/AquaCal/archive_scratch')" \
  > /c/Users/tucke/Desktop/Aqua/AquaCal/zip_extract.log 2>&1 &
disown
```
**Leave the scratch tree in place.** Task 3 runs the D-15 gate-2 and gate-4 assertions against it;
Task 4 deletes it.

**5. Confirm the zip's shape** (fast, reads the central directory only):
```
python -c "
import zipfile,os
z='C:/Users/tucke/Desktop/Aqua/AquaCal/real-rig-calib.zip'
zf=zipfile.ZipFile(z); names=zf.namelist()
print(sorted({n.split('/')[0] for n in names}))
print('entries', len(names), 'bytes', os.path.getsize(z))
print('extrinsic pngs', len([n for n in names if n.startswith('real-rig/extrinsic/') and n.endswith('.png')]))"
```

**Do not commit anything while this runs.**
  </how-to-verify>
  <acceptance_criteria>
    - `real-rig-calib.zip` exists and its namelist has exactly one top-level entry, `real-rig`.
    - The zip contains all six `real-rig/reference_outputs/*` files plus `real-rig/reference_calibration.json`, `real-rig/README.md` and both configs.
    - The zip contains exactly `262 * 13 == 3406` entries under `real-rig/extrinsic/` ending in `.png`, and at least 13 under `real-rig/intrinsic/`.
    - The zip's size is between 3.5 GB and 5.5 GB.
    - The md5 (32 hex chars) and the exact `size_bytes` integer are in the resume signal and in the SUMMARY, computed from the final zip and nothing else.
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/archive_scratch/real-rig/config_paper.yaml` exists after extraction, and the scratch tree is left in place for Task 3.
    - `git status --porcelain` in the repo is clean — nothing was moved out of `experiments/results/` by this task.
    - The SUMMARY lists the md5 and byte size of each of the six copied reference-output files, plus both wall-clock runtimes and the interpreter path confirmed before launch.
  </acceptance_criteria>
  <verify>
    <automated>python -c "
import zipfile,os
z='C:/Users/tucke/Desktop/Aqua/AquaCal/real-rig-calib.zip'
assert os.path.exists(z)
zf=zipfile.ZipFile(z)
names=zf.namelist()
tops={n.split('/')[0] for n in names}
assert tops=={'real-rig'}, tops
for req in ['real-rig/config_paper.yaml','real-rig/config_quickstart_not_paper.yaml','real-rig/README.md','real-rig/reference_calibration.json','real-rig/reference_outputs/diagnostics.json','real-rig/reference_outputs/calibration.json']:
    assert req in names, req
pngs=[n for n in names if n.endswith('.png')]
ext=[n for n in pngs if n.startswith('real-rig/extrinsic/')]
assert len(ext)==262*13, len(ext)
assert 3.5e9 < os.path.getsize(z) < 5.5e9, os.path.getsize(z)
assert os.path.exists('C:/Users/tucke/Desktop/Aqua/AquaCal/archive_scratch/real-rig/config_paper.yaml')
print('ok', len(names),'entries', os.path.getsize(z),'bytes')"</automated>
  </verify>
  <done>The zip exists with the required nested layout, the full frameset and the six reference outputs; its md5 and size are recorded; the scratch extraction is in place for Task 3; the repo is untouched.</done>
  <resume-signal>Type "zip ok" with the md5, the exact size_bytes, the entry count and both wall-clock runtimes, or paste the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: D-15 gates 2 and 4, and write the archive manifest document</name>
  <files>.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md</files>
  <read_first>
    - Task 2's resume signal and log — the md5, the `size_bytes`, and the confirmation that `<scratch>/real-rig/` exists
    - `src/aquacal/datasets/loader.py` lines 33-63 and `src/aquacal/datasets/download.py` lines 38-98 and 151-171 (what `checksum` format the downloader expects — note the `md5:` prefix in the current manifest — and how the extraction path is resolved)
    - `src/aquacal/datasets/data/manifest.json` (the three fields plan 21-10 will change and their current values)
    - `src/aquacal/config/__init__.py` / `src/aquacal/config/loader.py` (the exact `load_config` import path and signature)
  </read_first>
  <action>
Every command in this task is seconds-scale: it reads the already-extracted scratch tree and never
touches the zip. **Do not rebuild the zip, do not re-extract it, and do not delete the scratch
tree** — the multi-GB steps are Tasks 2 and 4 and are checkpoints for the reason in CLAUDE.md.

**Run in the AquaCal conda env.** Git Bash's `python` is Anaconda base; confirm first:
`python -c "import sys; print(sys.executable)"`.

`<scratch>` below is `C:/Users/tucke/Desktop/Aqua/AquaCal/archive_scratch`.

**Gate 2 — checksum, size, extraction path.** Take the md5 and `size_bytes` verbatim from Task 2's
resume signal; they are two of the three values the human transcribes in plan 21-09. Then prove
the extraction layout by asserting `<scratch>/real-rig/config_paper.yaml` exists — this is exactly
the nesting `loader.py`'s `if (_cache_path / name).exists()` branch resolves — and record the
top-level listing of `<scratch>/real-rig/` with per-entry sizes.

**Gate 4 — both configs load and validate under v2.0.0 semantics.** From the extracted scratch
directory (so the relative `paths` resolve), call the library's own loader on each config:
```
python -W error::DeprecationWarning -c "
import os
os.chdir(r'C:/Users/tucke/Desktop/Aqua/AquaCal/archive_scratch/real-rig')
from aquacal.config import load_config
for name in ('config_paper.yaml','config_quickstart_not_paper.yaml'):
    c=load_config(name)
    print(name, 'ok', c.frame_step, c.max_calibration_frames, len(c.cameras))
"
```
Both must load without exception and without any DeprecationWarning naming `initial_distances`;
`-W error::DeprecationWarning` is what makes that assertion binding. This is where the folded
2026-02-24 todo lands: confirm `load_config` sees a per-camera `initial_water_z` mapping and that
the deprecated `initial_distances` compatibility shim in `pipeline.py` is never exercised. Record
the observed `interface` values in the SUMMARY, settling DATA-01's open question about whether the
shipped `initial_distances` was a scalar or carried pre-v1.4 physical-gap semantics.

Also assert the frame counts the configs will actually see:
```
python -c "
import glob
d=sorted(glob.glob(r'C:/Users/tucke/Desktop/Aqua/AquaCal/archive_scratch/real-rig/extrinsic/*'))
print(len(d), [len(glob.glob(x+'/*.png')) for x in d])"
```
Every extrinsic camera must report 262.

**Write `.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md`**,
the document plan 21-09's human step transcribes from. It must contain, each on its own clearly
labelled line so it can be copied without interpretation:
  - `zenodo_filename: real-rig-calib.zip`
  - `size_bytes: <exact integer>`
  - `checksum: md5:<32 hex chars>` (in the `md5:` prefixed form `download.py` expects)
  - `zenodo_record_id: TBD — minted in plan 21-09` (the only value not knowable pre-publish)
  - the full extracted directory listing, one line per top-level entry with its size
  - the gate-2 and gate-4 evidence: the extraction-path assertion, both `load_config` results,
    and the per-camera extrinsic frame counts
  - a `## Gates` table with rows for all four D-15 gates and their status
    (`2 PASS`, `4 PASS`, `1 pending — plan 21-08`, `3 pending — plan 21-08`)

Leave the scratch tree in place — Task 4 deletes it.
  </action>
  <verify>
    <automated>python -c "
import re
t=open('.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md',encoding='utf-8').read()
assert re.search(r'size_bytes:\s*\d{9,}', t), 'size_bytes missing or implausible'
assert re.search(r'checksum:\s*md5:[0-9a-f]{32}', t), 'checksum missing or malformed'
assert 'zenodo_filename: real-rig-calib.zip' in t
assert 'zenodo_record_id' in t
assert '262' in t
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `21-ARCHIVE-MANIFEST.md` contains a line matching `size_bytes:\s*\d{9,}` and a line matching `checksum:\s*md5:[0-9a-f]{32}`, both equal to the values Task 2 reported.
    - It contains `zenodo_filename: real-rig-calib.zip` and a `zenodo_record_id` line explicitly marked TBD.
    - It contains a `## Gates` table with four rows; gates 2 and 4 read PASS and gates 1 and 3 read pending with plan 21-08 named.
    - The extraction assertion is recorded: `<scratch>/real-rig/config_paper.yaml` existed after extraction.
    - `python -W error::DeprecationWarning -c "...load_config..."` exited 0 for **both** configs, and the transcript is in the manifest document.
    - Every one of the 13 extrinsic camera directories reported 262 PNGs in the extracted copy.
    - The scratch extraction directory still exists at the end of this task (Task 4 removes it), and no zip was rebuilt or re-extracted here.
    - The SUMMARY answers DATA-01's open question explicitly: what form `initial_water_z` takes in the shipped configs and whether the `initial_distances` shim was exercised.
  </acceptance_criteria>
  <done>Gates 2 and 4 are green with recorded evidence, and the two pre-computable manifest values are written down before any upload.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Delete the scratch extraction (LONG — orchestrator/user only)</name>
  <files>none (deletes outside the repo)</files>
  <read_first>
    - Task 3's completed manifest document — the gate evidence must be recorded before the evidence's source is deleted
  </read_first>
  <action>
**CHECKPOINT — NOT AUTONOMOUS.** The orchestrator or the user performs this task by running the
command in `<how-to-verify>` below. Do NOT execute this task from inside an executor subagent:
deleting ~4.4 GB across ~3,500 files can exceed a subagent's tool-call ceiling, and per CLAUDE.md
an executor that backgrounds it stalls permanently. Record the command, its exit code and its
wall-clock runtime in the SUMMARY.
  </action>
  <what-built>
The `<scratch>` extraction Task 2 produced and Task 3 gated against — a second ~4.4 GB copy of the
archive that is no longer needed once `21-ARCHIVE-MANIFEST.md` records the evidence.
  </what-built>
  <how-to-verify>
**DO NOT RUN THIS FROM INSIDE AN EXECUTOR SUBAGENT.**

Confirm first that Task 3's manifest exists and carries the gate evidence — once the scratch tree
is gone, re-deriving it costs another full extraction:
```
grep -c 'checksum: md5:' .planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md
```
Then delete, detached, per CLAUDE.md's long-run rules:
```
nohup rm -rf /c/Users/tucke/Desktop/Aqua/AquaCal/archive_scratch \
  > /c/Users/tucke/Desktop/Aqua/AquaCal/scratch_rm.log 2>&1 &
disown
```
**Delete only `archive_scratch`.** `archive_staging/` and `real-rig-calib.zip` must survive —
plan 21-09 uploads that zip and plan 21-08 runs the §3 reproduction against the staged tree.

Confirm:
```
test -e /c/Users/tucke/Desktop/Aqua/AquaCal/archive_scratch && echo STILL THERE || echo gone
test -f /c/Users/tucke/Desktop/Aqua/AquaCal/real-rig-calib.zip && echo zip ok
test -d /c/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig && echo staging ok
```
  </how-to-verify>
  <acceptance_criteria>
    - `21-ARCHIVE-MANIFEST.md` contained the `checksum: md5:` line before the deletion started.
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/archive_scratch` no longer exists.
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/real-rig-calib.zip` still exists and its byte size still equals the `size_bytes` recorded in the manifest.
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig/` still exists with its `extrinsic/` and `intrinsic/` subtrees intact.
    - `git status --porcelain` in the repo shows only the manifest document from Task 3.
  </acceptance_criteria>
  <verify>
    <automated>python -c "
import os,re
assert not os.path.exists('C:/Users/tucke/Desktop/Aqua/AquaCal/archive_scratch'), 'scratch not deleted'
z='C:/Users/tucke/Desktop/Aqua/AquaCal/real-rig-calib.zip'
assert os.path.exists(z), 'zip missing'
assert os.path.isdir('C:/Users/tucke/Desktop/Aqua/AquaCal/archive_staging/real-rig'), 'staging missing'
t=open('.planning/phases/21-new-feature-documentation-dataset-refresh/21-ARCHIVE-MANIFEST.md',encoding='utf-8').read()
m=re.search(r'size_bytes:\s*(\d+)', t)
assert m and int(m.group(1))==os.path.getsize(z), (m and m.group(1), os.path.getsize(z))
print('ok')"</automated>
  </verify>
  <done>The scratch copy is gone, the zip and the staging tree survive, and the zip's size still matches the recorded manifest value.</done>
  <resume-signal>Type "scratch cleared" with the deletion's wall-clock runtime and the confirmed zip size, or paste the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged bytes -> permanent public DOI | Everything in this zip becomes irrevocably public in plan 21-09 |
| archive md5 -> reader's only integrity check | `download_with_progress` validates the manifest checksum; a wrong value silently breaks every reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07-01 | Tampering | archive integrity for every downstream reader | mitigate | md5 and byte size are computed from the **final** zip bytes and from nothing else, recorded in the `md5:` prefixed form `download.py:151-171` expects, and re-verified against the uploaded file in plan 21-10 before the manifest is committed |
| T-21-07-02 | Information disclosure | absolute local paths inside the published configs | mitigate | Acceptance criteria assert no `paths.*_videos` value contains `C:` or `.avi`, and the README carries no local path; the release config's absolute video paths are replaced wholesale |
| T-21-07-03 | Tampering | premature deletion of the repo's only copy of the reference outputs | mitigate | Task 2 **copies**, never moves; the repo-side deletion is plan 21-11 and is gated on the publish in 21-09. Acceptance requires `git status --porcelain` clean there |
| T-21-07-04 | Spoofing | shipping a config that silently differs from the one that produced §3 | mitigate | The automated verify diffs `cameras` order and eight solver settings against `release_calibration/config.yaml`; the transposed `e3v82f9`/`e3v82e0` order in the currently published archive is called out explicitly |
| T-21-07-05 | Repudiation | uncertainty about which `calibration.json` shipped | mitigate | The md5 and byte size of all six copied reference-output files are recorded in the SUMMARY |
| T-21-07-06 | Denial of service | executor stalling on the ~4.4 GB zip build, extraction or deletion | mitigate | Tasks 2 and 4 are `checkpoint:human-action`, `autonomous: false`, with an explicit do-not-run-from-an-executor banner and `nohup … & disown`; Task 3 is seconds-scale and is forbidden from rebuilding, re-extracting or deleting the archive |
| T-21-07-07 | Denial of service | deleting the scratch tree before its gate evidence is recorded | mitigate | Task 4 asserts the manifest's `checksum: md5:` line exists before deleting, and deletes only `archive_scratch` — the zip and the staging tree are asserted to survive |
| T-21-07-SC | Tampering | package installs | mitigate | Nothing is installed; `shutil.make_archive`, `zipfile`, `hashlib`, `yaml` and the already-installed `aquacal` are all that is used |
</threat_model>

<verification>
- `python -c "import zipfile; ..."` confirms one top-level `real-rig` folder and 3,406 extrinsic PNGs.
- `load_config` succeeds on both configs under `-W error::DeprecationWarning`.
- `21-ARCHIVE-MANIFEST.md` carries a 32-hex md5 and a 9+-digit `size_bytes`.
- The scratch tree is gone; the zip and the staging tree survive.

**D-15 gates 1 and 3 are NOT satisfied by this plan.** Do not proceed to the publish checkpoint
until plan 21-08 marks them PASS.
</verification>

<success_criteria>
One archive exists carrying both configs, the full frameset and the run's own reference outputs;
its md5 and byte size are computed and recorded before any upload; it extracts to the nested
layout `loader.py:60` resolves; both configs load and validate under v2.0.0 with no
`initial_distances` shim exercised; and every multi-gigabyte step ran outside an executor
subagent.
</success_criteria>

<output>
Create `.planning/phases/21-new-feature-documentation-dataset-refresh/21-07-SUMMARY.md` when done.
</output>
