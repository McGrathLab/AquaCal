---
phase: 21-new-feature-documentation-dataset-refresh
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/tutorials/03_cli_walkthrough.md
  - docs/tutorials/index.md
autonomous: true
requirements: [DOCS-05, DATA-02]

must_haves:
  truths:
    - "A reader can go from `pip install aquacal` to a validated real-rig calibration using only the CLI, following one page (D-05)."
    - "Every number printed on that page is quoted from a named file inside the published archive, not from a pasted console session (D-06)."
    - "The page states which config reproduces the paper's §3 numbers and which one explicitly does not (D-12)."
    - "The page is the doc surface D-15 gate 3 runs verbatim, so its commands are executable as written."
  artifacts:
    - path: "docs/tutorials/03_cli_walkthrough.md"
      provides: "End-to-end CLI walkthrough over the real-rig Zenodo archive"
      contains: "reference_outputs/diagnostics.json"
      min_lines: 180
    - path: "docs/tutorials/index.md"
      provides: "Nav entry and toctree stem"
      contains: "03_cli_walkthrough"
  key_links:
    - from: "docs/tutorials/03_cli_walkthrough.md"
      to: "aquacal.datasets.load_example"
      via: "documented download step"
      pattern: "load_example"
    - from: "docs/tutorials/03_cli_walkthrough.md"
      to: "config_paper.yaml"
      via: "the §3-reproducing invocation"
      pattern: "config_paper\\.yaml"
---

<objective>
Write the docs' first end-to-end CLI walkthrough, and make it the new home for the real-rig
dataset (D-05, D-17).

Purpose: the CLI is AquaCal's preferred entrypoint, yet the only end-to-end worked example uses
the Python API. `docs/guide/cli.md` is a 197-line reference, not a walkthrough. This page is a
net-new deliverable, not a consolation for deleting notebook 01's zenodo branch — treat it that
way (user's framing, `<specifics>`).

Output: `docs/tutorials/03_cli_walkthrough.md` plus its nav entry. This page is what D-15 gate 3
runs verbatim against the staged archive in plan 21-08, so every command must be executable
exactly as printed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-PATTERNS.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-FRAMESET-PROVENANCE.md
@CLAUDE.md
</context>

<interfaces>
**The archive layout this page must describe is FIXED by this phase and is not the executor's
choice.** Plan 21-07 builds exactly this; write the page against it:

```
real-rig-calib.zip
└── real-rig/
    ├── README.md
    ├── config_paper.yaml                  # frame_step: 1 over the pre-subsampled frames — REPRODUCES §3
    ├── config_quickstart_not_paper.yaml   # frame_step: 5 — fast first contact, does NOT reproduce §3
    ├── reference_calibration.json         # read by load_example(); copy of the paper run's calibration
    ├── extrinsic/<camera>/frame0000.png … frame0261.png     (13 cameras x 262 frames)
    ├── intrinsic/<camera>/frame0000.png …                   (13 cameras, ragged counts)
    └── reference_outputs/                 # the paper run's own outputs (DATA-01b)
        ├── calibration.json
        ├── diagnostics.json
        ├── reprojection_residuals.csv
        ├── reconstruction_errors.csv
        ├── exp2_spatial_errors.csv
        └── interface_ablation_conditioning.npz
```

`load_example("real-rig")` returns an `ExampleDataset` whose `cache_path` resolves the nested
`real-rig/` folder (`src/aquacal/datasets/loader.py:33-63`).

**The nine §3 quantities and their exact values** — read them from
`C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/diagnostics.json`, which is byte-for-byte
the file that ships as `reference_outputs/diagnostics.json`:

| §3 quantity | `diagnostics.json` location | value |
|---|---|---|
| Mean reprojection | mean of the 12 per-camera values | 0.87863 px |
| Per-camera range | min `e3v829d`, max `e3v83f0` | 0.53938 – 2.40788 px |
| Auxiliary fisheye | `auxiliary_cameras.e3v8250.reprojection_rms` | 15.13432 px |
| Inter-corner MAE | `reconstruction.mean` | 0.00026815 m |
| Inter-corner RMSE | `reconstruction.rmse` | 0.00067407 m |
| Mean relative error | `reconstruction.percent_error` | 0.44692 % |
| Comparisons | `reconstruction.num_comparisons` | 7762 |
| Recovered water_z | `camera_heights.water_z` | 1.03055 m |
| Camera height range | min `e3v83e9`, max `e3v83f1` | 1.00828 – 1.08153 m |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/tutorials/03_cli_walkthrough.md</name>
  <files>docs/tutorials/03_cli_walkthrough.md</files>
  <read_first>
    - `docs/guide/cli.md` (full — the `**Syntax:**` fenced-bash convention, the argument tables, and the numbered `**Examples:**` comment style to reuse for every command block)
    - `docs/tutorials/01_full_pipeline.ipynb` markdown cells 0, 2 and 22 (tutorial voice: short intro, bold-lead bullets, and the `| Symptom | Likely Cause | Fix |` troubleshooting table)
    - `docs/tutorials/index.md` (the two existing tutorial blurbs, whose shape Task 2 copies)
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/diagnostics.json` — **read this file and quote the actual values; do not copy the table in `<interfaces>` without confirming it**
    - `C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/config.yaml` (the settings `config_paper.yaml` carries)
    - `src/aquacal/datasets/loader.py` lines 33-63 (`load_example` return shape and the nested-path rule)
    - `src/aquacal/cli.py` (confirm the exact `aquacal calibrate` / `aquacal compare` flag spellings before printing them)
  </read_first>
  <action>
Create `docs/tutorials/03_cli_walkthrough.md`, titled
`# Tutorial 03: Calibrate the Real Rig from the Command Line`. MyST syntax throughout
(`:::{admonition}` colon fences), matching the guide pages.

Structure:

**Intro (2 paragraphs).** What the reader ends up with: a full 13-camera refractive calibration
of a real underwater rig, reproducing the numbers published in the AquaCal paper's §3, driven
entirely by `aquacal` CLI commands. State the two prerequisites — `pip install aquacal` and
~4.4 GB of free disk for the dataset — and state up front that this is the only tutorial that
uses real hardware data; the two notebooks are synthetic and fast.

**`## 1. Get the dataset`.** Show the one-liner that downloads and caches the archive:
```bash
python -c "from aquacal.datasets import load_example; print(load_example('real-rig').cache_path)"
```
Explain that this downloads the Zenodo archive once, validates its md5 against the manifest
shipped in the package, extracts it, and prints the cache directory. Say the download is ~4.4 GB
and happens exactly once. Then show the directory listing of the extracted archive as a fenced
```text block, exactly matching the layout in `<interfaces>`, with a one-line comment on each
entry. Add a `:::{admonition} Two configs ship in this archive :class: warning` box stating
plainly: `config_paper.yaml` reproduces the paper's §3 numbers and takes roughly 50 minutes;
`config_quickstart_not_paper.yaml` subsamples the frames for a fast first run and its numbers
**do not** reproduce §3 and must not be quoted (D-12).

**`## 2. A fast first run`.** `cd` into the cache directory, then:
```bash
aquacal calibrate config_quickstart_not_paper.yaml -v
```
Explain `-v` surfaces optimizer progress and that `python -u` is only needed when piping to a
file. Describe what the console prints in terms of the three-stage model (in-air intrinsics,
extrinsic initialisation by best-first pose-graph traversal, joint refractive bundle adjustment)
— **three stages, never four**. State that this run writes into `output/`. **Print no numeric
results for this run** — say explicitly that its numbers are not a reference and the reader
should compare against §3 only after the full run in step 3.

**`## 3. Reproduce the paper's numbers`.**
```bash
aquacal calibrate config_paper.yaml -v
```
Note the ~50 minute runtime and the ~11 GiB peak memory measured on a 16 GB machine, and that
the run should have the machine to itself. Then a `### What you should see` subsection giving
the nine §3 quantities as a `| Quantity | Reference value | Where it comes from |` table. **Every
row's third column must name the file and the JSON path inside the archive**, e.g.
`reference_outputs/diagnostics.json` -> `reconstruction.num_comparisons`. Immediately before the
table, one sentence: "Every number in this table is read from
`reference_outputs/diagnostics.json` inside the archive — the paper run's own output — so you can
diff your `output/diagnostics.json` against it directly." This sentence and the per-row file
attribution are D-06's requirement and are non-negotiable; a pasted console transcript is
forbidden.
Follow with the concrete diff command:
```bash
python -c "import json; d=json.load(open('output/diagnostics.json')); r=json.load(open('reference_outputs/diagnostics.json')); print(d['reconstruction']['num_comparisons'], r['reconstruction']['num_comparisons'])"
```

**`## 4. Inspect the result`.** Show `aquacal compare` used against
`reference_outputs/calibration.json` (confirm the exact subcommand and flag names against
`src/aquacal/cli.py` before writing them). Point at the other files in `output/` and link to
[Benchmarking & Diagnostics](../guide/benchmarking.md) for `benchmark.json` and the opt-in
trace/conditioning outputs.

**`## 5. Run it on your own rig`.** Two short paragraphs: `aquacal init --intrinsic-dir ...
--extrinsic-dir ...` generates a starting config from your own video directories, then link to
[Configuration Reference](../guide/configuration.md) and [CLI Reference](../guide/cli.md).

**`## Troubleshooting`.** A `| Symptom | Likely cause | Fix |` table with at least four rows,
including: checksum mismatch on download (partial download — delete the cache dir and retry);
`0 frames detected` / `VideoCapture` returning nothing (wrong Python environment — the AquaCal
environment, not a bare system Python); an out-of-memory kill on `config_paper.yaml` (the run
needs ~11 GiB; close other applications or use the quickstart config); numbers that do not match
§3 (confirm you ran `config_paper.yaml`, not the quickstart config).

**`## See Also`.** Bullet list linking `../guide/cli.md`, `../guide/configuration.md`,
`../guide/benchmarking.md`, and the two notebooks.

Constraints:
- No fenced block may contain a machine-specific absolute path from the author's disk.
- Do not state a Zenodo DOI or record id — plan 21-09 mints it and 21-10 owns the manifest.
  Refer to the archive only through `load_example("real-rig")`.
  </action>
  <verify>
    <automated>python -c "
t=open('docs/tutorials/03_cli_walkthrough.md',encoding='utf-8').read()
need=['load_example','config_paper.yaml','config_quickstart_not_paper.yaml','reference_outputs/diagnostics.json','7762','1.03055','0.44692','15.13432','aquacal calibrate','aquacal init','benchmarking.md']
missing=[n for n in need if n not in t]
assert not missing, missing
assert len(t.splitlines())>=180, len(t.splitlines())
import re
assert not re.search(r'C:[\\\\/]Users', t), 'absolute author path leaked'
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/tutorials/03_cli_walkthrough.md` exists and is >= 180 lines.
    - It contains the literals `config_paper.yaml`, `config_quickstart_not_paper.yaml`, `reference_outputs/diagnostics.json`, `load_example`, `aquacal calibrate`, `aquacal init`.
    - All nine §3 values appear: `0.87863`, `0.53938`, `2.40788`, `15.13432`, `0.26815` (or `0.00026815`), `0.67407` (or `0.00067407`), `0.44692`, `7762`, `1.03055`, `1.00828`, `1.08153`.
    - `grep -c 'reference_outputs/' docs/tutorials/03_cli_walkthrough.md` returns >= 9 — every quoted number names its source file inline (D-06).
    - `grep -ci 'stage 4\|four stages\|four calibration stages' docs/tutorials/03_cli_walkthrough.md` returns 0.
    - `grep -c 'zenodo.org/record\|10\.5281' docs/tutorials/03_cli_walkthrough.md` returns 0 — no DOI is hardcoded here.
    - `grep -c 'C:[\\/]Users' docs/tutorials/03_cli_walkthrough.md` returns 0.
    - The troubleshooting table has >= 4 data rows: `python -c "import re;t=open('docs/tutorials/03_cli_walkthrough.md',encoding='utf-8').read();i=t.index('## Troubleshooting');assert len([l for l in t[i:].splitlines() if l.startswith('|')])>=6"` exits 0.
  </acceptance_criteria>
  <done>A complete CLI walkthrough exists whose every quoted number names its source file inside the archive, and which distinguishes the paper config from the quickstart config.</done>
</task>

<task type="auto">
  <name>Task 2: Register the tutorial in the tutorials index and build the docs</name>
  <files>docs/tutorials/index.md</files>
  <read_first>
    - `docs/tutorials/index.md` (full file — the `## Tutorial NN: Title` + paragraph + `**Start here** if ...` shape, and the hidden toctree)
    - `docs/conf.py` lines 17-45 (myst/nbsphinx settings; confirms a `.md` stem sits in the same toctree as `.ipynb` stems)
    - `.github/workflows/docs.yml` line 30 (the exact build command)
  </read_first>
  <action>
In `docs/tutorials/index.md`:
1. Change the opening line "Interactive Jupyter notebook tutorials ... with real and synthetic
   data." so it no longer promises real data from the notebooks — the notebooks become
   synthetic-only in plan 21-04. Replace with a sentence covering both forms: two fast synthetic
   notebooks plus one written command-line walkthrough over the published real-rig dataset.
2. Add a third section after Tutorial 02, matching the existing blurb shape exactly:
   `## Tutorial 03: Calibrate the Real Rig from the Command Line`, one paragraph describing the
   end-to-end CLI run over the Zenodo `real-rig` archive and the §3 reproduction, then
   `**Start here** if you want to reproduce the published results or drive AquaCal from the
   command line.`
3. Add the bare stem `03_cli_walkthrough` to the hidden `:::{toctree}` block after
   `02_synthetic_validation`.

Then build the docs exactly as CI does, in the AquaCal conda env:
`sphinx-build -W --keep-going -b html docs docs/_build/html`
Fix any error in files this plan owns. Do NOT commit `docs/_build/`.
  </action>
  <verify>
    <automated>sphinx-build -W --keep-going -b html docs docs/_build/html</automated>
  </verify>
  <acceptance_criteria>
    - `sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
    - `docs/_build/html/tutorials/03_cli_walkthrough.html` exists.
    - `grep -c '^03_cli_walkthrough$' docs/tutorials/index.md` returns 1.
    - `grep -c 'Tutorial 03' docs/tutorials/index.md` returns 1.
    - `grep -c 'real and synthetic' docs/tutorials/index.md` returns 0 (the opening line was updated).
    - `git status --porcelain docs/_build` produces no output.
  </acceptance_criteria>
  <done>Tutorial 03 has an index blurb and a toctree stem, the index no longer promises real data from the notebooks, and the `-W` docs build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> published docs site | This page is published publicly by `.github/workflows/docs.yml` |
| Zenodo -> reader machine | The page instructs readers to download and extract a 4.4 GB third-party-hosted archive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Tampering | archive integrity on the reader's machine | mitigate | The page routes the download exclusively through `load_example("real-rig")`, which validates the manifest md5 via `download_with_progress` — it never tells a reader to fetch a raw URL by hand, and the troubleshooting table names checksum mismatch as a first-class symptom with "delete the cache and retry" as the fix |
| T-21-03-02 | Information disclosure | author's local filesystem layout | mitigate | Acceptance criterion greps for `C:[\\/]Users` and requires 0 matches; no `paths:` block from the release config is pasted |
| T-21-03-03 | Spoofing | a stale or wrong DOI printed in docs | mitigate | The page hardcodes no DOI or record id; the single source of truth is `manifest.json`, updated once in plan 21-10 |
| T-21-03-SC | Tampering | package installs | mitigate | This plan installs nothing; the page instructs `pip install aquacal` from PyPI only |
</threat_model>

<verification>
- `sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
- All nine §3 values and >= 9 `reference_outputs/` attributions appear in the page.
- No absolute author path and no DOI literal appear in the page.
</verification>

<success_criteria>
`docs/tutorials/03_cli_walkthrough.md` is a complete, executable end-to-end CLI walkthrough over
the real-rig archive; every number it prints is attributed inline to a named file inside that
archive (D-06); the paper config and the quickstart config are distinguished unambiguously
(D-12); the page is registered in the tutorials nav and the `-W` build is green.
</success_criteria>

<output>
Create `.planning/phases/21-new-feature-documentation-dataset-refresh/21-03-SUMMARY.md` when done.
</output>
