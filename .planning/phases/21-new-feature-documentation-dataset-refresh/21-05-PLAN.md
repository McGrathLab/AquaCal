---
phase: 21-new-feature-documentation-dataset-refresh
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - pyproject.toml
  - requirements.txt
autonomous: true
requirements: [DOCS-05, DATA-02, DATA-03]

must_haves:
  truths:
    - "DOCS-05's text no longer promises `calc-index`, and does name the two new deliverables this phase ships (D-01, D-02, D-05)."
    - "DATA-02's acceptance no longer says 'the path the notebook resolves' — no notebook resolves it after D-18."
    - "DATA-03's text reflects that the real-data narration moved to the CLI tutorial, not that it was deleted."
    - "A fresh `pip install aquacal` cannot resolve OpenCV 5.0."
    - "DATA-01b appears in the ROADMAP's Phase 21 requirement list, matching the traceability table."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reworded DOCS-05 / DATA-02 / DATA-03 text with an amendment note"
      contains: "docs/guide/benchmarking.md"
    - path: "pyproject.toml"
      provides: "Upper-bounded OpenCV constraint"
      contains: "opencv-python>=4.6,<5.0"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 21 requirement list must include DATA-01b"
      pattern: "DATA-01b"
---

<objective>
Bring the written requirements in line with what this phase actually ships, and close the OpenCV
pin todo.

Purpose: DOCS-05's text names `aquacal calc-index`, which does not exist in `src/` — it is
deferred Phase 20's INDEX-02 deliverable (D-01). DATA-02's acceptance is written against "the
path the notebook resolves", which goes vacuous the moment D-18 deletes notebook 01's zenodo
branch. Leaving either as written means the phase closes against text it did not satisfy.

Output: reworded requirement entries with an audit trail, a ROADMAP correction, and a verified
OpenCV upper bound. This plan changes no source code and no docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reword DOCS-05, DATA-02, DATA-03 and add DATA-01b to the ROADMAP phase entry</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 70-95 (the DOCS-05 and DATA-* entries) and lines 425-445 (the traceability table, which already maps DATA-01b to Phase 21)
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-CONTEXT.md` § "Requirement text that must be reworded by this phase" (the four exact rewordings D-01 and D-17/D-18 require)
    - `.planning/ROADMAP.md` § "Phase 21" (its `**Requirements**:` line omits DATA-01b)
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` in place. Do not tick any checkbox — closure happens at phase
end, not here.

**DOCS-05** (currently: "Everything this milestone adds is documented — `calc-index`, the
`benchmark.json` schema, the trace and conditioning flags, and `shared_interface` framed as an
ablation option"). Rewrite to drop `calc-index` and name the two new deliverables:
> Everything this milestone adds is documented — the `benchmark.json` schema, the optimization
> trace and conditioning outputs (new `docs/guide/benchmarking.md`), and `shared_interface`
> framed as an ablation option — plus a written end-to-end CLI walkthrough
> (`docs/tutorials/03_cli_walkthrough.md`), which is the docs' first command-line worked example.

Append, on its own indented line beneath the entry:
> *Amended 2026-08-10 (Phase 21, D-01): `aquacal calc-index` removed from scope. It is Phase 20's
> INDEX-02 deliverable and Phase 20 is deferred on measured evidence (MF-13); its documentation
> rides with it. `docs/guide/benchmarking.md` and the CLI tutorial added (D-02, D-05).*

**DATA-02** — replace "verified to download, checksum, and extract at the path the notebook
resolves" with "verified to download, checksum, and extract at the path
`docs/tutorials/03_cli_walkthrough.md` and `experiments/e2_real_rig.py` resolve". Append:
> *Amended 2026-08-10 (Phase 21, D-18): no notebook resolves the archive after notebook 01's
> Zenodo branch was deleted; the CLI tutorial and the experiment suite are its only consumers.*

**DATA-03** — keep "Both tutorial notebooks are re-executed with fresh committed outputs, and any
narration the outputs contradict is updated", and append to the sentence:
"; the real-data narration this originally anticipated moves to
`docs/tutorials/03_cli_walkthrough.md`, and both notebooks become fast and synthetic-only".
Append:
> *Amended 2026-08-10 (Phase 21, D-17/D-19): notebook 01 loses its Zenodo path; notebook 02
> defaults to `RIG_SIZE = "small"`.*

**DATA-01** — do not change the requirement text. Append a note recording what the phase settled:
> *Note (Phase 21, folded todo 2026-02-24): the shipped archive config carried a scalar
> `initial_water_z`; the deprecated `initial_distances` form is not reintroduced. Both regenerated
> configs are validated through `load_config()` under v2.0.0 semantics — D-15 gate 4, plan 21-07.*

**DATA-01a / DATA-01b** — text unchanged.

Finally, edit `.planning/ROADMAP.md` § "Phase 21" so its `**Requirements**:` line reads
`DOCS-05, DATA-01, DATA-01a, DATA-01b, DATA-02, DATA-03` — DATA-01b is already mapped to Phase 21
in the traceability table and the omission is a roadmap bug. Change nothing else in ROADMAP.md;
plan counts and the plan list are the orchestrator's to write at the end of planning.
  </action>
  <verify>
    <automated>python -c "
r=open('.planning/REQUIREMENTS.md',encoding='utf-8').read()
assert 'docs/guide/benchmarking.md' in r
assert '03_cli_walkthrough' in r
assert 'the path the notebook resolves' not in r
i=r.index('**DOCS-05**'); seg=r[i:i+900]
assert 'calc-index' not in seg.split('*Amended')[0], 'calc-index still in DOCS-05 requirement text'
rd=open('.planning/ROADMAP.md',encoding='utf-8').read()
j=rd.index('### Phase 21:')
assert 'DATA-01b' in rd[j:j+1200]
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'the path the notebook resolves' .planning/REQUIREMENTS.md` returns 0.
    - The DOCS-05 requirement sentence (the text before its `*Amended` note) does not contain `calc-index`; the `*Amended` note does contain it, with the reason.
    - `.planning/REQUIREMENTS.md` contains both `docs/guide/benchmarking.md` and `03_cli_walkthrough`.
    - Exactly four `*Amended 2026-08-10 (Phase 21` / `*Note (Phase 21` annotations were added (DOCS-05, DATA-02, DATA-03, DATA-01): `grep -c 'Phase 21, D-\|Phase 21, folded todo' .planning/REQUIREMENTS.md` returns 4.
    - The ROADMAP's Phase 21 `**Requirements**:` line lists all six IDs including `DATA-01b`.
    - No checkbox changed state: `git diff .planning/REQUIREMENTS.md | grep -c '^[-+].*\[x\]'` returns 0.
  </acceptance_criteria>
  <done>Four requirement entries carry corrected text plus a dated amendment note, and the ROADMAP's Phase 21 requirement list matches the traceability table.</done>
</task>

<task type="auto">
  <name>Task 2: Verify — and if needed restore — the OpenCV upper bound</name>
  <files>pyproject.toml, requirements.txt</files>
  <read_first>
    - `pyproject.toml` (the `dependencies` array, around line 33)
    - `requirements.txt` (around line 10)
    - `.planning/phases/21-new-feature-documentation-dataset-refresh/21-PATTERNS.md` § "`pyproject.toml` / `requirements.txt`" — which records both files as **already** carrying `<5.0` as of 2026-08-10, so CONTEXT.md's "unbounded above" framing may be stale
  </read_first>
  <action>
This is a verification task first and an edit task only if verification fails.

Check both files for the OpenCV constraint. The required form is exactly
`opencv-python>=4.6,<5.0` in `pyproject.toml`'s `dependencies` array and
`opencv-python>=4.6,<5.0` in `requirements.txt`.

- If both already match: change nothing. Record in the SUMMARY that folded todo
  `2026-08-05-pin-opencv-below-5-0.md` was already satisfied, quoting both lines with their line
  numbers, and note that CONTEXT.md's "unbounded above" description was stale.
- If either is missing the upper bound: add `,<5.0` to it, leaving the lower bound alone.

Then confirm the constraint actually binds in the resolver, without installing anything:
`python -c "import cv2, packaging.specifiers as s; assert s.SpecifierSet('>=4.6,<5.0').contains(cv2.__version__), cv2.__version__; print('opencv', cv2.__version__, 'satisfies the pin')"`

Also grep the repo for any *other* place OpenCV is constrained that would need the same bound —
check `environment.yml`, `docs/requirements.txt`, `.github/workflows/*.yml`, and any `[project.optional-dependencies]`
extras in `pyproject.toml`. Report what you find; add the bound anywhere a version range is
declared without an upper bound, and record any file you deliberately left alone and why.

Do NOT touch the version string or CHANGELOG — python-semantic-release owns both, and this
milestone cuts v2.0.0 (Phase 22 resolves version strings).
  </action>
  <verify>
    <automated>python -c "
import re
p=open('pyproject.toml',encoding='utf-8').read(); r=open('requirements.txt',encoding='utf-8').read()
assert 'opencv-python>=4.6,<5.0' in p, 'pyproject'
assert 'opencv-python>=4.6,<5.0' in r, 'requirements.txt'
import cv2, packaging.specifiers as s
assert s.SpecifierSet('>=4.6,<5.0').contains(cv2.__version__), cv2.__version__
print('ok', cv2.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'opencv-python>=4.6,<5.0' pyproject.toml` returns 1 and `grep -c 'opencv-python>=4.6,<5.0' requirements.txt` returns 1.
    - `grep -rn 'opencv' --include='*.toml' --include='*.txt' --include='*.yml' --include='*.yaml' . | grep -v '<5.0'` returns no line that declares a version constraint without an upper bound (informational lines with no version are fine; list them in the SUMMARY).
    - `git diff --stat pyproject.toml requirements.txt` shows either 0 changed lines (already satisfied) or exactly the added `,<5.0` bound(s).
    - `git diff pyproject.toml | grep -c 'version'` returns 0 — no version string was touched.
    - The SUMMARY states explicitly whether the folded todo required an edit or was already satisfied, with quoted lines and line numbers.
  </acceptance_criteria>
  <done>Both dependency declarations carry `<5.0`, every other OpenCV constraint in the repo is audited, and the installed OpenCV is confirmed to satisfy the pin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> user install | `pip install aquacal` resolves dependency ranges at install time on an untrusted-to-us index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05-01 | Tampering / supply chain | unbounded `opencv-python>=4.6` | mitigate | Task 2 asserts `opencv-python>=4.6,<5.0` in both `pyproject.toml` and `requirements.txt` and audits every other OpenCV constraint in the repo; an OpenCV 5.0 resolve would break a fresh install out of the box, including the tutorials this phase publishes |
| T-21-05-02 | Repudiation | silent requirement rewrite | mitigate | Every reworded entry carries a dated `*Amended 2026-08-10 (Phase 21, D-NN)` note naming the decision that authorised it, so the change is auditable rather than a quiet scope reduction |
| T-21-05-03 | Elevation of privilege | version/CHANGELOG hand-edit | mitigate | Explicit prohibition in the action plus an acceptance criterion asserting `git diff pyproject.toml` touches no version line; python-semantic-release owns the v2.0.0 bump |
| T-21-05-SC | Tampering | package installs | mitigate | This plan installs nothing; it only reads the already-installed `cv2.__version__` to confirm the pin binds |
</threat_model>

<verification>
- `grep -c 'the path the notebook resolves' .planning/REQUIREMENTS.md` returns 0.
- `grep -c 'opencv-python>=4.6,<5.0' pyproject.toml requirements.txt` returns 1 for each file.
- The ROADMAP's Phase 21 requirement line contains `DATA-01b`.
</verification>

<success_criteria>
DOCS-05, DATA-02 and DATA-03 read as what this phase ships, each with a dated amendment note
citing its decision id; DATA-01 records the `initial_distances` resolution; the ROADMAP's Phase 21
requirement list includes DATA-01b; OpenCV is upper-bounded below 5.0 in every declaration and the
audit is recorded.
</success_criteria>

<output>
Create `.planning/phases/21-new-feature-documentation-dataset-refresh/21-05-SUMMARY.md` when done.
</output>
