---
phase: 19.4-grid-family-clearance-floor-fix
plan: 08
type: execute
wave: 4
depends_on: ["19.4-03", "19.4-06", "19.4-07"]
files_modified:
  - experiments/rerun_19_4.sh
  - experiments/check_rerun_gates.py
  - experiments/prelaunch_gate.sh
  - tests/unit/test_rerun_gates.py
  - experiments/rerun_19_4_state.tsv
autonomous: true
requirements: [SC-7]
must_haves:
  truths:
    - "The queue runs RISK-FIRST — e6 first, e4 second — so both high-risk stages complete inside 4 h (SC-7, D-19.4-16)"
    - "A cross-stage git_sha mismatch is a HARD FAILURE, not a warning"
    - "The abort protocol is written into the script's own comments so it is not decided mid-run"
    - "This plan WRITES the queue; it does NOT run it"
  artifacts:
    - path: "experiments/rerun_19_4.sh"
      provides: "Risk-first resumable eight-stage queue with a dry-run harness"
      contains: "STAGES=(e6_repeat1 e4 e6_repeat2 e6_seed43 e7 e1 e5 e3)"
    - path: "experiments/check_rerun_gates.py"
      provides: "Gate list extended for 19.4's band CSVs; cross-artifact git_sha check retained"
    - path: "experiments/prelaunch_gate.sh"
      provides: "ARCHIVES_PRESENT narrowed to {e4, e6}"
  key_links:
    - from: "experiments/rerun_19_4.sh"
      to: "experiments/rerun_19_4_state.tsv"
      via: "append-only start/complete TSV; is_stage_complete via awk, never grep -P"
      pattern: "state_complete|is_stage_complete"
---

<objective>
Write the risk-first, resumable production queue for this phase, extend the gate script for
19.4's artifacts, and narrow the prelaunch gate's expected archive set (D-19.4-16, SC-7).

Purpose: 19.3's queue ran cheapest-first, which optimises for "most stages finished" rather than
"earliest signal on the riskiest path" — e4 did not start until 230 min in. E4 is the highest-risk
stage: it is the only one exercising the `n_cameras` axis, it carries the 10.26 GiB peak on its
16-camera cells, and it is 132.5 min of exposure. Front-loading risk is what makes the
abort-and-restart rule affordable: restarting after 100 minutes is a nuisance; restarting after
400 is how people talk themselves into a midstream edit.

Output: `experiments/rerun_19_4.sh`, an extended `check_rerun_gates.py`, a narrowed
`prelaunch_gate.sh`, and dry-run evidence that the queue's sequencing, resume and kill-mid-stage
behaviour work.

**THIS PLAN WRITES THE QUEUE. IT DOES NOT RUN IT.** Executing the ~9 h 30 min production queue is
plan 09 and nothing else. This plan MUST NOT invoke `experiments/rerun_19_4.sh` without
`RERUN_19_4_DRY_RUN=1`, MUST NOT run E4, E6, E7, E1, E5 or E3 at production scale, and MUST NOT
launch anything detached.

**Stage list is gated on plan 03's verdict.** If `19.4-03-SUMMARY.md`'s `## Approved verdict`
section says AMENDED, encode the replacement stage list recorded there instead of the default
below, and note the substitution in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-CONTEXT.md
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-PATTERNS.md
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-RESEARCH.md
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-03-SUMMARY.md
@.planning/phases/19.3-scenario-geometry-and-convergence/19.3-09-SUMMARY.md
@CLAUDE.md

<interfaces>
Direct ancestor to adapt — `experiments/rerun_19_3.sh` (290 lines):
  set -u -o pipefail
  OUT_DIR="experiments/results"
  STATE_FILE="experiments/rerun_19_3_state.tsv"
  STAGES=(e3 e7 e1 e5 e6_repeat1 e6_repeat2 e4)          # line 93 — cheapest-first, to be replaced
  is_stage_complete()  # awk-based, chosen over `grep -P` after a live locale warning on this box
  state_start() / state_complete()   # append-only TSV: stage, index, start|complete, ISO-8601 UTC, exit code
  _dry_run_active() / _dry_run_stub()  # RERUN_19_3_DRY_RUN + RERUN_19_3_DRY_RUN_CMD, lines 138-143
  run_stage_e3 .. run_stage_e4       # lines 145-236, one function per stage
  dispatch case block                # lines 253-259
  `bash rerun_19_3.sh N` skips stages < N

Gate script — `experiments/check_rerun_gates.py`:
  check_e1/e3/e4/e5/e6/e7(out_dir) -> list[GateResult]   # lines 390-572
  _collect_all_json_paths(out_dir)                        # 573
  _check_git_sha_consistency(out_dir) -> GateResult        # 594-627, gate 3: every git_sha across
                                                           #   the whole run must be IDENTICAL; FAIL otherwise
  run_all_gates(out_dir) -> list[GateResult]               # 630
Regression coverage: tests/unit/test_rerun_gates.py (23 tests) — extend, do not fork.

Prelaunch gate — `experiments/prelaunch_gate.sh`:
  five checks: TREE_CLEAN, SUITE_GREEN (unfiltered), HEAD_RECORDED, ARCHIVES_PRESENT, WORKTREES_CLEAN
  ARCHIVES_PRESENT block at lines 118-172 parses the expected archive set out of a plan SUMMARY
  (`PLAN03_SUMMARY` at line 44 points at 19.3's plan 03) rather than hardcoding it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write experiments/rerun_19_4.sh with the risk-first stage order</name>
  <files>experiments/rerun_19_4.sh</files>
  <read_first>
    - experiments/rerun_19_3.sh (read the whole file — state-file skeleton at 83-116, dry-run at 129-143, the per-stage runners at 145-236, the dispatch case at 253-259, the gate invocation at 118-127 and 270-274, and the detached-launch comment at 78-81)
    - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-CONTEXT.md (D-19.4-16's stage table and abort protocol; D-19.4-13's measured costs)
    - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-03-SUMMARY.md (the `## Approved verdict` section — CONFIRMED or AMENDED)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-09-SUMMARY.md (Deviation 2 — the bare-`python` gate invocation pitfall; Deviation 3 — the orchestrator-inline execution precedent)
  </read_first>
  <action>
    Create `experiments/rerun_19_4.sh` by adapting `rerun_19_3.sh`. Copy the state-file mechanism
    verbatim (append-only TSV, awk-based `is_stage_complete` — never `grep -P`, which produced a
    live locale warning on this box), the dry-run substitution pattern, the `bash rerun_19_4.sh N`
    resume-from-stage-N argument, and the per-stage runner shape. Rename the state file to
    `experiments/rerun_19_4_state.tsv` and the dry-run environment variables to
    `RERUN_19_4_DRY_RUN` / `RERUN_19_4_DRY_RUN_CMD`.

    Set the stage list to D-19.4-16's risk-first order:
    `STAGES=(e6_repeat1 e4 e6_repeat2 e6_seed43 e7 e1 e5 e3)`.
    Cumulative completion targets, to record in the header comment: e6_repeat1 ~100 min,
    e4 ~232 min, e6_repeat2 ~328 min, e6_seed43 ~428 min, e7+band ~484 min, e1+band ~547 min,
    e5 then e3 ~569 min (9 h 30). Both high-risk stages complete inside 4 h; the total wall clock
    is unchanged from cheapest-first.

    Discretionary call to take and document: the e7 and e1 band runs are folded INTO their
    respective stages rather than split into separate state-file entries. `run_stage_e7` runs the
    single-seed production run first, then the 10-seed band; `run_stage_e1` does the same. This
    matches D-19.4-16's own eight-row table and is safe because the abort rule is already
    "restart from stage 1", not "resume". State the tradeoff in a comment: a mid-band death
    re-runs that stage's single-seed portion too.

    Every stage runner uses `python -u` (Python block-buffers stdout to a pipe; without `-u` a
    detached run's log is empty whether it is progressing or hung) and the shared `${OUT_DIR}`.
    `--force` on the production runs. Do NOT pass `--no-fail-fast` to any stage — the queue runs
    unattended and D-19.4-11's fail-fast is exactly what it needs.

    `run_stage_e6_seed43` runs E6 at seed 43 into its own output subdirectory, not into
    `${OUT_DIR}` — E6's `_SCENARIO_IDENTITY_KEYS` omits `seed`, so two runs at different seeds
    into one output directory compare identical and the second silently resumes the first. That
    latent defect is a Deferred Idea and must NOT be fixed here; work around it with a separate
    output directory and leave a comment naming the defect.

    Fix 19.3's known pitfall from the start: invoke the gate script with the pinned environment
    interpreter, not bare `python`. If the executor cannot establish the pinned interpreter path,
    keep the bare call and add a comment explicitly re-verifying that `check_rerun_gates.py` is
    stdlib-only — do not leave the question open.

    Gate invocation after each stage records the verdict as a FINDING and does NOT auto-abort the
    queue on a gate FAIL — that is 19.3's proven behaviour and this script must not silently
    exceed it. The abort-on-src-defect rule is an OPERATOR judgment made by reading gate verdicts
    and logs, not an automatic script behaviour.

    Write D-19.4-16's abort protocol into the script header as four numbered comment lines:
    (1) a src defect at any stage means abort the queue, fix, restart from stage 1;
    (2) NEVER edit `src/` or `experiments/` while the queue is in flight — 19.3's run holds one
    git sha across every artifact and a midstream edit destroys that silently, yielding a result
    set assembled from two trees, unreportable and invisible until someone diffs provenance;
    (3) each stage records its git sha and a cross-stage mismatch is a HARD failure;
    (4) commit nothing while the queue runs — a per-cell `git rev-parse` would split an artifact's
    recorded sha across the boundary.

    Distinguish the two resume semantics in comments, as `rerun_19_3.sh` already does: automatic
    resume and `bash rerun_19_4.sh N` exist for INFRASTRUCTURE failures (box reboot), never for
    src-defect recovery, which is always restart-from-stage-1.

    Copy the detached-launch invocation into the header verbatim in the 19.4 form:
    `nohup bash experiments/rerun_19_4.sh > experiments/rerun_19_4.log 2>&1 & disown`, with the
    note that `run_in_background` must NEVER be used — that harness kills background commands at
    35-50 min and this project has already lost three sweeps to it.

    **Do not execute this script for real.** Verification is dry-run only (Task 3).
  </action>
  <verify>
    <automated>bash -n experiments/rerun_19_4.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n experiments/rerun_19_4.sh` exits 0 (syntax check only)
    - `grep -c 'STAGES=(e6_repeat1 e4 e6_repeat2 e6_seed43 e7 e1 e5 e3)' experiments/rerun_19_4.sh` returns 1 — unless plan 03's verdict was AMENDED, in which case the encoded list matches that verdict verbatim and the SUMMARY records the substitution
    - `grep -v '^#' experiments/rerun_19_4.sh | grep -c 'python -u'` equals the number of stage runners (at least 8) — every production invocation is unbuffered
    - `grep -v '^#' experiments/rerun_19_4.sh | grep -c 'no-fail-fast'` returns 0
    - `grep -v '^#' experiments/rerun_19_4.sh | grep -c 'grep -P'` returns 0
    - `grep -c 'rerun_19_4_state.tsv' experiments/rerun_19_4.sh` is at least 1 and `grep -c 'rerun_19_3_state.tsv' experiments/rerun_19_4.sh` returns 0
    - The header contains all four numbered abort-protocol lines and the string `disown`
    - The header contains `run_in_background` in a prohibition, and the string `35-50`
  </acceptance_criteria>
  <done>`experiments/rerun_19_4.sh` exists, is syntactically valid, encodes the risk-first eight-stage order, and carries the pre-committed abort protocol in its header.</done>
</task>

<task type="auto">
  <name>Task 2: Extend check_rerun_gates.py for 19.4's artifacts and narrow prelaunch_gate.sh</name>
  <files>experiments/check_rerun_gates.py, experiments/prelaunch_gate.sh, tests/unit/test_rerun_gates.py</files>
  <read_first>
    - experiments/check_rerun_gates.py (read the whole file — the per-experiment `check_e*` functions at 390-572, `_collect_all_json_paths` at 573, `_check_git_sha_consistency` at 594-627, `run_all_gates` at 630)
    - tests/unit/test_rerun_gates.py (the existing 23-test regression suite to extend, not fork)
    - experiments/prelaunch_gate.sh (the ARCHIVES_PRESENT block at 118-172 and `PLAN03_SUMMARY` at line 44)
    - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-01-SUMMARY.md (the archive directory names this phase produced)
  </read_first>
  <action>
    Extend `check_rerun_gates.py` IN PLACE rather than forking — its `_check_git_sha_consistency`
    already implements exactly D-19.4-16's requirement that every `git_sha` found across the whole
    run be identical, with FAIL (not warn) on mismatch, and its 23-test regression suite is worth
    keeping.

    Add the two new band artifacts to the gate coverage: `interface_ablation_band.csv` (E7) and
    `exp1_band.csv` (E1). For each, assert the file exists after its stage, that it carries a
    `seed` column, and that the number of distinct seeds equals the length of the `seeds` list
    recorded in that experiment's provenance sidecar. Confirm `_collect_all_json_paths` already
    sweeps the sidecars carrying the new `seeds` field so the cross-artifact `git_sha` check still
    covers them; widen the glob only if it does not.

    Do NOT widen the cross-artifact `git_sha` check to include the E2 record `benchmark_grid.csv`'s
    `real_rig` row feeds from — that is an explicit Deferred Idea.

    Extend `tests/unit/test_rerun_gates.py` with cases for the two new band gates, including a
    negative case where the band's distinct-seed count disagrees with the sidecar's `seeds` list
    and the gate returns FAIL.

    In `experiments/prelaunch_gate.sh`, repoint `PLAN03_SUMMARY` at this phase's archiving plan
    SUMMARY (`.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-01-SUMMARY.md`) and
    update the directory-name pattern the ARCHIVES_PRESENT block parses so it matches
    `experiments/archive/e[0-9]+-2026-08-04-pre-interface-fix`. The expected set narrows to
    {e4, e6} — NOT the six or seven of 19.3 — because E1, E3, E5 and E7 are proven inert, not
    archived. Remove or repoint the hardcoded E3 special case at line ~152 accordingly. Leave
    TREE_CLEAN, SUITE_GREEN (unfiltered), HEAD_RECORDED and WORKTREES_CLEAN unchanged.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_rerun_gates.py -x -q &amp;&amp; bash -n experiments/prelaunch_gate.sh</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_rerun_gates.py -x -q` exits 0 and collects at least 27 tests (23 existing plus at least 4 new)
    - `grep -c "interface_ablation_band.csv" experiments/check_rerun_gates.py` is at least 1
    - `grep -c "exp1_band.csv" experiments/check_rerun_gates.py` is at least 1
    - `grep -c "def _check_git_sha_consistency" experiments/check_rerun_gates.py` returns 1 and its FAIL branch is unchanged (`git diff` on that function shows no behavioural edit)
    - `bash -n experiments/prelaunch_gate.sh` exits 0
    - `grep -c "19.4-01-SUMMARY.md" experiments/prelaunch_gate.sh` returns 1
    - `grep -v '^#' experiments/prelaunch_gate.sh | grep -c "pre-depth-fix"` returns 0
    - `python -m experiments.check_rerun_gates --help` exits 0
  </acceptance_criteria>
  <done>The gate script covers both band CSVs with its git_sha hard-failure intact, and the prelaunch gate expects exactly the {e4, e6} archive set this phase produced.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the queue's sequencing, resume and kill-mid-stage mechanics by dry run</name>
  <files>experiments/rerun_19_4_state.tsv</files>
  <read_first>
    - experiments/rerun_19_4.sh (post-Task-1)
    - experiments/rerun_19_3.sh (lines 129-143 — the dry-run substitution pattern being reused)
    - .planning/phases/19.3-scenario-geometry-and-convergence/19.3-08-SUMMARY.md (Finding 1 — `--smoke` does NOT exercise E6's production resume path, so it is not proof of resume)
  </read_first>
  <action>
    Exercise the queue script three ways, all with `RERUN_19_4_DRY_RUN=1` so no real compute runs,
    and all SYNCHRONOUSLY IN THE FOREGROUND:

    1. **Full dry run from a clean state.** Delete any existing
       `experiments/rerun_19_4_state.tsv`, run
       `RERUN_19_4_DRY_RUN=1 bash experiments/rerun_19_4.sh`, and assert the state file records
       exactly eight `complete` lines in the risk-first order.
    2. **Kill mid-stage.** Reset the state file, run with a `RERUN_19_4_DRY_RUN_CMD` that exits
       non-zero on the third stage, and assert the state file shows a `start` line for that stage
       with no matching `complete` line — a started-but-died stage must be treated as not-done.
    3. **Resume.** From the state left by (2), re-run with a succeeding dry-run command and assert
       the already-complete stages are skipped and the died stage is re-run.

    Additionally assert `bash experiments/rerun_19_4.sh 3` (resume-from-stage-N) skips stages 1
    and 2, and record in the SUMMARY that this argument exists for INFRASTRUCTURE failures only —
    a src defect is always restart-from-stage-1.

    Record every command and its observed state-file contents in the SUMMARY. Do NOT treat
    `--smoke` as evidence of resume behaviour: 19.3-08 Finding 1 established that E6's `--smoke`
    path always passes `force=True` and therefore never exercises production resume.

    **This task MUST NOT run the queue for real.** Every invocation carries
    `RERUN_19_4_DRY_RUN=1`. Do not use `nohup`, do not use `disown`, do not use
    `run_in_background`, and do not launch anything detached — that is plan 09's job and plan
    09's alone.
  </action>
  <verify>
    <automated>rm -f experiments/rerun_19_4_state.tsv &amp;&amp; RERUN_19_4_DRY_RUN=1 bash experiments/rerun_19_4.sh &amp;&amp; awk -F'\t' '$3 == "complete"' experiments/rerun_19_4_state.tsv | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The clean full dry run exits 0 and `awk -F'\t' '$3 == "complete"' experiments/rerun_19_4_state.tsv | wc -l` prints 8
    - `awk -F'\t' '$3 == "complete" {print $1}' experiments/rerun_19_4_state.tsv | tr '\n' ' '` prints `e6_repeat1 e4 e6_repeat2 e6_seed43 e7 e1 e5 e3 ` (or plan 03's amended order)
    - The kill-mid-stage run leaves a `start` line with no matching `complete` line for the killed stage, verified by awk
    - The resume run skips every previously-complete stage (its log names them as skipped) and re-runs the killed one
    - `RERUN_19_4_DRY_RUN=1 bash experiments/rerun_19_4.sh 3` produces no `start` line for stages 1 or 2
    - No process was launched with `nohup`, `disown`, or `run_in_background` during this task
    - `git status --porcelain experiments/results/` produces no output — the dry run touched no artifact
  </acceptance_criteria>
  <done>The queue script's sequencing, kill-mid-stage detection, resume and resume-from-N behaviour are all demonstrated by dry run, with the observed state-file contents recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queue script → 9 h 30 min of unattended compute | The script is the only thing standing between a src defect and a result set assembled from two git trees. |
| gate script verdicts → the decision to accept a run | A gate that silently passes an inconsistent sha makes the whole artifact set unreportable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.4-22 | Tampering | one-git-sha-per-artifact property | mitigate | `_check_git_sha_consistency`'s FAIL branch is preserved unmodified; the abort protocol forbidding midstream `src/` and `experiments/` edits is written into the script header as four numbered lines. |
| T-19.4-23 | Repudiation | a resumed stage recorded as complete when it died | mitigate | `is_stage_complete` only returns true on a matching `complete` line; Task 3 case 2 proves a started-but-died stage is treated as not-done. |
| T-19.4-24 | Denial of service | accidental real launch during a dry-run test | mitigate | Every Task 3 invocation carries `RERUN_19_4_DRY_RUN=1`; the plan forbids `nohup`, `disown` and `run_in_background` by name and states that execution is plan 09's job. |
| T-19.4-25 | Tampering | E6 seed-43 output silently resuming the seed-42 run | mitigate | `run_stage_e6_seed43` writes to its own output subdirectory; the underlying `_SCENARIO_IDENTITY_KEYS` defect is named in a comment and left as a Deferred Idea. |
| T-19.4-SC | Tampering | package installs | accept | No packages are installed in this phase. |
</threat_model>

<verification>
- `bash -n experiments/rerun_19_4.sh` and `bash -n experiments/prelaunch_gate.sh` both exit 0
- `python -m pytest tests/unit/test_rerun_gates.py -x -q` exits 0 with at least 27 tests
- Three dry-run scenarios (full, kill-mid-stage, resume) all behave as specified
- `python -m pytest tests/ -m "not slow" -q` exits 0
</verification>

<success_criteria>
- SC-7 (script half): the queue encodes the risk-first order so e6 and e4 both complete inside 4 h,
  the abort protocol is pre-committed in the script header, and a cross-stage `git_sha` mismatch
  is a hard failure
- The prelaunch gate expects exactly the {e4, e6} archive set
- Nothing was executed at production scale and nothing was launched detached
</success_criteria>

<output>
Create `.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-08-SUMMARY.md` when done.
</output>
