---
phase: 19.4-grid-family-clearance-floor-fix
plan: 07
type: execute
wave: 6
depends_on: ["19.4-06"]
files_modified:
  - .planning/MANUSCRIPT-FINDINGS.md
  - .planning/knowledge-base.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [GRID-01, GRID-02, GRID-03, GRID-04, GRID-05]
must_haves:
  truths:
    - "The two deferred verification steps are RECORDED as deferred, with what each would have proven -- not quietly dropped (D-19.4-05)"
    - "MF-08's E4/E6 subsection reflects what is true after the fix, since it was written against the pre-fix diagnosis"
    - "E4 and E6 still carry NO accuracy claim -- this phase makes a seed band obtainable, it does not measure one (D-19.3-17)"
    - "The durable lesson lands in .planning/knowledge-base.md, which is tracked, rather than only in a phase document"
    - "GRID-01..05 are ticked and the ROADMAP Phase 19.4 entry is closed with its real plan count"
  artifacts:
    - path: ".planning/MANUSCRIPT-FINDINGS.md"
      provides: "MF-08's E4/E6 subsection updated against the delivered fix"
      contains: "MF-08"
    - path: ".planning/knowledge-base.md"
      provides: "the durable lesson: a per-scenario constraint enforced against an import-time constant"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 19.4 entry citing GRID-01..05 as Complete"
      pattern: "GRID-0"
---

<objective>
Close the phase honestly: record what was deliberately NOT run and what it would have proven,
correct the manuscript-findings entry that was written against the pre-fix picture, bank the
lesson where it is tracked, and tick the trackers after a human has checked the honesty of all of
it.

Purpose: D-19.4-05 defers two verification steps worth ~7 hours on schedule grounds, and states
that if they are deferred they must be recorded with what they would have proven — not quietly
dropped. The cost of deferring is specific and namable: the `n_cameras` axis is exercised only by
construction, never end to end.

Output: a deferral record, an updated MF-08, a knowledge-base entry, ticked requirements, and a
closed roadmap entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<unattended_execution_boundary>
**Task 2 of this plan is the phase's single expected end of unattended execution. Reaching it is
SUCCESS, not failure.**

`workflow.auto_advance` is `false`, so it genuinely HALTS — it is not rubber-stamped. Task 1 is
autonomous and may run without a human. Task 2 is an honesty judgement about what this phase did
and did not establish, and no test can make it.

**State of the phase when it pauses at Task 2:**
- The fix is landed, the seed-42 sweep is reproduced and compared, and every gate is green.
- The deferral record, the MF-08 update and the knowledge-base entry are written.
- **Trackers are NOT yet updated.** GRID-01..05 are still unticked in `.planning/REQUIREMENTS.md`
  and ROADMAP.md's Phase 19.4 entry still reads "Plans: TBD".
- The closing commit has not been made.

**What to look at first when returning:**
1. The deferral record — does it name what steps 6 and 7 would have proven, or does it read as a
   justification for not running them?
2. MF-08's E4/E6 subsection — does any sentence now claim accuracy for E4 or E6? None has a
   measured seed band, so no such claim is supportable however plausible it looks.
3. Whether anything in this phase's documents quotes an optimality figure beyond one significant
   figure (there should be none).
</unattended_execution_boundary>

<context>
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-CONTEXT.md
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-06-SUMMARY.md
@.planning/MANUSCRIPT-FINDINGS.md
@.planning/ROADMAP.md

<interfaces>
What MF-08's E4/E6 subsection says today (corrected at 19.3's checkpoint, commit `0054cd9`):
    - "E4 and E6 CANNOT be seed-swept as shipped" -> corrected to: E6 can; 29 of seeds 0-499
      clear both draws.
    - "every configuration builds its own floor" -> corrected to: the floor is invariant to layout
      and spacing (180 arrays); it varies with `n_cameras`.
    - "the `depth_range=None` fix is verified inert at seed 42" -> corrected to: inert for the
      CALIBRATION scene only; it shifts the HOLDOUT scene by up to 0.469 mm.
    - E6's verdict: no accuracy claim. Reason: no band has been measured, not that none can exist.

What this phase changes about those statements:
    - "29 of 500 seeds are legal" becomes a statement about the PRE-fix code. Post-fix, all seeds
      are legal by construction — proven over 13,500 scenarios.
    - `depth_range=None` remains non-inert; the delivered fix is the `max()` form, and its
      inertness is now proven on BOTH scenes at pose level AND end to end at seed 42.
    - E6's and E4's accuracy verdict is UNCHANGED: still no claim.

What this phase does NOT change:
    - E4 and E6 still have no measured seed band, so D-19.3-17 still forbids any accuracy claim
      for either. A band is now OBTAINABLE (~85 min per seed for E6) but measuring one is separate
      work, explicitly deferred by CONTEXT.md.
    - The two seed-fragile spots 19.3 surfaced (`scale/double_scale` optimality on the intrinsic
      pass; `layout/line` parameter recovery spreading ~4x) are real, are not this phase's to fix,
      and are recorded so they are not rediscovered as new.

Tracking-file facts:
    `./CLAUDE.md` and all of `.claude/` are gitignored — anything written there is local-only.
    Durable, shareable project lessons go in `.planning/knowledge-base.md`, which IS tracked.
    The milestone already cuts v2.0.0 from Phase 19.3's `feat!:`. This phase adds no breaking
    public-API change, so its closing commit needs no `!` scope.
</interfaces>

**Never quote an optimality figure beyond one significant figure.**

**Anti-pattern 7 is blocking and it is the lesson that produced this phase.** Do not let any
document written here turn a single-seed observation into a general effect — including in the
other direction. "The fix works at seed 42" is a single-seed observation; what makes the legality
claim general is the 13,500-scenario sweep, not the seed-42 run.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the deferrals, update MF-08, and bank the lesson</name>
  <read_first>
    - .planning/MANUSCRIPT-FINDINGS.md MF-08 § E4/E6 in full (the four corrected statements)
    - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-06-SUMMARY.md (the comparison table and the run's status counts)
    - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-CONTEXT.md (D-19.4-05's deferral table and its "what deferring costs, stated plainly" paragraph; the Deferred Ideas section)
    - .planning/knowledge-base.md (its section structure and the level of detail existing entries carry)
  </read_first>
  <files>.planning/MANUSCRIPT-FINDINGS.md, .planning/knowledge-base.md</files>
  <action>
    **Write the deferral record** into this plan's SUMMARY as a first-class section, not a
    footnote. For each deferred step, name the cost, what it would have proven, and why the
    substitute evidence is or is not equivalent:

    - **Step 6 — E6 at seeds 43 and 47 (~2.5-5 h).** Would have proven that a fixed sweep
      CONVERGES at a previously-failing seed, not merely that it runs. Substitute evidence: 19.3
      ran E6 at seeds 62 and 28 — both naturally legal, no fix needed — and both returned
      `{'ok': 14}` with guard count 0 and `reconstruction_rmse_mm` within ~5% of seed 42
      everywhere. That is n=3 including the published seed. **It bounds the risk; it does not
      establish a band**, and seeds 43 and 47 specifically have never completed a single E6
      configuration. State that plainly.
    - **Step 7 — E4 grid at a non-42 seed (~2-2.5 h).** Would have proven the `n_cameras` axis end
      to end. **What deferring costs, in one sentence:** the `n_cameras` axis — the one axis the
      diagnosis measured as genuinely floor-moving, differing across {8,12,16} for 11 of 23 probed
      seeds — is exercised only by construction, never end to end, and E4 has up to six legality
      draws per seed. The 13,500-scenario sweep and plan 04's three-camera-count regression cover
      construction; nothing covers the solve.
    - Record that D-19.4-06's fail-fast makes both steps far cheaper to ATTEMPT later — a broken
      fix now surfaces in seconds rather than after 74-84 minutes — so if the calendar loosens,
      step 6 is the first thing to add back. The deferral stands on schedule grounds
      (2026-08-21 deadline, Phase 21's dataset regeneration still ahead), not on technical ones.

    **Update MF-08's E4/E6 subsection.** Add a dated note (2026-08-04, Phase 19.4) recording:
    the defect is fixed; the "29 of 500 seeds are legal" figure is now a statement about the
    pre-fix code and post-fix every seed is legal by construction, proven over 13,500 scenarios;
    the `depth_range=None` non-inertness finding STANDS and is why the delivered fix is the
    `max()` form; and the seed-42 published sweep was reproduced end to end with the diff confined
    to two recording-only columns. Point at `19.4-DECLARED-DIFF.md` and the plan-06 comparison
    table.

    **Do not add an accuracy claim for E4 or E6.** Neither has a measured seed band. D-19.3-17 is
    non-negotiable and this project has paid for it three times. Say explicitly in the note that a
    band is now obtainable and remains unmeasured, and that the verdict is therefore unchanged.

    **Write the lesson into `.planning/knowledge-base.md`**, which is tracked, in the form the
    file's existing entries use. The lesson is not "E6 was broken"; it is the defect class: *a
    per-scenario geometric constraint enforced against a module-level constant derived from one
    particular scenario*. Include the three tells that identify it anywhere else in the codebase —
    a module-level `CONST = some_function(seeded_input)`; a guard that re-derives per call while
    its comparison value is frozen at import; and a "verified inert" claim that covered only one of
    two construction sites. Also record that the guard's own suggested remedy was wrong, and why a
    remedy that a guard recommends still needs measuring.
  </action>
  <acceptance_criteria>
    - The SUMMARY contains a deferral section naming both steps, their costs, and what each would have proven.
    - MF-08 carries a dated Phase 19.4 note; `grep -c "19.4" .planning/MANUSCRIPT-FINDINGS.md` is at least 1.
    - Source assertion: no sentence added to MF-08 asserts accuracy for E4 or E6; the note states the verdict is unchanged and why.
    - `.planning/knowledge-base.md` gains an entry describing the defect CLASS with its three tells, not just this incident.
    - No optimality figure anywhere in the added text is quoted beyond one significant figure.
    - No file under the manuscript tree is modified — it is read-only from this repo.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "19.4" .planning/MANUSCRIPT-FINDINGS.md && grep -qi "clearance" .planning/knowledge-base.md</automated>
  </verify>
  <done>The deferrals are recorded with what they would have proven, MF-08 reflects the delivered fix without gaining an unsupported accuracy claim, and the defect class is banked in the tracked knowledge base.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review the honesty of the close, then tick the trackers</name>
  <action>Walk the user through five honesty checks on what this phase claims and does not claim, then, after approval, tick GRID-01..05 in REQUIREMENTS.md, close the ROADMAP Phase 19.4 entry with its real plan count, and make the closing commit. THIS IS THE PHASE'S SINGLE EXPECTED END OF UNATTENDED EXECUTION — halting here is the designed outcome, not a failure.</action>
  <what-built>
    A grid-family depth range derived per scenario from its own `water_zs`; a published-geometry
    anchor renamed so it cannot be misread as a floor; E6's scale axis moved onto a relative extent
    factor with the baseline extent pinned; fail-fast on both long runs with an explicit
    `--no-fail-fast`; a seed-42 pose anchor and an exact-equality gate; a 13,500-scenario legality
    sweep in the fast suite; and a reproduced seed-42 E6 sweep whose diff against the committed CSV
    is confined to two recording-only columns declared in advance.

    Trackers are deliberately still unticked and the closing commit is unmade — both wait on this
    approval.
  </what-built>
  <how-to-verify>
    This one is manual because it is a judgement about what the evidence supports, which no test
    can make.

    1. **The deferrals.** Read the deferral section. Does it name what steps 6 and 7 would have
       proven, and what deferring them costs — specifically that the `n_cameras` axis is exercised
       only by construction, never end to end? Or does it read as a justification for skipping
       them? D-19.4-05 requires the former.
    2. **No accuracy claim.** Read MF-08's updated E4/E6 note. Confirm no sentence claims accuracy
       for E4 or E6. Neither has a measured seed band; a band is now obtainable and remains
       unmeasured. D-19.3-17 has been paid for three times.
    3. **The generality of the legality claim.** Confirm the claim "legal at any seed" is sourced
       to the 13,500-scenario sweep, not to the seed-42 run. Anti-pattern 7 is the anti-pattern
       that produced this phase, and it applies to this phase's own conclusions.
    4. **The declared diff held.** Read plan 06's comparison table. Confirm exactly two columns
       changed, on exactly twelve rows, and that the declaration was written and committed before
       the run — not after.
    5. **The anchor was never touched.** Confirm `tests/data/grid_seed42_pose_anchor.csv` has not
       been modified since plan 01 committed it (`git log --follow` on that path shows one commit).
       If a later plan regenerated it, the byte-identity gate proved nothing and that is a blocking
       finding.

    After approval, update the trackers:
    - Tick GRID-01..05 in `.planning/REQUIREMENTS.md` and flip their traceability rows from
      `Pending` to `Complete`.
    - Update `.planning/ROADMAP.md`'s Phase 19.4 entry: replace "**Requirements**: TBD" with
      GRID-01..05, replace "**Plans**: TBD" with the real count, and write the plan list with one
      line per plan and its wave, following Phase 19.3's format. Note against success criterion 4
      that its end-to-end form was DEFERRED by D-19.4-05, with the automated construction-level
      substitute named — do not tick it as if it were delivered as written.
    - Update the Phase 19.4 row wherever the roadmap carries a completion table.

    The closing commit is `feat(19.4):` — **no `!` scope**. The milestone already cuts v2.0.0 from
    Phase 19.3's breaking change; this phase touches only `experiments/` and adds no breaking
    public API. Never hand-edit the version or the CHANGELOG.
  </how-to-verify>
  <acceptance_criteria>
    - All five review points confirmed and recorded in the SUMMARY.
    - GRID-01..05 are ticked in `.planning/REQUIREMENTS.md` and their traceability rows read `Complete`.
    - `.planning/ROADMAP.md`'s Phase 19.4 entry names GRID-01..05, carries the real plan count and plan list, and marks success criterion 4 as deferred with its substitute named.
    - The closing commit is `feat(19.4):` with no `!`.
    - `tests/data/grid_seed42_pose_anchor.csv` has exactly one commit in its history.
  </acceptance_criteria>
  <resume-signal>Type "approved" to close the phase, or name what needs revising in the deferral record or the MF-08 note.</resume-signal>
</task>

</tasks>

<verification>
- `PYTHONPATH="$(pwd)" ~/anaconda3/envs/AquaCal/python.exe -m pytest tests/ -q` UNFILTERED -> 0 failures.
- `git log --follow --oneline -- tests/data/grid_seed42_pose_anchor.csv` -> exactly one commit.
- GRID-01..05 ticked in `.planning/REQUIREMENTS.md`; Phase 19.4 closed in `.planning/ROADMAP.md`.
- No file under the manuscript tree modified.
- **No long run in this plan.** Do NOT launch E4, E6, or any sweep here — plan 06 owns all runs and
  the deferred steps 6 and 7 are out of scope by decision, not by omission.
- Security: no packages installed; N/A per `19.4-RESEARCH.md` § Security Domain.
- Commit as `feat(19.4):` — no `!` scope.
</verification>

<success_criteria>
- Both deferred verification steps are recorded with their cost and what they would have proven.
- MF-08's E4/E6 subsection reflects the delivered fix and gains no accuracy claim.
- The defect class is banked in `.planning/knowledge-base.md` with the three tells that identify it.
- The "legal at any seed" claim is sourced to the 13,500-scenario sweep, not to one seed.
- GRID-01..05 ticked; ROADMAP Phase 19.4 closed with its real plan count and criterion 4 marked deferred.
- The seed-42 pose anchor was never regenerated.
</success_criteria>

<output>
Create `.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-07-SUMMARY.md` when done.
Record: the deferral section verbatim, the MF-08 note as written, the knowledge-base entry's
heading, the five checkpoint review outcomes, and the final GRID-01..05 status.
</output>
