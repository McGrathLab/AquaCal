---
phase: 19.4-grid-family-clearance-floor-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/data/grid_seed42_pose_anchor.csv
  - tests/unit/test_grid_seed42_pose_inertness.py
  - experiments/_grid_pose_anchor_gen.py
autonomous: true
requirements: [GRID-02]
must_haves:
  truths:
    - "GRID-01..05 exist in .planning/REQUIREMENTS.md, unticked, in a new Grid-Family Clearance Floor section, with completion-table rows and an updated coverage count"
    - "A committed anchor records every rvec/tvec of every grid-family seed-42 scene the fix can touch, captured from the UNFIXED tree"
    - "A suite test compares live scene construction against that anchor by EXACT equality and is green on the unfixed tree"
    - "The anchor covers BOTH the calibration scene (seed) and the holdout scene (seed + 1_000_000) for every cell -- the holdout half is the half the naive fix moves"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "GRID-01..05 requirement IDs and traceability rows"
      contains: "GRID-01"
    - path: "tests/data/grid_seed42_pose_anchor.csv"
      provides: "pre-fix rvec/tvec for every seed-42 grid-family scene"
      contains: "scenario_key"
    - path: "tests/unit/test_grid_seed42_pose_inertness.py"
      provides: "exact-equality inertness gate against the anchor"
      contains: "assert_array_equal"
    - path: "experiments/_grid_pose_anchor_gen.py"
      provides: "the regeneration script, so the anchor is reproducible not hand-made"
      contains: "def main"
  key_links:
    - from: "tests/unit/test_grid_seed42_pose_inertness.py"
      to: "tests/data/grid_seed42_pose_anchor.csv"
      via: "file read + np.testing.assert_array_equal"
      pattern: "grid_seed42_pose_anchor"
    - from: "tests/unit/test_grid_seed42_pose_inertness.py"
      to: "experiments.e4_benchmark_grid.build_grid_scenario"
      via: "live reconstruction of each anchored scene"
      pattern: "build_grid_scenario"
---

<objective>
Establish the gate that protects the published numbers, BEFORE any source changes. Capture every
seed-42 grid-family scene's board poses from the current (unfixed) tree into a committed anchor,
and write the exact-equality test that will hold the fix to it. Also write this phase's
requirement IDs into `.planning/REQUIREMENTS.md`.

Purpose: bit-inertness at seed 42 is only provable against a record of what shipped. That record
must be taken before the code moves. `depth_range=None` — the guard's own suggested remedy — is
bit-identical for the calibration scene and shifts the HOLDOUT scene by up to 0.469 mm; a proof
that covers only the calibration half would have approved it (D-19.4-02, and an earlier
"verified inert" note that had to be retracted).

Output: GRID-01..05 in REQUIREMENTS.md, a committed pose anchor, a regeneration script, and a
test that is green on the unfixed tree and becomes the fix's gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-CONTEXT.md
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-RESEARCH.md
@.planning/debug/e6-seed-locked-clearance-floor.md
@.planning/REQUIREMENTS.md

<interfaces>
Contracts the executor needs. Do NOT go exploring for these.

From experiments/e4_benchmark_grid.py (CURRENT, unfixed — do not change it in this plan):
    GRID_DEPTH_RANGE: tuple[float, float]      # (1.181852154281008, 2.0), frozen at import
    GRID_LAYOUT, GRID_SPACING, GRID_HEIGHT_ABOVE_WATER, GRID_BOARD_CONFIG, GRID_N_WATER
    DECLARED_CELLS                              # the nine (n_cameras, n_frames) cells
    def build_grid_scenario(n_cameras, n_frames, seed, *, layout=GRID_LAYOUT,
        depth_range=None, xy_extent=None, spacing=None, height_above_water=None,
        n_water=..., name=...) -> SyntheticScenario
    def default_xy_extent_for_layout(n_cameras, layout, spacing) -> float

From experiments/e6_generalization_sweep.py (CURRENT, unfixed):
    BASELINE_N_CAMERAS = 12; BASELINE_N_FRAMES = 100; BASELINE_LAYOUT = "grid"
    LAYOUT_AXIS_VALUES = ["grid", "ring", "line"]
    SCALE_AXIS_VALUES: list[(label, depth_range|None, xy_extent|None, spacing|None)]
        # ("half_scale", (1.181852154281008, 1.590926077140504), 0.5*xy, 0.5*spacing)
        # ("default",    None, None, None)
        # ("double_scale", (1.181852154281008, 2.818147845718992), 2.0*xy, 2.0*spacing)

From aquacal.config.schema:
    SyntheticScenario.board_poses -> list[BoardPose]; BoardPose has .rvec (3,), .tvec (3,)

House pattern for a frozen anchor: tests/unit/test_datasets.py
    `_parse_pose_csv` + `np.testing.assert_array_equal` — EXACT equality only, never
    `pytest.approx`, never a tolerance. The anchor is regenerated deliberately and the
    regeneration is recorded; it is never edited to make a test pass.
</interfaces>

**The holdout seed is `seed + 1_000_000`.** Both E4's `run_grid_cell` and E6's
`run_configuration` build a SECOND scenario at that seed with its own camera array and its own
`max(water_zs)`. Anchoring only the calibration scene is the exact mistake this plan exists to
prevent.

**Anti-pattern 3 is blocking.** This anchor is a byte-identity gate. If a later plan fails
against it, **the fix moves — never the tolerance, and never the anchor.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write GRID-01..05 into REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md lines 124-195 (the `### Scenario Geometry` block — GEOM-01..06 is the format precedent) and lines 315-345 (the traceability table and coverage block)
    - .planning/ROADMAP.md § Phase 19.4 (goal, four success criteria, the two traps)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Insert a new `### Grid-Family Clearance Floor` section immediately AFTER the
    `### Scenario Geometry` block's closing "**Deliberately not requirements here.**" paragraph
    and BEFORE `## Sequencing Constraints`. Follow GEOM-01..06's shape exactly: a dated
    provenance line naming the source, a short problem paragraph, then `- [ ] **GRID-NN**:`
    bullets. Leave every box UNTICKED — plan 19.4-07 ticks them.

    Provenance line: added 2026-08-04 with the insertion of Phase 19.4; source
    `.planning/debug/e6-seed-locked-clearance-floor.md` (status `diagnosed`); decisions locked as
    D-19.4-01..08 in that phase's `19.4-CONTEXT.md`.

    Problem paragraph: `GRID_DEPTH_RANGE[0]` is a per-scenario geometric constraint enforced
    against an import-time constant derived from one particular scenario — the seed-42 12-camera
    array. `max(water_zs)` is a pure function of `(seed, n_cameras)`, so the frozen value is legal
    only for arrays whose deepest interface does not exceed the baseline's: **29 of seeds 0-499
    (5.8%)** clear both of E6's two independent draws. Seed 42 is the anchor, not a robust choice —
    its calibration floor equals the constant by construction and its holdout floor clears it by
    ~0.5 mm. The deliverable is legality at any seed with **every published number unmoved**.

    The five requirements, written as observable outcomes (not tasks):

    - **GRID-01**: No module-level constant is used as a clearance floor. Every grid-family
      scenario derives its depth range from the `water_zs` that scenario was built with, at
      construction time, through ONE shared helper that takes `water_zs` as a parameter and never
      rebuilds a camera array of its own (D-19.4-01, D-19.4-07). The helper resolves
      `max(published_anchor_floor, board_clearance_floor(...))` (D-19.4-02); `depth_range=None` as
      the remedy is forbidden, because it is bit-identical for the calibration scene but shifts
      the holdout scene by up to **0.469 mm**. The published-geometry anchor constant is retained
      as the `max()`'s lower bound, renamed and re-documented so no new consumer reads it as
      "the floor" — deleting it would break seed-42 inertness.
    - **GRID-02**: Seed-42 bit-inertness is proven for BOTH the calibration and the holdout scene,
      on all poses' `rvec` and `tvec` by exact equality, across E4's `{8,12,16} x {50,100,200}`
      cells and every distinct E6 scene including both scale values — against an anchor captured
      from the pre-fix tree, as a test in the suite.
    - **GRID-03**: `build_grid_scenario` never raises across 500 seeds x `{8,12,16}` cameras x
      `{grid,ring,line}` layouts x 3 spacings — 13,500 scenarios — as a non-`slow` test in the
      suite. Its absence is why this shipped; legality is asserted exhaustively, never
      demonstrated at one seed (anti-pattern 7).
    - **GRID-04**: E4 and E6 abort on the FIRST cell/configuration recording `status="failed"`,
      exit non-zero, and print the configuration key, the exception, and — for a clearance
      rejection — both the supplied minimum and the derived floor. Opt-out is explicit
      (`--no-fail-fast`), preserving 19.2-21's pre-authorised recorded failure for E4's 16x200
      cell (D-19.4-06). Today all 14 configurations can fail and the process still exits 0; that
      cost 74 and 84 minutes at seeds 47 and 50.
    - **GRID-05**: The depth-range payload change is recorded, not discovered. E6's
      `_SCENARIO_IDENTITY_KEYS` needs no migration — all twelve committed `e6_configs/*.json`
      still match at seed 42 (D-19.4-03) — and `E6_COLUMNS`' `depth_range_min`/`depth_range_max`
      are populated from the RESOLVED per-scenario range, with the resulting null-to-value diff on
      baseline rows declared in advance, bounded to those two columns, and confirmed by a seed-42
      byte-comparison against the committed `generalization_sweep.csv` (D-19.4-04).

    Then add `| GRID-01 | Phase 19.4 | Pending |` through `GRID-05` to the traceability table,
    immediately after the `GEOM-06` row and before `INDEX-01`. Update the coverage block from
    46/46 to **51 total / 51 mapped / 0 unmapped**. Append a `*Last updated: 2026-08-04 — inserted
    Phase 19.4 ...*` line following the existing footer convention, naming the source diagnosis
    and noting that this phase adds no breaking public-API change (the milestone already cuts
    v2.0.0 from Phase 19.3).
  </action>
  <acceptance_criteria>
    - `grep -c "GRID-0" .planning/REQUIREMENTS.md` returns at least 11 (5 bullets + 5 table rows + section prose).
    - All five bullets are UNTICKED: `grep -c "\- \[x\] \*\*GRID-" .planning/REQUIREMENTS.md` returns 0.
    - The traceability table has exactly one row per GRID-01..05, each reading `Phase 19.4 | Pending`.
    - The coverage block reads 51 total, 51 mapped, 0 unmapped.
    - The new section sits between `### Scenario Geometry` and `## Sequencing Constraints`.
    - Source assertion: no GRID bullet is phrased as a task ("add", "implement", "refactor") — each
      states a condition that is either true or false of the delivered tree.
  </acceptance_criteria>
  <verify>
    <automated>~/anaconda3/envs/AquaCal/python.exe -c "import re,sys,pathlib; t=pathlib.Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); ids=set(re.findall(r'GRID-0[1-5]',t)); rows=len(re.findall(r'\|\s*GRID-0[1-5]\s*\|\s*Phase 19\.4\s*\|\s*Pending\s*\|',t)); bullets=len(re.findall(r'- \[ \] \*\*GRID-0[1-5]\*\*',t)); sys.exit(0 if len(ids)==5 and rows==5 and bullets==5 and '51 total' in t else 1)"</automated>
  </verify>
  <done>GRID-01..05 exist as unticked outcome-shaped requirements with traceability rows and a corrected coverage count.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the pre-fix seed-42 pose anchor from the UNFIXED tree</name>
  <read_first>
    - experiments/e4_benchmark_grid.py lines 230-262 (GRID_DEPTH_RANGE and its baseline array), 490-620 (build_grid_scenario), and the `DECLARED_CELLS` definition
    - experiments/e6_generalization_sweep.py lines 130-185 (`_BASELINE_EXTENT_ABOVE_FLOOR`, `_scaled_depth_range`, `SCALE_AXIS_VALUES`) and `build_axis_configurations`
    - tests/unit/test_datasets.py `_parse_pose_csv` and `test_generate_real_rig_trajectory_matches_frozen_anchor` (the house anchor pattern)
  </read_first>
  <files>experiments/_grid_pose_anchor_gen.py, tests/data/grid_seed42_pose_anchor.csv</files>
  <action>
    **Do not modify `experiments/e4_benchmark_grid.py` or `experiments/e6_generalization_sweep.py`
    in this plan.** The anchor's whole value is that it was taken from the tree as it shipped. If
    either file has already been edited, STOP and report — do not regenerate from a modified tree.

    Write `experiments/_grid_pose_anchor_gen.py`, a small standalone regeneration script (a
    `main()` plus a `if __name__ == "__main__":` guard, Google-style docstrings, Ruff-clean). It
    enumerates every distinct grid-family seed-42 scene the fix can touch and writes one CSV row
    per board pose.

    The scene set, and it must be enumerated FROM the modules' own constants rather than
    hand-listed, so it cannot drift:

    1. **E4 cells** — for each `(n_cameras, n_frames)` in `e4.DECLARED_CELLS` (the nine
       `{8,12,16} x {50,100,200}` cells), call `build_grid_scenario(n_cameras, n_frames, seed)`
       for `seed = 42` AND for `seed = 1_000_042`. That mirrors `run_grid_cell`'s own two calls
       exactly — 18 scenarios. E4 sweeps `n_cameras`, which is the axis that MOVES the derived
       floor (11 of 23 probed seeds differ across {8,12,16}), so all three counts are load-bearing.
    2. **E6 distinct scenes** — for each entry produced by `e6.build_axis_configurations()`,
       reduce to the distinct `config_key` set and skip any whose scene fields duplicate one
       already captured (the `index` axis varies `n_water` only and builds an identical scene to
       the baseline; capture the baseline once). For each remaining config call
       `build_grid_scenario` with that config's `n_cameras`, `layout`, `depth_range`, `xy_extent`,
       `spacing` at `n_frames = e6.BASELINE_N_FRAMES`, again for `seed = 42` and
       `seed = 1_000_042`. This must include `layout/ring`, `layout/line`, `scale/half_scale` and
       `scale/double_scale` — the two scale values are the entries that pass an EXPLICIT
       `depth_range` and are therefore invisible to any fix that only touches the `None` branch.

    Emit `tests/data/grid_seed42_pose_anchor.csv` with a header row and columns
    `scenario_key,frame,rvec_0,rvec_1,rvec_2,tvec_0,tvec_1,tvec_2`. `scenario_key` must be a
    stable, self-describing string that a reader can map back to a construction call without
    running anything — e.g. `e4_cam12_frames100_calib`, `e4_cam16_frames200_holdout`,
    `e6_scale_half_scale_calib`. Write every float with `repr()` (full float64 round-trip
    precision), NOT formatted to fixed decimals — a truncated anchor is not a bit-identity anchor.
    Sort rows by `(scenario_key, frame)` so the file diffs deterministically.

    Run the script **synchronously in the foreground** with
    `PYTHONPATH="$(pwd)" ~/anaconda3/envs/AquaCal/python.exe -u experiments/_grid_pose_anchor_gen.py`.
    It builds scenes only — no calibration — and the diagnosis measures scene construction at
    0.593 ms each, so this completes in well under a minute. **Do not background it.**

    Record in the SUMMARY: the scenario count, the total row count, and the resolved
    `depth_range` each scene was built with (they should all be the frozen
    `(1.181852154281008, 2.0)` except the two scale entries).
  </action>
  <acceptance_criteria>
    - `git diff --stat experiments/e4_benchmark_grid.py experiments/e6_generalization_sweep.py` is EMPTY — the anchor came from the unmodified tree.
    - The CSV exists, has a header, and contains at least 26 distinct `scenario_key` values, each appearing in a `_calib` and a `_holdout` form.
    - Every E4 cell in `DECLARED_CELLS` appears; all three camera counts and all three frame counts are present.
    - `e6_scale_half_scale_*` and `e6_scale_double_scale_*` keys are present.
    - Source assertion: the generator enumerates from `e4.DECLARED_CELLS` and `e6.build_axis_configurations()`; `grep -c "DECLARED_CELLS" experiments/_grid_pose_anchor_gen.py` is at least 1 and no cell tuple is hand-written as a literal list.
    - Round-trip assertion: re-reading the CSV with `float()` and re-running the generator produces bit-identical values (run the generator twice, diff the outputs).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -z "$(git diff --name-only experiments/e4_benchmark_grid.py experiments/e6_generalization_sweep.py)" && PYTHONPATH="$(pwd)" ~/anaconda3/envs/AquaCal/python.exe -c "import csv,sys; rows=list(csv.DictReader(open('tests/data/grid_seed42_pose_anchor.csv'))); keys={r['scenario_key'] for r in rows}; sys.exit(0 if len(keys)>=26 and any('scale_half' in k for k in keys) and any('holdout' in k for k in keys) and len(rows)>2000 else 1)"</automated>
  </verify>
  <done>A reproducible generator and a committed anchor record every seed-42 grid-family scene's poses at full float64 precision, taken from the unmodified tree, calibration and holdout alike.</done>
</task>

<task type="auto">
  <name>Task 3: Write the exact-equality inertness gate and prove it green pre-fix</name>
  <read_first>
    - tests/data/grid_seed42_pose_anchor.csv (the header and a few rows, to fix the parse contract)
    - tests/synthetic/test_guard_inertness.py module docstring (the house framing for an inertness proof and its "this module must not be edited to add a tolerance" clause)
    - tests/unit/test_datasets.py `_parse_pose_csv`
  </read_first>
  <files>tests/unit/test_grid_seed42_pose_inertness.py</files>
  <behavior>
    - For every `scenario_key` in the anchor, reconstructing that scene live and stacking
      `[rvec, tvec]` per pose yields an array EXACTLY equal to the anchor's rows, same shape,
      compared with `numpy.testing.assert_array_equal`. Never `pytest.approx`, never `atol`.
    - The test is parametrized per `scenario_key`, so a failure names WHICH scene moved and
      whether it was the calibration or the holdout half.
    - A guard test asserts the anchor itself is non-vacuous: at least 26 scenario keys, every key
      present in both `_calib` and `_holdout` form, and every E4 declared cell represented.
      Without it, an emptied anchor would make the whole gate pass silently.
    - The live reconstruction is driven by parsing `scenario_key` back into its construction
      arguments through the SAME enumeration the generator uses — import the enumeration from
      `experiments/_grid_pose_anchor_gen.py` rather than re-deriving it, so the test and the
      anchor cannot describe different scene sets.
  </behavior>
  <action>
    Create `tests/unit/test_grid_seed42_pose_inertness.py`. Give it a module docstring in the
    house form: state that this is GRID-02's gate, that it protects every published E4 and E6
    number, that `depth_range=None` was measured to move the HOLDOUT scene by up to 0.469 mm and
    would have been approved by a calibration-only proof, and — verbatim in substance — that **if
    this module fails, the fix moves; this module is never edited to add a tolerance and the
    anchor is never regenerated to accommodate a change** (anti-pattern 3, blocking).

    Refactor the generator's scene enumeration into an importable function (e.g.
    `iter_anchor_scenarios() -> Iterator[tuple[str, SyntheticScenario]]` or, better, a
    `(key, build_kwargs)` generator so the test controls construction) and have BOTH the generator
    and this test consume it. Do not duplicate the enumeration.

    Mark nothing `slow`. Scene construction only, no calibration; the whole module must run in
    seconds.

    Run it now, on the unfixed tree, and confirm it passes. **A green run here is the point** — it
    establishes that the anchor faithfully describes what shipped. If it does not pass on the
    unfixed tree, the anchor or the enumeration is wrong; fix that, do not weaken the assertion.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_grid_seed42_pose_inertness.py -q` passes on the UNFIXED tree with at least 26 parametrized cases.
    - Source assertion: `grep -c "approx\|atol\|rtol\|allclose" tests/unit/test_grid_seed42_pose_inertness.py` returns 0.
    - Source assertion: `grep -v '^#' tests/unit/test_grid_seed42_pose_inertness.py | grep -c "assert_array_equal"` is at least 1.
    - The non-vacuity guard test exists and fails if the anchor is truncated (demonstrate by pointing it at an empty temp file in the test itself, or by asserting explicit minimum counts).
    - `git diff --name-only experiments/e4_benchmark_grid.py experiments/e6_generalization_sweep.py` is still empty.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH="$(pwd)" ~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_grid_seed42_pose_inertness.py -q</automated>
  </verify>
  <done>The inertness gate exists, is exact-equality, covers calibration and holdout for every anchored scene, and is green against the code as it shipped.</done>
</task>

</tasks>

<verification>
- `PYTHONPATH="$(pwd)" ~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_grid_seed42_pose_inertness.py -q` -> 0 failures on the UNFIXED tree.
- `PYTHONPATH="$(pwd)" ~/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_e4.py tests/unit/test_experiments_e6.py -q` -> unchanged from the phase's starting state (this plan changes no source).
- `git diff --name-only` lists only `.planning/REQUIREMENTS.md`, `tests/data/`, `tests/unit/test_grid_seed42_pose_inertness.py`, `experiments/_grid_pose_anchor_gen.py`.
- Security: no packages installed; `security_enforcement` N/A per `19.4-RESEARCH.md` § Security Domain (no auth, network, or untrusted-input surface).
- Commit as `test(19.4-01):`. No `!` scope — this phase adds no breaking public-API change.
</verification>

<success_criteria>
- GRID-01..05 are in REQUIREMENTS.md, unticked, outcome-shaped, with traceability rows and coverage 51/51/0.
- The anchor covers all nine E4 cells and every distinct E6 scene, calibration AND holdout, at full float64 precision.
- The anchor was generated from a tree with zero modifications to the two experiment modules.
- The inertness test is exact-equality, non-vacuous, parametrized per scene, and green pre-fix.
- No source file under `experiments/` (other than the new generator) or `src/` was touched.
</success_criteria>

<output>
Create `.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-01-SUMMARY.md` when done.
Record: the exact GRID-01..05 text as written, the scenario count and row count in the anchor,
the resolved `depth_range` each anchored scene was built with, and confirmation that
`git diff` on the two experiment modules was empty at capture time.
</output>
