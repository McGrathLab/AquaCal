---
phase: 19.4-grid-family-clearance-floor-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/archive/e4-2026-08-04-pre-interface-fix/README.md
  - experiments/archive/e4-2026-08-04-pre-interface-fix/benchmark_grid.csv
  - experiments/archive/e4-2026-08-04-pre-interface-fix/benchmark_grid.tex
  - experiments/archive/e6-2026-08-04-pre-interface-fix/README.md
  - experiments/archive/e6-2026-08-04-pre-interface-fix/generalization_sweep.csv
  - experiments/archive/README.md
autonomous: true
requirements: [SC-5]
must_haves:
  truths:
    - "A reader can diff a post-fix E4 or E6 artifact against a byte-for-byte pre-fix copy without git archaeology (D-19.4-10)"
    - "No archive directory is created for E7 — E7 is provably inert and archiving it would record nothing (CONTEXT.md CORRECTION)"
  artifacts:
    - path: "experiments/archive/e4-2026-08-04-pre-interface-fix/README.md"
      provides: "E4 pre-interface-fix provenance, five required sections"
      contains: "## Provenance"
    - path: "experiments/archive/e6-2026-08-04-pre-interface-fix/README.md"
      provides: "E6 pre-interface-fix provenance, five required sections"
      contains: "## Provenance"
    - path: "experiments/archive/README.md"
      provides: "Index rows for both new archive generations"
      contains: "pre-interface-fix"
  key_links:
    - from: "experiments/archive/e4-2026-08-04-pre-interface-fix/README.md"
      to: "experiments/results/benchmark.json environment.git_sha"
      via: "provenance sha read from the experiment's own sidecar, never from HEAD"
      pattern: "Producing commit|git_sha"
---

<objective>
Archive the pre-correction E4 and E6 artifacts under `experiments/archive/` before any code
change moves them (D-19.4-10).

Purpose: E4's and E6's committed numbers WILL move in this phase. The house convention (seven
existing archive generations, `e2-2026-07-30-pre-pnp-guard/` is the template) preserves the
"before" state as a single diffable artifact instead of forcing a reader to reconstruct one
commit's worth of `experiments/results/` per number.

Output: two archive directories with byte-for-byte artifact copies and provenance READMEs, plus
two new rows in `experiments/archive/README.md`'s Index table.

**E7 is explicitly NOT archived.** Per `19.4-CONTEXT.md` § CORRECTION, E7 runs the `"realistic"`
scenario, which resolves to `generate_real_rig_array()`'s frozen shared `WATER_Z` and never
reaches `generate_camera_array`. Archiving artifacts that this phase will reproduce byte-for-byte
records nothing. Do not create `experiments/archive/e7-2026-08-04-*`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-CONTEXT.md
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-PATTERNS.md
@CLAUDE.md

Shell is Git Bash (MINGW64). Paths are `/c/Users/...`, never `/mnt/c/...`. Commands run from the
project root with no `cd` prefix.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive E4's pre-interface-fix artifacts with a provenance README</name>
  <files>experiments/archive/e4-2026-08-04-pre-interface-fix/README.md, experiments/archive/e4-2026-08-04-pre-interface-fix/benchmark_grid.csv, experiments/archive/e4-2026-08-04-pre-interface-fix/benchmark_grid.tex</files>
  <read_first>
    - experiments/archive/e2-2026-07-30-pre-pnp-guard/README.md (the template D-19.4-10 names)
    - experiments/archive/e4-2026-08-02-pre-depth-fix/README.md (the previous E4 generation)
    - experiments/archive/README.md (the "five things" convention, lines 32-60)
    - experiments/results/benchmark.json (source of `environment.git_sha`)
  </read_first>
  <action>
    Create `experiments/archive/e4-2026-08-04-pre-interface-fix/` and copy
    `experiments/results/benchmark_grid.csv` and `experiments/results/benchmark_grid.tex` into it
    byte-for-byte (same two files the `e4-2026-08-02-pre-depth-fix/` generation copied).
    Do NOT copy `experiments/results/e4_cells/` — point at it with a `git show <sha>:<path>`
    instruction instead, matching the existing convention for files that trip
    `check-added-large-files` (`--maxkb=1000`) or `detect-secrets` (any
    `assemble_benchmark_record`-shaped JSON carries a 40-hex `environment.git_sha`).

    Write `README.md` with the five required sections in the exact order the template uses:
    `# E4 baseline — archived 2026-08-04, before the single-flat-interface fix`,
    `## Why this exists`, `## Provenance`, `## What is copied here, and what is not`,
    `## Why it is NOT under experiments/results/`, `## How to use it`.

    The `## Why this exists` section must state the defect precisely: `generate_camera_array`
    (`src/aquacal/datasets/synthetic.py:255-260`) applied its `height_variation` jitter to
    `water_z` — the world-frame Z of the water *surface* — while holding every camera at
    `C_z = 0`, giving the ground truth one water surface per camera. Measured displacement over
    31,680 corner observations: mean 1.42 px, max 6.33 px, against an E4/E6 reprojection RMS of
    ~0.4-0.9 px. State plainly that this figure is ONE seed and ONE trajectory — enough to
    establish the defect is real and dominant, NOT an effect size (blocking anti-pattern 7).

    The `## Provenance` section's producing commit MUST be read from
    `experiments/results/benchmark.json`'s `environment.git_sha` field — never from the archiving
    commit's HEAD. This sourcing rule is copied verbatim from the convention document.
  </action>
  <verify>
    <automated>test -f experiments/archive/e4-2026-08-04-pre-interface-fix/README.md &amp;&amp; test -f experiments/archive/e4-2026-08-04-pre-interface-fix/benchmark_grid.csv &amp;&amp; diff experiments/results/benchmark_grid.csv experiments/archive/e4-2026-08-04-pre-interface-fix/benchmark_grid.csv</automated>
  </verify>
  <acceptance_criteria>
    - `diff experiments/results/benchmark_grid.csv experiments/archive/e4-2026-08-04-pre-interface-fix/benchmark_grid.csv` produces no output and exits 0
    - `diff experiments/results/benchmark_grid.tex experiments/archive/e4-2026-08-04-pre-interface-fix/benchmark_grid.tex` exits 0
    - The README contains all five headings: `## Why this exists`, `## Provenance`, `## What is copied here, and what is not`, `## Why it is NOT under`, `## How to use it`
    - The 40-hex sha quoted under `## Provenance` equals `environment.git_sha` read from `experiments/results/benchmark.json` (assert by comparing the two strings, not by eye)
    - The README contains the string `1.42 px` AND an explicit one-seed/one-trajectory caveat
    - `experiments/archive/e7-2026-08-04-pre-interface-fix` does NOT exist
  </acceptance_criteria>
  <done>E4's pre-fix artifacts and provenance README exist under the new archive directory, byte-identical to the live results.</done>
</task>

<task type="auto">
  <name>Task 2: Archive E6's pre-interface-fix artifacts with a provenance README</name>
  <files>experiments/archive/e6-2026-08-04-pre-interface-fix/README.md, experiments/archive/e6-2026-08-04-pre-interface-fix/generalization_sweep.csv</files>
  <read_first>
    - experiments/archive/e6-2026-08-02-pre-depth-fix/README.md (the previous E6 generation)
    - experiments/results/e6_provenance.json (source of `environment.git_sha`)
    - experiments/archive/README.md (the "five things" convention)
  </read_first>
  <action>
    Create `experiments/archive/e6-2026-08-04-pre-interface-fix/` and copy
    `experiments/results/generalization_sweep.csv` into it byte-for-byte (the single file the
    `e6-2026-08-02-pre-depth-fix/` generation copied). Do NOT copy `experiments/results/e6_configs/`
    or `e6_provenance.json` — point at both with `git show <sha>:<path>`.

    Write `README.md` with the same five sections. Beyond the shared interface defect, `## Why this
    exists` must record E6's own two specific consequences: (a) `GRID_DEPTH_RANGE[0]` re-derives
    from 1.181852154 to 1.176215948 — a 5.636 mm drop equal to cam7's seed-42 jitter, the camera
    whose water surface sat deepest; (b) E6's seed-locking (legal at ~6% of seeds) is cured BY the
    interface fix, because post-fix `max(water_zs) == height_above_water` at every seed
    (D-19.4-15, measured over 3,000 draws: exactly one distinct derived floor, 1.176215948246).

    Read the producing sha from `experiments/results/e6_provenance.json`'s `environment.git_sha`,
    never from HEAD.
  </action>
  <verify>
    <automated>test -f experiments/archive/e6-2026-08-04-pre-interface-fix/README.md &amp;&amp; diff experiments/results/generalization_sweep.csv experiments/archive/e6-2026-08-04-pre-interface-fix/generalization_sweep.csv</automated>
  </verify>
  <acceptance_criteria>
    - `diff experiments/results/generalization_sweep.csv experiments/archive/e6-2026-08-04-pre-interface-fix/generalization_sweep.csv` exits 0 with no output
    - The README contains all five required headings
    - The README contains both `1.181852154` and `1.176215948`
    - The sha under `## Provenance` equals `environment.git_sha` in `experiments/results/e6_provenance.json`
    - `git status --porcelain experiments/results/` reports no modification to any live result file
  </acceptance_criteria>
  <done>E6's pre-fix artifact and provenance README exist under the new archive directory, byte-identical to the live result.</done>
</task>

<task type="auto">
  <name>Task 3: Append both generations to the archive index</name>
  <files>experiments/archive/README.md</files>
  <read_first>
    - experiments/archive/README.md (the `## Index` table, lines 20-30)
  </read_first>
  <action>
    Append exactly two rows to the `## Index` table in `experiments/archive/README.md`, matching
    the existing five-column shape `| Directory | Experiments covered | Archived | Precedes |
    Producing commit(s) |`. The `Precedes` cell for both reads: the single-flat-interface fix
    (D-19.4-09) — `generate_camera_array`'s `height_variation` jitter moved from `water_z` to
    `C_z`, so all cameras share one water surface. E6's row additionally names the
    `GRID_DEPTH_RANGE` re-derivation (D-19.4-12/15).

    Add no row for E7. Add no new convention text — the "five things" checklist is unchanged.
  </action>
  <verify>
    <automated>grep -c "pre-interface-fix" experiments/archive/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'e4-2026-08-04-pre-interface-fix' experiments/archive/README.md` returns 1
    - `grep -c 'e6-2026-08-04-pre-interface-fix' experiments/archive/README.md` returns 1
    - `grep -c 'e7-2026-08-04' experiments/archive/README.md` returns 0
    - Both new rows have exactly 5 pipe-delimited cells, matching the existing rows
    - `python -m pytest tests/unit/test_experiments_provenance.py -x` exits 0 (archived copies are outside `experiments/results/` and are not swept into the live-provenance gate)
  </acceptance_criteria>
  <done>The index lists both new archive generations and no E7 generation; the provenance gate test still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → `experiments/results/` | Live, manuscript-facing artifacts. This plan reads them and must not write them. |
| repo → `experiments/archive/` | Write-once historical record. An overwrite destroys the only "before" state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.4-01 | Tampering | `experiments/results/*` | mitigate | This plan copies FROM results, never TO them. Acceptance criterion asserts `git status --porcelain experiments/results/` is empty. |
| T-19.4-02 | Repudiation | archive README provenance sha | mitigate | The sha is read from the experiment's own `environment.git_sha` sidecar field, never from archiving-commit HEAD; acceptance asserts string equality against the sidecar. |
| T-19.4-03 | Information disclosure | `detect-secrets` on 40-hex `git_sha` | accept | Large/entropy-tripping files are referenced via `git show <sha>:<path>` rather than copied, per the existing convention. No hook bypass. |
| T-19.4-SC | Tampering | package installs | accept | This phase installs no packages (RESEARCH.md § Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `python -m pytest tests/unit/test_experiments_provenance.py -x` exits 0
- `git status --porcelain experiments/results/` produces no output
- Both archive READMEs satisfy the five-section convention
</verification>

<success_criteria>
- Two archive directories exist (`e4-` and `e6-2026-08-04-pre-interface-fix/`), each with a
  five-section README and byte-identical artifact copies
- No E7 archive directory exists
- `experiments/archive/README.md` gained exactly two index rows
- No live result file was modified
</success_criteria>

<output>
Create `.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-01-SUMMARY.md` when done.
</output>
