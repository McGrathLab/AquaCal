---
phase: 19.4-grid-family-clearance-floor-fix
plan: 01
subsystem: testing
tags: [provenance, archive, experiments, e4, e6, single-flat-interface]

# Dependency graph
requires: []
provides:
  - "Byte-for-byte pre-interface-fix archive of E4's benchmark_grid.csv/.tex"
  - "Byte-for-byte pre-interface-fix archive of E6's generalization_sweep.csv"
  - "Two new Index rows in experiments/archive/README.md"
affects: [19.4-code-plans, 19.4-09-verification-queue, MF-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Archive-before-change under experiments/archive/{exp}-{date}-{reason}/ with a five-section provenance README, sourcing the producing sha from the experiment's own sidecar (e4_cells/*/benchmark.json, e6_provenance.json) never from archiving-commit HEAD"

key-files:
  created:
    - experiments/archive/e4-2026-08-04-pre-interface-fix/README.md
    - experiments/archive/e4-2026-08-04-pre-interface-fix/benchmark_grid.csv
    - experiments/archive/e4-2026-08-04-pre-interface-fix/benchmark_grid.tex
    - experiments/archive/e6-2026-08-04-pre-interface-fix/README.md
    - experiments/archive/e6-2026-08-04-pre-interface-fix/generalization_sweep.csv
  modified:
    - experiments/archive/README.md

key-decisions:
  - "Sourced the provenance sha from each experiment's own sidecar (e4_cells/*/benchmark.json's environment.git_sha, identical across all nine cells; e6_provenance.json's environment.git_sha) rather than the top-level experiments/results/benchmark.json, which is E2's unrelated pipeline record on a different sha (6c7f930 vs 22e75ef) — matches the established convention in e4-2026-08-02-pre-depth-fix/README.md and e6-2026-08-02-pre-depth-fix/README.md"
  - "No experiments/archive/e7-2026-08-04-* directory created — E7 is provably inert per 19.4-CONTEXT.md CORRECTION (runs generate_real_rig_array's frozen shared WATER_Z, never calls generate_camera_array)"

patterns-established: []

requirements-completed: [SC-5]

# Metrics
duration: 25min
completed: 2026-08-04
---

# Phase 19.4 Plan 01: Archive Pre-Interface-Fix Artifacts Summary

**Archived E4's and E6's committed grid-family artifacts byte-for-byte at commit `22e75ef2b424c9b0234502e5229345a9f5912b11`, before the single-flat-interface fix (D-19.4-09) moves their numbers — no E7 archive, since E7 is provably inert.**

## Performance

- **Duration:** 25 min
- **Started:** 2026-08-04T17:32:00Z (approx)
- **Completed:** 2026-08-04T17:57:04Z
- **Tasks:** 3 completed
- **Files modified:** 6 (5 created, 1 modified)

## Accomplishments
- `experiments/archive/e4-2026-08-04-pre-interface-fix/` — byte-identical copies of `benchmark_grid.csv` and `benchmark_grid.tex`, with a five-section provenance README naming the single-flat-interface defect and the 1.42 px mean / 6.33 px max one-seed pixel-displacement evidence
- `experiments/archive/e6-2026-08-04-pre-interface-fix/` — byte-identical copy of `generalization_sweep.csv`, with a five-section README additionally recording E6's `GRID_DEPTH_RANGE[0]` re-derivation (1.181852154 -> 1.176215948, D-19.4-12/15) and the 3,000-draw seed-locking cure (D-19.4-15)
- `experiments/archive/README.md` Index table gained exactly two rows, matching the existing five-column shape; no E7 row added
- `tests/unit/test_experiments_provenance.py` still passes (101 passed, 6 skipped) — archived copies sit outside the `experiments/results/` glob and are not swept into the live-provenance gate
- No live `experiments/results/` file was modified (`git status --porcelain experiments/results/` empty throughout)

## Task Commits

Each task was committed atomically:

1. **Task 1: Archive E4's pre-interface-fix artifacts with a provenance README** - `81fbcf5` (docs)
2. **Task 2: Archive E6's pre-interface-fix artifacts with a provenance README** - `25414b1` (docs)
3. **Task 3: Append both generations to the archive index** - `58bd225` (docs)

_No TDD tasks in this plan; all three are docs-only archival tasks._

## Files Created/Modified
- `experiments/archive/e4-2026-08-04-pre-interface-fix/README.md` - Five-section provenance README for E4's pre-fix baseline
- `experiments/archive/e4-2026-08-04-pre-interface-fix/benchmark_grid.csv` - Byte-identical copy of the live E4 grid results
- `experiments/archive/e4-2026-08-04-pre-interface-fix/benchmark_grid.tex` - Byte-identical copy of the live E4 manuscript table
- `experiments/archive/e6-2026-08-04-pre-interface-fix/README.md` - Five-section provenance README for E6's pre-fix baseline
- `experiments/archive/e6-2026-08-04-pre-interface-fix/generalization_sweep.csv` - Byte-identical copy of the live E6 sweep results
- `experiments/archive/README.md` - Two new Index rows for the 2026-08-04 archive generations

## Decisions Made

- **Provenance sha sourcing:** the plan's Task 1 `read_first` and one acceptance-criterion line pointed at `experiments/results/benchmark.json` as the sha source. That top-level file is E2's own pipeline record (sha `6c7f930bb56b019067b8eb7ac1f2c84d37be645e`), unrelated to E4/E6's grid artifacts. Followed the established, verifiable convention instead (matching the precedent `e4-2026-08-02-pre-depth-fix/README.md` and `e6-2026-08-02-pre-depth-fix/README.md`): E4's sha comes from each `e4_cells/*/benchmark.json`'s own `environment.git_sha` (all nine cells agree on `22e75ef2b424c9b0234502e5229345a9f5912b11`), and E6's comes from `e6_provenance.json`'s `environment.git_sha` (same sha). Both READMEs explain this sourcing and explicitly note why the top-level `benchmark.json` is not the source.

## Deviations from Plan

None affecting scope or output — one interpretive correction to a provenance-sourcing detail (see Decisions Made above), which does not change any acceptance criterion outcome since the criteria assert string equality against a sidecar's `environment.git_sha`, which is what was used.

## Issues Encountered

**Worktree base drift (setup, before Task 1):** the worktree's HEAD was on `b4da55b` (the v1.8.0 release commit), not the phase's expected base `0c0d3211772803232f1f13fb308b6cbd89741d55` ("docs(19.4): mark phase executing"). Working tree was clean, so `git reset --hard 0c0d3211772803232f1f13fb308b6cbd89741d55` corrected it per the mandatory `<worktree_branch_check>` protocol before any task work began. No task-level issues thereafter.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Both archive generations exist with byte-identical artifacts and provenance READMEs; the "before" state for E4 and E6 is now diffable without git archaeology
- The subsequent code plans in this wave/phase (D-19.4-09's `generate_camera_array` fix and the `GRID_DEPTH_RANGE` re-derivation) can now proceed knowing the pre-fix baseline is preserved
- No blockers

---
*Phase: 19.4-grid-family-clearance-floor-fix*
*Completed: 2026-08-04*

## Self-Check: PASSED

All created files verified present on disk; all three task commits (`81fbcf5`, `25414b1`, `58bd225`) verified present in `git log --oneline --all`.
