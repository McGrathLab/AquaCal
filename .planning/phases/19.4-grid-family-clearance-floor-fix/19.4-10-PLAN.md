---
phase: 19.4-grid-family-clearance-floor-fix
plan: 10
type: execute
wave: 6
depends_on: ["19.4-09"]
files_modified:
  - .planning/MANUSCRIPT-FINDINGS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-10-SUMMARY.md
autonomous: false
requirements: [SC-3, SC-5, SC-5a, SC-8]
must_haves:
  truths:
    - "E1, E3, E5 and E7 are proven inert by byte-comparison against their committed artifacts, not merely by source-level argument (SC-3)"
    - "MF-08's E4/E6 rows reflect the corrected geometry; MF-05's numbers are reproduced, not replaced"
    - "A new finding records that synthetic validation was performed against a ground truth with per-camera water surfaces, and that this was found and corrected pre-resubmission"
    - "Every banded number in MF-08 names the committed artifact that regenerates it (SC-5a)"
  artifacts:
    - path: ".planning/MANUSCRIPT-FINDINGS.md"
      provides: "Updated MF-05 and MF-08, plus a new MF for the interface defect"
      contains: "single flat interface"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability rows for SC-1..SC-8 mapped to phase 19.4"
  key_links:
    - from: ".planning/MANUSCRIPT-FINDINGS.md"
      to: "experiments/results/interface_ablation_band.csv and exp1_band.csv"
      via: "each banded number names the artifact that regenerates it"
      pattern: "_band.csv"
---

<objective>
Turn the completed run into the phase's actual deliverable: a coherent, current, defensible record
of what moved, what did not, and where each number comes from.

Purpose: the end state this phase serves is good-quality CURRENT numbers across ALL experiments,
ready for analysis and transfer into the publication — a result set a reviewer could re-run and
reproduce, and that the author can lift numbers from directly. The interface fix was the means.
This plan is where the means becomes the end.

Output: the inertness verdict established by byte-comparison, the E4/E6 movement characterised,
MF-05 and MF-08 updated, a new manuscript finding recorded, and the phase's traceability closed.

**This plan changes no code and runs no experiment.** If any inertness comparison fails, that is a
DEFECT to escalate — the FIX moves, never the tolerance (blocking anti-pattern 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-CONTEXT.md
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-RESCOPE-PROPOSAL.md
@.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-09-SUMMARY.md
@.planning/MANUSCRIPT-FINDINGS.md
@.planning/ROADMAP.md
@CLAUDE.md

The manuscript tree at
`C:\Users\tucke\OneDrive - Georgia Institute of Technology\Thesis\Spinoffs\papers\aquacal\`
is READ-ONLY from this repo. Findings are recorded in `.planning/MANUSCRIPT-FINDINGS.md`; the
user owns the manuscript and makes the edits there.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the inertness verdict and characterise E4/E6 movement by byte-comparison</name>
  <files>.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-10-SUMMARY.md</files>
  <read_first>
    - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-09-SUMMARY.md (the single git sha, stage exit codes and gate verdicts)
    - experiments/archive/e4-2026-08-04-pre-interface-fix/ and e6-2026-08-04-pre-interface-fix/ (the pre-fix baselines from plan 01)
    - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-04-SUMMARY.md (the source-level inertness proof this task's byte-comparison completes)
  </read_first>
  <action>
    Establish the inertness verdict for E1, E3, E5 and E7 by comparing their regenerated artifacts
    against the versions committed before this phase, using `git diff` on
    `experiments/results/`. The four inert experiments' committed CSVs and `.tex` outputs must show
    no change. Report per experiment: file, changed / unchanged, and — if changed — the exact
    columns and row count affected.

    This is the byte-comparison arm that completes SC-3. Plan 04 established the source-level
    guarantee (none of the four reaches `generate_camera_array` in production); plan 09's `--check`
    stages and this diff establish the output-level one. E5 in particular was NOT `--check`ed in
    plan 04 (22 min) and is covered here for the first time.

    **If any of the four moved, STOP.** Record it as a DEFECT with the affected file and columns,
    escalate to the user, and do not proceed to Task 2. Do not raise `CHECK_RTOL`, do not widen a
    tolerance, and do not reclassify the experiment as "expected to move". Blocking anti-pattern 3
    is binding here: the FIX moves.

    Then characterise E4's and E6's movement against the archived pre-fix baselines. Report, for
    each: which columns moved, the distribution of the change, and whether any cell changed
    `status`. For E6, report the count of legal configurations before and after, and confirm the
    seed-43 run completed — end-to-end evidence the origin defect is gone (D-19.4-15).

    Report movement WITHOUT an accuracy claim for E4 and E6 (D-19.3-17): neither has a seed band,
    so neither can support one. Say so explicitly rather than implying otherwise. Do not quote the
    1.42 px headline as an effect size anywhere — it is one seed and one trajectory (blocking
    anti-pattern 7). Do not judge convergence by reprojection RMS (blocking anti-pattern 2);
    optimality and guard count are the discriminators, and never quote an optimality figure beyond
    one significant figure.
  </action>
  <verify>
    <automated>git diff --stat experiments/results/</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only experiments/results/` lists no E1, E3, E5 or E7 committed artifact (specifically none of `exp1_parameter_errors.csv`, `exp2_depth_generalization.csv`, `exp3_xy_vs_z_anisotropy.csv`, `index_sensitivity.csv`, `interface_ablation.csv`, and E3's outputs) — or, if it does, the SUMMARY records it as a DEFECT and the plan stopped
    - `git diff --name-only experiments/results/` DOES list `benchmark_grid.csv` and `generalization_sweep.csv` — E4 and E6 moved, which is the point
    - The SUMMARY contains a per-experiment inertness table with an UNCHANGED or DEFECT verdict for each of e1, e3, e5, e7
    - The SUMMARY reports E6's legal-configuration count before and after, and the seed-43 completion
    - `grep -c "1.42 px" .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-10-SUMMARY.md` — any occurrence is accompanied by the one-seed/one-trajectory caveat in the same paragraph
    - No file under `src/` or `experiments/` was modified: `git status --porcelain src/ experiments/*.py` produces no output
  </acceptance_criteria>
  <done>The four inert experiments are confirmed unchanged by byte-comparison, and E4's and E6's movement is characterised with an explicit no-accuracy-claim statement.</done>
</task>

<task type="auto">
  <name>Task 2: Update MF-05 and MF-08 and record the new interface finding</name>
  <files>.planning/MANUSCRIPT-FINDINGS.md</files>
  <read_first>
    - .planning/MANUSCRIPT-FINDINGS.md (MF-05 at line ~386, MF-08 at ~650, MF-09 at ~1057 — MF-09 is the edit map and locates every finding by line; read it before any statement about where a finding lands)
    - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-RESCOPE-PROPOSAL.md (the five-source audit table and the 31,680-observation pixel measurement)
    - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-CONTEXT.md (§ CORRECTION — MF-05 does NOT move)
  </read_first>
  <action>
    Update MF-05: its numbers are REPRODUCED, not replaced. E7 is inert under this fix — its
    production `"realistic"` scenario resolves to `generate_real_rig_array()`'s frozen shared
    `WATER_Z` and never reaches `generate_camera_array`. What MF-05 gains is a committed,
    regenerable artifact: `experiments/results/interface_ablation_band.csv`. Name that file
    explicitly beside every banded number, and correct any text in MF-05 implying its band lives
    only in gitignored `seed_sweep_19_3/` output. Retain MF-05's existing framing that the shared
    pairing is 10/10 (p=0.00098) and the refined pairing 8/10 (p=0.055) unless this run's band
    contradicts it, in which case report the contradiction plainly rather than smoothing it.

    Update MF-08's E4 and E6 rows with the values measured in Task 1, and state for each that it
    reports movement with NO accuracy claim (D-19.3-17, no seed band). Add the artifact name
    beside every banded number in MF-08 — including `exp1_band.csv` for the 97-178x ratio spread
    and the "2 of 10 seeds exceed 2 mm" finding, both of which now have a regenerable artifact for
    the first time. Correct MF-08's earlier statement that E7's numbers move: they do not.

    Append a NEW finding (next MF number in sequence) recording plainly: synthetic validation for
    E4 and E6 was performed against a ground truth in which each camera had its own water surface,
    contradicting the single-flat-interface premise the method and the manuscript rest on; the
    audit found two of five scenario sources affected; the measured displacement over 31,680 corner
    observations was mean 1.42 px, max 6.33 px against a reprojection RMS of ~0.4-0.9 px, so the
    modelling error exceeded the residual being reported; the defect was found and corrected
    pre-resubmission. State the one-seed/one-trajectory caveat in the same paragraph as the
    1.42 px figure — it establishes the defect is real and dominant and is NOT an effect size.
    Note the consequence the rescope proposal identifies: the correction strengthens E7's published
    conclusion rather than weakening it, because on corrected geometry shared IS the correct model.
    This is the kind of thing a reviewer who reads the generator would otherwise find first.

    Follow MF-09's location conventions for anything that names a manuscript location. Write only
    into `.planning/`; the manuscript tree is read-only.
  </action>
  <verify>
    <automated>grep -c "interface_ablation_band.csv" .planning/MANUSCRIPT-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interface_ablation_band.csv" .planning/MANUSCRIPT-FINDINGS.md` is at least 1
    - `grep -c "exp1_band.csv" .planning/MANUSCRIPT-FINDINGS.md` is at least 1
    - A new `## MF-NN` heading exists whose number is one greater than the previous highest, and its body contains `1.42 px`, `6.33 px`, `31,680`, and an explicit one-seed/one-trajectory caveat
    - MF-08's E4 and E6 rows each contain the phrase `no accuracy claim`
    - MF-05 contains no statement that E7's numbers move under this phase's fix
    - `git status --porcelain` shows changes only under `.planning/`
    - No file under the OneDrive manuscript root was written
  </acceptance_criteria>
  <done>MF-05 and MF-08 reflect the corrected split of what moved and what did not, every banded number names its regenerating artifact, and a new finding records the interface defect and its correction.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Close the phase — traceability, roadmap, and an honesty review</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the Traceability table showing "TBD" for 19.4)
    - .planning/ROADMAP.md (the Phase 19.4 entry at line ~393, including its `**Requirements**: TBD` line and its `Plans:` list)
    - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-COVERAGE-MATRIX.md
    - .planning/MANUSCRIPT-FINDINGS.md (post-Task-2)
  </read_first>
  <what-built>
    A closed phase: the interface fix, the re-derived clearance floor, `--seeds` band modes on E1
    and E7, fail-fast on E4 and E6, a risk-first queue executed once under one git sha, an
    inertness verdict established by byte-comparison, and updated manuscript findings.
  </what-built>
  <how-to-verify>
    Before approving, walk these six checks:
    1. **Coverage.** Every one of SC-1..SC-8 (including SC-5a) is satisfied by a named artifact or
       test, and `.planning/REQUIREMENTS.md`'s Traceability table now maps each to phase 19.4
       rather than TBD. ROADMAP's Phase 19.4 `**Requirements**` line no longer reads TBD, and its
       `Plans:` list names all ten plans.
    2. **Inertness honesty.** E1, E3, E5 and E7 are recorded as UNCHANGED on evidence, not on
       argument. No tolerance anywhere was widened to produce that verdict.
    3. **Claim honesty.** E4 and E6 are reported as movement with NO accuracy claim. E7 remains the
       milestone's only surviving accuracy claim, and it now has a committed band artifact behind
       it.
    4. **Headline honesty.** The 1.42 px figure appears nowhere as an effect size. No optimality
       figure is quoted beyond one significant figure. No convergence judgment rests on
       reprojection RMS.
    5. **Provenance.** One git sha across every artifact in `experiments/results/`, equal to the
       HEAD recorded before launch, and the archived pre-fix copies for E4 and E6 are intact and
       readable.
    6. **Audit outcome.** The D-19.4-17 coverage matrix's verdict was honoured — if it said
       AMENDED, the queue actually ran the amended stage list.
  </how-to-verify>
  <action>
    Update `.planning/REQUIREMENTS.md`'s Traceability table with a row for each of SC-1..SC-8
    (including SC-5a) mapped to phase 19.4 and to the artifact or test that satisfies it, and
    replace ROADMAP.md's Phase 19.4 `**Requirements**: TBD` line and its `Plans: TBD` list with
    the real requirement IDs and all ten plan filenames with one-line objectives. Then present the
    six honesty checks below to the user and walk them together. Record each check and its verdict
    in `19.4-10-SUMMARY.md`. Do not mark the phase complete without the user's approval.
  </action>
  <verify>
    <human-check>User walks the six honesty checks and approves phase closure, or names the failing check.</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md`'s Traceability table has a row for each of SC-1..SC-8 (incl. SC-5a) mapped to phase 19.4 with a named satisfying artifact or test
    - `grep -c "Requirements.*TBD" .planning/ROADMAP.md` returns 0 within the Phase 19.4 section
    - ROADMAP's Phase 19.4 `Plans:` list contains ten checked entries, one per plan file
    - The user has walked all six checks and either approved or named a specific correction
    - Each of the six checks is recorded in `19.4-10-SUMMARY.md` with its verdict
  </acceptance_criteria>
  <resume-signal>Type "approved" to close the phase, or name the check that failed and what must change.</resume-signal>
  <done>Traceability and the roadmap are closed, the six honesty checks are walked and recorded, and the phase is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo → the manuscript | Findings recorded here become published claims. An overstated finding is the expensive failure mode. |
| inertness verdict → the phase's central claim | A verdict reached by relaxing a comparison rather than by evidence invalidates everything downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.4-30 | Tampering | inertness comparison thresholds | mitigate | Task 1 stops on any movement in the inert set and forbids raising `CHECK_RTOL` or reclassifying the experiment; the checkpoint's check 2 re-verifies. |
| T-19.4-31 | Repudiation | a banded number with no regenerating artifact | mitigate | Task 2 requires every banded number in MF-05/MF-08 to name its artifact; acceptance greps for both band CSV filenames. |
| T-19.4-32 | Tampering | the read-only manuscript tree | mitigate | Findings are written only into `.planning/MANUSCRIPT-FINDINGS.md`; acceptance asserts `git status --porcelain` shows changes only under `.planning/` and that nothing under the OneDrive root was written. |
| T-19.4-33 | Information disclosure | overstating the 1.42 px headline | mitigate | Acceptance requires the one-seed/one-trajectory caveat in the same paragraph as the figure; the checkpoint's check 4 re-verifies. |
| T-19.4-SC | Tampering | package installs | accept | No packages are installed in this phase. |
</threat_model>

<verification>
- `git diff --name-only experiments/results/` shows E4/E6 changed and E1/E3/E5/E7 unchanged
- `.planning/MANUSCRIPT-FINDINGS.md` names both band CSVs and carries a new MF for the interface defect
- REQUIREMENTS.md and ROADMAP.md no longer read TBD for phase 19.4
- The six honesty checks are walked and recorded
</verification>

<success_criteria>
- SC-3 (complete): E1, E3, E5 and E7 proven inert by byte-comparison as well as source-level argument
- SC-5 / SC-5a (reported): E4/E6 movement characterised with no accuracy claim; every banded number
  names the artifact that regenerates it
- SC-8 (closed): the coverage matrix's verdict was honoured by what the queue actually ran
- Phase traceability and roadmap entries are complete
</success_criteria>

<output>
Create `.planning/phases/19.4-grid-family-clearance-floor-fix/19.4-10-SUMMARY.md` when done,
including the per-experiment inertness table and the six honesty checks with their verdicts.
</output>
