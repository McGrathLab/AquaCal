---
phase: 19.4-grid-family-clearance-floor-fix
plan: 10
wave: 6
status: complete
requirements-completed: [SC-3, SC-5, SC-5a, SC-8]
decisions-implemented: [D-19.3-17, D-19.4-15, D-19.4-17]
files-modified:
  - .planning/MANUSCRIPT-FINDINGS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-09-SUMMARY.md
  - .planning/phases/19.4-grid-family-clearance-floor-fix/19.4-10-SUMMARY.md
key-decisions:
  - "Inertness verified against commit 2a623f9 rather than the working tree, because plan 09's artifacts were already committed (0ffbe15). Evidence is identical; the archived pre-fix baselines were confirmed content-identical to 2a623f9 modulo line endings."
  - "MF-08's 19.3 movement table was PRESERVED and a 19.4 subsection added beside it, rather than overwriting rows. Overwriting would have falsified the depth-fix record, which is a different phase's finding."
  - "19.4-09-SUMMARY.md was written as a prerequisite: it did not exist, and this plan depends on and reads it."
---

# Plan 19.4-10 Summary — inertness verdict, findings, phase closure

## Task 1 — the inertness verdict, by byte-comparison

**Method.** `git diff 2a623f9..HEAD -- experiments/results/`. Plan 09's artifacts were committed
before this plan ran, so the comparison references the pre-run commit rather than the working
tree. The archived pre-fix baselines
(`experiments/archive/e{4,6}-2026-08-04-pre-interface-fix/`) were verified **content-identical**
to `2a623f9`'s committed versions modulo CRLF/LF, so the two possible baselines agree.

### The four inert experiments — UNCHANGED, on evidence

| exp | data artifact | verdict |
|---|---|---|
| **E1** | `exp1_parameter_errors.csv`, `exp2_depth_generalization.csv`, `exp3_xy_vs_z_anisotropy.csv` | **UNCHANGED** — absent from the diff entirely |
| **E3** | E3's committed outputs | **UNCHANGED** — only `e3_provenance.json` appears, and only its `git_sha` |
| **E5** | `index_sensitivity.csv` | **UNCHANGED** — only `e5_provenance.json` appears, and only its `git_sha`. **First runtime verification of E5**, deferred from plan 04 |
| **E7** | `interface_ablation.csv` | **UNCHANGED** — absent from the diff entirely |

The eight benchmark/provenance sidecars belonging to these four experiments **differ only in
`git_sha` and `seconds`** — verified by filtering the diff for any other changed line, which
returned nothing for all eight files. Solver diagnostics are bit-identical: `optimality`
0.0016619306568941283, `nfev` 14, `njev` 12, `status` 2 on E1-refractive and E7-shared_fixed
alike, unchanged.

**No tolerance was widened, no `CHECK_RTOL` raised, and no experiment reclassified.** SC-3 is
discharged at the output level, completing plan 04's source-level argument.

### E4 — movement, NO ACCURACY CLAIM

Nine queue-produced cells; row 10 (`real_rig_13cam_200fr`) is E2's and is untouched.

| column | movement |
|---|---|
| `status` | `ok` before and after, all 9 cells — **no status change** |
| `degenerate_observations_at_solution` | **0** before and after, all 9 cells |
| `n_observations` | −0.15% to −0.40% (e.g. 697 → 695, 1205 → 1201) |
| `optimality_stage3_interface_optimization` | stays ≤ **2e-02** throughout (1 s.f.) |
| `reprojection_rms` | unchanged within ±0.06% |
| `validation_3d_error_mean` | 0.0350 → 0.0335 mm on average, but **moves both directions per cell** (−17% to +9%) |

**E4 reports movement with no accuracy claim** (D-19.3-17 — E4 has no seed band). The mean 3D-error
shift is far inside seed-to-seed variation this design cannot measure, and per-cell signs disagree.
It must not be read as an improvement.

### E6 — movement, NO ACCURACY CLAIM

14 configurations. **Legal-configuration count: 14 before, 14 after** — all `status="ok"` in both,
guard count 0 in both.

| axis/config | `water_z_error_mm_mean` pre | post |
|---|---|---|
| index/baseline | 3.4057 | 0.7127 |
| index/1.36 … 1.55 | 3.4057–3.4575 | 0.0270–0.9841 |
| layout/line | 3.4523 | 1.2584 |
| layout/ring | 3.4057 | 0.4203 |
| scale/double_scale | 3.4057 | 2.5180 |
| scale/half_scale | 3.4083 | 0.3244 |

|abs| mean **3.4148 → 0.6727 mm**; max 3.4575 → 2.5180.

**The interpretable finding is the pre-fix constancy, not the magnitude.** `3.4057` appears
*exactly* in 10 of 14 rows — identical across refractive index 1.333→1.55, across grid/line/ring
layouts, and across half/double scale. A quantity that ignores every variable the experiment
manipulates is not measuring the experiment; it is the ground-truth defect showing through. After
the fix the column varies with configuration, as an estimate should. **This is a mechanism
statement, not an effect size: one seed, one trajectory.**

`depth_range_min` moved on the two `scale` configs only, 1.181852 → 1.176216 — the re-derived,
seed-invariant clearance floor (SC-4).

**D-19.4-15 evidence — the origin defect is gone.** `results_e6_seed43/generalization_sweep.csv`
records **14/14 `ok` at seed 43**, a seed from the ~94%-failing population pre-fix. `repeat2` at
seed 42 also gives 14/14.

### Runtime

E1/E7 solve a provably identical problem yet ran ~2× slower (62.4 → 125.5 s; 63.9 → 127.1 s).
Because the problem is bit-identical, the slowdown **cannot** be the geometry fix; it tracks the
machine. This is evidence for the null and is recorded in `19.4-RUNTIME-OBSERVATION.md` as an
**open question**. Nothing here says the fix made calibration faster or slower.

## Task 2 — manuscript findings

- **MF-05 — reproduced, not replaced.** Recomputed from the newly committed
  `interface_ablation_band.csv`: `fixed` 10/10 shared-better, range [+0.919, +1.879], no zero
  crossing; `refined` 8/10, crosses zero, range [−0.471, +2.524]; all sixteen cells of the per-arm
  spread table match exactly. Source-of-truth line now names the committed artifact instead of
  gitignored scratch output. **Noted for the author:** the entry's p-values are one-sided
  (0.00098 = 2⁻¹⁰; 0.055 = 56/1024); two-sided they are 0.00195 and 0.109. `fixed` clears 0.05
  either way, `refined` under neither — which reinforces the existing "tendency, not a rule"
  framing rather than changing it. Recorded, not silently corrected.
- **MF-08 — a 19.4 subsection added, the 19.3 table preserved.** E4/E6 rows as above, each stating
  *no accuracy claim*. Corrects the earlier implication that E7's numbers move — scoped precisely:
  E7 moved under 19.3's depth fix, and does not move under 19.4's interface fix. Every banded
  number now names its regenerating artifact (`exp1_band.csv`, `interface_ablation_band.csv`).
  E1's `14,949` guard count reproduces exactly, so that bookkeeping finding stands.
- **MF-10 — new.** Records the per-camera-water-surface defect: two of five scenario sources
  affected; mean 1.42 px / max 6.33 px over 31,680 corner observations against a ~0.4–0.9 px
  reprojection RMS, so the modelling error exceeded the residual being reported; found and
  corrected pre-resubmission. The one-seed/one-trajectory caveat sits in the same sentence as the
  1.42 px figure. Notes that the correction **strengthens** E7's published conclusion, and
  recommends a one-sentence §3 disclosure.

## Task 3 — the six honesty checks

| # | check | verdict |
|---|---|---|
| 1 | **Coverage** — SC-1..SC-8 mapped to 19.4 with a named satisfying artifact; ROADMAP not TBD; ten plans listed | **PASS** — traceability rows + a per-SC artifact table added to REQUIREMENTS.md; ROADMAP already carried the SC list; plans 04/06/07/08/09 checked off, plan 10 pending this checkpoint |
| 2 | **Inertness honesty** — E1/E3/E5/E7 UNCHANGED on evidence, no tolerance widened | **PASS** — byte-comparison above; the only differences anywhere are `git_sha` and `seconds` |
| 3 | **Claim honesty** — E4/E6 movement with no accuracy claim; E7 the only surviving accuracy claim, now with a committed band | **PASS** — stated in both the SUMMARY and MF-08; `interface_ablation_band.csv` committed |
| 4 | **Headline honesty** — 1.42 px never an effect size; optimality ≤ 1 s.f.; no convergence judged by reprojection RMS | **PASS** — caveat co-located in MF-10 and here; optimality quoted as "≤ 2e-02"; convergence judged on optimality and guard count only |
| 5 | **Provenance** — one git sha across all artifacts, equal to pre-launch HEAD; archives intact | **PASS** — `ALL gate3_git_sha_consistency` PASS at `2a623f9`; `rerun_19_4_frozen_sha.txt` matches; archives verified content-identical to the pre-run baseline |
| 6 | **Audit outcome** — the D-19.4-17 coverage matrix verdict honoured | **PASS** — verdict was CONFIRMED; queue ran `STAGES=(e6_repeat1 e4 e6_repeat2 e6_seed43 e7 e1 e5 e3)` verbatim |

**APPROVED by the user 2026-08-05.** All six checks walked and accepted; no check was named as
failing and no correction was requested. Phase 19.4 is CLOSED.

No test-suite run was performed at closure, deliberately: this plan changed no code and ran no
experiment (`git status --porcelain src/ experiments/*.py` empty throughout). The phase's last code
change was plan 08, whose post-merge suite was 1436 passed / 6 skipped / 0 failed.

## The three obligations this plan was required to carry

1. **E5's runtime byte-comparison — DISCHARGED.** `index_sensitivity.csv` is untouched;
   `e5_provenance.json` differs only in `git_sha`. Plan 04's source-level proof is no longer being
   leaned on for E5.
2. **The R1.4 prose condition — CARRIED.** The response-letter text for R1.4 must state the
   sweep's finding as **"movement demonstrated, no accuracy claim"**, never "accuracy holds across
   the sweep". Recorded here and in MF-08's 19.4 subsection.
3. **The runtime observation is an OPEN QUESTION.** See above; the new evidence points at the
   machine, and the phrase "the fix made calibration faster/slower" appears nowhere.

## Outstanding, not blocking closure

- `gate4_band` FAILs ×2 — recording-only, diagnosed, tracked in
  `.planning/todos/pending/2026-08-05-band-sidecar-competes-with-production-benchmark-record.md`.
- Two todos raised while monitoring: the opencv-5.0 pin, and a review of whether E1's `n_water=1.0`
  baseline can carry §3's refractive-vs-non-refractive claims given that
  `e1_refractive_comparison.py:34` records E1 as demoted from any accuracy claim by D-19.3-17,
  while the abstract's ~135× and six other §3 numbers are E1 outputs.
- `.continue-here.md` must be deleted when this phase closes.
