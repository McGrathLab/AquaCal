---
phase: 19.4-grid-family-clearance-floor-fix
plan: 06
subsystem: testing
tags: [experiments, seed-band, reproducibility, e1, pandas, argparse]

# Dependency graph
requires:
  - phase: 19.4-05
    provides: "experiments/_io.py: parse_seed_list and run_seed_band; experiments/e7_interface_ablation.py: --seeds precedent"
provides:
  - "experiments/e1_refractive_comparison.py: a --seeds flag emitting a committed, reproducible exp1_band.csv"
  - "experiments/_io.py: run_seed_band's docstring now documents the E4/E6 extension path and its deferred cost"
affects: [19.4-09-verification-queue, e4-e6-seed-band-future-work]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "E1 consumes the same shared band-execution primitive (run_seed_band/parse_seed_list) plan 05 added to _io.py and wired into E7 -- adding a band mode to a second experiment script was pure configuration, no changes to the shared mechanism itself"
    - "Script-local --seeds flag layered on parents=[build_experiment_arg_parser()], mirroring E7's and E4's --cell pattern"

key-files:
  created:
    - tests/unit/test_e1_band_mode.py
  modified:
    - experiments/e1_refractive_comparison.py
    - experiments/_io.py

key-decisions:
  - "exp1_band.csv covers exp2_depth_generalization.csv's territory (test_depth_m x model), per the plan's discretionary A2 resolution -- NOT exp1_parameter_errors.csv or exp3_xy_vs_z_anisotropy.csv"
  - "A --seeds run never writes any of E1's four single-seed CSVs -- only a plain --seed run produces those, keeping the --check baseline comparison target untouched"
  - "The band CSV write always overwrites (force implied for that one file only); force is NOT implied for the two benchmark JSON sidecars, which still honor --force/resumability normally -- mirrors E7 exactly"
  - "e1_benchmark_<model>.json's solver_config gains an additive seeds list during a band run, built from the LAST seed's diagnostics/timings/accuracy (one provenance record cannot represent N independent solves) -- same convention plan 05 chose for E7"
  - "The shared _io.py mechanism (parse_seed_list/run_seed_band) required ZERO functional changes to support E1 -- only its docstring was extended additively with an E4/E6 extension note, per this plan's explicit instruction to consume, not reimplement"

requirements-completed: [SC-5a]

# Metrics
duration: ~75min (excluding ~70min of foreground verification: test_e1_band_mode.py 30m35s, combined symmetry suite 38m14s, production --check ~6min)
completed: 2026-08-04
---

# Phase 19.4 Plan 06: E1 --seeds Band Mode + E1/E7 Symmetry Check Summary

**Wired a `--seeds` flag into E1 mirroring E7's mechanism exactly, emitting a committed `exp1_band.csv` over `exp2_depth_generalization.csv`'s territory so MF-08's 97-178x ratio spread and "2 of 10 seeds exceed 2 mm" finding become regenerable artifacts instead of gitignored planning-document numbers.**

## Performance

- **Duration:** ~75 min of edits/commits; verification (foreground, synchronous per project policy) added ~70 min: `test_e1_band_mode.py` alone took 30m35s (10 tests, each constructing and solving `--smoke`-scale two-model calibrations), the combined symmetry suite (`test_e1_band_mode.py` + `test_e7_band_mode.py` + `test_seed_band_io.py`, 33 tests) took 38m14s, and `python -u -m experiments.e1_refractive_comparison --check` at production ("realistic", 12-camera) scale took ~6 min.
- **Tasks:** 2/2 completed
- **Files modified:** 2 source files, 1 new test file

## Accomplishments
- `experiments/e1_refractive_comparison.py` gained a script-local `--seeds` flag (mutually exclusive with `--check`, mirroring E7's `_validate_e7_args`/`_validate_e1_args` pattern), emitting `exp1_band.csv` at `len(seeds) * len(TEST_DEPTHS) * len(MODELS)` rows (160 at production scale), and additively stamping `solver_config["seeds"]` into both `e1_benchmark_refractive.json` and `e1_benchmark_nonrefractive.json`.
- Verified, not assumed: the single-seed production path (`--seed 42` default, `--check`) is byte-identical after this change -- `python -u -m experiments.e1_refractive_comparison --check` printed "Fresh output matches committed baseline within tolerance." for all three committed CSVs and exited 0.
- Confirmed E1's and E7's `--seeds` behavior is symmetric on all four rules named in Task 2 (mutual exclusivity with `--check`, band-only CSV write, force scoped to the band CSV alone, provenance `seeds` field shape) -- all four PASS with zero divergence, no code correction needed in either script.
- `experiments/_io.py`'s `run_seed_band` docstring now names the concrete extension path for E4/E6 (a `runner(seed) -> DataFrame` closure, a script-local `--seeds` flag, a `_validate_*_args` extension) and records the deferred cost (E6 x10 ~16.6 h, E4 x10 ~22.1 h, ~39 h together) -- the shared mechanism itself required NO functional change to support a second consumer.
- Added no `--seeds` flag to E4 or E6 (`grep -c '"--seeds"' experiments/e4_benchmark_grid.py experiments/e6_generalization_sweep.py` returns 0 for both) -- out of scope by design (D-19.4-13).

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire --seeds into E1 and emit exp1_band.csv** - `ee79645` (feat)
2. **Task 2: Symmetry check across E1 and E7, and a written extension path for E4/E6** - `fb4cafc` (docs)

_No plan-metadata commit was made for STATE.md/ROADMAP.md -- those are owned by the wave orchestrator per this plan's execution instructions._

## Files Created/Modified
- `experiments/e1_refractive_comparison.py` - Added `--seeds` to `build_arg_parser`, `_validate_e1_args` (rejects `--seeds` + `--check`), `_run_band` (resolves seeds, runs E1's existing per-seed depth-generalization path via `run_seed_band`, writes `exp1_band.csv` with implied force, writes both benchmark JSON sidecars with an additive `seeds` list), and `BAND_KEY_COLUMNS = ["seed", "test_depth_m", "model"]`. Module docstring's "Emits" section documents `exp1_band.csv` and the `--seeds` band-mode rules, explicitly stating E1 carries NO accuracy claim (D-19.3-17) and is inert under this phase's interface fix. `CHECK_RTOL = 1e-6` unchanged (verified by `grep -c`).
- `experiments/_io.py` - `run_seed_band`'s docstring extended additively with a concrete "adding E4/E6 later" section naming the three things a future caller supplies and the deferred cost (`16.6`/`22.1` hours). No change to `parse_seed_list`, `run_seed_band`'s signature, or its execution logic -- consumed as-is by E1, per the plan's explicit instruction.
- `tests/unit/test_e1_band_mode.py` - 10 tests covering every `<behavior>` bullet: CLI help/validation, band-CSV row count and columns (superset of `EXP2_COLUMNS` plus `seed`), the "no single-seed CSVs from a band run" rule (all four: `exp1_parameter_errors.csv`, `exp2_depth_generalization.csv`, `exp2_spatial_errors.csv`, `exp3_xy_vs_z_anisotropy.csv`), per-model benchmark `seeds` list, `experiments/results/` untouched, and the non-band smoke path's unaffected behavior. All run at `--smoke` scale (`"ideal"` scenario, single test depth) against `tmp_path`.

## Decisions Made
- `exp1_band.csv` bands `exp2_depth_generalization.csv`'s territory exactly, per the plan's discretionary resolution of RESEARCH assumption A2 -- 8 `TEST_DEPTHS` x 2 `MODELS`, no different natural row shape was found on reading the code, so no correction was needed.
- A `--seeds` run never writes any of E1's four single-seed CSVs (only a plain `--seed` run produces them) -- keeps the `--check` gate's comparison target untouched by any band run, mirroring E7's rule for `interface_ablation.csv`.
- Force is implied for the band CSV write only; the two benchmark JSON writes during a band run still honor `--force`/resumability normally -- identical to E7's convention.
- The benchmark payload written during a band run reflects the LAST seed in the resolved list, with the `seeds` list itself recording which N were actually run -- identical to E7's convention (one provenance record cannot represent N independent solves).

## Deviations from Plan

None - plan executed exactly as written. Both tasks' `<behavior>` and `<acceptance_criteria>` bullets were implemented and verified directly (not merely by exiting-0 test runs): `--help` output inspected for `--seeds`; `--seeds --check` error message inspected for both flag names; band CSV row count, columns, and seed values inspected; `git status --porcelain experiments/results/` checked empty before and after every verification command; `--check` re-run at full production scale to confirm the single-seed path is untouched; the four symmetry rules verified by direct code inspection (`force=True`/`force=force` grep across both scripts) in addition to the passing test suite. `experiments/_io.py`'s shared mechanism needed no functional change -- only its docstring was extended additively, exactly as the plan anticipated and instructed.

## Issues Encountered

**Worktree base drift, not a plan defect.** At startup, this worktree's HEAD was on `b4da55b` (`chore(release): 1.8.0`), an old commit predating the entire `experiments/` package's existence in this branch's history -- `git merge-base HEAD a36a30058a4e4e9d2003f6ecc61a663dffb3c4fa` returned `b4da55b`, not the expected base, and `experiments/e1_refractive_comparison.py` did not exist on disk. Per the plan's `<worktree_branch_check>` step, ran `git reset --hard a36a30058a4e4e9d2003f6ecc61a663dffb3c4fa`, which corrected HEAD to the expected wave-1-merged base and restored the `experiments/` package. All subsequent work proceeded from the correct base; verified afterward that plan 05's `parse_seed_list`/`run_seed_band`/E7 `--seeds` wiring were present as expected.

**Verification runtime, not a defect.** Both `tests/unit/test_e1_band_mode.py` (10 tests) and the plan's combined symmetry-check command (33 tests across three files) ran considerably longer in the foreground than a typical unit-test invocation, because several tests genuinely construct and solve `--smoke`-scale two-model E1 calibrations (Stage 2 + Stage 3 + intrinsic-refinement pass, twice per seed, twice per test). All were run synchronously in the foreground per this project's binding rule against backgrounding long executor commands, polled to genuine completion, and read for their exact printed pass/fail counts -- none were treated as passing without an observed exit code:
- `tests/unit/test_e1_band_mode.py`: **10 passed**, 0 failed, in 405.52s (single-test check) confirming environment sanity, then **10 passed** in the full run (30m35s).
- `tests/unit/test_e1_band_mode.py tests/unit/test_e7_band_mode.py tests/unit/test_seed_band_io.py`: **33 passed**, 0 failed, in 2294.91s (38m14s).
- `python -u -m experiments.e1_refractive_comparison --check`: exit 0, all three committed CSVs ("Fresh output matches committed baseline within tolerance.").

No production experiment stage (E1 at full 10-seed band, E3/E4/E5/E6, `rerun_19_*.sh`) was run. `git status --porcelain experiments/results/` was empty both before and after every verification command.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- E1's and E7's `--seeds` mode are both ready for plan 19.4-09's overnight queue to run their production 10-seed bands (`--seeds <10 comma-separated values> --out experiments/results`), which this plan deliberately did not run (forbidden by its own scope: E1's band is ~57 min of production compute, plan 09's stage).
- `experiments/_io.py`'s `parse_seed_list`/`run_seed_band` remain available for E4/E6 to adopt later as configuration, now with an explicit docstring pointer naming the three pieces a future caller supplies and the ~39 h deferred cost -- no new mechanism needed, confirmed by this plan's zero-change consumption for a second experiment.
- No blockers. `experiments/results/` is untouched by this plan; the committed `exp1_parameter_errors.csv`/`exp2_depth_generalization.csv`/`exp3_xy_vs_z_anisotropy.csv` baselines are confirmed byte-identical via `--check` at production scale.

---
*Phase: 19.4-grid-family-clearance-floor-fix*
*Completed: 2026-08-04*
