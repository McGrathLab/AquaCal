---
phase: 17-per-camera-interface-ablation-mode
plan: 05
type: execute
wave: 3
depends_on: ["17-03"]
files_modified:
  - tests/synthetic/test_per_camera_interface.py
  - tests/unit/test_optim_common.py
autonomous: true
requirements: [IFACE-05]
must_haves:
  truths:
    - "shared_interface=True (default) produces bit-identical numbers to the pre-change shared path, proven by an exact-equality (rtol=0, atol=0) regression test"
    - "Per-camera mode with equal initial water_z on shared-interface ground truth recovers the shared solution: recovered per-camera water_z values agree with each other and with the shared-mode solution to tight tolerance, and extrinsics/RMS match the shared-mode result"
    - "The bit-exactness check covers the packing/structure layer (packed vector, sparsity, bounds, groups) AND an end-to-end Stage-3 result on a fixed synthetic scenario"
  artifacts:
    - path: "tests/synthetic/test_per_camera_interface.py"
      provides: "equal-seed recovery test + end-to-end shared-mode bit-exact regression"
      contains: "shared_interface"
    - path: "tests/unit/test_optim_common.py"
      provides: "packing/structure-layer bit-identity assertions for shared mode"
      contains: "shared_interface"
  key_links:
    - from: "test_per_camera_interface (recovery)"
      to: "optimize_interface(shared_interface=False, equal seeds)"
      via: "assert recovered per-camera water_z ~ shared solution"
      pattern: "shared_interface\\s*=\\s*False"
---

<objective>
Prove IFACE-05: (1) `shared_interface=True` is bit-unchanged from current behavior, and (2)
per-camera mode with equal initial values recovers the shared solution on shared-interface ground
truth. This is the phase's correctness safety net — an invalid per-camera parameterization would
otherwise pass silently.

Purpose: the whole ablation is only trustworthy if the default path is provably untouched and the
per-camera path provably degenerates back to the shared answer when the ground truth is shared. These
two tests are the guarantee.
Output: a dedicated synthetic test module plus packing-layer bit-identity assertions.
</objective>

<execution_context>
@C:/Users/tucke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tucke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-per-camera-interface-ablation-mode/17-CONTEXT.md
@.planning/phases/17-per-camera-interface-ablation-mode/17-03-PLAN.md

<interfaces>
Synthetic ground-truth helpers (tests/synthetic/, imported in tests/synthetic/test_full_pipeline.py):
  from .ground_truth import (SyntheticScenario, create_scenario, generate_camera_array,
    generate_camera_intrinsics, generate_synthetic_detections, compute_calibration_errors)
  A scenario carries scenario.intrinsics, scenario.extrinsics, scenario.water_zs (dict),
  scenario.board_poses, scenario.board_config, scenario.noise_std. In a SHARED-interface scenario all
  scenario.water_zs values correspond to a single global water surface (per-camera distance derived as
  water_z - C_z), so per-camera water_z solved back should be ~equal.

optimize_interface(detections, intrinsics, initial_extrinsics, board, reference_camera,
  initial_water_zs=None, interface_normal=None, n_air, n_water, loss, loss_scale, min_corners,
  use_sparse_jacobian=True, verbose=1, normal_fixed=True, observer=None, shared_interface=True)
  -> (opt_extrinsics, opt_distances(dict[str,float]), board_poses_list, rms)

test_full_pipeline.py `_run_calibration_stages` (line 28) shows the Stage 2 -> Stage 3 wiring pattern
to reuse (build_pose_graph, estimate_extrinsics, then optimize_interface).

_optim_common functions all take a trailing shared_interface=True (plan 17-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Packing/structure-layer bit-identity assertions (shared mode)</name>
  <files>tests/unit/test_optim_common.py</files>
  <action>
Add a focused test class asserting the shared-mode packing/structure layer is unchanged by the
shared_interface addition. For a small multi-camera, multi-frame fixture, assert EXACT equality
(np.testing.assert_array_equal, i.e. rtol=0/atol=0) between:
  - pack_params(...) with no shared_interface arg vs pack_params(..., shared_interface=True);
  - build_bounds, build_jacobian_sparsity, and build_structural_column_groups likewise;
  - and that the shared-mode packed vector has exactly ONE water_z entry at the expected index
    (n_tilt_params + n_extrinsic_params) and total length matching the pre-change formula
    (n_tilt + 6*(n_cams-1) + 1 + 6*n_frames [+ 4*n_cams]).
This locks the shared path at the unit layer. (Plan 17-01 Task 3 added a lighter guard; this makes the
IFACE-05 intent explicit and self-contained in one place.)
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_optim_common.py -q -k "bit or identity or shared"</automated>
  </verify>
  <done>Shared-mode pack/bounds/sparsity/groups are proven exactly equal with and without
  shared_interface=True; the single-water_z layout is asserted.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end shared-mode bit-exact regression + equal-seed recovery (IFACE-05)</name>
  <files>tests/synthetic/test_per_camera_interface.py</files>
  <action>
Create tests/synthetic/test_per_camera_interface.py (module docstring; import the ground_truth helpers
as test_full_pipeline.py does). Build ONE fixed, deterministic shared-interface scenario (fixed seed,
noise_std=0.0 or a small fixed value) and run Stage 2 -> Stage 3 once to obtain the shared-mode
solution, following `_run_calibration_stages`' wiring.

Test A — shared-mode end-to-end bit-exact regression: run optimize_interface(..., shared_interface=True)
twice on the identical inputs and assert the two results are exactly equal (assert_array_equal on
result RMS, each camera's R/t, and the distances dict) — a determinism/characterization guard proving
the shared path is stable and side-effect-free. (Mark @pytest.mark.slow if it exceeds a couple seconds.)

Test B — equal-seed recovery (the core IFACE-05 claim): on the SAME shared-interface scenario, run
optimize_interface(..., shared_interface=False) with all initial water_z seeds EQUAL (e.g. every camera
0.15m). Assert:
  - the recovered per-camera water_z values agree with each other to tight tolerance
    (np.ptp(list(distances.values())) < tol, e.g. a few millimeters), i.e. per-camera mode recovers a
    single shared interface when the truth is shared;
  - the recovered per-camera water_z mean matches the shared-mode solution's water_z to tight tolerance;
  - extrinsics (camera centers) and final RMS match the shared-mode solution to tight tolerance.
Choose tolerances that pass on noiseless/low-noise synthetic data and document them inline. Mark slow if
needed. Keep the scenario small (few cameras, few frames) so it runs fast.
  </action>
  <verify>
    <automated>python -m pytest tests/synthetic/test_per_camera_interface.py -q</automated>
  </verify>
  <done>Shared-mode end-to-end result is reproducible/exact; per-camera mode with equal seeds on shared
  ground truth recovers agreeing per-camera water_z that match the shared solution, plus matching
  extrinsics and RMS.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/synthetic/test_per_camera_interface.py tests/unit/test_optim_common.py -q` passes
  (run without `-m "not slow"` to include any slow-marked recovery test).
- The recovery test genuinely exercises shared_interface=False with equal seeds and compares to the shared solution.
- No regression: `python -m pytest tests/ -m "not slow" -q`.
</verification>

<success_criteria>
- shared_interface=True proven bit-identical at the packing layer AND stable end to end.
- Per-camera mode with equal initial values recovers the shared solution (agreeing per-camera water_z,
  matching mean/extrinsics/RMS) on shared-interface ground truth.
</success_criteria>

<output>
After completion, create `.planning/phases/17-per-camera-interface-ablation-mode/17-05-SUMMARY.md`
</output>
