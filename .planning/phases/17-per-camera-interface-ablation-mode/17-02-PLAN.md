---
phase: 17-per-camera-interface-ablation-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aquacal/config/schema.py
  - src/aquacal/calibration/pipeline.py
  - src/aquacal/cli.py
  - docs/guide/refractive_geometry.md
  - tests/unit/test_pipeline.py
autonomous: true
requirements: [IFACE-01]
must_haves:
  truths:
    - "CalibrationConfig has a shared_interface: bool = True field, documented as an analysis/ablation option not a recommended setting"
    - "load_config accepts interface.shared_interface as pass-through (no cross-field validation), defaulting to True when absent"
    - "aquacal init writes a commented-out, self-describing shared_interface line in the interface section"
    - "The refractive-geometry docs carry a short stub noting the per-camera ablation mode exists and is analysis-only"
    - "In per-camera mode (shared_interface=False) a partial initial_water_z dict passes load_config (the 'must cover all cameras' hard-fail is skipped) so it can reach the pipeline's per-camera seed resolver; in shared mode a partial dict still raises exactly as today"
  artifacts:
    - path: "src/aquacal/config/schema.py"
      provides: "shared_interface config field with ablation-framing docstring"
      contains: "shared_interface"
    - path: "src/aquacal/calibration/pipeline.py"
      provides: "load_config pass-through of shared_interface"
      contains: "shared_interface"
    - path: "src/aquacal/cli.py"
      provides: "commented shared_interface line in the generated config template"
      contains: "shared_interface"
    - path: "docs/guide/refractive_geometry.md"
      provides: "short ablation-mode stub"
      contains: "shared_interface"
  key_links:
    - from: "src/aquacal/calibration/pipeline.py (load_config)"
      to: "CalibrationConfig(shared_interface=...)"
      via: "interface.get('shared_interface', True)"
      pattern: "shared_interface"
---

<objective>
Expose the `shared_interface` ablation flag at the config surface: add the field to
`CalibrationConfig`, pass it through the YAML loader, emit a discoverable commented-out line from
`aquacal init`, and add a short docs stub where the refractive/interface options are described. The
one piece of loader logic that IS coupled to the flag: the existing "initial_water_z dict must cover
all cameras" hard-fail must become CONDITIONAL — it still raises in shared mode (default), but is
skipped in per-camera mode so a partial dict can reach the pipeline's per-camera seed resolver
(plan 17-04). This is the IFACE-01 config-and-framing half; the runtime WARNING and the optimizer
wiring are plan 17-03.

Purpose: make the ablation mode discoverable and self-describing without a CLI flag, while framing
it (in the field docstring, the generated config comment, and the docs stub) as analysis/ablation
only — the shared-interface assumption underlies the paper's central claim.
Output: `shared_interface` field + loader pass-through + init template line + docs stub, with a
loader test.
</objective>

<execution_context>
@C:/Users/tucke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tucke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-per-camera-interface-ablation-mode/17-CONTEXT.md

<interfaces>
CalibrationConfig dataclass lives in src/aquacal/config/schema.py (fields end ~line 341, near
refine_intrinsics / save_stage_calibrations / save_optimization_trace / save_conditioning / seed /
initial_water_z). Field docstrings are in the class docstring Args block (~lines 242-296).

load_config is in src/aquacal/calibration/pipeline.py:
  - line 257: `interface = data.get("interface", {})`; n_air/n_water/normal_fixed parsed lines 258-260.
  - lines 262-368: initial_water_z parsing. TWO near-identical dict branches exist: the deprecated
    `initial_distances` branch (~283-319) and the `initial_water_z` branch (~332-368). BOTH contain
    the SAME missing-camera hard-fail:
        missing_cameras = set(data["cameras"]) - set(raw_distances.keys())
        if missing_cameras:
            raise ValueError("initial_water_z dict must cover all cameras. Missing: ...")
    This raise runs unconditionally today and rejects any partial dict before the pipeline is reached.
    The shared-mode consumption of initial_water_z (scalar handling, positive-value checks,
    extra-camera warning, and the final dict build) stays untouched; only the missing-camera raise is
    gated on shared_interface.
  - lines 416-423: observability + seed parsing (the `internals`/`seed` block).
  - lines 425-459: `return CalibrationConfig(...)` construction.

aquacal init config template is in src/aquacal/cli.py, `interface:` section built ~lines 580-593
(n_air / n_water / normal_fixed / commented initial_water_z). The `internals:` block (~lines 619-622)
already models the "commented-out line with inline note" style.

docs/guide/refractive_geometry.md, lines 105-114, is the "Gotcha: water_z is a Z-coordinate"
admonition ending with "all cameras share the same water_z value (the global water surface Z)" —
the natural anchor for the ablation-mode stub.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared_interface field to CalibrationConfig</name>
  <files>src/aquacal/config/schema.py</files>
  <action>
Add a `shared_interface: bool = True` field to `CalibrationConfig`, placed with the other
optimization/behavior flags (e.g. near `refine_intrinsics` or the save_* flags — before
`initial_water_z` is fine). Add a corresponding entry to the class docstring Args block that frames
it exactly as an analysis/ablation option, NOT a recommended setting: it makes each optimized camera
solve its own water_z; the shared-interface assumption underlies the paper's central claim; per-camera
mode exists only for degeneracy/ablation analysis and is not recommended for production calibration.
Keep the default True. Do not add any __post_init__ validation coupling it to other fields.
  </action>
  <verify>
    <automated>python -c "from aquacal.config.schema import CalibrationConfig; import dataclasses; f={x.name:x for x in dataclasses.fields(CalibrationConfig)}; assert 'shared_interface' in f and f['shared_interface'].default is True; print('ok')"</automated>
  </verify>
  <done>CalibrationConfig.shared_interface exists, defaults True, and its docstring frames it as
  analysis/ablation only.</done>
</task>

<task type="auto">
  <name>Task 2: Loader pass-through + conditional coverage gate + aquacal init template line</name>
  <files>src/aquacal/calibration/pipeline.py, src/aquacal/cli.py</files>
  <action>
pipeline.py load_config:
  1. Parse the flag EARLY — immediately after `normal_fixed = interface.get("normal_fixed", True)`
     (line ~260), and BEFORE the `# Parse initial_water_z` block at line ~262, read
     `shared_interface = bool(interface.get("shared_interface", True))`. It must be in scope before
     the two dict branches so the coverage check can see it. Pass-through only — no other cross-field
     validation at load time (per CONTEXT: the per-camera path's own seed/key warnings, added in plan
     17-04, cover the real edge cases).
  2. Gate the missing-camera hard-fail in BOTH dict branches (the deprecated `initial_distances`
     branch ~286-291 and the `initial_water_z` branch ~335-340). Change each
        if missing_cameras:
            raise ValueError("initial_water_z dict must cover all cameras. Missing: ...")
     to raise ONLY when `shared_interface` is True, e.g. `if missing_cameras and shared_interface:`.
     When `shared_interface` is False, do NOT raise — leave `initial_water_z` as the partial dict the
     user supplied (the pipeline's `_resolve_per_camera_water_z_seeds` in plan 17-04 fills the missing
     cameras with 0.15m and warns). Both copies MUST be changed identically. Leave the positive-value
     check, the extra-camera warning, and the scalar handling exactly as they are — only the
     missing-camera raise is conditional.
  3. Add `shared_interface=shared_interface` to the `CalibrationConfig(...)` construction (~line 425).

cli.py init template: in the `interface:` section list (near the commented `# initial_water_z` line),
add a commented-out, self-describing line EXACTLY of the form:
  `  # shared_interface: true  # set false for per-camera water_z ablation (analysis only, not recommended)`
Place it as its own list entry so it renders inert by default but discoverable.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_pipeline.py -q -k "config or load or init or water_z or shared_interface"</automated>
  </verify>
  <done>load_config parses shared_interface early (default True), passes it to CalibrationConfig, and
  gates BOTH missing-camera raises on shared_interface (raises in shared mode, skips in per-camera
  mode); the generated config template contains the commented shared_interface line.</done>
</task>

<task type="auto">
  <name>Task 3: Docs stub + loader test</name>
  <files>docs/guide/refractive_geometry.md, tests/unit/test_pipeline.py</files>
  <action>
docs/guide/refractive_geometry.md: immediately after the "Gotcha: water_z is a Z-coordinate"
admonition (~line 114, where it states all cameras share one water_z), add a SHORT stub (a few
sentences or a small admonition) noting that an opt-in `shared_interface: false` config option makes
each camera solve its own water_z, that it exists only for degeneracy/ablation analysis (e.g. the WP6
experiment) and is NOT a recommended production setting, and that the full write-up is deferred to a
later documentation pass. Do NOT write the full worked example — stub only.

tests/unit/test_pipeline.py: add load_config tests (model after existing config-loading tests; build
the YAML the same way the neighboring load_config tests do — temp file / dict):
  (a) `interface: {shared_interface: false}` yields config.shared_interface is False;
  (b) YAML omitting it yields config.shared_interface is True (default);
  (c) CONDITIONAL COVERAGE GATE — a partial `initial_water_z` dict (missing at least one camera) with
     `shared_interface: false` loads WITHOUT raising, and config.initial_water_z is the partial dict as
     supplied (the pipeline fills it later); the SAME partial dict with `shared_interface: true` (or
     omitted) still raises ValueError mentioning the missing camera(s) — proving shared-mode behavior
     is unchanged. Cover both key spellings is not required, but the test should exercise the modern
     `initial_water_z` key at minimum.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_pipeline.py -q -k "shared_interface or config or load or water_z"</automated>
  </verify>
  <done>refractive_geometry.md carries the ablation stub; loader tests prove shared_interface parses
  (False when set, True by default) AND that a partial initial_water_z dict is accepted in per-camera
  mode but still rejected in shared mode.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/test_pipeline.py -q` passes, including the new shared_interface loader test.
- `grep -n "shared_interface" src/aquacal/config/schema.py src/aquacal/calibration/pipeline.py src/aquacal/cli.py docs/guide/refractive_geometry.md` shows all four surfaces updated.
- No regression: `python -m pytest tests/ -m "not slow" -q`.
</verification>

<success_criteria>
- shared_interface: bool = True exists on CalibrationConfig with ablation-framing docstring.
- YAML loader parses it early and passes it through; default True.
- The "must cover all cameras" hard-fail is gated on shared_interface in BOTH dict branches: raises in
  shared mode, skipped in per-camera mode so a partial dict reaches the pipeline's seed resolver.
- aquacal init emits the commented, inline-noted shared_interface line.
- A short ablation-only docs stub exists where refractive options are described.
</success_criteria>

<output>
After completion, create `.planning/phases/17-per-camera-interface-ablation-mode/17-02-SUMMARY.md`
</output>
