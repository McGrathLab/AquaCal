---
phase: 29.2-merge-release-and-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - .planning/phases/29.2-merge-release-and-publish/29.2-release-rehearsal.txt
  - .planning/phases/29.2-merge-release-and-publish/29.2-sdist-listing.txt
autonomous: true
requirements: [RUN-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The release toolchain `release.yml` installs is pinned to an exact, verified-working pair — `python-semantic-release==10.6.1` and `gitpython==3.1.59` (D-29.2-28) — so a dependency released the day before the merge cannot stop the release job from starting."
    - "`semantic-release version --print`, run against a real `--no-ff` merge of `results/rerun-freeze-02` into a branch literally named `main`, prints a string that equals `2.1.0` exactly — not merely 'a version appeared' and not `3.0.0` (D-29.2-18, D-29.2-20, D-29.2-30)."
    - "The rehearsal produces the actual release commit, the annotated `v2.1.0` tag, the bumped `pyproject.toml` and `CITATION.cff`, and the generated `CHANGELOG.md` section — so every element of ROADMAP criterion 3 is seen before any irreversible act, not after."
    - "The sdist that would reach PyPI carries `src/` plus five metadata files and nothing else; the assertion is the member list, and the size is recorded as corroborating context with its explanation (D-29.2-32)."
    - "Every mutation in this plan happens inside a throwaway `/tmp` clone whose `origin` points at a local bare repository, so no push, tag or merge can reach GitHub or the working repository (D-29.2-31)."
    - "The generated `CHANGELOG.md` is read and its size recorded before the tag is public, and it ships as generated (D-29.2-29)."
    - statement: "The commit-count and conventional-type figures in ROADMAP Phase 29.2 are re-derived from the parser itself at rehearsal time rather than carried forward, because they were already stale once (D-29.2-19)."
      verification: backstop
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "The pinned release toolchain with its rationale stated at the constant"
      contains: "python-semantic-release=="
    - path: ".planning/phases/29.2-merge-release-and-publish/29.2-release-rehearsal.txt"
      provides: "The framed rehearsal transcript — question, pass condition, verbatim commands with exit codes, prohibitions honoured"
    - path: ".planning/phases/29.2-merge-release-and-publish/29.2-sdist-listing.txt"
      provides: "The sdist member list with the 1.8.0 -> 2.0.1 -> 2.1.0 size series and its explanation"
  key_links:
    - "`.github/workflows/release.yml`'s pinned version literals are the SAME strings the rehearsal venv installs — the executor reads the pins out of the workflow file rather than retyping them, so the rehearsal cannot silently test a different toolchain than CI will run (D-29.2-28b)."
    - "The clone's `origin` remote is re-pointed to a local bare repository BEFORE `semantic-release version` runs. A clone of a local repository inherits that repository's path as `origin`, so skipping this step would push the release commit and the `v2.1.0` tag into the working repository."
    - "`pyproject.toml` `[tool.semantic_release] build_command = \"python -m build\"` is what produces the sdist during the rehearsal, so the artifact inspected in Task 3 is the one PSR itself would build."
  prohibitions:
    - "Never create, rename, switch, merge or delete a branch in the working repository. The merge simulation belongs in the /tmp clone."
    - "Never push, tag, or create a GitHub release from this plan. The rehearsal's remote must be a local bare repository."
    - "Never reword, amend, rebase or drop any commit on `results/rerun-freeze-02` — rewriting one rewrites every sha after it, including shas the `rerun-freeze-02` tag and the Phase 29 records cite."
    - "Never assert the sdist is healthy by its byte size. A healthy 2.1.0 sdist is 1.7x larger than 1.8.0's."
    - "Never edit `pyproject.toml`, `CITATION.cff` or `CHANGELOG.md` by hand — semantic-release writes all three."
    - "Never report only the flattering number. Record what the command printed, including a figure that disagrees with an expectation."
---

<objective>
Pin the release toolchain, then rehearse the ENTIRE release end to end in a throwaway clone —
merge, version arithmetic, commit, tag, changelog and built artifact — so that every fact ROADMAP
criteria 2 and 3 depend on is measured before anyone touches an irreversible control.

Purpose: this is the phase's tracer slice. It wires one path through every layer the release
touches (dependency pin -> toolchain -> merge commit -> parser -> version files -> tag ->
`build_command` -> sdist) and it is completely reversible, because it happens in `/tmp`. Research
found three defects by executing rather than reasoning; the most serious of them —
`release.yml` cannot start at all today (D-29.2-28) — would have failed the release *after* the
merge, on a one-way-door day. Running this first turns that class of surprise into a measurement.

Output: a pinned `release.yml`, a framed rehearsal transcript, and an sdist member listing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.2-merge-release-and-publish/29.2-CONTEXT.md
@.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md
@.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md
@.planning/phases/29.2-merge-release-and-publish/29.2-VALIDATION.md
</context>

## Reading order, and which document wins

Read `29.2-CONTEXT.md` first, then `29.2-RESEARCH.md`. RESEARCH.md's section
*"THREE DECISIONS REST ON A WRONG PREMISE — READ THIS FIRST"* is an authoritative correction layer
over CONTEXT.md, and CONTEXT.md has already been amended to absorb it. **Where the two still appear
to disagree, RESEARCH.md's measurements win**, because they were executed rather than reasoned.
RESEARCH.md is dated 2026-08-26 and declares itself valid for seven days; Finding 1 is a live
upstream situation.

## Artifacts this phase produces

The full phase-level list, so no downstream reader treats any of these as drift. This plan owns
rows 1-3.

| Artifact | Kind | Owning plan |
|---|---|---|
| `.github/workflows/release.yml` — pinned install line + rationale block | modified | 29.2-01 |
| `29.2-release-rehearsal.txt` | new evidence file | 29.2-01 |
| `29.2-sdist-listing.txt` | new evidence file | 29.2-01 |
| `.pre-commit-config.yaml` — corrected `exclude:` on `trailing-whitespace` and `end-of-file-fixer`; `.planning/` arm on `detect-secrets`; three rationale blocks | modified | 29.2-02 |
| `tests/unit/test_optim_common.py` — two assertions converted to ULP bounds (`maxulp=4`, `maxulp=8`) plus the rewritten docstring | modified | 29.2-03 |
| `29.2-smoke-runs.txt` | new evidence file | 29.2-03 |
| `.planning/todos/pending/2026-08-26-d4-exact-equality-anchors-are-platform-pinned.md` | modified | 29.2-03 |
| `29.2-precommit-allfiles-proof.txt` | new evidence file | 29.2-04 |
| The pull request into `main` | new service-side state | 29.2-04 |
| `29.2-release-verification.txt` | new evidence file | 29.2-05 |
| `29.2-publication-record.txt` | new evidence file | 29.2-06, 29.2-07 |
| `29.2-PHASE-RECORD.md` | new phase record | 29.2-08 |
| Zenodo Record B published; Record A's `isSourceOf` back-link | new service-side state | 29.2-07 |

<tasks>

<task type="auto">
  <name>Task 1: Pin the release toolchain in release.yml, with the reason at the constant</name>
  <reversibility rating="reversible">A two-line workflow edit; undone by deleting the version specifiers.</reversibility>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - `.github/workflows/release.yml` — the whole file is 33 lines. Line 27 is the install step being changed. **Line 10 carries `if: "!startsWith(github.event.head_commit.message, 'chore(release):')"` — the loop-breaker that has correctly suppressed a release loop on every cut in this repository's history (RESEARCH §Q6b, measured three times). Do not disturb it while editing the adjacent step.**
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *FINDING 1* — the measured break, the two upstream issue numbers, the open-and-unmerged fix PR, and the 3.1.59-works / 3.1.60-fails table.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md` § *2. `.github/workflows/release.yml`* — the house conventions to preserve, and the finding that **no workflow in this repository pins anything**, so there is no house style for the pin itself.
    - `.pre-commit-config.yaml` lines 24-54 — the rationale-block shape this comment must follow (comment inside the step, directly above the value; names the cause; states why the alternative cannot hold; names the retirement condition).
  </read_first>
  <action>
Change the `Install python-semantic-release and build` step so it installs an exact, verified-working
pair rather than resolving whatever is latest at merge time. Per D-29.2-28 pin **both** packages by
exact version: `python-semantic-release` at `10.6.1` and `gitpython` at `3.1.59`. Keep `build` on the
same line — that idiom is stable across this repository (`publish.yml` uses it too) — and leave it
unpinned unless doing so breaks the `- name:` / `run:` one-liner shape the other five workflows share.

Add a comment block immediately above the `run:` line, inside the step mapping, following the
`.pre-commit-config.yaml` rationale convention rather than the sparse workflow comment style.
PATTERNS.md § 2 rules that this is the right shape because the pin narrows a control in response to
a live upstream cause. The block must state, in this order:

1. **What the pin governs** — the toolchain the release job installs.
2. **The measured cause** — GitPython 3.1.60 (released 2026-08-25) removed the `Actor.name_email_regex`
   class attribute as ReDoS hardening; python-semantic-release reads that attribute unconditionally
   while loading its config at `semantic_release/cli/config.py:745`, so every `semantic-release`
   subcommand dies before touching anything. Give the measurement: 3.1.59 works, 3.1.60 raises an
   `AttributeError` immediately.
3. **Why the alternative cannot hold** — PSR declares `gitpython~=3.0`, so a fresh unpinned install
   resolves to the broken version; PSR's own latest release (10.6.1, 2026-07-06) predates the removal
   and its fix PR is open and unmerged, so **there is no released PSR that works with the current
   GitPython**. Bounding only one of the two packages therefore does not close it.
4. **Why exactness rather than a floor or a ceiling** — this workflow runs on one-way-door days and
   this phase exists because a floating dependency moved on one. Reproducibility beats freshness here.
5. **The retirement condition** — drop the pin once a released PSR version ships the fix (upstream
   issues 1475 and 1476, PR 1477).

Preserve every surrounding house convention: `- name:` on the step, `run:` inline for the one-liner,
`actions/checkout@v4` and `actions/setup-python@v5` untouched, the quoted `python-version: '3.12'`
untouched, and `${{ secrets.RELEASE_TOKEN }}` referenced exactly as it is today.

State in the comment that no other workflow in this repository pins anything, so this step
deliberately diverges from house style rather than matching it — PATTERNS.md § *No Analog Found*
asks that this be recorded rather than papered over.

Do not touch any other step, any other workflow, or `pyproject.toml`.
  </action>
  <verify>
    <automated>grep -qE 'python-semantic-release==10\.6\.1' .github/workflows/release.yml &amp;&amp; grep -qE 'gitpython==3\.1\.59' .github/workflows/release.yml &amp;&amp; grep -qF "!startsWith(github.event.head_commit.message, 'chore(release):')" .github/workflows/release.yml &amp;&amp; python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/release.yml')); j=d['jobs']['release']; assert len(j['steps'])==4, j; assert j['steps'][3]['env']['GH_TOKEN']; print('release.yml parses, 4 steps, token step intact')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/release.yml` names both `python-semantic-release==10.6.1` and `gitpython==3.1.59` on the install step.
    - The workflow still parses as YAML and the `release` job still has exactly four steps in the original order.
    - The `chore(release):` loop-breaker on the job's `if:` is byte-identical to what it was before this task.
    - A comment block sits inside the install step, above `run:`, and names: GitPython 3.1.60, `Actor.name_email_regex`, `semantic_release/cli/config.py:745`, the fact that no released PSR works with current GitPython, and the retirement condition.
    - `git diff --stat -- .github/workflows/release.yml` shows changes confined to that one file, and `git diff --quiet -- pyproject.toml CITATION.cff CHANGELOG.md` exits 0.
  </acceptance_criteria>
  <done>The release job installs a deterministic toolchain, and the next reader of that line learns why from the line itself rather than from a commit message.</done>
</task>

<task type="tracer">
  <name>Task 2: Rehearse the whole release end to end in a throwaway clone, and assert the bump is exactly 2.1.0</name>
  <precondition>`git rev-parse --abbrev-ref HEAD` reports `results/rerun-freeze-02`, `git rev-parse --verify refs/remotes/origin/main` resolves, and `python3 -m venv --help` succeeds — the rehearsal clones from the working repository's own refs and needs an isolated interpreter.</precondition>
  <files>.planning/phases/29.2-merge-release-and-publish/29.2-release-rehearsal.txt</files>
  <read_first>
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *Q1* end to end — in particular Q1d (the `--print` flag table and the rejected flag ordering), Q1e (the branch-name constraint and the clone resolution), and Q1-REHEARSAL (the executed result this task reproduces: commit `chore(release): 2.1.0`, annotated tag, 1,196 changelog lines, a 200,937-byte sdist).
    - `pyproject.toml` lines 129-143 — `[tool.semantic_release]`: `version_toml`, `version_variables`, `commit_message`, `build_command`, `tag_format`, and the `commit_parser_options` tag lists. Note RESEARCH §Q1.0's finding that `branch = "main"` on line 132 is a dead PSR-v7 key and is **not** what enforces the branch-name constraint.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md` § *4. Evidence artifacts* — the four conventions this transcript must carry: the self-naming banner with a provenance key/value block, `Question:` and `Pass condition:` stated **before** any number, `  $ <command>` -> raw output -> `(exit code N)`, and a prohibitions-honoured block backed by measurements.
    - `.github/workflows/release.yml` — read the pinned literals back out of the file rather than retyping them.
  </read_first>
  <action>
Run the entire release, for real, in a disposable clone. Nothing in this task may touch the working
repository beyond a read-only `git fetch origin` (permitted, and necessary so `origin/main` is
current; it updates remote-tracking refs only and creates, renames and switches nothing).

Use the fixed scratch root `/tmp/aquacal-29.2-rehearsal` so later tasks and the evidence file can
name the same paths. Build it in this order, and treat step 3 as the safety gate the whole task
rests on:

1. **Clone.** `git clone --no-hardlinks --no-local` the working repository into
   `/tmp/aquacal-29.2-rehearsal/repo`. Both flags together force a full object copy; the `.git`
   directory is 25 MB, so the copy is free and cannot share objects with the original.
2. **Materialise a branch literally named `main` inside the clone.** The working repository has no
   local `main` — only the remote-tracking ref — and a clone of a local repository copies local heads
   only. Fetch it across explicitly with a refspec that maps the source's remote-tracking ref to a
   local head in the clone. PSR matches the checked-out branch name against its release-group pattern,
   which defaults to `(main|master)`, and refuses with a *message* rather than a version on anything
   else — a plan that grepped for a version string would read that refusal as "no output" instead of
   as an error (D-29.2-31, RESEARCH §Q1e).
3. **Re-point the clone's `origin` at a local bare repository, and prove it.** A clone of a local
   repository inherits that repository's filesystem path as `origin`. `semantic-release version`
   pushes. Initialise a bare repository under the scratch root, `git remote set-url origin` to it,
   and assert with a string equality that `git remote get-url origin` is now exactly that bare path.
   **Do not proceed past this step until that equality holds.** Also run PSR with `GH_TOKEN` and
   `GITHUB_TOKEN` removed from the environment and pass the flag that suppresses VCS release
   creation, so even a misconfigured remote cannot reach an API.
4. **Build the pinned venv from the workflow's own literals.** Create a venv under the scratch root
   and install the exact specifiers **extracted from `.github/workflows/release.yml` by grep**, plus
   `build`. Do not retype the versions — reading them out of the workflow is what makes this a
   rehearsal of CI rather than a rehearsal of the planner's memory (D-29.2-28b). Record the resolved
   versions of `python-semantic-release` and `gitpython` from `pip show`, and assert
   `semantic-release --version` exits 0. That single assertion is what Finding 1 costs to detect.
5. **Simulate the merge.** Check out `main` in the clone and merge `results/rerun-freeze-02` with an
   explicit no-fast-forward merge and a merge message shaped like GitHub's
   (`Merge pull request #<n> from McGrathLab/results/rerun-freeze-02`). RESEARCH §Q6a records that
   the merge commit's own message is what `release.yml`'s loop-breaker sees, and §Q1a records that
   PSR ignores the merge commit's message entirely — what matters is that the branch's commits become
   reachable. Assert the merge commit has two parents.
6. **Re-derive D-29.2-19's figures from the parser's own inputs**, because the ROADMAP's 406/43/29
   were already stale once and RESEARCH measured 431 at its own run: count commits in
   `origin/main..results/rerun-freeze-02`, and count `feat`, `fix` and any subject carrying an
   exclamation-marked type. **Record the numbers that come out, not the numbers anyone expected.**
7. **Assert the bump.** Run `semantic-release version --print` from the pinned venv and assert its
   output **string-equals** `2.1.0`. Use `--print`; the flag in the other position that CONTEXT.md's
   parenthetical named is rejected outright by the CLI (D-29.2-30, RESEARCH §Q1d). This one assertion
   is what settles D-29.2-20's open question about the `docs(roadmap):` commit whose body contains a
   breaking-change phrase mid-sentence: if the footer regex matched it, this prints `3.0.0`.
8. **Run the release for real, in the clone.** Execute the full `semantic-release version` with VCS
   release suppressed. Then inspect and record: the release commit's subject and its stat block; that
   the tag object is an annotated tag rather than a lightweight ref; that `pyproject.toml` and
   `CITATION.cff` both read `2.1.0` at the tag; and the size of the `CHANGELOG.md` insertion.
9. **Read the changelog.** D-29.2-29 rules that it ships as generated. Record its line count, its
   entry count, its sub-headings, and the fact that a substantial minority of entries carry internal
   plan scopes. This is the review the author would otherwise only get after the tag was public
   (RESEARCH §Q6e). It is recorded, not gated.

Write `29.2-release-rehearsal.txt` in the established evidence-file shape. It must open by naming
itself and stating the **Question** and the **Pass condition** before any number appears, so the bar
cannot be retrofitted to the result. Its provenance block records the scratch paths, the resolved
toolchain versions, the source sha, and the merge commit. Every command appears verbatim with its
raw output and its exit code. It closes with a **prohibitions-honoured** block, each line backed by a
measurement: the working repository's `git status --porcelain` output before and after are identical;
`git branch --list` in the working repository is unchanged; the clone's `origin` was a local bare
path; nothing was pushed; no committed artifact was regenerated; and a `Scope:` paragraph stating
what the rehearsal does NOT establish — it proves the bump arithmetic and the artifact, not GitHub's
behaviour, and per D-29.2-28b it must be re-run immediately before the merge because the upstream
situation is live.

Leave the scratch root in place; plan 29.2-05 re-runs against it.
  </action>
  <verify>
    <automated>test -f /tmp/aquacal-29.2-rehearsal/repo/dist/aquacal-2.1.0.tar.gz &amp;&amp; [ "$(git -C /tmp/aquacal-29.2-rehearsal/repo remote get-url origin)" = "/tmp/aquacal-29.2-rehearsal/fakeremote.git" ] &amp;&amp; [ "$(git -C /tmp/aquacal-29.2-rehearsal/repo cat-file -t v2.1.0)" = "tag" ] &amp;&amp; [ "$(git -C /tmp/aquacal-29.2-rehearsal/repo show v2.1.0:pyproject.toml | sed -n 's/^version = "\(.*\)"$/\1/p' | head -1)" = "2.1.0" ] &amp;&amp; [ "$(git -C /tmp/aquacal-29.2-rehearsal/repo show v2.1.0:CITATION.cff | sed -n 's/^version: *//p' | head -1)" = "2.1.0" ] &amp;&amp; grep -q 'Pass condition:' .planning/phases/29.2-merge-release-and-publish/29.2-release-rehearsal.txt &amp;&amp; grep -q '2.1.0' .planning/phases/29.2-merge-release-and-publish/29.2-release-rehearsal.txt &amp;&amp; git diff --quiet &amp;&amp; [ "$(git rev-parse --abbrev-ref HEAD)" = "results/rerun-freeze-02" ] &amp;&amp; [ "$(git branch --list --format='%(refname:short)' | tr '\n' ',')" = "results/rerun-freeze-01,results/rerun-freeze-02," ]</automated>
  </verify>
  <acceptance_criteria>
    - `semantic-release version --print`, run in the clone on a branch named `main` after the no-fast-forward merge, produced output that string-equals `2.1.0`. The transcript records the raw output and its exit code.
    - The clone's `origin` URL equals the local bare repository path — asserted by string equality, before any PSR invocation.
    - The clone carries an annotated `v2.1.0` tag; `pyproject.toml` and `CITATION.cff` both read `2.1.0` at that tag; a `dist/aquacal-2.1.0.tar.gz` exists.
    - The transcript records the resolved `python-semantic-release` and `gitpython` versions, and those match the specifiers grepped out of `.github/workflows/release.yml`.
    - The transcript records the re-derived commit count and the `feat` / `fix` / breaking-marker counts as measured, alongside the ROADMAP's stale 406/43/29, with both numbers visible.
    - The transcript records `CHANGELOG.md`'s generated line count and entry count.
    - The working repository is untouched: `git diff --quiet` exits 0, HEAD is still `results/rerun-freeze-02`, and `git branch --list` still names exactly the two `results/*` branches.
    - The transcript carries a `Scope:` paragraph naming what the rehearsal does not establish, and a prohibitions-honoured block whose lines each cite a measurement.
  </acceptance_criteria>
  <done>Every element of ROADMAP criteria 2 and 3 — a merge commit, an exactly-2.1.0 bump, the tag, both version files, the changelog and the built artifact — has been produced and inspected once, with no real branch, remote, tag or artifact touched.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the sdist's member list, and record the size series with its explanation</name>
  <precondition>`/tmp/aquacal-29.2-rehearsal/repo/dist/aquacal-2.1.0.tar.gz` exists — Task 2's `build_command` produced it.</precondition>
  <files>.planning/phases/29.2-merge-release-and-publish/29.2-sdist-listing.txt</files>
  <read_first>
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *Q4* — Q4a's measured member list, Q4b's finding that the ABSENCE of a `setuptools-scm`-style file-finder entry point is the entire reason the sdist is clean, Q4c's listing commands, and Q4d's size table (1.8.0 at 120,434 B, 2.0.1 at 174,448 B, 2.1.0 at 200,937 B) with the ruling that D-29.2-17's "in line with 1.8.0's" would false-alarm on a healthy artifact.
    - `pyproject.toml` lines 1-3 and 80-88 — `[build-system] requires`, `[tool.setuptools.packages.find] where = ["src"]`, and `[tool.setuptools.package-data]`. There is no `MANIFEST.in` in this repository.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md` § *4* — the headline-numbers-with-`expected` table shape from `29-commit-manifest.txt`, which is what this file's summary block should look like.
  </read_first>
  <action>
List the members of the rehearsal's sdist and assert the list positively — a whitelist of what IS
there, which is strictly stronger than a blacklist of what is not, and which is what D-29.2-32 means
by "assert the member list, never the size".

Strip the leading `aquacal-2.1.0/` component from each entry, reduce to distinct top-level names, and
assert that set is exactly: `LICENSE`, `PKG-INFO`, `README.md`, `pyproject.toml`, `setup.cfg`, `src`.
Also list the wheel's top-level entries for completeness.

Write `29.2-sdist-listing.txt` carrying:

- The self-naming banner and provenance block (which build, in which clone, at which sha).
- The **measured-beside-expected** summary table: the distinct top-level member set against its
  expectation, the number of entries under `src/`, and the archive's byte size.
- The **size series with its explanation**, stated so a reader cannot mistake it for a finding:
  120,434 B at 1.8.0 -> 174,448 B at 2.0.1 -> 200,937 B at 2.1.0, monotonic, and attributable to real
  library growth across phases 23-26 rather than artifact leakage — which the member list above
  proves independently. State plainly that D-29.2-17's original size-comparison instruction, taken
  literally, would have raised a false alarm on a healthy artifact, and that the member list is the
  assertion while the size is corroboration.
- **Why the sdist is clean, recorded at the check** so a future contributor understands what they
  would be turning on: this project has no `MANIFEST.in`, and its build requirements name no
  file-finder plugin. Such a plugin replaces setuptools' narrow default walk with "every file tracked
  by git", which in this repository would sweep in all 1,693 tracked files including the committed
  run artifacts and the planning tree. The absence of one is the reason, not luck.
- A prohibitions-honoured block: the build ran in the clone, not the project tree; nothing was
  uploaded anywhere; no packaging configuration was changed. D-29.2-17's caution is honoured by
  verifying, and an explicit pin of the sdist's file set stays deferred (CONTEXT § Deferred Ideas).
  </action>
  <verify>
    <automated>[ "$(tar -tzf /tmp/aquacal-29.2-rehearsal/repo/dist/aquacal-2.1.0.tar.gz | sed 's#^aquacal-2\.1\.0/##' | cut -d/ -f1 | grep -v '^$' | sort -u | tr '\n' ',')" = "LICENSE,PKG-INFO,README.md,pyproject.toml,setup.cfg,src," ] &amp;&amp; grep -q '120,434' .planning/phases/29.2-merge-release-and-publish/29.2-sdist-listing.txt &amp;&amp; grep -q '200,937' .planning/phases/29.2-merge-release-and-publish/29.2-sdist-listing.txt &amp;&amp; grep -qi 'MANIFEST.in' .planning/phases/29.2-merge-release-and-publish/29.2-sdist-listing.txt</automated>
  </verify>
  <acceptance_criteria>
    - The distinct top-level member set of `aquacal-2.1.0.tar.gz` equals exactly the six expected names, asserted by string equality rather than by a pattern search.
    - `29.2-sdist-listing.txt` records the measured member set beside its expectation.
    - The file records all three sdist sizes with the monotonic-growth explanation, and states explicitly that a literal size comparison against 1.8.0 would have false-alarmed.
    - The file records that there is no `MANIFEST.in` and no file-finder plugin, and what adding one would turn on.
    - The file carries a prohibitions-honoured block confirming the build happened in the clone and nothing was uploaded.
  </acceptance_criteria>
  <done>The artifact that would reach PyPI has been built and enumerated, and the one check standing between a permanent upload and a leaked artifact has been made against the property rather than against a proxy for it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public package index -> GitHub Actions runner | `pip install` in `release.yml` resolves third-party code at job time and executes it with `contents: write` and a PAT in the checkout's auth header |
| working repository -> `/tmp` clone -> `origin` remote | the rehearsal runs a tool that commits, tags and **pushes**; a mis-set remote sends all three to the real repository or to GitHub |
| built artifact -> PyPI | the sdist enumerated here is shape-identical to the one a permanent upload would carry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.2-01 | Tampering | `.github/workflows/release.yml` install step | high | mitigate | Task 1 pins `python-semantic-release==10.6.1` and `gitpython==3.1.59` exactly, so the release job cannot execute a version of either package that nobody has run. The pin is verified end to end by Task 2, which installs from the workflow file's own literals. |
| T-29.2-02 | Tampering | the rehearsal's `origin` remote | high | mitigate | Task 2 step 3 re-points `origin` to a local bare repository and gates further execution on a string-equality assertion of `git remote get-url origin`; PSR additionally runs with `GH_TOKEN`/`GITHUB_TOKEN` stripped and VCS-release creation suppressed. |
| T-29.2-03 | Tampering | `aquacal-2.1.0.tar.gz` members | medium | mitigate | Task 3 asserts a positive whitelist of top-level members before any upload path exists. The default setuptools walk is narrow (no `MANIFEST.in`, no file-finder plugin), so this is a low-likelihood/high-impact check kept because a PyPI version can never be replaced. |
| T-29.2-04 | Spoofing | `secrets.RELEASE_TOKEN` (PAT) validity | medium | accept | `gh secret list` proves existence and creation date, never validity. Expiry produces a loud `actions/checkout` failure at step 1 with nothing published; recovery is regenerating the PAT and re-running. Carried as a watch-point in plan 29.2-05, not as work here. |
| T-29.2-05 | Denial of Service | `semantic-release version` aborting mid-run | low | accept | Measured benign (D-29.2-33): PSR writes the version files, runs `build_command`, and aborts before commit, tag and push. The runner's checkout is ephemeral, so a half-way abort leaves `main` untouched and nothing half-published. |
| T-29.2-SC | Tampering | `pip install` of the pinned release toolchain | high | mitigate | RESEARCH § *Package Legitimacy Audit* checked `python-semantic-release`, `GitPython` and `build` against the PyPI JSON API: all three are long-established (2015, 2008, PyPA), all map to their stated source repositories, and there are no `[ASSUMED]`, `[SUS]` or `[SLOP]` verdicts. This phase adds no new package to the project; the only dependency-specification change is adding an upper bound to something already installed, which narrows rather than widens the supply-chain surface. No legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.github/workflows/release.yml` parses, names both exact pins, and its `chore(release):` loop-breaker is unchanged.
- `semantic-release version --print` in the clone printed a string equal to `2.1.0`.
- The clone carries an annotated `v2.1.0` tag with both version files reading `2.1.0`.
- The sdist's distinct top-level members equal the six expected names exactly.
- The working repository is byte-unchanged apart from the two new evidence files and the one workflow edit: `git status --porcelain` names nothing under `src/`, `tests/` or `experiments/`, HEAD is still `results/rerun-freeze-02`, and `git branch --list` names exactly two branches.
- Both evidence files carry a `Question:`/`Pass condition:` opener, verbatim commands with exit codes, and a prohibitions-honoured block.
</verification>

<success_criteria>
The release pipeline has been executed once, in full, against the real commit history, with a
deterministic toolchain, before anything irreversible has been touched — and the exact version string
it produces has been asserted rather than assumed.
</success_criteria>

<output>
Create `.planning/phases/29.2-merge-release-and-publish/29.2-01-SUMMARY.md` when done.
</output>
