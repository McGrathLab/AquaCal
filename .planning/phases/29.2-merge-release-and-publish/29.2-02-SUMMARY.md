---
phase: 29.2-merge-release-and-publish
plan: 02
subsystem: infra
tags: [pre-commit, ci, byte-integrity, detect-secrets, regex-exclude]

requires:
  - phase: 29-gate-verification-results-commit
    provides: the 227-file provenance commit 70e783f whose bytes these hooks must not rewrite
  - phase: 29.1-post-run-fixes-re-freeze
    provides: the freeze01_run_output/ archive-aside move that the optional archive segment covers
provides:
  - "`trailing-whitespace` and `end-of-file-fixer` scoped by one shared, hand-alternated exclude that leaves zero leftovers for either hook across the tracked tree"
  - "a `.planning/` arm on `detect-secrets`, with the security cost written at the constant"
  - "three rationale blocks recording the measurements, each arm justified by name, each with a retirement condition"
  - "a static seven-hook accounting of CI's `pre-commit run --all-files` file selection"
affects: [29.2-04 whole-tree hook sweep, 29.2 PR into main, CI pre-commit job]

actuals:
  tokens: 2201
  tasks: 3
  commits: 2

tech-stack:
  added: []
  patterns:
    - "reason-at-the-constant: a comment block inside the hook mapping, directly above the value it governs, naming measurements rather than fears, justifying each regex arm by name and giving it a retirement condition"
    - "one hook-level exclude per hook, hand-alternated — never a second `exclude:` key, never a top-level one"

key-files:
  created: []
  modified:
    - .pre-commit-config.yaml

key-decisions:
  - "Implemented D-29.2-02b, not the superseded D-29.2-02: the mirrored results-only pattern contributes zero coverage to `trailing-whitespace`."
  - "Extended the `detect-secrets` exclude rather than regenerating `.secrets.baseline` (D-29.2-27), and folded the reasoning into the hook's existing 30-line block rather than starting a second one."
  - "Did not mark RUN-05 complete — all eight plans in this phase carry it and it closes on the Zenodo publish in plan 08, not here."

metrics:
  duration: ~25 min
  completed: 2026-08-26

status: complete
---

# Phase 29.2 Plan 02: Repair the three failing pre-commit hooks Summary

All three hooks that would fail CI's whole-tree `pre-commit run --all-files` sweep are repaired at
the configuration level — the two formatting hooks now share one corrected byte-integrity exclude
that covers the driver-state artifacts a results-only pattern never reached, and `detect-secrets`
gained a `.planning/` arm with the scope it narrows recorded beside it.

## What was built

| Task | Change | Commit |
|---|---|---|
| 1 | `trailing-whitespace`'s one-arm `^\.planning/` replaced by, and `end-of-file-fixer` given, the D-29.2-02b pattern; 76-line rationale block above the pair | `e671074` |
| 2 | `\.planning/.*` alternative added to the `detect-secrets` exclude; 28 lines appended to its existing rationale block | `d9e8f49` |
| 3 | Static seven-hook file-selection validation over the live tracked tree — read-only, no commit (it changes nothing; results are below, as the plan specified) | — |

The installed pattern, on both formatting hooks, byte-identical:

```
^(\.planning/|experiments/((pre_rerun_baseline|freeze01_run_output)/)?(driver_state/|results[^/]*/|run_experiment_suite_state\.))
```

The `detect-secrets` pattern keeps its three original alternatives and gains a fourth:

```
^(docs/tutorials/.*\.ipynb|experiments/(pre_rerun_baseline/|freeze01_run_output/)?results[^/]*/.*|tests/fixtures/.*\.json|\.planning/.*)$
```

## Task 3 — the static measurement, beside research's figures

Measured 2026-08-26 by enumerating `git ls-files` and applying each hook's own `exclude` in Python,
which is what pre-commit does after it assembles the candidate list. **No hook was run** — proving
the fix by execution is plan 29.2-04's whole-tree assertion, and a dirty tree would make it
meaningless.

| Quantity | Research (Finding 2 / Q3d) | Measured now | Difference |
|---|---|---|---|
| Tracked files | 1,693 | **1,708** | +15 |
| Excluded by the byte-integrity pattern | 1,427 | **1,442** | +15 |
| `.py` among them | 14 | **14** | 0 |
| `.py` outside `.planning/` | 0 | **0** | 0 |

**The difference is explained, not reconciled away:** the tracked tree gained exactly 15 files since
research ran, every one of them a planning document belonging to this phase (CONTEXT, RESEARCH,
PATTERNS, the plans, the todo). Both counts moved by the same 15 because all 15 are matched by the
`\.planning/` arm. A larger count here is the expected behaviour of a prefix arm over a growing
tree, not a widening of scope — the arm's boundary is unchanged.

Extension histogram of the 1,442 excluded files:

```
md 688, json 473, csv 107, log 47, done 36, txt 34, yaml 16, py 14, tex 10, tsv 6
```

Split by tree: **762 under `.planning/`, 680 under `experiments/`.** Nothing else is touched.

**Source-file exposure.** The 14 excluded `.py` files all sit under `.planning/` — nine under
`.planning/probes/` and five under `.planning/phases/*/`. They are irrelevant to linting because the
exclusion is **hook-level** and the ruff hooks are a separate `repo:` entry with their own file
selection. Confirmed programmatically that the separation still holds: the `ruff` hook carries no
`exclude` key, and the configuration has no top-level `exclude` (which would combine with rather
than replace the hook-level ones).

**`detect-secrets`:** all four representative finding paths — spanning both finding types, the
quoted-SHA `Hex High Entropy String` hits and the audit-table `Basic Auth Credentials` row — are now
excluded, and the three original arms still match `docs/tutorials/quickstart.ipynb`,
`experiments/results/benchmark.json` and `tests/fixtures/anchor.json`. The hook's `--baseline
.secrets.baseline` argument is unchanged and `.secrets.baseline` is byte-unchanged
(`git diff --quiet` clean). Its excluded set is 1,358 tracked files.

### All seven hooks accounted for

Research's own recorded lesson is that counting only the blockers you already know about is how the
third one went unnoticed. So, all seven:

| Hook | Status | Basis |
|---|---|---|
| `trailing-whitespace` | **repaired here** | would have rewritten 19 files; new pattern leaves 0 |
| `end-of-file-fixer` | **repaired here** | would have rewritten 300 files; new pattern leaves 0 |
| `detect-secrets` | **repaired here** | exited 123 on 10 findings, all `.planning/`; all now excluded |
| `ruff` (`--fix`) | clean, unchanged | RESEARCH § Q3f: `ruff check --force-exclude` over all 166 tracked `.py` — "All checks passed!" |
| `ruff-format` | clean, unchanged | RESEARCH § Q3f: "166 files already formatted" |
| `check-yaml` | clean, unchanged | RESEARCH § Q3f: `yaml.safe_load_all` over all 27 tracked YAML, 0 failures |
| `check-added-large-files` | trivial pass, unchanged | RESEARCH § Q3f: v5.0.0 filters to `git diff --staged --diff-filter=A` unless `--enforce-all`; nothing is staged on a clean CI checkout |

Note carried forward for 29.2-04: `ruff` runs with `--fix`, so if the whole-tree sweep ever finds a
lint it will **rewrite source**, and that shows up in the sweep's `git status --porcelain` assertion
as a byte-integrity failure wearing a lint-failure costume. It is clean today.

## Verification

- `.pre-commit-config.yaml` parses as YAML; both byte-integrity hooks carry the same non-empty `exclude` (asserted).
- That pattern matches all eight representative at-risk paths — the live suite-state TSV, its stage log, both archived driver-state copies, the committed results tree, the pre-run baseline tree, a `.gitkeep` and a planning markdown — and none of the four representative hand-written paths (`src/aquacal/core/refractive_geometry.py`, `tests/unit/test_optim_common.py`, `pyproject.toml`, `README.md`).
- The `detect-secrets` pattern matches all four representative finding paths, retains its three original arms, and newly excludes nothing under `src/`, `scripts/` or `.github/workflows/`.
- No top-level `exclude`; the `ruff` hook carries no `exclude`.
- `.secrets.baseline` byte-unchanged.
- `git diff --stat` across the plan shows exactly one modified file, `.pre-commit-config.yaml` (104 insertions, 2 deletions), and nothing under the source, test or experiments trees.
- Facts in the rationale blocks were re-measured on this checkout before being written: `git show 70e783f | grep -c '^\ No newline at end of file'` = **143**; the suite-state TSV is **40 lines, all 5 fields, 20 ending in a tab**; `e2_band.log` has **185** padded lines; the `.gitkeep` placeholders are **1 byte**; the two md5 anchors are **57279708f6106f411d1fe03ed2698291** and **b6515ed77ed04268608b74217716020b**.

## Deviations from Plan

**1. [Rule 1 — correctness] RUN-05 not marked complete in REQUIREMENTS.md**

- **Found during:** the state-update step.
- **Issue:** the plan's frontmatter carries `requirements: [RUN-05]`, and the executor's default is to check that requirement off on plan completion. But all **eight** plans in phase 29.2 carry RUN-05, and RUN-05 is *"the Zenodo record is split into immutable inputs and a versioned results record"* — it closes on plan 29.2-08's publish, not on a pre-commit config change. Checking it off here would record a requirement as satisfied while the work that satisfies it is six plans away.
- **Fix:** left RUN-05 `Pending`. It should be marked complete when the phase's publish plan lands.
- **Files modified:** none.

Everything else executed exactly as written.

## Notes for the next plan

- **29.2-04 owns the proof.** This plan deliberately ran no hook. The whole-tree `pre-commit run --all-files` sweep, `git status --porcelain` empty, the two md5 anchors unmoved, and `awk -F'\t' '{print NF}' experiments/run_experiment_suite_state.7005a27.tsv | sort -u` printing exactly `5` are its four independent invariants. The tree must be clean when it runs.
- The two commits here are the only changes to `.pre-commit-config.yaml`; the file is otherwise untouched, including the ruff entry, `check-yaml`, `check-added-large-files`, the local pre-push hooks and the removed-test-hook comment.

## Self-Check: PASSED

- `.pre-commit-config.yaml` present and parsing — FOUND
- Commit `e671074` — FOUND
- Commit `d9e8f49` — FOUND
