---
phase: 29.2-merge-release-and-publish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .pre-commit-config.yaml
autonomous: true
requirements: [RUN-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`trailing-whitespace` and `end-of-file-fixer` both carry the corrected single-regex exclude from D-29.2-02b, verified to leave zero leftovers for either hook across all 1,693 tracked files — and NOT the mirrored results-only pattern of the superseded D-29.2-02, which contributes zero coverage to `trailing-whitespace`."
    - "The byte-sensitive artifacts the hooks would actually have rewritten are covered: `experiments/run_experiment_suite_state.7005a27.tsv`, whose trailing tab is a load-bearing empty fifth column, and the four scipy stage logs whose column padding carries meaning, at both the live path and the archive path (D-29.2-02c)."
    - "`detect-secrets` carries a `.planning/` arm, because the third failing hook exits 123 on ten findings that are all quoted 40-hex commit shas and one audit-table row documenting a credential form (D-29.2-27)."
    - "Each of the three changes carries a rationale block immediately above its value, naming the measurement rather than a fear, justifying each regex arm by name, and stating a retirement condition (D-29.2-04, and the file's own 30-line precedent)."
    - "The deliberate cost of the `detect-secrets` change is written down at the constant in as many words: the planning tree stops being secret-scanned."
    - "The exclusion lives in `.pre-commit-config.yaml` rather than in the CI workflow, so a local whole-tree run is armed identically to CI and cannot silently rewrite the deliberately-newline-less artifacts either; CI-only scoping was rejected for exactly that reason (D-29.2-01)."
    - "The exclusion is hook-level, so the ruff hooks — a separate repository entry with their own file selection — are untouched, and no top-level exclude is introduced (D-29.2-01)."
    - "The two md5 anchors were never the bytes at risk from these two hooks; they live under the committed results tree and were already covered. The plan states this rather than repeating the wrong framing (D-29.2-02c)."
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "Three repaired hooks, each with its reason stated at the constant"
      contains: "driver_state/"
  key_links:
    - "pre-commit accepts exactly ONE `exclude` per hook, so `trailing-whitespace`'s existing `^\\.planning/` cannot be added alongside the new pattern — it must be hand-alternated into a single regex. Adding a second `exclude:` key silently loses one of them."
    - "The exclusion must stay at hook level. A top-level `exclude:` combines with rather than replaces hook-level ones and would silently de-scope `ruff`, `check-yaml` and `detect-secrets` too; this file has none today."
    - "`.pre-commit-config.yaml` is what CI's `pre-commit` job runs on both push and pull_request, so this file is the merge gate for ROADMAP criterion 1."
  prohibitions:
    - "Never run the all-files hook sweep from this plan. Proving the fix is plan 29.2-04's task, and it is a whole-tree assertion that a dirty tree makes meaningless."
    - "Never regenerate `.secrets.baseline`. The author ruled on 2026-08-26 that the exclude is extended instead, and the file's own reasoning explains why a baseline entry cannot hold when every phase lands new commit shas into plan prose."
    - "Never edit, reformat or regenerate any file under the committed run-artifact trees. The whole point of this plan is that those bytes are not rewritten."
    - "Never add a top-level exclude to this configuration."
    - "Never narrow a security control without writing its cost down beside the value."
    - "Never silence a hook by deleting it. If a control is ever removed, record what was removed, the cost that motivated it, and the gap it leaves — the file already does this once, for a removed test hook."
---

<objective>
Repair all three failing hooks in `.pre-commit-config.yaml` so CI's `pre-commit` job can pass on a
pull request that carries the run artifacts, and write the reason for each change beside the value
it governs.

Purpose: ROADMAP criterion 1. CI runs the all-files hook sweep on both `push: [main]` and
`pull_request: [main]`, and the 227-file provenance commit put artifacts into the repository that
three separate hooks would rewrite or reject. CONTEXT.md framed this as a two-hook problem;
research measured it as a three-hook problem, and measured that the pattern originally chosen to
protect the byte-sensitive files protects none of the ones actually at risk. Both corrections are
absorbed here.

Output: one configuration file, three repaired hooks, three rationale blocks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.2-merge-release-and-publish/29.2-CONTEXT.md
@.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md
@.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md
@.planning/todos/pending/2026-08-26-no-pull-request-into-main-until-ci-pre-commit-is-scoped.md
</context>

## Reading order, and which document wins

Read `29.2-CONTEXT.md` first, then `29.2-RESEARCH.md`. **D-29.2-02 is marked SUPERSEDED and must not
be implemented** — RESEARCH.md's Finding 2 measured that its mirrored pattern contributes zero
coverage to one of the two hooks. D-29.2-02b carries the corrected regex and is the one to use.
D-29.2-27 is new in research and is the third hook. Where CONTEXT.md and RESEARCH.md still appear to
disagree, **RESEARCH.md's measurements win**, because they were executed rather than reasoned.

## Artifacts this plan produces

| Artifact | Kind |
|---|---|
| `.pre-commit-config.yaml` — corrected `exclude:` on `trailing-whitespace` and `end-of-file-fixer`, a `.planning/` arm on `detect-secrets`, three rationale blocks | modified |

<tasks>

<task type="auto">
  <name>Task 1: Give trailing-whitespace and end-of-file-fixer the corrected byte-integrity exclude</name>
  <reversibility rating="reversible">A config-file pattern on two hooks; undone by deleting the two lines and restoring the one-arm pattern that was there before.</reversibility>
  <files>.pre-commit-config.yaml</files>
  <read_first>
    - `.pre-commit-config.yaml` — the whole file is 86 lines. Lines 9-17 are the `pre-commit-hooks` repository entry being changed; `trailing-whitespace` at line 12 already carries a one-arm exclude and `end-of-file-fixer` at line 14 carries none. Lines 24-54 are the rationale-block shape to copy. Lines 1-7 are the ruff entry, which this task does not touch.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *FINDING 2* — the measured rewrite sets (300 files for one hook, 19 for the other), the ten uncovered files listed by path, the two-line sample showing the tab-terminated fifth column, the five planning files the results-only pattern leaves behind, and the corrected regex verified to leave zero leftovers across all 1,693 tracked files.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *Q3a* and § *Q3c* — the empirical proof that a hand-alternated single regex works and that the exclude is matched with a search rather than a full match against the repository-relative POSIX path; and the ruling against a top-level exclude.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md` § *1* — the seven copyable conventions distilled from the existing 30-line rationale block, and the list of measured facts the new block must contain.
    - `.planning/todos/pending/2026-08-26-no-pull-request-into-main-until-ci-pre-commit-is-scoped.md` — the 143 / 439 / two-md5-anchor evidence this constraint was originally filed with.
  </read_first>
  <action>
Replace `trailing-whitespace`'s existing one-arm exclude with the corrected pattern from D-29.2-02b,
and add the identical pattern to `end-of-file-fixer`, which has none today.

The pattern is a single anchored alternation covering the planning tree plus three generated-artifact
families under the experiments tree — the results directories, the driver-state directory, and the
suite-state file prefix — with the optional archive segment that covers the two archive-aside
relocations. Take the exact regex from D-29.2-02b in CONTEXT.md or from RESEARCH.md's Finding 2;
both carry it verbatim and identically. Do not retype it from memory and do not simplify it.

Two mechanical constraints govern how it is written:

- pre-commit accepts **exactly one** `exclude` per hook — a single string, never a list. The planning
  arm the first hook already has therefore has to be **hand-alternated into** the new regex, not added
  as a second key. Adding a second key silently drops one of the two.
- Keep both excludes at **hook level**. This configuration has no top-level exclude, and one would
  combine with rather than replace the hook-level ones, silently de-scoping the ruff hooks,
  `check-yaml` and `detect-secrets` as well.

Write one rationale block immediately above the pair, inside the repository entry, following the
existing 30-line precedent's shape. Per PATTERNS.md convention 6 the load-bearing element is that it
records **measurements, not fears**. It must name:

1. **What the exclude governs** and why these files are byte-sensitive by design: 143 committed
   artifacts deliberately lack a final newline, with 439 trailing-whitespace advisories besides, all
   inside the 227-file provenance commit.
2. **The driver-state arm, justified by name.** The suite-state file has 40 lines, every one carrying
   five tab-separated fields, and 20 of them terminate in a tab — that trailing tab **is** the empty
   fifth column, the exit-code field, empty on the start rows. Stripping it makes those 20 lines
   four-field while the rest stay five-field. State this as the arm's reason.
3. **The stage-log arm, justified by name.** The four stage logs are scipy verbose least-squares
   tables whose column padding would be stripped on 185 of 723 lines in the largest of them.
4. **What the archive segment covers** — the same trees relocated byte-identically by the two
   archive-aside moves, exactly as the existing block already explains for the other hook.
5. **A correction of the framing that produced the wrong pattern.** The two md5 anchors
   (`57279708f6106f411d1fe03ed2698291` for the real-rig metrics and
   `b6515ed77ed04268608b74217716020b` for the interface ablation band) live under the committed
   results tree and were **always** covered; they were never the bytes at risk from these two hooks.
   Name the gate that rests on them so the next reader knows what breaking them would cost.
6. **What the one hook would do to a one-byte file** — the two placeholder files under the planning
   tree contain a single newline, and version 5.0.0 of the fixer truncates an all-newline file to
   zero bytes rather than normalising it. That is why the planning arm belongs on both hooks and not
   only on the one that already had it.
7. **The retirement condition for each arm**, in the shape the existing block uses for its own.

Close the block with the categorical one-liner the existing block uses in its own register: none of
these file types is a place a formatting fix belongs, because their exact bytes are the record.

Do not touch the ruff entry, `check-yaml`, `check-added-large-files`, the local pre-push hooks, or
the removed-test-hook comment at the end of the file.
  </action>
  <verify>
    <automated>python3 -c "
import re, yaml
d = yaml.safe_load(open('.pre-commit-config.yaml'))
hooks = {h['id']: h for r in d['repos'] for h in r['hooks']}
tw, eof = hooks['trailing-whitespace'], hooks['end-of-file-fixer']
assert tw.get('exclude') == eof.get('exclude'), (tw.get('exclude'), eof.get('exclude'))
pat = re.compile(tw['exclude'])
must_match = [
    'experiments/run_experiment_suite_state.7005a27.tsv',
    'experiments/run_experiment_suite_state.7005a27.stagelogs/e2_band.log',
    'experiments/freeze01_run_output/driver_state/run_experiment_suite_state.3ab9c13.tsv',
    'experiments/freeze01_run_output/driver_state/run_experiment_suite_state.3ab9c13.stagelogs/e2_band.log',
    'experiments/results/real_rig_metrics.json',
    'experiments/pre_rerun_baseline/results/benchmark.json',
    '.planning/phases/29.1-post-run-fixes-re-freeze/.gitkeep',
    '.planning/milestones/v1.6-ROADMAP.md',
]
must_not = ['src/aquacal/core/refractive_geometry.py', 'tests/unit/test_optim_common.py', 'pyproject.toml', 'README.md']
for p in must_match: assert pat.search(p), 'NOT excluded but must be: ' + p
for p in must_not: assert not pat.search(p), 'excluded but must not be: ' + p
assert 'exclude' not in d, 'a top-level exclude was introduced'
ruff = hooks['ruff']
assert ruff.get('exclude') is None, 'the ruff hook must keep its own file selection'
print('exclude pattern covers all 8 at-risk paths, spares all 4 hand-written paths, hook-level only')
"</automated>
  </verify>
  <acceptance_criteria>
    - `trailing-whitespace` and `end-of-file-fixer` carry byte-identical `exclude` strings.
    - That regex matches the live suite-state file, its stage logs, the archived driver-state copies, the committed results tree, the pre-run baseline tree, a planning placeholder file and a planning markdown file — all eight asserted programmatically.
    - That regex does not match any hand-written source file, test file, `pyproject.toml` or `README.md`.
    - The configuration has no top-level `exclude`, and the ruff hook carries no `exclude` of its own.
    - A rationale block sits inside the repository entry above the pair and names, at minimum: the 143 deliberate missing newlines, the tab-terminated fifth column on 20 of 40 lines, the 185-of-723 stage-log padding lines, both md5 anchor digests with the note that they were never the bytes at risk here, the one-byte placeholder truncation, and a retirement condition for each arm.
    - `git diff --stat` shows exactly one modified file.
  </acceptance_criteria>
  <done>Both formatting hooks are scoped so that neither CI nor a local run can rewrite a committed artifact's bytes, and the next person to read the pattern learns from the pattern why every arm is there.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the detect-secrets exclude with a planning-tree arm, and write down what it costs</name>
  <reversibility rating="reversible">One additional alternative in an existing regex; undone by deleting it. It narrows a security control's scope, so the rationale has to live at the value — but the change itself is two characters plus a comment.</reversibility>
  <files>.pre-commit-config.yaml</files>
  <read_first>
    - `.pre-commit-config.yaml` lines 19-54 — the `detect-secrets` entry, its existing 30-line rationale block, and its current exclude, which covers notebook tutorials, the committed results trees and the fixture JSON. This is the block being extended, and it is also the shape the extension must match.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *FINDING 3* — the ten findings by file and line, the two finding types, the audit-table row that trips the credential detector, and the state of `.secrets.baseline` (generated 2026-07-28, six files, zero planning entries, last touched by a commit that did not refresh it).
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *Open Questions* item 1 and § *Security Domain* — the two dispositions considered and the note that a permanently-red gate is the same failure mode as the removed test hook this file already documents.
    - `.pre-commit-config.yaml` lines 72-86 — the removed-test-hook comment. It is the file's own precedent for recording a control change honestly, including the gap it leaves.
  </read_first>
  <action>
Add a planning-tree arm to the `detect-secrets` hook's existing exclude alternation. Keep the
existing three alternatives exactly as they are; this is an extension, not a rewrite, and the
anchoring and trailing-match shape of the existing pattern must be preserved.

Extend the existing rationale block rather than starting a new one — the hook already carries the
argument this change reuses, and splitting it would leave two half-arguments. The extension must
state:

1. **What the new arm covers and why every finding is a false positive of a species already
   described.** Ten findings, all under the planning tree: high-entropy hex strings that are the
   40-character frozen commit shas quoted inside plan prose, plus one row of an audit table that
   *documents* a credential-bearing URL form as an inventory line with a count of zero. The existing
   block's own reasoning — provenance fields that are high-entropy by design and take a new value on
   every legitimate regeneration — applies to these unchanged.
2. **Why the alternative cannot hold**, reusing the existing block's argument in substance: every
   phase lands new commit shas into new plan documents, so a baseline entry would have to be
   re-pinned every phase. The current baseline was generated 2026-07-28, names six files, and holds
   zero planning entries — it predates three phases entirely. Say that the author ruled on 2026-08-26
   to extend the exclude rather than regenerate the baseline, and that this is the same
   one-shape-one-reasoning move the byte-integrity arms use.
3. **The cost, stated deliberately and without softening — this sentence has no analog in the
   existing block and is the genuinely new element.** The planning tree stops being secret-scanned.
   Say so plainly. Pair it with the file's own recorded lesson: a gate that cries wolf gets bypassed
   wholesale, which costs more than the scan it was performing — the same reasoning that retired the
   test hook documented at the end of this file.
4. **The retirement condition**, in the shape the existing arms use: what would have to change for
   this arm to be dropped.

Do not regenerate `.secrets.baseline` and do not change its path argument. Do not touch the other
three alternatives in the pattern. Do not touch any other hook.
  </action>
  <verify>
    <automated>python3 -c "
import re, yaml
d = yaml.safe_load(open('.pre-commit-config.yaml'))
hooks = {h['id']: h for r in d['repos'] for h in r['hooks']}
ds = hooks['detect-secrets']
pat = re.compile(ds['exclude'])
findings = [
  '.planning/phases/28-full-suite-production-run/28-03-PLAN.md',
  '.planning/phases/29-gate-verification-results-commit/29-08-SUMMARY.md',
  '.planning/phases/29-gate-verification-results-commit/29-zenodo-record-a.txt',
  '.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md',
]
for p in findings: assert pat.search(p), 'still scanned but must not be: ' + p
for p in ['docs/tutorials/quickstart.ipynb', 'experiments/results/benchmark.json', 'tests/fixtures/anchor.json']:
  assert pat.search(p), 'existing arm regressed: ' + p
for p in ['src/aquacal/cli.py', 'scripts/zenodo_upload.py', '.github/workflows/release.yml']:
  assert not pat.search(p), 'newly excluded but must not be: ' + p
assert ds['args'] == ['--baseline', '.secrets.baseline'], ds['args']
print('detect-secrets exclude covers all 4 finding paths, keeps its 3 original arms, spares source')
" &amp;&amp; git diff --quiet -- .secrets.baseline</automated>
  </verify>
  <acceptance_criteria>
    - The `detect-secrets` exclude matches every planning path the hook reported a finding in, asserted programmatically over four representative paths spanning both finding types.
    - The three pre-existing alternatives still match their original targets — the notebook tutorials, the committed results trees and the fixture JSON.
    - The pattern does not newly exclude anything under the source tree, the scripts directory or the workflows directory.
    - The hook's baseline argument is unchanged and `.secrets.baseline` is byte-unchanged.
    - The rationale block states, in as many words, that the planning tree stops being secret-scanned, and pairs that with the cries-wolf lesson the file already records.
    - The rationale block names the baseline's generation date, its six-file scope and its zero planning entries, and gives a retirement condition.
  </acceptance_criteria>
  <done>The third failing hook passes, the reason is written beside the value in the shape the file already uses, and the security scope this narrows is recorded rather than absorbed silently.</done>
</task>

<task type="auto">
  <name>Task 3: Statically verify all seven hooks' file selection without running any of them</name>
  <precondition>Both edits from Tasks 1 and 2 are present in `.pre-commit-config.yaml` — this task reads the file, it does not create the patterns it checks.</precondition>
  <files>.pre-commit-config.yaml</files>
  <read_first>
    - `.pre-commit-config.yaml` — as edited by Tasks 1 and 2.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *Q3f* — the seven-hook table, and the measured verdicts on the four hooks this plan does not change: the two ruff hooks are clean over all 166 tracked source files, `check-yaml` is clean over all 27 tracked YAML files, and the large-file check is a trivial pass on a clean checkout because it filters to newly-added staged files unless told otherwise.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *Q3d* — the file-set measurement method to reproduce: enumerate the tracked tree, apply each hook's regex, and report the counts and extensions.
  </read_first>
  <action>
Reproduce the static half of research's measurement against the edited configuration, so the edits
are validated before plan 29.2-04 spends a whole-tree hook sweep on them. **This task runs no hook.**
It enumerates the tracked tree and applies each hook's own `exclude` regex in Python, which is what
pre-commit itself does after it assembles the candidate list.

Produce, and report in the plan summary rather than in a committed evidence file:

- For each of the two byte-integrity hooks: the number of tracked files the pattern excludes, and the
  extension histogram of that set. Compare against research's figures and **record both numbers if
  they differ**, with the difference explained rather than reconciled away — the tracked tree has
  gained planning documents since research ran, so a larger count is expected and is not a defect.
- The count of tracked source files the pattern excludes, and where they live. Research measured that
  every source file the pattern touches sits under the planning tree and is therefore irrelevant,
  because the exclusion is hook-level and the ruff hooks are a separate repository entry with their
  own file selection. Confirm that separation still holds by checking that the ruff entry carries no
  inherited exclude.
- For `detect-secrets`: that all four representative finding paths are now excluded and that the
  three original arms still match.
- A one-line statement for each of the four unchanged hooks, citing research's measured verdict, so
  the summary accounts for all seven rather than only the three that were edited. Research's own
  recorded lesson here is that counting only the blockers you already know about is how the third one
  went unnoticed.

Append nothing to any tracked file except `.pre-commit-config.yaml`, and change nothing in it —
this task is read-only over the configuration it validates.
  </action>
  <verify>
    <automated>python3 -c "
import re, subprocess, collections, yaml
d = yaml.safe_load(open('.pre-commit-config.yaml'))
hooks = {h['id']: h for r in d['repos'] for h in r['hooks']}
tracked = subprocess.run(['git','ls-files'], capture_output=True, text=True, check=True).stdout.split()
pat = re.compile(hooks['trailing-whitespace']['exclude'])
matched = [p for p in tracked if pat.search(p)]
ext = collections.Counter(p.rsplit('.',1)[-1] if '.' in p.rsplit('/',1)[-1] else '(none)' for p in matched)
print('tracked:', len(tracked), 'excluded:', len(matched))
print('extensions:', dict(ext.most_common(10)))
py_outside_planning = [p for p in matched if p.endswith('.py') and not p.startswith('.planning/')]
assert not py_outside_planning, py_outside_planning
assert len(matched) > 1000, len(matched)
assert hooks['end-of-file-fixer']['exclude'] == hooks['trailing-whitespace']['exclude']
print('no excluded .py outside the planning tree; both byte-integrity hooks agree')
" &amp;&amp; python3 -c "
import subprocess
files = subprocess.run(['git','diff','--name-only'], capture_output=True, text=True, check=True).stdout.split()
extra = [f for f in files if f != '.pre-commit-config.yaml']
assert not extra, 'files modified outside the configuration: ' + repr(extra)
print('modified tracked files:', files or '(none - earlier tasks already committed)')
"</automated>
  </verify>
  <acceptance_criteria>
    - The excluded-file count and extension histogram for the byte-integrity pattern are measured against the live tracked tree and reported in the summary, beside research's figures, with any difference explained.
    - No source file outside the planning tree is excluded by the byte-integrity pattern, asserted programmatically.
    - Both byte-integrity hooks carry identical patterns, asserted programmatically.
    - The summary accounts for all seven hooks — the three edited here and the four research measured clean — not only the ones that were changed.
    - No file under the source, test or experiments trees appears in `git diff --stat`.
  </acceptance_criteria>
  <done>The two edits are validated against the real tracked tree before a whole-tree hook sweep is spent on them, and all seven hooks are accounted for rather than only the ones that were known to be broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed provenance artifacts -> automated formatting hooks | a hook with write access to the checkout can silently alter bytes whose exactness is the scientific record |
| planning prose -> `detect-secrets` scan scope | this plan deliberately removes a tree from a credential-scanning control |
| repository configuration -> CI merge gate | `.pre-commit-config.yaml` is what the pull request's `pre-commit` job executes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.2-06 | Tampering | `trailing-whitespace` / `end-of-file-fixer` over committed artifacts | high | mitigate | Task 1 installs the pattern verified to leave zero leftovers for both hooks over all tracked files, and Task 3 re-asserts its coverage against the live tree programmatically. Plan 29.2-04 then proves it by execution against the four independent invariants. |
| T-29.2-07 | Information Disclosure | `detect-secrets` scope narrowed over the planning tree | medium | accept | A real credential pasted into a plan document would no longer be caught by this hook. Accepted deliberately per D-29.2-27 with the cost written at the constant: the ten current findings are all false positives of a species the hook's own rationale already covers, the alternative disposition is structurally unable to hold, and a permanently-red gate gets bypassed wholesale — which costs the whole scan, not one tree. Compensating controls: the credential-bearing surfaces in this phase are the tokens themselves, which are never written to a file, and the tree is not published. |
| T-29.2-08 | Repudiation | a security control changed without a written reason | medium | mitigate | Both Task 1 and Task 2 require the rationale at the value, matching the file's own 30-line precedent, including the explicit cost sentence and a retirement condition. The file already carries this convention for a removed hook. |
| T-29.2-09 | Tampering | accidental top-level `exclude` de-scoping every hook | high | mitigate | Task 1 forbids it explicitly, and both Task 1 and Task 3 assert programmatically that no top-level `exclude` key exists and that the ruff hook carries no inherited exclude. |
| T-29.2-SC | Tampering | `pip install` of any package-manager dependency | high | mitigate | This plan installs nothing. RESEARCH § *Package Legitimacy Audit* records that the phase adds no new external package and returns no `[ASSUMED]`, `[SUS]` or `[SLOP]` verdicts; the pinned hook revisions in this file are unchanged. No legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.pre-commit-config.yaml` parses as YAML, and both byte-integrity hooks carry the same non-empty `exclude`.
- That pattern excludes all eight representative at-risk paths and none of the four representative hand-written paths.
- The `detect-secrets` pattern excludes all four representative finding paths and retains its three original arms.
- No top-level `exclude` exists; the ruff hook carries no `exclude`.
- `.secrets.baseline` is byte-unchanged.
- `git diff --stat` shows exactly one modified file, and nothing under the source, test or experiments trees.
- Three rationale blocks are present, each naming a measurement, each justifying its arms by name, each carrying a retirement condition, and the `detect-secrets` one stating its cost in as many words.
</verification>

<success_criteria>
All three hooks that would fail CI's whole-tree sweep are repaired at the configuration level, so a
local run is armed identically to CI; no committed artifact's bytes can be rewritten by either
formatting hook; and every change carries its reason and its cost beside the value it governs.
</success_criteria>

<output>
Create `.planning/phases/29.2-merge-release-and-publish/29.2-02-SUMMARY.md` when done.
</output>
