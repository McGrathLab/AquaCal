---
phase: 29.2-merge-release-and-publish
plan: 08
type: execute
wave: 6
depends_on: ["29.2-07"]
files_modified:
  - .planning/phases/29.2-merge-release-and-publish/29.2-PHASE-RECORD.md
  - .planning/todos/pending/2026-08-26-no-pull-request-into-main-until-ci-pre-commit-is-scoped.md
  - .planning/todos/pending/2026-08-26-publish-record-b-and-add-record-a-back-link.md
autonomous: true
requirements: [RUN-05]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The superseding statement lives in THIS phase's record and nowhere else. The frozen tag's annotated message and the prior audit's ruling still say three, and they stay readable as what was true when they were written — the same provenance reasoning that declined to edit an earlier finding in place (D-29.2-10)."
    - "The new invariant is stated without softening and with its anomaly conditions: the whole suite reports exactly two failures, both marked slow, both reading constants from the fixtures tree, and the not-slow selection reports none. Zero and three are both anomalies, and the record says what each would mean."
    - "The record carries an approval-status line per irreversible act — the stale deployment's ending, the merge, the package-index approval, the results record's publication, and the inputs record's edit — naming who approved and when, which is the written form of the prepare-and-verify split (D-29.2-24)."
    - "The two todos whose resolving phase is this one are discharged with their evidence named, and the partially-folded third stays open describing only the two members it still owns."
    - "Every ROADMAP success criterion for this phase gets one row with its measured value, its evidence file and a verdict — including any that did not go the expected way, reported rather than reconciled."
    - "The record names the residuals honestly: the two remaining exact-equality anchors, the dataset manifest that still names one record where there are now two, the archive service's deprecation posture, and the stale output trees — each with where it goes and why it is not repaired here."
  artifacts:
    - path: ".planning/phases/29.2-merge-release-and-publish/29.2-PHASE-RECORD.md"
      provides: "Verdict-first phase record with the superseding statement, per-act approval lines, one row per criterion, and numbered residuals"
      contains: "Approval status"
  key_links:
    - "The superseding statement supersedes a count that is quoted inside an immutable annotated tag message. The tag cannot be corrected, so this record is the only correctable surface — which is exactly why the statement belongs here and why the prior artifacts must be left alone."
    - "The two discharged todos are what made this phase's constraints findable outside the roadmap. Discharging them with their evidence named is what stops the next reader re-discovering a constraint that has already been satisfied."
    - "The phase record's residual entries are the handoff to the post-submission phase; anything not written there is work that has to be re-derived from evidence files."
  prohibitions:
    - "Never rewrite a prior artifact to agree with this record. Superseding statements live in the superseding document, never in the superseded one."
    - "Never claim the suite is clean. Two ruled failures remain and no artifact of this phase may imply otherwise."
    - "Never soften a measurement to make a criterion read better. Report what was measured, including anything that disagreed with an expectation."
    - "Never close a residual by omitting it. A residual that is not written down is work that has to be re-discovered."
    - "Never edit the manuscript, its citation or its data-availability wording; those belong to the manuscript session."
---

<objective>
Write the phase record that carries the superseding statement, the per-act approval lines and one row
per success criterion — then discharge the two todos this phase owns and leave the third describing
only what remains.

Purpose: this phase supersedes a documented invariant that is quoted inside an immutable tag message,
and it performed five irreversible acts on the author's press. Neither of those survives in a usable
form unless it is written down here: the tag cannot be corrected, so this record is the only
correctable surface, and an approval trail that lives only in a service's activity log is not
findable from the planning tree. The residuals are the handoff — anything not written here has to be
re-derived from evidence files by whoever comes next.

Output: one phase record, two discharged todos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29.2-merge-release-and-publish/29.2-CONTEXT.md
@.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md
@.planning/phases/29.2-merge-release-and-publish/29.2-05-SUMMARY.md
@.planning/phases/29.2-merge-release-and-publish/29.2-07-SUMMARY.md
@.planning/phases/29-gate-verification-results-commit/29-PHASE-RECORD.md
@.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md
</context>

## Artifacts this plan produces

| Artifact | Kind |
|---|---|
| `29.2-PHASE-RECORD.md` | new phase record |
| `.planning/todos/pending/2026-08-26-no-pull-request-into-main-until-ci-pre-commit-is-scoped.md` | discharged |
| `.planning/todos/pending/2026-08-26-publish-record-b-and-add-record-a-back-link.md` | discharged |

<tasks>

<task type="auto">
  <name>Task 1: Write the phase record — verdicts first, the superseding statement, and one approval line per irreversible act</name>
  <reversibility rating="costly">The superseding statement itself is an ordinary editable document. What is not editable is the surface it supersedes: the count it corrects is quoted inside an immutable annotated tag message, so this record is the only place the correction can ever live. No checkpoint is inserted, because that door was walked through when the tag was pushed in a prior phase — gating it now would be re-asking a decision already made. The rating is recorded so the constraint survives.</reversibility>
  <files>.planning/phases/29.2-merge-release-and-publish/29.2-PHASE-RECORD.md</files>
  <read_first>
    - `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md` lines 1-46 — the structural analog. Copy: bolded one-line verdicts at the very top before any evidence; an explicit statement of why the most important section is first; an approval-status line naming who approved and when; the measurement-without-softening section with its verbatim counts and node ids; and the per-item disagreement table, whose row for the fixed member is already written and should be quoted rather than re-derived.
    - `.planning/phases/29-gate-verification-results-commit/29-PHASE-RECORD.md` — the one-row-per-criterion table shape, the `WHAT REMAINS` ordered-steps section, and the open-items-handed-forward list.
    - `.planning/phases/29-gate-verification-results-commit/29-rails-after.txt` — the numbered residual convention: each entry states what fails, where it is already filed, and why it is not repaired here, closing on a statement that there are no others.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md` § *5* and § *S5* and § *S6* — the header conventions, the approval-status line per irreversible act, and the supersede-without-rewriting rule.
    - All seven prior summaries and all six evidence files in this phase directory — every measured value in the record comes from one of them, cited by filename.
    - `.planning/ROADMAP.md` § *Phase 29.2* — the five success criteria, in their own words.
  </read_first>
  <action>
Write `29.2-PHASE-RECORD.md` in the verdict-first shape this project uses for its rulings.

**Open with bolded one-line verdicts, before any evidence**, and say why that ordering was chosen —
a reader who takes only one thing from the document should take the most important one. The verdicts
cover: whether the hook job passed on the pull request without a byte being rewritten; whether the
default branch carries a merge commit and the version was cut; whether the released version is
installable; whether both archive records are published and mutually reachable; and whether
publication preceded submission.

**Write the superseding statement, and place it high**, because it is the thing this record exists to
correct. Follow the measurement-without-softening convention: state the new invariant as measured —
the whole suite reports exactly two failures, both marked slow, both reading constants from the
fixtures tree; the not-slow selection reports none — and name the node ids verbatim. State what each
anomaly would mean: a zero would mean the two remaining ruled anchors had been silenced; a three
would mean something new had broken. **State explicitly that the prior invariant's "three", the
frozen tag's annotated message, and the prior audit's section all stay readable as what was true when
they were written, and that nothing was rewritten to agree with this** — the same reasoning that
declined to edit an earlier finding in place. Name why the third member left the set: it had no
captured anchor at all, and its disagreement was between the library's vectorized evaluation and the
test's scalar evaluation of the same formula on the same machine, which is not the cross-platform
question the remaining two carry. Quote the already-written disagreement-table row rather than
re-deriving it.

**Write one approval-status line per irreversible act**, naming the act, who approved it, the date,
and where the evidence lives. There are five: the stale deployment's ending, the merge, the
package-index approval, the results record's publication, and the inputs record's metadata edit. This
is the prepare-and-verify split in document form, and it is what makes the split auditable rather
than merely claimed.

**Write one row per ROADMAP success criterion**, each with its measured value, its evidence file and
a verdict. Report anything that disagreed with an expectation as it was measured — including the
stale commit figures the roadmap carried, with the re-derived numbers beside them, and the sdist size
comparison that would have false-alarmed if taken literally.

**Record the three research findings and what each cost**, briefly, because they are the phase's most
transferable content: a release workflow that could not start because of an unpinned dependency
released the day before the phase was scoped; an exclude pattern that covered none of the files
actually at risk from one of the two hooks it was written for; and a third failing hook nobody had
counted. Each was found by executing rather than by reading, and each was found before an
irreversible act — which is what the prepare-and-verify split exists to buy.

**Close with numbered residuals**, each stating what remains, where it is already filed, and why it
is not repaired here, and ending with a statement that there are no others. The residuals are: the
two remaining exact-equality anchors and their open todo; the dataset manifest that still names a
single record where there are now two; the archive service's deposit-interface deprecation posture;
the stale output trees awaiting their purge; the release credential's unverifiable validity as a
standing watch-point; and the narrowed scope of the credential scanner over the planning tree,
recorded as an accepted cost with its reasoning.

**Add a `WHAT REMAINS` section** in the prior record's shape, listing what the next session needs in
order: report both records' identifiers to the manuscript session, whose edits those are and are not;
and the post-submission items with their owning phase.

Do not edit any prior phase's artifacts, the frozen tag, or the manuscript.
  </action>
  <verify>
    <automated>test -f .planning/phases/29.2-merge-release-and-publish/29.2-PHASE-RECORD.md &amp;&amp; python3 -c "
s = open('.planning/phases/29.2-merge-release-and-publish/29.2-PHASE-RECORD.md').read()
for token in ['Approval status', 'WHAT REMAINS', 'RESIDUAL', 'test_matches_frozen_anchor', 'test_matches_pre_change_anchor']:
    assert token in s, 'missing: ' + token
assert s.count('Approval status') >= 5, 'expected one approval-status line per irreversible act, found %d' % s.count('Approval status')
assert '2.1.0' in s
import re
assert re.search(r'\b2 failed\b|\btwo failures\b', s), 'the new invariant is not stated'
print('phase record carries verdicts, %d approval lines, residuals and the superseding statement' % s.count('Approval status'))
" &amp;&amp; git diff --quiet -- .planning/phases/29-gate-verification-results-commit/ .planning/phases/29.1-post-run-fixes-re-freeze/ &amp;&amp; [ "$(git tag -l rerun-freeze-02)" = "rerun-freeze-02" ]</automated>
  </verify>
  <acceptance_criteria>
    - `29.2-PHASE-RECORD.md` opens with bolded one-line verdicts before any evidence, and states why that ordering was chosen.
    - The superseding statement names the new invariant as measured, names both remaining node ids verbatim, states what a zero and a three would each mean, and states explicitly that the prior artifacts and the frozen tag's message stay unrewritten.
    - The reason the third member left the set is stated as the vectorized-versus-scalar mechanism, with the already-written disagreement-table row quoted.
    - At least five approval-status lines are present, one per irreversible act, each naming the act, the approver, the date and the evidence file.
    - Each ROADMAP success criterion has one row with its measured value, its evidence file and a verdict, and the stale roadmap figures appear beside their re-derived replacements.
    - The three research findings are recorded with what each cost and the fact that each was found before an irreversible act.
    - Numbered residuals are present, each with what remains, where it is filed and why it is not repaired here, closing on a statement that there are no others.
    - A `WHAT REMAINS` section lists the next session's ordered items.
    - `git diff --quiet` is clean over the Phase 29 and Phase 29.1 directories, and the frozen tag still resolves.
  </acceptance_criteria>
  <done>The correction to an invariant quoted in an immutable tag message lives somewhere correctable, the five author acts have a written approval trail, and the residuals are a handoff rather than a rediscovery.</done>
</task>

<task type="auto">
  <name>Task 2: Discharge the two todos this phase owns, each with its evidence named</name>
  <precondition>`29.2-PHASE-RECORD.md` exists and carries the per-criterion table — the discharge notes cite it, so writing them first would cite a document that does not exist.</precondition>
  <files>.planning/todos/pending/2026-08-26-no-pull-request-into-main-until-ci-pre-commit-is-scoped.md, .planning/todos/pending/2026-08-26-publish-record-b-and-add-record-a-back-link.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-26-no-pull-request-into-main-until-ci-pre-commit-is-scoped.md` — the whole file: its constraint, its evidence figures, and the fence it records as lifted in this phase.
    - `.planning/todos/pending/2026-08-26-publish-record-b-and-add-record-a-back-link.md` — the whole file: its ownership statement, its state table, and its note that this closes the one criterion that stayed open.
    - `.planning/todos/` — inspect how a discharged todo is represented in this project. If there is an established completed or resolved location, follow it; if todos are discharged in place with a resolution note, follow that instead. **Determine the convention from the tree, do not invent one**, and state in the summary which convention was found and followed.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-precommit-allfiles-proof.txt` and `29.2-publication-record.txt` — the evidence each discharge cites.
  </read_first>
  <action>
Discharge both todos whose resolving phase is this one, following whatever convention this project
already uses for discharged todos rather than inventing one.

**The hook-scoping todo.** Its constraint was that no pull request should be opened until the
whole-tree hook sweep was scoped, because it would put the formatting hooks through the committed run
artifacts. Record the discharge with its evidence: the configuration change that scoped three hooks,
the whole-tree sweep that ran and exited zero, and the four independent assertions that proved nothing
was rewritten — the unchanged working-tree status, both md5 anchors, and the driver-state field count.
Add the correction this phase found, because the todo's own framing was incomplete: the pattern
originally proposed would have left the byte-sensitive driver-state file and its stage logs
unprotected, and a third hook was failing that the todo did not count. Cite the evidence file by
name.

**The publication todo.** Its obligation was to publish the results record after the version was cut,
then add the inputs record's structured pointer. Record the discharge with its evidence: the minted
identifier, the publication date, the verified structured pointer, and the confirmation that the
rendered description was read end to end before the press. Record both records' version and concept
identifiers in the discharge note, so the identifiers are findable from the todo tree as well as from
the phase record.

For each, name the phase record section that carries the full account, so a reader who starts at the
todo tree can get to the evidence in one hop.

Do not discharge the third, partially-folded todo. It stays open describing the two anchors it still
owns, and plan 29.2-03 already amended it to match.
  </action>
  <verify>
    <automated>python3 -c "
import glob, os, sys
names = ['2026-08-26-no-pull-request-into-main-until-ci-pre-commit-is-scoped.md',
         '2026-08-26-publish-record-b-and-add-record-a-back-link.md']
pending = '.planning/todos/pending'
found = {}
for n in names:
    hits = [p for p in glob.glob('.planning/todos/**/' + n, recursive=True)]
    assert hits, 'todo vanished entirely: ' + n
    found[n] = hits
    still_pending = [p for p in hits if os.path.dirname(p).endswith('pending')]
    if still_pending:
        s = open(still_pending[0]).read().lower()
        assert 'discharged' in s or 'resolved' in s or 'complete' in s, 'still pending with no resolution note: ' + n
        print('discharged in place:', n)
    else:
        print('moved out of pending:', hits)
# the partially-folded third todo stays open
d4 = os.path.join(pending, '2026-08-26-d4-exact-equality-anchors-are-platform-pinned.md')
assert os.path.exists(d4), 'the partially-folded todo must remain pending'
print('d4 todo correctly still pending')
" &amp;&amp; grep -rqE '10\.5281/zenodo\.' .planning/todos/ </automated>
  </verify>
  <acceptance_criteria>
    - Both todos whose resolving phase is this one are discharged following the convention already present in the todo tree, and the summary states which convention was found and followed.
    - Each discharge names its evidence file and the phase-record section carrying the full account.
    - The hook-scoping discharge records the correction to the todo's own framing: the originally proposed pattern would have left the driver-state file and its stage logs unprotected, and a third hook was failing that the todo did not count.
    - The publication discharge records both records' version and concept identifiers, the publication date, and the confirmation that the rendered description was read before the press.
    - The partially-folded third todo remains pending and still describes the two anchors it owns.
  </acceptance_criteria>
  <done>Both constraints this phase was created to satisfy are marked satisfied with their evidence one hop away, and the one that was only partly folded is still open describing exactly what remains.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the requirement, the roadmap and the project state against what was measured</name>
  <precondition>`29.2-PHASE-RECORD.md` carries the per-criterion table and both todos are discharged — this task reconciles the planning documents against that record rather than against the plans' intentions.</precondition>
  <files>.planning/phases/29.2-merge-release-and-publish/29.2-PHASE-RECORD.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the requirement's entry, its checkbox, and its 2026-08-25 re-scoping ruling.
    - `.planning/ROADMAP.md` § *Phase 29.2* — the five criteria, the stale commit figures, and the plans list.
    - `.planning/STATE.md` — the current-position block, which still describes this phase as pending planning and still names the deferred criterion as open.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-PHASE-RECORD.md` — the measured values every reconciliation must agree with.
    - `.planning/todos/pending/2026-08-26-phase-30-dependency-still-names-the-passed-2026-08-21-submission-date.md` — a roadmap correction filed for the next phase, deliberately not this phase's scope. Confirm it is still pending rather than fixing it here.
  </read_first>
  <action>
Bring the planning documents into agreement with what was measured, and be explicit about what is
being claimed.

**The requirement.** Its publication half is what this phase closed. Mark it against the measured
evidence and cite the phase record, and state in the marking that the requirement closes on the
ordering — publication before submission — rather than on the calendar date its original wording
named, which the author's ruling replaced with the submission event. If the paper is not yet
submitted, say so, and say why that satisfies the ordering.

**The roadmap.** Correct the stale commit and conventional-type figures in this phase's entry to the
re-derived values, and mark each success criterion against its measured outcome with a pointer to the
phase record. **Leave the prior phase's entries alone** — this is a scoped edit to this phase's
section only, never a rewrite of the file.

**The project state.** Update the current-position block to describe the measured end state: the
phase's outcome, the released version, both records' identifiers, and what the next session owns.

**State the one thing that is not being claimed.** The manuscript's own citation and its
data-availability wording are the manuscript session's edits and are not made from this repository.
The phase record already says this; the reconciliation must not accidentally imply otherwise by
marking something complete that this repository does not control.

**Confirm, rather than fix, the two post-submission roadmap items** that touch this phase's output:
the dataset manifest that now names one record where there are two, and the roadmap dependency that
still names a passed calendar date. Both are filed against the next phase. Check they are still
pending and record that they were checked.

Append a short reconciliation section to `29.2-PHASE-RECORD.md` listing every planning document
touched, what changed in it, and what was deliberately left unchanged.
  </action>
  <verify>
    <automated>grep -qE 'RUN-05' .planning/REQUIREMENTS.md &amp;&amp; python3 -c "
s = open('.planning/ROADMAP.md').read()
import re
seg = re.search(r'### Phase 29\.2:.*?(?=^### Phase 30)', s, re.S | re.M)
assert seg, 'the phase section is missing from the roadmap'
body = seg.group(0)
assert '406' not in body, 'the stale commit figure is still in the roadmap entry'
assert 'TBD' not in body, 'the plans list is still a placeholder'
# prior phases untouched
assert '### Phase 29:' in s and '### Phase 30:' in s, 'other roadmap phases were damaged'
print('roadmap phase entry reconciled; neighbouring phases intact')
" &amp;&amp; grep -qE '29\.2' .planning/STATE.md &amp;&amp; test -f .planning/todos/pending/2026-08-26-repoint-the-dataset-manifest-after-the-zenodo-split.md &amp;&amp; test -f .planning/todos/pending/2026-08-26-phase-30-dependency-still-names-the-passed-2026-08-21-submission-date.md &amp;&amp; grep -qi 'reconcil' .planning/phases/29.2-merge-release-and-publish/29.2-PHASE-RECORD.md</automated>
  </verify>
  <acceptance_criteria>
    - The requirement is marked against measured evidence with a citation to the phase record, and the marking states that it closes on ordering rather than on the original calendar date.
    - The roadmap's entry for this phase carries the re-derived commit and conventional-type figures, no longer carries the stale ones, and no longer carries a placeholder plans list.
    - The roadmap's neighbouring phase entries are intact — the edit was scoped to this phase's section.
    - The project-state current-position block describes the measured end state, both records' identifiers, and what the next session owns.
    - The reconciliation explicitly states that the manuscript's citation and data-availability wording are not this repository's edits.
    - Both post-submission todos that touch this phase's output are confirmed still pending, and that check is recorded.
    - A reconciliation section in the phase record lists every planning document touched, what changed, and what was deliberately left unchanged.
  </acceptance_criteria>
  <done>The planning documents describe what was measured rather than what was intended, the corrections are scoped rather than wholesale, and the one claim this repository cannot make is explicitly not made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's record -> an immutable annotated tag message | the invariant being corrected is quoted in a surface that can never be edited, so the correction has exactly one possible home |
| planning documents -> the next session's starting assumptions | anything a residual omits is work that has to be re-discovered from evidence files |
| this repository -> the manuscript | the paper's citation and data-availability wording are edited elsewhere, and a reconciliation that implied otherwise would be a false claim of completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.2-36 | Repudiation | rewriting prior artifacts to agree with the new invariant | high | mitigate | Explicitly prohibited, and Task 1's verify asserts a clean diff over the two prior phase directories and confirms the frozen tag still resolves. The superseding statement is confined to this phase's own record by construction. |
| T-29.2-37 | Repudiation | an approval trail that lives only in a service's activity log | medium | mitigate | One approval-status line per irreversible act is required in the phase record, each naming the act, the approver, the date and the evidence file; Task 1's verify counts them and fails below five. |
| T-29.2-38 | Spoofing | a record that implies the suite is clean | high | mitigate | Prohibited outright, and the superseding statement is required to name both remaining node ids verbatim and to state what a zero-failure count would mean — namely that the ruled anchors had been silenced. The verify asserts both node ids are present. |
| T-29.2-39 | Tampering | a whole-file rewrite of the roadmap while correcting one phase entry | high | mitigate | Task 3 requires a scoped edit to this phase's section only, and its verify asserts that the neighbouring phase entries still exist after the change. |
| T-29.2-40 | Repudiation | claiming completion of work this repository does not control | medium | mitigate | The reconciliation is required to state explicitly that the manuscript's citation and data-availability wording are edited in a different session, and the phase record repeats it. |
| T-29.2-41 | Repudiation | a residual closed by omission | medium | mitigate | The residual convention requires each entry to state what remains, where it is filed and why it is not repaired here, and to close on a statement that there are no others — which makes an omission a visible contradiction rather than a silence. Task 3 additionally confirms the two post-submission todos are still pending rather than assuming it. |
| T-29.2-SC | Tampering | package-manager installs | high | mitigate | This plan installs nothing. RESEARCH § *Package Legitimacy Audit* records no new external package for this phase and no `[ASSUMED]`, `[SUS]` or `[SLOP]` verdicts. No legitimacy checkpoint is required. |
</threat_model>

<verification>
- `29.2-PHASE-RECORD.md` exists, opens with verdicts, carries at least five approval-status lines, names both remaining failing node ids, states the new invariant, and closes with numbered residuals and a `WHAT REMAINS` section.
- The Phase 29 and Phase 29.1 directories are byte-unchanged and the frozen tag still resolves.
- Both todos whose resolving phase is this one are discharged following the tree's existing convention; the partially-folded third remains pending.
- The roadmap's entry for this phase carries the re-derived figures and no placeholder, and its neighbouring phase entries are intact.
- The requirement and the project-state block describe the measured end state, and the reconciliation names what this repository does not claim.
- Both post-submission todos that touch this phase's output are confirmed still pending.
</verification>

<success_criteria>
The correction this phase owes to a documented invariant lives in the one place it can, the five
irreversible acts have a written approval trail findable from the planning tree, and every residual
is a handoff with a destination rather than a rediscovery.
</success_criteria>

<output>
Create `.planning/phases/29.2-merge-release-and-publish/29.2-08-SUMMARY.md` when done.
</output>
