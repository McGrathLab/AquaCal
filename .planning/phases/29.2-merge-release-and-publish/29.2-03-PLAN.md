---
phase: 29.2-merge-release-and-publish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_optim_common.py
  - .planning/phases/29.2-merge-release-and-publish/29.2-smoke-runs.txt
  - .planning/todos/pending/2026-08-26-d4-exact-equality-anchors-are-platform-pinned.md
autonomous: true
requirements: [RUN-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The one D4 member that is not marked slow passes on this machine, so CI's `test` job — which runs the not-slow selection on two operating systems across two interpreter versions — is not reddened by it on the release pull request (D-29.2-05)."
    - "Exactly two of the four assertions in that test change. The two that measured zero mismatches across all 22 rows keep exact equality; only the radial-distance assertion and the chord-incidence assertion, which measured 6/22 at worst 1 unit and 3/22 at worst 2 units, become bounded (D-29.2-07)."
    - "The bound is a hard count of representable steps, not a relative tolerance. A relative tolerance cannot express a fixed step count, because one step maps to a relative size anywhere across a factor-of-two range depending on position within the binade — and being twice as loose as intended is the exact failure the original docstring existed to prevent (D-29.2-08)."
    - "The chosen bounds carry four-fold headroom above the locally measured worst cases, because CI runs on machines whose vector dispatch this project does not control and the whole diagnosis is that the gap IS a dispatch artifact. A bound equal to the local maximum would be pinned to this machine by construction."
    - "The docstring states the mechanism rather than restating a policy: the library evaluates the radial distance vectorized over many points while the test evaluates it scalar on one, and the array and scalar paths of the same library on the same machine may differ by one step. This corrects the framing the open todo carries, which mis-classified this member as a platform-pinned captured constant (D-29.2-06)."
    - "The other two D4 members are untouched, and nothing is silenced. Both remain visible failures carrying the cross-platform question the open todo owns (D-29.2-09)."
    - "The new full-suite invariant is measured, not assumed: the whole suite reports exactly two failures, both marked slow, both reading constants from the fixtures tree; the not-slow selection reports none (D-29.2-10)."
    - "All seven smoke invocations from CI's experiments job run at this branch's code and their exit codes are recorded — a job that has never been exercised against this branch, turned into a measurement where failure is cheap (D-29.2-11)."
  artifacts:
    - path: "tests/unit/test_optim_common.py"
      provides: "Two step-bounded assertions and a docstring that states the mechanism at the constant"
      contains: "maxulp"
    - path: ".planning/phases/29.2-merge-release-and-publish/29.2-smoke-runs.txt"
      provides: "The seven smoke invocations and the two suite counts, verbatim with exit codes, before-and-after in one place"
    - path: ".planning/todos/pending/2026-08-26-d4-exact-equality-anchors-are-platform-pinned.md"
      provides: "The amended todo naming two members rather than three, and why the third left the set"
  key_links:
    - "The library writes the radial distance into the sink at `src/aquacal/calibration/_optim_common.py:992`, computing it vectorized at `src/aquacal/core/refractive_geometry.py:676`; the test recomputes it scalar at `tests/unit/test_optim_common.py:1490`. That asymmetry IS the defect being documented — not a drift in either formula."
    - "The chord-incidence value is derived from the radial distance through two library calls, so its measured worst case of two steps is exactly one inherited step plus at most one rounding in each call. The measurement agreeing with the mechanism is what makes the diagnosis credible."
    - "CI's `test` job runs the not-slow selection, so a not-slow failure is a red check on the release pull request while a slow failure is not. That distinction is the entire reason this member is fixed here and the other two are not."
  prohibitions:
    - "Never silence a ruled defect. Do not mark, deselect or otherwise suppress any of the three D4 members; the value of a documented expected count comes entirely from the members being visible."
    - "Never widen the comparison to a relative-tolerance idiom whose default sits ten orders of magnitude above the measured gap. A bound that loose would pass a sink that had drifted to a different but nearby formula, which is the failure the original docstring named."
    - "Never restore exact equality by making the test recompute the value vectorized to match the library. That passes by coincidence of dispatch on this machine and can break on a runner with a different vector width — more fragile, not less."
    - "Never edit the other two D4 member files. They belong to the open post-submission todo."
    - "Never edit the guard-inertness test module. Its exact-equality policy is correct for its own subject."
    - "Never re-run an experiment stage or regenerate a committed artifact. The smoke invocations write to temporary directories only, and the frozen trees must be byte-unchanged afterwards."
    - "Never adjust a reported count toward its expectation. Record what the suite printed."
---

<objective>
Convert the two drifting assertions in the one non-slow D4 member to documented step-count bounds,
then measure everything CI will measure — the not-slow selection, the whole suite, and all seven
experiment smoke invocations — so no unknown reaches the release pull request as a red check.

Purpose: CI's `test` job runs the not-slow selection on two operating systems across two interpreter
versions. This member is not marked slow, so it reddens two cells on the pull request. The author's
standard for this phase was set by the question *"what is the real cost?"* — and the answer, from
instrumenting all 22 rows, is that two of four assertions move and two do not. The experiments smoke
job has never been exercised against this branch at all, which makes it an unknown rather than a
risk; this plan turns it into a measurement while a failure is still cheap.

Output: one modified test method, one evidence file, one amended todo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.2-merge-release-and-publish/29.2-CONTEXT.md
@.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md
@.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md
@.planning/todos/pending/2026-08-26-d4-exact-equality-anchors-are-platform-pinned.md
@.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md
</context>

## Reading order, and which document wins

Read `29.2-CONTEXT.md` first, then `29.2-RESEARCH.md`. Where they appear to disagree, **RESEARCH.md's
measurements win**. For this plan specifically: CONTEXT.md's discretion note deferred the exact
mechanism; RESEARCH.md § Q2 read numpy's source, executed the edge cases, and returns a concrete
recommendation with the exact figures. Use it. Note also that the **open todo's framing is wrong for
this member** and the plan must not inherit it — D-29.2-06 is the correction.

## Artifacts this plan produces

| Artifact | Kind |
|---|---|
| `tests/unit/test_optim_common.py` — two assertions converted, one docstring rewritten, one small assertion helper | modified |
| `29.2-smoke-runs.txt` | new evidence file |
| `.planning/todos/pending/2026-08-26-d4-exact-equality-anchors-are-platform-pinned.md` | modified |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert the two drifting assertions to documented step-count bounds</name>
  <reversibility rating="reversible">Two assertion lines and a docstring in one test file, with zero change to the library.</reversibility>
  <files>tests/unit/test_optim_common.py</files>
  <behavior>
    - The test method still collects the same 22 rows and still recomputes the geometry inline from the library's own formulas — nothing about what it proves changes.
    - The camera-height and corner-height assertions still compare with exact equality and still fail if either drifts by a single step. Both measured zero mismatches across all 22 rows.
    - The radial-distance assertion passes when the sink and the recomputed scalar differ by at most four representable steps, and fails at five.
    - The chord-incidence assertion passes at at most eight representable steps and fails at nine.
    - A failure still prints the offending row dict, because that is how every other assertion in this method reports, and it additionally prints the observed step count — so a reader sees both the bound and what was actually measured.
    - A sink that had drifted to a different but nearby formula still fails: eight steps at the measured magnitude is roughly one part in ten to the fifteenth, about nine orders of magnitude tighter than the relative-tolerance idiom this comparison must not become.
  </behavior>
  <read_first>
    - `tests/unit/test_optim_common.py` lines 1450-1500 — the whole method. Its docstring is at 1452-1459; the assertion block is at 1493-1500; the scalar recomputation of the radial distance is at 1490. Note the two local conventions the replacement must preserve: **every assertion in this method carries the row dict as its message**, and **assertion messages in this file explain what a failure would mean**, not merely what failed.
    - `src/aquacal/core/refractive_geometry.py` lines 661 and 675-679 — the library's vectorized computation the test recomputes scalar. **Read only; do not edit.**
    - `src/aquacal/calibration/_optim_common.py` line 992 — where the value is written into the sink. **Read only; do not edit.**
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *Q2a* through § *Q2d* — the function's exact semantics (the bound is inclusive; the default is already a one-step bound, not exact equality; it returns the per-element step-difference array), its correctness across zero and sign, the rejection of both alternatives with the reason, the recommended bounds with the headroom argument, and the availability note that the mechanism is stable across every version CI will install.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md` § *3* — the five-part documentation structure to copy from `tests/unit/test_datasets.py:958-973` (what the constant governs; the measurement in absolute and relative terms; the mechanism; why this value in orders of magnitude both ways; an explicit instruction to the next reader). **Copy that documentation shape, not its relative-tolerance mechanism.** Note also that this will be the first use of this assertion anywhere in the test tree, so the docstring carries more weight than usual.
    - `tests/unit/test_datasets.py` lines 955-973 — the analog itself, verbatim.
    - `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md` lines 40-46 — the per-item disagreement table. The row for this member is already written and should be quoted rather than re-derived.
  </read_first>
  <action>
Change exactly two of the four assertions in
`TestPerObservationDetailSinks::test_detail_sink_recomputed_geometry_matches_projector`.

**Leave the camera-height and corner-height assertions exactly as they are.** Both measured zero
mismatches across all 22 rows, so exact equality is the true statement about them and loosening them
would discard information for nothing.

**Convert the radial-distance assertion and the chord-incidence assertion** to bounded comparisons
using `np.testing.assert_array_max_ulp`, with `maxulp=4` for the radial distance and `maxulp=8` for
the chord incidence. The module already imports numpy, so no new import is needed. These bounds are
four times the locally measured worst cases of 1 and 2, which is the smallest step that is clearly
not a pin to this machine — CI runs on machines whose vector dispatch this project does not control,
and the diagnosis is precisely that the gap is a dispatch artifact.

**Resolve the message convention explicitly rather than dropping it.** `assert_array_max_ulp` accepts
no message argument, yet every assertion in this method carries the row dict as its message and this
file's convention is that a message explains what a failure would *mean*. Add a small private
module-level helper — `_assert_max_ulp(actual, desired, maxulp, row)` — that delegates to
`np.testing.assert_array_max_ulp` and, on `AssertionError`, re-raises with the library's own message
(which names the observed step count) joined to the row dict. Give the helper a two-line docstring
saying why it exists: numpy's assertion reports the observed gap but not which row produced it, and
this method's convention is that both are visible. Returning the library call's value from the helper
keeps the per-element step array available to a future caller that wants to record it.

**Rewrite the method docstring** per D-29.2-08, following the five-part structure from the analog:

1. **What the comparison governs** — that the three geometric quantities must be the projector's own,
   not an approximation.
2. **The measurement**, from the 22-row instrumentation: the two height quantities agree on all 22
   rows; the radial distance disagrees on 6 of 22 by at most one step; the chord incidence disagrees
   on 3 of 22 by at most two. Quote the recorded disagreement — a sink value of `0.10681280743540097`
   against a recomputed `0.10681280743540099`.
3. **The mechanism, stated as a correction** — the library computes the radial distance vectorized
   over many points while this test computes it scalar on one element, and numpy's array and scalar
   paths can differ by one representable step **on the same machine**. This is a dispatch asymmetry,
   not a captured constant, and not a platform pin: it was invisible on one operating system only
   because that build dispatched differently. Say so explicitly, because the open todo classifies
   this member the other way and a reader who follows the todo here will draw the wrong conclusion.
   The chord incidence inherits that one step through two library calls, which is why its bound is
   larger — one inherited step plus at most one rounding each.
4. **Why these values, in orders of magnitude both ways** — four-fold headroom above the measured
   worst cases, and, at the measured magnitude, eight steps is about `1.11e-16` absolute and
   `1.04e-15` relative, roughly nine orders of magnitude tighter than the relative-tolerance idiom
   whose default this comparison must never become. State the number, not the adjective.
5. **An explicit instruction to the next reader** — do not widen these bounds to a relative tolerance,
   and do not "fix" this by making the test recompute the value vectorized to match the library.
   The second is the tempting move and it is worse: identical dispatch depends on array length and
   memory alignment, so the test would pass by coincidence here and could break on a runner with a
   different vector width.

Add a one-line inline comment at each of the two changed assertions naming its measured worst case
and its bound, so a reader who scrolls straight to the assertion sees the number without scrolling
back to the docstring.

Do not touch the library. Do not touch the other two D4 member files. Do not touch the
guard-inertness test module, whose exact-equality policy is correct for its own subject. Do not
suppress, deselect or mark any test.
  </action>
  <verify>
    <automated>python3 -m pytest "tests/unit/test_optim_common.py::TestPerObservationDetailSinks::test_detail_sink_recomputed_geometry_matches_projector" -q -p no:cacheprovider 2>&amp;1 | tail -3 | grep -qE '1 passed' &amp;&amp; [ "$(grep -c 'maxulp=4' tests/unit/test_optim_common.py)" = "1" ] &amp;&amp; [ "$(grep -c 'maxulp=8' tests/unit/test_optim_common.py)" = "1" ] &amp;&amp; [ "$(grep -c '_assert_max_ulp' tests/unit/test_optim_common.py)" -ge "3" ] &amp;&amp; [ "$(grep -c 'h_c_m..] == h_c' tests/unit/test_optim_common.py)" = "1" ] &amp;&amp; [ "$(grep -c 'h_q_m..] == h_q' tests/unit/test_optim_common.py)" = "1" ] &amp;&amp; grep -q '0.10681280743540097' tests/unit/test_optim_common.py &amp;&amp; git diff --quiet -- tests/unit/test_discard_accounting.py tests/unit/test_pipeline.py tests/synthetic/test_guard_inertness.py src/</automated>
  </verify>
  <acceptance_criteria>
    - The target test node passes on this machine, reported as `1 passed` — not skipped, not deselected, not marked expected-to-fail.
    - The file contains exactly one `maxulp=4` and exactly one `maxulp=8`.
    - The two height assertions still compare with exact equality, asserted by grep against their source lines.
    - The helper is defined once and called twice.
    - The docstring quotes the measured disagreement pair verbatim and states the vectorized-versus-scalar mechanism as a correction of the open todo's classification.
    - The docstring names the absolute and relative size of the larger bound and how many orders of magnitude tighter it is than the forbidden idiom.
    - The docstring carries an explicit instruction against both widening the bound and restoring exact equality by matching the library's vectorization.
    - `git diff --quiet` is clean over the other two D4 member files, the guard-inertness module, and the whole source tree.
  </acceptance_criteria>
  <done>The one D4 member CI's not-slow selection runs now passes, with a bound that still fails loudly on a real formula change, and the reason lives at the assertion rather than in a commit message.</done>
</task>

<task type="auto">
  <name>Task 2: Measure both suites and all seven smoke invocations, and record them with their exit codes</name>
  <precondition>Task 1's change is present in the working tree — the counts recorded here are the post-fix counts, and running this before the fix would record the wrong invariant.</precondition>
  <files>.planning/phases/29.2-merge-release-and-publish/29.2-smoke-runs.txt</files>
  <read_first>
    - `.github/workflows/test.yml` lines 33-34 and lines 75-83 — the exact test command the `test` job runs and the seven smoke invocations of the `experiments-smoke` job, verbatim and in order. Run those seven lines, in that order, not a reconstruction of them.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md` § *4* — the evidence-file conventions: the self-naming banner with a labelled provenance block whose last clause pre-empts the reader's objection; a **both-numbers-in-one-place** section immediately after the header; `  $ <command>` then raw output then `(exit code N)` on its own line; and a prohibitions-honoured block backed by measurements.
    - `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md` lines 24-36 — the measurement-without-softening convention and the three failing node ids as they stood before this plan.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-VALIDATION.md` § *Test Infrastructure* — the expected end-state counts and the runtime expectations (the not-slow selection around five minutes; the whole suite around twenty-eight).
  </read_first>
  <action>
Take three measurements and record all of them, including any that disagree with expectation.

**Measurement 1 — the not-slow selection.** Run the `test` job's own command over the test tree.
Expect no failures. This is the command that runs on four CI cells, so a failure here is a red check
on the pull request.

**Measurement 2 — the whole suite.** Run it without the marker filter. Expect **exactly two
failures**, both marked slow, both reading constants from the fixtures tree, and record their node
ids verbatim. Gate on the failure count only — the passed and skipped counts differ between recorded
environments and are not a stable invariant. Record the before-and-after in one place: three failures
at the frozen tag, two now, with the node id that left the set named. Add the sentence the convention
requires: neither number was adjusted toward the expectation.

**Measurement 3 — the seven smoke invocations.** Run all seven, in the order the workflow lists them,
each as its own command, each with its exit code recorded. This job has never been exercised against
this branch's code, so treat every non-zero exit as a finding to report rather than a thing to fix
quietly — and if one does fail, stop and report it rather than proceeding, because it is a red check
on the pull request and it belongs in the go/no-go rather than in a workaround. All seven write into
temporary directories, so the frozen trees must be byte-unchanged afterwards; assert that they are.

Write `29.2-smoke-runs.txt` in the established shape:

- A self-naming banner and a labelled provenance block: date, branch, head sha, the interpreter's
  full path and version, and the pytest version — with the clause that pre-empts the objection,
  namely that the same interpreter took all three measurements so the counts compare like with like.
- A **both-numbers-in-one-place** table immediately after the header: the whole-suite count before
  and after, and the not-slow count before and after.
- A statement of the new invariant in the register the prior audit used — two is the expected
  whole-suite count, and both zero and three are anomalies: a zero would mean the two remaining ruled
  anchors had been silenced, a three would mean something new had broken.
- The three measurement sections, each with the command verbatim, its raw output and its exit code.
- A prohibitions-honoured block backed by measurements: no experiment stage was re-run; no committed
  artifact was regenerated, evidenced by a clean diff over the experiments tree; the two remaining
  D4 members were not silenced, evidenced by their node ids still appearing in the failure list; and
  the two md5 anchors are unmoved.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -m "not slow" -q -p no:cacheprovider &amp;&amp; python3 -m pytest tests/ -q -p no:cacheprovider 2>&amp;1 | tail -20 | tee /tmp/29.2-full.txt | grep -qE '(^|[^0-9])2 failed' &amp;&amp; grep -q 'test_matches_frozen_anchor' /tmp/29.2-full.txt &amp;&amp; grep -q 'test_matches_pre_change_anchor' /tmp/29.2-full.txt &amp;&amp; for m in e1_refractive_comparison e2_real_rig e7_interface_ablation e3_derived_quantities e4_benchmark_grid e5_index_sensitivity e6_generalization_sweep; do python3 -m experiments.$m --smoke > /tmp/29.2-smoke-$m.log 2>&amp;1 || { echo "SMOKE FAILED: $m"; exit 1; }; done &amp;&amp; git diff --quiet -- experiments/ &amp;&amp; [ "$(md5sum experiments/results/real_rig_metrics.json | cut -d' ' -f1)" = "57279708f6106f411d1fe03ed2698291" ] &amp;&amp; [ "$(md5sum experiments/results/interface_ablation_band.csv | cut -d' ' -f1)" = "b6515ed77ed04268608b74217716020b" ] &amp;&amp; grep -q 'exit code' .planning/phases/29.2-merge-release-and-publish/29.2-smoke-runs.txt</automated>
  </verify>
  <acceptance_criteria>
    - The not-slow selection reports no failures.
    - The whole suite reports exactly two failures, and both remaining D4 node ids appear in its output.
    - All seven smoke invocations exit zero, run in the workflow's own order, from the workflow's own command lines.
    - The experiments tree is byte-unchanged after the smoke runs, and both md5 anchors match their recorded digests.
    - `29.2-smoke-runs.txt` carries the self-naming banner, the provenance block with the interpreter path and version, the both-numbers table, the new-invariant statement naming what a zero and a three would each mean, all ten commands verbatim with exit codes, and a prohibitions-honoured block backed by measurements.
    - The file states explicitly that no number was adjusted toward its expectation.
  </acceptance_criteria>
  <done>Every check CI will run on the pull request has been run here first, and the new whole-suite invariant is a measurement in an evidence file rather than an expectation in a plan.</done>
</task>

<task type="auto">
  <name>Task 3: Amend the D4 todo to name two members, and record why the third left the set</name>
  <precondition>The whole-suite measurement from Task 2 exists and names exactly two failing node ids — the todo is being amended to match a measured state, not an anticipated one.</precondition>
  <files>.planning/todos/pending/2026-08-26-d4-exact-equality-anchors-are-platform-pinned.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-26-d4-exact-equality-anchors-are-platform-pinned.md` — the whole file. Its frontmatter names three files; its title says three; its *What fails* section lists three node ids and a whole-suite count. Its body convention is: why this is filed rather than fixed, then what fails with verbatim counts and node ids, then what would fix it, then the evidence cross-references. Preserve that convention.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-CONTEXT.md` D-29.2-06, D-29.2-09 and D-29.2-10 — the mis-classification, the untouched pair, and the instruction that prior artifacts are superseded in this phase's record and never rewritten.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md` § *6* — the mechanical shape of this amend, and § *S6* on superseding without rewriting.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-smoke-runs.txt` — Task 2's measured counts, which this amend must agree with.
  </read_first>
  <action>
Amend the open todo so it describes the two members it still owns, and records honestly why the third
is no longer among them.

Mechanically: retitle it from three anchors to two; drop `tests/unit/test_optim_common.py` from the
frontmatter file list; drop that member's node id from the *What fails* list; and update the
whole-suite count quoted in that section to the count Task 2 measured, citing the evidence file it
came from rather than restating it from memory. Leave `resolves_phase` pointing where it does — the
cross-platform question the todo exists to answer belongs to the post-submission phase and is not
answered here.

Add a short new section recording the departure. It must say three things:

1. **What was done** — the member's two drifting assertions became documented step-count bounds in
   Phase 29.2, and it now passes.
2. **Why its framing here was wrong** — this member has no captured anchor at all. It recomputes the
   geometry inline and compares against the sink, so the disagreement is between the library's
   vectorized evaluation and the test's scalar evaluation of the same formula on the same machine.
   The cross-platform question this todo poses genuinely does not arise for it, and the todo said
   otherwise. Record that as a correction of this document, not as a defect of the phase that wrote
   it — the classification was reasonable from the failure list alone.
3. **Why the other two are different and stay** — both read constants from the fixtures tree, both
   are marked slow, and the question of what an anchor means across platforms is live for them.

Preserve the do-not-silence instruction exactly as written; it still governs the remaining two, and
Phase 29's precedent of repairing eight rail failures without suppressing one is the reason it is
there.

**Do not rewrite any prior artifact to agree with this.** The frozen tag's annotated message and the
pre-push audit's section still say three, and they stay readable as what was true when they were
written. The superseding statement belongs in this phase's own record, which plan 29.2-08 owns —
the same reasoning that declined to edit an earlier finding in place. Note in the amend where that
superseding statement will live.
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
p = '.planning/todos/pending/2026-08-26-d4-exact-equality-anchors-are-platform-pinned.md'
s = open(p).read()
fm = s.split('---')[1]
assert 'test_discard_accounting.py' in fm and 'test_pipeline.py' in fm, fm
assert 'test_optim_common.py' not in fm, 'the fixed member is still in the frontmatter file list'
assert s.count('test_detail_sink_recomputed_geometry_matches_projector') >= 1, 'the departure is not recorded'
body = s.split('---',2)[2]
assert 'What fails' in body and 'do not' in body.lower()
title = re.search(r'^title: (.+)$', fm, re.M).group(1)
assert 'three' not in title.lower(), title
print('todo names two members; the fixed one is out of the file list and its departure is recorded')
" &amp;&amp; grep -qi 'vectoriz\|scalar' .planning/todos/pending/2026-08-26-d4-exact-equality-anchors-are-platform-pinned.md &amp;&amp; git diff --quiet -- .planning/phases/29-gate-verification-results-commit/ .planning/phases/29.1-post-run-fixes-re-freeze/</automated>
  </verify>
  <acceptance_criteria>
    - The todo's title names two anchors, not three.
    - The frontmatter file list carries the two remaining member files and no longer carries the fixed one.
    - A new section records the departure, naming the fixed node id, the vectorized-versus-scalar mechanism, and the fact that this document's own classification of that member was wrong.
    - The whole-suite count quoted in *What fails* matches the count in `29.2-smoke-runs.txt` and cites it.
    - The do-not-silence instruction is preserved verbatim in substance.
    - `git diff --quiet` is clean over the Phase 29 and Phase 29.1 directories — nothing prior was rewritten to agree with this.
  </acceptance_criteria>
  <done>The open todo describes the two members it still owns, and the reason the third left is written down where the next reader of the todo will find it rather than inferred from a shrinking failure list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test assertion -> the scientific claim it guards | loosening a numeric comparison can convert a real formula regression into a silent pass, and this comparison is what proves the detail sink carries the projector's own quantities |
| frozen artifact trees -> the smoke invocations | seven experiment entry points execute against the committed tree; a mode that wrote into it would alter provenance bytes |
| documented failure count -> the record | an expected-count invariant that is edited to match reality rather than measured stops being evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.2-10 | Tampering | the two converted assertions in `tests/unit/test_optim_common.py` | high | mitigate | The bound is a hard count of representable steps with four-fold headroom over the measured worst cases, roughly nine orders of magnitude tighter than the relative-tolerance idiom D-29.2-08 forbids. A drifted formula still fails. The two assertions that measured no disagreement keep exact equality, and the acceptance criteria assert that programmatically rather than trusting the diff. |
| T-29.2-11 | Tampering | the frozen experiment trees during the seven smoke invocations | high | mitigate | Every smoke path writes into a temporary directory; the task asserts a clean diff over the experiments tree and re-checks both md5 anchors after all seven runs, so a mode that wrote into the tree is caught rather than assumed absent. |
| T-29.2-12 | Repudiation | silencing a ruled defect to obtain a green board | high | mitigate | Suppressing any of the three members is prohibited, and the target test's verify asserts the node reports as passed rather than as skipped or deselected. The whole-suite verify asserts both remaining node ids still appear in the failure output, so a later suppression is detected rather than merely forbidden. |
| T-29.2-13 | Repudiation | rewriting prior records to agree with the new count | medium | mitigate | Task 3 forbids it and asserts a clean diff over the Phase 29 and Phase 29.1 directories. The superseding statement is deliberately routed to this phase's own record instead. |
| T-29.2-SC | Tampering | `pip install` of any package-manager dependency | high | mitigate | This plan installs nothing; it uses the interpreter and the already-installed development dependencies. RESEARCH § *Package Legitimacy Audit* records that this phase adds no new external package and returns no `[ASSUMED]`, `[SUS]` or `[SLOP]` verdicts. No legitimacy checkpoint is required. |
</threat_model>

<verification>
- The target D4 node reports `1 passed`.
- Exactly one `maxulp=4` and one `maxulp=8` appear in the changed file; the two height assertions still compare with exact equality; the assertion helper is defined once and called twice.
- The not-slow selection reports no failures; the whole suite reports exactly two, and both remaining D4 node ids appear.
- All seven smoke invocations exit zero; the experiments tree diff is clean and both md5 anchors are unmoved.
- The todo names two members and records why the third left.
- Nothing under the source tree, the other two D4 member files, the guard-inertness module, or the Phase 29 and 29.1 directories has changed.

## Probe edges surfaced for RUN-05

Four applicable edges were surfaced by the deterministic probe and none were pre-resolved. All four
are accounted for below; none is silently dropped. Three are answered from measured facts and lifted
into `must_haves.truths` on the plans that own them; one remains a flagged assumption.

| Edge | Disposition | Where it is answered |
|---|---|---|
| `ordering` — when elements compare equal, is output order specified and stable? | **Answered.** This phase IS an ordering contract: the version must exist before PyPI is approved, PyPI before Record B is published, Record B before Record A's back-link, and publication before submission. Lifted into `must_haves.truths` on plans 29.2-05, 29.2-06 and 29.2-07. | D-29.2-16, D-29.2-25, D-29.2-26 |
| `concurrency` — if interrupted or run in parallel, what is guaranteed? | **Answered.** Measured: the release tool writes the version files, runs the build, and aborts before commit, tag and push, so a mid-run failure leaves nothing half-published and the ephemeral checkout needs no cleanup. Lifted into `must_haves.truths` on plan 29.2-05. A concurrency group on the release job additionally serialises runs. | D-29.2-33, RESEARCH § Q6f, `release.yml` line 11 |
| `adjacency` — when two things are exactly equal or just touch, do they merge, collide, or separate? | **Answered.** Three measured boundary behaviours: the step bound is inclusive, so a gap exactly equal to the bound passes and one more fails; both package indices reject a re-upload of an existing name-and-version pair with a client error, and the target version is free on both; and re-running the release tool against an existing tag is idempotent, reporting the version as already released rather than cutting a second one. Lifted into `must_haves.truths` on plans 29.2-03 and 29.2-06. | RESEARCH § Q2a, § Q6d, § Q1-REHEARSAL |
| `empty` — what is the result for empty, single-element, or null input? | **FLAGGED ASSUMPTION — unresolved.** Two empty-input cases are measured and safe: an empty releasable commit set makes the release tool report that no release will be made (which is the silent squash failure mode plan 29.2-05 guards against), and an empty pending-deployments response is the post-cancellation success signal. But **what this phase does if the pull request's check board comes back with zero checks reported** — workflows disabled at the repository level, or a trigger that does not fire — is specified nowhere in any source artifact. Plan 29.2-04 therefore treats a zero-check board as NOT-green and halts. **That is a planner-authored assumption, not a measured behaviour, and it is recorded here as one.** | flagged; no source |

## Prohibition recall for RUN-05

Run in-prompt over the phase requirement. Stage 1 over-produced roughly fifteen must-NOT candidates;
Stage 2 dropped the routine-engineering ones — flag ordering, proxy-versus-property assertions,
command syntax — which belong to the edge probe and to the tasks' own verify gates. Ten survivors
were kept as values, safety and scientific-record items and are authored **descriptor-less** into the
`must_haves.prohibitions:` block of the plan each one governs, so each disposes flagged-unverified.

**Canon-referral breadcrumb (kept candidate, deliberately not minted):** credential handling — never
writing the Zenodo token, the release PAT or any index credential into a file, a log or a command-line
argument — is canon security, covered by `/gsd-secure-phase` and by the `detect-secrets` hook; a
breadcrumb is recorded here and no bespoke prohibition is minted for it.
</verification>

<success_criteria>
The one D4 member CI's not-slow selection runs passes with a documented, still-strict bound; the new
whole-suite invariant and all seven smoke paths are measured rather than assumed; and the open todo
describes exactly the two members it still owns.
</success_criteria>

<output>
Create `.planning/phases/29.2-merge-release-and-publish/29.2-03-SUMMARY.md` when done.
</output>
