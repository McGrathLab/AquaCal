---
phase: 29.2-merge-release-and-publish
plan: 04
type: execute
wave: 2
depends_on: ["29.2-01", "29.2-02", "29.2-03"]
files_modified:
  - .planning/phases/29.2-merge-release-and-publish/29.2-precommit-allfiles-proof.txt
autonomous: true
requirements: [RUN-05]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The exact command CI runs — the whole-tree hook sweep — has been run once locally, and it has never been run anywhere in this project before; every prior hook invocation was scoped to named files (D-29.2-03)."
    - "The proof asserts FOUR independent things, not one. Exit zero is not the bar: the working tree's status output must be unchanged, both md5 anchors must be unmoved, and the driver-state file must still carry five tab-separated fields on every one of its lines."
    - "The five-field assertion is in the proof because the status check catches that something was rewritten while the field count names WHICH invariant was at stake — and only the named one survives into the evidence file for a future reader."
    - "The pull request into the default branch exists, is open, targets that branch from the results branch, and was created without merging anything."
    - "The check board is reported per job with its conclusion and its link, so the author reads evidence rather than a claim. The test job contributes four rows from its two-by-two matrix, alongside the hook job and the experiments job."
    - "A board reporting zero checks is treated as NOT green and halts, because a workflow that never fired proves nothing about the tree."
    - "The default branch carries neither classic protection nor a newer ruleset, so red checks do not mechanically block a merge. That is context for the author's judgment and explicitly not a licence to merge red (D-29.2-22)."
  artifacts:
    - path: ".planning/phases/29.2-merge-release-and-publish/29.2-precommit-allfiles-proof.txt"
      provides: "The four-assertion proof, recorded verbatim with exit codes, plus the pull request number and the per-job check board"
  key_links:
    - "This plan's proof is only meaningful on a committed-clean tree, because its central assertion is that the working tree's status output did not change across the hook run. Plans 29.2-01 through 29.2-03 must have committed everything they produced before this runs."
    - "The hook configuration this proves is the one plans 29.2-02 wrote; the sweep is the first execution of those patterns against the real tree, and it is what converts a static regex check into a proof."
    - "The pull request's `pre-commit` job runs this same command on a fresh checkout, so a local pass here is the strongest available predictor of that job's colour."
  prohibitions:
    - "Never run the whole-tree hook sweep before the exclude edits have landed and been committed. Before them it rewrites the byte-sensitive artifacts, and on a dirty tree the proof's own assertion is meaningless."
    - "Never merge, never enable auto-merge, and never approve anything. This plan opens a pull request and reports; the merge is the author's act."
    - "Never restore a rewritten artifact and re-run to obtain a pass. If the sweep rewrites anything, that is the finding — report it with the file list rather than repairing around it."
    - "Never treat exit zero alone as the proof. A hook that rewrote a file and then reported success is the exact failure this proof exists to catch."
    - "Never report a green board without the per-job rows behind it."
---

<objective>
Prove, by executing it, that CI's whole-tree hook sweep passes on this tree without rewriting a
single byte — then open the pull request and report its check board with evidence.

Purpose: ROADMAP criterion 1, and the gate every later step depends on. The command has never been
invoked anywhere in this project; every hook run in Phase 29 was scoped to named files. Running it
here, once, on a committed-clean tree, is what turns "the excludes look right" into a measurement.
The pull request follows immediately, because a green board on the real runners is the last thing
that can be established before the author touches an irreversible control.

Output: one evidence file carrying the four assertions, the pull request number, and the board.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.2-merge-release-and-publish/29.2-CONTEXT.md
@.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md
@.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md
@.planning/phases/29.2-merge-release-and-publish/29.2-02-SUMMARY.md
@.planning/phases/29.2-merge-release-and-publish/29.2-03-SUMMARY.md
@.planning/todos/pending/2026-08-26-no-pull-request-into-main-until-ci-pre-commit-is-scoped.md
</context>

## Artifacts this plan produces

| Artifact | Kind |
|---|---|
| `29.2-precommit-allfiles-proof.txt` | new evidence file |
| The pull request from the results branch into the default branch | new service-side state |

<tasks>

<task type="auto">
  <name>Task 1: Run the whole-tree hook sweep and assert all four invariants</name>
  <precondition>`git status --porcelain` produces no output — every artifact from plans 29.2-01 through 29.2-03 is committed. The central assertion of this task is that this output does not change across the hook run, so a dirty starting tree makes the proof meaningless; halt and report rather than stashing or committing on the tree's behalf.</precondition>
  <files>.planning/phases/29.2-merge-release-and-publish/29.2-precommit-allfiles-proof.txt</files>
  <read_first>
    - `.github/workflows/test.yml` lines 42-57 — the `pre-commit` job: its interpreter version, its unpinned install of the hook runner, and the exact sweep command. Reproduce that install unpinned, as CI does, and record which version resolved.
    - `.pre-commit-config.yaml` — as plan 29.2-02 left it. All seven hooks run under this sweep, not only the three that were repaired.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-CONTEXT.md` D-29.2-03 including its 2026-08-26 amendment — the four assertions, both md5 digests verbatim, and the field-count check with its command.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *Q3f* — what each of the seven hooks was measured to do, so an unexpected result can be recognised as unexpected. Note in particular that the lint hook runs with automatic fixing enabled, so had it found anything it would have rewritten source and broken the status assertion; it is clean today but it is not guarded by anything except this job.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *Environment Availability* — the hook runner is not installed on this machine. Install it into an isolated environment rather than into the base interpreter.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md` § *4* and § *S3* — the evidence-file conventions, and the instruction that where a command has never been run in this project, the file must say so.
  </read_first>
  <action>
Run CI's whole-tree hook sweep once, on a committed-clean tree, and assert all four things D-29.2-03
requires. Capture everything as you go; this file is the artifact.

Before the run, record the baseline: the working tree's status output (which must be empty), both md5
digests, and the driver-state file's distinct field count. After the run, record all three again and
compare.

The four assertions, all of which must hold:

1. **Exit zero** from the sweep. Record the exit code explicitly on its own line, in the established
   convention, rather than inferring it from the absence of an error.
2. **The working tree's status output is unchanged and still empty.** This is the assertion that
   catches a hook that rewrote a file and then reported success.
3. **Both md5 anchors are unmoved** — `57279708f6106f411d1fe03ed2698291` for the real-rig metrics and
   `b6515ed77ed04268608b74217716020b` for the interface ablation band. These are what
   `gate3_git_sha_consistency` rests on.
4. **The driver-state file still carries five tab-separated fields on every line.** Take the distinct
   field count across all lines and assert it is the single value five. The status check would catch a
   rewrite generically; this assertion names the specific invariant, and it is the one that survives
   into the evidence file where a future reader can see what was at stake.

Install the hook runner the way CI does — unpinned — into an isolated environment, and record the
version that resolved. Record the per-hook result lines the sweep prints, all seven of them, not only
the three that were repaired.

**If anything was rewritten, that is the finding.** Record the file list and stop. Do not restore and
re-run to obtain a pass; the whole point of the assertion is that a rewrite is reportable.

Write `29.2-precommit-allfiles-proof.txt`:

- Self-naming banner and a labelled provenance block: date, branch, head sha, the resolved hook-runner
  version, the interpreter, and the commit that carried the configuration change.
- An explicit statement, in the file, that **this command has never been invoked anywhere in this
  project before** — every prior hook run was scoped to named files — so a reader understands why a
  clean result here is news rather than routine.
- The four assertions as four labelled sections, each with `  $ <command>` verbatim, its raw output,
  and `(exit code N)` on its own line.
- A before-and-after block putting the baseline and post-run values side by side for all three
  measured quantities, in the both-numbers convention.
- A prohibitions-honoured block backed by measurements: no committed artifact was regenerated; no
  hook was skipped, disabled or bypassed; the configuration was not edited during the run; and the
  sweep was run on a committed-clean tree so the status assertion means what it says.
- A `Scope:` paragraph: this proves the hooks against **this** tree at **this** sha. CI runs the same
  command on a fresh checkout, which is the same tree, so the prediction is strong — but it is a
  prediction until the job reports.
  </action>
  <verify>
    <automated>[ -z "$(git status --porcelain --untracked-files=all -- ':!*29.2-precommit-allfiles-proof.txt')" ] &amp;&amp; [ "$(md5sum experiments/results/real_rig_metrics.json | cut -d' ' -f1)" = "57279708f6106f411d1fe03ed2698291" ] &amp;&amp; [ "$(md5sum experiments/results/interface_ablation_band.csv | cut -d' ' -f1)" = "b6515ed77ed04268608b74217716020b" ] &amp;&amp; [ "$(awk -F'\t' '{print NF}' experiments/run_experiment_suite_state.7005a27.tsv | sort -u | tr '\n' ',')" = "5," ] &amp;&amp; grep -qE 'exit code 0' .planning/phases/29.2-merge-release-and-publish/29.2-precommit-allfiles-proof.txt &amp;&amp; grep -q '57279708f6106f411d1fe03ed2698291' .planning/phases/29.2-merge-release-and-publish/29.2-precommit-allfiles-proof.txt &amp;&amp; grep -q 'b6515ed77ed04268608b74217716020b' .planning/phases/29.2-merge-release-and-publish/29.2-precommit-allfiles-proof.txt &amp;&amp; grep -qi 'never been' .planning/phases/29.2-merge-release-and-publish/29.2-precommit-allfiles-proof.txt</automated>
  </verify>
  <acceptance_criteria>
    - The sweep exited zero, and the exit code is recorded on its own line in the evidence file.
    - The working tree's status output is empty after the run, apart from the evidence file being written.
    - Both md5 digests match the recorded anchors, asserted by string equality against the literals.
    - The driver-state file's distinct field count is exactly the single value five.
    - The evidence file records all seven per-hook result lines, the resolved hook-runner version, and the statement that this command had never been run in this project.
    - The evidence file carries a before-and-after block, a prohibitions-honoured block backed by measurements, and a `Scope:` paragraph.
  </acceptance_criteria>
  <done>The command that has blocked this pull request since the provenance commit landed has been run once, locally, and proved not to have touched a byte — with the specific invariant named rather than only generically observed.</done>
</task>

<task type="auto">
  <name>Task 2: Open the pull request into the default branch, without merging anything</name>
  <precondition>`gh auth status` reports an authenticated account with repository scope, and `gh pr list --state open` returns an empty list — no pull request has ever been opened on this repository, so an existing one would mean something unexpected happened.</precondition>
  <files>.planning/phases/29.2-merge-release-and-publish/29.2-precommit-allfiles-proof.txt</files>
  <read_first>
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *Q5a* — the creation and status-reporting commands, and the note that the creation command never merges. § *Q5c* — the repository's merge settings: all three merge methods are enabled, the automatic branch deletion setting is off, no ruleset exists, and the default branch is unprotected.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md` § *No Analog Found* — no pull request has ever been opened on this repository, so there is no body template to match; the nearest shape is the verdict-first opener of the pre-push audit.
    - `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md` lines 1-19 — that verdict-first shape: bolded one-line verdicts at the very top, before any evidence, with an explicit note on why the most important section is first.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-01-SUMMARY.md`, `29.2-02-SUMMARY.md`, `29.2-03-SUMMARY.md` — what the three wave-one plans actually delivered, which is what the body summarises.
    - `.planning/phases/29-gate-verification-results-commit/29-PHASE-RECORD.md` § *WHAT REMAINS* — the ordered steps this pull request is step one of.
  </read_first>
  <action>
Create the pull request from the results branch into the default branch using the creation command,
with the body supplied from a file. Creating a pull request never merges anything.

Write the body in the verdict-first shape this project uses for its rulings, since there is no
existing pull-request convention to match. It must open with bolded one-line verdicts before any
evidence, and say why that ordering was chosen. It must carry:

- **What this merges** — the results branch, its commit count against the default branch as
  re-derived during the rehearsal rather than as quoted from the roadmap, and what the branch
  contains: the full production suite run, its committed artifacts, and every library change from the
  phases that produced them.
- **What must happen after the merge, and in what order** — the release job fires on the merge commit,
  cuts the version, bumps both version files, writes the changelog, and tags; the tag fires the
  publish workflow, which reaches a human approval gate before the package index. State that the
  rehearsal already executed all of this against a throwaway clone and what it produced.
- **The one instruction that cannot be got wrong: this must be merged with a merge commit, never a
  squash.** Explain the consequence in one sentence — a squash collapses every commit into a single
  message and the release tool parses only that message, so the release silently does not happen.
  Note that the repository has all three merge methods enabled and the button remembers the last
  method used, so the method must be positively selected rather than accepted.
- **A second instruction: decline the delete-branch prompt after merging.** Both the branch and the
  tag are kept deliberately; the tag names the frozen sha the published record cites, and the pair is
  the record of the two run attempts. The repository's automatic-deletion setting is already off, so
  this is a matter of declining a button rather than changing a setting.
- **The evidence links** — the four evidence files this phase has produced so far.

After creation, record in `29.2-precommit-allfiles-proof.txt`: the pull request number, its URL, its
base and head refs, and a confirmation that no merge, auto-merge or approval was performed.

Do not merge. Do not enable auto-merge. Do not approve. Do not request a review that would
auto-merge on approval. Do not push any new commit to the branch after creation without re-running
Task 1's proof, because the board would then describe a tree that no longer exists.
  </action>
  <verify>
    <automated>PR=$(gh pr list --state open --base main --head results/rerun-freeze-02 --json number,baseRefName,headRefName,state,mergedAt --jq '.[0]') &amp;&amp; echo "$PR" &amp;&amp; [ "$(echo "$PR" | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d["baseRefName"], d["headRefName"], d["state"], d["mergedAt"])')" = "main results/rerun-freeze-02 OPEN None" ] &amp;&amp; grep -qE 'https://github.com/McGrathLab/AquaCal/pull/[0-9]+' .planning/phases/29.2-merge-release-and-publish/29.2-precommit-allfiles-proof.txt</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one open pull request exists, from the results branch into the default branch, in the open state and unmerged.
    - Its body opens with bolded one-line verdicts, states the merge-commit instruction with its consequence, states the decline-deletion instruction, and links the phase's evidence files.
    - The evidence file records the pull request number, URL, base and head refs, and a confirmation that nothing was merged, auto-merged or approved.
    - No commit was pushed to the branch after the proof in Task 1 was taken.
  </acceptance_criteria>
  <done>The pull request exists and carries, in its own body, the two instructions whose omission would silently cost the release.</done>
</task>

<task type="auto">
  <name>Task 3: Report the check board per job, with links, and rule green or not-green</name>
  <precondition>The pull request created in Task 2 is open, and its number is recorded in `29.2-precommit-allfiles-proof.txt`.</precondition>
  <files>.planning/phases/29.2-merge-release-and-publish/29.2-precommit-allfiles-proof.txt</files>
  <read_first>
    - `.planning/phases/29.2-merge-release-and-publish/29.2-RESEARCH.md` § *Q5a* — the watch and machine-readable status commands, and the note that the test job is a two-by-two matrix so it contributes four rows alongside the hook job and the experiments job.
    - `.github/workflows/test.yml` — the three jobs and their matrix, so the expected row count can be stated before the board is read rather than after.
    - `.planning/phases/29.2-merge-release-and-publish/29.2-PATTERNS.md` § *S5* — verdict first, evidence after, and name who presses the button. The go/no-go report to the author follows this shape.
  </read_first>
  <action>
Wait for the checks to settle, then report the board as evidence rather than as a claim.

State the expected row count **before** reading the board: four from the test matrix plus the hook
job plus the experiments job. Then fetch the per-check rows in machine-readable form — name,
state, bucket and link for each — and record every row verbatim in the evidence file.

Rule the board explicitly:

- **Green** requires every expected row present and successful. A green board means the three
  repairs held on the real runners, which is what ROADMAP criterion 1 asks for.
- **Not green** covers three distinct cases, and the report must distinguish them, because they have
  different consequences: a failing job (report which, with its link and the failing step); a missing
  job (a workflow that did not fire); and **a board reporting zero checks at all**. Treat the last as
  not-green and halt. A workflow that never ran proves nothing about the tree, and no source artifact
  in this phase specifies what a zero-check board means — this is recorded as a flagged planner
  assumption rather than as a measured behaviour.

Record the ruling in the evidence file with the full board beneath it, and report it to the author in
the verdict-first shape: the verdict on one line, then the rows, then the note that the merge is the
author's act and that plan 29.2-05 owns the go/no-go.

Record alongside it, as context and explicitly not as a licence: the default branch carries neither
classic protection nor a newer ruleset, so a red check does not mechanically block a merge. State in
as many words that this is why the repairs exist — so the board goes green on its merits, not so that
a red one can be merged past.

Do not merge, approve, re-run, or cancel anything. If a job failed, report it and stop; the repair
belongs in its own plan against a fresh measurement, not in a workaround here.
  </action>
  <verify>
    <automated>PRNUM=$(gh pr list --state open --base main --head results/rerun-freeze-02 --json number --jq '.[0].number') &amp;&amp; gh pr checks "$PRNUM" --json name,state,bucket,link > /tmp/29.2-checks.json &amp;&amp; python3 -c "
import json
rows = json.load(open('/tmp/29.2-checks.json'))
assert rows, 'ZERO checks reported - treated as NOT green, halting per plan'
bad = [r for r in rows if r.get('bucket') not in ('pass',)]
print('rows:', len(rows))
for r in rows: print(' ', r['name'], r['state'], r.get('bucket'), r.get('link'))
assert len(rows) >= 6, 'expected at least 6 rows: 4 test-matrix cells + pre-commit + experiments-smoke'
assert not bad, bad
" &amp;&amp; grep -qiE 'pre-commit' .planning/phases/29.2-merge-release-and-publish/29.2-precommit-allfiles-proof.txt</automated>
  </verify>
  <acceptance_criteria>
    - The expected row count is stated in the evidence file before the board is recorded.
    - At least six rows are present — four test-matrix cells, the hook job and the experiments job — and every row is successful.
    - Every row's name, state, bucket and link is recorded verbatim.
    - A zero-row board is ruled not-green and halts, and the evidence file records that this rule is a planner assumption rather than a measured behaviour.
    - The evidence file records that the default branch is unprotected and carries no ruleset, framed as context and explicitly not as permission to merge red.
    - Nothing was merged, approved, re-run or cancelled.
  </acceptance_criteria>
  <done>The author has a green board with one row per job and a link behind each, and the phase's last automatically-establishable fact is established.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated formatting hooks -> committed provenance bytes | this is the first whole-tree execution of hooks with write access over the run artifacts |
| local machine -> the repository host | the pull request is created with a credential that also has merge rights |
| CI check board -> the author's merge decision | the board is the evidence the irreversible merge rests on, so a mis-read board is a mis-made decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.2-14 | Tampering | the whole-tree hook sweep over committed artifacts | high | mitigate | Four independent assertions are taken with a before-and-after baseline: exit code, unchanged working-tree status, both md5 anchors, and the driver-state field count. A rewrite that reported success is caught by assertions two through four. The sweep is prohibited from running before the exclude edits land, and the precondition halts on a dirty tree so the status assertion means what it says. |
| T-29.2-15 | Elevation of Privilege | the repository credential during pull-request creation | high | mitigate | The creation command never merges. The plan explicitly prohibits merging, auto-merge, approval and any review request that would auto-merge, and Task 2's verify asserts the pull request is in the open, unmerged state rather than trusting that the prohibition held. |
| T-29.2-16 | Spoofing | a check board that reports no checks | medium | mitigate | A zero-row board is ruled not-green and halts. It is the one case where an absent signal could be mistaken for an unproblematic one, and no source artifact specifies it, so it is handled conservatively and the assumption is recorded as an assumption. |
| T-29.2-17 | Repudiation | reporting a green board without the rows behind it | medium | mitigate | Every row's name, state, bucket and link is recorded verbatim in the evidence file, and the expected row count is stated before the board is read so a missing job is visible as a shortfall rather than invisible as a silence. |
| T-29.2-18 | Elevation of Privilege | an unprotected default branch | low | accept | Neither classic protection nor a ruleset exists, so red checks do not mechanically block a merge. Accepted as the repository's standing configuration; changing it is out of this phase's scope. Compensating control: the three repairs exist so the board goes green on its merits, the fact is recorded as context rather than as permission, and the merge itself is gated behind a decision checkpoint in plan 29.2-05. |
| T-29.2-SC | Tampering | unpinned install of the hook runner | medium | accept | Task 1 deliberately installs it unpinned into an isolated environment because that is exactly what CI does, and a pinned local install would prove something CI does not run. The resolved version is recorded. RESEARCH § *Package Legitimacy Audit* returns no `[ASSUMED]`, `[SUS]` or `[SLOP]` verdicts for this phase and adds no new package to the project. The hook repository revisions themselves are pinned in the configuration and unchanged. |
</threat_model>

<verification>
- The whole-tree sweep exited zero; the working-tree status is unchanged and empty; both md5 anchors match their literals; the driver-state field count is the single value five.
- The evidence file records all seven per-hook lines, the resolved hook-runner version, the never-run-before statement, a before-and-after block, a prohibitions-honoured block and a `Scope:` paragraph.
- Exactly one open, unmerged pull request exists from the results branch into the default branch, and its body carries the merge-commit and decline-deletion instructions.
- The check board reports at least six rows, all successful, each recorded with its link.
- Nothing was merged, approved, auto-merged, re-run or cancelled.
</verification>

<success_criteria>
ROADMAP criterion 1 is proved locally against four independent invariants and then confirmed on the
real runners, and the pull request the author will merge exists with a green board and the two
instructions that cannot be got wrong stated in its own body.
</success_criteria>

<output>
Create `.planning/phases/29.2-merge-release-and-publish/29.2-04-SUMMARY.md` when done.
</output>
