---
phase: 27-frozen-single-sha-handoff-package
plan: 08
type: execute
wave: 2
depends_on: ["27-01", "27-02", "27-03", "27-05"]
files_modified:
  - experiments/configs/e2_release_linux.yaml
  - experiments/run_experiment_suite.sh
  - experiments/suite_expectations.json
  - experiments/EXPECTATIONS.md
  - tests/unit/test_run_experiment_suite_dryrun.py
  - tests/unit/test_e2_release_config.py
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "The exact E2 inputs the frozen run uses are committed inside the frozen sha, not left on a Desktop outside it"
    - "Neither Windows literal is reachable as a default: GATE_PYTHON detects then falls back, and E2_RELEASE_CONFIG defaults to the in-repo Linux config"
    - "The resolved gate interpreter is printed loudly on success, not only on failure"
    - "A BLAS thread cap is exported for concurrent stages only; the four serial_alone timing stages see no cap variable at all"
    - "The run emits an environment lockfile artifact beside the run manifest"
  artifacts:
    - path: "experiments/configs/e2_release_linux.yaml"
      provides: "the frozen E2 image-set release config, frame_step 1, max_calibration_frames 200, 13 extrinsic paths"
      contains: "frame_step: 1"
    - path: "experiments/run_experiment_suite.sh"
      provides: "portable interpreter/config resolution, the two-regime thread pin, and the lockfile call site"
      contains: "SUITE_THREAD_CAP"
  key_links:
    - from: "E2_RELEASE_CONFIG default"
      to: "experiments/configs/e2_release_linux.yaml"
      via: "in-repo default with SUITE_E2_RELEASE_CONFIG as the repointer"
      pattern: "configs/e2_release_linux.yaml"
    - from: "run_one_stage's dispatch"
      to: "STAGE_CONCURRENCY[name]"
      via: "cap exported only when the stage is concurrent"
      pattern: "STAGE_CONCURRENCY"
    - from: "run_stage_preflight"
      to: "python -m experiments._env_lock --out ${OUT_DIR}"
      via: "artifact write beside the run manifest, non-fatal"
      pattern: "_env_lock"
---

<objective>
Make the driver portable to the Linux target and put E2's exact inputs inside the frozen sha.

**D-11** — the current off-repo config at
`C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/config.yaml` has **`frame_step: 30`**,
while the manifest's verified signature is *"13 × 262 extrinsic frames at `frame_step: 1` /
`max_calibration_frames: 200`"*. The Desktop config is **not** the one that produces the
signature pre-flight asserts. Committing the target's config puts the exact inputs inside the
artifact instead of leaving provenance outside it (the F-001 shape).

**D-12** — both Windows literals become detect-then-fallback, with a loud resolution log.

**D-14** — pin BLAS threads for the CONCURRENT stages only. The four `serial_alone` timing
stages stay at the library default because every historical measurement was taken unpinned;
pinning them would silently change what is being timed.

**D-13's driver half** — the lockfile emitter gets its call site beside the run-manifest write.

Purpose: nothing on the target should need an edit after transfer (criterion 4).
Output: one committed config, three driver edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-frozen-single-sha-handoff-package/27-CONTEXT.md
@.planning/phases/27-frozen-single-sha-handoff-package/27-PATTERNS.md
@.planning/phases/27-frozen-single-sha-handoff-package/27-TARGET-FACTS.md
@aquacal_data/real-rig/real-rig/config_paper.yaml
@experiments/run_experiment_suite.sh
</context>

<interfaces>
- `emit_invocation_configs(source_path, target_dir)` — `experiments/e2_real_rig.py:1126`. Raises
  `ValueError` if `target_dir` resolves to, or inside, `source_path.parent`. With the config at
  `experiments/configs/`, the default `E2_INVOCATION_DIR` (`experiments/results_e2_invocations`)
  is **not** inside it, so the refusal does not fire — assert this, do not assume it.
- `build_internals_variant_config(source_text, output_dir, overrides)` —
  `experiments/e2_real_rig.py:1029`. It **requires a top-level `paths.output_dir` line** (raises
  otherwise) and it **creates the `internals:` block if the source has none**
  (`:1066-1071`). `config_paper.yaml` has `paths.output_dir` and no `internals` block.
- `E2_INVOCATION_VARIANTS` — `experiments/e2_real_rig.py:930-970`: the classification variant
  sets `internals.log_all_observation_depths: True`; timing and memory set it False.
- `_load_stage_attributes` — `run_experiment_suite.sh:1884-1934`: reads `depends_on`,
  `concurrency`, `frame_class`, `est_hours` from `suite_expectations.json` via a one-shot Python
  call emitting TSV into `while IFS=$'\t' read`, and **fatals** on a stage missing from the
  manifest. `STAGE_CONCURRENCY[name]` is the array the pool already consults (`:2008`, `:2040`).
- `run_one_stage` — `:1808-1874`. The dispatch line `"run_stage_${name}"` at `:1831` is the ONE
  place both the serial path and the pool path pass through.
- `SUITE_THREAD_CAP` — the environment variable plan 27-05's manifest record reads. This plan
  exports it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Commit the Linux E2 release config (D-11)</name>
  <files>experiments/configs/e2_release_linux.yaml, tests/unit/test_e2_release_config.py</files>
  <read_first>
    - `aquacal_data/real-rig/real-rig/config_paper.yaml` in full — the exact structural analog: it
      already points at **image directories**, already sets `frame_step: 1` and
      `max_calibration_frames: 200`, and already declares 13 extrinsic paths (12 main + 1
      auxiliary), which is exactly `cheap_check.n_extrinsic_videos: 13`. Its header comment is
      also the right precedent: it states the frame-step reasoning explicitly.
    - `.planning/phases/27-frozen-single-sha-handoff-package/27-TARGET-FACTS.md` — the measured
      image-set root and the 13 per-camera directory names on the target.
    - `experiments/e2_real_rig.py:1029-1110` (`build_internals_variant_config`) and `:1126-1200`
      (`emit_invocation_configs`).
    - `experiments/run_experiment_suite.sh:913-1010` — `_preflight_frameset`, whose probe
      resolves each declared path with a bare `pathlib.Path(p)` **against the process cwd**, and
      the driver runs from the repo root.
  </read_first>
  <action>
Create `experiments/configs/e2_release_linux.yaml`, copying `config_paper.yaml`'s structure and
answering the three questions the analog does not:

  1. **Path absoluteness.** `config_paper.yaml` uses paths relative to the archive root because
     `aquacal` is launched from there. The driver runs from the repo root and the image set lives
     elsewhere on the target, and the pre-flight probe resolves declared paths against the
     process cwd. Use **absolute target paths**, taken verbatim from `27-TARGET-FACTS.md`, for
     all 13 `intrinsic_videos` and all 13 `extrinsic_videos` entries. State this choice in the
     file's header comment along with the cwd reasoning.
  2. **Where it lives.** `experiments/configs/` — a new directory. This is not cosmetic: with the
     source config at `experiments/configs/`, `emit_invocation_configs`' release-tree write
     refusal does not fire against the default `experiments/results_e2_invocations` target,
     whereas placing the config at `experiments/` directly **would** make that target a child of
     the source directory and fire it. Record that reasoning in the header comment.
  3. **The `internals` block.** `build_internals_variant_config` inserts an `internals:` block
     when the source has none, and the three variants set `log_all_observation_depths`
     themselves — so no base block is required. Verify that by running the emitter (Task 1's
     test), not by assuming it. What IS required is a top-level `paths.output_dir` line; keep it.

Set `detection.frame_step: 1` and `optimization.max_calibration_frames: 200` (D-11), and carry
`config_paper.yaml`'s header explanation of why `frame_step` is 1 over already-subsampled images.
Note in the header that the Desktop config's `frame_step: 30` is a different configuration and is
**not** the one that produces the signature pre-flight asserts.

Write `tests/unit/test_e2_release_config.py` asserting: the file parses as YAML; exactly 13
`extrinsic_videos` and 13 `intrinsic_videos` entries; `frame_step == 1`;
`max_calibration_frames == 200`; a `paths.output_dir` key exists; and
`emit_invocation_configs(<this config>, tmp_path)` returns three paths without raising, with the
classification variant carrying `log_all_observation_depths: true`. Use `tmp_path` as the target
so nothing is written into a results tree.
  </action>
  <acceptance_criteria>
    - `python -c "import yaml;d=yaml.safe_load(open('experiments/configs/e2_release_linux.yaml'));print(len(d['paths']['extrinsic_videos']), len(d['paths']['intrinsic_videos']), d['detection']['frame_step'], d['optimization']['max_calibration_frames'])"` prints `13 13 1 200`.
    - `python -m pytest tests/unit/test_e2_release_config.py -q` exits 0, including the
      `emit_invocation_configs` case.
    - The header comment states all three decisions (absolute paths + cwd, directory choice +
      the write-refusal reason, the internals finding).
    - Every declared path is absolute and matches `27-TARGET-FACTS.md` verbatim.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unit/test_e2_release_config.py -q</automated>
  </verify>
  <done>The exact inputs of the frozen E2 run are committed, and the invocation-config generator accepts them.</done>
</task>

<task type="auto">
  <name>Task 2: Both Windows literals become detect-then-fallback (D-12)</name>
  <files>experiments/run_experiment_suite.sh, tests/unit/test_run_experiment_suite_dryrun.py</files>
  <read_first>
    - `experiments/run_experiment_suite.sh:369-380` — `E2_RELEASE_CONFIG` and the comment that
      already names Phase 27 as the repointer, plus `BASELINE_DIR` at `:392` as the sibling
      precedent for "default is in-repo, env var repoints".
    - `:396-410` — `GATE_PYTHON` and its EXISTING detect-then-fallback block. D-12 widens this
      chain; it does not invent one. Copy the shape exactly: env override first, probe with
      `[ -x ]` **and** `command -v`, warn to stderr naming what was tried, then degrade.
    - `.planning/phases/27-frozen-single-sha-handoff-package/27-TARGET-FACTS.md` § Interpreter —
      what the middle rung will actually resolve to on the target.
    - `:1840-1860` — the D-50 override-flag restatement, and the standing rule that **no new
      pre-flight refusal is added** (§ D cut three; do not add a fourth).
  </read_first>
  <action>
`GATE_PYTHON`: make the chain three rungs — (1) the `PRELAUNCH_GATE_PYTHON` env override;
(2) a conda env named `AquaCal` on **either** platform, i.e. both the Windows
`.../envs/AquaCal/python.exe` form and the POSIX `.../envs/AquaCal/bin/python` form, probed
under the conda roots the target actually has (see `27-TARGET-FACTS.md`); (3) bare `python`.
Print the resolution **loudly on success too**, not only on failure — one line naming which rung
won and the absolute path it resolved to. Keep the existing stderr WARNING for the degrade case,
including its note that gate verdicts may fail to import pandas.

`E2_RELEASE_CONFIG`: swap only the default, to `experiments/configs/e2_release_linux.yaml`. Keep
the `SUITE_E2_RELEASE_CONFIG` variable name — it is already threaded into the pre-flight
heredoc's env at `:928`. Update the surrounding comment: the Desktop path is now reachable only
via that variable.

**State the consequence in the comment, because it is load-bearing for plan 27-10:** on the
Windows box the new default's absolute target paths do not resolve, so `_preflight_frameset`
reports ABSENT. A local run on Windows must therefore pass
`SUITE_E2_RELEASE_CONFIG=C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/config.yaml`
(or `--skip-e2`, which DECLARES a synthetic-only run). That is exactly the escape hatch D-12
preserves — the Git Bash box stays the place defects get diagnosed. Add **no** new refusal and
**no** sixth override flag.

Add dry-run tests: the resolution line appears in stdout on a successful resolve; the
`E2_RELEASE_CONFIG` default is the in-repo path when `SUITE_E2_RELEASE_CONFIG` is unset, and the
override wins when it is set.
  </action>
  <acceptance_criteria>
    - `bash -n experiments/run_experiment_suite.sh` exits 0.
    - `grep -c 'Desktop/Aqua' experiments/run_experiment_suite.sh` returns 0 outside comments:
      `grep -v '^\s*#' experiments/run_experiment_suite.sh | grep -c 'Desktop/Aqua'` returns 0.
    - `grep -v '^\s*#' experiments/run_experiment_suite.sh | grep -c 'configs/e2_release_linux.yaml'` returns ≥ 1.
    - `grep -c 'envs/AquaCal/bin/python' experiments/run_experiment_suite.sh` returns ≥ 1.
    - The manifest's `preflight.overrides` still has exactly five entries.
    - `python -m pytest tests/unit/test_run_experiment_suite_dryrun.py -k "gate_python or release_config or resolution" -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n experiments/run_experiment_suite.sh &amp;&amp; python -m pytest tests/unit/test_run_experiment_suite_dryrun.py -k "gate_python or release_config or resolution" -q</automated>
  </verify>
  <done>Neither Windows literal is a default; both resolutions are logged; the Windows escape hatch is documented in the script.</done>
</task>

<task type="auto">
  <name>Task 3: Two-regime thread pin and the lockfile call site (D-14, D-13)</name>
  <files>experiments/run_experiment_suite.sh, experiments/suite_expectations.json, experiments/EXPECTATIONS.md, tests/unit/test_run_experiment_suite_dryrun.py</files>
  <read_first>
    - `experiments/run_experiment_suite.sh:1808-1874` (`run_one_stage`, dispatch at `:1831`) and
      `:1884-1934` (`_load_stage_attributes`).
    - `:864-875` — `run_stage_preflight`'s run-manifest call site. **Copy the invocation shape,
      not the hard-abort half**: the lockfile is an artifact, not a refusal.
    - `.planning/probes/2026-08-18-solver-concurrency/FINDINGS.md` — median 0.99 cores of 20,
      mean 1.20, p95 2.01, peak 2.56.
    - `.planning/phases/27-frozen-single-sha-handoff-package/27-CONTEXT.md` § D-14, § D-15.
    - `tests/unit/test_run_experiment_suite_dryrun.py:619-770` — `TestConcurrencyConstraints`,
      and `run_driver`'s `stub=` parameter, which exists for exactly this kind of assertion.
  </read_first>
  <action>
Wrap the dispatch line in `run_one_stage` so that, and **only** when
`STAGE_CONCURRENCY[${name}] == "concurrent"`, the stage's child process sees
`OMP_NUM_THREADS`, `MKL_NUM_THREADS` and `OPENBLAS_NUM_THREADS` set to the cap. Use a subshell
or an explicit `unset` so the four `serial_alone` stages (`e4`, `e4_repeat`, `e2_timing`,
`e2_memory`) see **no** such variable at all — an exported empty string is not the same as unset
to OpenBLAS, and that distinction is the whole point.

Set the cap to **2**, and state the derivation in the comment: the probe measured a solve
holding a median 0.99 cores of 20 with p95 2.01, so 2 costs nothing where 4–5 processes compete.
Export it as `SUITE_THREAD_CAP` (the contract plan 27-05's manifest record reads), overridable
by the environment, so the value the manifest records and the value enforced come from one place.

Do not hardcode a second stage list anywhere — read the split from `STAGE_CONCURRENCY`, which
`_load_stage_attributes` already populates from `suite_expectations.json`.

Then add the lockfile call site next to the run-manifest write in `run_stage_preflight`:
`"${GATE_PYTHON}" -m experiments._env_lock --out "${OUT_DIR}" --force`, logging the outcome. A
non-zero exit is **logged and the run continues** — no refusal, no override flag (§ D cut three,
P26-D-50 binds every survivor, and D-12 says do not add a fourth).

Register `environment_lock.txt` in `experiments/suite_expectations.json` as an artifact of the
`preflight` stage under both profiles, with a rationale naming D-13. **A manifest edit is a
three-file change:** run `python -m experiments.render_expectation_sheet --write` then
`--check`, and run the sheet test.

If `27-TARGET-FACTS.md` recorded that the measured frameset does **not** clear
`min_total_bytes: 4000000000`, re-derive that number from the measurement in the same manifest
edit and update `min_total_bytes_rationale` to say what it now discriminates and what it was
measured against. A changed number without a changed rationale is the FIX-06 shape. If the floor
was cleared, change nothing and say so in the SUMMARY.

Add dry-run tests: a stub that echoes `OMP_NUM_THREADS` shows the cap for a concurrent stage and
shows it **unset** for a `serial_alone` stage; the lockfile artifact appears in the sandboxed
out dir after pre-flight.
  </action>
  <acceptance_criteria>
    - `bash -n experiments/run_experiment_suite.sh` exits 0.
    - `grep -c 'SUITE_THREAD_CAP' experiments/run_experiment_suite.sh` returns ≥ 1.
    - `grep -c '_env_lock' experiments/run_experiment_suite.sh` returns ≥ 1.
    - A dry-run test asserts `OMP_NUM_THREADS` is set for a concurrent stage and **absent** (not
      empty) for a `serial_alone` stage, and it passes.
    - `python -m experiments.render_expectation_sheet --check` exits 0 and
      `python -m pytest tests/unit/test_expectations.py -q` exits 0.
    - `python -m pytest tests/unit/test_run_experiment_suite_dryrun.py -k "Concurrency or thread or env_lock" -q` exits 0.
    - `tests/unit/test_expectations.py::test_serial_alone_is_exactly_the_four_timing_stages`
      still passes — the four unpinned stages are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>bash -n experiments/run_experiment_suite.sh &amp;&amp; python -m experiments.render_expectation_sheet --check &amp;&amp; python -m pytest tests/unit/test_run_experiment_suite_dryrun.py -k "Concurrency or thread or env_lock" tests/unit/test_expectations.py -q</automated>
  </verify>
  <done>Concurrent stages run capped, the four timing stages run unpinned, and the lockfile is written as an artifact that can never refuse a run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed config → public GitHub | the config carries absolute filesystem paths from the target machine |
| environment variables → child processes | the driver now exports thread caps into stage subprocesses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08-01 | Information disclosure | `experiments/configs/e2_release_linux.yaml` | mitigate | The config's 26 absolute paths become public at plan 27-11's push. If any contains a personal identifier (a home directory with a username), flag it in the SUMMARY so plan 27-10's pre-push audit puts it in front of the author for an explicit decision. Never invent a sanitised path that then fails to resolve on the target. |
| T-27-08-02 | Tampering | timing measurements | mitigate | The cap is exported only where `STAGE_CONCURRENCY == "concurrent"`, and a dry-run test asserts the variable is ABSENT (not empty) for the four `serial_alone` stages. Pinning a timing stage would silently change what is being timed — the record of both regimes lives in the run manifest (plan 27-05). |
| T-27-08-03 | Denial of service | lockfile emission inside pre-flight | mitigate | A non-zero exit is logged and the run continues. No fourth pre-flight refusal, no sixth override flag; `preflight.overrides` stays at five entries. |
| T-27-08-04 | Spoofing | interpreter resolution | mitigate | The three-rung chain probes with `[ -x ]` and `command -v` and prints the absolute path it resolved on success, so a run against the wrong interpreter is visible in the log rather than inferred from a failure. |
</threat_model>

<verification>
**Targeted test command for this plan:**
`python -m pytest tests/unit/test_e2_release_config.py tests/unit/test_expectations.py tests/unit/test_run_experiment_suite_dryrun.py -q`

If the dry-run file approaches the tool ceiling, narrow with
`-k "Concurrency or Preflight or gate_python or release_config or thread or env_lock"` and say so
in the SUMMARY.

**Do NOT run the full test suite** — the orchestrator runs `pytest tests/` at the post-merge
gate. **Do NOT run `run_experiment_suite.sh --smoke`**: ~11 minutes, over the ceiling, and plan
27-10 owns that pass. **Do NOT run anything over SSH** — plan 27-01 already measured the target
and plan 27-12 owns everything that executes there. **Do NOT re-tune `SUITE_WORKERS`** (D-15:
confirm, do not re-tune). **Do NOT touch `experiments/check_rerun_gates.py`,
`experiments/reconstruction_bootstrap.py`, `pyproject.toml` or `requirements.txt`.**

Use the AquaCal conda interpreter for pytest.
</verification>

<success_criteria>
- `experiments/configs/e2_release_linux.yaml` exists, parses, declares 13+13 absolute paths,
  `frame_step: 1`, `max_calibration_frames: 200`, and feeds `emit_invocation_configs` without
  raising.
- No Windows literal is reachable as a default; both resolutions are logged.
- The cap reaches concurrent stages only; the four timing stages see no cap variable.
- `environment_lock.txt` is registered in the manifest and the sheet is regenerated.
- `bash -n` clean; targeted tests green.
</success_criteria>

<output>
Create `.planning/phases/27-frozen-single-sha-handoff-package/27-08-SUMMARY.md` when done.
One commit per decision: D-11, D-12, then D-14/D-13's driver half.
</output>
