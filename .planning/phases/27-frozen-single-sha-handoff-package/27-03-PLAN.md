---
phase: 27-frozen-single-sha-handoff-package
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/suite_expectations.json
  - experiments/EXPECTATIONS.md
  - experiments/check_rerun_gates.py
  - tests/unit/test_expectations.py
  - tests/unit/test_rerun_gates.py
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "The gate roll-up over the preserved smoke tree at profile 'smoke' reports 0 FAIL"
    - "No assertion was deleted to achieve that: every gate that fires at 'full' still fires at 'full'"
    - "Phase 25's per-observation classification table and E2 h_q logging flag are confirmed registered in the driver's manifest, with a written finding"
  artifacts:
    - path: "experiments/suite_expectations.json"
      provides: "smoke profile tags that match what the smoke code paths actually write"
      contains: "rows_rationale"
    - path: "experiments/EXPECTATIONS.md"
      provides: "the regenerated §7 sheet matching the edited manifest"
    - path: "experiments/check_rerun_gates.py"
      provides: "profile-aware E4/E5/E6 checkers"
      contains: "profile"
  key_links:
    - from: "run_all_gates"
      to: "check_e4 / check_e5 / check_e6 / check_e6_seed_band"
      via: "profile keyword threaded through"
      pattern: "profile=profile"
    - from: "experiments/suite_expectations.json"
      to: "experiments/EXPECTATIONS.md §7"
      via: "python -m experiments.render_expectation_sheet --write"
      pattern: "render_expectation_sheet"
---

<objective>
Make the whole `smoke` profile truthful so a smoke pass can exit 0 (D-20).

This is not tidiness. D-05 makes the on-target `--smoke` run the venue that catches a
Linux-specific failure — and with 12 known reds, one new Linux red is invisible in the noise.

**The 12 failures are exactly reproducible today**, over the preserved 26-10 smoke tree (see
`<preconditions>` — it is gitignored, not committed):

    check_rerun_gates.py experiments/results_smoke --profile smoke
    -> TOTAL: 71 PASS, 9 N/A, 12 FAIL

and they decompose into three classes, all pre-existing, none a driver defect:

| n | Verdict | Source | Remedy |
|---|---------|--------|--------|
| 3 | `completeness:` on `structural_scaling.csv`, `e5_provenance.json`, `fd_jacobian_accuracy.json` | the manifest tags them `["smoke","full"]` but the smoke code paths never write them | retag `full`-only |
| 2 | `E5 gate1_guard_count` + `gate3_provenance` on `e5_provenance.json not found` | `check_e5` is not profile-aware | absence at smoke becomes `N/A` |
| 2 | `E4 gate1_guard_count` + `gate2_status` on `benchmark_grid.csv not found` | `check_e4` is not profile-aware (**the manifest entry is ALREADY `full`-only — D-20 class 3 is satisfied at the manifest and the residual FAILs come from the per-experiment checker**) | absence at smoke becomes `N/A` |
| 4 | `E6 gate4_optimality` on four `e6_configs/*.json` | collapsed smoke solve | `check_optimality=(profile != "smoke")` |
| 1 | `E6 gate_e6_seed_band:cameras_axis missing [12,16]` | collapsed smoke axis | expectation becomes profile-dependent |

**Constraint that governs every edit here:** do not weaken a gate to make it pass. P26-D-49
already says smoke asserts EXISTENCE only; this makes the manifest and the checkers match that
rule. Nothing that fires at `full` may stop firing at `full`.

Purpose: give plan 27-12's on-target run a usable signal.
Output: a truthful smoke profile, plus the D-24 confirmation that Phase 25's outputs are
registered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-frozen-single-sha-handoff-package/27-CONTEXT.md
@.planning/phases/27-frozen-single-sha-handoff-package/27-PATTERNS.md
@.planning/phases/26-full-suite-driver-handoff-readiness/26-10-SUMMARY.md
@experiments/suite_expectations.json
@experiments/_expectations.py
</context>

<preconditions>
**`experiments/results_smoke/` is GITIGNORED** (`.gitignore:430`) and exists only in the main
working tree at `C:/Users/tucke/PycharmProjects/AquaCal/experiments/results_smoke`. It is the
preserved 26-10 acceptance-pass output and it is this plan's cheap, deterministic test fixture.

If you are executing in a git worktree, that directory will NOT be present in your checkout —
point the gate at the absolute path above instead. The gate only reads it; never write into it,
never re-run the suite to regenerate it.
</preconditions>

<interfaces>
Contracts the executor needs; do not go looking for them.

- `GateResult(experiment, gate, verdict, detail)` — `verdict` is `"PASS" | "FAIL" | "N/A"`
  (`check_rerun_gates.py:92-107`). `"N/A"` is the established verdict for *"this artifact's stage
  has not run at production shape"* — see `check_e6_seed_band:1000-1008`.
- `run_all_gates(out_dir: Path, *, stage: str | None = None, profile: str | None = None)`
  (`:1978`). `profile` is currently threaded ONLY into `check_completeness`; every other checker
  is called positionally with `out_dir` alone.
- `_check_json_artifact(experiment, label, record, *, check_guard, check_optimality,
  require_water_index, water_index_keys=...)` (`:420`) — `check_optimality` is already the seam;
  it needs no change.
- `check_completeness(out_dir, *, profile, stage)` in `experiments/_expectations.py:153-197` —
  the only profile-aware checker today, and the shape to imitate: `profile` is **keyword-only**
  and validated against the module-level `PROFILES` tuple, raising `ValueError` naming the
  offender.
- `PROFILES` — the tuple of legal profile names; `profile=None` means "no profile selected" and
  must keep every checker at its pre-existing, strictest behaviour.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Retag the three smoke-unwritable artifacts and regenerate the sheet (D-20 class 1)</name>
  <files>experiments/suite_expectations.json, experiments/EXPECTATIONS.md, tests/unit/test_expectations.py</files>
  <read_first>
    - `experiments/suite_expectations.json` — the `artifacts` list, specifically the
      `structural_scaling.csv`, `e5_provenance.json`, `fd_jacobian_accuracy.json` and
      `benchmark_grid.csv` entries. Note `benchmark_grid.csv` is **already** `["full"]` and
      carries its reason in `rows_rationale` — that is the house style to copy.
    - `experiments/EXPECTATIONS.md` — its header, which states that §7 is a generated region and
      that `tests/unit/test_expectations.py -k sheet` fails when it drifts.
    - `experiments/e3_derived_quantities.py:1106-1126` — the smoke branch that returns before
      `_write_tier4`, i.e. the code location that is `structural_scaling.csv`'s reason.
    - `experiments/_expectations.py:191-196` (the profile filter) and `:212-222` (the
      `conditional` PASS path).
    - `tests/unit/test_expectations.py:195-263` — `TestProfiles`.
  </read_first>
  <action>
Change `"profiles": ["smoke","full"]` to `["full"]` on exactly three artifact entries:
`structural_scaling.csv` (stage `e3`), `e5_provenance.json` (stage `e5`),
`fd_jacobian_accuracy.json` (stage `fd_jacobian`). Touch no other entry.

Every number and tag in this manifest carries its own rationale field — a changed tag without a
changed reason is the FIX-06 shape. Add or extend a rationale on each of the three naming the
code location that makes it `full`-only:
`e3_derived_quantities.py:1106-1126` returns before `_write_tier4`; the `_run_smoke` paths of
`e5_index_sensitivity.py` and `fd_jacobian_accuracy.py` return before their sidecar writes.
Note that `fd_jacobian_accuracy.csv` IS written under smoke and stays `["smoke","full"]` — only
the `.json` sidecar moves.

Leave `benchmark_grid.csv` alone: it is already `["full"]`. Record that as a finding — D-20's
class 3 is satisfied at the manifest, and its two residual FAILs come from `check_e4`, which
Task 2 fixes.

Then, mandatorily:

    python -m experiments.render_expectation_sheet --write
    python -m experiments.render_expectation_sheet --check

The rendered §7 counts move (the "expected under the `full` profile" and smoke tallies). Add or
extend a `TestProfiles` case asserting these three artifacts are `full`-only for the stated
reason, so the retag cannot silently revert.
  </action>
  <acceptance_criteria>
    - `python -c "import json;d=json.load(open('experiments/suite_expectations.json'));print(sorted(a['name'] for a in d['artifacts'] if a['profiles']==['smoke','full'] and a['name'] in ('structural_scaling.csv','e5_provenance.json','fd_jacobian_accuracy.json')))"` prints `[]`.
    - `python -m experiments.render_expectation_sheet --check` exits 0.
    - `python -m pytest tests/unit/test_expectations.py -q` exits 0, including `-k sheet`.
    - Each of the three edited entries has a rationale string naming a `file:line`.
    - The manifest still lists 62 artifacts and the five `preflight.overrides`.
  </acceptance_criteria>
  <verify>
    <automated>python -m experiments.render_expectation_sheet --check &amp;&amp; python -m pytest tests/unit/test_expectations.py -q</automated>
  </verify>
  <done>Three retags landed with reasons, the sheet is regenerated, the sheet test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread profile into the E4, E5 and E6 checkers (D-20 classes 2 and 3)</name>
  <files>experiments/check_rerun_gates.py, tests/unit/test_rerun_gates.py</files>
  <read_first>
    - `experiments/check_rerun_gates.py:690-720` — `check_e4`, whose `_check_guard_column` /
      `_check_status_column` on a `None` dataframe produce the two `benchmark_grid.csv not found`
      FAILs.
    - `:722-755` — `check_e5`, whose `_check_json_artifact` on a missing `e5_provenance.json`
      produces `gate1_guard_count` + `gate3_provenance` FAILs, and which already demonstrates the
      `N/A` idiom twice in the same function.
    - `:758-789` — `check_e6` and the `check_optimality=True` seam.
    - `:988-1010` — `check_e6_seed_band`'s existing `N/A`-on-absent-CSV precedent.
    - `:1075-1110` — the `cameras_axis` block and `_E6_EXPECTED_CAMERA_VALUES`, with its comment
      `# NOT derived from the manifest and NOT changed: the 'cameras' axis survives D-40.`
    - `:1978-2044` — `run_all_gates` and its docstring.
    - `experiments/_expectations.py:153-197` — the keyword-only, `PROFILES`-validated shape.
    - `tests/unit/test_rerun_gates.py:550` (`TestGate4Optimality`) and `:1067`
      (`TestCheckE6SeedBand`).
  </read_first>
  <behavior>
    - `profile="smoke"`, `benchmark_grid.csv` absent → E4 emits `N/A` (not FAIL) for the two
      column gates, with a detail naming the collapsed smoke path.
    - `profile="smoke"`, `e5_provenance.json` absent → E5 emits `N/A` for `gate1_guard_count`
      and `gate3_provenance`.
    - `profile="smoke"`, an `e6_configs/*.json` with null optimality → no `gate4_optimality`
      FAIL; the guard and provenance gates on the same record still run.
    - `profile="smoke"`, band CSV with only the `cameras=8` axis value → `cameras_axis` is
      `PASS` or `N/A` naming the collapsed scale, never FAIL.
    - `profile="full"` (and `profile=None`) → every one of the above is FAIL exactly as today.
      This is the regression that matters most: assert it explicitly.
    - An unknown profile string raises `ValueError` naming the offender.
  </behavior>
  <action>
Add a keyword-only `profile: str | None = None` parameter to `check_e4`, `check_e5`, `check_e6`
and `check_e6_seed_band`, validated against `PROFILES` the way `check_completeness` does. Thread
it from `run_all_gates`, which already accepts it. Update `run_all_gates`'s docstring: `profile`
no longer only selects the completeness gate.

Four call-site changes:

  - `check_e6`: `check_optimality=(profile != "smoke")` on the `_check_json_artifact` call for
    `e6_configs/*.json`. When suppressed, still emit a `GateResult` for `gate4_optimality` with
    verdict `N/A` and a detail naming the reason (a collapsed smoke solve records no meaningful
    optimality) — a suppressed gate must remain visible in the verdict block, never vanish.
  - `check_e6_seed_band`: when `profile == "smoke"` and the `cameras` axis is present but does
    not carry all of `_E6_EXPECTED_CAMERA_VALUES`, emit `N/A` naming the collapsed axis instead
    of FAIL. **Do not change `_E6_EXPECTED_CAMERA_VALUES`** — the constant carries the comment
    saying it survives P26-D-40; make the expectation profile-dependent, not the constant. A
    missing `axis`/`axis_value` column pair stays FAIL at every profile.
  - `check_e4`: when `profile == "smoke"` and `benchmark_grid.csv` is absent, emit `N/A` for the
    two column gates. A present-but-malformed grid still FAILs at every profile.
  - `check_e5`: when `profile == "smoke"` and `e5_provenance.json` is absent, emit `N/A` for
    `gate1_guard_count` and `gate3_provenance`. A present-but-bad record still FAILs.

Keep emitting `GateResult`s throughout so `main()`'s verdict-block formatting is unchanged; do
not introduce a second result type (the late-import dance in `_expectations.py:50-61` exists
precisely to avoid that). `run_gate_check` in the driver always returns 0 and the caller reads
`LAST_GATE_EXIT` — do not touch that contract.

Add tests to `tests/unit/test_rerun_gates.py` covering every case in `&lt;behavior&gt;`, including
the `profile="full"` / `profile=None` regressions.
  </action>
  <acceptance_criteria>
    - `python experiments/check_rerun_gates.py experiments/results_smoke --profile smoke` prints
      a line matching `TOTAL: .* 0 FAIL` and exits 0.
    - The same command **without** `--profile` still reports the pre-existing FAILs (profile-free
      behaviour is unchanged) — record the before/after counts in the SUMMARY.
    - `grep -c '_E6_EXPECTED_CAMERA_VALUES = (8, 12, 16)' experiments/check_rerun_gates.py`
      returns 1 (the constant is untouched).
    - `python -m pytest tests/unit/test_rerun_gates.py -q` exits 0.
    - No `GateResult` construction was deleted: every gate name present in the pre-edit verdict
      block is still present in the post-edit one (compare the two `[VERDICT] EXP gate` name
      sets; only verdicts may change).
  </acceptance_criteria>
  <verify>
    <automated>python experiments/check_rerun_gates.py experiments/results_smoke --profile smoke &amp;&amp; python -m pytest tests/unit/test_rerun_gates.py -q</automated>
  </verify>
  <done>The smoke roll-up over the preserved tree is 0 FAIL, with the same gate names present and nothing weakened at `full`.</done>
</task>

<task type="auto">
  <name>Task 3: Verify and record Phase 25's driver registration (D-24, ROADMAP criterion 6)</name>
  <files>.planning/phases/27-frozen-single-sha-handoff-package/27-03-SUMMARY.md</files>
  <read_first>
    - `experiments/suite_expectations.json` — the `degenerate_observations.csv` and
      `all_observation_depths.csv` entries, and the six
      `e{1,5,7}[_seed_band]_degeneracy_breakdown.json` sidecars.
    - `experiments/_expectations.py:212-222` — the `conditional` flag's PASS path.
    - `.planning/phases/25-*/25-CONTEXT.md` § D-08/D-09/D-10 — the conditional-emission rules
      those entries must honour.
    - `src/aquacal/config/schema.py` — the `internals.log_all_observation_depths` flag, and
      `experiments/e2_real_rig.py:930-970` (`E2_INVOCATION_VARIANTS`), which is where the driver
      sets it true for the classification invocation only.
  </read_first>
  <action>
**This is VERIFY-AND-RECORD, not build (D-24). Do not re-derive or re-register what is already
there.** Produce a table in the SUMMARY with one row per Phase 25 artifact:

  | artifact | in manifest? | stage | profiles | conditional | rule honoured? |

covering `degenerate_observations.csv`, `all_observation_depths.csv` and the six
`*_degeneracy_breakdown.json` sidecars. For each, assert: it appears in `artifacts`; its `stage`
is the one that writes it; its `profiles` tag matches where it can actually be written; and
where emission is conditional, `conditional: true` is set so `_expectations.py:212-222` scores
absence as PASS.

Then assert the E2 `h_q` logging flag half of criterion 6: the driver's classification
invocation config sets `internals.log_all_observation_depths: true` and the timing/memory
variants set it false, which is why `all_observation_depths.csv` is written by exactly one
invocation.

**Fallback if an assertion fails:** state the specific gap as a finding, add the missing entry
following the house style (name/dir/stage/profiles/conditional/rows_rationale), regenerate the
sheet with `python -m experiments.render_expectation_sheet --write`, and re-run the sheet test —
a manifest edit is a three-file change. Do not close this task on an unverified assumption.
  </action>
  <acceptance_criteria>
    - The SUMMARY contains the eight-row table with a per-row verdict.
    - `python -c "import json;d=json.load(open('experiments/suite_expectations.json'));n={a['name'] for a in d['artifacts']};print(all(x in n for x in ('degenerate_observations.csv','all_observation_depths.csv')))"` prints `True`.
    - Both Phase 25 CSVs have `conditional: true` and `profiles == ["full"]`, or the deviation is
      recorded as a finding with a stated reason.
    - If any manifest edit was needed: `python -m experiments.render_expectation_sheet --check`
      exits 0 and `python -m pytest tests/unit/test_expectations.py -k sheet -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m experiments.render_expectation_sheet --check &amp;&amp; python -m pytest tests/unit/test_expectations.py -q</automated>
  </verify>
  <done>Criterion 6 is answered with evidence: eight artifacts checked, each with a verdict, and the h_q flag's invocation wiring confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expectation manifest → every gate verdict | the manifest is the single source of what the run must produce; a wrong tag silently changes what "green" means |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | gate assertions | mitigate | The acceptance criteria assert the `full`/`None` profile behaviour is unchanged and that no gate name disappears from the verdict block — a suppressed gate becomes `N/A`, visible, never absent. Plan 23-02's rule: a gate that cannot pass is worse than no gate; the answer is a correct gate, not a removed one. |
| T-27-03-02 | Repudiation | manifest edits without reasons | mitigate | Every retag gains a rationale field naming a `file:line`, per the manifest's own house style. A changed number without a changed rationale is the FIX-06 shape. |
| T-27-03-03 | Information disclosure | none | accept | This plan touches JSON/Markdown/Python in a local scientific pipeline with no network surface and no secrets. |
</threat_model>

<verification>
**Targeted test command for this plan:**
`python -m pytest tests/unit/test_expectations.py tests/unit/test_rerun_gates.py -q`
plus the cheap roll-up `python experiments/check_rerun_gates.py experiments/results_smoke --profile smoke`
(reads the preserved smoke tree; runs no experiment; seconds).

**Do NOT run the full test suite** — the orchestrator runs `pytest tests/` at the post-merge
gate. **Do NOT run `run_experiment_suite.sh --smoke`**: it takes ~11 minutes, over the tool
ceiling, and plan 27-10 owns that pass. **Do NOT edit** `experiments/run_experiment_suite.sh`
(plans 27-02 and 27-08 own it) or any experiment script — D-20 is manifest/gate-side only, with
no experiment code changes.

Use the AquaCal conda interpreter for pytest and for `check_rerun_gates.py` (it imports pandas
and `aquacal.datasets.synthetic`); Git Bash's bare `python` is Anaconda base.
</verification>

<success_criteria>
- `check_rerun_gates.py experiments/results_smoke --profile smoke` exits 0 with `0 FAIL`.
- Nothing that fails at `full` today stops failing at `full`.
- `render_expectation_sheet --check` exits 0.
- Criterion 6 answered with an eight-row evidence table.
</success_criteria>

<output>
Create `.planning/phases/27-frozen-single-sha-handoff-package/27-03-SUMMARY.md` when done.
One commit per decision: D-20's manifest retag, D-20's gate threading, then D-24's finding.
</output>
