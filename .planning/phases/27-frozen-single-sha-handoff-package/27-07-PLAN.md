---
phase: 27-frozen-single-sha-handoff-package
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/FROZEN-ROWS.md
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "Every ledger row this run will NOT regenerate is classified in-repo, with the sha and machine it was measured at"
    - "RL-determinism is recorded as unregenerable by construction, naming P26-D-42 (e6_repeat2 is OFF)"
    - "Phase 30's dangling-reference list falls out of this note for free"
  artifacts:
    - path: "experiments/FROZEN-ROWS.md"
      provides: "artifact / sha / machine / why-not-regenerated for every deliberately-frozen ledger row"
      contains: "results_linux32gb"
  key_links:
    - from: "experiments/FROZEN-ROWS.md"
      to: "experiments/EMITTER-COVERAGE.md's EMITTER-BACKED, NOT REGENERATED rows"
      via: "the two reports partition the ledger between them"
      pattern: "NOT REGENERATED"
---

<objective>
D-19: write an in-repo note classifying every ledger row this run will **not** regenerate, with
the sha and machine each was measured at.

This turns "stale reference" into "stated provenance" **before** Phase 30 purges the archive
trees, and hands Phase 30 its dangling-reference list for free. Without it, the purge either
strands a manuscript citation or is blocked on re-deriving what it points at.

The population is small and already located:
  - **three rows** cite `archive/e6-2026-08-02-pre-depth-fix/...` and
    `archive/e2-2026-07-30-pre-pnp-guard/...` — pre-fix trees;
  - **six rows** cite `results_linux32gb/...` — the earlier machine (`e2_timing/`,
    `e2_memory/benchmark.json` ×2, `e2_memory/`, `e4/benchmark_grid.csv`, and the
    `linux32gb_scope.json` group);
  - **seven rows** are `KEEP-FROZEN-5f`, all naming `real_rig_metrics.json` — they have an
    emitter and are frozen behind the OpenCV 4.13 pin rather than re-derived (4.14.0 detected
    1.95% fewer corners and moved reconstruction RMSE +7.8%);
  - **`RL-determinism`** is unregenerable by construction, because **P26-D-42 turned `e6_repeat2`
    OFF**.

Purpose: state the provenance of everything the re-run will not touch, while the trees still exist.
Output: `experiments/FROZEN-ROWS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-frozen-single-sha-handoff-package/27-CONTEXT.md
@.planning/phases/27-frozen-single-sha-handoff-package/27-PATTERNS.md
@experiments/EXPECTATIONS.md
</context>

<constraints>
**The manuscript tree is READ-ONLY from this repo.** The ledger lives at

    C:\Users\tucke\OneDrive - Georgia Institute of Technology\Thesis\Spinoffs\papers\aquacal\numbers-ledger.tsv

Read it. Never edit it. Cite rows by their `id` column.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Classify the frozen rows</name>
  <files>experiments/FROZEN-ROWS.md</files>
  <read_first>
    - `numbers-ledger.tsv` (read-only) — filter to rows whose `artifact` names an `archive/...`
      or `results_linux32gb/...` path, plus every row with `verdict == KEEP-FROZEN-5f`, plus
      `RL-determinism`.
    - `experiments/EXPECTATIONS.md` §2/§3 — the pre-declared-mismatch tables, and the framing to
      copy: *a pre-declared mismatch is expected output, not a finding.*
    - `experiments/archive/` and any surviving `results_linux32gb` tree in the repo — read the
      provenance sidecars (`*_provenance.json`, `benchmark.json` `environment` blocks) to recover
      each tree's recorded `git_sha` and machine. Cite what you find; where a sha is not
      recoverable, say so explicitly rather than guessing.
    - `.planning/phases/27-frozen-single-sha-handoff-package/27-CONTEXT.md` § D-19 and the
      standing constraint P26-D-42.
    - `tests/unit/test_expectations.py::test_no_expectation_names_results_e6_repeat2` (`:353`) —
      the manifest already pins `e6_repeat2`'s absence negatively.
  </read_first>
  <action>
Write the note with a table keyed on the ledger `id`:

  | ledger id | artifact | recorded sha | machine | why this run will not regenerate it |

Fill `recorded sha` and `machine` from the artifact's own provenance block wherever one exists;
where it does not, write `not recorded` — never a guess. Group the rows into four sections that
mirror the four causes: **pre-fix archive trees**, **the earlier Linux machine**, **frozen behind
the OpenCV 4.13 pin**, and **unregenerable by construction**.

For the OpenCV group, state the measured reason the pin exists rather than restating "it is
pinned": 4.14.0 detected 1.95% fewer corners and moved reconstruction RMSE +7.8%, which is why
`real_rig_metrics.json` is frozen rather than re-derived.

For `RL-determinism`, state plainly that no run at this freeze can produce it because
`e6_repeat2` is off (P26-D-42), and that the manifest asserts that absence.

Close with two sections:
  - **What Phase 30 inherits** — this table IS the dangling-reference list; every row here is a
    citation that must survive the archive purge (POST-03) or be re-pointed.
  - **What this note does NOT do** — it does not license the numbers. A stated provenance makes a
    number *interpretable*, not *current*. Use `EXPECTATIONS.md`'s bold-callout voice.
  </action>
  <acceptance_criteria>
    - `test -f experiments/FROZEN-ROWS.md` exits 0.
    - The table covers at least the three `archive/...` rows, the six `results_linux32gb/...`
      rows, the seven `KEEP-FROZEN-5f` rows and `RL-determinism` — 17 rows minimum, each keyed
      by ledger `id`.
    - `grep -c 'e6_repeat2' experiments/FROZEN-ROWS.md` returns ≥ 1.
    - `grep -c 'not recorded' experiments/FROZEN-ROWS.md` is either 0 or matches the number of
      rows whose provenance genuinely could not be recovered — no cell is left blank and no sha
      is invented.
    - The "What Phase 30 inherits" and "What this note does NOT do" sections are present.
  </acceptance_criteria>
  <verify>
    <automated>test -f experiments/FROZEN-ROWS.md &amp;&amp; grep -q e6_repeat2 experiments/FROZEN-ROWS.md &amp;&amp; grep -q results_linux32gb experiments/FROZEN-ROWS.md</automated>
  </verify>
  <done>Every row this run will not regenerate is classified with its sha, machine and reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manuscript tree (author's, off-repo) → this repo | the ledger is read; nothing is written back |
| this repo → public GitHub | the note becomes public at plan 27-11's push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07-01 | Tampering | `numbers-ledger.tsv` | mitigate | Read-only constraint stated at the top and enforced by citing rows by `id`; nothing is written outside `experiments/`. |
| T-27-07-02 | Repudiation | invented provenance | mitigate | `not recorded` is required where a sha or machine cannot be recovered from an artifact's own provenance block. An invented sha is worse than an absent one — it is the F-001 shape this milestone exists to close. |
| T-27-07-03 | Information disclosure | note becomes public | accept | The note carries artifact paths, shas and a machine class ("the earlier 32 GB Linux box"). No credentials, no hostnames — refer to machines by class, not by name. |
</threat_model>

<verification>
**Targeted test command for this plan:** none — this plan adds one Markdown file and changes no
code.

**Do NOT run the full test suite** — the orchestrator runs `pytest tests/` at the post-merge
gate. **Do NOT edit the manuscript tree.** **Do NOT edit `experiments/EXPECTATIONS.md`** (plan
27-03 owns it this wave; it is a generated-region file). **Do NOT delete or move any
`archive/...` or `results_linux32gb/...` tree** — the purge is Phase 30 / POST-03, and this note
is written precisely so that purge can happen safely later.
</verification>

<success_criteria>
- `experiments/FROZEN-ROWS.md` exists with ≥ 17 classified rows in four cause-groups.
- Every row carries a sha and machine, or an explicit `not recorded`.
- `RL-determinism` is recorded as unregenerable, naming P26-D-42.
- Phase 30's dangling-reference list is derivable from the note alone.
</success_criteria>

<output>
Create `.planning/phases/27-frozen-single-sha-handoff-package/27-07-SUMMARY.md` when done.
</output>
