---
phase: 27-frozen-single-sha-handoff-package
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/EMITTER-COVERAGE.md
autonomous: false
requirements: [RUN-01]

must_haves:
  truths:
    - "Every named artifact in the manuscript numbers ledger has a confirmed emitter in the frozen code, located to a file:line"
    - "The three genuine emitter candidates (M-L281-19mm, M-L281-135x, RL-determinism) are each resolved to one of: emitter exists, emitter added, or unregenerable-by-construction"
    - "The report states plainly that an emitter existing does not make the number right"
  artifacts:
    - path: "experiments/EMITTER-COVERAGE.md"
      provides: "the artifact -> emitter file:line walk over all 27 distinct ledger artifacts, with a per-row verdict"
      contains: "file:line"
  key_links:
    - from: "experiments/EMITTER-COVERAGE.md"
      to: "numbers-ledger.tsv's artifact column"
      via: "one row per distinct artifact, all 27"
      pattern: "artifact"
---

<objective>
ROADMAP criterion 5: **every §3-facing number has a generating emitter in the frozen code.** A
number that is hand-asserted with no artifact behind it cannot be made traceable after the
freeze — the fix is an emitter, and Phase 29 is too late to add one (D-18).

D-16 makes this a **written report, not a gate**, deliberately: the input
(`numbers-ledger.tsv`) lives outside the repo and is the author's to edit, which is exactly the
"gate that cannot pass" shape plan 23-02 warned about.

**Start from D-17's result, not from scratch.** The row-level triage is already done and its
shape verified against the file: 131 rows, 27 distinct artifacts, 78 `KEEP-VERIFIED`, 46 `EDIT`
(all `applied`), 7 `KEEP-FROZEN-5f`; 21 rows have an empty `artifact` column and 11 of those are
non-`EDIT`. Most of the 11 are legitimately not emitter-backed — `M-L271-noise` / `M-L268-noise`
are pixel-noise *inputs*, `M-L245-spacing` / `M-L276-spacing` are apparatus facts,
`RL-idx-seawater` / `RL-idx-temp` are physical constants, `M-L246-frames` is a frame count. The
remaining work is confirming an emitter for the 27 named artifacts and resolving three candidate
rows — **not re-classifying 131 rows**.

Purpose: close criterion 5 before the tag, when an emitter can still be added.
Output: `experiments/EMITTER-COVERAGE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-frozen-single-sha-handoff-package/27-CONTEXT.md
@.planning/phases/27-frozen-single-sha-handoff-package/27-PATTERNS.md
@experiments/EXPECTATIONS.md
@experiments/suite_expectations.json
</context>

<constraints>
**The manuscript tree is READ-ONLY from this repo.** The ledger lives at

    C:\Users\tucke\OneDrive - Georgia Institute of Technology\Thesis\Spinoffs\papers\aquacal\numbers-ledger.tsv

Read it. **Never edit it, never write beside it, never regenerate it.** Its columns are
`id, file, line_hint, locator, current_text, target_text, quantity, artifact, derivation,
verdict, lockstep_group, lockstep_value, status, forbid_global, note`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Walk the 27 named artifacts to their emitters</name>
  <files>experiments/EMITTER-COVERAGE.md</files>
  <read_first>
    - `numbers-ledger.tsv` at the path above (read-only) — the `artifact` and `verdict` columns.
    - `experiments/EXPECTATIONS.md` — the house style: a "written before the fact" opener, dense
      tables with a per-row verdict AND a per-row reason, bold anti-misreading callouts, and
      **every claim carrying a `file:line`**.
    - `experiments/suite_expectations.json` → `artifacts` (62 entries with `name`, `dir`,
      `stage`) — the fastest route from an artifact name to the stage that writes it.
  </read_first>
  <action>
Build the report's core table, one row per **distinct** artifact (27 of them), with columns:

  | artifact | ledger rows | emitting stage | emitter (`file:line`) | verdict |

Derive the emitter by locating the write in the frozen code — the `open(...,"w")`,
`to_csv`, `json.dump` or `write_text` call that produces that filename — and cite it as
`file:line`. The report's whole value is that column; a row without one is not done.

Verdicts, exactly three:
  - **EMITTER-BACKED** — a write exists in the frozen code and the artifact is in
    `suite_expectations.json`, so the run is expected to produce it.
  - **EMITTER-BACKED, NOT REGENERATED** — a write exists, but this run will not re-produce that
    particular tree (the `archive/...` and `results_linux32gb/...` artifacts, and the 7
    `KEEP-FROZEN-5f` rows naming `real_rig_metrics.json`, which are frozen behind the OpenCV
    4.13 pin rather than re-derived). Cross-reference plan 27-07's note, which classifies these.
  - **NO EMITTER** — a genuine criterion-5 gap. Task 2 handles these.

Add the sections `EXPECTATIONS.md`'s style requires:
  - an opener saying why the report exists and that it is written **before** the freeze on
    purpose, because after the freeze an emitter cannot be added;
  - a short section listing the 11 non-`EDIT` empty-artifact rows and why each is legitimately
    not emitter-backed (inputs, apparatus facts, physical constants, a frame count);
  - a bold callout, in `EXPECTATIONS.md`'s §4 voice: **an emitter existing does not make the
    number right.** Existence and row count are not correctness.
  </action>
  <acceptance_criteria>
    - `experiments/EMITTER-COVERAGE.md` exists and its main table has 27 artifact rows.
    - Every row whose verdict is EMITTER-BACKED (either kind) carries a `file:line` emitter
      reference — verify by inspection that no such row's emitter cell is empty or says "TBD".
    - The 11 non-`EDIT` empty-artifact rows are enumerated by ledger `id` with a stated reason.
    - The bold "an emitter existing does not make the number right" callout is present.
    - `git status --porcelain` shows no change under the OneDrive manuscript tree (it is outside
      the repo; assert that no write was attempted).
  </acceptance_criteria>
  <verify>
    <automated>test -f experiments/EMITTER-COVERAGE.md &amp;&amp; grep -c ':' experiments/EMITTER-COVERAGE.md</automated>
  </verify>
  <done>All 27 artifacts walked, each with a verdict and, where applicable, an emitter file:line.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the three genuine candidates (D-18)</name>
  <files>experiments/EMITTER-COVERAGE.md</files>
  <read_first>
    - The ledger rows `M-L281-19mm` (refractive Z-RMSE at 2.5 m, body), `M-L281-135x`
      (depth-axis improvement ratio, body) and `RL-determinism` (E6 paired-repeat determinism) —
      their `quantity`, `derivation` and `note` columns say what the number is and how it was
      obtained.
    - `experiments/e1_refractive_comparison.py` — what it writes and where, for the two `M-L281`
      candidates (both are E1 depth-axis quantities).
    - `.planning/phases/27-frozen-single-sha-handoff-package/27-CONTEXT.md` § D-18, § D-19, and
      the standing constraint **P26-D-42: `e6_repeat2` is OFF**, which is why `RL-determinism`
      is unregenerable by construction.
    - `tests/unit/test_expectations.py::test_no_expectation_names_results_e6_repeat2` — the
      manifest already pins that fact negatively.
  </read_first>
  <action>
For each of the three, reach exactly one conclusion and record it in the report with evidence:

  - **Emitter exists** — cite the `file:line` and the artifact it writes. Most likely for the two
    `M-L281` rows if the quantity is already a column of `exp1_band.csv` /
    `exp1_parameter_band.csv` or a field of an `e1_benchmark_*.json` record. Say which.
  - **Emitter must be added (D-18)** — the number is computable from an artifact this run WILL
    produce, but nothing writes it. Add the emitter. It must be **additive**: write a NEW field
    or a NEW artifact, never reshape an existing one, because § D cut the schema edits and an
    additive write carries far less risk. Add a test asserting the new field/artifact is written,
    and register it in `experiments/suite_expectations.json` if it is a new file — remembering
    that a manifest edit is a three-file change (`--write` the sheet, run the sheet test).
  - **Unregenerable by construction** — state why, and hand it to plan 27-07's frozen-row note
    rather than inventing an emitter for a run that will not happen. `RL-determinism` is this
    case: P26-D-42 turned `e6_repeat2` off.

**Stop-and-report condition:** if a candidate needs an emitter that cannot be written from an
artifact this run produces — i.e. it needs a NEW experiment or a NEW measurement — do not
improvise one and do not quietly defer it. Record it as a blocking finding and stop, so the
orchestrator can take it to the author. Criterion 5 says a genuine gap sends the freeze back to
this phase; that is a decision for the author, not for the executor.
  </action>
  <acceptance_criteria>
    - Each of `M-L281-19mm`, `M-L281-135x`, `RL-determinism` has exactly one of the three
      conclusions recorded, with evidence.
    - If an emitter was added: a targeted test asserting the new write passes, and — if a new
      artifact file was registered — `python -m experiments.render_expectation_sheet --check`
      exits 0 and `python -m pytest tests/unit/test_expectations.py -k sheet -q` exits 0.
    - If no emitter was added, the report states for each candidate why none is needed.
    - No stop-and-report condition is left unreported.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'M-L281-19mm' experiments/EMITTER-COVERAGE.md</automated>
  </verify>
  <done>Three candidates resolved; any added emitter is additive, tested and registered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author review of the coverage verdict</name>
  <what-built>
    `experiments/EMITTER-COVERAGE.md` — 27 artifacts walked to their emitters, the 11
    legitimately-unbacked ledger rows enumerated, and the three genuine candidates resolved.
  </what-built>
  <how-to-verify>
    1. Read `experiments/EMITTER-COVERAGE.md` top to bottom.
    2. Confirm the NO EMITTER count. If it is zero, criterion 5 is closed and the freeze can
       proceed. If it is non-zero, the freeze goes back to this phase — the emitter is added and
       the tag is re-cut (D-03/D-18), which is a normal outcome, not a failure signal.
    3. Spot-check three `file:line` emitter citations against the code.
    4. Confirm nothing in the OneDrive manuscript tree was modified.
  </how-to-verify>
  <action>
Present `experiments/EMITTER-COVERAGE.md` to the author with the NO EMITTER count stated up
front, and apply any correction they give before closing the plan.
  </action>
  <acceptance_criteria>
    - The author states the NO EMITTER count and whether the freeze proceeds.
    - Any disputed row is corrected before the plan closes.
  </acceptance_criteria>
  <resume-signal>Type "approved" plus the NO EMITTER count, or list the rows to revisit.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manuscript tree (author's, off-repo) → this repo | the ledger is read; nothing is written back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | Tampering | `numbers-ledger.tsv` | mitigate | Read-only constraint stated at the top of the plan and asserted in Task 1's criteria. The report is written in-repo; the ledger is never opened for writing. |
| T-27-06-02 | Information disclosure | `EMITTER-COVERAGE.md` becomes public at plan 27-11's push | mitigate | The report cites artifact names and `file:line` references from this repo only. Do not paste unpublished manuscript prose or `current_text`/`target_text` values into it — cite the ledger `id` instead. |
| T-27-06-03 | Repudiation | unverifiable coverage claim | mitigate | Every EMITTER-BACKED verdict must carry a `file:line`; the author spot-checks three in Task 3. |
</threat_model>

<verification>
**Targeted test command for this plan:** none unless Task 2 adds an emitter, in which case run
only that emitter's own test file plus, if the manifest changed,
`python -m pytest tests/unit/test_expectations.py -q`.

**Do NOT run the full test suite** — the orchestrator runs `pytest tests/` at the post-merge
gate. **Do NOT run any experiment** to "check a number"; this plan locates emitters, it does not
re-derive values. **Do NOT edit the manuscript tree.** **Do NOT edit** `experiments/EXPECTATIONS.md`
(it is a generated-region file owned by plan 27-03 this wave) — write a new file instead.
</verification>

<success_criteria>
- `experiments/EMITTER-COVERAGE.md` exists with 27 artifact rows, each with a verdict.
- Every EMITTER-BACKED row carries a `file:line`.
- The three candidates are resolved, with any added emitter additive and tested.
- The author has stated the NO EMITTER count and whether the freeze proceeds.
</success_criteria>

<output>
Create `.planning/phases/27-frozen-single-sha-handoff-package/27-06-SUMMARY.md` when done.
</output>
