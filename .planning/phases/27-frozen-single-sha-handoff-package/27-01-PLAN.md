---
phase: 27-frozen-single-sha-handoff-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/27-frozen-single-sha-handoff-package/27-TARGET-FACTS.md
autonomous: false
requirements: [RUN-01]

must_haves:
  truths:
    - "The Linux run machine's CPU count, RAM, free disk and Python/conda situation are recorded in-repo"
    - "The E2 image set's on-target location is recorded, with a measured byte sum over exactly the 13 extrinsic directories the pre-flight probe will sum"
    - "Whether 4_000_000_000 (the manifest's min_total_bytes floor) is cleared by that measurement is stated as a verdict, not assumed"
  artifacts:
    - path: ".planning/phases/27-frozen-single-sha-handoff-package/27-TARGET-FACTS.md"
      provides: "The measured target facts that D-10's byte floor, D-11's config paths, D-12's GATE_PYTHON middle rung and D-15's RSS confirmation all read from"
      contains: "min_total_bytes"
  key_links:
    - from: "27-TARGET-FACTS.md"
      to: "experiments/configs/e2_release_linux.yaml (plan 27-08)"
      via: "the recorded extrinsic/intrinsic image-set roots become the config's paths"
      pattern: "extrinsic"
    - from: "27-TARGET-FACTS.md"
      to: "experiments/suite_expectations.json preflight.frameset.cheap_check"
      via: "the measured byte sum is compared against min_total_bytes"
      pattern: "4000000000"
---

<objective>
Measure the Linux run machine, once, before any code in this phase is written against
assumptions about it.

Four later plans read this note rather than guessing: 27-08 needs the E2 image set's real
on-target paths to author the committed release config (D-11); 27-02/27-08 need the measured
byte sum over the 13 extrinsic directories to decide whether `min_total_bytes: 4000000000`
still discriminates (D-10 says this must be **measured on the target, not assumed**); 27-08
needs to know which interpreter `GATE_PYTHON`'s new middle rung will find (D-12); 27-12 needs
RSS/CPU headroom to confirm `SUITE_WORKERS` 4–5 without re-tuning it (D-15).

Purpose: turn four assumptions into four measurements before the freeze window opens.
Output: `27-TARGET-FACTS.md` — measurements only, no secrets, no hostnames.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-frozen-single-sha-handoff-package/27-CONTEXT.md
@.planning/phases/27-frozen-single-sha-handoff-package/27-PATTERNS.md
@CLAUDE.md
</context>

<preconditions>
**The SSH connection details are NOT recorded anywhere in the repo (D-06).** Host/alias, user
and key must be supplied by the author before Task 2 runs. Task 1 is a blocking checkpoint for
exactly that reason. Do not invent a hostname, do not guess a username, do not probe the network.
</preconditions>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Obtain the target's connection details and the image-set root</name>
  <read_first>
    - `.planning/phases/27-frozen-single-sha-handoff-package/27-CONTEXT.md` § D-06, § D-08
  </read_first>
  <what-needed>
    Four values, supplied by the author:
      1. SSH target as it would be typed (`user@host`, or an alias configured in `~/.ssh/config`).
      2. The identity file, if it is not the default key.
      3. The absolute path on the target of the E2 **image set** root — the directory whose
         children are the per-camera `extrinsic/<cam>/` and `intrinsic/<cam>/` trees (D-08: the
         image set, not the video set, and it covers BOTH intrinsics and extrinsics).
      4. The absolute path where the frozen clone should live on the target.
  </what-needed>
  <how-to-verify>
    Confirm with one cheap round trip before proceeding:
      `ssh &lt;target&gt; 'echo ok; uname -s'`
    It must print `ok` and `Linux` within seconds. If it hangs or prompts, stop and report —
    do not retry into a background job.
  </how-to-verify>
  <action>
Ask the author for the four values above and record them in your working notes only. Verify
reachability with the single cheap round trip in `<how-to-verify>`. Write none of them to disk.
  </action>
  <acceptance_criteria>
    - All four values are recorded in the executor's working notes.
    - `ssh &lt;target&gt; 'echo ok; uname -s'` exits 0 and prints `Linux`.
    - No credential, key material, hostname or username is written into any file under
      `.planning/` or `experiments/` — those trees become public at plan 27-11's push.
  </acceptance_criteria>
  <resume-signal>Type the four values, or "abort" if the machine is not reachable yet.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Measure the machine and the frameset</name>
  <files>(none — measurement only)</files>
  <read_first>
    - `experiments/run_experiment_suite.sh:913-1010` — `_preflight_frameset`, so the byte sum you
      measure is the same set the probe will sum: the values of `paths.extrinsic_videos` only,
      thirteen of them, and nothing else.
    - `experiments/suite_expectations.json` → `preflight.frameset.cheap_check` (`n_extrinsic_videos`,
      `min_total_bytes`, `min_total_bytes_rationale`) and `preflight.free_space_floor_gib`.
    - `.planning/phases/27-frozen-single-sha-handoff-package/27-CONTEXT.md` § D-10, § D-12, § D-15.
  </read_first>
  <action>
Run each of these as a SEPARATE `ssh` invocation. Every one returns in seconds; none may be
backgrounded (D-07). Record raw output verbatim.

  1. Machine: `uname -a`, `nproc`, `free -b`, `cat /proc/cpuinfo | grep -m1 "model name"`.
  2. Disk: `df -B1 &lt;clone-parent-dir&gt;` — compare free bytes against
     `preflight.free_space_floor_gib` (20 GiB).
  3. Frameset shape: under the image-set root, list the `extrinsic/` children, count them, and
     count files per child. Expect 13 directories (12 main + 1 auxiliary, matching
     `cheap_check.n_extrinsic_videos`).
  4. Frameset bytes: `du -sb` over each of the 13 extrinsic directories AND their sum. This
     single number is what D-10's recursive byte sum will produce. State explicitly whether it
     clears `min_total_bytes: 4000000000`, and by what margin.
  5. Same shape+count measurement for `intrinsic/`.
  6. Interpreter: `conda env list` (or `ls ~/*conda*/envs`), and for any `AquaCal` env found,
     its `bin/python -V`. Also `command -v python python3` and `git --version`. This decides
     what D-12's middle rung (`.../envs/AquaCal/bin/python`) will resolve to.
  7. Note whether the clone target directory already exists and is non-empty.

Do NOT build an environment, do NOT clone the repo, do NOT run any AquaCal code here — that is
plan 27-12's job, and it must happen against the tag, not against an ad-hoc copy.
  </action>
  <acceptance_criteria>
    - Every command above ran as its own foreground `ssh` call and returned inside the tool
      ceiling. No command was backgrounded.
    - The extrinsic directory count is stated as a number and compared against 13.
    - The extrinsic byte total is stated as an exact integer and compared against 4000000000
      with an explicit PASS/FAIL verdict.
    - The resolved AquaCal interpreter path (or its absence) is recorded verbatim.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this task is a remote measurement; its verification is Task 3's note, whose completeness is asserted by the criteria above.</automated>
  </verify>
  <done>All seven measurements captured verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Write 27-TARGET-FACTS.md</name>
  <files>.planning/phases/27-frozen-single-sha-handoff-package/27-TARGET-FACTS.md</files>
  <read_first>
    - `experiments/EXPECTATIONS.md` — the house style for an in-repo measurement note: an opener
      stating why it exists, dense tables with a per-row verdict and a per-row reason, and bold
      callouts for what a number does NOT prove.
  </read_first>
  <action>
Write the note with these sections:

  - **Why this exists** — it is the measured input to D-10's byte floor, D-11's config paths,
    D-12's interpreter chain and D-15's worker confirmation. Written before the freeze on
    purpose.
  - **Machine** — kernel, CPU model, logical CPUs, RAM bytes, free disk at the clone parent,
    versus `free_space_floor_gib: 20`.
  - **The E2 image set** — the root path, the 13 extrinsic directories with per-directory file
    counts and byte sums, the total, and the verdict against `min_total_bytes: 4000000000`.
    Include the same table for `intrinsic/`.
  - **Byte-floor finding** — one of: *"clears the floor, no manifest change needed"*, or *"does
    not clear the floor; the floor must be re-derived from this measurement (D-10 — the check's
    shape is fixed, the number may not be), which plan 27-08 does"*. State which.
  - **Interpreter** — what `conda env list` showed and what path D-12's middle rung will resolve.
  - **Headroom** — RAM against the measured RSS classes from
    `.planning/probes/2026-08-18-solver-concurrency/`: 30 frames &lt; 1 GiB, 100 frames 2.7–3.5 GiB,
    200 frames 9.3–11.3 GiB. State whether `SUITE_WORKERS` 4–5 holds. **Do not re-tune it**
    (D-15); a short stage's RSS is a floor, not a setting.
  - **What this note does NOT prove** — nothing here proves the code runs on the target. That is
    plan 27-12's clean-clone verification, and it is the only venue that can catch a Linux-only
    failure.

**Redaction rule, non-negotiable:** this file is committed and becomes public at plan 27-11's
push. Refer to the machine as "the Linux run machine". No hostname, no username, no key path.
If the image-set root path contains a personal identifier (e.g. a home directory with a
username), write it as recorded but flag it in a `&gt; **Pre-push flag**` callout so plan 27-11's
audit puts it in front of the author.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/27-frozen-single-sha-handoff-package/27-TARGET-FACTS.md` exits 0.
    - `grep -c 'min_total_bytes' 27-TARGET-FACTS.md` returns ≥ 1.
    - `grep -Ec 'SUITE_WORKERS' 27-TARGET-FACTS.md` returns ≥ 1.
    - The note contains an explicit byte-floor verdict line (the words "clears the floor" or
      "does not clear the floor").
    - `grep -Eic '(ssh-rsa|BEGIN [A-Z ]*PRIVATE KEY|password)' 27-TARGET-FACTS.md` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/27-frozen-single-sha-handoff-package/27-TARGET-FACTS.md &amp;&amp; grep -q min_total_bytes .planning/phases/27-frozen-single-sha-handoff-package/27-TARGET-FACTS.md</automated>
  </verify>
  <done>The note exists, carries a byte-floor verdict, an interpreter finding and a worker-headroom finding, and contains no credential or hostname.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this machine → the Linux run machine | commands are executed on a remote host over SSH |
| this repo → public GitHub | everything written here becomes public at plan 27-11's push of 218 commits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Information disclosure | `27-TARGET-FACTS.md` | mitigate | Redaction rule in Task 3: no hostname, username or key path; a personal-identifier path is flagged with a `Pre-push flag` callout that plan 27-11's audit reads. Grep criterion asserts no key material. |
| T-27-01-02 | Tampering | remote command execution | mitigate | Every remote call in Task 2 is read-only (`uname`, `df`, `du`, `ls`, `conda env list`). No write, no install, no clone. Task 1 verifies the connection with a trivial echo before anything else runs. |
| T-27-01-03 | Repudiation | measurement provenance | accept | Measurements are transcribed by hand into a committed note rather than machine-captured. Accepted: the note is the input to a later on-target verification (27-12) that re-derives the same facts against the real clone. |
</threat_model>

<verification>
Targeted check for this plan: none of the repo's Python is touched, so there is no test to run.
**Do NOT run the full test suite** — the orchestrator runs `pytest tests/` at the post-merge
gate. Do NOT clone, build an environment, or run the driver on the target here; plan 27-12 owns
all of that and it must happen against the tag.
</verification>

<success_criteria>
- `27-TARGET-FACTS.md` exists with all six sections.
- The extrinsic byte total is a measured integer with a stated verdict against 4000000000.
- The AquaCal interpreter path on the target is recorded or its absence stated.
- No hostname, username or key material anywhere in the committed note.
</success_criteria>

<output>
Create `.planning/phases/27-frozen-single-sha-handoff-package/27-01-SUMMARY.md` when done.
</output>
