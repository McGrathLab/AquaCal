---
phase: 27-frozen-single-sha-handoff-package
plan: 10
type: execute
wave: 3
depends_on: ["27-02", "27-03", "27-04", "27-05", "27-06", "27-07", "27-08", "27-09"]
files_modified:
  - .planning/phases/27-frozen-single-sha-handoff-package/27-PREPUSH-AUDIT.md
autonomous: false
requirements: [RUN-01]

must_haves:
  truths:
    - "A full local --smoke pass over all 20 stages exits 0 before anything is pushed or tagged"
    - "Every one of the 12 pre-existing smoke failures is cleared, and no new failure appeared"
    - "The 218 commits about to become public have been scanned for credentials and personal absolute paths, and the author has approved the exposure"
  artifacts:
    - path: ".planning/phases/27-frozen-single-sha-handoff-package/27-PREPUSH-AUDIT.md"
      provides: "the smoke acceptance record and the public-exposure scan, with the author's decision"
      contains: "0 FAIL"
  key_links:
    - from: "the local --smoke pass"
      to: "the driver's final exit code"
      via: "gates record into a sticky failure file; pre-flight aborts"
      pattern: "SUITE_FAILED"
---

<objective>
Two gates that must both clear **before** the tag is cut.

**The smoke acceptance pass.** D-05 makes the on-target smoke run the venue that catches a
Linux-specific failure, and that only works if a green smoke means something. Plans 27-03 and
27-04 between them close all 12 known FAILs and the exit-1 stage. Prove it here, locally, where
a red is cheap — a defect found now costs one commit; found after the tag it costs a new tag
(D-03), and found after transfer it sends the freeze back to this phase (criterion 4).

**The pre-push exposure audit.** The branch `experiments/full-suite-rerun` has **no upstream and
is 218 commits ahead of `origin/main`** — nothing about this work is on GitHub. Plan 27-11 makes
all of it public in one push. This is the one genuinely irreversible step in the phase, and it
is the last moment a credential or a personal absolute path can be caught.

Purpose: make the tag worth cutting, and the push safe to make.
Output: `27-PREPUSH-AUDIT.md`, plus the author's explicit go/no-go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-frozen-single-sha-handoff-package/27-CONTEXT.md
@.planning/phases/26-full-suite-driver-handoff-readiness/26-10-SUMMARY.md
@CLAUDE.md
</context>

<orchestrator_only>
**Task 1 is the ORCHESTRATOR's, never an executor's.** The smoke pass ran **11 min 05 s** at
sha `88512b7`, which is past the 600 s tool ceiling. A Bash call over the ceiling is
auto-backgrounded and the tool replies *"You will be notified when it completes"* — true for the
orchestrator, **false for a subagent**, for which ending its turn IS completion. This project has
lost multiple hours to exactly that. Launch it detached with `nohup ... & disown` and poll.
</orchestrator_only>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: ORCHESTRATOR — the local --smoke acceptance pass</name>
  <read_first>
    - `.planning/phases/26-full-suite-driver-handoff-readiness/26-10-SUMMARY.md` — the previous
      acceptance pass: 20 stages, 11 min 05 s, 71 PASS / 9 N/A / 12 FAIL, one stage exiting 1.
      That is the baseline this pass must beat.
    - `experiments/run_experiment_suite.sh` — its header (the two abort rules) and `--help`.
    - `experiments/HANDOFF.md` (plan 27-09) — the documented invocation.
    - `CLAUDE.md` § "Never let a subagent background a long run" and § "Always run calibration
      unbuffered".
  </read_first>
  <what-built>
    Plans 27-02, 27-03, 27-04, 27-05 and 27-08 between them: a path-kind-agnostic pre-flight, an
    exit-code-aware resume, a truthful smoke profile, profile-aware E4/E5/E6 gates, `--out`-honest
    `reconstruction_bootstrap`, a lockfile artifact and a two-regime thread pin.
  </what-built>
  <how-to-verify>
    Run the pass yourself, detached, from the repo root:

    1. **Point `SUITE_E2_RELEASE_CONFIG` at the Desktop config.** Plan 27-08 changed the default
       to the in-repo Linux config, whose absolute target paths do not resolve on this box, so
       pre-flight would report ABSENT. The Desktop config
       (`C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/config.yaml`) is the Windows
       escape hatch D-12 deliberately preserves, and it also exercises the FILE branch of D-10's
       path-kind-agnostic check.
    2. Launch with `nohup ... &amp; disown`, unbuffered, redirecting to a log. Do **not** use
       `run_in_background`; the harness kills it at ~35–50 min.
    3. Poll the log and the process listing until the driver exits. A quiet transcript is not a
       dead run — check the process listing before relaunching anything.
    4. Read the final exit code, the roll-up verdict block, and
       `experiments/run_experiment_suite_state.&lt;sha&gt;.failures.txt`.
  </how-to-verify>
  <action>
Launch the local `--smoke` pass yourself, detached, with `SUITE_E2_RELEASE_CONFIG` pointed at the
Desktop config, following `<how-to-verify>` step by step. Poll until the driver exits, then read
the exit code, the roll-up verdict block and the failures file, and compare them against 26-10's
recorded inventory.
  </action>
  <acceptance_criteria>
    - The driver's final exit code is **0**.
    - The end-of-run roll-up reports `0 FAIL`.
    - All 20 stages exit 0 — in particular `reconstruction_bootstrap`, which exited 1 at
      `88512b7`.
    - The failures file is empty or absent.
    - `run_manifest.json` and `environment_lock.txt` both exist in the smoke out dir, and the
      manifest carries the thread-cap key and both stage lists.
    - Compared against 26-10's inventory: all 12 FAILs cleared and **no new FAIL appeared**.
      Record the before/after verdict counts.
    - Nothing was written into `experiments/results/` — the smoke run writes only to
      `experiments/results_smoke*` trees.
  </acceptance_criteria>
  <resume-signal>Type "smoke green" plus the roll-up counts, or paste the failing verdict lines.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scan the 218 commits for anything that must not become public</name>
  <files>.planning/phases/27-frozen-single-sha-handoff-package/27-PREPUSH-AUDIT.md</files>
  <read_first>
    - `.gitignore` — specifically that `./CLAUDE.md` (`:216`) and all of `.claude/` (`:214`) are
      ignored, so local-only guidance is already excluded, and `experiments/results_smoke/`
      (`:430`) is ignored.
    - `.planning/phases/27-frozen-single-sha-handoff-package/27-TARGET-FACTS.md` and
      `experiments/configs/e2_release_linux.yaml` — the two files most likely to carry a personal
      absolute path, both flagged by their own plans' threat models.
  </read_first>
  <action>
Scan what the push will publish, and write the findings up.

  1. Establish the size of the exposure: the commit count and changed-file list between
     `origin/main` and `HEAD` (`git log --oneline origin/main..HEAD`,
     `git diff --stat origin/main...HEAD`). Confirm the count against the recorded 218 and note
     any drift.
  2. Scan the **tracked content at HEAD** (not just the diff — a secret added and later modified
     is still in history, but the working set is what a reader sees first) for:
     private-key headers, `ssh-rsa`, `password`, `passwd`, `secret`, `token`, `api[_-]?key`,
     `Bearer `, AWS-style `AKIA`, and `.env`-style assignment lines.
  3. Scan for **personal absolute paths**: `C:/Users/`, `C:\\Users\\`, `/home/`, `OneDrive`,
     `Desktop`. Classify each hit as one of:
       - **intentional and required** — e.g. the Desktop config path surviving as a comment or an
         override example in the driver (D-12 keeps it reachable via `SUITE_E2_RELEASE_CONFIG`),
         or the target paths inside `experiments/configs/e2_release_linux.yaml`;
       - **incidental** — a path in a doc or planning note that could be generalised;
       - **must not ship** — anything carrying a credential.
  4. Scan the diff for large binary additions that were not intended to travel
     (`git diff --stat origin/main...HEAD` sorted by size).
  5. Confirm no `v*`-shaped tag is about to be pushed: `git tag --list 'v*'` versus what plan
     27-11 will push. Note explicitly that `publish.yml` triggers on `push: tags: ['v*']` (PyPI),
     `release.yml` on `push: branches: [main]`, and `test.yml` on push-to-main or PR-to-main —
     so pushing THIS branch plus a non-`v*` tag fires **no workflow at all** (D-02).

Write `27-PREPUSH-AUDIT.md` with: the exposure size, a table of every hit with its
classification and a decision, the CI-trigger confirmation, and the smoke acceptance record from
Task 1 (counts before and after). **Do not "fix" a path by editing it into something that will
not resolve on the target** — a broken config is worse than a published directory name. Flag it
for the author instead.
  </action>
  <acceptance_criteria>
    - `27-PREPUSH-AUDIT.md` exists and states the commit count between `origin/main` and `HEAD`.
    - Every credential-pattern scan is recorded with its result; any non-zero hit is listed
      individually with a decision.
    - Every personal-absolute-path hit is classified into one of the three categories.
    - The audit states, with the workflow filenames, that the planned push fires no CI workflow.
    - The audit carries Task 1's smoke counts.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/27-frozen-single-sha-handoff-package/27-PREPUSH-AUDIT.md &amp;&amp; grep -q 'publish.yml' .planning/phases/27-frozen-single-sha-handoff-package/27-PREPUSH-AUDIT.md</automated>
  </verify>
  <done>Everything about to become public has been looked at, classified and written down.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Author approves the exposure</name>
  <decision>Whether to publish the 218 commits on `experiments/full-suite-rerun` to `origin`.</decision>
  <context>
    D-01 makes the push a prerequisite, not a detail: a real `git clone --branch &lt;tag&gt;` is what
    makes "clean checkout" (criterion 2) meaningful and keeps `.git` present, which the driver
    needs for `FROZEN_SHA`, the sha-derived state path, `git describe --tags --long --dirty` in
    the run manifest, and Gate 3's cross-artifact sha assertion.

    It is also irreversible in practice. Read `27-PREPUSH-AUDIT.md` before deciding.
  </context>
  <options>
    <option id="push-as-is">
      <name>Push as audited</name>
      <pros>Unblocks the tag and the on-target verification immediately; every classified hit is
        recorded as an accepted exposure.</pros>
      <cons>Any incidental personal path becomes permanently public in history.</cons>
    </option>
    <option id="remediate-first">
      <name>Remediate the flagged items, then push</name>
      <pros>Keeps incidental paths out of public history.</pros>
      <cons>Costs commits inside the freeze window; a path edited to something that does not
        resolve on the target would be caught only at plan 27-12, forcing a second tag.</cons>
    </option>
    <option id="hold">
      <name>Hold — do not push yet</name>
      <pros>Nothing is exposed.</pros>
      <cons>Phase 27 cannot close; criterion 2 cannot be satisfied without a clean clone.</cons>
    </option>
  </options>
  <action>
Present `27-PREPUSH-AUDIT.md` and the three options to the author, then record their choice and
the date verbatim in that file.
  </action>
  <acceptance_criteria>
    - The author's choice is recorded verbatim in `27-PREPUSH-AUDIT.md`, with the date.
    - If `remediate-first`, the specific items are listed and the remediation lands before plan
      27-11 runs.
  </acceptance_criteria>
  <resume-signal>Select: push-as-is, remediate-first, or hold.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private repository state → public GitHub | 218 previously-private commits become world-readable in one push |
| tag namespace → CI | a tag name matching `v*` would trigger the PyPI publish workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10-01 | Information disclosure | the 218-commit push | mitigate | Task 2's credential and personal-path scan over tracked content at HEAD plus the diff, with a per-hit classification, and Task 3's blocking author decision. `./CLAUDE.md` and all of `.claude/` are already gitignored. |
| T-27-10-02 | Elevation of privilege | unintended PyPI publish | mitigate | Task 2 confirms, by workflow filename and trigger, that the planned branch+tag push fires no workflow. Plan 27-11 re-asserts the tag name does not match `v*` before pushing. |
| T-27-10-03 | Repudiation | an unverified "it works" claim | mitigate | Task 1 is the orchestrator's and its acceptance is the driver's own exit code plus the roll-up counts, compared line by line against 26-10's recorded inventory. A subagent's return text is never the evidence. |
</threat_model>

<verification>
**Targeted commands for this plan:** git inspection commands (seconds) and the detached smoke
run (~11 min, orchestrator-launched, polled).

**Do NOT run the full test suite here** — the orchestrator runs `pytest tests/` at the post-merge
gate for waves 1 and 2 before this plan starts. **Do NOT dispatch Task 1 to an executor.** **Do
NOT push or tag anything in this plan** — plan 27-11 owns both, and it runs only after Task 3's
decision. **Do NOT run the suite without `--smoke`**: the production run is Phase 28, is 15–16 h,
and must happen on the Linux machine at the frozen sha.
</verification>

<success_criteria>
- Local `--smoke` exits 0 with a `0 FAIL` roll-up and all 20 stages at exit 0.
- `27-PREPUSH-AUDIT.md` records the exposure, every hit and its classification, and the CI-trigger
  confirmation.
- The author has chosen push-as-is, remediate-first or hold, and the choice is recorded.
</success_criteria>

<output>
Create `.planning/phases/27-frozen-single-sha-handoff-package/27-10-SUMMARY.md` when done.
</output>
