---
phase: 29.1-post-run-fixes-re-freeze
plan: 04
type: execute
wave: 2
depends_on: [29.1-01]
files_modified:
  - experiments/HANDOFF.md
  - .planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md
  - .planning/todos/pending/2026-08-20-POST-SUBMISSION-frozen-package-install-command-omits-required-extras.md
  - .planning/todos/done/
autonomous: true
requirements: [SC-5]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An operator following experiments/HANDOFF.md verbatim from a fresh clone builds an environment in which e3 runs to completion and the run manifest's cpu_count_logical and ram_total_bytes are populated (D-11)."
    - "Every place in the shipped tree that tells someone how to install this package has been inspected and ruled on, not just section 1.2 (D-11)."
    - "The correction is proven by execution from a clean environment, not by asserting a string changed (D-11)."
  artifacts:
    - experiments/HANDOFF.md
    - .planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md
    - .planning/todos/done/2026-08-20-POST-SUBMISSION-frozen-package-install-command-omits-required-extras.md
  key_links:
    - "pyproject.toml's `dev` extra -> pytest -> tests/unit/test_experiments_e3_constants.py -> e3_derived_quantities.py:243's DECLARED_CONSTANTS import. A section-3-facing generator depends on a test runner; without it e3 exits 1 on both passes."
    - "pyproject.toml's `bench` extra -> psutil -> aquacal.io.benchmark.capture_environment -> _run_manifest.py's REQUIRED_MANIFEST_FIELDS. Without it gate3_run_manifest_fields FAILs for a reason that has nothing to do with the run."
---

<objective>
Correct the frozen package's own install instructions so a fresh clone can run the suite by
following them verbatim, and prove the correction by building an environment from them.

Purpose: `experiments/HANDOFF.md` §1.2 says `pip install -e .`, which installs runtime
dependencies only. The suite needs two extras. Without `pytest` (`dev`) e3 dies outright on
both `--check` and `--force`, taking `code_constants.csv`, `newton_iterations.csv`,
`cpr_grouping.csv`, `e3_provenance.json` and the LaTeX fragments with it. Without `psutil`
(`bench`) two `REQUIRED_MANIFEST_FIELDS` go null. This was found on the run machine on
2026-08-19 and ruled "record, do not refreeze" because the run had to start that night; this
phase is where the refreeze happens, so the deferral is discharged here.

Output: a corrected §1.2, a ruling on every other install site in the shipped tree, and an
execution record from a clean environment.

The phase's tracer is `29.1-01` task 1. This plan is an independent expansion slice.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-post-run-fixes-re-freeze/29.1-CONTEXT.md
@.planning/todos/pending/2026-08-20-POST-SUBMISSION-frozen-package-install-command-omits-required-extras.md
@experiments/HANDOFF.md
@pyproject.toml
</context>

## Phase environment (all plans)

    PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python

python 3.11.15, cv2 4.13.0.92, numpy 2.4.6, scipy 1.17.1. There is no bare `python` on this
box's PATH — always use an absolute interpreter path. `conda` is initialised in `~/.bashrc`
and is therefore absent from a non-interactive shell; call interpreters by absolute path.

## Hard constraints (all plans)

1. **`src/` must not be modified.** In particular, do NOT apply the todo's option 2 (moving
   `DECLARED_CONSTANTS` out of the test module) or option 3 (promoting `psutil` to a runtime
   dependency). Both are correct long-term and both are out of this phase's scope: option 2
   touches the source of section-3-facing constants and needs the suite re-run behind it.
   D-11 authorises the doc-only fix, which is the whole fix for reproducibility.
2. **Do not regenerate or edit any committed artifact under `experiments/results/`.**
   `git diff --quiet -- experiments/results/` must exit 0 for this plan.
3. **Do not weaken `gate3_run_manifest_fields` to tolerate the nulls.** The gate was right.
4. **The driver exits non-zero on a healthy run** — acceptance is the roll-up, never `$?`.
5. **File ownership in this wave:** plan 29.1-02 owns `experiments/README.md`. If task 1's
   enumeration finds a site there that needs changing, record the finding and hand it to plan
   29.1-06 rather than editing the file.
6. **Leave the working tree clean where this plan can affect it.** An editable install writes
   `*.egg-info/` (gitignored); verify `git status --porcelain -- src/ pyproject.toml build/ dist/`
   is empty before finishing. Scope it that way rather than globally: plans 29.1-02 and 29.1-05
   run in the same wave and a global check would see their in-flight work, not yours.

## Artifacts this phase produces

No new gate result identifiers are introduced anywhere in the phase.

| Artifact | Kind | Plan |
|---|---|---|
| `rerun-freeze-02` | annotated git tag (must NOT match `v*`) | 08 |
| `experiments/freeze01_run_output/` | directory (`git mv` target for the 2026-08-20 run output) | 06 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-GATE-BEFORE-AFTER.md` | file | 06 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-VERIFICATION-BAR.md` | file | 07 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-FREEZE-RECORD.md` | file | 08 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md` | file | 08 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md` | file | 03 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md` | file | 04 |
| `.planning/todos/pending/2026-08-20-e7-band-mirrors-e1-benchmark-overwrite-hazard.md` | new todo | 02 |
| 7 todo files moved `pending/` → `done/` with a `## Resolved` block | moved files | 01, 02, 04, 05 |
| `gate1_guard_count:benchmark_grid.csv` | EXISTING gate id; PASS message gains an exemption clause | 01 |
| `e1_seed_band_degeneracy_breakdown.json` | expectation REMOVED; its `completeness:` result leaves the roll-up | 02 |

<tasks>

<task type="auto">
  <name>Task 1: Correct section 1.2, and rule on every other install site in the shipped tree</name>
  <files>experiments/HANDOFF.md, .planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md</files>
  <read_first>
    - `experiments/HANDOFF.md` §1.1-§1.3 — the interpreter note, the install block at ~:49, and
      the dependency table that follows it.
    - `pyproject.toml` `[project.optional-dependencies]` — the `dev`, `docs` and `bench` extras.
    - `.planning/todos/pending/2026-08-20-POST-SUBMISSION-frozen-package-install-command-omits-required-extras.md`
      — why each extra bites, and why it did not show up on Windows.
    - `.planning/phases/27-frozen-single-sha-handoff-package/27-ONTARGET-VERIFICATION.md` §6 —
      the on-target record this deviation was found in.
  </read_first>
  <action>
    **Correct §1.2.** Replace the install command with the extras form, and add a short
    sentence per extra saying what breaks without it — `dev` because a section-3-facing
    artifact generator imports `DECLARED_CONSTANTS` from a test module which imports the test
    runner, so e3 exits 1 on both passes and takes five artifacts with it; `bench` because
    two `REQUIRED_MANIFEST_FIELDS` are populated from `psutil` inside a `try/except` and go
    null without it, failing `gate3_run_manifest_fields` for a reason unrelated to the run.
    Extend the dependency table in the same section with the two extras so the table and the
    command agree. Say plainly that this was found on the run machine on 2026-08-19 and that
    it did not surface on the Windows development box because both packages were present
    there — that is the Linux-only failure class the on-target verification venue exists to
    catch, and it is the strongest argument for keeping that step.

    Do not touch §1.1's interpreter guidance or the OpenCV pin block; both are correct.

    **Then enumerate every other install site in the shipped tree** and rule on each, writing
    the result to
    `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md` as a table of
    `file:line | command | audience | ruling | reason`. Search the tracked tree outside
    `.planning/` for editable-install invocations. Expect at minimum: `CONTRIBUTING.md`,
    `requirements.txt`, the four `.github/workflows/*.yml` files, `experiments/README.md`,
    `experiments/prelaunch_gate.sh` and `src/aquacal/io/benchmark.py`'s comment.

    Ruling categories, and the distinction that matters:
    - `corrected` — the site tells a suite operator how to build a run environment and omitted
      the extras. Only `experiments/HANDOFF.md` is expected here.
    - `correct as-is — different audience` — a contributor or CI install whose extra set is
      right for what it does.
    - `correct as-is — not a first install` — a `--no-deps` metadata-refresh command. These
      exist to re-stamp the installed version after a `pyproject.toml` change and **must not
      gain extras**; adding them would change what the command does.
    - `handed to plan 29.1-06` — a site in a file another plan owns this wave.

    A ruling of "no change" is a real outcome and must carry its reason, so the next reader
    does not re-open the question.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; grep -q 'dev,bench' experiments/HANDOFF.md && grep -q 'psutil' experiments/HANDOFF.md && grep -q 'pytest' experiments/HANDOFF.md && $PY -c "
import pathlib, re
t = pathlib.Path('.planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md').read_text()
rows = [l for l in t.splitlines() if re.match(r'^\|\s*[A-Za-z_.]+\S*:\d+', l) or re.match(r'^\|\s*\S+\.(md|txt|yml|sh|py)\b', l)]
assert len(rows) >= 8, f'expected the full site enumeration, got {len(rows)}'
assert sum('corrected' in r for r in rows) >= 1
assert all(('corrected' in r) or ('correct as-is' in r) or ('29.1-06' in r) for r in rows), [r for r in rows if 'correct' not in r and '29.1-06' not in r]
print(f'OK {len(rows)} sites ruled')
" && git diff --quiet -- experiments/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `experiments/HANDOFF.md` §1.2's install command carries both extras, with a sentence per
      extra naming what breaks without it and the dependency table extended to match (D-11).
    - The Linux-only nature of the failure and the 2026-08-19 on-target finding are recorded
      in §1.2.
    - `29.1-INSTALL-SITES.md` enumerates at least eight sites, each with an audience and a
      ruling drawn from the four categories, each ruling carrying its reason.
    - Every `--no-deps` metadata-refresh site is ruled `correct as-is — not a first install`
      and is unmodified.
    - `experiments/README.md` is unmodified by this plan (owned by 29.1-02 this wave).
    - `src/` is unmodified.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation only; no code or dependency metadata moves (D-11).</reversibility>
  <done>The instructions say what the suite actually needs, and every other install site has a recorded ruling.</done>
</task>

<task type="auto">
  <name>Task 2: Prove it by building an environment from the corrected instructions</name>
  <files>.planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md, .planning/todos/done/2026-08-20-POST-SUBMISSION-frozen-package-install-command-omits-required-extras.md</files>
  <precondition>`conda` is available at `/home/tlancaster/anaconda3/bin/conda` and there is disk headroom for one additional python-3.11 environment. Assert both before starting; halt and report if either is unmet rather than substituting a venv, because the run machine's documented environment kind is conda.</precondition>
  <read_first>
    - The corrected `experiments/HANDOFF.md` §1.2 from task 1 — the command under test is the
      one that file now prints, read from the file, not retyped from this plan.
    - `experiments/_run_manifest.py` :82-100 (`REQUIRED_MANIFEST_FIELDS`) and
      `src/aquacal/io/benchmark.py` :110-160 (`capture_environment`'s psutil branch).
    - `experiments/e3_derived_quantities.py` around :243 — the `DECLARED_CONSTANTS` import
      that pulls in the test module.
  </read_first>
  <action>
    D-11 is verified **by execution from a clean environment**, not by asserting the string
    changed. Build one and use it.

    1. Create a fresh conda environment at python 3.11 with a name that says what it is
       (`aquacal-freeze02-cleanenv`). Do not reuse `aquacal-freeze01` — reusing an environment
       that already has both packages is exactly how this defect stayed invisible on Windows.
    2. Install from this clone using the command §1.2 now prints, verbatim.
    3. Assert both extras arrived: import the test runner and the system-metrics package in
       that interpreter and print their versions.
    4. Assert `capture_environment()` returns non-null values for both required manifest fields.
    5. Run e3's `--check` pass against `experiments/results` — read-only, and it is the pass
       the driver runs first — and assert exit 0.
    6. Run e3's `--force` pass into a scratch directory **outside** `experiments/`, and assert
       exit 0 and that all five of its artifacts appear there. Never point `--force` at
       `experiments/results`; that would overwrite committed artifacts.
    7. Assert `git status --porcelain -- src/ pyproject.toml build/ dist/` is empty afterwards,
       and that `git diff --quiet -- experiments/results/` still holds. Scoped, not global: two
       other plans share this wave.

    Record the transcript — environment name, resolved package versions for the two extras,
    the two e3 exit codes, and the two manifest field values — as an `## Execution record`
    section appended to `29.1-INSTALL-SITES.md`, dated.

    The negative half (that a runtime-only environment fails) is already measured and recorded
    on the target on 2026-08-19 in `27-ONTARGET-VERIFICATION.md` §6; cite it rather than
    rebuilding a deliberately broken environment.

    Then close the defect todo: `git mv` it to `.planning/todos/done/` with a `## Resolved`
    block naming D-11, the corrected command, the execution record, and — explicitly — that
    the todo's options 2 and 3 remain open and were deliberately not taken because they touch
    `src/` and require the suite re-run behind them. File nothing new; the todo's own body
    already carries those options for whoever picks them up post-submission.

    Leave the clean environment in place until plan 29.1-07's fresh-clone verification has
    run; name it in the SUMMARY so that plan can decide whether to reuse or rebuild it.
  </action>
  <verify>
    <automated>CLEAN=/home/tlancaster/anaconda3/envs/aquacal-freeze02-cleanenv/bin/python; test -x "$CLEAN" && "$CLEAN" -c "
import pytest, psutil
from aquacal.io.benchmark import capture_environment
env = capture_environment()
assert env['cpu_count_logical'] is not None, env
assert env['ram_total_bytes'] is not None, env
print('pytest', pytest.__version__, 'psutil', psutil.__version__, 'cpu', env['cpu_count_logical'], 'ram', env['ram_total_bytes'])
" && "$CLEAN" -m experiments.e3_derived_quantities --check --out experiments/results > /tmp/29_1_04_e3check.log 2>&1; echo "e3 --check exit=$?" && SCRATCH=$(mktemp -d) && "$CLEAN" -m experiments.e3_derived_quantities --force --out "$SCRATCH" > /tmp/29_1_04_e3force.log 2>&1 && ls "$SCRATCH" && test -f "$SCRATCH/code_constants.csv" && test -f "$SCRATCH/e3_provenance.json" && git diff --quiet -- experiments/results/ && test -z "$(git status --porcelain -- src/ pyproject.toml build/ dist/)" && grep -q '## Execution record' .planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md && test -f .planning/todos/done/2026-08-20-POST-SUBMISSION-frozen-package-install-command-omits-required-extras.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A fresh conda environment exists, built only from the command `experiments/HANDOFF.md`
      §1.2 now prints (D-11).
    - Both extras import in that interpreter and their versions are recorded.
    - `capture_environment()` returns non-null `cpu_count_logical` and `ram_total_bytes` there.
    - e3's `--check` pass exits 0 against `experiments/results`, and its `--force` pass exits 0
      into a scratch directory outside `experiments/`, producing all five artifacts.
    - `git diff --quiet -- experiments/results/` exits 0 and
      `git status --porcelain -- src/ pyproject.toml build/ dist/` is empty.
    - `29.1-INSTALL-SITES.md` carries a dated `## Execution record`.
    - The defect todo is in `.planning/todos/done/` with a `## Resolved` block citing D-11 and
      stating that its options 2 and 3 stay open by decision.
  </acceptance_criteria>
  <done>An environment built by following the shipped instructions verbatim runs the stage that used to die, with the manifest fields populated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → the run environment | `pip install -e ".[dev,bench]"` resolves packages from a public index into the environment that will produce published numbers. |
| shipped instructions → operator action | `HANDOFF.md` travels inside the frozen sha and is the only environment specification the receiving machine gets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-11 | Tampering | `pyproject.toml` dependency set | high | mitigate | This plan changes documentation only. No dependency is added, removed or re-pinned; the OpenCV `==4.13.*` pin and both extras are already declared. `git diff --quiet -- pyproject.toml` after task 1. |
| T-29.1-12 | Denial of service | `experiments/results/` | high | mitigate | e3's `--force` pass is directed at a scratch directory outside `experiments/`; `git diff --quiet -- experiments/results/` is an acceptance criterion, so a misdirected `--force` fails its own verify. |
| T-29.1-13 | Spoofing | editable install target | medium | mitigate | The clean environment is built from this clone and the verify imports `aquacal` through that interpreter; a stale editable install pointing at another checkout would surface as a different resolved path. Task 2 records the interpreter path in the execution record. |
| T-29.1-SC | Tampering | pip installs | high | accept | **Phase-level ruling.** This is the first of only two plans in the phase that run a package-manager install (plan 29.1-08 repeats the same install in a fresh clone), and it installs exactly the extras already declared in this repository's own `pyproject.toml` (`dev`: pytest, pytest-cov, ruff, pre-commit, python-semantic-release; `bench`: psutil>=5.9) — the same set CI has installed since Phase 02 and the same set present in the verified `aquacal-freeze01` environment. No new package is introduced by this phase, so no RESEARCH.md Package Legitimacy Audit is required. Precedent: `19.1-07-PLAN.md` T-19.1-SC. |
</threat_model>

<verification>
- `experiments/HANDOFF.md` §1.2's command carries both extras and the dependency table matches.
- `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md` enumerates and rules
  on every install site, and carries a dated `## Execution record`.
- The clean environment's interpreter imports both extras, returns non-null manifest fields,
  and runs e3 to completion on both passes.
- `git diff --quiet -- src/ pyproject.toml experiments/results/ experiments/README.md` exits 0.
- `git status --porcelain -- src/ pyproject.toml build/ dist/` is empty (scoped, because two
  other plans share this wave).
</verification>

<success_criteria>
SC-5 is satisfied: a fresh clone can run the suite by following the bundled instructions
verbatim, and that claim is backed by an execution record from an environment built exactly
that way — not by a diff on a string.
</success_criteria>

<output>
Create `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-04-SUMMARY.md` when done.
</output>
