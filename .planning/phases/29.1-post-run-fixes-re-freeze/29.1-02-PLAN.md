---
phase: 29.1-post-run-fixes-re-freeze
plan: 02
type: execute
wave: 2
depends_on: [29.1-01]
files_modified:
  - experiments/suite_expectations.json
  - experiments/EXPECTATIONS.md
  - experiments/README.md
  - experiments/e1_refractive_comparison.py
  - tests/unit/test_e1_band_mode.py
  - tests/unit/test_expectations.py
  - tests/unit/test_stale_provenance_strings.py
  - .planning/todos/pending/2026-08-20-three-manifests-expect-an-e1-band-sidecar-the-design-never-specified.md
  - .planning/todos/pending/2026-08-20-e1-band-logs-Wrote-for-writes-that-were-skipped.md
  - .planning/todos/pending/2026-08-20-e1-band-scope-string-still-claims-ten-seeds-after-ruling-A1-cut-it-to-four.md
  - .planning/todos/pending/2026-08-20-e7-band-mirrors-e1-benchmark-overwrite-hazard.md
  - .planning/todos/done/
autonomous: true
requirements: [SC-2, SC-3, SC-4]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full-profile roll-up over experiments/results contains no result naming e1_seed_band_degeneracy_breakdown.json, and every e1_band completeness result is PASS (D-04)."
    - "suite_expectations.json, EXPECTATIONS.md, README.md and the 2026-08-15 design table all agree that E1's band writes no degeneracy sidecar (D-04)."
    - "_run_band prints a write confirmation only when the record was actually written, and prints a distinct kept-existing line otherwise (D-06)."
    - "A band run can never overwrite an existing e1_benchmark_<model>.json, not even under --force -- the policy is enforced by the call, not by the resumability guard (D-07)."
    - "e1_seed_band_provenance.json's scope field states the seed count, row counts, noise levels and depths derived from the run that wrote it, so it cannot go stale again (D-08, D-10)."
  artifacts:
    - experiments/suite_expectations.json
    - experiments/EXPECTATIONS.md
    - experiments/README.md
    - experiments/e1_refractive_comparison.py
    - tests/unit/test_e1_band_mode.py
    - .planning/todos/pending/2026-08-20-e7-band-mirrors-e1-benchmark-overwrite-hazard.md
  key_links:
    - "_expectations._validate_manifest enforces bidirectional agreement between a stage's `produces` list and its artifact entries, so both suite_expectations.json sites must be removed in the same edit or load_expectations() raises (D-04)."
    - "write_direct_call_benchmark's False return (_io.py:875-881) -> _run_band's print. Today the print ignores the return; after this plan the print is the return (D-06)."
    - "run_experiment_suite.sh's manifest declares e1_benchmark_<model>.json as produced by stage `e1` (which runs with --force) and e1_band depends_on e1, so the band's write is unreachable in a full-suite run and the hazard is a --force or standalone band run (D-07)."
---

<objective>
Fix all three of E1's band defects: unclaim the degeneracy sidecar the design never
specified, stop the band claiming writes it did not make (and close the overwrite hazard
behind that claim), and make the band's `scope` field state the domain that was actually
run.

Purpose: SC-2 is the second of the two real FAILs in the 2026-08-20 roll-up. SC-3 is
load-bearing for the re-run — the band's benchmark write is a silent overwrite waiting for a
`--force` pass. SC-4 is a provenance artifact currently authorising a published claim over a
domain that was never executed.

Output: three manifests in agreement with the design table, a band whose log is true and
whose write policy is enforced rather than accidental, and a `scope` field derived from the
run rather than frozen into a literal.

The phase's tracer is `29.1-01` task 1 (writer → CSV → gate → roll-up), already proven.
This plan is expansion work along that same path plus two adjacent E1 sites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-post-run-fixes-re-freeze/29.1-CONTEXT.md
@.planning/todos/pending/2026-08-15-suspend-programmatic-check-for-reshaped-artifacts.md
@experiments/e1_refractive_comparison.py
@experiments/_expectations.py
</context>

## Phase environment (all plans)

    PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python

python 3.11.15, cv2 4.13.0.92, numpy 2.4.6, scipy 1.17.1. There is no bare `python` on this
box's PATH — always use `$PY`.

## Hard constraints (all plans)

1. **`src/` must not be modified.** `git diff --stat rerun-freeze-01..HEAD -- src/` prints nothing.
2. **Do not regenerate any committed artifact under `experiments/results/`.** The corrected
   artifacts come from the re-run, which is out of scope. `git diff --quiet -- experiments/results/`
   must exit 0 for this plan.
3. **The driver exits non-zero on a healthy run** — acceptance is the roll-up, never `$?`.
4. **Do not add pre-flight override flags.**
5. **File ownership in this wave:** this plan owns `experiments/README.md`. Plan 29.1-04 runs
   in the same wave and must not edit it.

## Artifacts this phase produces

No new gate result identifiers are introduced anywhere in the phase.

| Artifact | Kind | Plan |
|---|---|---|
| `rerun-freeze-02` | annotated git tag (must NOT match `v*`) | 08 |
| `experiments/freeze01_run_output/` | directory (`git mv` target for the 2026-08-20 run output) | 06 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-GATE-BEFORE-AFTER.md` | file | 06 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-VERIFICATION-BAR.md` | file | 07 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-FREEZE-RECORD.md` | file | 08 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md` | file | 08 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md` | file | 03 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md` | file | 04 |
| `.planning/todos/pending/2026-08-20-e7-band-mirrors-e1-benchmark-overwrite-hazard.md` | new todo | 02 |
| 7 todo files moved `pending/` → `done/` with a `## Resolved` block | moved files | 01, 02, 04, 05 |
| `gate1_guard_count:benchmark_grid.csv` | EXISTING gate id; PASS message gains an exemption clause | 01 |
| `e1_seed_band_degeneracy_breakdown.json` | expectation REMOVED; its `completeness:` result leaves the roll-up | 02 |

<tasks>

<task type="auto">
  <name>Task 1: Unclaim e1_seed_band_degeneracy_breakdown.json from all three manifests</name>
  <files>experiments/suite_expectations.json, experiments/EXPECTATIONS.md, experiments/README.md</files>
  <read_first>
    - `experiments/suite_expectations.json` — the `e1_band` stage's `produces` list (~:444-449)
      and the artifact entry (~:716-731).
    - `experiments/_expectations.py:90-127` (`_validate_manifest`) — why both sites must move together.
    - `experiments/EXPECTATIONS.md` ~:186 and `experiments/README.md` ~:132 (stage 16's row).
    - `.planning/todos/pending/2026-08-15-suspend-programmatic-check-for-reshaped-artifacts.md`
      § *The new sidecar* — the authoritative writer table D-04 rests on.
  </read_first>
  <action>
    Per D-04, remove the artifact rather than adding a writer. The design table has `--seeds`
    rows for E5 and E7 and deliberately none for E1, and `_run_band`'s docstring and code
    already match the table.

    Remove all three claims:
    1. `experiments/suite_expectations.json` — drop the name from the `e1_band` stage's
       `produces` list AND drop the whole artifact object. `_validate_manifest` fails loudly
       if only one moves, which is the check that proves you did both.
    2. `experiments/EXPECTATIONS.md` — remove its row from the artifact table.
    3. `experiments/README.md` — remove it from stage 16's expected-artifacts column, leaving
       the three artifacts the band does write.

    Record D-05's evidence where a future reader will meet the question rather than only in
    the todo: add one short note beside the `e1_band` stage entry in `suite_expectations.json`
    (extend the stage `description`, do not invent a new key) saying that E1's band writes no
    degeneracy sidecar by design, that E1's synthetic scenario produces zero degenerate
    observations so the sidecar would carry per-seed denominators only, and that the design
    table — not emptiness — is the reason (E7's band sidecar is equally all-zero on real
    counters and is written anyway). Cite D-04/D-05.

    Do NOT add a `write_degeneracy_breakdown` call site. Do NOT alter E5's or E7's band
    sidecar entries.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; $PY -c "
from experiments._expectations import load_expectations
m = load_expectations()
names = {a['name'] for a in m['artifacts']}
assert not any('e1_seed_band_degeneracy' in n for n in names), sorted(n for n in names if 'e1_seed_band' in n)
stage = next(s for s in m['stages'] if s['id'] == 'e1_band')
assert not any('e1_seed_band_degeneracy' in p for p in stage['produces']), stage['produces']
assert {'e5_seed_band_degeneracy_breakdown.json', 'e7_seed_band_degeneracy_breakdown.json'} <= names
print('OK')
" && $PY experiments/check_rerun_gates.py experiments/results --profile full > /tmp/29_1_02_gate.txt 2>&1; ! grep -q 'e1_seed_band_degeneracy' /tmp/29_1_02_gate.txt && ! grep -qE '^\[FAIL\].*e1_band' /tmp/29_1_02_gate.txt && tail -1 /tmp/29_1_02_gate.txt | grep -qx 'TOTAL: 176 PASS, 7 N/A, 0 FAIL' && $PY -m pytest tests/unit/test_expectations.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `load_expectations()` succeeds — proof that both `suite_expectations.json` sites moved together (D-04).
    - No line of the full-profile gate output names `e1_seed_band_degeneracy_breakdown.json`,
      and no `e1_band` result is FAIL.
    - The roll-up reads exactly `TOTAL: 176 PASS, 7 N/A, 0 FAIL` — the phase's headline
      outcome, reached with plan 29.1-01's E4 fix already merged. If the totals differ, STOP
      and explain in the SUMMARY; do not adjust a gate to reach the number.
    - E5's and E7's band degeneracy sidecars are still claimed (no collateral unclaiming).
    - `EXPECTATIONS.md` and `README.md` stage 16 no longer name the artifact; all four
      documents (three manifests plus the design table) now agree.
    - `tests/unit/test_expectations.py` passes.
  </acceptance_criteria>
  <reversibility rating="reversible">Three manifest entries; re-adding them plus a writer is a normal change (D-04).</reversibility>
  <done>The second of the two production FAILs is gone and the roll-up is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the band's write policy enforced, and its log true</name>
  <files>experiments/e1_refractive_comparison.py, tests/unit/test_e1_band_mode.py, .planning/todos/pending/2026-08-20-e7-band-mirrors-e1-benchmark-overwrite-hazard.md</files>
  <read_first>
    - `experiments/e1_refractive_comparison.py` :1050-1090 (`_run_band` docstring), :1209-1213
      (the "must never overwrite" comment), :1279-1330 (the benchmark write loop and the
      unconditional print).
    - `experiments/_io.py` :874-893 — `write_direct_call_benchmark`'s skip-and-return-`False`.
    - `experiments/e1_refractive_comparison.py` :826-860 — `_run_full`'s write of the same two
      records, which passes `seed=args.seed`.
    - `experiments/suite_expectations.json` — stage `e1` (`--force`, produces both
      `e1_benchmark_*.json`) and stage `e1_band` (`depends_on: ["e1"]`, no `--force`).
    - `tests/unit/test_e1_band_mode.py` — the existing smoke-scale band fixtures.
  </read_first>
  <behavior>
    - A band run into an out_dir where `e1_benchmark_<model>.json` already exists leaves both
      files byte-identical, even when the run is given `--force`.
    - That same run prints a kept-existing line naming the path, and prints no line claiming a write.
    - A band run into an empty out_dir creates both records and prints a write confirmation
      naming each path.
    - The created records carry `solver_config["seeds"]` and `solver_config["seed"]`.
    - `exp1_band.csv`, `exp1_parameter_band.csv` and `e1_seed_band_provenance.json` are still
      written unconditionally (force implied for band output, D-19.4-14) — this change must
      not touch them.
  </behavior>
  <action>
    **The resolution chosen for D-07, and state it in the code so it is not re-derived.** The
    `:1209` comment, the manifest, and the band sidecar's own reason for existing all say band
    mode does not own these records; only the docstring and the call site dissent, and the
    docstring was written to describe the call site. Resolve in favour of the comment, in the
    form that keeps every reading true:

    > Band mode may CREATE `e1_benchmark_<model>.json` when it is absent, and must NEVER
    > overwrite one that exists — enforced at the call, not delegated to the resumability guard.

    Concretely, in `_run_band`'s benchmark loop:
    - Pass `force=False` unconditionally at this call site, ignoring the run's `--force`. Add a
      comment saying that this is the one place `--force` is deliberately not honoured, and
      why: `--force` is what would otherwise turn the policy in the comment above into a lie,
      silently republishing two records with the last seed's values. Cite D-07 and D-19.4-14.
    - Capture the writer's return value and branch the output on it (D-06): print the write
      confirmation only on `True`; on `False` print a distinct line naming the path and saying
      the existing single-seed record was kept because band mode never overwrites it. Do not
      re-emit the writer's own `logger.info` skip text verbatim — two identical claims in one
      log is the shape this defect had.
    - Update `_run_band`'s docstring so it stops asserting that the band writes both sidecars
      unconditionally; say it writes them only when absent, and that the band's own seed record
      lives in `e1_seed_band_provenance.json`.
    - Update the `:1209` comment so it states the policy as enforced rather than aspirational,
      and names the mechanism (`force=False` at the call).

    **Record two corrections the sources get wrong**, in the same comment, because both were
    relied on when the defect was filed:
    - The claim that a clean `experiments/results/` removes the skip does not hold for a
      full-suite run: `e1_band` `depends_on` `e1`, and stage `e1` runs with `--force`, so both
      records always exist by the time the band runs. The live hazards are a `--force` band run
      and a standalone band run into an out_dir where `e1` has not run.
    - `gate3_provenance` does not depend on this write: `_run_full` already stamps
      `seed=args.seed` onto both records.

    **E7 carries the identical call and the identical comment** (`e7_interface_ablation.py`
    :779, :860) and is deliberately out of this phase's scope. Do not edit it. Instead file
    `.planning/todos/pending/2026-08-20-e7-band-mirrors-e1-benchmark-overwrite-hazard.md`
    describing the same hazard at E7's site, naming the asymmetry this task introduces and the
    resolution E1 took, so the next person to touch E7's band finds it. Note the asymmetry in
    E1's comment too.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; $PY -m pytest tests/unit/test_e1_band_mode.py -q 2>&1 | tail -3 && test -f .planning/todos/pending/2026-08-20-e7-band-mirrors-e1-benchmark-overwrite-hazard.md && $PY -c "
import ast, pathlib
src = pathlib.Path('experiments/e1_refractive_comparison.py').read_text()
tree = ast.parse(src)
fn = next(n for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name == '_run_band')
calls = [n for n in ast.walk(fn) if isinstance(n, ast.Call) and getattr(n.func, 'id', '') == 'write_direct_call_benchmark']
assert len(calls) == 1, len(calls)
kw = {k.arg: k.value for k in calls[0].keywords}
assert 'force' in kw and isinstance(kw['force'], ast.Constant) and kw['force'].value is False, ast.dump(kw['force'])
assert any(isinstance(n, ast.If) for n in ast.walk(fn)), 'expected a branch on the writer return'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Every behaviour in `<behavior>` has a named test in `tests/unit/test_e1_band_mode.py`,
      including the `--force` case, and all pass.
    - `_run_band`'s single `write_direct_call_benchmark` call passes the literal `False` for
      `force` — the policy is enforced by the call, not by the resumability guard (D-07).
    - Output is branched on the writer's return value: a write confirmation only on a real
      write, a distinct kept-existing line otherwise (D-06).
    - `_run_band`'s docstring, the `:1209` comment and the call site all state the same policy.
    - The comment records both source corrections (the dependency-ordering fact, and that
      `gate3_provenance` is satisfied by `_run_full`).
    - `experiments/e7_interface_ablation.py` is unmodified, and the E7 follow-up todo exists
      and names the asymmetry.
    - The three band-owned outputs (`exp1_band.csv`, `exp1_parameter_band.csv`,
      `e1_seed_band_provenance.json`) still write unconditionally.
  </acceptance_criteria>
  <reversibility rating="reversible">A one-argument change plus a branch; reverting restores the prior call exactly (D-07).</reversibility>
  <done>No log line claims a write that did not happen, and no band run can overwrite a single-seed benchmark record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Derive the band's scope field from the run instead of freezing it into a literal</name>
  <files>experiments/e1_refractive_comparison.py, tests/unit/test_stale_provenance_strings.py, .planning/todos/done/2026-08-20-three-manifests-expect-an-e1-band-sidecar-the-design-never-specified.md, .planning/todos/done/2026-08-20-e1-band-logs-Wrote-for-writes-that-were-skipped.md, .planning/todos/done/2026-08-20-e1-band-scope-string-still-claims-ten-seeds-after-ruling-A1-cut-it-to-four.md</files>
  <read_first>
    - `experiments/e1_refractive_comparison.py` :1215-1275 — the `e1_seed_band_provenance.json`
      writer and the `scope` literal.
    - `experiments/run_experiment_suite.sh` :1452-1475 (`run_stage_e1_band`) — ruling A1 and the
      row-count arithmetic — and :354-357 (`E1_BAND_SEEDS`).
    - `.planning/todos/pending/2026-08-20-e1-band-scope-string-still-claims-ten-seeds-after-ruling-A1-cut-it-to-four.md`
      — the exact stale clauses (do not copy them forward).
    - `tests/unit/test_stale_provenance_strings.py` — the established claim-sentence gate
      pattern: assertions scoped to named files, phrased on sentences, never on bare numbers.
  </read_first>
  <behavior>
    - The written `scope` string reports the seed count and the seed list that the run actually
      swept, taken from `seeds`.
    - It reports the row counts of the two band CSVs this run emitted, taken from the emitted
      frames, not from a literal.
    - It reports the noise levels and depths this run used, so a `--smoke` band's scope is
      truthful about its collapsed axes rather than describing the production grid.
    - It cites ruling A1 by name as the reason the seed axis is the size it is.
    - It carries no forward-looking schedule clause naming a phase that "will" execute or
      verify the band.
    - `tests/unit/test_stale_provenance_strings.py` fails if any pre-ruling-A1 claim sentence
      reappears in `experiments/e1_refractive_comparison.py`.
  </behavior>
  <action>
    D-08 asks for four seeds and 256/384 rows. D-10 explains why a literal is the wrong shape:
    three stale rationales surfaced in one day and each was a claim outliving the conditions
    that produced it. So fix the class here, not just the instance — this field's whole job is
    stating the domain a published number may be quoted over.

    Rebuild the `scope` value as an f-string computed at write time from values already in
    scope in `_run_band`: the resolved `seeds` list and its length, the row counts of the band
    frame and the parameter-band frame, the effective `noise_levels`, and the effective depth
    list. Cite ruling A1 (`run_experiment_suite.sh`'s `run_stage_e1_band`) in prose as the
    reason the seed axis is sized as it is — a stable cross-reference is a citation, not a
    recomputed value, and stays.

    Keep, unchanged in substance, the parts of the current string that are genuinely static
    claims rather than measurements: the `realistic` 12-camera synthetic-geometry restriction,
    the warm-restart evidence that the non-refractive baseline is converged, the
    ill-conditioning caveat that travels with it (D-16), and D-19.3-17's qualified demotion of
    E1's accuracy claim with E2 carrying the accuracy claim against reality.

    Drop the forward-looking clause that names a phase as the one that "will" execute and
    verify the band. It is a schedule, not a domain, and it is the half of this string that
    will need re-dating after every run — which is exactly the defect.

    Then extend `tests/unit/test_stale_provenance_strings.py` with a claim-sentence gate for
    this file, following that module's existing conventions: anchor the repo root to the test
    file's own location, assert on whole claim sentences taken from the defect todo, and add a
    docstring note that this test file necessarily contains those sentences so any grep-based
    check elsewhere must stay filename-scoped.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; $PY -m pytest tests/unit/test_stale_provenance_strings.py tests/unit/test_e1_band_mode.py -q 2>&1 | tail -3 && $PY -c "
import pathlib, re
src = pathlib.Path('experiments/e1_refractive_comparison.py').read_text()
i = src.index('\"scope\"')
window = src[i:i + 4000]
assert 'ruling A1' in window or 'RULING A1' in window, 'ruling A1 must be cited in the scope field'
assert re.search(r'\{[^}]*seeds[^}]*\}', window), 'scope must interpolate the resolved seed list/count'
assert re.search(r'\{len\(', window), 'scope must interpolate at least one computed row count'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Every behaviour in `<behavior>` has a named test and all pass.
    - The `scope` field interpolates the resolved seed list/count and both emitted row counts
      rather than quoting literals (D-08, D-10).
    - The string cites ruling A1 for the seed-axis size (D-08).
    - No forward-looking "will be executed / will be verified in phase N" clause survives.
    - The static claims listed in the action (scenario restriction, warm-restart evidence,
      ill-conditioning caveat, D-19.3-17 qualification) are preserved in substance.
    - `tests/unit/test_stale_provenance_strings.py` gains a filename-scoped claim-sentence gate
      for `experiments/e1_refractive_comparison.py` and passes.
    - Close all three E1 defect todos: `git mv` each from `.planning/todos/pending/` to
      `.planning/todos/done/` with a `## Resolved` block naming the decision ids
      (D-04/D-05 for the sidecar, D-06/D-07 for the write policy, D-08 for the scope field),
      the resolution chosen for D-07, and the tests that pin each.
  </acceptance_criteria>
  <reversibility rating="reversible">A string construction change in one writer (D-08).</reversibility>
  <done>The band's provenance sidecar states the domain that was run, and cannot silently describe a different one next time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expectation manifest → gate verdict | Removing an expectation removes a check; the removal must be the design's, not a convenience. |
| band run → committed single-seed benchmark records | A band write can silently republish two records another stage owns. |
| provenance sidecar → published claim | `scope` states the domain a manuscript number may be quoted over. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-04 | Tampering | `suite_expectations.json` | high | mitigate | The removal is authorised by the 2026-08-15 design table (D-04) and is scoped to one artifact; task 1's verify asserts E5's and E7's band sidecars are still claimed, so a broader unclaiming fails its own check. |
| T-29.1-05 | Tampering | `e1_benchmark_<model>.json` | high | mitigate | `force=False` at the call site makes overwrite impossible from band mode; task 2 pins a `--force` test proving byte-identity. |
| T-29.1-06 | Repudiation | `e1_seed_band_provenance.json` `scope` | high | mitigate | Deriving the domain from the run removes the class of defect; a claim-sentence gate in `tests/unit/test_stale_provenance_strings.py` blocks reintroduction. |
| T-29.1-07 | Information disclosure | interpolated scope string | low | accept | The interpolated values (seed list, row counts, noise levels, depths) are already published in the sibling CSVs. |
| T-29.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing. See plan 29.1-04 for the phase-level ruling; the only installs in this phase are extras already declared in this repository's own `pyproject.toml`. |
</threat_model>

<verification>
- `$PY experiments/check_rerun_gates.py experiments/results --profile full | tail -1` reads
  `TOTAL: 176 PASS, 7 N/A, 0 FAIL`.
- `$PY -m pytest tests/unit/test_e1_band_mode.py tests/unit/test_expectations.py tests/unit/test_stale_provenance_strings.py -q` passes.
- `git diff --quiet -- experiments/results/` exits 0.
- `git diff --quiet -- experiments/e7_interface_ablation.py` exits 0.
- `git diff --stat rerun-freeze-01..HEAD -- src/` prints nothing.
</verification>

<success_criteria>
SC-2, SC-3 and SC-4 are satisfied, and with SC-1 already landed the end-of-run roll-up over
the 2026-08-20 tree reaches 0 FAIL without any gate being weakened.
</success_criteria>

<output>
Create `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-02-SUMMARY.md` when done.
</output>
