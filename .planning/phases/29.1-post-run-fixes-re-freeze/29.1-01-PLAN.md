---
phase: 29.1-post-run-fixes-re-freeze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/e4_benchmark_grid.py
  - experiments/check_rerun_gates.py
  - tests/unit/test_experiments_e4.py
  - tests/unit/test_rerun_gates.py
  - .planning/todos/pending/2026-08-20-e4-real-rig-row-nulls-a-guard-count-that-now-exists.md
  - .planning/todos/done/2026-08-20-e4-real-rig-row-nulls-a-guard-count-that-now-exists.md
autonomous: true
requirements: [SC-1]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running the full-profile gate over experiments/results reports 0 FAIL for E4's gate1_guard_count:benchmark_grid.csv (D-01, D-02)."
    - "The gate's own PASS line names the exempt row and why it is exempt, so a reader never has to open the source to learn that a pipeline row carries an ungated count (D-02)."
    - "_extract_pipeline_row returns 198 for the committed E2 benchmark record instead of None, so the next run publishes the value (D-01)."
    - "No comment anywhere in e4_benchmark_grid.py still claims E2's benchmark.json predates discard_stats threading (D-03)."
    - "The real-rig row's published status is still 'ok', not 'degenerate' -- the > 0 downgrade did not reach it (D-01)."
  artifacts:
    - experiments/e4_benchmark_grid.py
    - experiments/check_rerun_gates.py
    - tests/unit/test_experiments_e4.py
    - tests/unit/test_rerun_gates.py
    - .planning/todos/done/2026-08-20-e4-real-rig-row-nulls-a-guard-count-that-now-exists.md
  key_links:
    - "e4_benchmark_grid._extract_pipeline_row -> benchmark_grid.csv's degenerate_observations_at_solution cell -> check_rerun_gates._check_guard_column -> the end-of-run roll-up. Breaking any link converts one FAIL into a different FAIL (D-02)."
    - "build_grid_dataframe's `> 0` publish rule applies to record_source='assembled' rows only; the pipeline row is assembled in a separate branch that never reaches it. That separation is currently implicit and must become explicit (D-01)."
---

<objective>
Make E4's real-rig row stop failing Gate 1, by publishing the guard count E2 already
measured (198) and by teaching Gate 1 that a `record_source="pipeline"` row carries a
count it does not gate on.

Purpose: this is one of the two real FAILs in the 2026-08-20 production roll-up, and it is
produced by a code comment describing a state of the world that ended when Phase 24's
`discard_stats` threading landed. Both halves must land together: populating the field
alone makes Gate 1 fail the "non-zero" half of its predicate exactly as the null failed the
"missing" half (D-02).

Output: a corrected writer, a corrected gate, the rationale replaced at both sites, and
regression tests that pin both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-post-run-fixes-re-freeze/29.1-CONTEXT.md
@.planning/todos/pending/2026-08-20-e4-real-rig-row-nulls-a-guard-count-that-now-exists.md
@experiments/e4_benchmark_grid.py
@experiments/check_rerun_gates.py
</context>

## Phase environment (all plans)

The interpreter for every command in this plan is the verified conda env:

    PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python

python 3.11.15, cv2 4.13.0.92, numpy 2.4.6, scipy 1.17.1. Do not use a bare `python` —
this box has none on PATH.

## Hard constraints (all plans)

1. **`src/` must not be modified.** `git diff --stat rerun-freeze-01..HEAD -- src/` must
   print nothing at the end of this phase.
2. **Do not regenerate any committed artifact under `experiments/results/`.** This phase
   fixes code and manifests; the corrected artifacts are produced by the re-run, which is
   out of scope. `git diff --stat rerun-freeze-01..HEAD -- experiments/results/` must show
   only what Phase 29 already committed.
3. **The driver exits non-zero on a healthy run.** Never read `$?` from
   `run_experiment_suite.sh` as an acceptance signal — read the end-of-run roll-up.
4. **Do not add pre-flight override flags** (`--skip-e2` in particular silently makes a run
   synthetic-only).

## Artifacts this phase produces

Every symbol, file, directory and tag this phase creates or renames. No new gate result
identifiers are introduced anywhere in the phase.

| Artifact | Kind | Plan |
|---|---|---|
| `rerun-freeze-02` | annotated git tag (must NOT match `v*`) | 08 |
| `experiments/freeze01_run_output/` | directory (`git mv` target for the 2026-08-20 run output) | 06 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-GATE-BEFORE-AFTER.md` | file | 06 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-VERIFICATION-BAR.md` | file | 07 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-FREEZE-RECORD.md` | file | 08 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md` | file | 08 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md` | file | 03 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md` | file | 04 |
| `.planning/todos/pending/2026-08-20-e7-band-mirrors-e1-benchmark-overwrite-hazard.md` | new todo | 02 |
| 7 todo files moved `pending/` → `done/` with a `## Resolved` block | moved files | 01, 02, 04, 05 |
| `gate1_guard_count:benchmark_grid.csv` | EXISTING gate id; its PASS message gains the exemption clause | 01 |
| `e1_seed_band_degeneracy_breakdown.json` | expectation REMOVED; its `completeness:` result leaves the roll-up | 02 |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end -- the real-rig guard count reaches the grid and Gate 1 stops failing on it</name>
  <files>experiments/e4_benchmark_grid.py, experiments/check_rerun_gates.py</files>
  <read_first>
    - `experiments/e4_benchmark_grid.py` lines 1345-1412 (`_extract_pipeline_row`) and
      1495-1590 (`build_grid_dataframe`'s publish rule and the separate E2 row branch).
    - `experiments/check_rerun_gates.py` lines 592-620 (`_check_guard_column`) and 714-772
      (`check_e4`, the only caller for `benchmark_grid.csv`).
    - `.planning/todos/pending/2026-08-20-e4-real-rig-row-nulls-a-guard-count-that-now-exists.md`
      — the three resolutions considered and why option 3-plus-publish was chosen.
  </read_first>
  <action>
    **Branch first, before any edit (D-13).** This clone is currently sitting on
    `results/rerun-freeze-01` itself. Create and switch to a phase work branch based on it, so
    the phase's commits land on a branch rather than on the results branch, and so the base
    keeps `experiments/results/` present in the working tree for verification. Basing anywhere
    else would remove those 147 tracked files from the tree, because they exist only on this
    branch — the "merge, never checkout" hazard. This is the first action of the first task of
    the first wave; every later plan assumes it happened.

    Wire one path through both layers, in one task, because either half alone leaves a FAIL
    (D-02).

    **Writer half (D-01).** In `_extract_pipeline_row`, replace the hardcoded `None` for
    `degenerate_observations_at_solution` with the value read from the E2 record. Read it
    from `discard_stats` first and fall back to `problem_shape`; the committed record
    carries 198 under both keys. Use the existing `_get_nested` helper rather than adding a
    new accessor. If neither key is present, keep `None` — an un-instrumented record must
    stay distinguishable from a measured zero, which is the property Gate 1 was built on.

    **Gate half (D-02).** Teach `_check_guard_column` that rows whose `record_source` is
    `"pipeline"` carry a count it does not gate on. Chosen mechanism, and state it in the
    docstring so the next reader does not re-derive it: keep ONE gate result (do not add a
    second gate id — the roll-up's arithmetic is an audited number) and exclude pipeline
    rows from `bad_mask` when a `record_source` column is present. The exemption must be
    visible in the gate's own output: the PASS message names how many rows were gated, how
    many were exempt, each exempt row's `cell_key` and its count, and one clause saying why
    a pipeline row is exempt — it is a real-hardware anchor row whose count the library
    itself declines to treat as a verdict at 0.268% (`pipeline.py:1288`). When no
    `record_source` column exists, behaviour is unchanged: every row is gated.

    Do NOT weaken the predicate for `record_source="assembled"` rows. A synthetic cell with
    a non-zero or missing count must still FAIL.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; $PY experiments/check_rerun_gates.py experiments/results --profile full 2>&1 | tail -1 | grep -qx 'TOTAL: 176 PASS, 7 N/A, 1 FAIL' && $PY experiments/check_rerun_gates.py experiments/results --profile full 2>&1 | grep -E 'gate1_guard_count:benchmark_grid\.csv' | grep -q '^\[PASS\]' && $PY -c "
import json
from experiments.e4_benchmark_grid import _extract_pipeline_row
rec = json.load(open('experiments/results/benchmark.json'))
row = _extract_pipeline_row(rec)
assert row['degenerate_observations_at_solution'] == 198, row['degenerate_observations_at_solution']
assert row['record_source'] == 'pipeline'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - The working tree is on a phase work branch whose merge base is `results/rerun-freeze-01`,
      and `experiments/results/` still holds its 147 tracked files (D-13).
    - The full-profile roll-up over `experiments/results` reads exactly
      `TOTAL: 176 PASS, 7 N/A, 1 FAIL` — one more PASS than the 2026-08-20 baseline of
      175/7/2, and the single surviving FAIL is `e1_band completeness:e1_seed_band_degeneracy_breakdown.json`,
      which plan 29.1-02 owns. If the totals differ, STOP and explain the difference in the
      SUMMARY; do not adjust the gate to reach the number.
    - `E4 gate1_guard_count:benchmark_grid.csv` is `[PASS]` and its message names the
      exempt row `real_rig_13cam_200fr`, its count, and the reason for the exemption (D-02).
    - `_extract_pipeline_row` returns `198` for the committed `experiments/results/benchmark.json` (D-01).
    - A synthetic (`record_source="assembled"`) row with a non-zero count still FAILs — no
      blanket relaxation (D-02).
    - `experiments/results/benchmark_grid.csv` is byte-identical to its committed state: the
      CSV is NOT regenerated by this plan. `git diff --quiet -- experiments/results/` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">D-01 makes a CSV column populated for a row that has always been null; `benchmark_grid.tex` and any downstream reader must tolerate a value there, and reverting means re-nulling a number the manuscript may by then cite.</reversibility>
  <done>The gate roll-up moves from 2 FAIL to 1 FAIL, with the E4 half green and the reason legible in the gate's output.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the stale rationale, and make the pipeline row's exemption explicit at both sites</name>
  <files>experiments/e4_benchmark_grid.py</files>
  <read_first>
    - The comment block immediately above `degenerate_observations_at_solution` in
      `_extract_pipeline_row` (`experiments/e4_benchmark_grid.py`, ~:1376-1381) — the D-26
      claim being replaced.
    - `experiments/e4_benchmark_grid.py` ~:1505-1531 — the `> 0` publish rule, and the
      separate `e2_row` branch below it that hardcodes `status="ok"`.
    - `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-CONTEXT.md` § D-03.
  </read_first>
  <action>
    D-03 asks for a replacement, not a deletion: the old claim was true when written and was
    overtaken by Phase 24, and the next reader needs to be told what is true now.

    **Site 1 — `_extract_pipeline_row`.** Replace the D-26 comment with a comment that
    states: the value is now read from E2's own record (both `discard_stats` and
    `problem_shape` carry it since Phase 24's DEGEN-02 instrumentation); what the count is
    (real-hardware above-water board corners — 198 of 198 `above_interface`, 0
    `behind_camera`, 0 `interface_below_camera`, all `stage3_intrinsic_pass`, all continued
    via the pinhole extension); and that the row is deliberately exempt from the `> 0`
    status downgrade, naming the gate-side counterpart so the two sites point at each other.
    Cite D-01 and D-03. Do not restate the retired claim even as history — the replacement
    describes the present.

    **Site 2 — the publish rule in `build_grid_dataframe`.** The `> 0` downgrade currently
    sits inside the declared-cell loop and the E2 row is assembled in a separate branch
    below it, so the exemption is real but purely structural — nothing says it is
    intentional, and a future refactor that unified the two branches would silently flip the
    anchor row to `status="degenerate"`. Add a comment at the downgrade stating that it
    applies to `record_source="assembled"` rows only, that the pipeline row is exempt by
    design, and why (the library declines to call 0.268% a verdict; on measured hardware the
    remedy — reposition the board — is unavailable retrospectively, so a re-run reports the
    same count and the status must be one the project can live with permanently). Cite D-01.

    Where the two comments overlap, keep each self-contained; a reader arriving at either
    site must not need the other to understand the row.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; $PY -c "
import pathlib, re
src = pathlib.Path('experiments/e4_benchmark_grid.py').read_text()
comments = '\n'.join(l for l in src.splitlines() if l.lstrip().startswith('#'))
for token in ('D-01', 'D-03', 'above_interface', 'record_source'):
    assert token in comments, token
assert 'DEGEN-02' in comments or 'Phase 24' in comments
print('OK')
" && $PY -m pytest tests/unit/test_experiments_e4.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `_extract_pipeline_row`'s comment states the present-day source of the value, what the
      count physically is, and that the row is exempt from the downgrade, citing D-01/D-03.
    - The publish rule carries a comment scoping it to `record_source="assembled"` rows and
      naming the pipeline exemption as intentional, citing D-01.
    - Each comment is readable standalone and points at its counterpart.
    - `tests/unit/test_experiments_e4.py` still passes.
  </acceptance_criteria>
  <done>Both sites document the exemption; no comment in the file still describes the pre-Phase-24 world.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin both halves with regression tests, and close the defect todo</name>
  <files>tests/unit/test_experiments_e4.py, tests/unit/test_rerun_gates.py, .planning/todos/done/2026-08-20-e4-real-rig-row-nulls-a-guard-count-that-now-exists.md</files>
  <read_first>
    - `tests/unit/test_experiments_e4.py` — existing fixtures and how it builds a fake E2 record.
    - `tests/unit/test_rerun_gates.py` — existing `_check_guard_column` / `check_e4` coverage
      and its DataFrame construction helpers.
  </read_first>
  <behavior>
    - `_extract_pipeline_row` on a record carrying `discard_stats.degenerate_observations_at_solution = N`
      returns `N` (not `None`).
    - `_extract_pipeline_row` on a record carrying the count only under `problem_shape`
      returns it from there.
    - `_extract_pipeline_row` on a record carrying it in neither place returns `None` — the
      un-instrumented case stays distinguishable from a measured zero.
    - `_check_guard_column` on a frame with one `record_source="pipeline"` row holding a
      non-zero count and the rest `assembled` at zero returns PASS, and the message names
      the exempt row.
    - `_check_guard_column` on a frame where an `assembled` row holds a non-zero count
      returns FAIL even if a pipeline row is also present.
    - `_check_guard_column` on a frame where an `assembled` row's count is missing returns
      FAIL — the missing-is-not-zero rule is unchanged.
    - `_check_guard_column` on a frame with no `record_source` column keeps its pre-change
      behaviour exactly.
    - `build_grid_dataframe` leaves the real-rig row at `status="ok"` when its count is
      non-zero, while a synthetic cell with a non-zero count still becomes `"degenerate"`.
  </behavior>
  <action>
    Write the tests above first and watch them fail against the pre-change behaviour where
    that is meaningful, then confirm they pass against the code from tasks 1-2. Put the
    `_extract_pipeline_row` and `build_grid_dataframe` cases in
    `tests/unit/test_experiments_e4.py` and the gate cases in
    `tests/unit/test_rerun_gates.py`, following each file's existing fixture style rather
    than introducing a new one.

    Then close the defect todo: `git mv` it from `.planning/todos/pending/` to
    `.planning/todos/done/` and append a `## Resolved` block naming the resolution taken
    (option 3 extended — the value is published AND the pipeline row is exempted, at both
    sites), the decision ids D-01/D-02/D-03, the before/after roll-up totals, and the two
    test files that now pin it.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; $PY -m pytest tests/unit/test_experiments_e4.py tests/unit/test_rerun_gates.py -q 2>&1 | tail -3 && test -f .planning/todos/done/2026-08-20-e4-real-rig-row-nulls-a-guard-count-that-now-exists.md && ! test -f .planning/todos/pending/2026-08-20-e4-real-rig-row-nulls-a-guard-count-that-now-exists.md && grep -q '## Resolved' .planning/todos/done/2026-08-20-e4-real-rig-row-nulls-a-guard-count-that-now-exists.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Every behaviour listed in `<behavior>` has a named test.
    - `pytest tests/unit/test_experiments_e4.py tests/unit/test_rerun_gates.py` passes with 0 failures.
    - The defect todo is in `.planning/todos/done/` with a `## Resolved` block citing
      D-01/D-02/D-03 and the roll-up before/after (175/7/2 → 176/7/1 at this plan's boundary).
    - No test asserts on `experiments/results/` content in a way that would break when the
      re-run regenerates it — fixtures are synthetic, not the committed tree.
  </acceptance_criteria>
  <done>Both halves are pinned by tests and the todo is discharged with evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed artifact → gate verdict | `benchmark_grid.csv` is read by `check_rerun_gates.py`; a gate change is a change to what the project will accept as evidence. |
| experiment writer → published CSV column | `_extract_pipeline_row` decides what reaches a Zenodo-bound artifact and `benchmark_grid.tex`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-01 | Tampering | `_check_guard_column` | high | mitigate | The exemption is keyed to `record_source == "pipeline"` only, and Task 3 pins a FAIL case for an `assembled` row with a non-zero count and for one with a missing count. A blanket relaxation would pass those tests only by deleting them. |
| T-29.1-02 | Repudiation | `experiments/results/benchmark_grid.csv` | high | mitigate | The committed CSV is not regenerated. `git diff --quiet -- experiments/results/` is an acceptance criterion on task 1, so a "fix" that edits the artifact instead of the code fails its own verify. |
| T-29.1-03 | Information disclosure | gate PASS message | low | accept | The exemption message names a cell key and a count already published in `benchmark_grid.csv`; no new information is disclosed. |
| T-29.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing. No RESEARCH.md Package Legitimacy Audit is required because no package-manager install task exists here; the phase-level ruling is recorded in plan 29.1-04 (repeated in 29.1-08), and the only installs anywhere in this phase are extras already declared in this repository's own `pyproject.toml`. Precedent: `19.1-07-PLAN.md` T-19.1-SC. |
</threat_model>

<verification>
- `$PY experiments/check_rerun_gates.py experiments/results --profile full | tail -1` reads
  `TOTAL: 176 PASS, 7 N/A, 1 FAIL`.
- `$PY -m pytest tests/unit/test_experiments_e4.py tests/unit/test_rerun_gates.py -q` passes.
- `git diff --quiet -- experiments/results/` exits 0 (no committed artifact was touched).
- `git diff --stat rerun-freeze-01..HEAD -- src/` prints nothing.
</verification>

<success_criteria>
SC-1 is satisfied: E4's real-rig row no longer fails Gate 1, the count E2 measured is
published rather than nulled, the anchor row is not downgraded to `degenerate`, and the
rationale at both sites describes the world that exists.
</success_criteria>

<output>
Create `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-01-SUMMARY.md` when done.
</output>
