---
phase: 29.1-post-run-fixes-re-freeze
plan: 08
subsystem: release-engineering
tags: [freeze, tag, verification, exposure-audit, SC-5, SC-6]
status: complete

# Dependency graph
requires:
  - phase: 27-frozen-single-sha-handoff-package
    plan: "13"
    provides: "attempt 1's on-target verification procedure and its numbered attempt log -- the shape this plan repeats and the log it extends"
  - phase: 29.1-post-run-fixes-re-freeze
    plan: "04"
    provides: "the corrected HANDOFF.md section 1.2 install command -- the instructions this plan proves by executing them"
  - phase: 29.1-post-run-fixes-re-freeze
    plan: "06"
    provides: "the emptied experiments/results/ that the tag must ship for the re-run's pre-flight to accept it"
  - phase: 29.1-post-run-fixes-re-freeze
    plan: "07"
    provides: "the verification bar re-run, including the three test failures this plan had to rule on"
provides:
  - "rerun-freeze-02, an annotated tag at 7005a27, cut, verified from a fresh clone, approved and PUSHED -- the tag that licenses the v2.1 re-run"
  - "29.1-PREPUSH-AUDIT.md -- the exposure scan and the explicit D4 ruling"
  - "29.1-FREEZE-RECORD.md -- the freeze table, the fresh-clone verification, and the hazards the next operator needs"
  - "a two-row attempt log in 27-FREEZE-RECORD.md"
affects:
  - "the v2.1 full-suite re-run, which this tag licenses once pushed"
  - "RUN-05 (the Zenodo results package), which waits on that re-run"

tech-stack:
  added: []
  patterns:
    - "verify the tag from the artifact a receiving machine actually gets -- a fresh clone, not the working copy"
    - "prove a documentation correction by executing it, never by diffing the string"
    - "measure the one-way door's trigger pattern with the tool's own glob engine, not by eye"

key-files:
  created:
    - .planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md
    - .planning/phases/29.1-post-run-fixes-re-freeze/29.1-FREEZE-RECORD.md
  modified:
    - .planning/phases/27-frozen-single-sha-handoff-package/27-FREEZE-RECORD.md
    - .planning/phases/29.1-post-run-fixes-re-freeze/deferred-items.md
    - .planning/WINDOWS.md

decisions:
  - "Task 1's decision checkpoint was executed as measurement plus the dispatch's explicit pre-authorization of proceed-as-decided, rather than as an interactive stop. Every measurement it asks for was taken before any tag object existed, and all of it is presented at the Task 3 checkpoint -- which is where the irreversible step actually is."
  - "D4 ruled on rather than resolved: the tag is cut with three known exact-equality anchor failures, recorded as a reasoned exception in the same shape attempt 1 ruled on its 17 GATE FAIL findings. Nothing was deselected, xfailed, skipped, loosened or regenerated."
  - "The fresh-clone verification took the 4.35 GB Zenodo fetch cold rather than seeding the cache from attempt 1's clone, because re-proving that the pinned record still resolves is load-bearing before licensing a run."
  - "PRELAUNCH_GATE_PYTHON was set explicitly (D-28), unlike plan 29.1-07's bar run which left it unset and was saved only by PATH resolution."
  - "The remote branch results/rerun-freeze-01 was deliberately NOT fast-forwarded (author's ruling): it stays at 89c2092 as attempt 1's preserved endpoint, on the same principle that never moves a tag. Recorded in all three freeze records so the lagging pointer is not later read as an oversight."

metrics:
  duration: "~80 min"
  completed: 2026-08-24

actuals:
  tokens: 16604
  tasks: 3
  commits: 8
---

# Phase 29.1 Plan 08: Cut and Verify `rerun-freeze-02` Summary

**`rerun-freeze-02` is public at `7005a27`, proven before it was pushed from the artifact a receiving machine actually gets — a fresh clone, in an environment built by executing the tag's own corrected install command, clearing a `--smoke` roll-up of 72 PASS / 18 N/A / 0 FAIL with all 20 stages at exit 0 — and cut with three known test failures ruled on explicitly rather than made to disappear. The push created exactly the two refs its dry run predicted, `rerun-freeze-01` is untouched, and no workflow fired.**

## What was done

### Task 1 — the decision gate, measured before any tag object existed

Every measurement the checkpoint asks for was taken while no tag existed, so nothing was
foreclosed by the act of measuring:

| Evidence | Measured |
|---|---|
| `publish.yml` trigger, read from the file | `push: tags: ['v*']` |
| Proposed name against it | `rerun-freeze-02` — **does not match**, confirmed with `git for-each-ref 'refs/tags/v*'` (20 refs, not among them) rather than by eye |
| The other four workflow files | `release.yml` and `test.yml` on `main`; `docs.yml` on PRs to `main`; `slow-tests.yml` `workflow_dispatch` only — **fires nothing**, not "fires nothing I looked at" |
| `rerun-freeze-01` object id | tag `b31c8020403c11609b1fea5d330c29b58d902914` → commit `3ab9c13723202a58bb50e351b0b6bc0c0ffcd59c` |
| Version-tag count | 20 (`v*` and `v[0-9]*` alike) |
| Sha to be tagged | `7005a27` — *docs(29.1-07): record verification-bar completion in project state*, tree clean, `git describe` = `v2.0.1-346-g7005a27` with no `-dirty` |

One fact worth surfacing that the plan did not ask for: **`7005a27` differs from `a833a15`,
the sha plan 29.1-07's bar measured, only in `.planning/`.**
`git diff a833a15..HEAD -- . ':(exclude).planning'` prints nothing, so every byte of `src/`,
`experiments/`, `tests/` and `.github/` that the bar exercised is identical at the tagged sha.

### Task 2 — the tag, then the fresh-clone verification

The annotated tag was cut at `7005a27` (tag object `533f79fb…`), with a message naming the
phase, the seven defects closed, the predecessor it replaces, **and the three-test exception**.
The three invariants were re-asserted immediately: `rerun-freeze-01` unchanged, version-tag
count unchanged at 20, and `git describe --match 'v[0-9]*'` still `v2.0.1-346-g7005a27`.

Then verified from `/home/tlancaster/aquacal-frozen-rerun-freeze-02`, a `--no-hardlinks` clone
at the tag:

| Check | Result |
|---|---|
| Clone HEAD | `7005a277…` — the tagged commit |
| `experiments/results/` | **0 files** — the directory does not exist |
| `experiments/results_e2_band/` | absent → `gate_e2_band` lands in its `N/A` state, as 29.1-07 predicted |
| Environment | **new** conda env `aquacal-freeze02-clone`, built by executing the clone's own §1.2 |
| Import path | `<clone>/src/aquacal/__init__.py` — machine-checked for containment, not assumed |
| Pins | cv2 **4.13.0**, numpy 2.4.6, scipy 1.17.1, pytest 9.1.1, psutil 7.2.2 |
| Dry run | exit 0, 20/20, no override flags, `git status` empty afterwards |
| **`--smoke` roll-up** | **`TOTAL: 72 PASS, 18 N/A, 0 FAIL`** |
| Stages | **20/20 at exit 0**; `STAGE FAILED` lines: **0**; sticky `GATE FAIL`: 17, identical to attempt 1 |
| `gate3_run_manifest_clean_tree` | PASS |
| `gate3_git_sha_consistency` | PASS — every artifact carries `7005a277…` |
| Pre-flight frameset identity | **PASSED, `MATCH`** — `262 usable → 52 validation → 7762 comparisons` against the real image set |
| Manifest sha | `7005a2771aa115e4f4c1284cec7e145739586a4a`, `git_describe: v2.0.1-346-g7005a27`, `git_dirty: False` |

The plan's automated verify returns `OK`.

**SC-5 is proven at the suite level, not merely at the import.** The two failures the corrected
install command exists to prevent were both checked in the run itself: `e3` **exited 0** (it
exited 1 with `ModuleNotFoundError: No module named 'pytest'` on attempt 1's target), and
`gate3_run_manifest_fields` **PASSED** — *"all 17 required environment fields are present and
non-null"*, with `cpu_count_logical: 32` and `ram_total_bytes: 33351241728` populated by the
`bench` extra's psutil.

### Task 3 — the exposure audit, the approved push, and the records

- **`29.1-PREPUSH-AUDIT.md`** — the D4 ruling (§1), carry-forward rulings for D1 and D3, the
  fresh-clone summary, the exposure scan, the tag invariants, the push refspec with its dry
  run, the author's approval (§7.5), the ruling on `results/rerun-freeze-01` (§7.4) and the
  post-push assertions measured against the remote (§7.6).
- **`29.1-FREEZE-RECORD.md`** — the freeze table, the tag-name rationale, the full fresh-clone
  verification, the roll-up-is-acceptance ruling restated, and six hazards the next operator
  needs.
- **`27-FREEZE-RECORD.md`** — attempt 2 appended as row 2; attempt 1 marked **superseded**, its
  tag untouched.
- **`deferred-items.md`** — D4 closed out as *ruled on, not fixed*; D5 recorded as reproduced
  live; D6's prediction confirmed by measurement.
- **`.planning/WINDOWS.md`** — the three anchor failures registered as open `unmet-truth`
  entries, so the ship gate still sees them after these records scroll out of context.

## The D4 ruling, in one paragraph

`pytest tests/` at this sha reports **2407 passed, 26 skipped, 3 failed**. All three are
exact-equality comparisons against anchors captured on Windows, missing by 1 ULP to rel. 1.4e-9.
Plan 29.1-08 added the fact 29.1-07 lacked: **the three modules, `tests/fixtures/`,
`tests/conftest.py`, `pyproject.toml`, `src/` and `experiments/_degeneracy.py` are byte-identical
to `rerun-freeze-01`, so they fail at the predecessor tag too** — attempt 1's `0 failed` is a
Windows measurement, and `27-ONTARGET-VERIFICATION.md` never ran `pytest` on this box. Blocking
the freeze would therefore not avoid them; no reachable sha satisfies the condition. The tag was
cut with the exception recorded, in the same shape attempt 1 ruled on its 17 `GATE FAIL`
findings. **Nothing was deselected, marked `xfail`, skipped, loosened or regenerated, and no
artifact of this plan records "0 failed"** — the tag's own annotation message, the freeze record,
the attempt log, the deferred-items ledger and the windows ledger each state the failure
independently, so a later reader meets it whichever document they open first.

## Exposure audit result

**Credentials clean.** `detect-secrets` v1.5.0 against the committed baseline: **Passed**, with
the tree fingerprinted before and after to prove nothing was rewritten. An independent 8-pattern
sweep (GitHub PAT, AWS key, private-key header, Slack token, Google API key, PyPI token, Bearer
literal, inline-credential URL) returns **zero hits**. Every `secret`-shaped string in the
repository is a GitHub Actions reference or an OIDC `id-token: write` permission.

**Disclosure is one directory name.** Measured at the level of the disclosed strings rather than
the files carrying them — plan 29.1-06's move renames 229 files, so a filename comparison would
score already-public content as new. All 227 archived run-output files are already tracked on the
pushed `origin/results/rerun-freeze-01`; all 17 distinct `C:\Users\tucke\…` strings are already
public; `e2_release_linux.yaml` is byte-identical to the predecessor tag. The single incremental
disclosure is `/home/tlancaster/AquaCal_smoke_aside/2026-08-24-a833a15`, in three `.planning/`
documents.

## Deviations from Plan

### 1. [Procedural] Task 1's checkpoint executed as measured pre-authorization

- **Found during:** Task 1.
- **Situation:** `workflow.auto_advance` is `false`, so a `checkpoint:decision` would normally
  stop the executor. The dispatch instead authorized the local half explicitly — *"cut the
  annotated tag locally, run the fresh-clone verification, run the exposure audit, and write
  every record"* — which is verbatim the `proceed-as-decided` option, and hard-stopped the push.
- **What was done:** every measurement the checkpoint requires was taken **before any tag object
  existed**, with a standing condition to halt if any contradicted the plan's assumption (in
  particular, if the name had matched `v*`). None did. All of it is presented at the Task 3
  checkpoint, ahead of the irreversible step.
- **Why this is safe:** the plan itself rates a local tag `reversible` and the push `one-way`.
  Had the author chosen `different-name`, the remedy is `git tag -d`.

### 2. [Rule 3 - Blocking] `results/rerun-freeze-01` is 2 commits ahead of its remote

- **Found during:** Task 1, branch topology measurement.
- **Issue:** the local branch sits at `a6e7841`, the remote at `89c2092`. Both extra commits are
  ancestors of the tagged sha, so pushing the working branch publishes their content regardless;
  only the branch *pointer* would lag.
- **Action:** **not** silently folded into the push. Surfaced as an explicit open question in
  `29.1-PREPUSH-AUDIT.md` §7.4 — moving a branch pointer that marks where attempt 1's results
  ended is the author's call, not the executor's.

### 3. [Rule 2 - Missing safeguard] D5's trap was reproduced by this plan's own verification

- **Found during:** Task 2, after the `--smoke` pass.
- **Issue:** the verification left `run_experiment_suite_state.7005a27.tsv` with 20
  `complete … 0` lines in the clone. A **real** run at that sha in that clone would skip all 20
  stages and produce nothing — F-001's shape.
- **Fix:** archived to `/home/tlancaster/AquaCal_smoke_aside/2026-08-24-7005a27/` (with the
  smoke log, its roll-up, the install log, the import assertion and the dry-run log); the
  clone's `git status` is empty again. Recorded as a hazard in the freeze record with the
  operator instruction to clone the tag fresh, and appended to `deferred-items.md` as *D5
  UPDATE*.

### 4. [Rule 2 - Missing safeguard] `PRELAUNCH_GATE_PYTHON` set explicitly

- **Found during:** Task 2, dry run.
- **Issue:** D-28 asks for it, and plan 29.1-07's bar run left it unset — harmless there only
  because `python` on `PATH` happened to resolve correctly.
- **Fix:** set explicitly for both the dry run and the `--smoke` pass. Noted in the freeze record
  as a difference from the bar's invocation, and as a correction rather than a drift.

## Authentication gates

None.

## Deferred issues

- **D1** — the 8 provenance failures return when the re-run repopulates `experiments/results/`.
- **D6** — `check_e2_band`'s smoke-blind sibling resolution, unfixed; post-submission.
- **The anchor fixtures carry no platform field** — the actual repair behind D4, and a change to
  what the project accepts as an anchor. Outside a freeze window.

## Known Stubs

None. The two approval-dependent sections that were written **PENDING** — `29.1-PREPUSH-AUDIT.md`
§7.5 and `29.1-FREEZE-RECORD.md` § *The push* — were filled in from measurement after the push,
along with the attempt-log row and a new §7.6 of post-push assertions. No placeholder remains.

## Threat Flags

None. This plan introduces no network endpoint, no auth path, no file-access pattern and no
schema change. Its trust boundaries are the three the plan's own threat model already names,
and each was mitigated as specified: T-29.1-25 (publish-workflow trigger measured against all
five workflow files before any tag existed, push refspec names one tag), T-29.1-26 (predecessor
object id asserted before, after, and against the remote), T-29.1-27 (full exposure audit with
per-item verdicts), T-29.1-28 (new environment, import path machine-checked inside the clone),
T-29.1-29 (attempt 2 appended, attempt 1 not deleted or moved).

## The push — approved and executed

The author approved on 2026-08-24 after independently verifying the annotated tag object, the
predecessor's identity local and remote, the `--dry-run` output, the non-membership of
`rerun-freeze-02` in the 20 `v*` tags, and the clean tree. The §7.2 command ran verbatim:

    * [new branch]      phase/29.1-post-run-fixes -> phase/29.1-post-run-fixes
    * [new tag]         rerun-freeze-02 -> rerun-freeze-02

**Two ref creations, exit 0 — exactly what the dry run predicted. Nothing diverged from
prediction anywhere in this plan.**

| Post-push assertion, against the remote | Result |
|---|---|
| `rerun-freeze-02` tag object | `533f79fb…` — **identical to local**, so the annotated object survived rather than arriving as a lightweight ref |
| `rerun-freeze-02^{}` | `7005a2771aa115e4f4c1284cec7e145739586a4a` |
| `rerun-freeze-01` tag object / commit | `b31c8020…` / `3ab9c137…` — **unchanged** |
| Remote `v[0-9]*` tag count | **20** — unchanged |
| `origin/phase/29.1-post-run-fixes` | `ffbd647…` — matches local HEAD |
| `origin/results/rerun-freeze-01` | `89c2092` — **deliberately not moved** (author's ruling) |

**"No workflow fired" is measured, not argued.** The public Actions API reports no run at
`rerun-freeze-02`, `phase/29.1-post-run-fixes` or sha `7005a27`; the repository's most recent
run predates this push by twelve days. The same history contains `Publish to PyPI` runs at
`ref=v2.0.1` and `ref=v2.0.0` — so the trigger this naming convention exists to dodge is
demonstrably live, not theoretical, which is the strongest available evidence that the
precaution mattered.

The published HTTPS clone command was checked unauthenticated and resolves.

## Self-Check: PASSED

All six claimed files exist. All six claimed commits exist. `rerun-freeze-02` is an annotated
tag object pointing at `7005a2771aa115e4f4c1284cec7e145739586a4a`, and
`git describe --match 'v[0-9]*' rerun-freeze-02` still resolves to `v2.0.1-346-g7005a27`.
`27-FREEZE-RECORD.md`'s attempt log carries two rows. `rerun-freeze-01` still resolves to tag
object `b31c8020…`; the `v[0-9]*` tag count is unchanged at 20;
`git diff --stat rerun-freeze-01..HEAD -- src/` prints nothing. The working tree is clean.

**Forbidden-wording check:** a sweep for `0 failed` / `no failures` / `suite is clean` across all
four records returns two hits, both examined and both correct: `27-FREEZE-RECORD.md:39` is
attempt 1's own pre-existing table under *"What was verified before cutting"* (unmodified by this
plan), and `29.1-FREEZE-RECORD.md:102` is the **attempt 1 column** of a side-by-side row whose
attempt 2 column reads *"2407 passed, 26 skipped, 3 FAILED — ✗ does not clear"*. Filtered for
attribution, **no artifact attaches `0 failed` to attempt 2, `7005a27` or `rerun-freeze-02`.**
