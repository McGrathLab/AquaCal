---
phase: 29.1-post-run-fixes-re-freeze
plan: 05
type: execute
wave: 2
depends_on: [29.1-01]
files_modified:
  - .planning/MANUSCRIPT-FINDINGS.md
  - .planning/todos/pending/2026-08-15-classify-the-198-unprojectable-observations.md
  - .planning/todos/pending/2026-08-13-e4-aggregator-hardcodes-e2-benchmark-path.md
  - .planning/todos/pending/2026-08-20-degenerate-observations-csv-expected-at-a-path-e2-never-writes-it-to.md
  - .planning/todos/done/
autonomous: true
requirements: [SC-1]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The 198 unprojectable observations are settled in a committed, citable record: above-water board corners, with per-observation evidence, not a report."
    - "The two findings the 198 todo holds and nothing else records -- the residual-path-vs-export population trap and the refutation of the obliquity/TIR trigger -- survive its closure."
    - "The E4 aggregator path defect is confirmed already fixed against the current source at both build_grid_dataframe call sites, with the evidence recorded, rather than closed on assertion."
    - "A defect found while discharging these todos is filed rather than fixed, per the file-don't-fix directive."
  artifacts:
    - .planning/MANUSCRIPT-FINDINGS.md
    - .planning/todos/done/2026-08-15-classify-the-198-unprojectable-observations.md
    - .planning/todos/done/2026-08-13-e4-aggregator-hardcodes-e2-benchmark-path.md
    - .planning/todos/pending/2026-08-20-degenerate-observations-csv-expected-at-a-path-e2-never-writes-it-to.md
  key_links:
    - "experiments/results/benchmark.json's discard_stats -> the cause/fate decomposition that settles the 198 -> the per-observation table at experiments/results_e2_invocations/e2_classification/degenerate_observations.csv, which is the reproducible half."
    - "e4_benchmark_grid.resolve_e2_benchmark_path -> both build_grid_dataframe call sites (_run_check and _run_full). The defect todo says there are two; confirm both, because a fix at one leaves --out importing another machine's row."
---

<objective>
Discharge the two folded todos whose premises the 2026-08-20 run overtook: the 198
unprojectable observations, and E4's aggregator hardcoding E2's benchmark path. Close each
with evidence rather than assertion, and preserve what would otherwise be lost.

Purpose: the 198 todo opens by saying "what the 198 are is not established, and no committed
artifact can settle it". That was true when written and was overtaken by Phase 24's DEGEN-02
instrumentation, which did not exist yet. The run's own artifacts now settle it. The E4 path
todo was re-scoped to a pre-run blocker; the fix appears to have landed in Phase 23 as
FIX-05/D-09, and a todo left pending after its fix ships is the same class of defect as a
comment left stale after its world ended (D-10).

Output: one new `MANUSCRIPT-FINDINGS.md` entry, two closed todos, one new todo filed.

The phase's tracer is `29.1-01` task 1. This plan is the records half of the same E4/E2
neighbourhood and touches no code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-post-run-fixes-re-freeze/29.1-CONTEXT.md
@.planning/todos/pending/2026-08-15-classify-the-198-unprojectable-observations.md
@.planning/todos/pending/2026-08-13-e4-aggregator-hardcodes-e2-benchmark-path.md
</context>

## Phase environment (all plans)

    PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python

There is no bare `python` on this box's PATH — always use `$PY`.

## Hard constraints (all plans)

1. **`src/` must not be modified.** This plan modifies no code at all.
2. **Do not regenerate or edit any committed artifact under `experiments/results/`** or under
   `experiments/results_e2_invocations/`. `git diff --quiet -- experiments/results/`
   `experiments/results_e2_invocations/ src/` must exit 0 for this plan. Scope it that way
   rather than to all of `experiments/`: plans 29.1-02 and 29.1-04 run in the same wave and
   legitimately have work in flight elsewhere under that prefix.
3. **The manuscript tree is read-only from this repo.** `Spinoffs/papers/aquacal/` —
   `main.tex`, `supplement.tex`, `response-letter.md`, `numbers-ledger.tsv` — must not be
   edited here, including "obviously correct" single-number updates. The deliverable is the
   evidence and its derivation in `.planning/MANUSCRIPT-FINDINGS.md`, never the sentence.
4. **File, do not fix.** Anything found here that is not one of the two named todos gets a
   todo, not a patch.

## Artifacts this phase produces

No new gate result identifiers are introduced anywhere in the phase.

| Artifact | Kind | Plan |
|---|---|---|
| `rerun-freeze-02` | annotated git tag (must NOT match `v*`) | 08 |
| `experiments/freeze01_run_output/` | directory (`git mv` target for the 2026-08-20 run output) | 06 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-GATE-BEFORE-AFTER.md` | file | 06 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-VERIFICATION-BAR.md` | file | 07 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-FREEZE-RECORD.md` | file | 08 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md` | file | 08 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md` | file | 03 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md` | file | 04 |
| `.planning/MANUSCRIPT-FINDINGS.md` § MF-24 | new section | 05 |
| `.planning/todos/pending/2026-08-20-e7-band-mirrors-e1-benchmark-overwrite-hazard.md` | new todo | 02 |
| `.planning/todos/pending/2026-08-20-degenerate-observations-csv-expected-at-a-path-e2-never-writes-it-to.md` | new todo | 05 |
| 7 todo files moved `pending/` → `done/` with a `## Resolved` block | moved files | 01, 02, 04, 05 |
| `gate1_guard_count:benchmark_grid.csv` | EXISTING gate id; PASS message gains an exemption clause | 01 |
| `e1_seed_band_degeneracy_breakdown.json` | expectation REMOVED; its `completeness:` result leaves the roll-up | 02 |

<tasks>

<task type="auto">
  <name>Task 1: Settle the 198, and preserve the two findings recorded nowhere else</name>
  <files>.planning/MANUSCRIPT-FINDINGS.md, .planning/todos/done/2026-08-15-classify-the-198-unprojectable-observations.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-15-classify-the-198-unprojectable-observations.md` in
      full, including the two appended sections: the refutation of trigger (b) and the
      residual-path-vs-export warning. Both are the point of this task.
    - `experiments/results/benchmark.json` `discard_stats` — the cause and fate decomposition.
    - `experiments/results_e2_invocations/e2_classification/degenerate_observations.csv` — the
      committed per-observation table (12 columns, 198 rows) that makes the answer reproducible
      rather than reported.
    - `.planning/MANUSCRIPT-FINDINGS.md` — the MF-NN section format; the last entry is MF-23.
  </read_first>
  <action>
    Write a new `## MF-24` section in `.planning/MANUSCRIPT-FINDINGS.md` that settles the
    question and carries its derivation, then close the todo against it.

    **The verdict, and the evidence it rests on.** The 198 are above-water board corners.
    Support, all from the 2026-08-20 run at the frozen sha, each with the artifact path:
    - `benchmark.json` records the count under both `problem_shape` and `discard_stats`, with
      198 of 198 attributed to the above-interface cause, zero behind-camera, zero
      interface-below-camera, all in the stage-3 intrinsic pass, all continued via the pinhole
      extension.
    - The per-observation table carries, per row, the corner's signed height relative to the
      estimated interface, the camera's height, the radial offset, the chord incidence angle,
      whether the pinhole extension succeeded, and the NaN reason code. Report the measured
      ranges rather than a single example row, and state the stage grouping explicitly — a
      stage-agnostic row count double-counts observations flagged in both stage-3 passes.
    - The independent rate in `reconstruction_errors.csv`: 31 of 7762 validation corners
      reconstructing above the interface, concentrated in 2 of 52 frames, against the 198's
      0.268% of 73,975.
    - The camera-submerged bucket is eliminated by measurement: `camera_parameters.csv` puts
      every camera's height above the interface in the 1.047-1.113 m range.

    **Preserve both surviving findings as their own subsections**, because they are recorded
    nowhere else and the todo is about to be closed:
    1. *The residual-path-vs-export population trap.* The stage-3 residual vector covers 73,975
       observations over 12 cameras, while `reprojection_residuals.csv` and
       `per_corner_residuals` hold 23,028 over 13 including the auxiliary fisheye, which is
       excluded from stages 2 and 3 entirely. They differ by more than three times. The exports
       are a post-hoc reprojection evaluation, not the optimizer's residual vector — anything
       built against them silently measures the wrong population. Include the corollary that
       kills the shortcut: hunting the flat invalid-projection penalty in the exported
       residuals cannot detect behind-camera cases, because a penalty can fire inside the solve
       and never reach the export, so the exports' maximum being below the penalty value is not
       evidence.
    2. *The refutation of the obliquity/TIR trigger.* Three independent legs: the library's only
       total-internal-reflection check lives in a function with zero callers anywhere in `src/`,
       so the residual path never evaluates it; the Newton solve returns a crossing point
       satisfying Snell's law with an air-side angle below 90 degrees, which bounds the
       water-side angle below the critical angle by construction; and ground-truth measurement
       on both presets produced zero unprojectable corners at chord incidences well past the
       critical angle. Add the fourth leg this run supplies: the per-observation table's chord
       incidence column, whose measured range is far below the critical angle across all 198
       rows. Record that the `ideal` precedent was misread — its zero-corners-above-surface
       figure is a ground-truth statement while the guard counts at the optimizer's solution,
       so the two are not comparable.

    **State the consequence for the manuscript without writing the sentence.** The disclosure
    can now name the mechanism plainly rather than staying cause-agnostic, and the finding is
    benign: those observations carry no water-surface gradient, so they cannot bias the
    interface estimate. Note that on measured hardware the library's own remedy — reposition
    the board so no corner sits at or above the interface — is unavailable retrospectively, so
    a re-run reports the same count; that is why plan 29.1-01 chose to publish the value with a
    permanent exemption rather than a status the project expects to clear. Do not edit the
    manuscript.

    Then `git mv` the todo to `.planning/todos/done/` with a `## Resolved` block naming MF-24,
    the artifacts cited, and the fact that its opening premise was overtaken by DEGEN-02 rather
    than wrong when written.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; $PY -c "
import pathlib
t = pathlib.Path('.planning/MANUSCRIPT-FINDINGS.md').read_text()
assert '## MF-24' in t, 'MF-24 section missing'
sec = t[t.index('## MF-24'):]
for token in ('degenerate_observations.csv', 'benchmark.json', 'reconstruction_errors.csv', 'camera_parameters.csv'):
    assert token in sec, token
low = sec.lower()
assert 'population' in low and 'fisheye' in low, 'residual-path-vs-export trap must be preserved'
assert 'critical angle' in low, 'TIR refutation must be preserved'
print('OK')
" && test -f .planning/todos/done/2026-08-15-classify-the-198-unprojectable-observations.md && ! test -f .planning/todos/pending/2026-08-15-classify-the-198-unprojectable-observations.md && grep -q '## Resolved' .planning/todos/done/2026-08-15-classify-the-198-unprojectable-observations.md && git diff --quiet -- experiments/results/ experiments/results_e2_invocations/ src/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/MANUSCRIPT-FINDINGS.md` gains an `## MF-24` section stating the verdict with
      every supporting number tied to a named artifact path.
    - Both preserved findings appear as their own subsections with their full reasoning, not as
      one-line summaries.
    - The measured chord-incidence range from the per-observation table is reported as the
      run's own fourth leg of the TIR refutation.
    - The stage grouping caveat (never a stage-agnostic row count) is stated.
    - No file under `Spinoffs/`, `experiments/` or `src/` is modified.
    - The todo is in `.planning/todos/done/` with a `## Resolved` block naming MF-24.
  </acceptance_criteria>
  <reversibility rating="reversible">A records change; the todo can be reopened from git history (folded-todo decision, 29.1-CONTEXT.md).</reversibility>
  <done>The 198 are settled in a citable record, and nothing valuable was lost in closing the question.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the E4 aggregator path defect against the current source, then close it</name>
  <files>.planning/todos/done/2026-08-13-e4-aggregator-hardcodes-e2-benchmark-path.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-13-e4-aggregator-hardcodes-e2-benchmark-path.md` in full,
      including both appended sections: the 2026-08-15 re-scoping to a pre-run blocker, and the
      two corrections measured 2026-08-17 (the structurally always-red `--check`, and that
      `_run_check` is itself on the defective path, making two call sites rather than one).
    - `experiments/e4_benchmark_grid.py` :256-310 (`E2_BENCHMARK_PATH` and
      `resolve_e2_benchmark_path`), and both `build_grid_dataframe` call sites.
    - `experiments/e4_benchmark_grid.py` :215 (`CHECK_EXCLUDED_COLUMNS`) and the `--check`
      banner that prints what green does not cover.
  </read_first>
  <action>
    This todo appears to have been discharged by Phase 23's FIX-05/D-09 without being closed.
    Confirm it against the current source rather than assuming, because a todo left pending
    after its fix ships is the same defect class this phase exists to end (D-10).

    Check each of the todo's requirements in turn and record the evidence with file and line:
    - The real-rig record resolves relative to `--out` first, falls back to the
      `__file__`-anchored constant only when `--out` is the default tree, and returns nothing
      rather than importing across machines otherwise.
    - The `__file__` anchoring is preserved for the default path (removing it would reintroduce
      the cwd-relative bug the constant's comment documents).
    - The resolved path is announced, so a reader of an `--out` tree can tell whether the
      real-rig row is native.
    - A missing record produces an announced null row rather than a silent drop.
    - **Both** `build_grid_dataframe` call sites go through the resolver, not the constant.
      This is the correction the todo added on 2026-08-17; a fix at one site leaves `--check`
      under `--out` importing the other machine's row.
    - The always-red `--check` contract is settled — the two structurally irreproducible
      columns are excluded from cell comparison and the exclusion is printed unconditionally,
      pass or fail.

    If every requirement holds, `git mv` the todo to `.planning/todos/done/` with a
    `## Resolved` block that names the phase and decision that fixed it, quotes the resolver's
    three branches, cites both call sites by line, and records that the `--check` correction
    was settled by the excluded-columns contract rather than locally. If any requirement does
    NOT hold, do not close it: record precisely which, and stop — a partial fix here is a
    provenance failure at re-run time, and the re-run is what this phase licenses.

    Note in the `## Resolved` block that plan 29.1-06's archive-aside independently defuses the
    worst case (an `--out` run silently pairing one machine's synthetic cells with another
    machine's real-rig row can only happen while a stale record sits at the default path), and
    that the archive-aside does not replace this fix.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; $PY -c "
import ast, pathlib
src = pathlib.Path('experiments/e4_benchmark_grid.py').read_text()
tree = ast.parse(src)
calls = [n for n in ast.walk(tree) if isinstance(n, ast.Call) and getattr(n.func, 'id', '') == 'build_grid_dataframe']
assert len(calls) == 2, f'expected two call sites, found {len(calls)}'
for c in calls:
    args = [a for a in c.args]
    assert not any(isinstance(a, ast.Name) and a.id == 'E2_BENCHMARK_PATH' for a in args), ast.dump(c)
resolver_calls = [n for n in ast.walk(tree) if isinstance(n, ast.Call) and getattr(n.func, 'id', '') == 'resolve_e2_benchmark_path']
assert len(resolver_calls) == 2, f'expected two resolver calls, found {len(resolver_calls)}'
assert 'CHECK_EXCLUDED_COLUMNS' in src
print('OK')
" && test -f .planning/todos/done/2026-08-13-e4-aggregator-hardcodes-e2-benchmark-path.md && grep -q '## Resolved' .planning/todos/done/2026-08-13-e4-aggregator-hardcodes-e2-benchmark-path.md && git diff --quiet -- experiments/results/ experiments/results_e2_invocations/ src/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Each of the todo's requirements is checked against the current source and recorded with
      file and line evidence in the `## Resolved` block.
    - Both `build_grid_dataframe` call sites are confirmed to receive the resolver's result,
      never the module-level constant.
    - The `--check` contract's resolution is recorded, naming the excluded columns and that the
      exclusion is printed unconditionally.
    - The todo is closed only if every requirement holds; otherwise it stays pending with the
      gap recorded and the plan reports the blocker.
    - No file under `experiments/` or `src/` is modified by this task.
  </acceptance_criteria>
  <done>The path defect is confirmed fixed against the tree the freeze will carry, with the evidence written down.</done>
</task>

<task type="auto">
  <name>Task 3: File the conditional-artifact path mismatch found while discharging the 198</name>
  <files>.planning/todos/pending/2026-08-20-degenerate-observations-csv-expected-at-a-path-e2-never-writes-it-to.md</files>
  <read_first>
    - `experiments/suite_expectations.json` — the `degenerate_observations.csv` artifact entry
      (stage `e2_production`, `dir: experiments/results`, `conditional: true`) and its
      `rows_rationale`, which says the file is written only when at least one flagged row exists.
    - `experiments/results_e2_invocations/config_e2_classification.yaml` — the classification
      invocation's `paths.output_dir`.
    - `experiments/_expectations.py` `_check_artifact` — the conditional-absent branch, which
      returns PASS.
    - `.planning/RETURN-HANDOFF-2026-08-20.md` §6.5 — the file-don't-fix directive that produced
      this phase's five defect todos, and the format those todos take.
  </read_first>
  <action>
    Planning found a sixth defect the 2026-08-20 run surfaced and nobody filed. **File it; do
    not fix it.** It is outside this phase's six success criteria and fixing it would move a
    completeness gate inside a freeze window on the strength of a finding made this week.

    The finding: the manifest expects the conditional per-observation table under
    `experiments/results`, and its absence there is scored PASS on the stated grounds that a
    clean run legitimately produces no file. But the run was not clean — it flagged 198
    observations — and the table *was* written, into the classification invocation's own output
    directory, where it is committed with 198 rows. So the conditional PASS asserts the opposite
    of what happened: the gate reads "no flagged rows" from an absence that means "written
    elsewhere". A conditional whose absence is indistinguishable from a misdirected write is a
    gate that cannot fail.

    Write the todo to
    `.planning/todos/pending/2026-08-20-degenerate-observations-csv-expected-at-a-path-e2-never-writes-it-to.md`
    following the existing defect-todo format: front matter with `created`, `title`, `area:
    experiments`, `resolves_phase: 28`, and a `files:` list; then the finding, the evidence with
    paths and row counts, why it is a gate defect rather than a missing writer, and the
    resolution options with their trade-offs — point the expectation at the directory E2's
    classification invocation actually writes to; or make the conditional's predicate depend on
    the recorded flagged count so an absence with a non-zero count FAILs; or declare the
    invocation-dir copy the artifact of record and drop the `experiments/results` expectation.
    State plainly which option preserves the most information and why the choice is the
    author's. Cross-reference the same question for the sibling all-observation-depths artifact,
    whose expectation has the same shape.

    Do not change `suite_expectations.json`, `_expectations.py`, or any E2 code.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; T=.planning/todos/pending/2026-08-20-degenerate-observations-csv-expected-at-a-path-e2-never-writes-it-to.md; test -f "$T" && head -1 "$T" | grep -qx -- '---' && grep -q 'resolves_phase: 28' "$T" && grep -q 'area: experiments' "$T" && grep -qc 'results_e2_invocations' "$T" && git diff --quiet -- experiments/results/ experiments/results_e2_invocations/ src/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The new todo exists with valid front matter matching the other defect todos in the
      directory (`created`, `title`, `area`, `resolves_phase`, `files`).
    - It cites the committed table's path and its row count, and the manifest entry it
      contradicts.
    - It states at least three resolution options with trade-offs and names the choice as the
      author's.
    - It cross-references the sibling all-observation-depths expectation.
    - `git diff --quiet -- experiments/results/ experiments/results_e2_invocations/ src/` exits 0
      and no file under `experiments/` outside those prefixes was touched by this plan — nothing
      was fixed, only filed.
  </acceptance_criteria>
  <done>The sixth defect is on the record with its evidence, and the freeze window stays closed to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → manuscript | `MANUSCRIPT-FINDINGS.md` is what the manuscript session reads; a wrong derivation here becomes a wrong sentence there. |
| todo closure → future reader | A todo moved to `done/` stops being read; anything not preserved at closure is lost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-14 | Repudiation | closed todos | high | mitigate | Both preserved findings are asserted by task 1's verify against the MF-24 section text, so a closure that drops them fails its own check. Task 2 refuses to close on assertion and stops if any requirement does not hold. |
| T-29.1-15 | Tampering | `Spinoffs/papers/aquacal/` | high | mitigate | The manuscript tree is read-only from this repo by standing scope boundary; this plan's deliverable is the derivation, never the sentence, and the acceptance criteria forbid edits outside `.planning/`. |
| T-29.1-16 | Elevation of privilege | freeze-window scope | medium | mitigate | The sixth defect is filed rather than fixed; task 3's verify asserts `git diff --quiet` over `experiments/results/`, `experiments/results_e2_invocations/` and `src/`, and the acceptance criteria require this plan's own diff to touch nothing else under `experiments/`, so a fix disguised as a filing fails. |
| T-29.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing. See plan 29.1-04 for the phase-level ruling; the only installs in this phase are extras already declared in this repository's own `pyproject.toml`. |
</threat_model>

<verification>
- `.planning/MANUSCRIPT-FINDINGS.md` carries `## MF-24` with both preserved findings.
- `.planning/todos/done/` holds both discharged todos, each with a `## Resolved` block.
- `.planning/todos/pending/` holds the newly filed conditional-artifact todo.
- `git diff --quiet -- experiments/results/ experiments/results_e2_invocations/ src/` exits 0,
  and this plan's own diff touches nothing under `experiments/` — it changes no code and no artifact.
</verification>

<success_criteria>
The two folded todos are discharged with evidence, the two findings that existed only inside
the 198 todo are preserved in a document that outlives it, and the defect discovered on the
way is filed rather than smuggled into a freeze window.
</success_criteria>

<output>
Create `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-05-SUMMARY.md` when done.
</output>
