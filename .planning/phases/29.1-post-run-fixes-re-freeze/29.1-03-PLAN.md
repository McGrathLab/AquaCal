---
phase: 29.1-post-run-fixes-re-freeze
plan: 03
type: execute
wave: 3
depends_on: [29.1-01, 29.1-02]
files_modified:
  - .planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md
  - experiments/e1_refractive_comparison.py
  - experiments/e2_real_rig.py
  - experiments/e3_derived_quantities.py
  - experiments/e4_benchmark_grid.py
  - experiments/e5_index_sensitivity.py
  - experiments/e6_generalization_sweep.py
  - experiments/e7_interface_ablation.py
  - experiments/e7_focal_standoff_analysis.py
  - experiments/fd_jacobian_accuracy.py
  - experiments/reconstruction_bootstrap.py
  - experiments/run_experiment_suite.sh
  - tests/unit/test_stale_provenance_strings.py
  - .planning/todos/pending/2026-08-17-audit-static-strings-that-annotate-recomputed-values.md
  - .planning/todos/pending/2026-08-15-correct-stale-strings-in-e2-and-the-synthetic-generator.md
autonomous: true
requirements: [SC-4]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every static string in the run's writer modules that annotates a value the 2026-08-20 run recomputed is classified as either an attributed historical citation or an unattributed annotation (D-09)."
    - "Every string classified as an unattributed annotation either names its derivation and quotes no live value, or carries explicit run attribution (D-09)."
    - "A filename-scoped claim-sentence gate blocks reintroduction of every corrected claim (D-09, D-10)."
    - "The boundary of the sweep is reproducible: the candidate set is derived by a recorded command, not by taste (D-09)."
  artifacts:
    - .planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md
    - tests/unit/test_stale_provenance_strings.py
  key_links:
    - "suite_expectations.json's stage list -> the writer modules whose strings are in scope. Anything outside that set is out of D-09's bound and must be recorded as such rather than swept."
    - "tests/unit/test_stale_provenance_strings.py necessarily contains the corrected claim sentences, so every assertion must stay filename-scoped -- a repo-wide grep matches the gate itself after a perfect fix."
---

<objective>
Run the bounded stale-string sweep D-09 authorises: classify every static string in the
2026-08-20 run's writer modules that annotates a value that run recomputed, fix only the
unattributed-annotation category, and pin the corrections with a filename-scoped
claim-sentence gate.

Purpose (D-10): three stale rationales surfaced in a single day — the D-26 comment, the 198
todo's opening premise, and E1's band scope string. The pattern is a claim outliving the
conditions that produced it, and it has now twice produced a gate failure or a false
provenance statement. This is the last cheap moment to check, because after the re-freeze a
wrong number is a wrong number in the archive the paper cites.

Output: an audit table with a reproducible boundary, a small set of corrections, and a gate.

The phase's tracer is `29.1-01` task 1. This plan expands the same "what the suite says about
itself" concern into the remaining writer modules.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-post-run-fixes-re-freeze/29.1-CONTEXT.md
@.planning/todos/pending/2026-08-17-audit-static-strings-that-annotate-recomputed-values.md
@.planning/todos/pending/2026-08-15-correct-stale-strings-in-e2-and-the-synthetic-generator.md
@tests/unit/test_stale_provenance_strings.py
</context>

## Phase environment (all plans)

    PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python

There is no bare `python` on this box's PATH — always use `$PY`.

## Hard constraints (all plans)

1. **`src/` must not be modified.** That excludes `src/aquacal/datasets/synthetic.py`, whose
   four real-rig-standoff mentions were already checked and ruled accurate on 2026-08-17.
2. **Do not regenerate or edit any committed artifact under `experiments/results/`.**
   `git diff --quiet -- experiments/results/` must exit 0 for this plan.
3. **The driver exits non-zero on a healthy run** — acceptance is the roll-up, never `$?`.
4. **Do not add pre-flight override flags.**
5. **Do not widen past D-09's bound.** `experiments/` is ~500 KB of heavily-commented Python,
   most of it annotating values no recent run touched; findings there would be unevidenced.
   Anything outside the bound is recorded as out of scope, never swept.

## Artifacts this phase produces

No new gate result identifiers are introduced anywhere in the phase.

| Artifact | Kind | Plan |
|---|---|---|
| `rerun-freeze-02` | annotated git tag (must NOT match `v*`) | 08 |
| `experiments/freeze01_run_output/` | directory (`git mv` target for the 2026-08-20 run output) | 06 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-GATE-BEFORE-AFTER.md` | file | 06 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-VERIFICATION-BAR.md` | file | 07 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-FREEZE-RECORD.md` | file | 08 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md` | file | 08 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md` | file | 03 |
| `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-INSTALL-SITES.md` | file | 04 |
| `.planning/todos/pending/2026-08-20-e7-band-mirrors-e1-benchmark-overwrite-hazard.md` | new todo | 02 |
| 7 todo files moved `pending/` → `done/` with a `## Resolved` block | moved files | 01, 02, 04, 05 |
| `gate1_guard_count:benchmark_grid.csv` | EXISTING gate id; PASS message gains an exemption clause | 01 |
| `e1_seed_band_degeneracy_breakdown.json` | expectation REMOVED; its `completeness:` result leaves the roll-up | 02 |

<tasks>

<task type="auto">
  <name>Task 1: Enumerate and classify, with a reproducible boundary</name>
  <files>.planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-17-audit-static-strings-that-annotate-recomputed-values.md`
      — the classification rule (attribution, not pattern matching), the "why this is not a
      grep" section, and the known non-issues already checked on 2026-08-17.
    - `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-CONTEXT.md` § D-09, D-10.
    - `experiments/suite_expectations.json` — the stage list that defines which modules wrote
      the 2026-08-20 run's artifacts.
    - `tests/unit/test_stale_provenance_strings.py` — the existing gate and its grep-hygiene note.
  </read_first>
  <action>
    **Fix the boundary first, mechanically, and record the command.** The candidate file set is
    the eleven files listed in this plan's `files_modified` that are under `experiments/`: the
    ten stage-invocation modules named in `suite_expectations.json` plus the driver
    `run_experiment_suite.sh`. Nothing else is in scope. Shared helpers (`_io.py`,
    `_degeneracy.py`, `_run_manifest.py`, `_render.py`, `_expectations.py`) are out — they
    carry mechanism, not measured values — and `src/` is out by hard constraint. Record that
    exclusion list and its reason at the top of the audit.

    **Enumerate candidates** with a single recorded command over those files, matching comment
    and string-literal lines that contain either a high-precision decimal (three or more
    fractional digits) or an integer immediately followed by a quantity word the suite
    measures (rows, seeds, frames, observations, corners, cells, cameras, hours, minutes, px,
    mm, m). Paste that command verbatim into the audit so the boundary is re-derivable.

    **Triage in two passes, so the audit stays readable:**
    - Pass A — drop every hit whose quantity no 2026-08-20 artifact carries. Do not list these
      individually; record only a per-file count dropped and one sentence on what they were
      (design constants, algorithm parameters, prose about geometry, and so on).
    - Pass B — classify each survivor individually, one table row each:
      `file:line | quantity | classification | reason`, where classification is exactly
      `legitimate` or `defective`.
      - `legitimate` — the string names a specific historical run, release or archived record,
        so the number is a citation and is *supposed* to be frozen. `e2_real_rig.py`'s
        `0.8786`/`1.0191` mentions are the canonical example: they name the release run
        explicitly and they are the note that stops the pooled RMS being compared against the
        per-camera mean. They stay.
      - `defective` — the string annotates a field the code recomputes, with no attribution, so
        the number silently becomes a claim about the current run that nobody re-derives.

    A pass that finds nothing in the defective category is a valid and valuable outcome —
    record it as such rather than manufacturing a finding.

    Write the result to
    `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md` with sections:
    Boundary (files in, files out, and why), Enumeration command, Pass A summary, Pass B table,
    and Verdict. Leave the `Fixed` column empty for now; task 2 fills it.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; $PY -c "
import pathlib, re
p = pathlib.Path('.planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md')
t = p.read_text()
for h in ('## Boundary', '## Enumeration command', '## Pass A', '## Pass B', '## Verdict'):
    assert h in t, h
rows = [l for l in t.splitlines() if re.match(r'^\|\s*experiments/\S+:\d+', l)]
assert rows, 'Pass B table must carry at least one file:line row'
for r in rows:
    assert ('legitimate' in r) or ('defective' in r), r
print(f'OK {len(rows)} classified sites')
"</automated>
  </verify>
  <acceptance_criteria>
    - The audit names exactly which files are in the boundary and which are out, with a reason
      for each exclusion, including `src/` and the shared helper modules (D-09).
    - The enumeration command is pasted verbatim and re-running it reproduces the candidate set.
    - Pass A is summarised per file with counts and one sentence, not line by line.
    - Every Pass B survivor has a `file:line`, the quantity, a classification of exactly
      `legitimate` or `defective`, and a one-line reason.
    - `e2_real_rig.py`'s release-run citations are classified `legitimate` and are not proposed
      for change.
    - No site in `src/` appears anywhere in the table.
  </acceptance_criteria>
  <done>The sweep has a boundary a reader can re-derive and a verdict per site.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the defective category and pin it with a filename-scoped gate</name>
  <files>.planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md, tests/unit/test_stale_provenance_strings.py, and whichever of the eleven boundary files task 1 classified as defective</files>
  <read_first>
    - The audit produced by task 1 — the `defective` rows are this task's entire work list.
    - `tests/unit/test_stale_provenance_strings.py` in full, including its module docstring's
      grep-hygiene warning and its repo-root anchoring convention.
    - `experiments/e1_refractive_comparison.py`'s `scope` field as rewritten by plan 29.1-02
      task 3 — the pattern to follow when a string annotates a value the code has in hand.
  </read_first>
  <action>
    For each `defective` row, apply whichever of the two remedies fits, and record which in the
    audit's `Fixed` column:
    - **Name the derivation and quote no live value.** Preferred wherever the code has the value
      in scope at the point the string is built — interpolate it, as plan 29.1-02 did for E1's
      band scope. This is the only remedy that cannot rot.
    - **Carry explicit run attribution.** For a number that genuinely must stay frozen, name the
      run, release or archived record it comes from, so a reader knows it is a citation.

    Swapping one frozen number for another is not a remedy — it reproduces the defect one run
    later, which is exactly what the 2026-08-15 pass had to be blocked from doing.

    Then extend `tests/unit/test_stale_provenance_strings.py` with one assertion per corrected
    site, following the module's existing conventions exactly:
    - anchor the repository root to the test file's own location, never the process cwd;
    - scope every assertion to a named file constant — never a repo-wide walk, because this
      test module necessarily contains the corrected claim sentences itself and would match
      after a perfect fix;
    - assert on whole claim sentences, never on bare number tokens;
    - extend the module docstring to say the audit document under `.planning/` also contains
      those sentences and is likewise never scanned.

    If task 1's verdict was that the defective category is empty, this task's work is the gate
    only: add no corrections, and record in the audit that the gate exists to hold the
    already-correct state.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; $PY -m pytest tests/unit/test_stale_provenance_strings.py -q 2>&1 | tail -3 && $PY -c "
import pathlib, re
t = pathlib.Path('.planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md').read_text()
rows = [l for l in t.splitlines() if re.match(r'^\|\s*experiments/\S+:\d+', l)]
bad = [r for r in rows if 'defective' in r and not re.search(r'\|\s*(derivation|attribution)\b', r)]
assert not bad, bad
print('OK')
" && $PY -c "
import ast, pathlib
src = pathlib.Path('tests/unit/test_stale_provenance_strings.py').read_text()
assert 'parents[2]' in src, 'repo root must stay anchored to the test file location'
tree = ast.parse(src)
scanners = {'rglob', 'glob', 'iterdir', 'walk'}
called = {n.func.attr for n in ast.walk(tree) if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute)}
assert not (called & scanners), f'assertions must stay filename-scoped; found tree-scanning call(s): {sorted(called & scanners)}'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Every `defective` row in the audit has a non-empty `Fixed` column naming the remedy
      applied, exactly one of `derivation` or `attribution`.
    - No correction replaces one frozen number with another frozen number.
    - `tests/unit/test_stale_provenance_strings.py` gains one claim-sentence assertion per
      corrected site, stays filename-scoped, keeps its repo-root anchoring, and passes.
    - The test module docstring names the audit document as a second file that contains the
      stale sentences by necessity.
    - `git diff --quiet -- src/` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Comment and string-literal edits in experiment modules, each pinned by a test (D-09).</reversibility>
  <done>Every unattributed annotation in the run's writer modules now names its derivation or its source, and a gate holds it there.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate the two stale-string todos with what this bounded pass covered</name>
  <files>.planning/todos/pending/2026-08-17-audit-static-strings-that-annotate-recomputed-values.md, .planning/todos/pending/2026-08-15-correct-stale-strings-in-e2-and-the-synthetic-generator.md, .planning/todos/pending/2026-08-14-decide-whether-e1-may-carry-absolute-accuracy-claims.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-17-audit-static-strings-that-annotate-recomputed-values.md`
      — its Acceptance section, which this pass partially discharges.
    - `.planning/todos/pending/2026-08-15-correct-stale-strings-in-e2-and-the-synthetic-generator.md`
      — its three named sites, all of which appear already corrected in the tree; confirm each
      against the current source before writing the annotation.
    - `.planning/todos/pending/2026-08-14-decide-whether-e1-may-carry-absolute-accuracy-claims.md`
      — the reviewed-not-folded todo whose text still describes the pre-ruling-A1 plan.
  </read_first>
  <action>
    Both stale-string todos stay **pending** per D-09 — only the bounded portion is discharged
    here. Append a dated `## Partially discharged in Phase 29.1` section to each, stating:
    what the bound was and why (D-09's "values the 2026-08-20 run recomputed"), which sites this
    pass classified and corrected, a pointer to `29.1-STALE-STRING-AUDIT.md`, and precisely what
    remains out of scope so a later pass does not re-derive the boundary.

    For the 2026-08-15 todo specifically, verify each of its three named sites against the
    current source and record the finding: whether the correction already landed (Phase 23's
    FIX-06) and, for its `synthetic.py` site, that `src/` is out of this phase's scope by hard
    constraint regardless of state.

    Separately, annotate
    `2026-08-14-decide-whether-e1-may-carry-absolute-accuracy-claims.md` with ruling A1 — its
    text still describes the ten-seed plan the ruling cut, and the accuracy-claim decision
    itself belongs to Phase 25/29, not here. Annotation only; leave it pending.
  </action>
  <verify>
    <automated>PY=/home/tlancaster/anaconda3/envs/aquacal-freeze01/bin/python; for f in 2026-08-17-audit-static-strings-that-annotate-recomputed-values 2026-08-15-correct-stale-strings-in-e2-and-the-synthetic-generator; do test -f ".planning/todos/pending/$f.md" || { echo "MISSING pending $f"; exit 1; }; grep -q 'Phase 29.1' ".planning/todos/pending/$f.md" || { echo "NOT ANNOTATED $f"; exit 1; }; done; grep -q 'A1' .planning/todos/pending/2026-08-14-decide-whether-e1-may-carry-absolute-accuracy-claims.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both stale-string todos remain in `.planning/todos/pending/` and each carries a dated
      partial-discharge section naming the bound, the sites covered, and what remains (D-09).
    - The 2026-08-15 todo's section records the current state of each of its three named sites
      against the source, including that its `src/` site is out of scope by hard constraint.
    - `2026-08-14-decide-whether-e1-may-carry-absolute-accuracy-claims.md` is annotated with
      ruling A1 and stays pending.
    - No todo is moved to `done/` by this task.
  </acceptance_criteria>
  <done>The remaining scope of each todo is explicit, so the next pass starts from a boundary rather than from the whole tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source comment → reader's belief | Comments in this project carry decision ids and rationale and are load-bearing; two of this phase's defects are stale comments, not stale code. |
| corrected string → regression gate | A gate written for this class necessarily contains the stale strings itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-08 | Repudiation | experiment module comments | high | mitigate | Corrections either interpolate the live value or name the run they cite; task 2's verify rejects any `defective` row whose remedy is neither. |
| T-29.1-09 | Tampering | `tests/unit/test_stale_provenance_strings.py` | medium | mitigate | Assertions stay filename-scoped and repo-root anchored; task 2's verify rejects `rglob`/`walk` in that module, which is what would let the gate match itself and be "fixed" by deletion. |
| T-29.1-10 | Tampering | `src/` | high | mitigate | `git diff --quiet -- src/` is an acceptance criterion; the sweep's boundary excludes `src/` explicitly and the audit must show no `src/` row. |
| T-29.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing. See plan 29.1-04 for the phase-level ruling; the only installs in this phase are extras already declared in this repository's own `pyproject.toml`. |
</threat_model>

<verification>
- `$PY -m pytest tests/unit/test_stale_provenance_strings.py -q` passes.
- `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-STALE-STRING-AUDIT.md` exists with all
  five sections and a classified row per Pass B survivor.
- `git diff --quiet -- src/` and `git diff --quiet -- experiments/results/` both exit 0.
- `$PY experiments/check_rerun_gates.py experiments/results --profile full | tail -1` still
  reads `TOTAL: 176 PASS, 7 N/A, 0 FAIL` — the sweep changed prose, not values.
</verification>

<success_criteria>
SC-4's wider half is satisfied: within D-09's bound, every string annotating a value the
2026-08-20 run recomputed is either an attributed citation or names its derivation, the
boundary is reproducible, and a gate blocks reintroduction.
</success_criteria>

<output>
Create `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-03-SUMMARY.md` when done.
</output>
