# 29.1-INSTALL-SITES — every install instruction in the shipped tree, ruled on

**Plan 29.1-04, task 1. Decision D-11.** SC-5.

D-11 asks for two things: correct `experiments/HANDOFF.md` §1.2 to
`python -m pip install -e ".[dev,bench]"`, and **check every other place the install command
appears rather than only that section**. This file is the second half. It enumerates every
install invocation in the tracked tree outside `.planning/` and rules on each one, so that a
later reader does not have to re-open a question that has already been settled.

## Method

```bash
git grep -n -E 'pip install' -- ':!.planning'
```

run at `phase/29.1-post-run-fixes`, plus a sweep for conda/`environment.yml`/`setup.py`/`tox.ini`
style install specifications (there are none in this repository; `docs/Makefile` is a Sphinx
driver and installs nothing). Seventeen files carry the string. Every hit is ruled below,
including the ones that are prose about installs rather than instructions to run one — because
"this line is descriptive, not an instruction" is itself a ruling worth recording once.

## Ruling categories

| Category | Meaning |
|---|---|
| `corrected` | The site tells a **suite operator** how to build a run environment and omitted the required extras. Changed by this plan. |
| `correct as-is — different audience` | A contributor, CI, or end-user install whose extra set is right for what that audience does. |
| `correct as-is — not a first install` | A `--no-deps` metadata-refresh command, or prose describing when editable-install metadata refreshes. These **must not gain extras**: adding them would change what the command does. |
| `handed to plan 29.1-06` | A site in a file another plan owns this wave. |

## The enumeration

| file:line | command | audience | ruling | reason |
|---|---|---|---|---|
| experiments/HANDOFF.md:49 | `python -m pip install -e ".[dev,bench]"` | suite operator building the run environment on the Linux run machine | corrected | **This is the defect.** It read `python -m pip install -e .`, which resolves runtime dependencies only. Without `pytest` (`dev`) e3 exits 1 on both `--check` and `--force`; without `psutil` (`bench`) `cpu_count_logical` and `ram_total_bytes` go null and `gate3_run_manifest_fields` FAILs. Both extras added, a sentence per extra saying what breaks without it, the dependency table extended to match, and the 2026-08-19 on-target finding plus its Linux-only nature recorded in the same section. |
| experiments/HANDOFF.md:53 | `python -m pip install -e .` (cited, not instructed) | same | correct as-is — not a first install | Added by this plan. It names the **wrong** command in order to say why it is insufficient. Changing it would destroy the sentence. |
| experiments/HANDOFF.md:70 | `pip install -e ".[dev,bench]"` (cited in the on-target note) | same | correct as-is — different audience | Added by this plan. Historical record of the correction applied on the target on 2026-08-19; already carries both extras. |
| CONTRIBUTING.md:21 | `pip install -e ".[dev]"` | contributor setting up a development checkout | correct as-is — different audience | `dev` is the right set for the documented next step (`python -m pytest tests/`) plus ruff and pre-commit. A contributor does not run the experiment suite from this document, and no unit test asserts the psutil-backed manifest fields are non-null — `capture_environment()` leaves them `None` behind a `try/except` and `tests/unit/test_benchmark.py` tolerates that by design. Adding `bench` here would imply the test suite needs it, which is false. |
| docs/contributing.md | `{include} ../CONTRIBUTING.md` | contributor, via the docs site | correct as-is — different audience | Not an install site of its own. It is a three-line MyST include of `CONTRIBUTING.md`, so it inherits that file's ruling and must not be edited separately. |
| requirements.txt:4 | `pip install -e ".[dev]"` (as a comment pointer) | pip-only user reading the runtime mirror | correct as-is — different audience | The file exists to mirror **runtime** dependencies for pip-only users; the comment points developers at the dev extra. It is not a run-environment instruction, and `pyproject.toml` remains the authoritative source. Per HANDOFF §1.2, `requirements.txt` is deliberately not tightened for this run. |
| .github/workflows/test.yml:31 | `pip install -e ".[dev]"` | CI, unit-test job | correct as-is — different audience | The job runs `pytest`. `bench` is not needed: nothing in the unit suite requires the psutil-backed fields to be non-null. |
| .github/workflows/test.yml:54 | `pip install pre-commit` | CI, lint job | correct as-is — different audience | Installs the linter only, deliberately without the package, so lint runs even if the package does not build. Adding extras here would defeat that isolation. |
| .github/workflows/test.yml:73 | `pip install -e ".[dev]"` | CI, `experiments-smoke` job | correct as-is — different audience | The nearest call. This job **does** run `e3_derived_quantities --smoke`, which is exactly the stage that dies without `pytest` — and `dev` supplies it, so the job is correct today. It never builds a `run_manifest.json` and never runs Gate 3, so the psutil-backed fields are unasserted there and `bench` buys nothing. Adding `bench` would be a defensible hardening (it would make CI's environment match the run environment) but it is **out of scope**: D-11 authorises a documentation fix, `.github/` is not part of the operator instruction set, and this plan may not touch anything that changes what CI resolves. Recorded here so the option is not lost. |
| .github/workflows/slow-tests.yml:42 | `pip install -e ".[dev]"` | CI, slow-marked test job | correct as-is — different audience | Same audience and same reasoning as `test.yml:31`. |
| .github/workflows/publish.yml:24 | `pip install -e ".[dev]"` | CI, pre-publish test gate | correct as-is — different audience | Runs `pytest tests/ -m "not slow"` before publishing. `dev` is the correct set. |
| .github/workflows/publish.yml:42 | `python -m pip install --upgrade pip build` | CI, build job | correct as-is — different audience | Installs the build frontend, not the package. Extras are meaningless here. |
| .github/workflows/docs.yml:26 | `pip install -e ".[dev,docs]"` | CI, documentation build | correct as-is — different audience | `docs` is the extra this job needs; `dev` comes along for the tooling. HANDOFF §1.2 now says explicitly that the run environment does **not** want `docs`, so the two sets are correctly different. |
| .github/workflows/release.yml:27 | `pip install python-semantic-release build` | CI, release job | correct as-is — different audience | Release tooling only; the package itself is never installed in this job. |
| experiments/README.md:538 | `pip install -e . --no-deps` | suite operator refreshing installed distribution metadata after a `pyproject.toml` version bump | correct as-is — not a first install | `--no-deps` is the point of the command: it re-stamps `importlib.metadata.version("aquacal")` without touching the resolved environment. Adding extras would make it resolve and install, which is precisely what this instruction exists to avoid. **No change needed, so nothing is handed to plan 29.1-06**; the file is owned by plan 29.1-02 this wave and is left untouched by this plan. |
| experiments/prelaunch_gate.sh:155 | `pip install -e . --no-deps` (inside the `ENV_VERSION_MATCH` failure remediation string) | suite operator, at pre-flight | correct as-is — not a first install | Same command and same reason as `experiments/README.md:538`. The gate has already imported `aquacal` successfully by the time it can emit this string, so a first install is not the situation being remedied — a stale version stamp is. |
| experiments/prelaunch_gate.sh:116 | `pip install -e .` (prose in the `ENV_VERSION_MATCH` comment) | maintainer reading the gate | correct as-is — not a first install | Descriptive: it names when editable-install metadata is written, in order to explain why the check exists. Not an instruction. |
| experiments/_run_manifest.py:22 | `pip install -e .` (prose in the F-002 module docstring) | maintainer reading the manifest module | correct as-is — not a first install | Descriptive, for the same reason: it explains why `installed_distribution_version` is not authoritative. Not an instruction. |
| src/aquacal/io/benchmark.py:89 | `pip install -e .` (prose in `capture_environment`'s docstring) | library reader | correct as-is — not a first install | Descriptive, explaining when `aquacal_version` refreshes. Not an instruction — and `src/` is frozen for this phase in any case (plan-level hard constraint 1). |
| tests/unit/test_benchmark.py:99 | `pip install` (prose in a test comment) | maintainer reading the test | correct as-is — not a first install | A comment describing what the fixture simulates (a tree with no `.git`). No command is run. |
| README.md:20 | `pip install aquacal` | PyPI end user | correct as-is — different audience | Installs the released wheel with runtime dependencies. An end user calibrating their own rig runs neither the experiment suite nor the test suite. |
| README.md:27 | `pip install aquacal` | PyPI end user, Quick Start | correct as-is — different audience | Same audience and same reasoning as `README.md:20`. |
| docs/tutorials/01_full_pipeline.ipynb:52 | `!pip install -q aquacal` | Colab tutorial reader | correct as-is — different audience | Released wheel in a hosted notebook. No extras apply. |
| docs/tutorials/02_synthetic_validation.ipynb:61 | `!pip install -q -e /content/AquaCal` | Colab tutorial reader, repo-checkout branch | correct as-is — different audience | Editable, but of a Colab checkout for a synthetic-only notebook. Line 51 states in the file itself that `pip install aquacal` alone is sufficient and that nothing there needs the repo's `tests/` — the e3 test-module dependency is not on this path. |
| docs/tutorials/03_cli_walkthrough.md:12 | `pip install aquacal` | tutorial reader | correct as-is — different audience | End-user prerequisite for the CLI walkthrough. |
| docs/tutorials/03_cli_walkthrough.md:178 | `pip install "opencv-python==4.13.*"` | tutorial reader debugging mismatched numbers | correct as-is — different audience | A targeted remedy for an OpenCV version mismatch, not a package install. It already carries the pin that matters. |

**26 sites, 17 files.** One `corrected` (the defect), five `correct as-is — not a first install`
that are `--no-deps` refreshes or prose, and the remainder `correct as-is — different audience`.
Nothing is handed to plan 29.1-06: the only site in a file another plan owns this wave
(`experiments/README.md:538`) needs no change, and `git diff --quiet -- experiments/README.md`
holds for this plan.

## What was deliberately **not** done

The todo behind this work offers three fixes. D-11 authorises only the first, and the other two
are recorded here so the choice is legible rather than forgotten:

- **Not taken (todo option 2):** moving `DECLARED_CONSTANTS` out of
  `tests/unit/test_experiments_e3_constants.py` into a non-test module, inverting the dependency
  so a §3-facing generator no longer needs a test runner. Correct long-term; touches the source
  of §3-facing constants and requires the suite re-run behind it. Out of this phase's scope
  (hard constraint 1).
- **Not taken (todo option 3):** promoting `psutil` from the `bench` extra into runtime
  dependencies, or giving `capture_environment()` a non-psutil fallback
  (`os.cpu_count()`, `/proc/meminfo`) so two `REQUIRED_MANIFEST_FIELDS` can never be null on
  Linux. Also correct long-term; also touches `src/` and `pyproject.toml`. Out of scope.
- **Not taken, and never to be taken:** weakening `gate3_run_manifest_fields` to tolerate the
  nulls. The gate was right — that is the whole reason the defect was visible at all.

Both open options stay in the todo's own body, which moves to `.planning/todos/done/` with a
`## Resolved` block naming them explicitly.
