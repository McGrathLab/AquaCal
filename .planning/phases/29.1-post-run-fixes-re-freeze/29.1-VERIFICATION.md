---
phase: 29.1-post-run-fixes-re-freeze
verified: 2026-08-24T18:50:00Z
status: passed
score: 7/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 29.1: Post-Run Fixes & Re-Freeze Verification Report

**Phase Goal:** The defects the 2026-08-20 production run surfaced are fixed, the frozen
package's install instructions match what the suite actually needs, and a new frozen sha is
cut and tagged so the suite can be re-run cleanly before the Zenodo results package is built.

**Verified:** 2026-08-24
**Status:** passed
**Re-verification:** No — initial verification

## Method

This phase is unusually well self-documented (`29.1-PREPUSH-AUDIT.md`, `29.1-FREEZE-RECORD.md`,
`29.1-GATE-BEFORE-AFTER.md`, `29.1-CONDITIONAL-GATE-RECORD.md`, `29.1-STALE-STRING-AUDIT.md`,
`29.1-INSTALL-SITES.md`, `deferred-items.md`) with claims stated at a level of precision that
is directly re-checkable. Rather than trust those documents, every load-bearing claim below was
independently re-derived from the current working tree, git history, and (where practical) by
executing code:

- Re-ran `check_rerun_gates.py --profile full` against the archived 2026-08-20 output myself and
  got `TOTAL: 176 PASS, 7 N/A, 0 FAIL` — matches every document's claim.
- Ran the three named D4 anchor tests directly (not from a saved log) and reproduced
  `3 failed` with the exact same node ids and failure shapes documented.
- Ran plan 29.1-02's `TestBandBenchmarkWritePolicy` and `TestBandScopeIsDerived` directly — 13/13
  passed, giving direct behavioral evidence for SC-3 and SC-4 (both state-transition /
  write-policy truths that presence-checking alone cannot confirm).
- Inspected `experiments/e4_benchmark_grid.py`, `experiments/check_rerun_gates.py`,
  `experiments/_expectations.py`, `experiments/suite_expectations.json`,
  `experiments/HANDOFF.md` directly for the SC-1, SC-5, SC-7 code claims rather than trusting
  summary prose.
- Verified `rerun-freeze-02`'s existence, sha, and identity both locally (`git tag`,
  `git show`) and via `git ls-remote --tags origin` against GitHub — confirms the tag was
  actually pushed, not just claimed.
- Independently confirmed `git diff --stat rerun-freeze-01..HEAD -- src/` is empty (src/
  untouched) and that the archive-aside commit (`24bf414`) is exactly 227 renames with 0
  creates/deletes.
- Inspected the still-present verification clone at
  `/home/tlancaster/aquacal-frozen-rerun-freeze-02` (left over from plan 29.1-08) and confirmed
  its `HEAD` is `7005a27`, its `experiments/results/` does not exist, and its editable install
  imports `aquacal` from inside that same clone under `aquacal-freeze02-clone` — reproducing the
  FREEZE-RECORD's fresh-clone assertions independently rather than re-reading them.

## Goal Achievement

### Observable Truths (Success Criteria, verbatim from ROADMAP.md)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | E4's real-rig row no longer fails Gate 1 for an unexplained reason | ✓ VERIFIED | `_extract_pipeline_row` (`e4_benchmark_grid.py:1345`) reads `degenerate_observations_at_solution` from E2's own record instead of hardcoding `None`; `_check_guard_column` (`check_rerun_gates.py`) exempts `record_source=="pipeline"` rows and names the exemption and its reason in its own PASS message. Re-ran the gate myself: `gate1_guard_count:benchmark_grid.csv` line reads `[PASS] ... 9 of 10 row(s) gated ... 1 row(s) exempt (real_rig_13cam_200fr=missing) -- a record_source='pipeline' row is a real-hardware anchor row whose count the library itself declines to treat as a verdict below pipeline.py:1288's 1% threshold...`. |
| SC-2 | `e1_seed_band_degeneracy_breakdown.json` exists or is unclaimed — consistently | ✓ VERIFIED | Grepped `suite_expectations.json`, `EXPECTATIONS.md`, `README.md` — the sidecar name appears nowhere in any of the three live manifests; it survives only inside `.planning/todos/done/*.md` closure records. The `e1_band completeness:...` FAIL line is absent from the re-run roll-up. |
| SC-3 | No log line claims a write that did not happen; the overwrite contradiction is resolved | ✓ VERIFIED (behavioral) | This is a state-transition truth (write-vs-skip, log-line-vs-actual-write), so presence alone would not suffice. Directly ran `tests/unit/test_e1_band_mode.py::TestBandBenchmarkWritePolicy` (6 cases, including `test_force_band_run_leaves_existing_records_byte_identical` and `test_kept_records_are_logged_and_no_write_is_claimed`) — **6/6 passed**. `_run_band`'s call site passes literal `force=False` regardless of the CLI `--force` flag (AST-checked in the plan's own verification and independently grepped: `write_direct_call_benchmark(..., force=False, ...)` at the call site with a comment explaining why). |
| SC-4 | E1's band scope string states the domain that was actually run | ✓ VERIFIED (behavioral) | Also a derived-value/state truth. Directly ran `TestBandScopeIsDerived` (7 cases) — **7/7 passed**. `scope` field is built from `len(seeds)`, the resolved seed list, and row counts read back off the frames just emitted, rather than a frozen ten-seed/640-960-row literal. |
| SC-5 | A fresh clone can run the suite by following the bundled instructions verbatim | ✓ VERIFIED | `experiments/HANDOFF.md:49` reads `python -m pip install -e ".[dev,bench]"`. Confirmed by execution, not just by reading the string: `aquacal-freeze02-clone` conda env still exists on disk; `python -c "import psutil, pytest, aquacal, sys; print(aquacal.__file__); print(sys.executable)"` in that env prints `/home/tlancaster/aquacal-frozen-rerun-freeze-02/src/aquacal/__init__.py` — i.e., the package imports from inside the clone built by that exact command, both extras present. |
| SC-6 | A new frozen sha is cut and tagged, replacing `rerun-freeze-01`, with the same on-target verification | ✓ VERIFIED | `git tag -l` shows `rerun-freeze-02` locally at commit `7005a2771aa115e4f4c1284cec7e145739586a4a`. `git ls-remote --tags origin` independently confirms the same tag object (`533f79fb...`) and dereferenced commit are live on `github.com/McGrathLab/AquaCal`. `rerun-freeze-01`'s tag object (`b31c8020...`) and commit (`3ab9c137...`) are unchanged, confirmed both locally and on the remote. The leftover verification clone at `/home/tlancaster/aquacal-frozen-rerun-freeze-02` independently confirms `HEAD=7005a27` and an empty `experiments/results/`. |
| SC-7 | The completeness gate stops reporting a PASS it cannot justify for conditional artifacts | ✓ VERIFIED | `experiments/suite_expectations.json` now declares both conditional artifacts' `dir` as `experiments/results_e2_invocations/e2_classification` (the directory the writer actually uses), not `experiments/results`. `experiments/_expectations.py` implements a three-verdict system (`PASS` when condition demonstrably did not hold, `FAIL` when it held or is found elsewhere, `N/A` when the predicate cannot be evaluated) replacing the old absence-is-PASS branch; grepped the codebase for `"legitimately absent"` — zero matches anywhere in `experiments/`. |

**Score:** 7/7 truths verified (0 present-but-behavior-unverified)

### Known, Ruled-On Exceptions (not gaps — read the phase's own framing)

These are explicitly **not** part of the seven success criteria and the phase's own documents
are explicit that they are open, unfixed, or a deliberate trade — reported here so this
verification does not silently absorb them into a clean pass, per the task's explicit
instruction to check the ruling is actually recorded rather than papered over.

1. **The full test suite does not pass (D4).** `pytest tests/` on this machine reports
   `2407 passed, 26 skipped, 3 failed`. **Independently reproduced**: I ran the three named
   tests directly —
   `tests/unit/test_discard_accounting.py::test_matches_frozen_anchor`,
   `tests/unit/test_optim_common.py::TestPerObservationDetailSinks::test_detail_sink_recomputed_geometry_matches_projector`,
   `tests/unit/test_pipeline.py::TestSolverConfigSeedIsInert::test_matches_pre_change_anchor`
   — and got `3 failed` in 37s, the exact same three node ids `29.1-PREPUSH-AUDIT.md` §1 and
   `deferred-items.md` D4 document. The ruling (proceed and record, not fix or loosen) is
   explicit in `29.1-PREPUSH-AUDIT.md` §1 and is also embedded in the `rerun-freeze-02` tag's
   own annotation message (`git show rerun-freeze-02 --no-patch`), so it survives independent of
   any single planning document. **No artifact claims "0 failed" for attempt 2** — checked:
   `29.1-FREEZE-RECORD.md`'s own comparison table explicitly marks attempt 2's full-suite row
   `2407 passed, 26 skipped, 3 FAILED — ✗ does not clear; ruled on, not waved past`, in contrast
   to attempt 1's `0 failed`. This exception is correctly and honestly recorded, not papered
   over.
2. **D1 — 8 `test_experiments_provenance.py` failures are dormant, not fixed.** Confirmed the
   mechanism: `experiments/results/` is empty (0 tracked files, verified with `git ls-files`),
   so `tests/unit/_baseline_paths.resolve_results_dir` falls back to the `pre_rerun_baseline`
   archive rather than genuinely resolving the defect. `deferred-items.md`'s "D1 UPDATE" section
   states this plainly ("Nothing was fixed... green here is the same green `rerun-freeze-01`
   had, not a repair") and warns the re-run's verifier will meet these 8 again. Honestly
   recorded.
3. **D3, D5, D6** — pre-commit formatting-hook exposure to committed artifacts, a `--smoke`/real-run
   state-file collision hazard, and `check_e2_band`'s `--smoke`-unaware directory resolution —
   are all filed as deliberate non-fixes with reasoning, not silently dropped. None of these
   affect SC-1..SC-7 as written.

None of the above are must-haves of this phase (they are explicitly out-of-scope defects or
platform findings the phase's own text says to record rather than resolve), so they do not
change the phase's `passed` status, but they are reported here in full per the verification
task's explicit instruction.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `experiments/e4_benchmark_grid.py` | Publishes E2's measured guard count for the pipeline row | ✓ VERIFIED | `_extract_pipeline_row` reads `discard_stats`/`problem_shape`; verified by direct inspection |
| `experiments/check_rerun_gates.py` | Exempts `record_source="pipeline"` rows, names why | ✓ VERIFIED | `_EXEMPT_RECORD_SOURCE = "pipeline"`, exemption message rendered in gate output; re-ran gate myself |
| `experiments/suite_expectations.json` | No E1 band sidecar entry; conditional artifacts point at real writer dirs | ✓ VERIFIED | grepped directly; `dir` fields now `results_e2_invocations/e2_classification` |
| `experiments/EXPECTATIONS.md`, `experiments/README.md` | Agree with `suite_expectations.json` | ✓ VERIFIED | sidecar absent from both; README stage 16 lists only the three artifacts actually written |
| `experiments/e1_refractive_comparison.py` | `force=False` literal at the band write call; derived `scope` field | ✓ VERIFIED | grepped call site; ran `TestBandBenchmarkWritePolicy`/`TestBandScopeIsDerived` directly (13/13 pass) |
| `experiments/HANDOFF.md` | `pip install -e ".[dev,bench]"` | ✓ VERIFIED | line 49, confirmed by execution against a live clone/env |
| `experiments/_expectations.py` | Three-verdict conditional-artifact resolution (PASS/FAIL/N/A) | ✓ VERIFIED | inspected `_resolve_dir`, the N/A branch, and the misplacement-search FAIL branch directly |
| `29.1-FREEZE-RECORD.md`, `29.1-PREPUSH-AUDIT.md` | Record the tag cut, fresh-clone verification, exposure audit, push | ✓ VERIFIED | cross-checked every quantitative claim (shas, tag objects, roll-up totals, test counts) against git and a live re-run |
| `rerun-freeze-02` (git tag) | Exists, pushed, `rerun-freeze-01` untouched | ✓ VERIFIED | `git tag`, `git ls-remote --tags origin` both confirm |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `e4_benchmark_grid._extract_pipeline_row` | `benchmark_grid.csv`'s guard-count cell | direct read of E2's `benchmark.json` | ✓ WIRED | confirmed by reading the function and by the gate's re-run output |
| `_check_guard_column`'s `bad_mask` | roll-up PASS/FAIL | `record_source` exemption | ✓ WIRED | re-ran gate myself, line reads PASS |
| `_run_band`'s write call | `write_direct_call_benchmark(force=...)` | literal `False`, not the CLI flag | ✓ WIRED (behavioral) | `TestBandBenchmarkWritePolicy::test_force_band_run_leaves_existing_records_byte_identical` passed directly |
| `_run_band`'s `scope` construction | `e1_seed_band_provenance.json` | read-back of just-emitted frame counts | ✓ WIRED (behavioral) | `TestBandScopeIsDerived` (7/7) passed directly |
| `suite_expectations.json`'s conditional `dir` | `aquacal.validation.diagnostics`'s actual output_dir | corrected manifest entry | ✓ WIRED | grepped `diagnostics.py` writer location vs. manifest `dir`; both `results_e2_invocations/e2_classification` |
| `HANDOFF.md §1.2`'s install command | a working environment that runs e3/gate3_run_manifest_fields | executed, not asserted | ✓ WIRED | `aquacal-freeze02-clone` env confirmed importing from the clone; FREEZE-RECORD's e3-exit-0 and gate3-PASS claims are internally consistent with independently-confirmed clone state |
| `git tag rerun-freeze-02` | GitHub remote | `git push` (named refs, no `--tags`/`--force`) | ✓ WIRED | `git ls-remote --tags origin` independently confirms identical tag object on the remote |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full-profile gate roll-up over the archived 2026-08-20 tree | `check_rerun_gates.py experiments/freeze01_run_output/results --profile full` | `TOTAL: 176 PASS, 7 N/A, 0 FAIL` | ✓ PASS |
| The three D4 anchor tests fail exactly as documented (not a claim of "passing") | `pytest tests/unit/test_discard_accounting.py::test_matches_frozen_anchor tests/unit/test_optim_common.py::TestPerObservationDetailSinks::test_detail_sink_recomputed_geometry_matches_projector tests/unit/test_pipeline.py::TestSolverConfigSeedIsInert::test_matches_pre_change_anchor -q` | `3 failed in 37.17s` | ✓ PASS (matches the ruled exception exactly) |
| E1 band write-policy and scope-derivation behavior (SC-3, SC-4) | `pytest tests/unit/test_e1_band_mode.py::TestBandBenchmarkWritePolicy tests/unit/test_e1_band_mode.py::TestBandScopeIsDerived -q` | `13 passed in 0.61s` | ✓ PASS |
| `src/` is byte-identical to `rerun-freeze-01` | `git diff --quiet rerun-freeze-01..HEAD -- src/; echo $?` | `0` | ✓ PASS |
| Archive move is a pure rename (D3-adjacent invariant asserted by SC-6/phase claims) | `git show -M 24bf414 --summary \| grep -c "^ rename"` and create/delete count | `227` renames, `0` create/delete | ✓ PASS |
| `rerun-freeze-02` is genuinely on the remote, not just local | `git ls-remote --tags origin \| grep rerun-freeze` | both `rerun-freeze-01` and `rerun-freeze-02` (+ deref) present, shas match local | ✓ PASS |

### Requirements Coverage

Per the phase's own `Requirements` field in ROADMAP.md ("Discharges the five todos listed under
Success Criteria; no new REQUIREMENTS.md IDs — this phase closes defects rather than adding
scope"), and confirmed by inspection of `.planning/REQUIREMENTS.md`: **no Phase 29.1 requirement
IDs exist in REQUIREMENTS.md**, and the file's own Traceability table reports "23/23 v2.1
requirements mapped" with no reference to 29.1 — consistent with the phase's stated intent. Each
PLAN's frontmatter instead declares `requirements: [SC-N]`, a phase-local label scheme, and all
seven SC-N labels (SC-1 through SC-7) are exercised by at least one plan:

| SC | Plans | Status |
|----|-------|--------|
| SC-1 | 29.1-01, 29.1-05 (evidence), 29.1-06 (measurement) | ✓ SATISFIED |
| SC-2 | 29.1-02, 29.1-06 | ✓ SATISFIED |
| SC-3 | 29.1-02 | ✓ SATISFIED |
| SC-4 | 29.1-02, 29.1-03 | ✓ SATISFIED |
| SC-5 | 29.1-04, 29.1-08 | ✓ SATISFIED |
| SC-6 | 29.1-07, 29.1-08 | ✓ SATISFIED |
| SC-7 | 29.1-09, 29.1-06 (measurement) | ✓ SATISFIED |

No orphaned requirements: the phase declares no global REQUIREMENTS.md IDs to check against,
and this was independently confirmed (no `29.1` or `SC-` rows exist in `.planning/REQUIREMENTS.md`).

### Anti-Patterns Found

Scanned every file touched by this phase's diff (`git diff results/rerun-freeze-01..HEAD --
<phase files>`) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` in added lines only (to avoid flagging
pre-existing text): **zero matches**. No debt markers introduced by this phase. No stub patterns
(`return null`, empty handlers, hardcoded-empty data flowing to output) found in the modified
gate/writer code — every changed code path was traced to a real read (`benchmark.json`,
`diagnostics.py`'s output, the just-emitted frame counts) rather than a static literal.

### Deviations Documented Per-Plan (spot-checked, not just trusted)

Two plans deviated from their own plan text, as the task briefing flagged:

- **29.1-03** widened its enumeration command mid-pass (from a `#`/quote-filtered grep to an
  unfiltered one) after finding it silently dropped a wrapped-docstring defect at `e1:43`, and
  additionally corrected two twin sites outside its declared file boundary
  (`_expectations.py:23`, `check_rerun_gates.py:621`/`:673`) on the grounds that the planner's
  stated exclusion premise ("mechanism, not measured values") was falsified by
  `_expectations.py` carrying the identical stale claim. Both deviations are logged with
  Rule 1/Rule 2 classifications, before/after evidence, and commit hashes. Independently
  re-checked: `check_rerun_gates.py:673`'s frozen-rate deletion and `_expectations.py:23`'s
  correction are both present in the current tree.
- **29.1-09** hit three Rule 1/3 auto-fixes (a stale `EXPECTATIONS.md` render, a test fixture
  duplicating `_resolve_dir`'s rule instead of calling it, and a scope note about which commit a
  durable test landed in) — all documented with before/after evidence and confirmed to have zero
  effect on any measured value or gate verdict (the roll-up stayed `176/7/0` throughout).

Both plans' corrective work is the correct fix for the defect they found (confirmed by direct
inspection of the corrected code, not just the summary's claim), and neither introduced scope
creep beyond what the deviation itself required.

### Human Verification Required

None. Every must-have truth was either directly re-executed (SC-3, SC-4, D4's three failures) or
confirmed by direct inspection of code/git state that leaves no interpretive gap (SC-1, SC-2,
SC-5, SC-6, SC-7). The one genuinely judgment-laden question this phase carried — whether to cut
the tag with 3 known anchor failures — was already resolved by the author's explicit approval on
2026-08-24, recorded in `29.1-PREPUSH-AUDIT.md` §7.5 and corroborated independently via the
pushed tag's own annotation message, which is the least-forgeable record of that ruling.

### Gaps Summary

No blocking gaps. All seven ROADMAP success criteria (SC-1 through SC-7) are independently
verified against the current codebase and git state, not merely against SUMMARY.md prose. The
phase's own documented exceptions (D1, D4 primarily) are honestly recorded as exceptions rather
than concealed, and this verification independently reproduced the evidence behind both (the
3-test pytest failure, the D1 dormancy mechanism).

One minor, non-blocking documentation-hygiene observation: `ROADMAP.md`'s top-level phase
checklist (line 112) still shows `- [ ]` for "Phase 29.1: Post-Run Fixes & Re-Freeze", and the
milestone Progress table does not yet carry a Phase 29.1 row — both inconsistent with the
phase's own plan checklist (`- [x]` on all 9 plans, line 609-617) and with `STATE.md`, which
records the phase as finished. This does not affect any success criterion and is not a gap in
the phase's delivered work, only in ROADMAP.md's own bookkeeping of it.

---

_Verified: 2026-08-24_
_Verifier: Claude (gsd-verifier)_
