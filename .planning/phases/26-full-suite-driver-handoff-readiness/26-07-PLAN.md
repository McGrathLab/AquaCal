---
phase: 26-full-suite-driver-handoff-readiness
plan: 07
type: execute
wave: 3
depends_on: ["26-01", "26-03"]
files_modified:
  - experiments/run_experiment_suite.sh
  - experiments/rerun_19_3.sh
  - .gitignore
  - tests/unit/test_suite_stage_list.py
autonomous: true
requirements: [DRIVER-01]

must_haves:
  truths:
    - "One driver, `experiments/run_experiment_suite.sh`, covers every invocation in the suite — the four band stages, E2's four invocations, and the three orphan scripts (D-01, D-25)."
    - "The driver is the union of the three existing drivers, lifted from `rerun_19_5.sh` (the most evolved base), not an extension of `rerun_19_3.sh` (ruling A3)."
    - "The state file's path embeds the frozen sha, so a state file from another commit is structurally unreachable (D-23 as halved by D-48)."
    - "A dry run cannot write the real run's state file (the 19.5 fix, absent from 19.3)."
    - "The gate interpreter is pinned with a fallback, never bare `python` (the 19.3 defect fixed in 19.4/19.5)."
    - "`e6_repeat2` is not a stage (D-42), and `--include-per-camera-latex` stays OFF (D-11)."
    - "Every declared stage has an expectation entry and every expectation has an owning stage, enforced by a unit test (D-33 form 3)."
  artifacts:
    - path: "experiments/run_experiment_suite.sh"
      provides: "The single suite driver: stage functions for every invocation, resumable state, pinned gate interpreter"
      contains: "STAGES=("
    - path: "tests/unit/test_suite_stage_list.py"
      provides: "Bidirectional stage/expectation coupling and the topological-order assertion over depends_on"
  key_links:
    - from: "tests/unit/test_suite_stage_list.py"
      to: "experiments/run_experiment_suite.sh"
      via: "regex parse of the STAGES=(...) array"
      pattern: "STAGES="
    - from: "tests/unit/test_suite_stage_list.py"
      to: "experiments/suite_expectations.json"
      via: "load_expectations()"
      pattern: "suite_expectations"
---

<objective>
Build the one driver. CONTEXT frames this as "rename and extend `rerun_19_3.sh`", but there are
THREE drivers on disk and `rerun_19_4.sh` (417 lines) and `rerun_19_5.sh` (487 lines) already
implement the band stages, `e4_repeat`, the hard-abort pre-flight probe, the pinned `GATE_PYTHON`
and the dry-run state-file separation — none of which 19.3 has. Ruling A3: this is a
**union-and-lift from 19.5**, and only seven invocations are genuinely missing.

Purpose: the six-sha provenance spine fractured precisely at the invocations no driver ran. One
driver invocation must cover the entire suite, so nothing is left for the Linux machine to discover
is missing.
Output: `experiments/run_experiment_suite.sh`, an updated `.gitignore`, and
`tests/unit/test_suite_stage_list.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/26-full-suite-driver-handoff-readiness/26-CONTEXT.md
@.planning/phases/26-full-suite-driver-handoff-readiness/26-RESEARCH.md
@.planning/phases/26-full-suite-driver-handoff-readiness/26-VALIDATION.md
@.planning/todos/pending/2026-08-15-make-the-suite-driver-cover-every-invocation.md
</context>

<interfaces>
<!-- Extracted from the codebase. Use directly; the executor should not have to hunt. -->

Machinery to LIFT from `experiments/rerun_19_5.sh` (the most evolved base):
- `is_stage_complete()` `:232-238` — awk over the state TSV; a start-line without a completion line
  means re-run from scratch.
- `state_start` `:240-244` / `state_complete` `:245-249` — ISO-stamped; **the only per-stage timing
  record that exists anywhere in this project.**
- `run_gate_check()` `:250-263` — PINNED interpreter version; always returns 0 so a gate FAIL never
  aborts the queue.
- `GATE_PYTHON` pin + fallback `:219-223` —
  `"${PRELAUNCH_GATE_PYTHON:-$HOME/anaconda3/envs/AquaCal/python.exe}"`, warning and falling back to
  bare `python` if absent. ⚠ `rerun_19_3.sh:121` uses bare `python`, a known defect: the gate imports
  pandas AND `aquacal.datasets.synthetic` / `experiments.e4_benchmark_grid`, and Git Bash's `python`
  is Anaconda base.
- Dry-run state-file separation `:174-185` — **absent from 19.3.** Without it a dry run leaves a
  state file that makes the next real launch a silent no-op: every stage skipped, exit 0, no
  artifacts. Found 2026-08-06 by dry-running the script and inspecting what it left behind.
- `_dry_run_active()` `:265-274` / `_dry_run_stub()` `:276-278` — `RERUN_19_5_DRY_RUN` /
  `RERUN_19_5_DRY_RUN_CMD`. Every stage function routes through them.
- `run_one_stage()` `:425-470` — skip-if-complete, skip-if-below-`START_STAGE`, `state_start`,
  `case` dispatch, `state_complete`, then the per-stage `run_gate_check` with a `case` picking the
  out dir.
- `main()` `:472-487` — `cd "${REPO_ROOT}"`, the `set -u -o pipefail` preamble at `:166`, and the
  documented detached launch `nohup bash <script> > <log> 2>&1 & disown`.
- `e6_repeat2`'s isolation template lives in `rerun_19_3.sh:204-228` — `tee` + `PIPESTATUS[0]` +
  `grep -c "already exists (resumability)"` (0 expected). Keep the technique available for any
  stage that must not reuse checkpoints, even though the stage itself is OFF (D-42).

Every invocation already present across the three drivers (verbatim), with its source:
`e3 --check` / `e3 --force` `rerun_19_3.sh:150,157` · `e7 --force` `19_4:298` ·
`e7 --seeds` `19_4:305` · `e1 --force` `19_4:322` · `e1 --seeds` `19_4:329` ·
`e5 --force` `19_3:184` · `e5 --seeds ... --force` `19_5:421-422` ·
`e6_repeat1` (rm -rf `${OUT_DIR}/e6_configs`, rm -f `generalization_sweep.csv e6_provenance.json`,
then `--force`) `19_3:195-200` · `e6 --seeds ... --force` `19_5:326-327` ·
`e4 --force` `19_3:235` · `e4_repeat` (3 cells x 2 repeats then `--splice-repeat`) `19_5:349-372` ·
`e2_band` emit `19_5:389-393` and per-seed run `19_5:404-407` ·
`prelaunch_probe` heredoc calling `experiments.check_rerun_gates.legality_probe` `19_5:296-311`.

The SEVEN genuinely missing invocations:
M1 E2 production/classification · M2 E2 timing · M3 E2 memory (each `--config <variant> --out <dir>
--force`, variants generated by plan 26-06's `--emit-invocation-configs`) ·
M4 `python -u -m experiments.e7_focal_standoff_analysis --out "${OUT_DIR}"` ·
M5 `python -u -m experiments.reconstruction_bootstrap --out "${OUT_DIR}" --force` ·
M6 `python -u -m experiments.fd_jacobian_accuracy --out "${OUT_DIR}" --force` ·
M7 E1's noise-axis band (uniform grid per ruling A1 — no new E1 flag; `NOISE_LEVELS` at
`e1_refractive_comparison.py:217` is already `[0.25, 0.5, 0.82, 1.2]` and `_run_band` is a strict
cartesian `seeds x NOISE_LEVELS`).

`.gitignore` lines that pin the OLD stem and need a new-stem counterpart: `:256-258`
(`experiments/rerun_19_5_state.dryrun.tsv`), `:267` (`!experiments/rerun_19_5.log`).
</interfaces>

<executor_rules>
- Worktree executors MUST `export PYTHONPATH="$(pwd)/src"` before pytest.
- Interpreter: `$HOME/anaconda3/envs/AquaCal/python.exe`.
- **Do NOT run `python -m pytest tests/`** — the orchestrator runs the unfiltered suite post-merge.
- **Do NOT execute the driver at any scale, not even `--smoke`.** The full `--smoke` pass is the
  ORCHESTRATOR's run (D-34). An executor that backgrounds a long run has stalled permanently and
  will not come back — this has cost this project hours across phases 19.2–19.4. You may run
  `bash -n experiments/run_experiment_suite.sh` (syntax check) and nothing else.
- **Do NOT delete or move `experiments/rerun_19_4.sh` or `experiments/rerun_19_5.sh`.** They are the
  source of the lift and are archived by plan 26-09, AFTER their stage functions are in the new
  driver. Deleting them before the lift is verified destroys the reference.
- **Do NOT rewrite the driver in Python** (D-26). The bash script encodes `tee`/`PIPESTATUS` exit
  capture, the resumability skip grep, the started-versus-completed distinction and `disown`
  semantics, all easy to lose in translation.
- **Do NOT implement pre-flight, the sticky-failure flag, the end-of-run roll-up, or the concurrency
  pool** — plan 26-08 owns them and depends on this plan's stage list.
- **Do NOT edit `experiments/suite_expectations.json`** (plan 26-03 owns it). If a stage id here
  disagrees with the manifest, change THIS file, or report the discrepancy.
- Write portability-sensitive constructs conservatively (`date -u`, `awk`, `du`, `git describe`) so
  Phase 27's Linux check finds nothing (D-35).
</executor_rules>

<decisions_implemented>
D-25 (`git mv` to `experiments/run_experiment_suite.sh`, state and frozen-sha files follow the same
stem, header claims the entry-point role), D-26 (no Python rewrite), D-23 as HALVED by D-48 (keep
the sha-derived state path, cut the HEAD-vs-state refusal), D-27 (only the RUN MACHINE's tree must
not move; planning-box commits continue during the run), D-37 (shortest-first ordering, subject to
`depends_on`), D-11 (`--include-per-camera-latex` stays OFF), D-42 (`e6_repeat2` OFF), D-12
(`--baseline-dir` threaded at the archive), D-15/D-16 (E2's four invocations), D-40 (E6's `--axes`),
ruling A1 (uniform E1 noise grid), ruling A3 (union-and-lift from 19.5).
</decisions_implemented>

<tasks>

<task type="auto">
  <name>Task 1: git mv the driver to run_experiment_suite.sh and lift 19.5's machinery and header</name>
  <read_first>
    - `experiments/rerun_19_5.sh` in full (487 lines) — it is the base
    - `experiments/rerun_19_3.sh` header lines 1-100 (the shortest-first rationale, the
      started-vs-completed state distinction, E3's ordering, the no-tree-mutation guarantee, the
      `nohup ... & disown` launch line) and `:204-228` (the `e6_repeat2` isolation template)
    - `.planning/phases/26-full-suite-driver-handoff-readiness/26-CONTEXT.md` D-01, D-03, D-23,
      D-25, D-26, D-27, D-37, D-48, D-50
    - `.gitignore:250-270`
  </read_first>
  <files>experiments/run_experiment_suite.sh, experiments/rerun_19_3.sh, .gitignore</files>
  <action>
    `git mv experiments/rerun_19_3.sh experiments/run_experiment_suite.sh` — D-25 chose this name
    over `run_suite.sh` because in this repo "run the suite" already means pytest in every CLAUDE.md
    warning. Then replace its body with the lifted 19.5 machinery listed in `<interfaces>`.

    State and frozen-sha files follow the same stem, and the state path embeds the frozen sha
    (D-48's surviving half of D-23):
    `experiments/run_experiment_suite_state.<short_sha>.tsv`, with the dry-run variant at
    `...state.<short_sha>.dryrun.tsv`. A state file from another commit is then structurally
    unreachable rather than merely detected. **Do NOT add the HEAD-vs-state refusal** — D-48 cut it
    because it is the half that can wrongly block a 3 a.m. resume. State files accumulate one per
    sha; that is acceptable and should be said in the header.

    Rename the dry-run environment variables to the new stem:
    `RUN_EXPERIMENT_SUITE_DRY_RUN` and `RUN_EXPERIMENT_SUITE_DRY_RUN_CMD`. Keep
    `PRELAUNCH_GATE_PYTHON` unchanged — it is shared with `prelaunch_gate.sh`. No test sets the old
    names today (verified), so this is a driver-local rename plus the doc references.

    Write the header. It must state, together and adjacently, the two rules that look contradictory
    to a reader who meets only one:
    - **D-01:** a completeness or gate FAILURE never aborts the queue; it sets a sticky flag that
      makes the driver's FINAL exit non-zero, with a loud terminal summary naming every missing or
      short artifact. The rationale, verbatim in substance: this project's injury has never been "we
      kept running after a gate failed" — it has been **a run that exited 0 and looked green while a
      band CSV was never produced at all** (F-001).
    - **D-03:** PRE-FLIGHT failure ABORTS. It runs before stage 1, so nothing is lost, and the whole
      point is trading minutes for twenty-plus hours. D-01 governs stages 1..N only.
    - **D-50:** every pre-flight refusal prints the exact override flag that bypasses it, and
      nothing may abort once stage 1 has begun.
    - **D-27, restated precisely:** the commit rule relaxes under two-machine operation. The real
      constraint is that the RUN MACHINE's tree must not move — no pull, checkout or commit *there*.
      Work on the planning box, including commits and pushes, is safe and is expected to continue
      during the run.
    - **D-37:** shortest-first ordering holds — its purpose is to surface a systematic failure in
      seconds rather than after the longest stage — **but `depends_on` wins over wall clock**, and
      name the case: `e4` must follow E2's production run or `resolve_e2_benchmark_path`
      (`e4_benchmark_grid.py:298`) silently drops the real-rig row and `benchmark_grid.csv` comes
      back with 9 rows instead of 10.
    - The entry-point claim: this is THE suite driver; `rerun_19_{3,4,5}.sh` are historical and are
      archived in plan 26-09.
    - The detached launch line, unchanged in form:
      `nohup bash experiments/run_experiment_suite.sh > experiments/run_experiment_suite.log 2>&1 & disown`,
      with the standing NEVER-`run_in_background` warning.

    Update `.gitignore`: add new-stem counterparts to `:256-258` (ignore
    `experiments/run_experiment_suite_state.*.dryrun.tsv`) and `:267` (un-ignore
    `!experiments/run_experiment_suite.log`, since the production queue log IS a citable artifact —
    the human-readable record of which seeds were requested). Leave the old-stem rules alone; plan
    26-01 already moved those files.
  </action>
  <verify>
    <automated>bash -n experiments/run_experiment_suite.sh && grep -q 'GATE_PYTHON' experiments/run_experiment_suite.sh && grep -q 'RUN_EXPERIMENT_SUITE_DRY_RUN' experiments/run_experiment_suite.sh && ! grep -q 'RERUN_19_3_DRY_RUN' experiments/run_experiment_suite.sh && test ! -e experiments/rerun_19_3.sh && test -f experiments/rerun_19_5.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n experiments/run_experiment_suite.sh` exits 0 and `experiments/rerun_19_3.sh` no longer
      exists (`git log --follow --oneline -1 -- experiments/run_experiment_suite.sh` shows the rename).
    - `grep -c 'python\b' experiments/run_experiment_suite.sh` shows no BARE `python` used for the
      gate: verify `grep -n 'check_rerun_gates' experiments/run_experiment_suite.sh` and confirm
      every such line uses `"${GATE_PYTHON}"`.
    - The state-file assignment contains a short-sha expansion; confirm with
      `grep -n 'STATE_FILE=' experiments/run_experiment_suite.sh` showing a `rev-parse`-derived
      component, and that the dry-run branch assigns a DIFFERENT path.
    - `grep -c 'HEAD' experiments/run_experiment_suite.sh` does not introduce a refusal: there is no
      `exit` guarded on a state-file sha disagreeing with HEAD (D-48 cut it). Confirm by reading.
    - The header contains all of: `D-01`, `D-03`, `D-27`, `D-37`, `D-50`, and the string `F-001`.
    - `grep -c 'run_experiment_suite' .gitignore` returns at least 2.
    - `experiments/rerun_19_4.sh` and `experiments/rerun_19_5.sh` are still present and unmodified
      (`git diff --name-only` lists neither).
  </acceptance_criteria>
  <done>The driver is renamed, carries 19.5's machinery with a sha-derived state path and a pinned gate interpreter, and its header states D-01/D-03/D-27/D-37/D-50 together.</done>
</task>

<task type="auto">
  <name>Task 2: Populate the full stage list — every invocation, with depends_on made structural</name>
  <read_first>
    - `experiments/suite_expectations.json` (plan 26-03) — the stage ids, `depends_on`,
      `concurrency`, `out_dir` and `invocation` strings this task must match EXACTLY
    - `experiments/rerun_19_5.sh:280-424` (every existing stage function body, verbatim)
    - `experiments/rerun_19_4.sh:270-340` (the `e7`/`e7_band`/`e1`/`e1_band` stage functions)
    - `experiments/rerun_19_3.sh:145-240` in the git history (`git show HEAD~1:experiments/rerun_19_3.sh`)
      for `e3`, `e5`, `e6_repeat1`, `e4` and the `e6_repeat2` isolation template
    - `.planning/phases/26-full-suite-driver-handoff-readiness/26-RESEARCH.md`
      § The seven invocations genuinely absent, § Ordering constraints — five, not three, § SP-8
  </read_first>
  <files>experiments/run_experiment_suite.sh</files>
  <action>
    Write one `run_stage_<id>` function per stage id in `experiments/suite_expectations.json`, and a
    `STAGES=(...)` array listing them in a **topological order of the manifest's `depends_on` edges**,
    shortest-first within each dependency level (D-37). The bash array's ids must match the
    manifest's ids character for character — plan 26-03's test and this plan's Task 3 assert it.

    Lift each existing stage function verbatim from its source driver; do not re-derive flags from
    memory. Then add the seven missing invocations:
    - `e2_production`: run plan 26-06's `--emit-invocation-configs` into a variable-controlled
      directory, then invoke E2 with `--config <...config_e2_classification.yaml> --out "${OUT_DIR}"
      --force`. This is the run that carries `internals.log_all_observation_depths: true` (D-16) and
      the run that writes `experiments/results/benchmark.json`, which `e4` depends on (O4).
    - `e2_timing` and `e2_memory`: the other two generated configs, each into its OWN out dir
      (`OUT_DIR_E2_TIMING`, `OUT_DIR_E2_MEMORY`) so the gate can attribute artifacts to invocations.
      Both are `serial_alone` (D-52) — this task only declares them; plan 26-08 enforces it.
    - `e7_focal_standoff`: `python -u -m experiments.e7_focal_standoff_analysis --out "${OUT_DIR}"`.
      It reads the HARDCODED cwd-relative `Path("experiments/results")/interface_ablation_band.csv`
      (`:389`; its docstring says this is deliberate, "never the `--out` directory"), so it must
      follow `e7_band` and the driver's `cd "${REPO_ROOT}"` is what makes it resolve.
    - `reconstruction_bootstrap`: `--out "${OUT_DIR}" --force`. It reads
      `experiments/results/reconstruction_errors.csv` and the hard
      `REAL_RIG_METRICS_PATH = Path("experiments/results/real_rig_metrics.json")` (`:56`), so it must
      follow `e2_production`.
    - `fd_jacobian`: `--out "${OUT_DIR}" --force`. No external input; place it early — it is seconds
      and it exercises the queue's plumbing (D-37's purpose).
    - `e1_band`: the uniform noise grid of ruling A1. Set `E1_BAND_SEEDS` to a FOUR-seed list and
      invoke `python -u -m experiments.e1_refractive_comparison --seeds "${E1_BAND_SEEDS}" --out
      "${OUT_DIR}"`. **No new E1 flag exists and none is needed** — `_run_band` already loops
      `NOISE_LEVELS` (`[0.25, 0.5, 0.82, 1.2]`, `:217`) inside the per-seed runner, giving
      4 x 4 x 16 = 256 rows of `exp1_band.csv` and 384 of `exp1_parameter_band.csv`. Comment this
      arithmetic at the stage, and comment that 0.5 px is in the grid because the headline 97–178x
      band and all 16 ledger numbers backed by `exp1_band.csv` live there.
    - `e6_band`: add `--axes index,layout,cameras` (plan 26-05) for D-40's 14 x 6 = 84 rows.

    Thread `--baseline-dir "${BASELINE_DIR}"` into the two `--check` invocations that now accept it
    — E3's `--check` (which runs immediately before its `--force`, one atomic stage, O5) and any E2
    `--check` — with `BASELINE_DIR="experiments/pre_rerun_baseline/results"` as a variable at the
    top of the file, overridable by an environment variable so Phase 27 can repoint it.

    Make `E2_RELEASE_CONFIG` a variable with an environment override rather than the current literal
    `C:/Users/tucke/Desktop/Aqua/AquaCal/release_calibration/config.yaml` (`rerun_19_5.sh:209`) — it
    resolves on this box only and cannot travel to the Linux machine. Phase 27 owns portability, but
    the stage list must make it overridable, not literal.

    Encode every ordering constraint as a comment at the stage naming the anchor line, so a future
    reader who reorders the array meets the reason. Array order alone cannot express these; plan
    26-08's pool honours the manifest's `depends_on`.

    Declare, as comments at the relevant stages: `--include-per-camera-latex` stays OFF (D-11 — the
    flag renders `shared_interface=False` rows into `cpr_grouping.tex`, `tab:cpr` at
    `supplement.tex:449` has six rows all shared-interface, and the generated fragment is not
    `\input` anywhere); and `e6_repeat2` is deliberately absent (D-42 reverses D-09), with the
    isolation template retained in a comment for any future stage that must not reuse checkpoints.

    Keep every stage routed through `_dry_run_active` / `_dry_run_stub`, including the new ones.
    Plan 26-08's dry-run tests depend on this being universal.
  </action>
  <verify>
    <automated>bash -n experiments/run_experiment_suite.sh && $HOME/anaconda3/envs/AquaCal/python.exe -c "import json,re; man=json.load(open('experiments/suite_expectations.json')); txt=open('experiments/run_experiment_suite.sh').read(); arr=re.search(r'STAGES=\(([^)]*)\)', txt).group(1).split(); ids={s['id'] for s in man['stages']}; assert set(arr)==ids, (set(arr)^ids); assert all(('run_stage_'+a) in txt for a in arr); print('ok', len(arr))"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok` with the stage count matching the manifest.
    - `grep -c 'e7_focal_standoff_analysis\|reconstruction_bootstrap\|fd_jacobian_accuracy' experiments/run_experiment_suite.sh`
      returns at least 3 — the three orphan scripts are in the queue.
    - `grep -c 'config_e2_classification\|config_e2_timing\|config_e2_memory' experiments/run_experiment_suite.sh`
      returns at least 3 — E2's four invocations are distinct.
    - `grep -c 'e6_repeat2' experiments/run_experiment_suite.sh` finds it only inside a comment;
      `grep -c 'run_stage_e6_repeat2' experiments/run_experiment_suite.sh` returns 0.
    - `grep -c 'include-per-camera-latex' experiments/run_experiment_suite.sh` finds it only in a
      comment explaining that it stays off.
    - `grep -c 'axes index,layout,cameras' experiments/run_experiment_suite.sh` returns 1.
    - `grep -c 'baseline-dir' experiments/run_experiment_suite.sh` returns at least 1, and
      `BASELINE_DIR` is assigned with a `:-` environment override.
    - `E2_RELEASE_CONFIG` is assigned with a `:-` environment override, not a bare literal.
    - Every `python -u -m experiments.` invocation uses `-u` (unbuffered); confirm
      `grep -c 'python -m experiments\.' experiments/run_experiment_suite.sh` returns 0.
    - No `run_stage_` function bypasses `_dry_run_active`; confirm the count of `_dry_run_active`
      occurrences equals the number of `run_stage_` definitions.
  </acceptance_criteria>
  <done>Every stage id in the manifest has a `run_stage_` function, the seven missing invocations are present, and the array is a topological order of the manifest's dependency edges.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write tests/unit/test_suite_stage_list.py — stage/expectation bijection and ordering</name>
  <read_first>
    - `experiments/suite_expectations.json` and `experiments/_expectations.py` (plan 26-03)
    - `experiments/run_experiment_suite.sh` (Tasks 1 and 2)
    - `tests/unit/test_experiments_provenance.py:585-660` — the BIDIRECTIONAL assertion pattern
    - `.planning/phases/26-full-suite-driver-handoff-readiness/26-VALIDATION.md` § Per-Task
      Verification Map, DRIVER-01 rows
  </read_first>
  <files>tests/unit/test_suite_stage_list.py</files>
  <behavior>
    - Every id in the driver's `STAGES=(...)` array has a manifest entry, and every manifest stage id
      appears in the array — BIDIRECTIONAL, in the shape of
      `test_all_committed_csvs_have_a_named_record` / `test_csv_to_record_has_no_stale_entries`.
    - Every id in the array has a matching `run_stage_<id>` function definition in the script.
    - The array order is a valid topological order of the manifest's `depends_on` edges: for every
      edge `a -> b`, `a`'s index is strictly less than `b`'s.
    - The four named ordering constraints hold specifically, each with its own test and a failure
      message naming the anchor: O1 `e7_focal_standoff` after `e7_band`; O2
      `reconstruction_bootstrap` after `e2_production`; O4 `e4` after `e2_production`; D-52
      constraint 1, `e6_band` after `e6_repeat1`.
    - `e6_repeat2` is in neither the array nor the manifest (D-42).
    - The driver contains no bare `python ` invocation of `check_rerun_gates.py` — the pinned
      `GATE_PYTHON` is used everywhere.
    - The dry-run state path differs from the real state path (the 19.5 fix), asserted by parsing
      both assignments out of the script.
  </behavior>
  <action>
    Create `tests/unit/test_suite_stage_list.py`. Parse the driver with a module-level regex over
    `STAGES=\(([^)]*)\)` and a second over `^run_stage_([a-z0-9_]+)\(\)`, and load the manifest via
    `experiments._expectations.load_expectations()`. Degrade to a `pytest.skip` (not an error) if
    `experiments/run_experiment_suite.sh` is absent, matching the collection-time degradation pattern
    the provenance tests use.

    Implement the topological check yourself over the `depends_on` edges — do not add a dependency.
    On failure, the message must name the offending edge and both indices.

    Do NOT execute the driver. Every assertion is textual or over the manifest.
  </action>
  <verify>
    <automated>$HOME/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_suite_stage_list.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_suite_stage_list.py -x -q` passes with at least 9 tests collected.
    - The bidirectional test genuinely fails when broken: temporarily delete one id from the
      `STAGES=(...)` array, confirm the test fails naming that id, then restore. Record the observed
      message in the SUMMARY.
    - `grep -c 'subprocess' tests/unit/test_suite_stage_list.py` returns 0 — the driver is never
      executed by this file.
    - The four ordering tests exist by name; confirm with
      `pytest tests/unit/test_suite_stage_list.py --collect-only -q | grep -c 'order'` returning at
      least 4.
  </acceptance_criteria>
  <done>The stage list is coupled bidirectionally to the expectation manifest and its order is proven to respect every declared dependency edge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state file to queue behaviour | A consumable stale state file makes the whole suite a silent no-op that exits 0 |
| bash variable to `rm -rf` target | Isolated-dir stages delete a directory named by a variable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-21 | Spoofing | a renamed script consuming the old state file: every stage skipped, exit 0 | mitigate | D-48's sha-derived state path makes a foreign state file structurally unreachable; a test asserts the dry-run path differs from the real one |
| T-26-22 | Denial of service (own run) | a dry run leaving a state file that no-ops the next real launch | mitigate | Lift `rerun_19_5.sh:174-185`'s separate dry-run `STATE_FILE`; asserted textually |
| T-26-23 | Tampering | `rm -rf` on an interpolated path | mitigate | `OUT_DIR_E6_REPEAT2` / `OUT_DIR_E4_REPEAT` / `OUT_DIR_E2_BAND` stay LITERALS, never interpolated from argv, and `set -u` turns an unset variable into an error rather than `rm -rf /` |
| T-26-24 | Tampering (accidental) | the absolute `E2_RELEASE_CONFIG` path pointing outside the repo | mitigate | Made an overridable variable; `emit_seed_variant_configs`' release-tree write refusal (`e2_real_rig.py:874-878`) is preserved |
| T-26-25 | Repudiation | a gate silently ImportError-ing under bare `python` | mitigate | Pinned `GATE_PYTHON` with a warning fallback; a test asserts no bare-`python` gate invocation |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | This phase installs nothing; `pyproject.toml` is not in `files_modified` |
</threat_model>

<verification>
- `bash -n experiments/run_experiment_suite.sh` exits 0.
- `pytest tests/unit/test_suite_stage_list.py -q` green.
- `experiments/rerun_19_4.sh` and `rerun_19_5.sh` still present and unmodified.
</verification>

<success_criteria>
One driver on the path the header claims, covering every invocation in the suite, with the stage
list proven bidirectionally coupled to the expectation manifest and topologically consistent with
every declared ordering constraint.
</success_criteria>

<output>
Create `.planning/phases/26-full-suite-driver-handoff-readiness/26-07-SUMMARY.md` when done.
</output>
