---
phase: 26-full-suite-driver-handoff-readiness
plan: 13
type: execute
wave: 8
depends_on: ["26-11"]
gap_closure: true
files_modified:
  - experiments/e1_refractive_comparison.py
  - experiments/e7_interface_ablation.py
  - tests/unit/test_experiments_provenance.py
autonomous: true
requirements: [DRIVER-03]

must_haves:
  truths:
    - "`gate3_provenance` requires a seed at `record['seed']` or `record['solver_config']['seed']` (`check_rerun_gates.py:374-378`). E1's and E7's benchmark records carry NEITHER — and the records the 26-10 smoke pass wrote TODAY, with current code, still lack it. This is a live call-site gap, not a legacy-artifact problem."
    - "Six gated artifacts fail today and would fail identically in the frozen run: `e1_benchmark_refractive.json`, `e1_benchmark_nonrefractive.json`, and `e7_benchmark_{shared,percamera}_{fixed,refined}.json`."
    - "The writer has taken the seed since plan 19.2-02. `write_direct_call_benchmark` (`experiments/_io.py:788`) accepts an optional `seed` and folds it into a SHALLOW COPY as `solver_config['seed']`. What 19.2-02 did NOT do is pass it at E1's and E7's call sites — which is why regenerating those six records has never added a seed."
    - "`tests/unit/test_experiments_provenance.py` carries a `SEEDLESS_LEGACY_RECORDS` carve-out naming exactly these six, whose comment asserts the exemption 'is removed the moment any of these six is regenerated'. That premise is FALSE as written: regeneration alone never stamped a seed, because no call site passed one. The comment must be corrected, not merely worked around."
    - "That test module's `RESULTS_DIR` points at `experiments/pre_rerun_baseline/results` (`:45`), the ARCHIVED tree — so the carve-out tests keep passing regardless of this change, and this plan does not break them."
    - "E4 and E6 already satisfy the gate: `e4_cells/*/benchmark.json` carries `solver_config.seed=42` and `e6_configs/*.json` a top-level seed. E5's sidecar carries `seed` directly. E1 and E7 are the entire gap, which is why the failing set is exactly six."
  artifacts:
    - path: "experiments/e1_refractive_comparison.py"
      provides: "Seed provenance on E1's benchmark records"
      contains: "seed="
    - path: "experiments/e7_interface_ablation.py"
      provides: "Seed provenance on E7's benchmark records"
      contains: "seed="
  key_links:
    - from: "write_direct_call_benchmark(seed=...)"
      to: "check_rerun_gates._provenance_gaps"
      via: "solver_config.seed"
      pattern: "gate3_provenance"

executor_rules:
  - "Do NOT run the driver at any scale. Do NOT run `pytest tests/` — targeted files only."
  - "Do NOT modify `experiments/_io.py`. The writer's `seed` parameter and its duplicate-key guard are correct as they stand; this plan is call sites, one comment, and tests."
  - "Do NOT modify `experiments/check_rerun_gates.py`. Narrowing the gate is the WRONG fix — the seed is provenance these records should carry, which is why E4, E5 and E6 already carry it."
  - "Do NOT delete `SEEDLESS_LEGACY_RECORDS` or any test that reads it. Its members are archived artifacts under `experiments/pre_rerun_baseline/results/` that will never be rewritten; the carve-out stays, its stated REASON gets corrected."
  - "Do NOT touch `experiments/suite_expectations.json` — 26-12 is editing it in this same wave."
  - "The writer RAISES if the `solver_config` dict already contains a `seed` key (`_io.py:866-869`). Pass `seed=` as a keyword; never inject the key into the dict."
---

<objective>
Close a gate that cannot pass. `gate3_provenance` demands a seed on every direct-call benchmark
record; E1's and E7's call sites have never passed one, so six gate lines FAIL in every run — the
26-10 smoke pass included, on records it had just written.

Purpose: Phase 29's RUN-03 is "gates pass". Six unconditional FAILs make that unsatisfiable, and turn
the frozen run's gate output from a verdict into a list a human must hand-triage into "expected" and
"real" — which is the exact condition F-001 came out of. The fix is five keyword arguments.
Output: `seed=` at five call sites, a corrected carve-out comment, and tests that read the written
JSON rather than the call.
</objective>

<context>
@CLAUDE.md
@.planning/phases/26-full-suite-driver-handoff-readiness/26-CONTEXT.md
@.planning/phases/26-full-suite-driver-handoff-readiness/26-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pass the seed at E1's and E7's benchmark call sites</name>
  <read_first>
    - `experiments/_io.py:780-890` — `write_direct_call_benchmark`'s signature, the `seed` keyword, the
      shallow-copy fold into `solver_config`, and the `ValueError` guard at :866 that fires when the
      passed `solver_config` ALREADY holds a seed. Read this first: it determines the fix is a keyword
      argument, not a dict edit.
    - `experiments/e4_benchmark_grid.py:1089` — the reference call site that already does this
      correctly. Match its shape.
    - `experiments/e1_refractive_comparison.py:827, :952, :1296` and
      `experiments/e7_interface_ablation.py:688, :857` — the five call sites. Note :952 writes into a
      `tmp_path`, so establish what that path's records are used for before deciding its seed source.
  </read_first>
  <files>experiments/e1_refractive_comparison.py, experiments/e7_interface_ablation.py</files>
  <behavior>
    - Every `write_direct_call_benchmark` call in E1 and E7 passes `seed=` with the seed that generated
      THAT record's scenario — the value the record's own CSV row reports.
    - Under a band run each seed's record carries ITS seed, not the first one. Determine the in-scope
      local at each site; do not assume `args.seed` is correct on a band path, where it is not.
    - No call site builds `{"seed": ...}` into the `solver_config` dict — that trips the writer's guard.
    - Nothing else in either record changes: same schema_version, same keys, same values.
  </behavior>
  <action>
    Write the failing tests first, in `tests/unit/test_experiments_provenance.py`. For each experiment,
    run its smallest available path (E1's single-seed `--smoke`, E7's single-arm path) into a tmp dir,
    read the written JSON off disk, and assert `record["solver_config"]["seed"]` equals the seed
    requested. Assert on the FILE, never on a mock of the writer — the defect being closed is that a
    real file lacked a real field, and a mock-level assertion would have passed throughout.

    Then add `seed=` at the five call sites.

    Add one test per experiment that calls `check_rerun_gates._provenance_gaps` on the written record
    and asserts it returns `[]`. That is what ties the fix to the gate; presence-of-field tests alone
    would still pass if the gate looked elsewhere.
  </action>
  <verify>
    <automated>export PYTHONPATH="$(pwd)/src" &amp;&amp; $HOME/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_provenance.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The new tests fail before the change and pass after; show the RED output in the SUMMARY.
    - All five call sites pass `seed=`; quote each call-site line in the SUMMARY.
    - `_provenance_gaps` returns `[]` for a freshly written E1 record and a freshly written E7 record.
    - No tested path raises the writer's duplicate-seed `ValueError`.
    - `git diff experiments/_io.py experiments/check_rerun_gates.py experiments/suite_expectations.json`
      is empty.
  </acceptance_criteria>
  <done>Records written by today's E1 and E7 code carry a seed, and the gate's own gap-finder confirms it.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the carve-out's stated reason and cover all six artifacts</name>
  <read_first>
    - `tests/unit/test_experiments_provenance.py:92-110` — the `SEEDLESS_LEGACY_RECORDS` comment
      claiming plan 19.2-02 "added seed= to the direct-call path" and that the exemption "is removed
      the moment any of these six is regenerated".
    - `:45` — `RESULTS_DIR` now points at `experiments/pre_rerun_baseline/results`, the archived tree.
    - `:464` `test_seedless_carve_out_is_exact` and `:480`
      `test_seedless_carve_out_has_exactly_six_members_and_excludes_e2`.
  </read_first>
  <files>tests/unit/test_experiments_provenance.py</files>
  <behavior>
    - The carve-out comment says what is actually true: 19.2-02 added the writer's `seed` PARAMETER but
      not the E1/E7 call sites, so those six records stayed seedless through every regeneration until
      plan 26-13; the six named files are archived artifacts under
      `experiments/pre_rerun_baseline/results/` and are exempt because they will never be rewritten,
      not because regeneration would fix them.
    - The carve-out's membership and its two tests are unchanged in behavior — they still pass.
    - A test names all six current-code artifacts explicitly and asserts each clears
      `gate3_provenance` once written. Naming them individually is deliberate: a loop over "whatever
      files exist" passes vacuously when a path stops being written.
  </behavior>
  <action>
    Rewrite the comment block. Then add the six-artifact assertion.

    Where generating all six from scratch is too slow for a unit test, construct the missing records
    through the same writer with representative payloads rather than dropping coverage — and state in
    the test's docstring which of the six come from a real run and which are constructed, so a reader
    can see the strength of each.

    Do NOT build fixtures by copying records out of `experiments/pre_rerun_baseline/`: those are the
    artifacts that LACK the field, and a fixture made from them would encode the defect.

    Flag in the SUMMARY, without acting on it: because `RESULTS_DIR` points at the archived baseline,
    this whole module now validates an archive rather than the tree the frozen run will produce.
    Repointing it is Phase 29's business, not this plan's.
  </action>
  <verify>
    <automated>export PYTHONPATH="$(pwd)/src" &amp;&amp; $HOME/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_experiments_provenance.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All six artifact names appear literally in the new test; grep and quote.
    - `test_seedless_carve_out_is_exact` and
      `test_seedless_carve_out_has_exactly_six_members_and_excludes_e2` still pass, unmodified in
      behavior.
    - The corrected comment states that the call sites, not the writer, were the gap.
    - The targeted pytest run is green and completes in under 300 s.
    - The new test fails if `seed=` is removed from any one call site — verify by reverting one site
      temporarily and recording which test caught it.
  </acceptance_criteria>
  <done>The carve-out documents what actually happened, and the six records that fail the gate today are covered by a test naming each one.</done>
</task>

</tasks>

<verification>
- Targeted pytest green on `tests/unit/test_experiments_provenance.py`.
- `_provenance_gaps` returns `[]` for freshly written E1 and E7 records.
- `experiments/_io.py`, `experiments/check_rerun_gates.py` and `experiments/suite_expectations.json`
  are untouched.
- The SUMMARY flags the `RESULTS_DIR`-points-at-the-archive observation for Phase 29.
</verification>
