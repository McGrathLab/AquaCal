---
phase: 26-full-suite-driver-handoff-readiness
plan: 09
type: execute
wave: 6
depends_on: ["26-03", "26-07", "26-08"]
files_modified:
  - experiments/README.md
  - experiments/EXPECTATIONS.md
  - experiments/render_expectation_sheet.py
  - tests/unit/test_expectations.py
  - .planning/MANUSCRIPT-FINDINGS.md
  - experiments/rerun_19_4.sh
  - experiments/rerun_19_5.sh
  - experiments/pre_rerun_baseline/
autonomous: true
requirements: [DRIVER-01, DRIVER-03, DRIVER-04]

must_haves:
  truths:
    - "`experiments/README.md` §2 has one row per INVOCATION, so an operator following it produces the seed bands (D-36)."
    - "A written hand-verification expectation sheet exists BEFORE the run, and it is rendered from the expectation manifest with a test asserting it is up to date (D-05, D-08)."
    - "The `--check` contract across the deliberate re-base is documented, including that E2's `--check` survives on only one of its three artifacts (D-13, SP-5)."
    - "The `cpr_grouping.tex`-is-never-`\\input` finding is recorded as an MF-NN entry, and the manuscript is NOT edited (D-39)."
    - "`rerun_19_4.sh` and `rerun_19_5.sh` are archived AFTER their stage functions were lifted, leaving exactly one driver on disk (ruling A3)."
  artifacts:
    - path: "experiments/EXPECTATIONS.md"
      provides: "The hand-verification sheet: what each artifact should look like after the frozen run, rendered from the manifest plus hand-written --check contract prose"
      contains: "BEGIN GENERATED"
    - path: "experiments/render_expectation_sheet.py"
      provides: "The renderer that regenerates the sheet's generated region from suite_expectations.json"
      exports: ["render_sheet", "main"]
    - path: ".planning/MANUSCRIPT-FINDINGS.md"
      provides: "The MF-NN entry recording that cpr_grouping.tex is generated but never \\input"
  key_links:
    - from: "experiments/EXPECTATIONS.md"
      to: "experiments/suite_expectations.json"
      via: "render_expectation_sheet.py, freshness-tested"
      pattern: "suite_expectations"
    - from: "experiments/README.md"
      to: "experiments/run_experiment_suite.sh"
      via: "§2's invocation table naming the one driver"
      pattern: "run_experiment_suite"
---

<objective>
Write the documentation the frozen package ships with, record the manuscript finding, and archive the
two superseded drivers now that their stage functions have been lifted. Three drivers on disk, one of
which is the real one, is the footgun D-23 exists to close, one level up.

Purpose: `experiments/README.md` §2 as written lists `python -m experiments.e1_refractive_comparison`
with no `--seeds` row anywhere, so an operator following it produces no seed bands at all. And
DRIVER-03 requires the written expectation sheet to exist BEFORE the run, because the run is what it
will be checked against.
Output: a rewritten README §2/§7, `experiments/EXPECTATIONS.md` and its renderer, an MF-NN entry, and
the two old drivers moved into the archive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/26-full-suite-driver-handoff-readiness/26-CONTEXT.md
@.planning/phases/26-full-suite-driver-handoff-readiness/26-RESEARCH.md
</context>

<interfaces>
<!-- Extracted from the codebase and research. Use directly. -->

`experiments/README.md` is 475 lines. §2 spans **`:50` to `:297`** — a table PLUS six subsections:
`:50-64` preamble ("One row per artifact committed under `experiments/results/`") ·
`:66-84` accuracy-tree vs timing-tree (`results_linux32gb/`) ·
`:86-113` the 30-row artifact table (`| Paper artifact | Experiment | Command | Output file(s) |
Figure generator | Runtime |`) · `:115-135` the "three `generalization_sweep.csv` rows did not
converge" callout · `:137-168` E4 direct-call rationale + the `--check`-always-red explanation ·
`:170-204` `### DATA-01b` · `:205-239` pre- vs post-D-27 artifacts ·
`:240-256` `### cpr_grouping.csv is the sole origin of tab:cpr` · `:257-265` tilt-enabled ·
`:266-280` the seed carve-out · `:281-297` the four scripts with no row in the table.
§7 at `:394-468` is a shell block of reproduction commands **with the identical coverage gap**.

**Two stale statements the rewrite must FIX (both are current defects, not style):**
1. `:81-83` — "E4's aggregator reads the real-rig record from a hardcoded `E2_BENCHMARK_PATH`
   (`e4_benchmark_grid.py:226`)". FIX-05 (Phase 23) fixed this: the resolver is
   `resolve_e2_benchmark_path` at `:261` and the constant moved to `:256`. Both the line number and
   the claim are wrong now.
2. `:240` — "`cpr_grouping.csv` is the sole origin of `tab:cpr`" directly conflicts with D-39.

D-39's finding, verified 2026-08-18: `--include-per-camera-latex` renders `shared_interface=False`
rows into `cpr_grouping.tex`; `tab:cpr` lives at `supplement.tex:449` with six rows, **all
shared-interface**, and the generated fragment is **not `\input` anywhere** — the table is
hand-transcribed. Same class as "a hand-transcribed parameter count off by ten", and it interacts
with Phase 27's pre-freeze gate that every §3-facing number has a generating emitter.

The `--check` contract to document (D-13 — Phase 26 DOCUMENTS this; it does not reinvent it):
- Mechanism: `compare_experiment_csv(..., exclude_columns=())` at `experiments/_io.py:332-338`,
  documented `:365-382`; that docstring **already names Phase 26 / DRIVER-03** and warns the two
  must not diverge.
- The only in-repo list: `CHECK_EXCLUDED_COLUMNS = ("exit_code", "status_reason")` at
  `experiments/e4_benchmark_grid.py:215`. E4's `--check` is structurally ALWAYS-RED on those two
  columns while all 33 metric columns reproduce to 1e-6.
- Key property: `exclude_columns` affects the CELL-level comparison ONLY; the full-header comparison
  is never affected (`_io.py:369-372`), so a genuine schema change still fails loudly.
- Schemas that MOVED in Phases 23-25 and therefore pre-declare a header mismatch: `E5_COLUMNS`
  17→23, `ABLATION_COLUMNS` 17→23, `E6_COLUMNS` 31→33, `SPATIAL_COLUMNS` 6→12, and
  `e7_focal_standoff.csv`'s verdict strings.
- E2 (SP-5): of its three compared artifacts only `camera_parameters.csv` has a committed baseline;
  `reprojection_residuals.csv` and `reconstruction_errors.csv` are gitignored by DATA-01b policy
  (`.gitignore:238-239`) and ship only in the Zenodo archive. Plan 26-06 made those report N/A. The
  better-anchored E2 control is `check_e2_band`'s numeric comparison of `real_rig_metrics.json` at
  `_E2_METRICS_RTOL = 1e-6` (`check_rerun_gates.py:1340`).
- The verdict itself is settled and inherited (D-13): hand-verify this run against the written sheet,
  then re-baseline and restore automated checking in Phase 29.

`.planning/MANUSCRIPT-FINDINGS.md` runs to MF-22 (`## MF-22 — E1's accuracy ratio is a function of
detection noise...`). The new entry is **MF-23**.

Archive layout from plan 26-01: `experiments/pre_rerun_baseline/`, with loose driver state and logs
under `experiments/pre_rerun_baseline/driver_state/`.
</interfaces>

<executor_rules>
- Worktree executors MUST `export PYTHONPATH="$(pwd)/src"` before pytest.
- Interpreter: `$HOME/anaconda3/envs/AquaCal/python.exe`.
- **Do NOT run `python -m pytest tests/`** — the orchestrator runs the unfiltered suite post-merge.
- **Do NOT run the driver, `--smoke` or otherwise.** Plan 26-10 is the orchestrator's smoke pass.
- **`Spinoffs/papers/aquacal/` is READ-ONLY from this repo.** Record the finding; do not edit
  `main.tex`, `supplement.tex`, or any manuscript file. That is the manuscript session's work.
- **Do NOT purge anything.** The archive stays reachable for the whole run — E2's control and E3's
  tier diff both compare against it. The purge is Phase 30 / POST-03 (D-32).
- **Do NOT perform the dangling-reference audit** (`linux32gb_scope.json`, `check_rerun_gates.py`,
  test fixtures). That is Phase 30's, not this phase's — except for the README, which D-36 puts in
  scope here.
- Archive `rerun_19_4.sh` / `rerun_19_5.sh` only AFTER confirming the lift landed (plan 26-07's
  SUMMARY plus a grep of the new driver). Never before.
</executor_rules>

<decisions_implemented>
D-36 (README §2 rewritten BY HAND, one row per invocation; deliberately not rendered from the stage
list — the author's call, keep the prose natural), D-05/D-08 (the sheet is rendered from the single
manifest, with a freshness test), D-13 (document the `exclude_columns` contract; do not reinvent it),
D-39 (record MF-23; do not edit the manuscript), D-32 (nothing is purged), ruling A3 (archive both
old drivers in the DRIVER-04 close-out, after the lift).

⚠ **Conflict noted and resolved in favour of the sheet+test.** CONTEXT amendment § D's D-44 CUT the
renderer and the freshness test, arguing the sheet is authored once and frozen days later. The Phase
26 planning directive and `26-VALIDATION.md` both require them (`-k sheet`). They are cheap, they
make D-05's "one list" claim literally true, and a stale sheet is what a hand-verifier would be
checking against. If the author prefers D-44, delete `render_expectation_sheet.py` and the `-k sheet`
test and hand-maintain `EXPECTATIONS.md` — nothing else in the phase depends on the renderer.
</decisions_implemented>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite experiments/README.md §2 (and §7) to one row per invocation</name>
  <read_first>
    - `experiments/README.md` lines 50-297 and 394-468, in full
    - `experiments/run_experiment_suite.sh` — the authoritative invocation list (plan 26-07)
    - `experiments/e4_benchmark_grid.py:250-300` (`resolve_e2_benchmark_path` and the moved
      constant) — the ground truth for stale statement 1
    - `.planning/phases/26-full-suite-driver-handoff-readiness/26-CONTEXT.md` D-36, D-39
  </read_first>
  <files>experiments/README.md</files>
  <action>
    Rewrite §2's table to one row per INVOCATION rather than one row per committed artifact. Every
    row in `experiments/run_experiment_suite.sh`'s stage list gets a row: the four `--seeds` band
    stages, E2's four invocations, the three orphan scripts, and every single-seed run. Keep the
    existing columns' spirit (paper artifact, experiment, command, output files, figure generator,
    runtime) and add the wall-clock estimate and the serial/concurrent attribute from
    `experiments/suite_expectations.json`'s stage entries.

    Write it BY HAND (D-36 — deliberately not rendered from the stage list; the author's call, so the
    prose stays natural). Cross-check every command string against the driver so no row is
    aspirational.

    Fix the two stale statements:
    1. `:81-83` — replace the hardcoded-`E2_BENCHMARK_PATH` claim with the current mechanism:
       `resolve_e2_benchmark_path` at `e4_benchmark_grid.py:261`, the constant at `:256`, fixed by
       FIX-05 in Phase 23. Note the operational consequence, which is now an ordering constraint:
       branch 3 returns `None` and the real-rig row is silently dropped, so `e4` must run after E2's
       production run or `benchmark_grid.csv` comes back with 9 rows instead of 10.
    2. `:240-256` — rewrite `### cpr_grouping.csv is the sole origin of tab:cpr` to state D-39's
       verified finding: the generated `cpr_grouping.tex` fragment is **never `\input`**; `tab:cpr`
       at `supplement.tex:449` is hand-transcribed, six rows, all shared-interface; and
       `--include-per-camera-latex` therefore stays OFF (D-11). Point at MF-23 (Task 3) for the
       derivation. The README and the MF entry must agree.

    Add a short new subsection naming `experiments/run_experiment_suite.sh` as THE entry point, with
    the detached launch line, the `--skip-e2` override, the `SUITE_WORKERS` / `SUITE_SERIAL`
    variables, and a pointer to `experiments/EXPECTATIONS.md` for what the outputs should look like.

    Sweep §7 (`:394-468`) in the same pass. It is the identical coverage gap in a shell block — no
    `--seeds` row anywhere. D-36 names §2; leaving §7 stale would reintroduce the defect one section
    down.

    Also update the §2 preamble's framing: it says "one row per artifact committed under
    `experiments/results/`", and after plan 26-01 that directory is empty until the run — say where
    the committed baselines now live (`experiments/pre_rerun_baseline/`) and that they are purged in
    Phase 30, citing the `pre-rerun-baseline` tag.
  </action>
  <verify>
    <automated>grep -c 'run_experiment_suite.sh' experiments/README.md && grep -c -- '--seeds' experiments/README.md && ! grep -q 'e4_benchmark_grid.py:226' experiments/README.md && ! grep -q 'sole origin of' experiments/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--seeds' experiments/README.md` returns at least 4 — the four band stages each
      have a row.
    - `grep -c 'e7_focal_standoff_analysis\|reconstruction_bootstrap\|fd_jacobian_accuracy' experiments/README.md`
      returns at least 3.
    - `grep -c 'config_e2_classification\|config_e2_timing\|config_e2_memory' experiments/README.md`
      returns at least 3 — E2's four invocations are documented as distinct.
    - `grep -c 'e4_benchmark_grid.py:226' experiments/README.md` returns 0 and
      `grep -c 'resolve_e2_benchmark_path' experiments/README.md` returns at least 1.
    - `grep -c 'sole origin of' experiments/README.md` returns 0 and `grep -c 'MF-23' experiments/README.md`
      returns at least 1.
    - `grep -c 'pre_rerun_baseline' experiments/README.md` returns at least 1.
    - Every command string in §2 and §7 appears in `experiments/run_experiment_suite.sh`; state in
      the SUMMARY how this was checked and list any command in the README that the driver does not
      run (there should be none).
  </acceptance_criteria>
  <done>§2 and §7 have one row per invocation, name the one driver, and both stale statements are corrected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render and commit experiments/EXPECTATIONS.md, plus the --check contract prose</name>
  <read_first>
    - `experiments/suite_expectations.json` and `experiments/_expectations.py` (plan 26-03)
    - `tests/unit/test_expectations.py` (plan 26-03) — add to it; the `sheet` keyword was left free
    - `experiments/_io.py:325-400` (`compare_experiment_csv` and the `exclude_columns` docstring)
      and `experiments/e4_benchmark_grid.py:210-220` (`CHECK_EXCLUDED_COLUMNS`)
    - `src/aquacal/calibration/_observability.py` — the 32 `DISCARD_KEYS` and the rule that the two
      independent marginals each sum to the total and are **never additive together**
    - `.planning/phases/26-full-suite-driver-handoff-readiness/26-CONTEXT.md` D-05, D-08, D-13, D-44
  </read_first>
  <files>experiments/render_expectation_sheet.py, experiments/EXPECTATIONS.md, tests/unit/test_expectations.py</files>
  <behavior>
    - `render_sheet()` returns the generated region's text, derived entirely from
      `experiments/suite_expectations.json`.
    - `experiments/EXPECTATIONS.md` contains exactly one region delimited by
      `<!-- BEGIN GENERATED -->` / `<!-- END GENERATED -->`; everything outside it is hand-written
      prose the renderer never touches.
    - Running `python -m experiments.render_expectation_sheet --write` is idempotent: a second run
      leaves the file byte-identical.
    - The freshness test (selectable by `-k sheet`) fails when the manifest changes and the sheet is
      not regenerated, and its failure message names the exact command to fix it.
    - The generated region carries, per artifact: name, owning stage, directory, expected rows under
      `full`, whether it is conditional, whether it is immutable, and which columns carry only a
      SHAPE expectation.
  </behavior>
  <action>
    Create `experiments/render_expectation_sheet.py` with `render_sheet()` and a `main(argv=None)`
    exposing `--check` (exit non-zero when stale) and `--write`. Keep it small; it is a formatter,
    not a second source of truth.

    Create `experiments/EXPECTATIONS.md`. Outside the generated markers, hand-write the parts a
    machine cannot derive:

    1. **What this sheet is.** DRIVER-03 requires written expectations to replace bit-identity
      reproduction wherever a schema moved, and the sheet must exist BEFORE the run. Hand-verify the
      frozen run against it (D-13); Phase 29 re-baselines and restores automated checking.
    2. **The `--check` contract table**, one row per script, from the `<interfaces>` block above:
      which have `_run_check`, how many `compare_experiment_csv` calls, which schema MOVED in Phases
      23–25 and therefore pre-declares a header mismatch, and the two structurally-always-red E4
      columns with `CHECK_EXCLUDED_COLUMNS` as the named-not-heuristic mechanism. State the key
      property: `exclude_columns` affects the CELL-level comparison only, never the full-header
      comparison, so a genuine schema change still fails loudly.
    3. **The E2 honesty note (SP-5).** "`--check` survives on E3 and E2 only" is optimistic for E2:
      it survives on ONE of three artifacts, and only when the local tree is warm. Name
      `check_e2_band`'s `real_rig_metrics.json` comparison at `_E2_METRICS_RTOL = 1e-6` as the
      better-anchored control.
    4. **Existence and row count are not correctness.** A gauge-corrected column populated with
      uncorrected values passes every completeness check. That is this sheet's job, not the gate's —
      which is why the generated region marks shape-only columns explicitly.
    5. **Two reading rules for the hand-verifier.** Never quote `optimality` to more than one
      significant figure (`experiments/README.md:133-135`: it varies ~2x between runs of identical
      code). And the degeneracy breakdown's two marginals — by cause and by fate — each sum to the
      total and are **never additive together**; state the rule and the 32 `DISCARD_KEYS` source.
    6. **What this sheet cannot certify**, so nobody over-reads it: that the `full`-profile row counts
      are CORRECT rather than merely self-consistent (only Phase 28 produces them); that E2's ~1e-8
      control reproduces (needs the 4.35 GB frameset and 48–87 min); that the concurrency model does
      not OOM at 4–5 wide.

    Add the freshness test to `tests/unit/test_expectations.py` in a `TestExpectationSheet` class,
    named so `-k sheet` selects it.
  </action>
  <verify>
    <automated>$HOME/anaconda3/envs/AquaCal/python.exe -m experiments.render_expectation_sheet --check && $HOME/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_expectations.py -k sheet -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m experiments.render_expectation_sheet --check` exits 0 against the committed sheet.
    - `pytest tests/unit/test_expectations.py -k sheet -q` selects at least 2 tests, all pass.
    - The freshness test genuinely fails when broken: edit one row count in a scratch copy of the
      manifest, confirm `--check` exits non-zero and the test fails naming the regeneration command,
      then discard. Record the message in the SUMMARY.
    - `python -m experiments.render_expectation_sheet --write` twice leaves
      `git diff --stat experiments/EXPECTATIONS.md` empty.
    - `experiments/EXPECTATIONS.md` contains all of: `CHECK_EXCLUDED_COLUMNS`, `DATA-01b`,
      `_E2_METRICS_RTOL`, `optimality`, `DISCARD_KEYS`, and a "what this sheet cannot certify"
      section.
    - `grep -c 'BEGIN GENERATED' experiments/EXPECTATIONS.md` returns exactly 1.
  </acceptance_criteria>
  <done>The expectation sheet is committed, its generated region provably matches the manifest, and its hand-written prose carries the `--check` contract, the E2 qualification, and the two reading rules.</done>
</task>

<task type="auto">
  <name>Task 3: Record MF-23 and archive the two superseded drivers</name>
  <read_first>
    - `.planning/MANUSCRIPT-FINDINGS.md` — the last two entries (MF-21, MF-22) for the house entry
      format: heading, `**Extends:**` / `**Constrains:**` lines, derivation, and location refs
    - `.planning/phases/26-full-suite-driver-handoff-readiness/26-07-SUMMARY.md` — confirm the lift
      landed before archiving anything
    - `experiments/run_experiment_suite.sh` — grep it for each stage function the two old drivers
      contributed
    - `.planning/phases/26-full-suite-driver-handoff-readiness/26-CONTEXT.md` D-39, D-11, D-32
  </read_first>
  <files>.planning/MANUSCRIPT-FINDINGS.md, experiments/rerun_19_4.sh, experiments/rerun_19_5.sh, experiments/pre_rerun_baseline/</files>
  <action>
    Append **MF-23** to `.planning/MANUSCRIPT-FINDINGS.md`, matching the format of MF-21/MF-22.
    Content: `cpr_grouping.tex` is a GENERATED fragment that is never `\input`; `tab:cpr` at
    `supplement.tex:449` is hand-transcribed, six rows, all shared-interface; therefore
    `--include-per-camera-latex` stays OFF (D-11) and the generated fragment is currently decorative.
    Class it with "a hand-transcribed parameter count off by ten" from DRIVER-03's own "Do not" list,
    and record the interaction: Phase 27's pre-freeze gate requires every §3-facing number to have a
    generating emitter, and a hand-transcribed table has one that nothing consumes.
    **Record the derivation only. Do NOT edit the manuscript** — the tree is read-only from this repo
    and the edit is the manuscript session's call.

    Then, and only after confirming the lift: `git mv experiments/rerun_19_4.sh` and
    `experiments/rerun_19_5.sh` into `experiments/pre_rerun_baseline/driver_state/`, completing
    DRIVER-04 and leaving exactly ONE driver on disk. CONTEXT's D-25 names only `rerun_19_3.sh`;
    ruling A3 settles the rest, and the sequencing is mandatory — their stage functions are the
    source of the lift.

    Before moving, verify the lift by grepping the new driver for each function the two contributed:
    `run_stage_e6_band`, `run_stage_e4_repeat`, `run_stage_e2_band`, `run_stage_e5_band`,
    `run_stage_prelaunch_probe` (from 19.5) and `run_stage_e7`, `run_stage_e7_band`, `run_stage_e1`,
    `run_stage_e1_band` (from 19.4). If ANY is missing, STOP and report rather than archiving.

    Check for live references to the moved scripts and update only the ones that would break:
    `grep -rn 'rerun_19_[45]\.sh' --include='*.sh' --include='*.py' --include='*.md' .` — historical
    references inside `.planning/phases/19*/` SUMMARYs are history and must NOT be rewritten. The
    dangling-reference audit proper (`linux32gb_scope.json`, test fixtures) is Phase 30's.

    Nothing is deleted (D-32).
  </action>
  <verify>
    <automated>grep -q '^## MF-23' .planning/MANUSCRIPT-FINDINGS.md && test -f experiments/pre_rerun_baseline/driver_state/rerun_19_5.sh && test -f experiments/pre_rerun_baseline/driver_state/rerun_19_4.sh && test ! -e experiments/rerun_19_4.sh && test ! -e experiments/rerun_19_5.sh && test -f experiments/run_experiment_suite.sh && test $(ls experiments/*.sh | grep -c 'rerun_19') -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^## MF-23' .planning/MANUSCRIPT-FINDINGS.md` resolves, and the entry names
      `supplement.tex:449`, `cpr_grouping.tex`, `--include-per-camera-latex` and D-11.
    - `git status --porcelain Spinoffs/` is empty — the manuscript tree was not touched.
    - `ls experiments/*.sh` lists `run_experiment_suite.sh` plus only the non-suite helpers
      (`seed_sweep_19_3.sh`, `e6_legal_seed_probe.sh`, `prelaunch_gate.sh`); no `rerun_19_*.sh`.
    - `git show --stat --find-renames HEAD` shows R-status entries for both archived drivers and zero
      D-status entries.
    - All nine lifted stage functions are present in `experiments/run_experiment_suite.sh`; list the
      grep results in the SUMMARY.
    - `grep -rn 'rerun_19_[45]\.sh' --include='*.sh' --include='*.py' .` returns no live reference
      that would break at run time; any remaining hits are in `.planning/` history.
  </acceptance_criteria>
  <done>MF-23 is recorded without touching the manuscript, and exactly one suite driver remains on disk with both predecessors archived after the lift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation to operator action | A stale README row is what produced a run with no seed bands |
| generated fragment to published table | A generated `.tex` that nothing `\input`s gives false assurance that a number is emitter-backed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-31 | Spoofing | a documented command the driver does not run | mitigate | Every §2/§7 command is cross-checked against `run_experiment_suite.sh`; the check's result is recorded in the SUMMARY |
| T-26-32 | Repudiation | a hand-verification sheet that drifted from the manifest | mitigate | The generated region plus the `-k sheet` freshness test; demonstrated to fail when stale |
| T-26-33 | Tampering | editing the read-only manuscript tree | mitigate | Record-only (D-39); acceptance asserts `git status --porcelain Spinoffs/` is empty |
| T-26-34 | Repudiation / data loss | archiving the old drivers before the lift | mitigate | Nine named stage functions are grepped in the new driver first; STOP-and-report if any is missing |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | This phase installs nothing; `pyproject.toml` is not in `files_modified` |
</threat_model>

<verification>
- `python -m experiments.render_expectation_sheet --check` exits 0.
- `pytest tests/unit/test_expectations.py -q` green.
- `ls experiments/*.sh` shows exactly one suite driver.
- `git status --porcelain Spinoffs/` empty.
</verification>

<success_criteria>
The frozen package ships with a README that produces the whole suite when followed, a written
expectation sheet provably in step with the manifest, a recorded manuscript finding, and exactly one
driver on disk.
</success_criteria>

<output>
Create `.planning/phases/26-full-suite-driver-handoff-readiness/26-09-SUMMARY.md` when done.
</output>
