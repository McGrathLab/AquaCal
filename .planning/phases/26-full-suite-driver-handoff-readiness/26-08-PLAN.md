---
phase: 26-full-suite-driver-handoff-readiness
plan: 08
type: execute
wave: 4
depends_on: ["26-02", "26-03", "26-06", "26-07"]
files_modified:
  - experiments/run_experiment_suite.sh
  - experiments/suite_expectations.json
  - tests/unit/test_run_experiment_suite_dryrun.py
autonomous: true
requirements: [DRIVER-01, DRIVER-02]

must_haves:
  truths:
    - "Pre-flight runs before stage 1, emits the run manifest, and ABORTS on failure — and every refusal prints the exact override flag that bypasses it (D-03, D-19, D-50)."
    - "A completeness or gate FAILURE after stage 1 never aborts the queue; it sets a sticky flag that makes the driver's final exit non-zero with a loud summary (D-01)."
    - "Completeness is checked at all three points: pre-flight, after each stage, and an end-of-run roll-up over the whole tree (D-02)."
    - "A missing E2 frameset hard-fails pre-flight unless declared via `--skip-e2`, and pre-flight asserts frameset IDENTITY, not mere presence (D-14, D-17)."
    - "No dirty-tree refusal and no disk-headroom estimator exist; free space is logged against a crude absolute floor (D-46, D-47)."
    - "Timing-sensitive stages run serial and alone; accuracy stages run 4-5 wide, never overlapping `e6_repeat1` with `e6_band` and never two 200-frame-class stages at once (D-52)."
  artifacts:
    - path: "experiments/run_experiment_suite.sh"
      provides: "Pre-flight, the sticky-failure flag, the end-of-run roll-up, and the concurrency pool"
      contains: "SUITE_FAILED"
    - path: "tests/unit/test_run_experiment_suite_dryrun.py"
      provides: "The driver's first automated test scaffold: sequencing, resume, sticky exit, dry-run state separation, pre-flight abort"
  key_links:
    - from: "experiments/run_experiment_suite.sh"
      to: "experiments/_run_manifest.py"
      via: "pre-flight invocation of the manifest emitter"
      pattern: "_run_manifest"
    - from: "experiments/run_experiment_suite.sh"
      to: "experiments/check_rerun_gates.py --profile"
      via: "per-stage and roll-up completeness gate calls"
      pattern: "--profile"
---

<objective>
Give the driver its safety rails and its concurrency model, and give it the first automated test it
has ever had. There is no test of any driver shell script anywhere in this repo; the
`RERUN_19_3_DRY_RUN` mechanism exists but has only ever been driven by hand.

Purpose: D-01's sticky exit code is what kills the failure class this milestone exists to retire —
a run that exits 0 and looks green while an artifact was never produced. D-52's selective
concurrency takes the serial estimate from ~22–26 h to ~15–16 h, and it is a smaller change than
several items already cut from this phase.
Output: pre-flight, the sticky flag, the roll-up, the concurrency pool, and
`tests/unit/test_run_experiment_suite_dryrun.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/26-full-suite-driver-handoff-readiness/26-CONTEXT.md
@.planning/phases/26-full-suite-driver-handoff-readiness/26-RESEARCH.md
@.planning/phases/26-full-suite-driver-handoff-readiness/26-VALIDATION.md
</context>

<interfaces>
<!-- Extracted from the codebase. Use directly. -->

Already in place when this plan starts:
- `experiments/run_experiment_suite.sh` (plan 26-07) — lifted machinery, the full stage list, a
  sha-derived `STATE_FILE`, `_dry_run_active` / `_dry_run_stub` on every stage, `run_gate_check`
  which ALWAYS returns 0 so a gate FAIL never aborts the queue.
- `experiments/_run_manifest.py` (plan 26-02) — CLI `--out PATH [--force]`, writes
  `run_manifest.json`, exits non-zero on failure.
- `experiments/check_rerun_gates.py` (plan 26-03) — optional `--stage NAME` and
  `--profile {smoke,full}`; without them it behaves exactly as before.
- `experiments/_expectations.py` (plan 26-03) — `load_expectations()`, `check_completeness(out_dir,
  *, profile, stage=None)`.
- `experiments/suite_expectations.json` (plan 26-03) — every stage carries `depends_on`,
  `concurrency` (`serial_alone` / `concurrent`), `frame_class` (`none`/`30`/`100`/`200`) and
  `est_hours`.

The hard-abort pre-flight pattern already proven in production: `rerun_19_5.sh:455-463` — after
`state_complete`, if the stage name is the probe and its exit code is non-zero, log a FATAL line
naming what to fix and `exit "${exit_code}"`.

D-52's measured basis (`.planning/probes/2026-08-18-solver-concurrency/summary.json`): a solve holds
a median 0.99 cores of 20, mean 1.20, p95 2.01, peak 2.56, stable through Stage 3; E1 peak RSS
0.61 GiB. Peak RSS tracks frame count — 30 frames <1 GiB (E5), 100 frames 2.7–3.5 GiB (E6's band,
all 102 rows at `n_frames=100`), 200 frames 9.3–11.3 GiB (E2, E4). The probe's own caveat is
load-bearing: E1 is the cheapest and smallest solve in the suite and its peak RSS does not transfer.
**Do not widen the pool on the probe's `recommended_workers: 16`** — 4–5 is the author's decision
and is well inside the bound.

E2's frameset signature to assert (D-17): 262 usable frames → 52 validation → 7,762 comparisons.
The retired numbers "60 → 12 → 1,817" are the wrong archive's, and survived in a code comment on
this very branch (FIX-06) — a presence-only check passes cleanly on the wrong archive.
</interfaces>

<executor_rules>
- Worktree executors MUST `export PYTHONPATH="$(pwd)/src"` before pytest.
- Interpreter: `$HOME/anaconda3/envs/AquaCal/python.exe`.
- **Do NOT run `python -m pytest tests/`** — the orchestrator runs the unfiltered suite post-merge.
- **Do NOT run the driver at real scale and do NOT run `--smoke`.** The full `--smoke` pass is the
  ORCHESTRATOR's run (D-34, plan 26-10). You may run the driver ONLY under the dry-run seam, where
  every stage is substituted by a stub and the whole queue completes in about a second. Any dry-run
  invocation from a test must have an explicit pytest timeout.
- **Do NOT add a dirty-tree pre-flight refusal.** D-47 cut it: `experiments/results/` is tracked, so
  the run dirties its own working tree and the refusal would fire on RESUME, refusing every restart
  after the first crash. Gate 3 records dirtiness post-hoc instead (D-21), which can never kill a run.
- **Do NOT add a disk-headroom ESTIMATOR.** D-46 cut it: a wrong estimate is precisely the
  malformed-check failure mode this de-scoping targets. Log free space; refuse below a crude
  absolute floor.
- **Do NOT add a HEAD-vs-state-file refusal.** D-48 cut it; the sha-derived path already makes a
  foreign state file unreachable.
- **Do NOT change any stage's invocation.** Plan 26-07 owns the stage list; this plan adds the
  scheduler around it. In `experiments/suite_expectations.json` you may add ONLY a new top-level
  `preflight` block; do not touch `stages` or `artifacts`.
- **Do NOT rewrite the driver in Python** (D-26).
</executor_rules>

<decisions_implemented>
D-01 (sticky flag, non-zero final exit, loud summary; never abort mid-queue), D-02 (three check
points), D-03 (pre-flight aborts), D-14 (`--skip-e2` declares the omission; the gate records a
DECLARED REDUCTION), D-17 (frameset IDENTITY, sourced from the manifest), D-19 (manifest emitted
once at pre-flight), D-24 as NARROWED by D-46/D-47, D-33 form 2 (the dry-run harness extended over
all new stages), D-50 (every refusal prints its override flag; nothing aborts after stage 1),
D-52 (selective concurrency and its three hard constraints).
</decisions_implemented>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-flight — manifest emission, frameset identity, and refusals that print their override flag</name>
  <read_first>
    - `experiments/run_experiment_suite.sh` in full (plan 26-07)
    - `experiments/rerun_19_5.sh:280-311` (the `prelaunch_probe` stage and its heredoc calling
      `experiments.check_rerun_gates.legality_probe`) and `:455-463` (the hard-abort pattern)
    - `experiments/_run_manifest.py` (plan 26-02) and `experiments/_expectations.py` (plan 26-03)
    - `.planning/phases/26-full-suite-driver-handoff-readiness/26-CONTEXT.md` D-03, D-14, D-17,
      D-19, D-24, D-46, D-47, D-48, D-50
  </read_first>
  <files>experiments/run_experiment_suite.sh, experiments/suite_expectations.json</files>
  <action>
    Add a `preflight` block to `experiments/suite_expectations.json` — a NEW top-level key, leaving
    `stages` and `artifacts` untouched. It records the E2 frameset signature D-17 asserts
    (262 usable → 52 validation → 7,762 comparisons, plus the equivalent file/frame count the driver
    can check cheaply without running a calibration), the crude absolute free-space floor in GiB, and
    the name of the override flag for each refusal.

    Implement `run_stage_preflight` in the driver, running BEFORE stage 1 and following the
    hard-abort pattern already proven at `rerun_19_5.sh:455-463`. It performs, in this order:

    1. **Emit the run manifest** by invoking `"${GATE_PYTHON}" -m experiments._run_manifest --out
       "${OUT_DIR}"`. Written at pre-flight so a run that dies at stage 3 still has its environment
       recorded (D-19). A non-zero exit here aborts.
    2. **Frameset IDENTITY, not presence** (D-17). Read the expected signature from the manifest's
       `preflight` block — never a literal in the script. On mismatch, ABORT, and print the expected
       and observed values side by side. On ABSENCE, ABORT unless `--skip-e2` was passed; when it
       was, announce the DECLARED REDUCTION loudly, record it so the completeness gate's roll-up can
       report it, and note in the log that the run is synthetic-only. A *silent* skip must be
       impossible: "loud" alone is a log line and nobody reads the log overnight.
    3. **Non-empty output tree with no matching state file for this sha** (D-24, phrased exactly this
       way so a genuine resume still proceeds — otherwise the first crash bricks the recovery path
       the driver was built around). ABORT with the override flag printed.
    4. **Free space**: log it, and refuse below the crude absolute floor from the manifest. No
       estimator (D-46).
    5. **Pre-flight completeness**: call the completeness gate at the selected profile with no
       `--stage`, to check preconditions rather than outputs (D-02's first check point).
    6. Optionally warn — never abort — when the manifest's `wall_clock_summary` total exceeds a
       remaining-window value the operator may pass (D-38).

    **D-50 is the governing rule for every one of these:** each refusal must print the exact override
    flag that bypasses it (`--skip-e2`, `--allow-nonempty-out`, `--allow-low-disk`, etc.), so a
    malformed check costs one minute and one flag, never a night. Add those flags to the driver's
    argument handling.

    Explicitly do NOT add: a dirty-tree refusal (D-47), a disk-headroom estimator (D-46), a
    HEAD-vs-state refusal (D-48). Leave a comment at the pre-flight block naming all three and why
    each was cut, so a future reader does not "restore" them.

    Keep `prelaunch_probe` as its own stage where 19.5 had it — its `legality_probe` import is a
    cross-file contract with `check_rerun_gates.py` and its failure is also a hard abort.
  </action>
  <verify>
    <automated>bash -n experiments/run_experiment_suite.sh && $HOME/anaconda3/envs/AquaCal/python.exe -c "import json; d=json.load(open('experiments/suite_expectations.json')); p=d['preflight']; assert p['frameset']['usable_frames']==262 and p['frameset']['validation_frames']==52 and p['frameset']['comparisons']==7762; assert 'free_space_floor_gib' in p; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok` and `bash -n` exits 0.
    - `grep -c '\-\-skip-e2' experiments/run_experiment_suite.sh` returns at least 2 (the flag and
      the refusal message that names it).
    - Every pre-flight refusal message names an override flag: confirm by
      `sed -n '/run_stage_preflight/,/^}/p' experiments/run_experiment_suite.sh | grep -c 'override\|--allow\|--skip-e2'`
      returning at least 4.
    - `grep -c '262' experiments/run_experiment_suite.sh` returns 0 — the frameset signature is read
      from the manifest, never hardcoded in the script.
    - There is no dirty-tree refusal: `sed -n '/run_stage_preflight/,/^}/p' experiments/run_experiment_suite.sh | grep -c 'status --porcelain'`
      returns 0 (any mention of D-47 is in a comment only).
    - `grep -c '_run_manifest' experiments/run_experiment_suite.sh` returns at least 1, invoked via
      `"${GATE_PYTHON}"`.
    - `git diff experiments/suite_expectations.json` adds ONLY the `preflight` key; the `stages` and
      `artifacts` arrays are byte-unchanged (confirm with a JSON diff and state the result in the
      SUMMARY).
  </acceptance_criteria>
  <done>Pre-flight emits the manifest, asserts frameset identity from the manifest, refuses only the four narrowed conditions, and prints an override flag for each.</done>
</task>

<task type="auto">
  <name>Task 2: Add the sticky-failure flag, the end-of-run roll-up, and the selective concurrency pool</name>
  <read_first>
    - `experiments/run_experiment_suite.sh` — `run_gate_check`, `run_one_stage`, `main`
    - `experiments/rerun_19_5.sh:250-263` (`run_gate_check`'s always-return-0 contract) and
      `:425-470` (`run_one_stage`)
    - `experiments/suite_expectations.json` — `depends_on`, `concurrency`, `frame_class`
    - `.planning/probes/2026-08-18-solver-concurrency/FINDINGS.md`
    - `.planning/phases/26-full-suite-driver-handoff-readiness/26-CONTEXT.md` D-01, D-02, § E's D-52
  </read_first>
  <files>experiments/run_experiment_suite.sh</files>
  <action>
    **Sticky flag (D-01).** Introduce `SUITE_FAILED=0` at the top. `run_gate_check` keeps its
    always-return-0 contract UNCHANGED — D-01 is a change to that function's CALLER, not to it.
    The caller inspects the gate's exit code (capture it inside `run_gate_check` and expose it via a
    variable or a companion function) and sets `SUITE_FAILED=1` on a FAIL, then continues. `main`
    exits `${SUITE_FAILED}`. Nothing aborts after stage 1 (D-50).

    **End-of-run roll-up (D-02).** After the last stage, call the completeness gate once with no
    `--stage` selector over the whole tree at the run's profile, print a loud terminal summary block
    naming every missing or short artifact and any DECLARED REDUCTION recorded at pre-flight, and set
    `SUITE_FAILED=1` on any FAIL. This is the check whose absence produced F-001 — say so in a
    comment. Note also, in that comment, that `_check_git_sha_consistency` returns PASS over an empty
    tree (`check_rerun_gates.py:1749-1754`), which is exactly why the roll-up rather than Gate 3 is
    what catches this class; do NOT weaken Gate 3 to cover it.

    **Concurrency pool (D-52).** Replace `main`'s straight `for stage in "${STAGES[@]}"` with a
    scheduler that:
    - reads each stage's `depends_on`, `concurrency` and `frame_class` from
      `experiments/suite_expectations.json` via `"${GATE_PYTHON}"` (bash cannot parse JSON reliably;
      emit a simple TSV from a one-line Python call and read that);
    - launches at most `${SUITE_WORKERS:-4}` concurrent stages, bounded to 4–5 — **the author's
      decision, well inside the probe's bound; do not widen it on the probe's `recommended_workers:
      16`**, because E1 is the cheapest solve in the suite and its peak RSS explicitly does not
      transfer;
    - runs a `serial_alone` stage with nothing else in flight, and starts nothing else until it is
      done — the constraint exists to protect TIMING integrity and applies exactly to `e4`,
      `e4_repeat`, `e2_timing`, `e2_memory`;
    - never has two `frame_class == "200"` stages in flight at once (5 x 3.5 GiB plus one 200-frame
      stage is 27.8 of ~31 GiB, too tight);
    - never overlaps `e6_repeat1` with `e6_band` — enforced by the `depends_on` edge, which exists
      because `run_stage_e6_repeat1` does `rm -rf "${OUT_DIR}/e6_configs"` and removes
      `generalization_sweep.csv` / `e6_provenance.json` under the SHARED `OUT_DIR` that `e6_band`
      also writes;
    - preserves shortest-first order within each ready set (D-37);
    - preserves per-stage `state_start` / `state_complete` stamping and per-stage `run_gate_check`
      exactly as today, since those stamps are the only per-stage timing record that exists;
    - captures each concurrent stage's stdout to its own per-stage log via `tee` with
      `PIPESTATUS[0]` for the exit code — interleaved stdout from four stages is unreadable and the
      exit-code capture must not be lost.

    Add `SUITE_WORKERS` and `SUITE_SERIAL=1` (force fully serial) as environment overrides, and say
    in the header that Phase 27's Linux smoke confirms the same OpenBLAS build behaves the same way
    on the target — two minutes there, not a reason to defer.

    Do NOT attempt to split `e6_band` across processes by seed. It attacks the critical path (8.9 h,
    ~40% of the suite) but needs a merge step and provenance handling inside the experiment, and
    CONTEXT § E declines it explicitly.
  </action>
  <verify>
    <automated>bash -n experiments/run_experiment_suite.sh && grep -q 'SUITE_FAILED' experiments/run_experiment_suite.sh && grep -q 'SUITE_WORKERS' experiments/run_experiment_suite.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n experiments/run_experiment_suite.sh` exits 0.
    - `run_gate_check`'s body is unchanged: `git diff experiments/run_experiment_suite.sh` shows no
      removed line inside that function (the sticky flag is set by the caller).
    - `grep -c 'exit "\${SUITE_FAILED}"\|exit \${SUITE_FAILED}' experiments/run_experiment_suite.sh`
      returns at least 1, and it is in `main`.
    - `grep -c 'PIPESTATUS' experiments/run_experiment_suite.sh` returns at least 1 per concurrent
      launch path.
    - `grep -c 'serial_alone' experiments/run_experiment_suite.sh` returns at least 1 and
      `grep -c 'frame_class' experiments/run_experiment_suite.sh` returns at least 1 — the scheduler
      reads both attributes rather than hardcoding a stage list.
    - `SUITE_WORKERS`'s default is 4 or 5, not 16; confirm by reading the assignment.
    - `grep -c 'F-001' experiments/run_experiment_suite.sh` returns at least 1 (the roll-up comment).
  </acceptance_criteria>
  <done>A gate FAIL sets a sticky flag and the queue continues; the run ends with a roll-up and a non-zero exit when anything is missing; stages are scheduled by the manifest's dependency, concurrency and frame-class attributes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the driver's first test scaffold, tests/unit/test_run_experiment_suite_dryrun.py</name>
  <read_first>
    - `experiments/run_experiment_suite.sh` (Tasks 1 and 2)
    - `experiments/rerun_19_5.sh:174-185` (the dry-run state-file separation and its discovery note)
      and `:265-278` (`_dry_run_active` / `_dry_run_stub`)
    - `.planning/phases/26-full-suite-driver-handoff-readiness/26-VALIDATION.md` § Wave 0
      Requirements (this file is named there; **no driver-test scaffold exists anywhere in the repo**)
    - `tests/unit/test_e5_band_mode.py` — the `tmp_path` fixture shape to copy
  </read_first>
  <files>tests/unit/test_run_experiment_suite_dryrun.py</files>
  <behavior>
    - A full dry run (`RUN_EXPERIMENT_SUITE_DRY_RUN=1`, `RUN_EXPERIMENT_SUITE_DRY_RUN_CMD=true`)
      completes every stage, exits 0, and writes a state file with one `start` and one `complete`
      line per stage, in an order consistent with the manifest's `depends_on` edges.
    - **A dry run does NOT write the real state file.** Assert both that the dry-run path exists and
      that the real path does not. This is the 19.5 fix, and its absence in 19.3 is what made a
      real launch a silent no-op.
    - Resume: given a state file already carrying a `complete` line for stage N, a second dry run
      logs a SKIP for stage N and does not re-run it.
    - Started-but-not-completed: given a state file with only a `start` line for stage N, the stage
      IS re-run from scratch.
    - Sticky exit: with `RUN_EXPERIMENT_SUITE_DRY_RUN_CMD` arranged so one stage's gate reports a
      FAIL, the queue still runs every subsequent stage AND the driver's final exit code is non-zero.
    - Pre-flight abort: with a pre-flight precondition forced to fail, the driver exits before stage
      1, no stage state lines are written, and the printed message names the override flag (D-50).
    - `--skip-e2` produces a DECLARED REDUCTION line in the output and does not abort.
    - Fully serial mode (`SUITE_SERIAL=1`) and pooled mode both complete; in pooled mode no two
      `serial_alone` stages and no two `frame_class == "200"` stages overlap, asserted from the
      state file's ISO start/complete stamps.
  </behavior>
  <action>
    Create `tests/unit/test_run_experiment_suite_dryrun.py`. There is no scaffold to copy, so build
    one: a fixture that copies the driver into a `tmp_path` sandbox (or runs it with `OUT_DIR` and
    the state-file directory redirected via environment variables — prefer this, and add the
    environment overrides to the driver if they are missing), invokes it with
    `subprocess.run([... , "bash", script], env=..., timeout=120, capture_output=True)`, and returns
    the exit code, stdout and the parsed state TSV.

    Mark the module `@pytest.mark.timeout(120)` (or pass an explicit `timeout=` to every
    `subprocess.run`) so a hung driver fails the test rather than hanging the suite.

    **Every invocation in this file runs under the dry-run seam.** No stage may execute a real
    experiment. Assert this defensively: a test that greps the captured stdout for
    `python -u -m experiments.` actually EXECUTING (rather than being echoed by the stub) should
    fail. Prefer a `DRY_RUN_CMD` that writes a marker line per stage so the test can assert which
    stages ran without any experiment running.

    Parse the state TSV with the same three-field shape the driver writes
    (`name<TAB>idx<TAB>event<TAB>iso<TAB>exit_code`), and use the ISO stamps for the overlap
    assertions.
  </action>
  <verify>
    <automated>$HOME/anaconda3/envs/AquaCal/python.exe -m pytest tests/unit/test_run_experiment_suite_dryrun.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_run_experiment_suite_dryrun.py -x -q` passes with at least 9 tests
      collected, and the whole file completes in under 120 s.
    - A test asserts the real state file is ABSENT after a dry run; confirm it exists by name in
      `pytest tests/unit/test_run_experiment_suite_dryrun.py --collect-only -q`.
    - The sticky-exit test asserts BOTH that later stages ran AND that the final exit code is
      non-zero — a test that only checks the exit code would pass on an abort.
    - Every `subprocess.run` call in the file passes an explicit `timeout`; confirm with
      `grep -c 'timeout=' tests/unit/test_run_experiment_suite_dryrun.py` matching the count of
      `subprocess.run(`.
    - No test in the file runs an experiment: `grep -c 'RUN_EXPERIMENT_SUITE_DRY_RUN' tests/unit/test_run_experiment_suite_dryrun.py`
      is at least 1 per invocation helper, and the SUMMARY states the total wall clock of the file.
    - The overlap assertions are derived from the state file's ISO stamps, not from a mock.
  </acceptance_criteria>
  <done>The driver has an automated test covering sequencing, resume, the started-vs-completed distinction, dry-run state separation, the sticky exit, the pre-flight abort and the concurrency constraints — all in under two minutes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-flight verdict to a twenty-hour run | A malformed refusal costs a night; a missing one costs the run's validity |
| concurrent stages to shared `experiments/results/` | Two stages writing the same filename corrupt each other |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-26 | Spoofing | a run exiting 0 with a band CSV never produced | mitigate | D-01's sticky flag plus D-02's end-of-run roll-up; the dry-run test asserts both halves |
| T-26-27 | Denial of service (own run) | a refusal that fires on resume and kills a recoverable run | mitigate | D-47/D-46/D-48's cuts are enforced by acceptance criteria; D-50 requires every surviving refusal to print its override flag |
| T-26-28 | Spoofing | a presence-only frameset check passing on the wrong archive | mitigate | D-17's identity signature, read from the manifest; FIX-06's "60 → 12 → 1,817" is the precedent |
| T-26-29 | Tampering | concurrent stages clobbering shared artifacts | mitigate | `depends_on` for `e6_repeat1`/`e6_band`; filename disjointness already asserted against the manifest by plan 26-03's `TestConcurrencySafety` |
| T-26-30 | Denial of service | OOM at 4–5 wide | mitigate | `frame_class` admission control (at most one 200-frame stage); pool capped at the author's 4–5, never the probe's 16; `SUITE_SERIAL=1` escape hatch |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | This phase installs nothing; `pyproject.toml` is not in `files_modified` |
</threat_model>

<verification>
- `bash -n experiments/run_experiment_suite.sh` exits 0.
- `pytest tests/unit/test_run_experiment_suite_dryrun.py tests/unit/test_suite_stage_list.py -q` green.
- `git diff experiments/suite_expectations.json` adds only the `preflight` key.
</verification>

<success_criteria>
The driver refuses only what it must, prints an escape hatch for each refusal, never aborts after
stage 1, exits non-zero when anything is missing, schedules stages by declared dependency and
resource class, and is covered by an automated test for the first time.
</success_criteria>

<output>
Create `.planning/phases/26-full-suite-driver-handoff-readiness/26-08-SUMMARY.md` when done.
</output>
