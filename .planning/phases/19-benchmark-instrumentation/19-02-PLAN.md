---
phase: 19-benchmark-instrumentation
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/aquacal/calibration/interface_estimation.py
  - tests/unit/test_interface_estimation.py
autonomous: true
requirements: [BENCH-01, BENCH-03, BENCH-06]

must_haves:
  truths:
    - "optimize_interface passes ftol=1e-8, xtol=1e-8, gtol=1e-8 explicitly to least_squares, and a calibration run's result.x/nfev/cost are bit-identical to before this change"
    - "Calling optimize_interface with diagnostics_out=SolverDiagnostics() populates nfev/njev/cost/optimality/status/message plus P and column-group count read from the already-built jac_sparsity/groups arrays; njev is a populated int, never None, regardless of use_sparse_jacobian, because this site uses method='trf'"
    - "Calling register_auxiliary_camera with diagnostics_out=SolverDiagnostics() populates nfev/njev/cost/optimality/status/message with njev a populated int (this site also uses method='trf'); n_params/n_groups stay null with a stated reason (no column-grouping machinery at this site)"
  artifacts:
    - path: "src/aquacal/calibration/interface_estimation.py"
      provides: "diagnostics_out trailing parameter on optimize_interface and register_auxiliary_camera; explicit ftol/xtol/gtol on optimize_interface's least_squares call"
      contains: "diagnostics_out: SolverDiagnostics | None = None"
  key_links:
    - from: "optimize_interface's least_squares call (line 337)"
      to: "capture_solver_diagnostics(result, diagnostics_out, ...)"
      via: "call immediately after least_squares returns, before result.status is checked"
      pattern: "capture_solver_diagnostics\\(result, diagnostics_out"
    - from: "jac_sparsity / groups (already built at lines 292-314)"
      to: "diagnostics_out.n_params / diagnostics_out.n_groups"
      via: "jac_sparsity.shape[1] and int(groups.max()) + 1, captured without recomputation"
      pattern: "jac_sparsity.shape\\[1\\]"
---

<objective>
Wire `capture_solver_diagnostics` (from Plan 19-01) into `optimize_interface` (Stage 3,
`interface_estimation.py:337`) and `register_auxiliary_camera`
(`interface_estimation.py:672`) — the two BENCH-01 (D-07) call sites living in this file.
`optimize_interface` is also one of BENCH-06's two target sites (D-11: "Stage 3"), so this
plan also makes its `ftol`/`xtol`/`gtol` explicit and proves bit-exactness by regression test.
`register_auxiliary_camera` is NOT a BENCH-06 target (D-11) — it gets diagnostics capture only,
no tolerance change.

Purpose: today only `result.status`/`result.message` are read at both sites, and only on the
failure path (D-08). This plan makes that capture unconditional and additive via a trailing
`diagnostics_out` out-parameter, and locks in the termination criteria the paper supplement
states as AquaCal-set values rather than inherited SciPy defaults (BENCH-06, R3.2).
Output: both functions accept `diagnostics_out: SolverDiagnostics | None = None`;
`optimize_interface` passes tolerances explicitly; a bit-exact regression test proves no
numerical change.

**Both `least_squares` calls in this file use `method="trf"`** (confirmed live at both call
sites during planning). Per 19-RESEARCH.md's corrected Pitfall 5, `njev` is a populated `int`
at both sites regardless of `use_sparse_jacobian`/whether `jac` is callable or `"2-point"` —
`None` occurs only for SciPy's `"lm"` method, which neither site uses. Every test and
acceptance criterion in this plan asserting `njev` must assert it as a populated int, never
`None`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-benchmark-instrumentation/19-CONTEXT.md
@.planning/phases/19-benchmark-instrumentation/19-RESEARCH.md
@src/aquacal/calibration/interface_estimation.py
@.planning/phases/19-benchmark-instrumentation/19-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 19-01's output, src/aquacal/calibration/_observability.py -->
```
@dataclass
class SolverDiagnostics:
    nfev: int | None = None
    njev: int | None = None
    cost: float | None = None
    optimality: float | None = None
    status: int | None = None
    message: str | None = None
    ftol: float | None = None
    xtol: float | None = None
    gtol: float | None = None
    max_nfev_effective: int | None = None
    max_nfev_source: str | None = None
    n_params: int | None = None
    n_params_reason: str | None = None
    n_groups: int | None = None
    n_groups_reason: str | None = None

def capture_solver_diagnostics(
    result, diagnostics_out, *, ftol, xtol, gtol, max_nfev_effective, max_nfev_source,
    n_params=None, n_groups=None, n_params_reason=None, n_groups_reason=None,
) -> None: ...
```

<!-- Existing optimize_interface call site, src/aquacal/calibration/interface_estimation.py:289-354 -->
The `jac_sparsity` and `groups` (passed as `groups=build_structural_column_groups(...)`) local
variables already exist at lines 292-314, before the `least_squares` call at line 337. `P =
jac_sparsity.shape[1]`; `n_groups = int(groups.max()) + 1` where `groups` is the array
returned by `build_structural_column_groups(...)` (already computed as the `groups=` argument
to `make_sparse_jacobian_func` at line 306 — capture it into a local variable rather than
calling `build_structural_column_groups` a second time). This call site's `method="trf"` is
already in the existing `least_squares(...)` call (line 341) and is not changed by this plan.

Note `use_sparse_jacobian` is a real caller-facing parameter (default `True`); when `False`,
`jac_sparsity`/`groups` are never built, so `n_params`/`n_groups` must be `None` with a reason
(e.g. `"use_sparse_jacobian=False; no column-grouping structure was built"`), not silently
skipped. `njev` is unaffected by `use_sparse_jacobian` — SciPy's `method="trf"` populates it
either way (corrected Pitfall 5).

<!-- CORRECTED post-plan-check finding — this superseded the original plan's incorrect
assumption. See 19-RESEARCH.md Pitfall 5, corrected 2026-07-24: -->
```
least_squares(f, x0, method='trf', loss='huber', f_scale=1.0)   -> njev = 7    (an int)
least_squares(f, x0, method='lm')                                -> njev = None
```
Both `optimize_interface` and `register_auxiliary_camera` use `method="trf"` (confirmed at
`interface_estimation.py:341` and `interface_estimation.py:676` respectively), so `njev` is
always a populated int at both sites.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: optimize_interface — explicit tolerances + diagnostics capture + bit-exact test</name>
  <files>src/aquacal/calibration/interface_estimation.py, tests/unit/test_interface_estimation.py</files>
  <read_first>
    - src/aquacal/calibration/interface_estimation.py (lines 124-374 — optimize_interface's full body, including the `jac_sparsity`/`groups` construction at 289-314 and the `least_squares` call at 337-348)
    - src/aquacal/calibration/_observability.py (Plan 19-01's SolverDiagnostics/capture_solver_diagnostics contract, including the corrected njev docstring)
    - tests/unit/test_interface_estimation.py (existing `TestOptimizeInterfaceObserver` class, ~line 850, for the established bit-exact-with-vs-without-optional-param test pattern this task's new test must mirror)
  </read_first>
  <behavior>
    - `optimize_interface(..., diagnostics_out=None)` behaves identically to the function before
      this change (no new observable effect) — this is the primary regression guarantee.
    - `optimize_interface(..., diagnostics_out=diag)` populates `diag.nfev`, `diag.njev`,
      `diag.cost`, `diag.optimality`, `diag.status`, `diag.message`, `diag.ftol == 1e-8`,
      `diag.xtol == 1e-8`, `diag.gtol == 1e-8`, `diag.max_nfev_effective == x0.size * 100`,
      `diag.max_nfev_source == "scipy_auto"` (this call site never sets `max_nfev`),
      `diag.n_params == jac_sparsity.shape[1]`, `diag.n_groups == int(groups.max()) + 1` when
      `use_sparse_jacobian=True` (the default). `diag.njev` is asserted as a populated `int`
      (never `None`) in BOTH the `use_sparse_jacobian=True` and `use_sparse_jacobian=False`
      cases, since this call site always uses `method="trf"` — write a parametrized or
      duplicated test asserting `diag.njev is not None and isinstance(diag.njev, int)` under
      both `use_sparse_jacobian` values.
    - Regression test: running the same synthetic fixture twice — once on the pre-existing code
      path (implicit tolerances, no diagnostics_out) and once with explicit tolerances +
      diagnostics_out supplied — produces exactly equal `result.x`-derived outputs
      (`np.testing.assert_array_equal`, not `allclose`) for extrinsics, distances, board poses,
      and `rms_error`.
  </behavior>
  <action>
    In `optimize_interface` (interface_estimation.py:124), add a trailing keyword parameter
    `diagnostics_out: SolverDiagnostics | None = None` (import `SolverDiagnostics`,
    `capture_solver_diagnostics` from `aquacal.calibration._observability` alongside the
    existing `OptimizerObserver, build_parameter_labels` import at line 11). At the
    `least_squares(...)` call (line 337), add `ftol=1e-8, xtol=1e-8, gtol=1e-8` as explicit
    keyword arguments (these are SciPy 1.17's hardcoded defaults per 19-RESEARCH.md Q2 — the
    call becomes bit-identical by inspection, not just by test; `method="trf"` is already set
    and unchanged). Immediately after the
    `if result.status <= 0: raise ConvergenceError(...)` check (so the diagnostics reflect the
    actual terminal result whether or not the run is about to raise — capture happens
    unconditionally before the raise, matching D-08's "additive, not a refactor of error
    handling"), call `capture_solver_diagnostics(result, diagnostics_out, ftol=1e-8, xtol=1e-8,
    gtol=1e-8, max_nfev_effective=len(initial_params) * 100, max_nfev_source="scipy_auto",
    n_params=jac_sparsity.shape[1] if use_sparse_jacobian else None,
    n_groups=(int(groups.max()) + 1) if use_sparse_jacobian else None,
    n_params_reason=None if use_sparse_jacobian else "use_sparse_jacobian=False; no
    column-grouping structure was built", n_groups_reason=None if use_sparse_jacobian else
    "use_sparse_jacobian=False; no column-grouping structure was built")`. Capture the
    `groups` array returned by `build_structural_column_groups(...)` (line 306's `groups=`
    argument) into a named local variable before passing it to `make_sparse_jacobian_func`, so
    it is available for this diagnostics call without a second computation (BENCH-03's "measured
    from the live run rather than a separate script"). Add a
    `test_optimize_interface_diagnostics_out_populated_and_bit_exact` test to
    `tests/unit/test_interface_estimation.py` (in or near the existing
    `TestOptimizeInterfaceObserver` class) exercising both the population assertions
    (including the `njev`-is-a-populated-int assertion under both `use_sparse_jacobian` values)
    and the bit-exact regression comparison described in `<behavior>`.
  </action>
  <acceptance_criteria>
    - `grep -n "ftol=1e-8, xtol=1e-8, gtol=1e-8" src/aquacal/calibration/interface_estimation.py` matches the `optimize_interface` call site
    - `optimize_interface`'s signature includes `diagnostics_out: SolverDiagnostics | None = None` as its final parameter
    - The new test asserts `diag.njev is not None` and `isinstance(diag.njev, int)` for both `use_sparse_jacobian=True` and `use_sparse_jacobian=False`
    - `python -m pytest tests/unit/test_interface_estimation.py -k diagnostics_out -q` passes
    - `python -m pytest tests/unit/test_interface_estimation.py -q` passes in full (no regression)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unit/test_interface_estimation.py -k diagnostics_out -q</automated>
  </verify>
  <done>optimize_interface passes ftol/xtol/gtol explicitly, accepts diagnostics_out, populates it correctly including P/n_groups from the already-built sparsity arrays and a populated njev under both use_sparse_jacobian values, and a bit-exact regression test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: register_auxiliary_camera — diagnostics capture (no tolerance change)</name>
  <files>src/aquacal/calibration/interface_estimation.py, tests/unit/test_interface_estimation.py</files>
  <read_first>
    - src/aquacal/calibration/interface_estimation.py (lines 489-711 — register_auxiliary_camera's full body, including the `least_squares` call at line 672 and the union return type at 502-505)
    - .planning/phases/19-benchmark-instrumentation/19-CONTEXT.md (D-11 — this site is explicitly NOT a BENCH-06 target; D-15 — null values must carry a reason, never be silently omitted)
    - .planning/phases/19-benchmark-instrumentation/19-RESEARCH.md (Pitfall 5, corrected — `njev` is populated for `method='trf'` regardless of `jac='2-point'`; this call site uses `method="trf"` and NEVER sets `jac=`, i.e. it always runs the `'2-point'` finite-difference branch, which is exactly the case the corrected finding covers)
  </read_first>
  <behavior>
    - `register_auxiliary_camera(..., diagnostics_out=None)` behaves identically to before this
      change.
    - `register_auxiliary_camera(..., diagnostics_out=diag)` populates `diag.nfev`, `diag.cost`,
      `diag.optimality`, `diag.status`, `diag.message`, AND `diag.njev` — all as populated
      values, none `None`. This function never sets `jac=` (it always runs SciPy's `"2-point"`
      finite-difference path) and uses `method="trf"` (confirmed at
      `interface_estimation.py:676`); per 19-RESEARCH.md's corrected Pitfall 5, `njev` is a
      populated `int` here precisely BECAUSE `method="trf"` is used, regardless of the
      `"2-point"` jac. The original (superseded) assumption that `"2-point"` alone implies
      `njev=None` is false and must not be asserted or tested.
    - `diag.n_params` and `diag.n_groups` stay `None`; `diag.n_params_reason` and
      `diag.n_groups_reason` both state that this site uses no column-grouping structure (this
      null-with-reason is about the absence of sparse-Jacobian column grouping, which is a
      genuinely different and unrelated fact from `njev`'s availability — do not conflate the
      two null cases).
    - `diag.ftol == 1e-8`, `diag.xtol == 1e-8`, `diag.gtol == 1e-8` are recorded as the values
      in force (SciPy's implicit defaults — this function does not set them explicitly, and
      this task must NOT add explicit `ftol=`/`xtol=`/`gtol=` kwargs here, since D-11 excludes
      this site from BENCH-06).
  </behavior>
  <action>
    In `register_auxiliary_camera` (interface_estimation.py:489), add a trailing keyword
    parameter `diagnostics_out: SolverDiagnostics | None = None` (after the existing `verbose`
    parameter). Immediately after the `least_squares(...)` call at line 672 (before the
    `refine_intrinsics` branch that extracts `result.x` slices), call
    `capture_solver_diagnostics(result, diagnostics_out, ftol=1e-8, xtol=1e-8, gtol=1e-8,
    max_nfev_effective=len(x0) * 100, max_nfev_source="scipy_auto", n_params=None,
    n_groups=None, n_params_reason="register_auxiliary_camera uses dense 2-point FD; no
    column-grouping structure exists at this site", n_groups_reason="register_auxiliary_camera
    uses dense 2-point FD; no column-grouping structure exists at this site")`. Do not add
    `ftol=`/`xtol=`/`gtol=` as explicit keyword arguments to this `least_squares` call — those
    remain implicit SciPy defaults per D-11; only the *recorded* values in `SolverDiagnostics`
    state what is in force. Add
    `test_register_auxiliary_camera_diagnostics_out_populated_with_njev_and_null_grouping_reasons`
    to `tests/unit/test_interface_estimation.py` asserting: (1) `diag.njev is not None` and
    `isinstance(diag.njev, int)` — this is the corrected assertion replacing the plan-checker's
    flagged blocker; (2) `diag.n_params is None` and `diag.n_params_reason` is the stated
    column-grouping-absence reason; (3) `diag.n_groups` and `diag.n_groups_reason` mirror the
    same pattern.
  </action>
  <acceptance_criteria>
    - `register_auxiliary_camera`'s signature gains `diagnostics_out: SolverDiagnostics | None = None` as its final parameter
    - `grep -c "ftol=1e-8" src/aquacal/calibration/interface_estimation.py` returns exactly 1 (only `optimize_interface`'s Task 1 addition — this task adds zero explicit tolerance kwargs)
    - The new test asserts `diag.njev is not None` and `isinstance(diag.njev, int)` — NOT `diag.njev is None`
    - `diag.n_params is None` and `diag.n_params_reason` is a non-empty string naming the absence of column-grouping structure (this is a DIFFERENT null case from njev and must not be conflated with it)
    - `python -m pytest tests/unit/test_interface_estimation.py -k register_auxiliary_camera_diagnostics -q` passes
    - Full file test suite still green: `python -m pytest tests/unit/test_interface_estimation.py -q`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unit/test_interface_estimation.py -k register_auxiliary_camera_diagnostics -q</automated>
  </verify>
  <done>register_auxiliary_camera accepts diagnostics_out, populates solver diagnostics with njev correctly asserted as a populated int (not None), correctly-reasoned nulls for P/n_groups only, and does not gain explicit tolerance kwargs (D-11 respected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| In-process optimizer call | `least_squares` operates on detection data already validated by earlier pipeline stages; no new untrusted input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-19-02a | Tampering (numerical) | `optimize_interface`'s explicit `ftol`/`xtol`/`gtol` kwargs | mitigate | Values are the literal SciPy 1.17 hardcoded defaults (verified live in 19-RESEARCH.md); bit-exact regression test asserts zero behavior change |
| T-19-02b | Repudiation | Missing diagnostics on failure paths | mitigate | `capture_solver_diagnostics` is called unconditionally before the `ConvergenceError` raise, so a failed run's diagnostics are still captured, not lost |
</threat_model>

<verification>
- `python -m pytest tests/unit/test_interface_estimation.py -q` full file passes
- `python -m pytest tests/ -m "not slow" -q` passes (no cross-file regression)
- `ruff check src/aquacal/calibration/interface_estimation.py tests/unit/test_interface_estimation.py` clean
</verification>

<success_criteria>
- `optimize_interface` passes `ftol=1e-8, xtol=1e-8, gtol=1e-8` explicitly and is bit-exact by regression test (BENCH-06 satisfied for this site)
- Both `optimize_interface` and `register_auxiliary_camera` accept `diagnostics_out` and populate it correctly, including D-15-compliant null-with-reason handling for P/n_groups (BENCH-01, BENCH-03 satisfied for both sites)
- `njev` is asserted as a populated int at both sites in every test — no test or acceptance criterion in this plan asserts `njev is None` as an expected outcome
- No existing caller of either function (positional or keyword) is broken
</success_criteria>

<output>
Create `.planning/phases/19-benchmark-instrumentation/19-02-SUMMARY.md` when done
</output>
