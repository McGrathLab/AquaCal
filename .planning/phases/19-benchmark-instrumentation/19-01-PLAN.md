---
phase: 19-benchmark-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aquacal/calibration/_observability.py
  - tests/unit/test_observability.py
autonomous: true
requirements: [BENCH-01]

must_haves:
  truths:
    - "A caller can pass diagnostics_out=SolverDiagnostics() into a least_squares-wrapping function and read back nfev/njev/cost/optimality/status/message after the call returns"
    - "njev is documented and captured as a populated int for method='trf' regardless of whether jac is callable or '2-point' — the dataclass and helper make no false claim that njev is only available for a callable jac"
    - "A metric a call site cannot produce (P, n_groups) is recorded as null with a stated reason, never silently dropped"
  artifacts:
    - path: "src/aquacal/calibration/_observability.py"
      provides: "SolverDiagnostics dataclass and capture_solver_diagnostics() helper"
      contains: "class SolverDiagnostics"
    - path: "tests/unit/test_observability.py"
      provides: "Unit tests for the capture helper, including numpy-scalar-casting and the defensive (not real-site) njev-missing case"
  key_links:
    - from: "capture_solver_diagnostics(result, ...)"
      to: "SolverDiagnostics fields"
      via: "direct attribute assignment after least_squares returns"
      pattern: "capture_solver_diagnostics"
---

<objective>
Add the `SolverDiagnostics` dataclass and a pure `capture_solver_diagnostics()` helper to
`_observability.py`, establishing the contract that Plans 19-02 and 19-03 wire into the four
in-scope `least_squares` call sites (BENCH-01, D-07). This plan is the interface-first
contract step: it creates no threading into any optimizer function yet, only the data
structure and the capture logic those functions will call.

Purpose: BENCH-01 requires `nfev`, `njev`, `cost`, `optimality`, `status`, and termination
`message` captured from every in-scope `least_squares` call, without touching any function's
return tuple (which is public API — see Research Pitfall 1). A shared, tested capture helper
means the four downstream wiring plans (19-02, 19-03) each do a single mechanical call rather
than re-deriving the numpy-scalar-casting logic four times.
Output: `SolverDiagnostics` dataclass + `capture_solver_diagnostics()` in
`src/aquacal/calibration/_observability.py`, covered by new unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-benchmark-instrumentation/19-CONTEXT.md
@.planning/phases/19-benchmark-instrumentation/19-RESEARCH.md
@src/aquacal/calibration/_observability.py
</context>

<interfaces>
<!-- Existing OptimizerObserver/TraceRow pattern in _observability.py that SolverDiagnostics
must mirror stylistically (dataclass, Google docstring, no I/O, never retains `result` or
`result.jac`): -->

From src/aquacal/calibration/_observability.py (existing, unmodified by this plan):
```
@dataclass
class TraceRow:
    iteration: int
    n_fev: int
    cost: float
    step_norm: float
    optimality: float
    water_z: float
    tilt_rx: float
    tilt_ry: float
```

`OptimizerObserver.on_solution(result)` is the precedent for "read scalar fields from `result`
immediately after `least_squares` returns, never retain `result` itself." Note `OptimizerObserver`
is NOT exported from `aquacal.calibration.__init__` — it is imported directly from
`aquacal.calibration._observability` by `pipeline.py` and the four optimizer modules. This
plan's `SolverDiagnostics` follows the same precedent and must NOT be added to
`aquacal/calibration/__init__.py`'s `__all__` — it is a private-module helper type used only
in trailing keyword parameters of exported functions, exactly like `OptimizerObserver`.

<!-- CORRECTED post-plan-check finding — read this before writing any njev-related docstring
or test. 19-RESEARCH.md's original Pitfall 5 claimed njev is None whenever jac="2-point". That
claim was independently reproduced as FALSE and has been corrected in 19-RESEARCH.md: -->
```
least_squares(f, x0, method='trf', loss='huber', f_scale=1.0)   -> njev = 7    (an int)
least_squares(f, x0, method='lm')                                -> njev = None
```
SciPy scopes the `None` case to `method='lm'` only. All four in-scope call sites
(`interface_estimation.py:337`, `interface_estimation.py:672`, `refinement.py:237`,
`point_refinement.py:674`) use `method="trf"`, so `njev` is always a populated `int` at every
in-scope site, in both the `use_sparse_jacobian=True` (callable `jac`) and `False` (`"2-point"`)
branches. `getattr(result, "njev", None)` remains harmless defensive coding for this
generically-reusable helper, but the `None` branch does not occur at any of this codebase's
four in-scope sites and must never be baked into an acceptance criterion for them (that is
Plans 19-02/19-03's job to get right; this plan's own docstring/tests must not assert or imply
the opposite).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the SolverDiagnostics dataclass contract</name>
  <files>src/aquacal/calibration/_observability.py</files>
  <read_first>
    - src/aquacal/calibration/_observability.py (existing TraceRow dataclass and module docstring style, lines 1-72)
    - .planning/phases/19-benchmark-instrumentation/19-RESEARCH.md (Architecture Patterns, Pattern 1 — the exact field list and rationale for a mutable-out-parameter dataclass over a dict; Pitfall 5, corrected — the njev finding above)
  </read_first>
  <behavior>
    - This task defines the contract only; no test is written yet (Task 2 covers behavior).
      Task 1's sole verification is that the module still imports and the dataclass is
      constructible with all-default or all-explicit field values.
  </behavior>
  <action>
    Add a `@dataclass class SolverDiagnostics` to `src/aquacal/calibration/_observability.py`,
    placed after `TraceRow` and before `build_parameter_labels`. Fields, all with defaults so an
    empty `SolverDiagnostics()` is constructible (the caller mutates it in place after
    `least_squares` returns, mirroring `TraceRow`'s pattern but as an out-parameter rather than
    an accumulated list):
    `nfev: int | None = None`, `njev: int | None = None` (Google docstring must read: "Jacobian
    evaluation count. Always a populated int at this codebase's four in-scope call sites — all
    four use `method='trf'`, for which SciPy populates `njev` regardless of whether `jac` is a
    callable or the string `'2-point'`. `None` occurs only for SciPy's `'lm'` method, which no
    in-scope site uses (see 19-RESEARCH.md Pitfall 5, corrected 2026-07-24). Do NOT document or
    assume `None` means `jac='2-point'` — that claim was independently reproduced as false."),
    `cost: float | None = None`,
    `optimality: float | None = None`, `status: int | None = None`, `message: str | None = None`,
    `ftol: float | None = None`, `xtol: float | None = None`, `gtol: float | None = None`,
    `max_nfev_effective: int | None = None`, `max_nfev_source: str | None = None` (one of
    `"explicit"`, `"scipy_auto"`, or a call-site-specific label such as
    `"point_refinement_200x_auto"` — the field takes any string the caller supplies, this
    dataclass does not validate the value), `n_params: int | None = None`,
    `n_params_reason: str | None = None` (populated only when `n_params` is `None`, e.g.
    `"register_auxiliary_camera uses dense 2-point FD, no column-grouping structure exists"`),
    `n_groups: int | None = None`, `n_groups_reason: str | None = None` (same null-with-reason
    convention, per D-15 — never omit a key, always explain why it is `None`). Write a
    Google-style class docstring documenting every field's unit/meaning and the D-15
    null-with-reason convention explicitly, citing BENCH-01/BENCH-03/BENCH-06 by ID in the
    docstring so a future reader can trace which requirement each field group answers.
  </action>
  <acceptance_criteria>
    - `SolverDiagnostics()` (zero-argument) constructs successfully with every field `None`
    - `SolverDiagnostics(nfev=12, njev=3, cost=0.5, optimality=1e-6, status=2, message="ok")` constructs successfully
    - `python -c "from aquacal.calibration._observability import SolverDiagnostics"` exits 0
    - The class docstring names BENCH-01, BENCH-03, and BENCH-06 explicitly
    - The `njev` field's docstring does NOT state or imply "`None` when `jac` is `'2-point'`" — grep the file for that phrase to confirm it is absent (`grep -n "2-point" src/aquacal/calibration/_observability.py` must show no claim that `'2-point'` alone causes `njev=None`)
  </acceptance_criteria>
  <verify>
    <automated>python -c "from aquacal.calibration._observability import SolverDiagnostics; SolverDiagnostics(); print('ok')"</automated>
  </verify>
  <done>SolverDiagnostics dataclass exists with all 14 fields, a Google docstring that correctly scopes njev's None case to method='lm' only, and is importable and constructible with defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement capture_solver_diagnostics() and test it</name>
  <files>src/aquacal/calibration/_observability.py, tests/unit/test_observability.py</files>
  <read_first>
    - src/aquacal/calibration/_observability.py (this plan's Task 1 output — the SolverDiagnostics fields just added)
    - .planning/phases/19-benchmark-instrumentation/19-RESEARCH.md (Common Pitfalls 3 and 5 — numpy-scalar JSON serialization, and the CORRECTED njev finding: njev is None only for method='lm', never for the trf-based in-scope sites)
    - tests/unit/test_observability.py (existing test file structure and imports, if present; if it does not yet exist, follow the class-based structure used in tests/unit/test_interface_estimation.py's TestOptimizeInterfaceObserver)
  </read_first>
  <behavior>
    - Given a real `scipy.optimize.OptimizeResult`-like object with `nfev`, `njev`, `cost`,
      `optimality`, `status`, `message` all populated (the realistic case for every in-scope
      site, since all four use `method="trf"`): `capture_solver_diagnostics` populates every
      corresponding field on the passed-in `SolverDiagnostics` instance with the exact
      native-Python-typed value (`int`/`float`, not `np.int64`/`np.float64`), including
      `njev` as a populated `int` — this is the primary, expected case and must be asserted
      as such, not treated as optional.
    - Given a mock result object that has NO `njev` attribute at all (a purely defensive
      scenario for this generically-reusable capture function — this never occurs at any of
      this codebase's four in-scope call sites, all of which use `method='trf'` and therefore
      always populate `njev`; see 19-RESEARCH.md Pitfall 5, corrected): `capture_solver_diagnostics`
      still degrades gracefully, recording `njev` as `None` rather than raising
      `AttributeError` — this proves the `getattr(..., None)` defensive read works, it does
      NOT assert that any real in-scope call site behaves this way.
    - Given `n_params=None, n_groups=None` with reasons supplied: the reasons are recorded
      verbatim, and `n_params`/`n_groups` stay `None`.
    - Given `n_params=673, n_groups=13`: both are recorded as native `int`, no reason string.
    - Casting: passing `np.float64(1.23)` for `cost` and `np.int64(42)` for `nfev`/`njev` on the
      mock result produces `isinstance(diag.cost, float)`, `isinstance(diag.nfev, int)`, and
      `isinstance(diag.njev, int)`, all `True` (guards Research Pitfall 3 directly).
  </behavior>
  <action>
    Add a module-level function `capture_solver_diagnostics(result, diagnostics_out, *, ftol,
    xtol, gtol, max_nfev_effective, max_nfev_source, n_params=None, n_groups=None,
    n_params_reason=None, n_groups_reason=None) -> None` to `_observability.py`. It mutates
    `diagnostics_out` in place (no-op if `diagnostics_out is None`, so every call site can call
    it unconditionally): `diagnostics_out.nfev = int(result.nfev)`,
    `diagnostics_out.njev = int(njev) if (njev := getattr(result, "njev", None)) is not None
    else None`, `diagnostics_out.cost = float(result.cost)`,
    `diagnostics_out.optimality = float(result.optimality)`,
    `diagnostics_out.status = int(result.status)`, `diagnostics_out.message = str(result.message)`,
    plus direct assignment of `ftol`, `xtol`, `gtol`, `max_nfev_effective`, `max_nfev_source`,
    `n_params`, `n_groups`, `n_params_reason`, `n_groups_reason` from the function's own
    parameters (all already caller-supplied native values, no further casting needed there).
    Google-style docstring stating explicitly: (1) "Must be called only after `least_squares`
    returns; never read `result.jac`, `result.fun`, or `result.x` here (Research Pitfall 4 —
    doing so risks retaining large arrays and inflating the very peak-memory measurement
    BENCH-02 depends on being honest)." and (2) "`njev` is read defensively via `getattr` for
    reuse-safety, but at every one of this codebase's four in-scope call sites (all
    `method='trf'`) it is always populated as an int — the `None` branch is not expected there;
    see 19-RESEARCH.md Pitfall 5, corrected." Add `tests/unit/test_observability.py` (new file,
    or a new `TestCaptureSolverDiagnostics` class if the file already exists after another plan
    lands first) covering every case in the `<behavior>` block above using a small mock object
    (e.g. `types.SimpleNamespace(nfev=np.int64(5), cost=np.float64(0.1), optimality=np.float64(1e-7),
    status=2, message="converged", njev=np.int64(3))` for the primary populated-njev case, and a
    separate `types.SimpleNamespace(...)` deliberately constructed WITHOUT an `njev` attribute
    for the defensive-only case) — do not construct a real `OptimizeResult` via an actual
    `least_squares` call; this is a pure unit test of the capture function's contract.
  </action>
  <acceptance_criteria>
    - `capture_solver_diagnostics(mock_result, None, ftol=1e-8, xtol=1e-8, gtol=1e-8, max_nfev_effective=100, max_nfev_source="scipy_auto")` returns without raising when `diagnostics_out=None`
    - After a populated call using a mock result WITH `njev` set: `type(diag.nfev) is int`, `type(diag.njev) is int`, and `type(diag.cost) is float` (not numpy scalar types)
    - `diag.njev is None` only in the separate defensive test using a mock result that has no `njev` attribute at all — this test's name or docstring must make clear it is a generic-robustness check, not a claim about `jac='2-point'` behavior
    - `diag.n_params_reason` is the exact string passed in when `n_params=None`
    - `pytest tests/unit/test_observability.py -k capture_solver_diagnostics -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/unit/test_observability.py -k capture_solver_diagnostics -q</automated>
  </verify>
  <done>capture_solver_diagnostics() exists, handles the diagnostics_out=None no-op case, defensively reads njev without asserting it is ever None at a real in-scope site, casts every numeric field to native Python types, and all new unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| In-process function call | `capture_solver_diagnostics` reads attributes off a `scipy.optimize.OptimizeResult`-like object produced entirely within this process; no external or untrusted input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-19-01a | Information Disclosure | `capture_solver_diagnostics` retaining `result.jac`/`result.fun` | mitigate | Docstring explicitly forbids reading `result.jac`/`result.fun`/`result.x`; only scalar fields are read, matching `OptimizerObserver.on_solution`'s existing discipline |
| T-19-01b | Denial of Service (accidental) | Numpy-scalar cast failure raising mid-run | accept | Casts are `int()`/`float()`/`str()` on already-well-typed SciPy result fields; no untrusted input reaches this function |
</threat_model>

<verification>
- `python -m pytest tests/unit/test_observability.py -q` passes with the two new tasks' tests included
- `python -m pytest tests/unit/ -q` passes (no regression to existing `_observability.py` consumers, since this plan adds new names only, touching no existing function signature)
- `ruff check src/aquacal/calibration/_observability.py tests/unit/test_observability.py` clean
</verification>

<success_criteria>
- `SolverDiagnostics` and `capture_solver_diagnostics` are both defined in `_observability.py`, importable, and covered by passing unit tests
- Neither the dataclass docstring nor the helper's docstring claims `njev` is unavailable for `jac='2-point'` — that claim was independently disproven and corrected in 19-RESEARCH.md
- No existing test in the repository is broken (this plan is purely additive — no existing function's signature or return value changes)
- Plans 19-02 and 19-03 can `from aquacal.calibration._observability import SolverDiagnostics, capture_solver_diagnostics` without further contract changes
</success_criteria>

<output>
Create `.planning/phases/19-benchmark-instrumentation/19-01-SUMMARY.md` when done
</output>
