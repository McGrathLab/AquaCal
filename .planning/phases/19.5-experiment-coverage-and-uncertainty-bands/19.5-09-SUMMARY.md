---
phase: 19.5-experiment-coverage-and-uncertainty-bands
plan: 09
subsystem: infra
tags: [rerun-queue, gates, legality-probe, e6, e5, e2, e4, prelaunch-gate, bash]

# Dependency graph
requires:
  - phase: 19.5 (this phase), plans 05-08
    provides: "the --seeds/--emit-band-configs/--splice-repeat CLI surfaces (E5, E6, E2, E4) this queue invokes"
  - phase: 19.4-single-flat-interface
    provides: "the generate_camera_array clearance-floor fix (D-19.4-09/15) this plan's legality_probe re-verifies empirically"
provides:
  - "legality_probe(seeds, camera_counts) in check_rerun_gates.py -- D-19.5-04's structural re-verification, no calibration solve"
  - "prelaunch_gate.sh's sixth check, LEGALITY_PROBE, before SUITE_GREEN; SHA_FILE and archive-summary pointer renamed for 19.5"
  - "check_e6_seed_band, check_e5_seed_band, check_e2_band, check_e4_repeat gate functions, wired into run_all_gates"
  - "experiments/rerun_19_5.sh: the single risk-first production queue (prelaunch_probe, e6_band, e4_repeat, e2_band, e5_band), dry-run-verified only, NOT executed"
affects: ["19.5-10 (runs this queue)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Lazy (function-local) import of aquacal/experiments submodules inside legality_probe, so the module stays importable under both `python -m` and the bare-script invocation rerun_19_5.sh/prelaunch_gate.sh both use"
    - "Gate functions read status/seed-count/structural properties from written artifacts, never a process exit code (E4/E6 exit 0 on all-failed rows, MF-07)"
    - "e4_repeat's two repeats scheduled back-to-back inside one stage function, nothing else interleaved, so seconds_total_spread_pct measures the algorithm not concurrent machine load"

key-files:
  created:
    - experiments/rerun_19_5.sh
  modified:
    - experiments/check_rerun_gates.py
    - experiments/prelaunch_gate.sh
    - tests/unit/test_rerun_gates.py

key-decisions:
  - "check_e2_band reads an isolated sibling directory (out_dir.parent / results_e2_band), not out_dir itself, because emit_seed_variant_configs refuses to write into the release tree and the queue keeps the band in-repo but structurally separate from the single production E2 run's own artifacts"
  - "E2's band directory is experiments/results_e2_band/ (in-repo), not the Desktop path plan 07's SUMMARY used as an illustrative example -- Claude's discretion per the plan's file-layout latitude, chosen so check_e2_band wires into run_all_gates the same uniform way as the other three new gates"
  - "prelaunch_gate.sh's ARCHIVES_PRESENT check logic is left completely unchanged per the plan's explicit instruction, even though it will FAIL when actually run this phase -- see Known Issue below"

patterns-established:
  - "Six-check prelaunch gate order: TREE_CLEAN, LEGALITY_PROBE, SUITE_GREEN, HEAD_RECORDED, ARCHIVES_PRESENT, WORKTREES_CLEAN -- the cheap structural check runs before the expensive unfiltered suite"

requirements-completed: [COV-03, COV-04, COV-05, COV-06, COV-07]

# Metrics
duration: ~90min
completed: 2026-08-06
---

# Phase 19.5 Plan 09: Risk-First Production Queue and Gates Summary

**Wrote (did not run) `experiments/rerun_19_5.sh`, D-19.5-04's `legality_probe` structural re-verification, a sixth `prelaunch_gate.sh` check, and four new artifact gates for E6/E5/E2/E4's band CSVs -- all dry-run-verified, zero production solves.**

## Performance

- **Duration:** ~90 min
- **Started:** 2026-08-06T13:24:00Z (approx, worktree base checkout)
- **Completed:** 2026-08-06T13:36:06Z
- **Tasks:** 3 completed (Tasks 1+2 committed together, see Deviations)
- **Files modified:** 4 (1 created, 3 modified)

## Accomplishments

- `legality_probe(seeds, camera_counts)`: re-verifies D-19.5-04's claim empirically. For every `(seed, n_cameras)` in the queue's intended range (seeds 42-46, `n_cameras` {8,12,16} = 30 combinations), performs BOTH the calibration draw (`seed`) and holdout draw (`seed + 1_000_000`), builds the grid-family camera array via `generate_camera_array`, and checks the frozen `GRID_DEPTH_RANGE[0]` clears the draw's own derived `board_clearance_floor`. **Result: all 30 combinations PASS** -- confirmed empirically, not just by inspection of the D-19.4-15 finding it re-verifies. Completes in well under 2 seconds (< 60s ceiling). Proven to perform no calibration via an AST-level test (no `least_squares`/`calibrate_synthetic` reference anywhere in the module).
- `prelaunch_gate.sh` gains a sixth check, `LEGALITY_PROBE`, placed second (right after `TREE_CLEAN`, before the expensive unfiltered `SUITE_GREEN`) so an illegal seed is caught in seconds, not after an hour of pytest. `SHA_FILE` renamed to `experiments/rerun_19_5_frozen_sha.txt`; the archive-summary pointer renamed to point at this plan's own SUMMARY (this file). `ARCHIVES_PRESENT`'s own check logic is untouched per the plan's explicit instruction -- see Known Issue below.
- `check_e6_seed_band`, `check_e5_seed_band`, `check_e2_band`, `check_e4_repeat` added to `check_rerun_gates.py` and wired into `run_all_gates`. Each reads status counts / seed counts / structural properties from the written artifact -- never a process exit code, per this project's standing MF-07 rule that E4/E6 record a failure as a row and still exit 0.
- `experiments/rerun_19_5.sh`: the single risk-first queue over five stages (`prelaunch_probe`, `e6_band`, `e4_repeat`, `e2_band`, `e5_band`). `prelaunch_probe` is the one stage whose failure is a hard abort; every other stage's gate is a finding. Dry-run (`RERUN_19_5_DRY_RUN=1`) completes all five stages in ~2s, leaves `git status --porcelain experiments/results/` empty, writes one `complete` line per stage in `STAGES` order, and a second dry-run invocation correctly SKIPs all five (resume logic confirmed). 7 `python -u` invocations; zero tree-mutating git commands anywhere in the script (`grep -n 'git commit\|git add\|git checkout\|git stash'` empty).
- Real (non-dry-run) gate-check path smoke-tested against the committed `experiments/results/` tree via the pinned `GATE_PYTHON` interpreter: 81 PASS, 11 N/A, 10 FAIL -- the pre-existing 81/7/10 baseline plus 4 new N/A entries (the four new band gates correctly report N/A since their stages have not run).

## Task Commits

1. **Task 1+2: legality_probe, prelaunch LEGALITY_PROBE check, and four new band gate functions** - `b3442bc` (feat)
2. **[Rule 1 bug, found during Task 3] Deferred legality_probe's aquacal imports to call time** - `4885568` (fix)
3. **Task 3: rerun_19_5.sh -- the risk-first production queue** - `1de567a` (feat)

**Plan metadata:** (this commit, made by the orchestrator after merge -- this executor does not update STATE.md/ROADMAP.md per its instructions)

## Files Created/Modified

- `experiments/check_rerun_gates.py` - `legality_probe`, `check_e6_seed_band`, `check_e5_seed_band`, `check_e2_band`, `check_e4_repeat`, `_numeric_mismatches` helper; all four new gates wired into `run_all_gates`. `legality_probe`'s `aquacal`/`experiments.e4_benchmark_grid` imports are function-local (see Deviations).
- `experiments/prelaunch_gate.sh` - Sixth check `LEGALITY_PROBE` inserted before `SUITE_GREEN` (renumbered 1-6); `SHA_FILE`/archive-summary variable renamed for 19.5; closing message's runtime estimate updated to reference the 26h ceiling.
- `experiments/rerun_19_5.sh` (new) - The five-stage risk-first queue, modeled on `rerun_19_4.sh`'s structure (state machine, `run_gate_check`, `_dry_run_active`/`_dry_run_stub`, pinned `GATE_PYTHON`, ABORT PROTOCOL header).
- `tests/unit/test_rerun_gates.py` - 25 new tests (49 total): `TestLegalityProbe` (3), `TestCheckE6SeedBand` (5), `TestCheckE5SeedBand` (4), `TestCheckE2Band` (5), `TestCheckE4Repeat` (4), plus import additions.

## Decisions Made

- Combined Tasks 1 and 2 into one commit: both touch `check_rerun_gates.py`'s shared import block (`math`/`re` for Task 2's E2 numeric comparison, `Sequence` for Task 1's `legality_probe` signature), so splitting the diff would have required reconstructing an artificial partial state with no independent verification value -- the same reasoning plan 19.5-08 used for its own two tasks.
- `check_e2_band`'s `band_dir` parameter is derived in `run_all_gates` as `out_dir.parent / "results_e2_band"` rather than being a fixed constant, so the aggregator stays a single-argument CLI (`check_rerun_gates.py <out_dir>`) while still locating E2's isolated band directory.
- `rerun_19_5.sh`'s E2 band directory is `experiments/results_e2_band/` (in-repo), not the Desktop path plan 07's SUMMARY illustrated -- both are legal (the write refusal only blocks the release tree itself), and the in-repo choice keeps `check_e2_band`'s wiring uniform with the other three gates.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `legality_probe`'s module-level imports broke the existing bare-script gate invocation**
- **Found during:** Task 3, while dry-running `rerun_19_5.sh`'s own `run_gate_check` step (which invokes `check_rerun_gates.py` as a bare script, not via `-m`).
- **Issue:** Task 1's `from experiments.e4_benchmark_grid import (...)` was placed at module level. When `check_rerun_gates.py` is invoked as `python experiments/check_rerun_gates.py <out_dir>` (the exact pattern both `rerun_19_4.sh` and `rerun_19_5.sh`'s `run_gate_check` use), Python puts only the script's own directory on `sys.path`, not the repo root -- so `import experiments.e4_benchmark_grid` failed with `ModuleNotFoundError: No module named 'experiments'` for EVERY gate check, not just `legality_probe`'s own callers.
- **Fix:** Moved the `aquacal.datasets.synthetic`/`experiments.e4_benchmark_grid` imports inside `legality_probe` itself (its only caller), which is always invoked from a repo-root cwd context (the heredocs in `prelaunch_gate.sh` and `rerun_19_5.sh`, and the test file) where the import resolves correctly.
- **Files modified:** `experiments/check_rerun_gates.py`
- **Verification:** `tests/unit/test_rerun_gates.py` 49/49 still pass; the bare-script invocation against the committed `experiments/results/` tree now succeeds end to end (81 PASS, 11 N/A, 10 FAIL).
- **Committed in:** `4885568`

---

**Total deviations:** 1 auto-fixed (1 bug). No scope creep.
**Impact on plan:** The fix is purely internal to `check_rerun_gates.py`'s import placement; `legality_probe`'s public behavior, signature, and all 30 PASS verdicts are unchanged.

## Issues Encountered

**Known caveat in `prelaunch_gate.sh`'s `ARCHIVES_PRESENT` check (5th of 6), surfaced for plan 10's Task 1.** Per the plan's explicit instruction ("Keep every other check's logic unchanged"), `ARCHIVES_PRESENT`'s grep-for-archive-directories logic was left byte-identical to 19.4's version -- only the `PLAN03_SUMMARY` pointer was renamed, now pointing at this plan's own SUMMARY (this file). **Phase 19.5 archived nothing**: D-19.5-03 keeps every `src/` change inert this phase (COV-02's diagnostic work, if any, uses the shipped E3-style separate-function pattern rather than touching a hot path), so there is no `experiments/archive/eN-*-pre-interface-fix` set for this phase's grep pattern to find. **When plan 10 actually runs `prelaunch_gate.sh` for real, `ARCHIVES_PRESENT` is expected to FAIL** with "no archive directories could be parsed out of [this SUMMARY]" -- that FAIL is EXPECTED given 19.5's scope, not evidence of a real defect, and plan 10's Task 1 should treat it as such when presenting the six gate verdicts to the user for approval (rather than treating any FAIL as an automatic stop, per the checkpoint's own "Any FAIL in the prelaunch gate is a stop" instruction -- this specific, pre-documented FAIL is the one exception worth discussing with the user before treating it as blocking).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Plan 10's exact detached launch command:** `nohup bash experiments/rerun_19_5.sh > experiments/rerun_19_5.log 2>&1 & disown` -- run by the orchestrator, never a subagent, never `run_in_background`.
- **Dry-run wall time:** ~2 seconds for all five stages combined (well under the plan's 120s acceptance ceiling), confirming the queue's control flow is sound before any real solve is attempted.
- **legality_probe result:** all 30 `(seed, n_cameras, draw)` combinations over seeds 42-46 x `n_cameras` {8,12,16} PASS. The 19.4 clearance-floor fix is re-verified empirically -- the seed list does not need to change.
- **Runtime budget restated in the script's own header:** ~15 h nominal, ~24 h at 19.4's observed 1.6x overrun, **26 h proposed ceiling**. If exceeded, the orchestrator should stop after the currently-running stage completes.
- **Plan 10's Task 1 checkpoint should expect `ARCHIVES_PRESENT` to FAIL** for the documented, expected reason above (no archiving occurred this phase) -- present this to the user explicitly rather than treating it as an automatic block, and confirm the other five checks (especially `LEGALITY_PROBE`) all PASS before approving the 26-hour spend.
- No blockers for 19.5-10. The queue script, all five stage functions, and all gate functions are complete, tested, and dry-run-verified.

---
*Phase: 19.5-experiment-coverage-and-uncertainty-bands*
*Completed: 2026-08-06*
