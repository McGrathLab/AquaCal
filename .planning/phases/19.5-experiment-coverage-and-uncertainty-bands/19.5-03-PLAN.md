---
phase: 19.5-experiment-coverage-and-uncertainty-bands
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/e7_focal_standoff_analysis.py
  - tests/unit/test_e7_focal_standoff.py
  - experiments/results/e7_focal_standoff.csv
autonomous: true
requirements: [COV-08]

must_haves:
  truths:
    - "The focal/standoff degeneracy WP6 planned and MF-05 never reported is analyzed across all ten committed seeds and all four arms"
    - "The analysis is paired by seed, not a comparison of marginal spreads"
    - "A null result — no clean degeneracy signature in the refined arm — is recorded as the answer, not retried"
  artifacts:
    - path: "experiments/results/e7_focal_standoff.csv"
      provides: "Per-arm focal-drift vs standoff association across the ten committed seeds, with n stated"
      contains: "arm"
    - path: "experiments/e7_focal_standoff_analysis.py"
      provides: "Pure analysis functions over interface_ablation_band.csv, plus a five-flag CLI"
  key_links:
    - from: "experiments/e7_focal_standoff_analysis.py"
      to: "experiments/results/interface_ablation_band.csv"
      via: "read-only pandas load of the committed band"
      pattern: "interface_ablation_band"
---

<objective>
COV-08, first half — the zero-runtime E7 re-analysis. `interface_ablation_band.csv` already carries
`focal_drift_pct` and `standoff_m` per camera, per arm, per seed across ten seeds. WP6 planned the
focal/standoff pairing as the refine-ON deliverable and it was never reported; MF-05 analyzed only
`camera_height_drift_mm`.

Purpose: the degeneracy argument the paper makes at L149 has a column of evidence sitting
uninspected in a committed artifact.

Output: `experiments/results/e7_focal_standoff.csv` and the pure functions that build it. No solve,
no queue, no new calibration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**You must NOT run:** `python -m pytest tests/` (the full suite), `pytest -m "not slow"`, any
`experiments/rerun_*.sh`, or E7 itself. This plan reads a committed CSV. It never regenerates one.
The orchestrator owns the suite and the production queue.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/MANUSCRIPT-FINDINGS.md
@.planning/phases/19.5-experiment-coverage-and-uncertainty-bands/19.5-CONTEXT.md
@.planning/phases/19.5-experiment-coverage-and-uncertainty-bands/19.5-PATTERNS.md

<interfaces>
Input, already committed at `0ffbe15` — `experiments/results/interface_ablation_band.csv`,
481 lines (480 data rows = 10 seeds x 4 arms x 12 cameras). Header, verbatim:

```
arm,shared_interface,refine_intrinsics,intrinsics_source,camera,water_z_recovered_m,
water_z_gt_m,water_z_error_mm,camera_height_recovered_m,camera_height_gt_m,
camera_height_drift_mm,surface_height_sum_m,focal_length_recovered_px,focal_length_gt_px,
focal_drift_pct,standoff_m,reprojection_rms_px_control,seed
```
The four `arm` values are the shared/per-camera x fixed/refined cross
(`e7_interface_ablation.py`'s `BAND_KEY_COLUMNS = ["seed", "arm", "camera"]`).

Analysis precedent to follow —
`.planning/phases/19.2-experiment-execution-and-provenance/analyze_e7_spread.py` (60 lines).
It computes a **paired per-seed difference, a sign test, and a between-arm correlation**, and
explicitly rejects comparing marginal spreads because both arms are evaluated on the same seed's
scenario. Read it before writing anything; the same reasoning binds here.

MF-05's own accounting of what a ten-seed sign test can support, including that its p-values are
one-sided (0.00098 = 2^-10) and that the `refined` arm clears nothing at 0.05 two-sided.
</interfaces>
</context>

<design_decision>
**Placement: `experiments/`, not `.planning/phases/19.5-.../`.** PATTERNS.md flags this as an
unresolved discretion point, and the precedent (`analyze_e7_spread.py`) lives under `.planning/`.
The deciding consideration is COV-09: every band must name a **citable committed artifact**. A
`.planning/` one-off prints a report and leaves nothing to cite, and it sits outside the gate
scripts that check provenance. Both COV-08 analyses therefore live in `experiments/` and write into
`experiments/results/`.
</design_decision>

<tasks>

<task type="auto">
  <name>Task 1: Paired focal/standoff association functions and their tests</name>
  <files>experiments/e7_focal_standoff_analysis.py, tests/unit/test_e7_focal_standoff.py</files>
  <read_first>
    - .planning/phases/19.2-experiment-execution-and-provenance/analyze_e7_spread.py (all 60
      lines — the paired-statistics reasoning and its explicit rejection of marginal comparison)
    - experiments/results/interface_ablation_band.csv (the header and a dozen rows)
    - .planning/MANUSCRIPT-FINDINGS.md § MF-05 (what the ten-seed band already supports, and the
      one-sided p-value convention)
    - tests/unit/test_seed_band_io.py (the pure-function unit-test shape used here)
  </read_first>
  <action>
    Create `experiments/e7_focal_standoff_analysis.py` with pure functions taking an already-loaded
    DataFrame — no file paths inside the analysis functions, so the tests can pass tiny hand-built
    frames:

    - `focal_standoff_association(df, arm) -> dict` — for one arm, the per-seed association
      between `focal_drift_pct` and `standoff_m` across the twelve cameras. Return the per-seed
      correlation coefficients, their sign counts, `n_seeds`, and a one-sided sign-test p-value
      computed as `2 ** -n_seeds` when all signs agree and the exact binomial tail otherwise.
      Label the p-value field `p_one_sided` so the convention is unambiguous in the artifact
      (MF-05's p-values were one-sided and this caused confusion once already).
    - `paired_arm_difference(df, arm_a, arm_b, column) -> dict` — the per-seed paired difference
      in `column` between two arms, since both arms are evaluated on the same seed's scenario.
      Never compute or return a marginal spread comparison.
    - `degeneracy_verdict(association: dict, alpha: float = 0.05) -> str` — returns one of
      `"signature_present"`, `"no_signature"`, `"underpowered"`. `"no_signature"` is a valid,
      final answer.

    Add `tests/unit/test_e7_focal_standoff.py` covering all three.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_e7_focal_standoff.py -v</automated>
  </verify>
  <acceptance_criteria>
    - No analysis function opens a file; every one takes a DataFrame.
    - A test builds a frame where every seed's correlation is negative and asserts
      `p_one_sided == 2 ** -n_seeds` exactly.
    - A test builds a frame with mixed signs and asserts `p_one_sided > 0.05` and
      `degeneracy_verdict(...) != "signature_present"`.
    - A test asserts `paired_arm_difference` raises or reports explicitly when the two arms do
      not share an identical seed set — pairing must not silently degrade to an unpaired
      comparison.
    - A test asserts a single-seed input yields `degeneracy_verdict(...) == "underpowered"`.
    - The named pytest command exits 0 in under 30 s.
  </acceptance_criteria>
  <done>The statistics are pinned by tests before they are pointed at the real band.</done>
</task>

<task type="auto">
  <name>Task 2: CLI, run against the committed band, write the artifact</name>
  <files>experiments/e7_focal_standoff_analysis.py, experiments/results/e7_focal_standoff.csv</files>
  <read_first>
    - experiments/e7_focal_standoff_analysis.py (your Task 1 functions)
    - experiments/_io.py — `build_experiment_arg_parser`, `validate_args`, `resolve_out_dir`,
      `write_experiment_csv`
    - experiments/e5_index_sensitivity.py lines 736-760 (the minimal five-flag parser shape)
    - experiments/results/interface_ablation_band.csv
  </read_first>
  <action>
    Give the script a `main`/`build_arg_parser` on the shared five-flag contract, reading
    `experiments/results/interface_ablation_band.csv` and writing
    `experiments/results/e7_focal_standoff.csv` via `write_experiment_csv` with
    `key_columns=["arm"]`.

    Columns: `arm`, `n_seeds`, `n_cameras_per_seed`, `mean_within_seed_correlation`,
    `n_seeds_negative`, `n_seeds_positive`, `p_one_sided`, `verdict`, `scope`.

    The `scope` field carries the D-19.5-05 sentence: this is a re-analysis of the ten committed
    seeds of `interface_ablation_band.csv` at the `"realistic"` scenario, and it bounds the
    focal/standoff association within that band only.

    Under D-19.5-07, if the `refined` arm returns `"no_signature"`, that is the result. Write it,
    report it, and stop. Do not add seeds, change the statistic, or re-run E7 to improve it.

    Run the script once and commit the artifact.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_e7_focal_standoff.py -v && python -u -m experiments.e7_focal_standoff_analysis --out /tmp/e7fs</automated>
  </verify>
  <acceptance_criteria>
    - `experiments/results/e7_focal_standoff.csv` exists with exactly four data rows (one per
      arm) and every column named above.
    - Every row has `n_seeds == 10` — the committed band's full seed set, not a subset.
    - Every row's `scope` field is non-empty and names `interface_ablation_band.csv`.
    - `git diff --stat experiments/results/interface_ablation_band.csv` produces no output — the
      input was read, never rewritten.
    - The `--out /tmp/e7fs` invocation exits 0 in under 60 s and leaves
      `git status --porcelain experiments/results/` reporting only the new file.
  </acceptance_criteria>
  <done>COV-08's E7 half has a citable committed artifact, whatever the verdict turns out to be.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Read-only analysis of the repository's own committed CSV |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.5-03-01 | Information Disclosure | Publishing an unpaired marginal comparison as if it were a paired result | mitigate | `paired_arm_difference` must reject non-identical seed sets; test asserts it |
| T-19.5-03-02 | Repudiation | A one-sided p-value read as two-sided downstream | mitigate | Field is named `p_one_sided`; the MF entry in plan 11 must repeat the convention |
| T-19.5-03-03 | Tampering | Overwriting the committed `interface_ablation_band.csv` | mitigate | Acceptance criterion asserts it is byte-unchanged |
| T-19.5-03-SC | Tampering | npm/pip/cargo installs | N/A | No packages installed |
</threat_model>

<verification>
- `python -m pytest tests/unit/test_e7_focal_standoff.py -v` exits 0.
- `experiments/results/e7_focal_standoff.csv` has four rows, all with `n_seeds == 10`.
- `interface_ablation_band.csv` is byte-unchanged.
</verification>

<success_criteria>
The focal/standoff pairing WP6 planned is measured and on the record with its n, its p-value
convention, and its scope — including the case where the answer is "no signature".
</success_criteria>

<output>
Create `.planning/phases/19.5-experiment-coverage-and-uncertainty-bands/19.5-03-SUMMARY.md` when done.
State the four verdicts explicitly. If `refined` is `"no_signature"`, say so as the finding.
</output>
