---
phase: 16-experiment-observability-hooks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aquacal/datasets/synthetic.py
  - tests/unit/test_datasets.py
  - tests/unit/test_synthetic_sweep_axes.py
autonomous: true
requirements: [HOOK-05, HOOK-06]

must_haves:
  truths:
    - "A sweep can generate detections at a refractive index other than 1.333 and the scenario records which index was used"
    - "A sweep can vary tank scale and working distance independently of each other"
    - "A sweep can vary camera layout (ring, grid, line) independently of scale and distance"
    - "Every generated scenario carries ground-truth board poses, per-camera water_z, and the seed that produced it"
  artifacts:
    - path: "src/aquacal/datasets/synthetic.py"
      provides: "n_air/n_water plumbed through detection generation; SyntheticScenario records n_air, n_water, seed"
      contains: "n_water"
    - path: "tests/unit/test_synthetic_sweep_axes.py"
      provides: "Executable audit of the WP5 sweep axes"
      min_lines: 80
  key_links:
    - from: "generate_synthetic_detections"
      to: "aquacal.core.interface_model.Interface"
      via: "n_air/n_water forwarded into the Interface constructor"
      pattern: "n_air=n_air"
    - from: "create_scenario"
      to: "SyntheticScenario"
      via: "records n_air, n_water, seed as ground truth fields"
      pattern: "seed=seed"
---

<objective>
Close the one real HOOK-05 gap (refractive index is not plumbed through detection
generation at all) and prove the remaining WP5 sweep axes with an executable audit rather
than an assertion in prose. Also record the generating seed on `SyntheticScenario`, which is
the synthetic half of HOOK-06.

Purpose: WP4/WP5 need to calibrate at n=1.333 and evaluate against ground truth generated at
a different n. Today `generate_synthetic_detections` hardcodes `Interface`'s defaults, so
that experiment cannot be run at all. The other two sweep axes were audited as already
working — this plan pins them with tests so they cannot silently regress.
Output: index-sweepable synthetic generation, self-describing scenarios, and a sweep-axis
audit test file.
</objective>

<execution_context>
@C:/Users/tucke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tucke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/code-style.md
@.claude/rules/source-code.md
@.planning/phases/16-experiment-observability-hooks/16-CONTEXT.md
@.planning/phases/16-experiment-observability-hooks/16-RESEARCH.md
@src/aquacal/datasets/synthetic.py
@src/aquacal/datasets/__init__.py
@tests/unit/test_datasets.py

<interfaces>
Current signatures the executor is changing. Do not go hunting for these.

From `src/aquacal/core/interface_model.py` — the constructor already supports the indices;
only the caller fails to pass them:
```python
class Interface:
    def __init__(
        self,
        normal: Vec3,
        camera_distances: dict[str, float],
        n_air: float = 1.0,
        n_water: float = 1.333,
    ): ...
```

From `src/aquacal/datasets/synthetic.py`:
```python
@dataclass
class SyntheticScenario:
    name: str
    board_config: BoardConfig
    intrinsics: dict[str, CameraIntrinsics]
    extrinsics: dict[str, CameraExtrinsics]
    water_zs: dict[str, float]
    board_poses: list[BoardPose]
    noise_std: float
    description: str
    images: dict[str, dict[int, NDArray]] | None = None

def generate_synthetic_detections(
    intrinsics, extrinsics, water_zs, board, board_poses,
    noise_std: float = 0.0, min_corners: int = 8, seed: int = 42,
) -> DetectionResult: ...

def generate_camera_array(n_cameras, layout, spacing, height_above_water,
                          height_variation, seed=42) -> tuple[intrinsics, extrinsics, distances]
def generate_board_trajectory(n_frames, camera_positions, water_zs,
                              depth_range, xy_extent, seed=42) -> list[BoardPose]
def generate_dense_xy_grid(..., depth, xy_extent, seed=42) -> list[BoardPose]
def create_scenario(name: str, seed: int = 42) -> SyntheticScenario
```

The hardcoded construction to fix is at roughly `synthetic.py:496-499`:
```python
interface = Interface(
    normal=interface_normal,
    camera_distances={cam_name: water_zs[cam_name]},
)     # <-- no n_air / n_water passed, so always 1.0 / 1.333
```

`tests/synthetic/ground_truth.py` is a thin re-export shim over
`aquacal.datasets.synthetic` — it does NOT hold a second copy of these functions, so no
duplicate needs updating. Verify with `head -25 tests/synthetic/ground_truth.py` if unsure.
</interfaces>

<audit_findings>
RESEARCH.md §Q3 audited the WP5 sweep list against the code. Treat these as settled:

- **Layout** — genuinely implemented, not a stub. `generate_camera_array` builds real `ring`
  (`angles = np.linspace(0, 2*pi, n, endpoint=False)`, radius `spacing * n / (2*pi)`),
  `grid`, and `line` layouts. No new code needed; needs a test.
- **Tank scale vs. working distance** — already independently controllable at the generator-
  function level: `generate_camera_array(spacing=...)` sets rig/tank scale,
  `generate_board_trajectory(depth_range=..., xy_extent=...)` and
  `generate_dense_xy_grid(depth=...)` set working distance. They are separate parameters on
  separate functions. They are only coupled inside `create_scenario`'s three presets, which
  is fine — a sweep script calls the generators directly. No new code needed; needs a test.
- **Refractive index** — REAL GAP. Not plumbed anywhere in the call chain. This is the only
  place in HOOK-05 that requires production code changes.
- **Ground truth returned** — `SyntheticScenario` already carries `board_poses` and
  `water_zs`. Only `n_air`/`n_water`/`seed` are missing.
</audit_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plumb refractive index through detection generation and record it on the scenario</name>
  <files>src/aquacal/datasets/synthetic.py, tests/unit/test_datasets.py</files>
  <action>
1. Add `n_air: float = 1.0` and `n_water: float = 1.333` keyword parameters to
   `generate_synthetic_detections`, placed after `min_corners` and before `seed` so existing
   positional callers are unaffected. Forward them into the `Interface(...)` construction
   inside the per-camera loop (`Interface(normal=..., camera_distances=..., n_air=n_air,
   n_water=n_water)`). Document both in the Google-style docstring, and add a line to the
   docstring's summary explaining that generating at an index different from the one used
   for calibration is exactly the WP4 evaluation-under-perturbed-assumptions setup.

2. Add three fields to `SyntheticScenario`, all with defaults so no existing construction
   site breaks: `n_air: float = 1.0`, `n_water: float = 1.333`, `seed: int = 42`. Add them
   to the class docstring's Attributes block. They must be declared AFTER the existing
   required fields and alongside/after `images` to keep dataclass default ordering valid.

3. Add `n_air: float = 1.0, n_water: float = 1.333` parameters to `create_scenario` and pass
   them plus the incoming `seed` into every returned `SyntheticScenario(...)` literal (there
   is one per preset: `ideal`, `minimal`, `realistic`). `create_scenario` does not itself
   generate detections, so the indices are recorded as ground-truth metadata only — say so
   in the docstring, and note that the caller must pass the same values to
   `generate_synthetic_detections`.

**Zero-behavior-change requirement:** the defaults must reproduce today's values exactly
(1.0 / 1.333 / 42). Every existing call site must be untouched and still produce identical
detections.

Tests appended to `tests/unit/test_datasets.py`:
- `test_generate_detections_default_index_unchanged`: build a small scenario, generate
  detections with no index arguments and with explicit `n_air=1.0, n_water=1.333`; assert
  the two `DetectionResult`s are corner-for-corner identical
  (`np.testing.assert_array_equal` over each frame's corners). This is the regression guard.
- `test_generate_detections_index_changes_projection`: same scenario, generate at
  `n_water=1.50`; assert at least one shared corner's pixel coordinates differ by more than
  0.5 px from the 1.333 run. Proves the parameter actually reaches the projection and is not
  swallowed.
- `test_scenario_records_index_and_seed`: `create_scenario("ideal", seed=7, n_water=1.40)`
  returns a scenario with `seed == 7`, `n_water == 1.40`, `n_air == 1.0`.
- `test_scenario_defaults_backward_compatible`: `create_scenario("ideal")` has
  `n_water == 1.333`, `n_air == 1.0`, `seed == 42`.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_datasets.py -v</automated>
  </verify>
  <done>`generate_synthetic_detections(..., n_water=1.50)` produces measurably different pixel coordinates than the default; omitting the arguments produces bit-identical output to before the change; `SyntheticScenario` reports `n_air`, `n_water`, and `seed`.</done>
</task>

<task type="auto">
  <name>Task 2: Write the executable WP5 sweep-axis audit</name>
  <files>tests/unit/test_synthetic_sweep_axes.py</files>
  <action>
Create `tests/unit/test_synthetic_sweep_axes.py` with a module docstring stating this file
is the executable form of the HOOK-05 audit: it pins the WP5 sweep axes so a future
refactor cannot silently couple them.

Tests (all fast — use small camera counts and few frames, no optimization):

- `test_layout_axis_produces_distinct_geometries`: call `generate_camera_array` with
  `layout="ring"`, `"grid"`, `"line"` at fixed `n_cameras=6, spacing=0.1, seed=1`. Assert
  each returns 6 cameras and that the sorted camera-centre arrays differ between all three
  pairs (not merely a permutation — compare the sorted set of pairwise centre distances).
  Additionally assert the ring layout's centres are near-equidistant from their centroid
  (std of radii < 1% of the mean radius), proving it is a real ring, not a relabelled grid.
- `test_tank_scale_axis_is_independent`: `generate_camera_array` at `spacing=0.1` vs
  `spacing=0.4`, everything else fixed. Assert the mean pairwise camera-centre distance
  scales by roughly 4x (within 10%). Then build a board trajectory from BOTH camera sets
  using the SAME `depth_range` and assert the resulting board `tvec` z-values are
  statistically indistinguishable (compare means within 1 mm) — i.e. changing tank scale did
  not move the working distance.
- `test_working_distance_axis_is_independent`: fix the camera array; call
  `generate_board_trajectory` at `depth_range=(0.25, 0.45)` vs `(0.8, 1.0)`. Assert the mean
  board depth differs as expected while the camera extrinsics are byte-identical between the
  two calls — i.e. changing working distance did not move the rig.
- `test_refractive_index_axis_is_independent`: generate detections from one fixed scenario at
  `n_water=1.333` and `n_water=1.45`; assert detections differ, and assert the scenario's
  `extrinsics`, `water_zs`, and `board_poses` are unchanged between the two calls — i.e. the
  index axis moves only the optics.
- `test_scenario_carries_absolute_error_ground_truth`: for each preset name, assert the
  returned scenario exposes non-empty `board_poses`, a `water_zs` entry for every camera, and
  the `n_air`/`n_water`/`seed` fields — everything a sweep needs to compute absolute error.
- `test_seed_reproducibility_across_generators`: for `generate_camera_array`,
  `generate_board_trajectory`, `generate_dense_xy_grid`, and `generate_synthetic_detections`,
  assert same-seed calls are identical and different-seed calls differ. This is the HOOK-06
  synthetic-side proof.

Use `numpy.testing` assertions with explicit tolerances; no bare `assert x == y` on floats.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_synthetic_sweep_axes.py -v</automated>
  </verify>
  <done>All sweep-axis tests pass, demonstrating layout, tank scale, working distance, and refractive index each move only their own axis, and that every generator is seed-reproducible.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/unit/test_datasets.py tests/unit/test_synthetic_sweep_axes.py -v`
2. `python -m pytest tests/ -m "not slow" -q` — no regressions, especially in
   `tests/synthetic/` which consumes these generators through `ground_truth.py`.
3. `ruff check src/aquacal/datasets/synthetic.py && ruff format --check src/aquacal/datasets/synthetic.py`
</verification>

<success_criteria>
- `generate_synthetic_detections` accepts `n_air`/`n_water` and forwards them to `Interface`.
- Omitting those arguments reproduces pre-change detections bit-identically.
- `SyntheticScenario` records `n_air`, `n_water`, and `seed`.
- An executable audit proves layout, tank scale, working distance, and refractive index are
  independently controllable, and that all generators are seed-reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/16-experiment-observability-hooks/16-02-SUMMARY.md`.
State explicitly which parts of HOOK-05 required code (refractive index) and which were
confirmed already satisfied by test (layout, tank scale, working distance, ground truth).
</output>
