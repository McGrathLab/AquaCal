---
phase: 18-documentation-corrections-stage-model-reconciliation
plan: 07
type: execute
wave: 2
depends_on: ["18-02"]
files_modified:
  - src/aquacal/config/schema.py
  - src/aquacal/cli.py
  - src/aquacal/config/example_config.yaml
  - src/aquacal/calibration/refinement.py
  - src/aquacal/calibration/_optim_common.py
  - src/aquacal/calibration/_observability.py
  - src/aquacal/validation/conditioning.py
  - tests/synthetic/experiment_helpers.py
  - tests/synthetic/experiments.py
autonomous: true
requirements: [DOCS-06]

must_haves:
  truths:
    - "D-01: Module and schema docstrings and CLI config comments present the three-stage model, matching the machine keys plan 18-06 renamed"
    - "D-04: Intrinsic refinement is described as a mode/pass of Stage 3, never as a fourth stage, in every src/ docstring and config template"
    - "D-05: The pass is called Stage 3's second pass, with intrinsics unlocked in generated-config comments and module docstrings"
    - "The docstring stage-tag enumerations in _observability.py and conditioning.py list stage3_intrinsic_pass, matching what pipeline.py actually passes"
    - "A user running aquacal init gets a config template whose comments name no Stage 4"
    - "D-23: Auxiliary registration is named without a stage number in schema.py and the config template — never Stage 3b or Stage 4b"
    - "No behavioral change: every changed line is a docstring, comment, or comment-only YAML/template string"
  artifacts:
    - path: "src/aquacal/config/schema.py"
      provides: "CalibrationConfig field docs on the three-stage model"
      contains: "intrinsic pass"
    - path: "src/aquacal/cli.py"
      provides: "Generated-config comment strings on the three-stage model"
    - path: "src/aquacal/config/example_config.yaml"
      provides: "Shipped example config comments on the three-stage model"
  key_links:
    - from: "src/aquacal/_observability.py and src/aquacal/validation/conditioning.py docstrings"
      to: "the stage tags pipeline.py passes"
      via: "docstring enumeration of valid stage strings"
      pattern: "stage3_intrinsic_pass"
    - from: "src/aquacal/cli.py generated template"
      to: "src/aquacal/config/example_config.yaml"
      via: "both emit the same optimization-section comments and must agree"
      pattern: "refine_intrinsics"
---

<objective>
Bring the remaining `src/` docstrings, the `aquacal init` config template, the shipped example
config, and the synthetic experiment helpers onto the three-stage model (DOCS-06, code side,
part 2).

Purpose: Plan 18-06 renames the machine keys in `pipeline.py`. If the docstrings that ENUMERATE
those keys still say `"stage4"`, and the config template a user gets from `aquacal init` still
says `# Stage 4: refine focal lengths`, the rename is half-done and the two-vocabularies
problem persists in exactly the surfaces DOCS-06's requirement text names — "module and schema
docstrings, and CLI config comments". `refinement.py`'s module docstring literally opens with
"Stage 4 joint refinement", so a reader who opens the module meets the old model immediately.

Output: Docstring, comment, and template-string corrections across nine files. No numerics, no
control flow, no signature changes.

This plan is gated on plan 18-02's manuscript-vocabulary checkpoint. It runs in parallel with
plan 18-06 in the same wave — the two have zero file overlap by construction (18-06 owns
`pipeline.py` plus the six `tests/unit/` files that assert stage literals; this plan owns
everything else in `src/` plus the two `tests/synthetic/` helpers).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-CONTEXT.md
@.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-PATTERNS.md
@.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-02-SUMMARY.md

<interfaces>
<!-- Every remaining "Stage 4" / "stage4" site in src/ outside pipeline.py. -->
<!-- Confirmed by full-tree grep; this is the complete list. -->

src/aquacal/calibration/refinement.py:1     """Stage 4 joint refinement with optional intrinsics optimization.
src/aquacal/calibration/refinement.py:65    This is Stage 4 of the calibration pipeline. It takes the output of Stage 3
src/aquacal/calibration/_optim_common.py:5  used by both interface_estimation (Stage 3) and refinement (Stage 4).
src/aquacal/calibration/_observability.py:178   stage: Human-readable stage name (e.g. "stage3", "stage3_rerun", "stage4"),
src/aquacal/validation/conditioning.py:204      this report (e.g. "stage3", "stage4"), recorded in the JSON payload
src/aquacal/cli.py:602      "  # refine_intrinsics: false  # Stage 4: refine focal lengths and principal points",
src/aquacal/cli.py:603      "  # refine_auxiliary_intrinsics: false  # Stage 4b: refine auxiliary camera intrinsics",
src/aquacal/config/example_config.yaml:76   # max_calibration_frames: 150  # Max frames for Stage 3/4 optimization (null = no limit)
src/aquacal/config/example_config.yaml:78   # refine_intrinsics: false  # Stage 4: also optimize focal lengths and principal points
src/aquacal/config/example_config.yaml:80   # refine_auxiliary_intrinsics: false  # Stage 4b: also optimize auxiliary camera

<!-- schema.py CalibrationConfig docstring + inline comment, seven sites: -->
schema.py:236   max_calibration_frames: Maximum number of frames for Stages 3-4 optimization.
schema.py:242   refine_intrinsics: If True, Stage 4 jointly refines per-camera focal lengths
schema.py:245       Stage 3 converges reliably. Default False (Stage 4 skipped).
schema.py:256   auxiliary_cameras: ... excluded from joint Stage 3/4 optimization.
schema.py:261   refine_auxiliary_intrinsics: If True, Stage 4b refines auxiliary camera
schema.py:264       controls primary camera refinement in Stage 4). Distortion coefficients
schema.py:269   reject_outlier_frames: ... re-runs Stage 3 on the cleaned set before Stage 4.
schema.py:284   save_stage_calibrations: ... (Stage 3, the post-outlier-rejection re-run, and
                    Stage 4 when enabled) as loadable calibration JSON files
schema.py:328   refine_auxiliary_intrinsics: bool = False  # If True, Stage 4b refines ... in Stage 4)

<!-- tests/synthetic helper prose (not assertions — safe to reword): -->
tests/synthetic/experiment_helpers.py:52    refine_intrinsics: If True, run Stage 4 with intrinsic refinement
tests/synthetic/experiment_helpers.py:115   # Stage 4: Intrinsic refinement (if requested)
tests/synthetic/experiment_helpers.py:117   print("Stage 4: Intrinsic refinement...")
tests/synthetic/experiments.py:346          "Note: Distortion not refined in Stage 4 (all errors zero)",

<!-- Existing tests that touch these files' surfaces — must still pass: -->
tests/unit/test_cli.py:586      assert "refine_intrinsics" in raw_text     (key name only, not the comment)
tests/unit/test_schema.py:233   assert config.refine_intrinsics is False   (value only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the user-facing config surfaces — schema docstrings, aquacal init template, example config</name>
  <files>src/aquacal/config/schema.py, src/aquacal/cli.py, src/aquacal/config/example_config.yaml</files>

  <read_first>
    - `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-02-SUMMARY.md`
      — READ FIRST for the confirmed prose label. Default per D-05 (revised): "Stage 3's second
      pass, with intrinsics unlocked".
    - `src/aquacal/config/schema.py` lines 230-345 — the `CalibrationConfig` class docstring
      `Args:` block and the inline field comments; the nine stage sites interlock, since
      `refine_auxiliary_intrinsics`'s entry cross-references `refine_intrinsics`'s "Stage 4".
    - `src/aquacal/cli.py` lines 590-615 — the generated-config comment string list emitted by
      `aquacal init`.
    - `src/aquacal/config/example_config.yaml` lines 73-92 — the shipped `optimization:`
      section comments, which must stay consistent with what `aquacal init` emits.
    - `tests/unit/test_cli.py` around line 586 and `tests/unit/test_schema.py` around line 233 —
      confirm these assert on key names and values, not on the comment text being changed.
    - `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-PATTERNS.md`
      §5 — the schema field docstring style, including the `shared_interface` entry's shape.
  </read_first>

  <action>
    Rewrite every stage reference in the three user-facing config surfaces so intrinsic
    refinement reads as an optional pass of Stage 3 rather than a fourth stage (D-04, D-05).
    Change no default value, no field name, and no type.

    `src/aquacal/config/schema.py` — nine sites in the `CalibrationConfig` docstring and inline
    comments:
    - `max_calibration_frames`: "Maximum number of frames for Stages 3-4 optimization" becomes
      Stage 3 optimization (the intrinsic pass is part of Stage 3, so the range collapses).
    - `refine_intrinsics`: "If True, Stage 4 jointly refines..." and "Default False (Stage 4
      skipped)" both become Stage 3's second pass with intrinsics unlocked. Keep the substantive
      content: distortion coefficients are NOT refined, and only enable after Stage 3 converges
      reliably.
    - `auxiliary_cameras`: "excluded from joint Stage 3/4 optimization" becomes Stage 3.
    - `refine_auxiliary_intrinsics`: "If True, Stage 4b refines..." and "(which controls primary
      camera refinement in Stage 4)". Use the same "Auxiliary camera registration" label plan 18-06
      gives the pipeline console output — the paper gives this step no stage number at all
      (D-23), so do NOT write "Stage 3b" or "Stage 4b". Keep the two facts the entry carries: it requires
      `auxiliary_cameras`, and it is independent of `refine_intrinsics`.
    - `reject_outlier_frames`: "re-runs Stage 3 on the cleaned set before Stage 4" becomes
      before Stage 3's second pass.
    - `save_stage_calibrations`: "(Stage 3, the post-outlier-rejection re-run, and Stage 4 when
      enabled)" becomes the three tag names plan 18-06 actually emits — `stage3`,
      `stage3_rerun`, and `stage3_intrinsic_pass`. This is a user-facing description of files
      that appear on disk, so naming the literal tags is more useful than prose here.
    - The inline comment on the `refine_auxiliary_intrinsics: bool = False` field line (~328),
      which duplicates the docstring text.

    `src/aquacal/cli.py` (~lines 602-603) — the two generated-config comment strings emitted by
    `aquacal init`: `# Stage 4: refine focal lengths and principal points` and `# Stage 4b:
    refine auxiliary camera intrinsics`. These are what a new user's very first config file
    says, so they matter more than most docstrings.

    `src/aquacal/config/example_config.yaml` (~lines 76, 78, 80) — the matching three comments
    in the shipped `optimization:` section. `cli.py` and this file must say the same thing;
    they are two copies of the same guidance and drifting them apart is its own defect.

    Verify that `tests/unit/test_cli.py:586` (`assert "refine_intrinsics" in raw_text`) and
    `tests/unit/test_schema.py:233` still pass — they assert on the key name and the default
    value, neither of which changes here. If any other test asserts on the comment text, update
    it in this task.
  </action>

  <verify>
    <automated>python -m pytest tests/unit/test_cli.py tests/unit/test_schema.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE "Stage 4|Stage 4b|Stages 3-4|Stage 3/4" src/aquacal/config/schema.py` returns 0.
    - `grep -cE "Stage 4|Stage 4b" src/aquacal/cli.py` returns 0.
    - `grep -cE "Stage 4|Stage 4b|Stage 3/4" src/aquacal/config/example_config.yaml` returns 0.
    - `grep -c "stage3_intrinsic_pass" src/aquacal/config/schema.py` returns >= 1 (the
      `save_stage_calibrations` entry names the literal tags).
    - `grep -cE "Stage 3b|Stage 4b" src/aquacal/config/schema.py` returns 0 — auxiliary
      registration carries no stage number (D-23).
    - `grep -c "Auxiliary camera registration" src/aquacal/config/schema.py` returns >= 1 —
      the auxiliary label matches what plan 18-06 makes the pipeline print.
    - `grep -c "refine_intrinsics" src/aquacal/cli.py` is unchanged from its pre-task value —
      no config key was renamed.
    - `grep -c "refine_intrinsics: bool = False" src/aquacal/config/schema.py` returns 1 — the
      default is untouched.
    - `python -m pytest tests/unit/test_cli.py tests/unit/test_schema.py -q` exits 0.
    - `python -c "import yaml,pathlib; yaml.safe_load(pathlib.Path('src/aquacal/config/example_config.yaml').read_text())"` exits 0 — the YAML still parses.
    - `ruff check src/aquacal/config/schema.py src/aquacal/cli.py` exits 0.
    - `git diff src/aquacal/config/schema.py` shows no change to any field name, type
      annotation, or default value.
  </acceptance_criteria>

  <done>
    `schema.py`'s nine stage references, `cli.py`'s two generated-config comments, and
    `example_config.yaml`'s three comments all present the three-stage model, name auxiliary
    registration without a stage number, and enumerate the real stage tags where files on disk are
    described. No key, type, or default changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the remaining module and stage-tag docstrings</name>
  <files>src/aquacal/calibration/refinement.py, src/aquacal/calibration/_optim_common.py, src/aquacal/calibration/_observability.py, src/aquacal/validation/conditioning.py, tests/synthetic/experiment_helpers.py, tests/synthetic/experiments.py</files>

  <read_first>
    - `src/aquacal/calibration/refinement.py` lines 1-10 and 60-75 — the module docstring
      opening line and the `joint_refinement` docstring's "This is Stage 4 of the calibration
      pipeline" sentence.
    - `src/aquacal/calibration/_optim_common.py` lines 1-10 — the module docstring naming both
      consumers.
    - `src/aquacal/calibration/_observability.py` around line 178 — `OptimizerObserver`'s
      `stage:` Args entry enumerating valid stage names.
    - `src/aquacal/validation/conditioning.py` around line 204 — `save_conditioning_report`'s
      `stage:` Args entry, same pattern.
    - `src/aquacal/calibration/pipeline.py` — the stage tags plan 18-06 now passes, so the two
      enumerations match reality rather than an aspiration.
    - `tests/synthetic/experiment_helpers.py` lines 48-56 and 110-120, and
      `tests/synthetic/experiments.py` around line 346 — the four helper prose sites; confirm
      each is a docstring, comment, or print string rather than an assertion.
  </read_first>

  <action>
    Correct the six remaining `src/` stage references and the four `tests/synthetic/` helper
    prose sites. All are docstrings, comments, or print strings — no assertion depends on them.

    `src/aquacal/calibration/refinement.py`: the module docstring's opening line `"""Stage 4
    joint refinement with optional intrinsics optimization.` and `joint_refinement`'s "This is
    Stage 4 of the calibration pipeline. It takes the output of Stage 3". Both become
    Stage 3's second pass, with intrinsics unlocked. Keep the module NAME (`refinement.py`) and every
    function name unchanged — renaming a module is a behavioral change and is out of scope for
    this documentation-only phase.

    `src/aquacal/calibration/_optim_common.py`: the module docstring's `used by both
    interface_estimation (Stage 3) and refinement (Stage 4).` Both consumers are Stage 3 under
    the three-stage model, so distinguish them by what they optimize rather than by stage
    number.

    `src/aquacal/calibration/_observability.py` (~line 178): `stage: Human-readable stage name
    (e.g. "stage3", "stage3_rerun", "stage4"),`. Replace `"stage4"` with
    `"stage3_intrinsic_pass"` — this enumeration must match what `pipeline.py` actually passes
    after plan 18-06.

    `src/aquacal/validation/conditioning.py` (~line 204): `this report (e.g. "stage3",
    "stage4"), recorded in the JSON payload`. Same replacement.

    `tests/synthetic/experiment_helpers.py`: the `refine_intrinsics:` Args line "If True, run
    Stage 4 with intrinsic refinement", the section comment `# Stage 4: Intrinsic refinement (if
    requested)`, and the `print("Stage 4: Intrinsic refinement...")` string. Note these are
    experiment helpers rather than the library, but they are inside the repo and DOCS-06's sweep
    scope is `src/` plus `docs/` plus `README.md` — include them because leaving them creates
    the same mixed-vocabulary confusion for anyone reading the synthetic experiment code.

    `tests/synthetic/experiments.py` (~line 346): the note string "Note: Distortion not refined
    in Stage 4 (all errors zero)".

    Change no executable statement. Every changed line must be a docstring, comment, or a
    string literal that is printed rather than asserted on.
  </action>

  <verify>
    <automated>python -m pytest tests/ -m "not slow" -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rnE '"stage4"|Stage 4|Stage-4|stage 4' src/ --include=*.py --include=*.yaml`
      returns zero matches across the ENTIRE `src/` tree (this is the DOCS-06 code-side grep
      guard from 18-VALIDATION.md; it only passes once plan 18-06 has also landed, so run it
      after both plans merge and record the result in the summary).
    - `grep -cE "Stage 4" src/aquacal/calibration/refinement.py` returns 0.
    - `grep -cE "Stage 4" src/aquacal/calibration/_optim_common.py` returns 0.
    - `grep -c "stage3_intrinsic_pass" src/aquacal/calibration/_observability.py` returns >= 1.
    - `grep -c "stage3_intrinsic_pass" src/aquacal/validation/conditioning.py` returns >= 1.
    - `grep -c '"stage4"' src/aquacal/calibration/_observability.py src/aquacal/validation/conditioning.py` returns 0.
    - `grep -crE "Stage 4" tests/synthetic/` returns 0.
    - `grep -c "def joint_refinement" src/aquacal/calibration/refinement.py` returns 1 — the
      function name was not renamed.
    - `test -f src/aquacal/calibration/refinement.py` — the module was not renamed.
    - `python -m pytest tests/ -m "not slow"` exits 0.
    - `ruff check src/ tests/synthetic/` exits 0.
    - `git diff src/ tests/synthetic/` for this task shows only docstring, comment, and printed
      string-literal lines.
  </acceptance_criteria>

  <done>
    `refinement.py`, `_optim_common.py`, `_observability.py`, `conditioning.py`, and the two
    synthetic experiment helpers present the three-stage model; the two stage-tag enumerations
    list `stage3_intrinsic_pass`, matching what `pipeline.py` passes; no module or function was
    renamed and no executable line changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> new user's first config | `aquacal init`'s emitted comments are the first documentation most users read; stale stage language there propagates into user mental models and support questions |
| repo -> on-disk artifact expectations | The `stage:` docstring enumerations in `_observability.py` and `conditioning.py` tell a user which stage tags to expect in `internals/`; a stale enumeration is a false contract |
| repo -> python-semantic-release | Conventional commit type determines whether a release is cut |

Docstring, comment, and template-string edits only. No network surface, no user input reaching
changed code, no new dependencies, no new executable path.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07-DRIFT | Tampering (of a documented contract) | `stage:` enumerations in `_observability.py` / `conditioning.py` versus what `pipeline.py` passes | mitigate | Both enumerations are updated to `stage3_intrinsic_pass`, and the acceptance criteria assert both the presence of the new tag and the absence of `"stage4"` in those two files |
| T-18-07-SPLITCOPY | Tampering (divergent duplicates) | `cli.py` template comments versus `example_config.yaml` comments | mitigate | Both are edited in the SAME task with matching wording, and each has its own returns-0 grep criterion |
| T-18-07-BEHAV | Tampering (unintended behavior change) | `schema.py` field definitions | mitigate | Criteria pin `refine_intrinsics: bool = False` unchanged, `def joint_refinement` unchanged, `refinement.py` not renamed, the YAML still parsing, and a diff confined to comment/docstring lines |
| T-18-07-REL | Tampering (release metadata) | conventional commit type | mitigate | Commit as `docs:` — never `feat:`/`fix:`, which would cut an unintended python-semantic-release version on the next push to `main` |
| T-18-07-SC | Tampering (supply chain) | package installs | accept | No packages installed. Not applicable per 18-RESEARCH.md § Package Legitimacy Audit |
</threat_model>

<verification>
1. `grep -rnE '"stage4"|Stage 4|Stage 4b|Stages 3-4|Stage 3/4' src/ --include=*.py --include=*.yaml`
   returns zero matches (run after plan 18-06 has also merged).
2. `grep -crE "Stage 4" tests/synthetic/` returns 0.
3. `python -m pytest tests/ -m "not slow"` exits 0.
4. `python -m pytest tests/` (full suite) exits 0.
5. `ruff check src/ tests/synthetic/` exits 0.
6. `python -c "import yaml,pathlib; yaml.safe_load(pathlib.Path('src/aquacal/config/example_config.yaml').read_text())"` exits 0.
7. `git log --oneline -3` shows `docs(18-07):` subjects only — no `feat:` or `fix:`.
</verification>

<success_criteria>
- ROADMAP Phase 18 success criterion 5 is satisfied on the remaining code surfaces: module and
  schema docstrings and CLI config comments all present the three-stage model.
- The `stage:` enumerations match the tags `pipeline.py` actually passes after plan 18-06.
- `aquacal init` emits a config template with no Stage 4 reference, matching the shipped
  `example_config.yaml`.
- No module, function, field, type, or default changed.
</success_criteria>

<output>
Create `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-07-SUMMARY.md` when done
</output>
