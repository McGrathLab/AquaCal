---
phase: 18-documentation-corrections-stage-model-reconciliation
plan: 08
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05", "18-06", "18-07"]
files_modified:
  - docs/guide/optimizer.md
  - docs/guide/glossary.md
  - docs/guide/index.md
  - docs/guide/troubleshooting.md
  - docs/api/calibration.rst
  - docs/api/index.rst
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [DOCS-02, DOCS-06]

must_haves:
  truths:
    - "D-04: Every docs page presents three stages, with intrinsic refinement as an optional pass of Stage 3 rather than a fourth stage"
    - "D-05: The ex-Stage-4 is called Stage 3's second pass, with intrinsics unlocked in prose"
    - "D-08: docs/guide/optimizer.md's loss section names huber AND prints the Huber rho, not the soft-L1 formula, with loss_scale (1.0 px) as the transition point"
    - "D-16: docs/guide/index.md's 'Four-stage calibration pipeline' line is corrected, completing the edit whose first half plan 18-03 made"
    - "D-17: The sweep covers src/, docs/, and README.md as a grep-and-verify pass, not only the enumerated line numbers"
    - "D-18: .planning/ and CHANGELOG.md are untouched historical records"
    - "D-19: CLAUDE.md's four-stage line is corrected locally but never staged or reported as committed, because .gitignore:216 ignores it"
    - "The correct 'Why BFS?' paragraph body, the correct '98% sparse' claim, and the genuinely-BFS _find_connected_components all survive the sweep"
  artifacts:
    - path: "docs/guide/optimizer.md"
      provides: "Three-stage pipeline overview, mermaid diagram, folded intrinsic-pass section, and the Huber loss formula"
      contains: "huber"
    - path: "docs/guide/glossary.md"
      provides: "Corrected traversal term and three-stage references"
    - path: "docs/api/calibration.rst"
      provides: "Three-stage API section structure"
  key_links:
    - from: "docs/guide/optimizer.md loss section"
      to: "src/aquacal/config/schema.py robust_loss default"
      via: "documented default matches the dataclass field initializer"
      pattern: "huber"
    - from: "docs/guide/index.md"
      to: "docs/guide/optimizer.md"
      via: "Practical Guides bullet describing the pipeline stage count"
      pattern: "Three-stage|three-stage"
---

<objective>
Complete the documentation-side halves of DOCS-02 and DOCS-06: the three-stage model across
every guide and API page, the BFS -> best-first terminology in the remaining doc sites, and the
loss-function correction that currently prints a soft-L1 formula under a Huber-defaulted
optimizer.

Purpose: A reviewer following the paper's link to these docs currently meets a different
architecture than the one they just read — four stages where the paper describes three — and a
loss equation that does not match the code's default. `docs/guide/optimizer.md:120` labels the
loss "Soft-L1 (Huber-like)" and then prints the soft-L1 rho, while `config/schema.py:314` sets
`robust_loss: str = "huber"`; changing only the word would leave a wrong equation on the page,
which is why D-08 requires fixing both.

Output: Corrected prose across six tracked files plus one local-only edit.

This is the last wave. It runs after every plan that owns a shared file — 18-01 (optimizer.md
numbers), 18-03 (index.md registration, troubleshooting cross-links), 18-04 (optimizer.md image
reference, glossary bipartite definition) — and after the plan 18-02 checkpoint that confirms
the vocabulary. It also carries the phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-CONTEXT.md
@.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-RESEARCH.md
@.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-PATTERNS.md
@.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-02-SUMMARY.md

<interfaces>
<!-- Complete four-stage / Stage-4 / BFS inventory in docs/, README.md, CLAUDE.md.  -->
<!-- Verified by full-tree grep excluding docs/_build/. This is the sweep floor.    -->

docs/guide/optimizer.md:3     "runs four sequential stages"
docs/guide/optimizer.md:7     "The calibration proceeds through four stages:"
docs/guide/optimizer.md:15    S2["**Stage 2**<br/>Extrinsic Init<br/><small>(BFS/PnP)</small>"]
docs/guide/optimizer.md:17    S4["**Stage 4**<br/>Intrinsic Refinement<br/><small>(optional)</small>"]
docs/guide/optimizer.md:21-22 S3 --> S4  /  S4 --> O["Refined K, R, t<br/>water_z, boards"]
docs/guide/optimizer.md:26    class S1,S2,S3,S4 stage
docs/guide/optimizer.md:30    "Stages 3 and 4 use the same optimization infrastructure"
docs/guide/optimizer.md:57    "3. **BFS traversal**: Starting from the reference camera ..."
docs/guide/optimizer.md:71    "**Why BFS?**"          <- HEADING WRONG, PARAGRAPH BODY CORRECT
docs/guide/optimizer.md:96    "- **intrinsics (0)**: Not optimized in Stage 3 (deferred to Stage 4 if requested)."
docs/guide/optimizer.md:120-126  "**Loss function:** Soft-L1 (Huber-like) ..." + soft-L1 rho
docs/guide/optimizer.md:151    "## Stage 4: Intrinsic Refinement (Optional)"
docs/guide/optimizer.md:164-165 "- **Use Stage 4** if ... / - **Skip Stage 4** if ..."
docs/guide/optimizer.md:358    "- Validation errors in Stage 3/4 much higher than training errors"

docs/guide/glossary.md:7-8    "Auxiliary camera ... Stages 2-4"
docs/guide/glossary.md:35     "... Used in Stage 2 for extrinsic initialization via BFS traversal."
docs/guide/glossary.md:70     "## See Also ... The four calibration stages"

docs/guide/index.md:9         "- [Optimizer Pipeline](optimizer.md) — Four-stage calibration pipeline, ..."

docs/guide/troubleshooting.md:7,61,64,106,117,172,202,263,271   "Stage 3/4"   (9 occurrences)
docs/guide/troubleshooting.md:72                                "Stage 3 or 4 fails to converge"

docs/api/calibration.rst:8    "implements a four-stage pipeline for refractive multi-camera calibration."
docs/api/calibration.rst:38   "Stage 4: Refinement"  (section heading over automodule aquacal.calibration.refinement)
docs/api/index.rst:9          "- **calibration**: Four-stage calibration pipeline (intrinsics, extrinsics, interface estimation, refinement)"

README.md:10                  "- **Multi-camera pose graph** — BFS-based extrinsic initialization for camera arrays"

CLAUDE.md:45                  "Four-stage calibration pipeline. See `.planning/architecture.md` ..."   <- GITIGNORED, D-19

<!-- Loss ground truth (D-08): -->
src/aquacal/config/schema.py:314   robust_loss: str = "huber"   # "huber", "soft_l1", "linear"
src/aquacal/config/schema.py:315   loss_scale: float = 1.0      # pixels
src/aquacal/calibration/interface_estimation.py:134   second confirmation site
<!-- scipy.optimize.least_squares 'huber': rho(z) = z if z <= 1 else 2*sqrt(z) - 1, -->
<!-- with z = (f / f_scale)**2 and f_scale = loss_scale. Quadratic below the        -->
<!-- transition point, linear above it.                                             -->

<!-- DO NOT TOUCH (three correct things a blind sweep would break): -->
<!--  1. optimizer.md:71's paragraph BODY (non-overlapping-FOV rationale) — correct -->
<!--  2. optimizer.md's "98% sparse" claim — correct, 13/673 = 1.9% nonzero         -->
<!--  3. src/aquacal/calibration/extrinsics.py:200,208 — genuinely FIFO BFS (D-11)  -->
<!--  4. .planning/ and CHANGELOG.md — historical records (D-18)                    -->
<!--  5. src/aquacal.egg-info/PKG-INFO — build artifact derived from README.md      -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite optimizer.md to the three-stage model and fix the loss formula</name>
  <files>docs/guide/optimizer.md</files>

  <read_first>
    - `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-02-SUMMARY.md`
      — READ FIRST for the confirmed stage labels and traversal term. If the checkpoint returned
      "four stages / BFS", this task is re-scoped to the loss fix alone.
    - `docs/guide/optimizer.md` — all 374 lines. This is a full-page restructure, not a
      find-and-replace, so read the whole page before editing.
    - `src/aquacal/config/schema.py` lines 310-320 — the `robust_loss` and `loss_scale`
      defaults, the ground truth for D-08.
    - `src/aquacal/calibration/interface_estimation.py` around line 134 — the second site
      confirming `huber` is the default actually passed to `least_squares`.
    - `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-PATTERNS.md`
      §6 — the mermaid block, the loss block, and the Stage 4 section quoted verbatim.
    - `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-CONTEXT.md`
      § Specific Ideas — why the `Why BFS?` paragraph body must survive and the "98% sparse"
      claim must not be swept.
  </read_first>

  <action>
    Restructure `docs/guide/optimizer.md` onto the three-stage model and fix the loss section.
    Plans 18-01 and 18-04 already edited this file (the numeric section and the figure
    reference); do not revert either.

    Stage-count prose. Line 3's "runs four sequential stages" and line 7's "The calibration
    proceeds through four stages:" become three. Line 30's "Stages 3 and 4 use the same
    optimization infrastructure" becomes a statement that Stage 3 and its optional second
    pass share the infrastructure.

    Mermaid diagram (lines 9-28). Remove the `S4["**Stage 4**<br/>Intrinsic Refinement..."]`
    node and the `S3 --> S4` / `S4 --> O` edges; wire `S3 --> O` directly and indicate the
    optional second pass as part of Stage 3 — e.g. by extending the `S3` node's own label
    with a `<small>(+ optional second pass, intrinsics unlocked)</small>` line. Update the `class S1,S2,S3,S4
    stage` line to drop `S4`. Verify the diagram still renders in the Sphinx build; a dangling
    node reference in a mermaid block is a silent render failure, so check the built HTML.

    Stage 4 section (lines 151-171). The heading `## Stage 4: Intrinsic Refinement (Optional)`
    becomes a subsection of Stage 3 — demote it to `###` under the Stage 3 section and title it
    with the D-05 wording ("Stage 3's second pass, with intrinsics unlocked" or the label confirmed by
    18-02-SUMMARY.md). Move it physically so it sits inside the Stage 3 section rather than
    after it. Its body content is correct and should be preserved: the appended
    `intrinsics(4*N)` parameter block, the when-to-use guidance, the note that distortion
    coefficients are always held fixed, and the `{func}` cross-reference to
    `joint_refinement`. Rewrite its "**Use Stage 4** / **Skip Stage 4**" bullets in terms of
    enabling or skipping the intrinsic pass.

    Line 96's "- **intrinsics (0)**: Not optimized in Stage 3 (deferred to Stage 4 if
    requested)." becomes deferred to Stage 3's second pass. Line 358's "Validation errors
    in Stage 3/4 much higher than training errors" collapses to Stage 3.

    Loss function (lines 120-126) — D-08 requires fixing BOTH the name and the formula. The page
    currently reads "**Loss function:** Soft-L1 (Huber-like) loss for robustness to outliers"
    followed by the soft-L1 rho. The code default is `robust_loss = "huber"` with
    `loss_scale = 1.0` pixels. Replace the label with Huber, replace the displayed equation with
    the Huber rho in scipy's `least_squares` convention (quadratic in the residual below the
    transition point, linear above it, where the transition is at `loss_scale` = 1.0 px), and
    state `loss_scale` explicitly as that transition point in pixels. Verify the rendered
    equation against scipy's documented `huber` definition before committing — the whole point
    of this edit is that a plausible-looking wrong equation survived here once already. Keep the
    existing closing sentence about down-weighting large residuals while preserving gradient
    information; it is true of Huber too. Mention that `soft_l1` and `linear` remain selectable
    via the `optimization.robust_loss` config key, cross-linking the new
    `docs/guide/configuration.md` page created by plan 18-03.

    Terminology (DOCS-02). Line 15's mermaid label `<small>(BFS/PnP)</small>` and line 57's
    `3. **BFS traversal**:` bullet take the confirmed traversal term. Line 71's `**Why BFS?**`
    heading takes the term too — but its PARAGRAPH BODY is factually correct (it explains the
    non-overlapping-FOV rationale for chaining through the board) and must survive verbatim.
    Only the heading word changes.

    Do NOT touch the corrected numeric section from plan 18-01 (13-17 columns, 673/675/727,
    43-52x, 98% sparse) or the image directive from plan 18-04.
  </action>

  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && ! grep -qiE "four sequential stages|four stages|Stage 4" docs/guide/optimizer.md && grep -q "98% sparse" docs/guide/optimizer.md && grep -q "673" docs/guide/optimizer.md && grep -qi huber docs/guide/optimizer.md && sphinx-build -W --keep-going -b html docs docs/_build/html && echo OPT_OK'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -ciE "four sequential stages|four stages|Stage 4|S4\[" docs/guide/optimizer.md` returns 0.
    - `grep -c "class S1,S2,S3 stage" docs/guide/optimizer.md` returns 1 (the mermaid class line
      no longer lists S4).
    - `grep -ci "huber" docs/guide/optimizer.md` returns >= 1.
    - `grep -c "sqrt{1 + r^2}" docs/guide/optimizer.md` returns 0 — the soft-L1 formula is gone.
    - `grep -c "loss_scale" docs/guide/optimizer.md` returns >= 1 and the surrounding text names
      1.0 px as the transition point.
    - `grep -c "98% sparse" docs/guide/optimizer.md` returns exactly 1 — plan 18-01's correct
      claim survived.
    - `grep -c "673" docs/guide/optimizer.md` returns >= 1 — plan 18-01's numbers survived.
    - `grep -c "diagrams/pose_graph.png" docs/guide/optimizer.md` returns exactly 1 — plan
      18-04's image reference survived.
    - `grep -c "non-overlapping" docs/guide/optimizer.md` returns >= 1 — the correct `Why BFS?`
      paragraph body survived the heading change.
    - `grep -c "BFS" docs/guide/optimizer.md` returns 0.
    - `grep -c "configuration.md" docs/guide/optimizer.md` returns >= 1 (the loss-key cross-link).
    - `sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0, and
      `docs/_build/html/guide/optimizer.html` contains a rendered mermaid diagram with three
      stage nodes.
  </acceptance_criteria>

  <done>
    `optimizer.md` presents three stages in prose, in the mermaid diagram, and in its section
    structure, with the intrinsic pass demoted to a Stage 3 subsection; the loss section names
    Huber and prints the Huber rho with `loss_scale` as the transition point; the traversal term
    is corrected without damaging the correct rationale paragraph; and plans 18-01's and 18-04's
    edits are intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep the remaining doc pages, the API reference, README, and CLAUDE.md</name>
  <files>docs/guide/glossary.md, docs/guide/index.md, docs/api/calibration.rst, docs/api/index.rst, README.md, CLAUDE.md (local-only, gitignored)</files>

  <read_first>
    - `docs/guide/glossary.md` — all 71 lines: the `Auxiliary camera` entry (Stages 2-4), the
      `Pose graph` entry that plan 18-04 already made bipartite (only its traversal adjective
      remains), the `Bundle adjustment` entry (Stage 3 — already correct, leave it), and the
      `## See Also` footer's "The four calibration stages".
    - `docs/guide/index.md` line 9 — the "Four-stage calibration pipeline" bullet, the second
      half of D-16 that plan 18-03 deliberately left for this plan.
    - `docs/api/calibration.rst` — lines 8 and 38: the "four-stage pipeline" sentence and the
      `Stage 4: Refinement` section heading over the `aquacal.calibration.refinement`
      automodule. This file is NOT in 18-PATTERNS.md's inventory; it was found by the planner's
      own grep and is in scope under D-17's "line list is a floor, not a ceiling".
    - `docs/api/index.rst` line 9 — the "Four-stage calibration pipeline" subpackage
      description. Also outside 18-PATTERNS.md's inventory, also in scope under D-17.
    - `README.md` line 10 — "BFS-based extrinsic initialization for camera arrays".
    - `CLAUDE.md` line 45 — "Four-stage calibration pipeline."
    - `.gitignore` around line 216 — confirm `CLAUDE.md` is ignored before editing it.
    - `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-02-SUMMARY.md`
      — the confirmed vocabulary.
  </read_first>

  <action>
    Sweep the remaining tracked doc surfaces, then make one deliberately-uncommittable local edit.

    `docs/guide/glossary.md`:
    - `Auxiliary camera` entry: "Stages 2-4" becomes "Stages 2-3" (the intrinsic pass is part of
      Stage 3, so the range ends at 3).
    - `Pose graph` entry: apply the confirmed traversal term to the trailing "via BFS traversal"
      phrase. Plan 18-04 already corrected this entry's structural claim to bipartite; do not
      revert that.
    - `## See Also` footer: "The four calibration stages" becomes three.
    - Leave the `Bundle adjustment` entry alone — its "Stage 3" reference is already correct.

    `docs/guide/index.md` line 9: `- [Optimizer Pipeline](optimizer.md) — Four-stage calibration
    pipeline, bundle adjustment structure, and camera models` becomes three-stage. This completes
    D-16, whose first half (registering `configuration.md` in the bullet list and the toctree)
    plan 18-03 already shipped. Do not disturb that registration.

    `docs/api/calibration.rst`: line 8's "implements a four-stage pipeline" becomes three-stage.
    The `Stage 4: Refinement` section heading at line 38 sits over
    `.. automodule:: aquacal.calibration.refinement` — retitle it as Stage 3's second pass, with intrinsics
    unlocked. Keep the automodule directive and the module path exactly as they are; plan 18-07
    deliberately did not rename the module, so the directive must still resolve.

    `docs/api/index.rst` line 9: "**calibration**: Four-stage calibration pipeline (intrinsics,
    extrinsics, interface estimation, refinement)" becomes three-stage, keeping the
    parenthetical module list intact since those are real module names.

    `README.md` line 10: "**Multi-camera pose graph** — BFS-based extrinsic initialization for
    camera arrays" takes the confirmed traversal term. `src/aquacal.egg-info/PKG-INFO` carries a
    copy of this line but is a build artifact regenerated at packaging time — do NOT hand-edit it.

    `CLAUDE.md` line 45: "Four-stage calibration pipeline." becomes three-stage. IMPORTANT
    (D-19): `CLAUDE.md` is gitignored at `.gitignore:216`. Make the edit, but do NOT `git add`
    it, do NOT include it in any commit, and do NOT report it as a committed change in the
    summary. Record it in the summary as a local-only edit. `.claude/rules/*.md` IS tracked —
    grep it for stage language and correct anything found there in the normal, committed way.

    Explicitly out of scope, per D-18: `.planning/` and `CHANGELOG.md` are historical records and
    stay as written. This matches the convention set when the repo moved to the McGrathLab org.
    Also out of scope: the tutorial notebooks' "Stage 4 RMS" narration — `nbsphinx_execute =
    "never"` means they do not re-execute, and Phase 21 (DATA-03) re-runs them after all code
    phases land.
  </action>

  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && ! grep -riE "four.stage|four calibration stages|Stages 2-4" docs/ README.md --include=*.md --include=*.rst | grep -v _build && ! grep -qi BFS README.md && sphinx-build -W --keep-going -b html docs docs/_build/html && echo SWEEP_OK'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -riE "four.stage|four calibration stages|Stages 2-4" docs/ README.md --include=*.md --include=*.rst | grep -v _build` returns zero matches.
    - `grep -ci "BFS" README.md` returns 0.
    - `grep -ci "BFS" docs/guide/glossary.md` returns 0.
    - `grep -ci "bipartite" docs/guide/glossary.md` returns >= 1 — plan 18-04's correction survived.
    - `grep -c "(configuration.md)" docs/guide/index.md` returns exactly 1 — plan 18-03's
      registration survived.
    - `grep -cE "^configuration$" docs/guide/index.md` returns exactly 1 — the toctree entry survived.
    - `grep -c "Stage 4" docs/api/calibration.rst` returns 0, and
      `grep -c "automodule:: aquacal.calibration.refinement" docs/api/calibration.rst` returns 1 —
      the directive still resolves.
    - `grep -ci "four-stage" CLAUDE.md` returns 0 (local edit made).
    - `git status --porcelain CLAUDE.md` returns nothing — the file is gitignored, so no staged
      or unstaged tracked change may appear for it. If it DOES appear, stop: it is tracked after
      all and D-19's premise needs revisiting with the user.
    - `git diff --name-only HEAD` for this plan's commits lists no path under `.planning/` and
      does not list `CHANGELOG.md`.
    - `grep -criE "four.stage|Stage 4" .claude/rules/` returns 0.
    - `sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
  </acceptance_criteria>

  <done>
    Every tracked doc page, both API reference files, and README present the three-stage model
    and the confirmed traversal term; `CLAUDE.md` is corrected locally and left uncommitted;
    `.planning/` and `CHANGELOG.md` are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Collapse troubleshooting's Stage 3/4 phrasings and run the phase gate</name>
  <files>docs/guide/troubleshooting.md</files>

  <read_first>
    - `docs/guide/troubleshooting.md` — all 309 lines. The nine literal `Stage 3/4` occurrences
      (lines ~7, 61, 64, 106, 117, 172, 202, 263, 271) and the one `Stage 3 or 4` phrasing
      (~line 72). Note line 154 mentions frame rejection, not a stage — do not edit it.
    - `docs/guide/troubleshooting.md` around lines 136-165 and 300-309 — the cross-links plan
      18-03 added, which must survive.
    - `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-VALIDATION.md`
      § Sampling Rate, "Before `/gsd:verify-work`" — the phase-gate definition this task executes.
    - `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-RESEARCH.md`
      § Common Pitfalls, Pitfall 1 and 2 — why `make html` is insufficient and why
      `generate_all.py` must run standalone first.
  </read_first>

  <action>
    Two things: finish the troubleshooting sweep, then run the phase gate.

    Troubleshooting sweep. Under the three-stage model there is no Stage 4, and the optional second
    pass is still Stage 3 — so every `Stage 3/4` and `Stage 3 or 4` phrasing collapses
    to `Stage 3`. Apply this at all ten sites, including the one inside the YAML snippet comment
    (`max_calibration_frames: 150  # Use subset for faster Stage 3/4`) and the two bold
    occurrences. Where the surrounding sentence would read oddly after the collapse — for
    instance a heading like "Stage 3 or 4 fails to converge" — reword the sentence rather than
    leaving a grammatical seam. Do not touch line 154's frame-rejection description; it contains
    no stage reference. Do not disturb the `configuration.md` cross-links or the `## See Also`
    entry that plan 18-03 added.

    Phase gate. This is the last plan in the phase, so run and record the full gate defined in
    18-VALIDATION.md. Run `python docs/guide/_diagrams/generate_all.py` STANDALONE before the
    Sphinx build — `docs/conf.py`'s `run_diagram_generation` hook catches
    `subprocess.CalledProcessError` and only prints, so a generator crash is swallowed during a
    build and resurfaces far away as an unreadable-image warning. Then run the CI-equivalent
    Sphinx command with `-W`, not `make html`, whose empty default `SPHINXOPTS` passes where CI
    fails. Then run the full pytest suite including slow tests. Then run both grep guards. Record
    every command and its exit status in the plan summary; the phase's verification step reads
    them.
  </action>

  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && ! grep -qE "Stage 3/4|Stage 3 or 4" docs/guide/troubleshooting.md && python docs/guide/_diagrams/generate_all.py && sphinx-build -W --keep-going -b html docs docs/_build/html && python -m pytest tests/ -m "not slow" -q && echo GATE_OK'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE "Stage 3/4|Stage 3 or 4" docs/guide/troubleshooting.md` returns 0.
    - `grep -c "configuration.md" docs/guide/troubleshooting.md` returns >= 2 — plan 18-03's
      cross-links survived.
    - `grep -c "reject_outlier_frames" docs/guide/troubleshooting.md` is unchanged — D-15's
      "troubleshooting keeps its entries" held.
    - DOCS-02 grep guard: `grep -rniE "\bBFS\b|breadth.first" src/ docs/ README.md --include=*.py --include=*.md --include=*.rst --include=*.yaml | grep -v _build` returns EXACTLY the two
      `src/aquacal/calibration/extrinsics.py` lines inside `_find_connected_components` and
      nothing else.
    - DOCS-06 grep guard: `grep -rnE '"stage4"|stage4_joint_refinement|trace_stage4|calibration_stage4|Stage 4' src/ --include=*.py --include=*.yaml` returns zero matches.
    - `python docs/guide/_diagrams/generate_all.py` exits 0 (run standalone, before the build).
    - `sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
    - `python -m pytest tests/` (full suite, slow included) exits 0 with >= 799 passing —
      matching or exceeding the Phase 17 baseline.
    - `ruff check src/ tests/ docs/_static/scripts/` exits 0.
    - `git log --oneline -20` shows no `feat:` or `fix:` commit from this phase — every Phase 18
      commit is `docs:`, `refactor:`, or `test:`, so the next push to `main` cuts no unintended
      python-semantic-release version beyond what Phases 16-17 already queued.
  </acceptance_criteria>

  <done>
    No `Stage 3/4` phrasing remains in troubleshooting, plan 18-03's cross-links are intact, and
    the full phase gate — standalone diagram generation, CI-equivalent docs build, full test
    suite, both grep guards, and the commit-type audit — has been run with every result recorded
    in the summary.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> published docs site and paper reviewers | These pages are what a reviewer reaches by following the manuscript's link; a mismatched architecture or a wrong loss equation is a published-correctness failure |
| repo -> Sphinx build (CI) | `sphinx-build -W --keep-going` makes every warning fatal, so a broken mermaid node or a dangling reference fails CI |
| repo -> git index | `CLAUDE.md` is gitignored; an edit there must never be staged or reported as committed (D-19) |
| repo -> python-semantic-release | Conventional commit type determines whether a release is cut |

Prose edits to markdown and reStructuredText. No network surface, no user input, no new
dependencies, no new executable path.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08-BLIND | Tampering (over-broad edit) | blind sweep over "BFS", "Stage 4", or the numbers | mitigate | Every edit is located by its quoted text from the inventory above, never by regex substitution; acceptance criteria positively assert the survival of the three correct things — `optimizer.md`'s non-overlapping-FOV paragraph, the "98% sparse" claim, and `extrinsics.py`'s two genuine BFS mentions |
| T-18-08-REVERT | Tampering (silent revert of earlier work) | shared files owned by plans 18-01, 18-03, 18-04 | mitigate | Wave 3 ordering plus positive survival criteria: `673`/`98% sparse` (18-01), `(configuration.md)` bullet and bare `configuration` toctree entry (18-03), `diagrams/pose_graph.png` and `bipartite` (18-04) |
| T-18-08-FORMULA | Tampering (of published claims) | the Huber rho replacing the soft-L1 rho | mitigate | The equation is cross-checked against scipy's documented `huber` definition and against `schema.py:314`/`interface_estimation.py:134` before committing; a plausible-looking wrong equation is exactly what survived here once already |
| T-18-08-IGNORED | Repudiation (misreported change) | `CLAUDE.md` edit being staged or reported as committed | mitigate | `git status --porcelain CLAUDE.md` must return nothing, and the summary records it explicitly as a local-only edit (D-19) |
| T-18-08-HISTORY | Tampering (of historical records) | `.planning/` and `CHANGELOG.md` | mitigate | `git diff --name-only HEAD` for this plan's commits must list no `.planning/` path and not `CHANGELOG.md` (D-18) |
| T-18-08-BUILD | Denial of service (CI build) | mermaid node removal, retitled `.rst` section over an automodule | mitigate | `sphinx-build -W --keep-going` is the verify command on every task, plus an explicit check that the built HTML renders three stage nodes and that the `automodule:: aquacal.calibration.refinement` directive still resolves |
| T-18-08-REL | Tampering (release metadata) | conventional commit type across the whole phase | mitigate | Task 3's gate audits `git log --oneline -20` for any `feat:`/`fix:` from Phase 18 |
| T-18-08-SC | Tampering (supply chain) | package installs | accept | No packages installed. Not applicable per 18-RESEARCH.md § Package Legitimacy Audit |
</threat_model>

<verification>
1. `grep -riE "four.stage|four calibration stages|Stages 2-4|Stage 4|Stage 3/4" docs/ README.md --include=*.md --include=*.rst | grep -v _build` returns zero matches.
2. DOCS-02 grep guard returns exactly the two `_find_connected_components` lines.
3. DOCS-06 grep guard over `src/` returns zero matches.
4. `python docs/guide/_diagrams/generate_all.py` exits 0.
5. `sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
6. `python -m pytest tests/` exits 0 with >= 799 passing.
7. `ruff check src/ tests/ docs/_static/scripts/` exits 0.
8. `git status --porcelain CLAUDE.md` returns nothing.
9. `git log --oneline -20` contains no `feat:` or `fix:` commit from Phase 18.
</verification>

<success_criteria>
- ROADMAP Phase 18 success criterion 2 is fully satisfied: every doc site and `extrinsics.py`
  docstring that misused "BFS" now uses the confirmed term, except `_find_connected_components`.
- ROADMAP Phase 18 success criterion 5 is fully satisfied: console output, timing keys,
  module/schema docstrings, CLI config comments, and every docs page present the same
  three-stage model, and the documented loss default reads `huber` with a matching formula.
- No earlier plan's work was reverted.
- The phase gate ran in full, with every command and exit status recorded.
</success_criteria>

<output>
Create `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-08-SUMMARY.md` when done
</output>
