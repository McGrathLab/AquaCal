---
phase: 18-documentation-corrections-stage-model-reconciliation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [DOCS-02, DOCS-06]

must_haves:
  truths:
    - "D-07: The three-stage names and the BFS -> best-first wording are confirmed against the LIVE manuscript source (main.tex / supplement.tex), not the stale Desktop PDF"
    - "D-05: The ex-Stage-4 is described as Stage 3's second pass with intrinsics unlocked, matching main.tex's own wording"
    - "D-23: Auxiliary-camera registration carries no stage number — the paper excludes it from Stages 2 and 3 and describes it as a post-hoc 6-DOF/10-DOF refinement"
    - "DOCS-01, DOCS-03, and DOCS-04 were never blocked on this vocabulary question"
  artifacts:
    - path: ".planning/phases/18-documentation-corrections-stage-model-reconciliation/18-02-SUMMARY.md"
      provides: "The confirmed vocabulary contract, with verbatim manuscript citations, that plans 18-05/18-06/18-07/18-08 implement against"
      contains: "vocabulary"
  key_links:
    - from: "18-02-SUMMARY.md"
      to: "plans 18-05, 18-06, 18-07, 18-08"
      via: "depends_on frontmatter — every rename plan reads this summary before editing"
      pattern: "18-02"
---

<objective>
Record the confirmed stage and traversal vocabulary as a concrete string contract that the
four rename plans (18-05, 18-06, 18-07, 18-08) implement against.

**This plan was originally a blocking `checkpoint:human-verify`. The question it gated is
now answered and the gate is removed.** History, because it matters for trusting the
answer: `/gsd:discuss-phase` locked a three-stage vocabulary (D-04/D-05/D-06) and a
BFS -> best-first correction (D-02), with D-07 flagging that the manuscript was unreachable
so the wording was inferred. The research pass then found `C:\Users\tucke\Desktop\main.pdf`
and reported it said the *opposite* on both counts — four stages, BFS throughout. That PDF
turned out to be a stale 2026-06-15 export. The user identified the live paper source, and
reading it directly confirmed the originally locked vocabulary.

Output: a recorded decision. No files in the repo are modified by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-CONTEXT.md
@.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-RESEARCH.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Record the confirmed vocabulary contract</name>
  <files>.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-02-SUMMARY.md</files>

  <read_first>
    - `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-CONTEXT.md`
      — D-02, D-04, D-05 (revised), D-06, D-07 (resolved), D-23 (new). These carry the
      confirmed wording and the verbatim manuscript citations.
    - `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-CONTEXT.md`
      § Canonical References -> "The manuscript itself" — the authoritative file paths and
      the explicit warning against the stale Desktop PDF.
    - `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-RESEARCH.md`
      § Answers, Q2 — the superseded finding. Read it so the summary can state plainly that
      Q2's conclusion was based on a stale artifact and is now overturned.
  </read_first>

  <what-built>
    Nothing in the repo. This plan produces one planning artifact: the string contract the
    four rename plans read.

    The vocabulary is confirmed against the live manuscript source at
    `C:\Users\tucke\OneDrive - Georgia Institute of Technology\Thesis\Spinoffs\papers\aquacal\`:

    - `main.tex:208` — `\noindent\textbf{Three-stage calibration.}`
    - `main.tex:215` — "When intrinsic refinement is enabled, Stage~3 runs a second time,
      warm-started with each camera's focal length and principal point unlocked ($4N$
      further parameters, distortion held fixed)."
    - `main.tex:218` — "The second pass is optional because Stage~1 already constrains the
      intrinsics tightly."
    - `main.tex:222-224` — auxiliary cameras are excluded "from Stages~2 and~3", then
      "registered post-hoc against the frozen board placements and `water_z` via refractive
      Perspective-n-Point (PnP) initialization, followed by either a 6-DOF refinement ... or
      a 10-DOF refinement that adds focal length and principal point."
    - `main.tex` — zero occurrences of "BFS" or "breadth-first".
    - `supplement.tex:483-486` — "a best-first traversal alternates between camera and frame
      nodes, expanding next whichever node was reached by the highest-corner-count
      observation (rather than whichever lies fewest hops from the reference, as a
      breadth-first order would)".
  </what-built>

  <how-to-verify>
    Spot-check two of the citations directly against the live source before writing the
    summary — this plan's whole value is that the contract is grounded rather than inferred,
    and the previous grounding attempt read the wrong file:

    `grep -n "Three-stage calibration" "/c/Users/tucke/OneDrive - Georgia Institute of Technology/Thesis/Spinoffs/papers/aquacal/main.tex"`
    `grep -n "best-first traversal" "/c/Users/tucke/OneDrive - Georgia Institute of Technology/Thesis/Spinoffs/papers/aquacal/supplement.tex"`

    Both must return a match. If either does not, STOP and report — do not write a contract
    from CONTEXT.md alone, and do not substitute `C:\Users\tucke\Desktop\main.pdf`.

    Scope reminder: these files are READ-ONLY inputs. This phase does not edit the
    manuscript. Manuscript prose and figures are out of scope per `REQUIREMENTS.md`.
  </how-to-verify>

  <action>
    Write `18-02-SUMMARY.md` recording the confirmed contract. It must state, verbatim and
    unambiguously, the exact strings the rename plans will write:

    - **Stage model:** three stages. Stage 1 = in-air intrinsic calibration; Stage 2 =
      extrinsic initialization; Stage 3 = joint refractive bundle adjustment.
    - **Prose/console label for the ex-Stage-4:** "Stage 3's second pass, with intrinsics
      unlocked" (per revised D-05, matching `main.tex:215,218`). Not "Stage 4", and not the
      earlier inferred "optional intrinsic pass".
    - **Machine key form:** `stage3_intrinsic_pass`, replacing `stage4_joint_refinement`
      (timing key), `calibration_stage4.json`, `trace_stage4.csv`, and the `"stage4"` tag.
      `"stage3"` and `"stage3_rerun"` are unaffected.
    - **Traversal term:** "best-first" everywhere except `_find_connected_components`, which
      genuinely is breadth-first and is untouched (D-11).
    - **Auxiliary-camera registration:** label "Auxiliary camera registration" with NO stage
      number (per new D-23), DOF distinction (6-DOF vs 10-DOF) in the message body, existing
      stage-agnostic timing key `auxiliary_registration` retained.
    - **Per-plan verdict:** 18-05, 18-06, 18-07, 18-08 all proceed as planned, with 18-05
      and 18-06 taking the D-05 and D-23 string revisions above.

    Also record explicitly that `18-RESEARCH.md` § Answers Q2 is **superseded** — its
    four-stage/BFS conclusion came from the stale Desktop PDF — so a later reader of the
    research does not re-open a settled question.

    Name D-05, D-07, and D-23 as the decisions this summary carries, so the phase's
    decision-coverage gate can trace them.
  </action>

  <acceptance_criteria>
    - `18-02-SUMMARY.md` exists and contains the literal strings `Stage 3's second pass`,
      `stage3_intrinsic_pass`, `best-first`, and `Auxiliary camera registration`.
    - It contains the literal string `stage4_joint_refinement` in the context of what is
      being replaced, so the rename plans can grep the before/after pair from one place.
    - It states that `18-RESEARCH.md` Q2 is superseded and names the stale artifact
      (`Desktop\main.pdf`) so the finding is not re-derived.
    - It names D-05, D-07, and D-23.
    - `git status --porcelain src/ docs/ README.md` is empty — this plan modifies no
      repository source or documentation files.
    - The two grep spot-checks in `<how-to-verify>` both returned a match.
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'F=.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-02-SUMMARY.md; test -f "$F" && grep -q "Stage 3.s second pass" "$F" && grep -q "stage3_intrinsic_pass" "$F" && grep -q "best-first" "$F" && grep -q "Auxiliary camera registration" "$F" && test -z "$(git status --porcelain src/ docs/ README.md)" && echo CONTRACT_OK'</automated>
  </verify>

  <done>
    The confirmed stage and traversal vocabulary is recorded in 18-02-SUMMARY.md as concrete
    target strings plus a per-plan verdict for 18-05, 18-06, 18-07, and 18-08. No repository
    file was modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> published manuscript | The rename this contract authorizes cuts into machine-readable keys with no compat shim (D-01, D-03); a vocabulary mismatch with the published paper is a correctness failure that is expensive to unwind |
| manuscript tree -> repo | The manuscript source is a READ-ONLY input living outside this repo. Nothing in this phase may write to it |

This plan modifies no repository files, installs nothing, and adds no executable surface.
There is no applicable classic AppSec threat.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-DIVERGE | Tampering (of published claims) | stage/traversal vocabulary across `src/` and `docs/` | mitigate | The contract is grounded in verbatim citations from the live `main.tex`/`supplement.tex`, and the two grep spot-checks re-confirm them at execution time rather than trusting CONTEXT.md |
| T-18-02-STALE | Spoofing (of source authority) | `Desktop\main.pdf` being mistaken for the current draft | mitigate | The stale artifact is named explicitly in `<how-to-verify>` and the summary records that research Q2 is superseded. This threat already materialized once in this phase — the mitigation is the record, not vigilance |
| T-18-02-WRITE | Tampering | manuscript source tree | mitigate | Manuscript files are read-only inputs; no task in this phase writes outside the AquaCal repo |
| T-18-02-SC | Tampering (supply chain) | package installs | accept | No packages installed by this plan |
</threat_model>

<verification>
1. `18-02-SUMMARY.md` exists and records the contract with verbatim manuscript citations.
2. `git status --porcelain src/ docs/ README.md` shows nothing from this plan.
3. Every downstream rename plan (18-05, 18-06, 18-07, 18-08) can read a concrete target
   string from the summary rather than re-deriving it from CONTEXT.md or the manuscript.
</verification>

<success_criteria>
- The confirmed vocabulary is recorded as a concrete string contract for the four rename plans.
- The superseded research finding is marked as such, so the settled question stays settled.
- No file outside the AquaCal repo was modified.
</success_criteria>

<output>
Create `.planning/phases/18-documentation-corrections-stage-model-reconciliation/18-02-SUMMARY.md` when done
</output>
