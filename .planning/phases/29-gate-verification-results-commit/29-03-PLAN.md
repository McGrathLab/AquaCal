---
phase: 29-gate-verification-results-commit
plan: 03
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - .planning/phases/29-gate-verification-results-commit/analyze_e2_control.py
  - .planning/phases/29-gate-verification-results-commit/analyze_e7_before_after.py
  - .planning/phases/29-gate-verification-results-commit/29-e2-control.txt
  - .planning/phases/29-gate-verification-results-commit/29-e7-before-after.txt
autonomous: true
requirements: [RUN-03]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The E2 sanity control compares seed 42 against seed 42 and nothing else, and the seed appears in the control's own printed output on every comparison line, so a later reader cannot mistake it for a cross-seed comparison."
    - "The control reads the seed from each tree's sibling `benchmark.json` `solver_config['seed']` and asserts the two are equal, rather than assuming 42."
    - "The control names its baseline file explicitly and reports **both** available baselines separately: `experiments/pre_rerun_baseline/results/` (Windows, aquacal 1.8.0 — the ~1e-8 span the criterion asks about) and `experiments/freeze01_run_output/results/` (Linux, 2.0.1 — byte-identical, a different and weaker statement)."
    - "Worst-case relative drift against the pre-run baseline is below 1e-6, and the control exits non-zero if it is not."
    - "The E7 sign test is reported for **both** pairings across **both** trees, using the one-tailed statistic that reproduces the published 10-of-10, p = 0.00098 on the baseline tree."
    - "The E7 script derives the seed count from the data rather than hard-wiring it, so its own caveat text cannot contradict the domain it just measured."
    - "Both evidence files are produced by redirecting a script's stdout and are never hand-edited."
  artifacts:
    - .planning/phases/29-gate-verification-results-commit/analyze_e2_control.py
    - .planning/phases/29-gate-verification-results-commit/analyze_e7_before_after.py
    - .planning/phases/29-gate-verification-results-commit/29-e2-control.txt
    - .planning/phases/29-gate-verification-results-commit/29-e7-before-after.txt
  key_links:
    - "E2's seed band on `mean_per_camera_reprojection_px` spans 0.761 → 0.910 px, four orders of magnitude worse than the 1e-8 the criterion asks for — so the same-seed constraint is what separates a healthy run from an apparently catastrophic one."
    - "`interface_ablation_band.csv` is byte-identical between attempt 1 and attempt 2 (md5 `b6515ed77ed04268608b74217716020b`), which is what proves the refined pairing's 8→7 move is a FIX-02 effect from Phases 23-26 and not a re-run artefact."
    - "The E2 control is the only check on D-29-10's stop list that speaks to solver correctness; a failure here blocks publication, and nothing else in RUN-03 does."
  prohibitions:
    - "Do not run the E2 control across seeds — compare seed 42 to seed 42 only."
    - "Do not run E7's band by hand, and never with `--force`: a manual forced band run fires the known benchmark-overwrite hazard. This comparison is pure re-analysis of two committed CSVs — no solve."
    - "Do not execute `.planning/phases/19.2-experiment-execution-and-provenance/analyze_e7_spread.py`; its `ROOT` is a hard-coded Windows path to a sweep directory that no longer exists. Copy its recipe, not its entry point."
    - "Do not substitute a two-sided p-value or a different metric for the sign test — the module's own history records that one-sided and two-sided were conflated once already."
    - "Do not edit, regenerate or reformat anything under `experiments/results*/` (D-29-08)."
    - "Do not use `tests/unit/_baseline_paths.py`'s `resolve_results_dir()` in these scripts — it switches subject depending on whether the live tree holds a file, and these scripts must name their two trees explicitly."
    - "Do not edit §3 or any manuscript file; report the numbers and flag the move (D-29-16, D-29-19)."
---

<objective>
Produce the phase's two scientific verdicts: E2's same-seed sanity control, and E7's explicit
before/after ablation comparison.

Purpose: these are ROADMAP criteria 2 and 3, and neither has a runnable script in the repository
today. Criterion 2 is the run's only check that speaks to solver correctness — a drift to ~1e-2
means the run is broken in a way no completeness gate detects — and it is item 1 on D-29-10's
three-item stop list. Criterion 3 exists because FIX-02 gave E7 two extra free parameters per
interface, which could have softened a published 10-of-10 sign test; the ROADMAP's own wording is
that *"it is reported here, not discovered during manuscript re-verification."*

Research measured both to completion. This plan turns those measurements into committed,
re-runnable scripts and evidence files, so the numbers in the phase record have a reproducible
provenance rather than a citation to a research session.

Output: two analysis scripts and two evidence files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-gate-verification-results-commit/29-CONTEXT.md
@.planning/phases/29-gate-verification-results-commit/29-RESEARCH.md
@.planning/phases/29-gate-verification-results-commit/29-PATTERNS.md
@.planning/phases/29-gate-verification-results-commit/29-02-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols and files **created** by this plan (new — not pre-existing; do not treat as drift):

| Artifact | Kind |
|---|---|
| `.planning/phases/29-gate-verification-results-commit/analyze_e2_control.py` | new file |
| `BASELINE_PRERUN`, `BASELINE_FREEZE01`, `AFTER`, `SEED`, `DRIFT_LIMIT` | new module constants |
| `read_seed(benchmark_json)`, `compare(before_path, after_path, label)`, `main()` | new functions |
| `.planning/phases/29-gate-verification-results-commit/analyze_e7_before_after.py` | new file |
| `METRIC`, `PAIRS`, `BEFORE`, `AFTER` | new module constants |
| `sign_test(band_csv)`, `report(label, band_csv)`, `main()` | new functions |
| `.planning/phases/29-gate-verification-results-commit/29-e2-control.txt` | new evidence file |
| `.planning/phases/29-gate-verification-results-commit/29-e7-before-after.txt` | new evidence file |

**Where these two scripts live, and why.** Both go in the phase directory, not `scripts/`. The
in-repo precedent is `.planning/phases/19.2-experiment-execution-and-provenance/analyze_e7_spread.py`
— a one-off evidence producer that sits beside the record it produced. `scripts/` is the home for
reusable operational tooling (`extract_frames.py`, and this phase's `zenodo_upload.py`); these two
scripts answer one question about one pair of trees at one sha and then become history. Both
locations are ruff-covered (`[tool.ruff] exclude` does not list `.planning`), so the choice costs
no lint coverage.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: The E2 same-seed control — seed 42 against seed 42, both baselines, named</name>
  <precondition>Plan 29-02 has landed the returned run, so `experiments/results/real_rig_metrics.json` and `experiments/results/benchmark.json` exist.</precondition>
  <files>.planning/phases/29-gate-verification-results-commit/analyze_e2_control.py, .planning/phases/29-gate-verification-results-commit/29-e2-control.txt</files>
  <behavior>
    - Reading a tree's `benchmark.json` returns the integer under `solver_config["seed"]`; all three trees return 42.
    - Comparing the pre-run baseline against this run reports a relative drift below `1e-6` for every numeric field.
    - Comparing `freeze01_run_output` against this run reports relative drift of exactly `0.0` for every numeric field, because the two files are byte-identical.
    - `n_comparisons` compares exactly equal at `7762` in both comparisons — an integer field must not be reported as a rounding-scale drift.
    - Every comparison line printed contains the literal seed statement, so a line lifted out of context still says which seeds were compared.
    - Given a hypothetical drift at or above `1e-6`, `main()` returns a non-zero exit code.
  </behavior>
  <read_first>
    - `.planning/phases/19.2-experiment-execution-and-provenance/analyze_e7_spread.py` — the structural shell only: module docstring stating the criterion and why the obvious test is wrong, module-level path constants, `main() -> int`, plain `print()` to stdout, `raise SystemExit(main())` guard, numbered `=== (n) … ===` section banners and an explicit `=== VERDICT ===` block.
    - `.planning/probes/2026-08-17-phase-23-recon/e4_check_detail.py` lines 1-30 — the established one-off-analysis idiom: repo-relative path constants at module level plus a documented cwd.
    - `.planning/phases/29-gate-verification-results-commit/29-RESEARCH.md` — § *The E2 Same-Seed Control (ROADMAP criterion 2) — MEASURED*, including the seven-row drift table, the three-tree md5/environment table, and the three guardrails under *Guardrails the control's own output must carry*.
    - `.planning/phases/29-gate-verification-results-commit/29-PATTERNS.md` — § `analyze_e2_control.py`, including the *Non-negotiable output properties* list.
    - `experiments/results/real_rig_metrics.json` and `experiments/pre_rerun_baseline/results/real_rig_metrics.json` — the two files being compared; read them so the field set and the `provenance` block are known rather than guessed.
    - `pyproject.toml` lines 93-110 — ruff surface.
  </read_first>
  <action>
Write `.planning/phases/29-gate-verification-results-commit/analyze_e2_control.py` and redirect its
stdout to `29-e2-control.txt`.

**Three trees, named explicitly, never resolved dynamically.** Module-level constants, repo-root
relative, with the documented cwd being the repository root:

- `BASELINE_PRERUN = "experiments/pre_rerun_baseline/results/real_rig_metrics.json"` — Windows,
  aquacal 1.8.0, git sha `6c7f930b…`. This is the baseline ROADMAP criterion 2 means when it says
  "reproduces its pre-run numbers to ~1e-8".
- `BASELINE_FREEZE01 = "experiments/freeze01_run_output/results/real_rig_metrics.json"` — Linux,
  aquacal 2.0.1, git sha `3ab9c137…`, attempt 1's run.
- `AFTER = "experiments/results/real_rig_metrics.json"` — this run, git sha `7005a277…`.

Do **not** call `tests/unit/_baseline_paths.py`'s `resolve_results_dir()`. It switches subject
depending on whether the live tree holds a file, and the whole point here is that the two baselines
give different and differently-meaningful answers, so each must be named.

**Assert the seed rather than assume it.** `read_seed(benchmark_json)` loads the sibling
`benchmark.json` beside each `real_rig_metrics.json` and returns `solver_config["seed"]`. Read it
for all three trees, assert all three are equal, and print the value. If they are not equal, print
a loud verdict and return non-zero — a cross-seed comparison must fail closed, not silently
report nonsense. `SEED = 42` is the expected value, stated as an expectation to check, not as an
input.

**Every printed comparison line carries the seed.** The literal form is
`seed <n> vs seed <n>` at the head of each field line. ROADMAP criterion 2 is explicit about this:
E2's seed band on `mean_per_camera_reprojection_px` spans 0.761 → 0.910 px, so a cross-seed
comparison of the same quantity looks catastrophic, and a line lifted out of this file into a
record months from now must still say what it compared.

**`compare(before_path, after_path, label)`** loads both JSONs, iterates the union of their keys in
sorted order, skips the `provenance` block, and for each pair of numeric values prints the label,
the seed statement, the field name, both values at full repr precision, and the relative drift
`abs(b - a) / abs(a)` (guarding a zero denominator). Report integer-valued fields as exact-equal
rather than as a drift figure — `n_comparisons` is `7762` on both sides and is not a
floating-point quantity. Return the maximum relative drift seen.

**Report both comparisons, labelled, in that order**, and say what each one means:

- `pre_rerun_baseline -> freeze-02` is the criterion's comparison and spans Windows→Linux and
  aquacal 1.8.0→2.0.1. Expected worst case: **2.52e-08** on `inter_corner_rmse_mm`. The §3 headline
  `mean_per_camera_reprojection_px` is expected at **4.90e-09**. `water_z_m` at 2.79e-09.
- `freeze01_run_output -> freeze-02` is expected to be **exactly zero on every field**, because the
  two files are byte-identical (both md5 `57279708f6106f411d1fe03ed2698291`). Say in the output
  that this is a *different and weaker* statement than the criterion's — it holds two Linux runs at
  the same library version against each other, not the whole span.

Print the three trees' `git_sha`, `aquacal_version`, `numpy`, `opencv` and `os` values in the
header so the span each comparison covers is visible in the file itself.

**Verdict and exit code.** `DRIFT_LIMIT = 1e-6`. Print an explicit `=== VERDICT ===` block naming
the worst-case field and value for each comparison, stating PASS or FAIL against `DRIFT_LIMIT`, and
recording that this is D-29-10 stop-list item 1 — the one check whose failure blocks publication.
`main()` returns non-zero if the worst-case relative drift reaches `DRIFT_LIMIT`, or if the seeds
disagree.

Do not compare against `check_e2_band`'s within-run consistency check — that gate compares the
band's seed-42 record to the same-run `real_rig_metrics.json` at rtol 1e-6, is already inside the
`176/7/0` roll-up, and is not ROADMAP criterion 2.

Finish with `ruff check` and `ruff format --check` over the new file.
  </action>
  <verify>
    <automated>ruff check .planning/phases/29-gate-verification-results-commit/analyze_e2_control.py &amp;&amp; ruff format --check .planning/phases/29-gate-verification-results-commit/analyze_e2_control.py &amp;&amp; python .planning/phases/29-gate-verification-results-commit/analyze_e2_control.py &gt; .planning/phases/29-gate-verification-results-commit/29-e2-control.txt &amp;&amp; grep -qc 'seed 42 vs seed 42' .planning/phases/29-gate-verification-results-commit/29-e2-control.txt &amp;&amp; grep -q 'pre_rerun_baseline' .planning/phases/29-gate-verification-results-commit/29-e2-control.txt &amp;&amp; grep -q 'freeze01_run_output' .planning/phases/29-gate-verification-results-commit/29-e2-control.txt</automated>
  </verify>
  <acceptance_criteria>
    - `python .planning/phases/29-gate-verification-results-commit/analyze_e2_control.py` exits `0`.
    - Every field-comparison line in `29-e2-control.txt` contains the literal `seed 42 vs seed 42`.
    - The evidence file names both baseline paths in full: `experiments/pre_rerun_baseline/results/real_rig_metrics.json` and `experiments/freeze01_run_output/results/real_rig_metrics.json`.
    - The `pre_rerun_baseline` comparison reports `mean_per_camera_reprojection_px` drifting `0.8240385366779744` → `0.8240385407126619`, relative drift on the order of `4.9e-09`.
    - The reported worst-case relative drift for the `pre_rerun_baseline` comparison is on `inter_corner_rmse_mm` and is on the order of `2.5e-08` — below `DRIFT_LIMIT = 1e-6`.
    - The `freeze01_run_output` comparison reports relative drift `0` on every field.
    - `n_comparisons` is reported as exactly `7762` on both sides of both comparisons.
    - The header records the three trees' git shas: `6c7f930b…`, `3ab9c137…`, `7005a277…`.
    - The `=== VERDICT ===` block states PASS against `1e-6` and names this as D-29-10 stop-list item 1.
    - `ruff check` and `ruff format --check` on the new script both exit 0.
    - `git status --porcelain experiments/` shows only untracked run output — no tracked file under `experiments/` changed.
  </acceptance_criteria>
  <done>ROADMAP criterion 2 is measured, exits green, and is written down in a form that cannot be misread as a cross-seed comparison.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: The E7 before/after sign test — both pairings, both trees</name>
  <precondition>Plan 29-02 has landed the returned run, so `experiments/results/interface_ablation_band.csv` exists; `experiments/pre_rerun_baseline/results/interface_ablation_band.csv` and `experiments/freeze01_run_output/results/interface_ablation_band.csv` are already committed.</precondition>
  <files>.planning/phases/29-gate-verification-results-commit/analyze_e7_before_after.py, .planning/phases/29-gate-verification-results-commit/29-e7-before-after.txt</files>
  <behavior>
    - Applying the statistic to `experiments/pre_rerun_baseline/results/interface_ablation_band.csv` reproduces the published fixed-arm result: shared better in 10 of 10 seeds, no zero crossing, one-tailed p exactly `1/1024`.
    - The same tree's refined pairing gives 8 of 10, a zero crossing, and one-tailed p exactly `56/1024`.
    - Applying it to `experiments/results/interface_ablation_band.csv` gives fixed 10 of 10, no crossing, p exactly `1/1024` — unchanged.
    - The same tree's refined pairing gives 7 of 10, a zero crossing, and one-tailed p exactly `176/1024`.
    - The seed count `n` used in the p-value and in any caveat sentence is derived from the data (`len(per.columns)`), and equals 10 for these inputs.
    - A pairing whose arm is missing from a tree is reported as skipped, not silently dropped.
  </behavior>
  <read_first>
    - `.planning/phases/19.2-experiment-execution-and-provenance/analyze_e7_spread.py` (full) — the **exact** recipe analog. Copy `METRIC`, `PAIRS`, the `groupby(["arm","seed"]).mean().unstack("seed")` reshape, the `diff = per.loc[percam] - per.loc[shared]` orientation, `n_pos`, `crosses`, the one-tailed `p_one` sum, and `r`. Copy the reporting shape and the section banners. Do **not** copy `ROOT`, `load()`, the hard-wired `n = 5` caveat, or the `(n of 5)` line — all four are broken.
    - `.planning/phases/29-gate-verification-results-commit/29-RESEARCH.md` — § *The E7 Before/After Comparison (ROADMAP criterion 3 / D-29-16) — MEASURED*, including the four-row result table and the four findings beneath it.
    - `.planning/phases/29-gate-verification-results-commit/29-PATTERNS.md` — § `analyze_e7_before_after.py`.
    - `.planning/phases/29-gate-verification-results-commit/29-CONTEXT.md` — D-29-16 (measure, report both, flag only if the conclusion moved) and § Deferred Ideas (the E7 band overwrite hazard).
    - `experiments/results/interface_ablation_band.csv` — the column list; it carries `arm`, `seed` and `camera_height_drift_mm` directly, 480 rows = 4 arms × 10 seeds × 12 cameras, so the analog's per-seed concat collapses into a single `pd.read_csv`.
  </read_first>
  <action>
Write `.planning/phases/29-gate-verification-results-commit/analyze_e7_before_after.py` and redirect
its stdout to `29-e7-before-after.txt`.

**The statistic transfers verbatim; only the I/O changes.** Keep
`METRIC = "camera_height_drift_mm"` and
`PAIRS = [("shared_fixed", "percamera_fixed"), ("shared_refined", "percamera_refined")]` exactly.
Keep the reshape (`assign` absolute value, group by `("arm", "seed")`, mean, unstack the seed
axis), the difference orientation (`per.loc[percam] - per.loc[shared]`, where a positive value
means the shared arm is better), `n_pos = int((diff > 0).sum())`,
`crosses = bool(diff.min() < 0 < diff.max())`, the one-tailed exact sign test
`sum(comb(n, k) for k in range(n_pos, n + 1)) / 2 ** n`, and the between-arm correlation
`per.loc[shared].corr(per.loc[percam])`. That recipe is confirmed correct by reproducing its own
published output, so any substitution silently answers a different question.

**Replace the broken entry point.** Two named trees, module-level, repo-root relative:
`BEFORE = "experiments/pre_rerun_baseline/results/interface_ablation_band.csv"` and
`AFTER = "experiments/results/interface_ablation_band.csv"`. Each is a single `pd.read_csv`.

**Derive `n` from the data.** `n = len(per.columns)` — 10 here. The analog's closing caveat is
hard-wired to `n = 5` and its `(n of 5)` line has the same defect; either derive both from `n` or
drop the sentence. A caveat that contradicts the domain just measured is worse than no caveat.

**Print p at full precision.** The published figure `p = 0.00098` is `1/1024 = 0.0009765625`;
formatting at four decimal places rounds it to `0.0010` and loses the match. Print at least five
decimal places, and print the exact fraction (`n_pos`-through-`n` tail count over `2**n`) beside
it so the number is checkable by hand.

**Report both pairings for both trees**, in a table plus the analog's per-seed detail lines, and
then state the finding explicitly in a `=== VERDICT ===` block:

1. **The published primary conclusion HELD.** `shared_fixed` vs `percamera_fixed`: 10/10, no zero
   crossing, p = 1/1024 = 0.00098 — identical before and after. FIX-02's two extra free parameters
   per interface did **not** soften the fixed-intrinsics arm. Under D-29-16 this is a line in the
   record, not an author escalation. Expected `r`: +0.5567 before, +0.5059 after.
2. **The secondary refined pairing MOVED: 8/10 (p = 56/1024 = 0.05469) → 7/10
   (p = 176/1024 = 0.17188).** Both figures are published, in supplement §14 / MF-05. The
   *conclusion* is unchanged — the refined arm was already non-significant and is now more clearly
   so — but the digits moved. **Under D-29-16 this is the flag-to-author case.** Say so in the
   verdict block, in those words. Expected `r`: +0.8372 before, +0.8435 after.
3. **The move is not a re-run artefact.** Record the md5 of all three trees'
   `interface_ablation_band.csv`. Attempt 1 (`experiments/freeze01_run_output/results/`) and
   attempt 2 (`experiments/results/`) are both `b6515ed77ed04268608b74217716020b` — byte-identical
   — so the 8→7 move landed **before** attempt 1 and is a Phase 23-26 (FIX-02) effect. State this,
   so nobody attributes it to the run.
4. **§3 edits stay the author's.** The verdict block says the conclusion is reported here and that
   any manuscript change is the author's, per D-29-16 and D-29-19. Do not open, read for editing,
   or write any manuscript file.

Do not re-derive any of this by executing E7's band. A manual band run with the force flag fires
the known benchmark-overwrite hazard; this comparison is pure re-analysis of two already-written
CSVs, with no solve.

Finish with `ruff check` and `ruff format --check` over the new file.
  </action>
  <verify>
    <automated>ruff check .planning/phases/29-gate-verification-results-commit/analyze_e7_before_after.py &amp;&amp; ruff format --check .planning/phases/29-gate-verification-results-commit/analyze_e7_before_after.py &amp;&amp; python .planning/phases/29-gate-verification-results-commit/analyze_e7_before_after.py &gt; .planning/phases/29-gate-verification-results-commit/29-e7-before-after.txt &amp;&amp; grep -q 'shared_fixed' .planning/phases/29-gate-verification-results-commit/29-e7-before-after.txt &amp;&amp; grep -q 'shared_refined' .planning/phases/29-gate-verification-results-commit/29-e7-before-after.txt &amp;&amp; grep -q 'b6515ed77ed04268608b74217716020b' .planning/phases/29-gate-verification-results-commit/29-e7-before-after.txt</automated>
  </verify>
  <acceptance_criteria>
    - `python .planning/phases/29-gate-verification-results-commit/analyze_e7_before_after.py` exits `0`.
    - `29-e7-before-after.txt` reports four rows — two pairings × two trees — each carrying `n_pos`, `n`, the zero-crossing flag, the one-tailed p and the correlation.
    - Fixed pairing, baseline tree: `10` of `10`, no zero crossing, p printed as `0.00098` (or finer) and shown as `1/1024`.
    - Fixed pairing, this run's tree: `10` of `10`, no zero crossing, p printed as `0.00098` (or finer) — the published conclusion held.
    - Refined pairing, baseline tree: `8` of `10`, zero crossing present, p `0.05469` shown as `56/1024`.
    - Refined pairing, this run's tree: `7` of `10`, zero crossing present, p `0.17188` shown as `176/1024`.
    - `n` is reported as `10` and is shown to have been derived from the number of seed columns, not hard-coded.
    - The file records md5 `b6515ed77ed04268608b74217716020b` for both `experiments/freeze01_run_output/results/interface_ablation_band.csv` and `experiments/results/interface_ablation_band.csv`, and states that the byte identity places the refined-pairing move before attempt 1.
    - The `=== VERDICT ===` block states in words that the primary fixed-arm conclusion held, that the secondary refined pairing moved from 8/10 to 7/10, that this is the case D-29-16 requires be flagged to the author, and that §3 edits remain the author's.
    - `ruff check` and `ruff format --check` on the new script both exit 0.
    - `git status --porcelain experiments/` shows only untracked run output — no tracked file under `experiments/` changed, and no E7 stage was executed.
  </acceptance_criteria>
  <done>ROADMAP criterion 3 is discharged: both E7 numbers are measured, both are in the record, and the one conclusion that moved is raised explicitly rather than discovered later during manuscript re-verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed artifact CSV/JSON → published claim | Numbers read out of these files become the record a submitted paper's supplement is checked against. |
| baseline tree selection → interpretation | Two candidate baselines exist and give materially different answers; picking silently would misstate what was proven. |
| seed selection → verdict | A cross-seed comparison of the same quantity spans 0.761 → 0.910 px and would read as a catastrophic failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-16 | Repudiation | E2 control output | high | mitigate | The seed is read from each tree's `benchmark.json`, asserted equal across all three, and printed on every comparison line; a seed disagreement fails closed with a non-zero exit. |
| T-29-17 | Tampering (of interpretation) | baseline selection | high | mitigate | Both baselines are named by full path and reported separately, each labelled with what span it covers; `resolve_results_dir()` is deliberately not used because it switches subject. |
| T-29-18 | Tampering | `experiments/results*/` artifacts | high | mitigate | Both scripts open the CSV/JSON read-only; no E7 or E2 stage is executed, and the forced band run that carries the overwrite hazard is prohibited. |
| T-29-19 | Repudiation | E7 p-value precision | medium | mitigate | p printed at five or more decimal places with the exact integer fraction beside it, so the published `0.00098` is matched rather than rounded away to `0.0010`. |
| T-29-20 | Information disclosure | none | low | accept | Both scripts read only already-committed or about-to-be-committed scientific artifacts; there is no credential, no network call and no PII in scope. |
| T-29-21 | Elevation of privilege | none | low | accept | Both scripts are unprivileged, read-only, local analyses run by the operator; no new capability is granted. |
</threat_model>

<verification>
- Both scripts pass `ruff check` and `ruff format --check`.
- `analyze_e2_control.py` exits 0; every comparison line in `29-e2-control.txt` carries
  `seed 42 vs seed 42`; both baselines are named; worst-case relative drift is ~2.5e-08 against the
  pre-run baseline and exactly 0 against attempt 1.
- `analyze_e7_before_after.py` exits 0; `29-e7-before-after.txt` carries all four
  pairing × tree rows with `n = 10` derived from the data; fixed holds at 10/10 p = 1/1024 in both
  trees; refined moves 8/10 → 7/10; the byte-identity md5 is recorded.
- `git status --porcelain` shows no tracked file under `experiments/` modified.
- `python -m pytest tests/unit/test_experiments_provenance.py -q` is unchanged by this plan (these
  scripts touch no test and no tracked artifact).
</verification>

<success_criteria>
- ROADMAP criterion 2 passes and is recorded, same-seed and unambiguously labelled.
- ROADMAP criterion 3 is discharged with both pairings and both trees in the record, and the moved
  refined-pairing conclusion explicitly raised for the author.
- Neither result required running a stage, editing an artifact, or touching the manuscript.
</success_criteria>

<output>
Create `.planning/phases/29-gate-verification-results-commit/29-03-SUMMARY.md` when done.
</output>
