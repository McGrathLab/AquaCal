---
phase: 29-gate-verification-results-commit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/results/
  - experiments/results_e2_band/
  - experiments/results_e2_invocations/
  - experiments/results_e2_memory/
  - experiments/results_e2_timing/
  - experiments/results_e4_repeat/
  - experiments/run_experiment_suite_state.7005a27.tsv
  - experiments/run_experiment_suite_state.7005a27.failures.txt
  - experiments/run_experiment_suite_state.7005a27.stagelogs/
  - .planning/phases/29-gate-verification-results-commit/29-gates-full.txt
  - .planning/phases/29-gate-verification-results-commit/29-commit-manifest.txt
autonomous: true
requirements: [RUN-03, RUN-04]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The returned run's 461 files are present in the working clone at their original relative paths, and `~/aquacal-frozen-rerun-freeze-02-prod` is byte-for-byte what it was before — the copy reads from the production clone and never writes to it."
    - "`check_rerun_gates.py` at `--profile full` over the landed tree reports `TOTAL: 176 PASS, 7 N/A, 0 FAIL`, exits 0, and emits zero failure-verdict lines."
    - "`gate3_git_sha_consistency` passes on the single sha `7005a2771aa115e4f4c1284cec7e145739586a4a`, and `gate3_run_manifest_fields` reports all 17 environment fields present and non-null."
    - "Each of the six `experiments/results*/` destination trees holds 0 files immediately before the copy, and the copy halts rather than merging if any of them already holds a file."
    - "No file in the admitted commit set exceeds `check-added-large-files --maxkb=1000`; the largest, `experiments/results/interface_ablation_band.csv`, is 119,406 bytes."
    - "The admitted path set is a strict superset of attempt 1's commit `83da9b3`: the identical 209 paths plus exactly 18 `experiments/run_experiment_suite_state.*.stagelogs/*.log` files, with zero paths present in attempt 1 and absent now."
  artifacts:
    - .planning/phases/29-gate-verification-results-commit/29-gates-full.txt
    - .planning/phases/29-gate-verification-results-commit/29-commit-manifest.txt
  key_links:
    - "`check_e2_band` resolves E2's band as `out_dir.parent / 'results_e2_band'` — a **sibling** of `experiments/results`, not a child. All six output trees must land together or the roll-up silently drops below 176."
    - "Gate 3 reads `environment.git_sha` recorded inside the artifacts and never shells out to live git, which is exactly why a tree copied into a clone at a different HEAD still passes."
    - "`.gitignore:507`'s `!experiments/run_experiment_suite_state.*.stagelogs/*.log` re-inclusion, added deliberately in Phase 28 by commit `f399615`, is the entire reason the admitted set is 227 rather than attempt 1's 209."
  prohibitions:
    - "Do not edit, regenerate, reformat or re-run anything under `experiments/results*/` or `experiments/run_experiment_suite_state.*` — the run's outputs are immutable (D-29-08)."
    - "Do not edit `experiments/check_rerun_gates.py` or any other file inside the frozen `experiments/` tree; grading re-invokes the gate unchanged (D-29-11)."
    - "Do not write to, check out in, commit from, install into, or otherwise mutate `~/aquacal-frozen-rerun-freeze-02-prod`."
    - "Do not move the returned tree — copy it. The production clone stays the second independent copy."
    - "Do not change `.gitignore` or `.pre-commit-config.yaml` (D-29-17)."
    - "Do not run `pre-commit install` or `pre-commit run --all-files` — the formatting hooks would rewrite 147 of the 227 artifacts."
    - "Do not read the run's verdict from `$?` of the suite driver; acceptance is the roll-up `TOTAL:` line (D-01/D-02)."
---

<objective>
Land the returned production run in the working clone and grade it with the gate that already
exists, producing the phase's first two evidence files.

Purpose: RUN-03's central assertion is that `check_rerun_gates.py` passes over the **complete**
run — band runs and E2 included — with Gate 3's single-sha property holding. Research reproduced
exactly that in a disposable probe clone (`176 PASS, 7 N/A, 0 FAIL`, exit 0, zero failure lines,
`gate3_git_sha_consistency` green on `7005a277…`). This plan re-derives it in the real working
clone, on the branch the results will be committed to, and writes it down. It also takes the
read-only inventory of exactly which files the ignore rules admit, so plan 29-05's commit is a
mechanical act against a number already proven rather than a discovery.

Output: the six output trees plus three state artifacts present in the working clone,
`29-gates-full.txt`, and `29-commit-manifest.txt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-gate-verification-results-commit/29-CONTEXT.md
@.planning/phases/29-gate-verification-results-commit/29-RESEARCH.md
@.planning/phases/28-full-suite-production-run/28-RUN-RECORD.md
</context>

## Artifacts this phase produces

Files **created** by this plan (new — not pre-existing; do not treat as drift):

| Artifact | Kind |
|---|---|
| `.planning/phases/29-gate-verification-results-commit/29-gates-full.txt` | new evidence file |
| `.planning/phases/29-gate-verification-results-commit/29-commit-manifest.txt` | new evidence file |
| `experiments/results/`, `experiments/results_e2_band/`, `experiments/results_e2_invocations/`, `experiments/results_e2_memory/`, `experiments/results_e2_timing/`, `experiments/results_e4_repeat/` | populated (copied in verbatim; **not authored**) |
| `experiments/run_experiment_suite_state.7005a27.tsv`, `…failures.txt`, `…stagelogs/` | populated (copied in verbatim; **not authored**) |

## Flagged assumption (edge probe, RUN-03 — `unclassified`, unresolved)

> **RUN-03 / unclassified — "review manually."** RUN-03's wording is *"`check_rerun_gates.py`
> passes over the complete run"*, and "complete" is the load-bearing word. This plan reads it as
> **all six output trees plus the three state artifacts, at their original relative paths**, on the
> grounds that `check_e2_band` resolves a *sibling* of `out_dir` and would silently degrade
> otherwise. That reading is asserted here by the `461` file count and the `176/7/0` roll-up rather
> than assumed — but it is an interpretation, and it is flagged rather than auto-resolved.

## Ordering note the executor must not misread

`tests/unit/_baseline_paths.py`'s `resolve_results_dir()` prefers `experiments/results/` **only
while it holds a file**, and falls back to `experiments/pre_rerun_baseline/results/` otherwise.
The moment this plan's Task 1 completes, every tree-keyed test module changes subject from the
archive to the live tree. Two consequences:

1. `pytest tests/unit/test_experiments_provenance.py` **is expected to start failing** after this
   plan. That is the D1 prediction landing, not a regression, and it is repaired in plan 29-06.
2. The full D1 count of **8** only appears once the artifacts are **in the git index** —
   `_is_tracked()` shells `git ls-files --error-unmatch`, so the CSV rails discover nothing while
   the files are merely untracked. **Do not record a rail baseline in this plan**; plan 29-06
   measures it after plan 29-05's commit, which is the only point where the number is meaningful.

<tasks>

<task type="auto">
  <name>Task 1: Land the returned run in the working clone without touching the production clone</name>
  <precondition>`~/aquacal-frozen-rerun-freeze-02-prod` exists, is on detached HEAD `7005a2771aa115e4f4c1284cec7e145739586a4a`, and `git -C ~/aquacal-frozen-rerun-freeze-02-prod status --porcelain` lists exactly the 9 untracked top-level entries; the working clone is on branch `results/rerun-freeze-02` with an empty `git status --porcelain`.</precondition>
  <files>experiments/results/, experiments/results_e2_band/, experiments/results_e2_invocations/, experiments/results_e2_memory/, experiments/results_e2_timing/, experiments/results_e4_repeat/, experiments/run_experiment_suite_state.7005a27.tsv, experiments/run_experiment_suite_state.7005a27.failures.txt, experiments/run_experiment_suite_state.7005a27.stagelogs/</files>
  <read_first>
    - `.planning/phases/28-full-suite-production-run/28-RUN-RECORD.md` — the header (tag, sha, clone path) and § *The nine paths in the archive*; this is the document the phase opens from.
    - `.planning/phases/28-full-suite-production-run/freeze02-archive-manifest.txt` — per-path file counts and the 507-vs-461 reconciliation.
    - `.planning/phases/28-full-suite-production-run/rerun-freeze-02-output.sha256` — the two recorded archive hashes.
    - `.planning/phases/29-gate-verification-results-commit/29-RESEARCH.md` — § *Commit Mechanics* → *The returned tree, and where it is* / *The destination is empty and clean*.
    - `.planning/phases/29-gate-verification-results-commit/29-CONTEXT.md` — § Integration Points (the prod clone must not be mutated) and D-29-08.
  </read_first>
  <action>
Copy — never move — the nine untracked top-level entries from
`/home/tlancaster/aquacal-frozen-rerun-freeze-02-prod/experiments/` into
`/home/tlancaster/aquacal-frozen-rerun-freeze-01/experiments/`, preserving mode, ownership and
timestamps (`cp -a`) and preserving the original relative paths exactly. The nine are:
`results/`, `results_e2_band/`, `results_e2_invocations/`, `results_e2_memory/`,
`results_e2_timing/`, `results_e4_repeat/`, `run_experiment_suite_state.7005a27.tsv`,
`run_experiment_suite_state.7005a27.failures.txt`, and
`run_experiment_suite_state.7005a27.stagelogs/`.

**All six output trees must land together.** `check_rerun_gates.py:2262-2264` calls
`check_e2_band(out_dir.parent / "results_e2_band", …)` — E2's band is a **sibling** of `out_dir`,
not a child — so a partial landing degrades the roll-up below 176 with no obvious cause.

**Before the copy**, assert every one of the six destination trees holds zero files and that the
three state artifacts are absent. If any destination already holds a file, **stop and report**
rather than merging into it: a merged tree cannot honestly claim "everything here was produced by
this run."

**After the copy**, prove both sides:

*Destination.* The nine paths together hold **461** files. Spot-check byte integrity against the
digests research recorded: `experiments/results/real_rig_metrics.json` must be md5
`57279708f6106f411d1fe03ed2698291` and `experiments/results/interface_ablation_band.csv` must be
md5 `b6515ed77ed04268608b74217716020b`. Record both alongside the file counts.

*Source.* Re-run `git -C /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod status --porcelain`
and confirm it still lists exactly the same 9 untracked entries and nothing else, and that its HEAD
is still detached at `7005a2771aa115e4f4c1284cec7e145739586a4a`. The production clone is a second
independent copy of the run and stays that way.

Do not stage or commit anything in this task — plan 29-05 owns the commit. Do not run
`pre-commit install`; do not let any formatting hook see these files.
  </action>
  <verify>
    <automated>test $(find experiments/results experiments/results_e2_band experiments/results_e2_invocations experiments/results_e2_memory experiments/results_e2_timing experiments/results_e4_repeat experiments/run_experiment_suite_state.7005a27.stagelogs -type f 2>/dev/null | wc -l) -eq 423 &amp;&amp; test -f experiments/run_experiment_suite_state.7005a27.tsv &amp;&amp; test -f experiments/run_experiment_suite_state.7005a27.failures.txt &amp;&amp; md5sum -c &lt;&lt;&lt;"57279708f6106f411d1fe03ed2698291  experiments/results/real_rig_metrics.json" &amp;&amp; md5sum -c &lt;&lt;&lt;"b6515ed77ed04268608b74217716020b  experiments/results/interface_ablation_band.csv" &amp;&amp; test -z "$(git -C /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod diff --stat HEAD)"</automated>
  </verify>
  <acceptance_criteria>
    - `find experiments/results experiments/results_e2_band experiments/results_e2_invocations experiments/results_e2_memory experiments/results_e2_timing experiments/results_e4_repeat experiments/run_experiment_suite_state.7005a27.stagelogs -type f | wc -l` returns `423`, and adding the two loose state files gives the run's `461`.
    - `find experiments/results -type f | wc -l` returns `152`.
    - `find experiments/run_experiment_suite_state.7005a27.stagelogs -name '*.log' | wc -l` returns `18` and `-name '*.done' | wc -l` returns `18`.
    - `md5sum experiments/results/real_rig_metrics.json` is `57279708f6106f411d1fe03ed2698291`.
    - `md5sum experiments/results/interface_ablation_band.csv` is `b6515ed77ed04268608b74217716020b`.
    - `git -C /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod status --porcelain | wc -l` returns `9`, and `git -C /home/tlancaster/aquacal-frozen-rerun-freeze-02-prod rev-parse HEAD` returns `7005a2771aa115e4f4c1284cec7e145739586a4a`.
    - `ls /home/tlancaster/aquacal-frozen-rerun-freeze-01/.git/hooks/ | grep -vc '\.sample$'` returns `0` — no hook was installed by this task.
  </acceptance_criteria>
  <done>The complete returned run sits in the working clone at its original relative paths, byte-identical on the two anchor artifacts, and the production clone is provably untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Grade the landed tree with the frozen gate and write the evidence file</name>
  <precondition>Task 1 has landed all six output trees and the three state artifacts; the conda environment `aquacal-freeze02-cleanenv` is available for the working clone.</precondition>
  <files>.planning/phases/29-gate-verification-results-commit/29-gates-full.txt</files>
  <read_first>
    - `experiments/check_rerun_gates.py` — lines 2276-2331 (the CLI surface and the `TOTAL:` roll-up), lines 2011-2037 (`_check_git_sha_consistency`), lines 2255-2270 (the `check_e2_band` sibling resolution). **Read only. This file is inside the frozen tree and must not be edited.**
    - `.planning/phases/28-full-suite-production-run/freeze02-gates-full.txt` — the post-run gate re-run this plan's output must agree with line for line.
    - `.planning/phases/29-gate-verification-results-commit/29-RESEARCH.md` — § *The Grading Mechanics*, including the measured roll-up and the five Gate 3 lines.
  </read_first>
  <action>
Run the frozen gate over the landed tree at the full profile and capture stdout verbatim:

    python experiments/check_rerun_gates.py experiments/results --profile full \
      > .planning/phases/29-gate-verification-results-commit/29-gates-full.txt

Use the `aquacal-freeze02-cleanenv` interpreter. Capture the process exit code and append it to the
evidence file on its own labelled line — the standalone gate invocation does exit 0 on a healthy
tree (unlike the suite driver, whose non-zero exit is expected), but the roll-up `TOTAL:` line
stays the verdict of record per D-01/D-02.

Then assert, and record each assertion with its measured value in the evidence file:

- The roll-up line reads `TOTAL: 176 PASS, 7 N/A, 0 FAIL`.
- The count of lines whose verdict is a failure is zero.
- `gate3_git_sha_consistency` is PASS and names the single sha
  `7005a2771aa115e4f4c1284cec7e145739586a4a`.
- `gate3_run_manifest_present`, `gate3_run_manifest_fields` (all 17 environment fields present and
  non-null), `gate3_run_manifest_git_sha` and `gate3_run_manifest_clean_tree` are all PASS.

Finally, diff this output against Phase 28's `freeze02-gates-full.txt` and record whether the two
agree line for line. They were produced from the same artifacts by the same gate code and are
expected to agree; a divergence would mean something mutated the tree between the run machine and
here, which is exactly what this comparison exists to catch.

**Do not edit the gate, and do not edit any artifact to make a gate pass.** Under D-29-08 the run's
outputs are immutable; under D-29-11 the gate is already known clean and only the pytest rails are
in question.
  </action>
  <verify>
    <automated>grep -q '^TOTAL: 176 PASS, 7 N/A, 0 FAIL$' .planning/phases/29-gate-verification-results-commit/29-gates-full.txt &amp;&amp; test $(grep -c '^\[FAIL' .planning/phases/29-gate-verification-results-commit/29-gates-full.txt) -eq 0 &amp;&amp; grep -q 'gate3_git_sha_consistency.*7005a2771aa115e4f4c1284cec7e145739586a4a' .planning/phases/29-gate-verification-results-commit/29-gates-full.txt</automated>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: [FAIL -->
    <!-- The literals below are grepped against gate stdout captured by shell redirection, not against a file this task authors, so no comment written by the executor can reach them. -->
    - `.planning/phases/29-gate-verification-results-commit/29-gates-full.txt` contains the exact line `TOTAL: 176 PASS, 7 N/A, 0 FAIL`.
    - `grep -c '^\[FAIL' .planning/phases/29-gate-verification-results-commit/29-gates-full.txt` returns `0`.
    - `grep -c 'gate3_git_sha_consistency' …/29-gates-full.txt` returns at least `1`, and that line is a PASS naming `7005a2771aa115e4f4c1284cec7e145739586a4a`.
    - The evidence file records PASS for all four `gate3_run_manifest_*` gates, with `gate3_run_manifest_fields` stating all 17 environment fields present and non-null.
    - The gate's process exit code is recorded in the evidence file and is `0`.
    - A recorded line states whether `…/29-gates-full.txt` agrees line for line with `.planning/phases/28-full-suite-production-run/freeze02-gates-full.txt`, with the diff line count.
    - `git status --porcelain experiments/check_rerun_gates.py` is empty.
  </acceptance_criteria>
  <done>ROADMAP criterion 1 is demonstrated in the working clone and written down: the gate passes over the complete returned run, Gate 3's single-sha assertion included.</done>
</task>

<task type="auto">
  <name>Task 3: Inventory exactly which files the ignore rules admit</name>
  <precondition>Task 1 has landed the run; nothing has been staged or committed.</precondition>
  <files>.planning/phases/29-gate-verification-results-commit/29-commit-manifest.txt</files>
  <read_first>
    - `.gitignore` lines 460-520 — the `experiments/results*` rules and, at line 507, the deliberate `!experiments/run_experiment_suite_state.*.stagelogs/*.log` re-inclusion added in Phase 28 by commit `f399615`, with its own comment block explaining the 18 files.
    - `.pre-commit-config.yaml` — the `check-added-large-files --maxkb=1000` hook and the `detect-secrets` exclusion pattern.
    - `.planning/phases/29-gate-verification-results-commit/29-RESEARCH.md` — § *Commit Mechanics* → *What the ignore rules actually admit — 227 files*, its per-path table, and the attempt-1 diff.
    - `.planning/phases/29-gate-verification-results-commit/29-CONTEXT.md` — D-29-17 (no ignore-rule changes during this phase).
  </read_first>
  <action>
Produce the definitive admitted-path list, read-only, without staging anything, and write it to
`.planning/phases/29-gate-verification-results-commit/29-commit-manifest.txt`.

Derive the list from `git status --porcelain --untracked-files=all` restricted to the nine landed
paths — that enumeration is what git will actually admit, and it already applies the ignore rules.
Sort it. Then record, each with its measured value:

- The total admitted count. Expected: **227**.
- The per-path breakdown. Expected: `experiments/results/` **147** admitted of 152 on disk;
  `results_e2_invocations/` 25 of 119; `results_e2_band/` 7 of 136; `results_e4_repeat/` 4 of 4;
  `results_e2_timing/` 3 of 6; `results_e2_memory/` 3 of 6; the stagelogs directory 36 of 36
  (18 `.log` + 18 `.done`); plus the `.tsv` and the `.failures.txt`. Total on disk **461**,
  ignored **234**.
- The largest admitted file, in bytes. Expected: `experiments/results/interface_ablation_band.csv`
  at **119,406 bytes**, and **no admitted file above 1000 KB** — so
  `check-added-large-files --maxkb=1000` is not binding. Note explicitly that the ~11 MB
  `all_observation_depths.csv` D-29-17 cites as the tripwire lives at
  `experiments/results_e2_invocations/e2_classification/all_observation_depths.csv` and is
  **already ignored** by `.gitignore:478`; the decision is unaffected, only its arithmetic needed
  correcting.
- The five ignored files under `experiments/results/`, named: `calibration.json` (2.1 MB),
  `exp2_spatial_errors.csv` (13 MB), `interface_ablation_conditioning.npz` (3.2 MB),
  `reconstruction_errors.csv` (620 KB), `reprojection_residuals.csv` (1.2 MB).
- A path-for-path comparison against attempt 1's commit `83da9b3`
  (`git show --name-only --format="" 83da9b3`), with the run sha normalised in the state-file
  paths so the two sets are comparable. Expected: attempt 1 had **209**; attempt 2's admitted set
  is a **strict superset** — the identical 209 plus **18** stage logs, with **zero** paths present
  in attempt 1 and absent now. State both directions of the comparison explicitly, because a
  silent loss would be the dangerous failure and a silent gain would read as ignore-rule drift.
- The three generated LaTeX fragments that discharge D-29-19's traceability mechanism, confirmed
  present inside the 147: `experiments/results/benchmark_grid.tex`,
  `experiments/results/cpr_derived_values.tex`, `experiments/results/cpr_grouping.tex`.

Do not modify `.gitignore` or `.pre-commit-config.yaml` for any reason (D-29-17). If a count
disagrees with the expectation above, record the measured value and the discrepancy — do not adjust
an ignore rule to reach the expected number.
  </action>
  <verify>
    <automated>test $(grep -c '^experiments/' .planning/phases/29-gate-verification-results-commit/29-commit-manifest.txt) -ge 227 &amp;&amp; test $(grep -c '^experiments/results/' .planning/phases/29-gate-verification-results-commit/29-commit-manifest.txt) -ge 147 &amp;&amp; git diff --quiet -- .gitignore .pre-commit-config.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `29-commit-manifest.txt` lists exactly `227` admitted paths, each on its own line beginning `experiments/`.
    - Exactly `147` of those lines begin `experiments/results/`.
    - Exactly `18` of those lines match `experiments/run_experiment_suite_state\..*\.stagelogs/.*\.log`.
    - The file records the largest admitted file as `experiments/results/interface_ablation_band.csv` at `119406` bytes and states that no admitted file exceeds 1000 KB.
    - The file records the attempt-1 comparison in both directions: `18` paths added, `0` paths lost, against `83da9b3`'s `209`.
    - The file names all three of `benchmark_grid.tex`, `cpr_derived_values.tex`, `cpr_grouping.tex` as present in the admitted set.
    - `git diff --quiet -- .gitignore .pre-commit-config.yaml` exits 0 — neither file changed.
    - `git status --porcelain --untracked-files=no` reports nothing staged.
  </acceptance_criteria>
  <done>The exact commit set is enumerated, counted, size-checked and diffed against attempt 1 before a single file is staged, so plan 29-05's commit is a mechanical act rather than a discovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production clone → working clone | Scientific artifacts cross from a read-only, detached-HEAD clone into a branch that will be committed and pushed. |
| working tree → git object store | Files whose byte content is the evidence base cross into version control, where a formatting hook could rewrite them. |
| artifact bytes → gate verdict | The gate's roll-up is derived from artifact content; a mutation upstream of grading would be graded as authentic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-10 | Tampering | `experiments/results*/` byte content | high | mitigate | `cp -a` (copy, never move); md5 anchors asserted on `real_rig_metrics.json` and `interface_ablation_band.csv` immediately after landing; no hook installed (`.git/hooks/` asserted to hold only `*.sample`); no `pre-commit run --all-files`. |
| T-29-11 | Tampering | `~/aquacal-frozen-rerun-freeze-02-prod` | high | mitigate | Read-only access only; `git status --porcelain` and `rev-parse HEAD` re-asserted after the copy to prove the second independent copy of the run survives. |
| T-29-12 | Repudiation | grading verdict | medium | mitigate | Gate stdout captured verbatim by redirection into a committed evidence file and diffed line-for-line against Phase 28's own post-run capture; exit code recorded separately from the `TOTAL:` verdict. |
| T-29-13 | Tampering | `.gitignore` / `.pre-commit-config.yaml` | medium | mitigate | `git diff --quiet` asserted on both; D-29-17 forbids ignore-rule changes, so a count discrepancy is recorded rather than engineered away. |
| T-29-14 | Information disclosure | admitted commit set | medium | mitigate | The inventory names the five ignored files under `experiments/results/` explicitly, so nothing large or per-observation reaches git by accident; `detect-secrets` is run scoped to the staged set in plan 29-05. |
| T-29-15 | Spoofing | artifact provenance | high | mitigate | `gate3_git_sha_consistency` asserted on the single sha `7005a277…`; Gate 3 reads sha values recorded inside the artifacts, never live git, so a relocated tree cannot borrow the host clone's identity. |
</threat_model>

<verification>
- All nine landed paths present; 461 files total; 152 under `experiments/results/`.
- Both md5 anchors match the values recorded in `29-RESEARCH.md`.
- Production clone still reports 9 untracked entries at detached HEAD `7005a277…`.
- `29-gates-full.txt` carries `TOTAL: 176 PASS, 7 N/A, 0 FAIL`, gate exit code 0, zero
  failure-verdict lines, and all five Gate 3 lines PASS.
- `29-commit-manifest.txt` carries 227 admitted paths, 147 under `experiments/results/`, 18 stage
  logs, largest file 119,406 bytes, and the two-directional attempt-1 comparison.
- `.gitignore` and `.pre-commit-config.yaml` unchanged; `.git/hooks/` still holds only samples.
</verification>

<success_criteria>
- ROADMAP criterion 1 is demonstrated and recorded: the gate passes over the complete returned run
  including Gate 3's single-sha assertion.
- The exact 227-file admitted set is enumerated and proven a strict superset of attempt 1's 209.
- Nothing under `experiments/` was edited, and the production clone is provably unmutated.
</success_criteria>

<output>
Create `.planning/phases/29-gate-verification-results-commit/29-02-SUMMARY.md` when done.
</output>
