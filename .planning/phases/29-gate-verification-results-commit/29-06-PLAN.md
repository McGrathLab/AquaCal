---
phase: 29-gate-verification-results-commit
plan: 06
type: execute
wave: 4
depends_on: ["29-05"]
files_modified:
  - tests/unit/test_experiments_provenance.py
  - .planning/phases/29-gate-verification-results-commit/29-rails-before.txt
  - .planning/phases/29-gate-verification-results-commit/29-rails-after.txt
autonomous: true
requirements: [RUN-03]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The provenance rails are measured against the **committed** tree before any repair, and the measured failure set is recorded node id by node id — D-29-12 requires measurement, not a re-quoted prediction."
    - "`TestResolvedTreeIsObservable` confirms the resolved results tree is named `live`, not `archive`, so the rails are validating this run rather than changing subject to the pre-run baseline."
    - "Every one of the eight failures is repaired by correcting an assertion that encodes an expectation which moved by design — no artifact is edited, and nothing is deselected, xfailed, skipped, or loosened to a tolerance."
    - "`python -m pytest tests/unit/test_experiments_provenance.py -q` reports `0 failed` after the repairs, against `8 failed` before."
    - "The five tree-keyed modules that were already green stay green at `245 passed`."
    - "`python -m pytest tests/` reports exactly `3 failed` after the repairs — D4's three ruled exact-equality anchor misses and nothing else. Zero and four are both anomalies."
    - "The repairs land as a separate `fix(29):` commit, after and distinct from the artifacts commit, touching only `tests/unit/test_experiments_provenance.py`."
  artifacts:
    - .planning/phases/29-gate-verification-results-commit/29-rails-before.txt
    - .planning/phases/29-gate-verification-results-commit/29-rails-after.txt
  key_links:
    - "`_is_tracked()` shells `git ls-files --error-unmatch`, which reads the git index — so the CSV rails discover nothing while the artifacts are merely untracked. The full 8-failure count only exists after plan 29-05's commit, which is why this plan is downstream of it."
    - "`_discover_csv_files()` filters through `_is_tracked()` and `_discover_json_files()` does not — that single asymmetry is what lets a gitignored 2.1 MB `calibration.json` reach a rail whose stated scope is *committed* artifacts."
    - "`resolve_results_dir()` needs no change at all: it repoints itself from `archive` to `live` the moment the tree holds a file. Editing it would be a silent subject switch."
  prohibitions:
    - "Do not deselect, xfail, skip, or loosen any assertion to a tolerance — fix the assertion (D-29-13)."
    - "Do not edit, regenerate or reformat any artifact under `experiments/results*/` to make a rail pass (D-29-08)."
    - "Do not edit `tests/unit/_baseline_paths.py` — `resolve_results_dir()` already does the right thing by itself, and a change there would be a silent subject switch."
    - "Do not add `calibration.json` to `SELF_DESCRIBING_JSON`; it is not committed, so it has no business in a committed-artifact carve-out. Fix the discovery asymmetry instead."
    - "Do not change `_discover_json_files()` from `rglob` to `glob` — the recursive walk is deliberate, because E4's records live one level down in `e4_cells/<cell>/benchmark.json`. Change only the filter."
    - "Do not edit anything inside the frozen `experiments/` tree, including `check_rerun_gates.py`, `_run_manifest.py` and `suite_expectations.json`."
    - "Do not delete a superseded rationale — annotate it in the project's dated `# CORRECTED <date> (plan NN)` style."
    - "Do not amend or rewrite plan 29-05's artifacts commit; these repairs are a separate later commit."
---

<objective>
Repair the eight provenance-rail assertions that the repopulated tree exposes, so RUN-03's test
half lands at the ruled count.

Purpose: D-29-11 already scoped this correctly — the gate is clean, and what is in question is
`tests/unit/test_experiments_provenance.py`, which asserts artifact **metadata** (sha, seed,
environment fields, map coverage) and touches no measured value. A failure there cannot by
construction indicate the numbers are wrong. D-29-12 then demanded the count be measured rather
than re-quoted; research measured it, and D1's prediction proved exactly right: **8 failures, the
same 8 node ids**, every one of them an assertion encoding an expectation that moved by design.
Under D-29-08 and D-29-13, the check is fixed, not the run repeated — and nothing is deselected,
xfailed, skipped or loosened.

Output: a repaired test module, two rail evidence files, and one `fix(29):` commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-gate-verification-results-commit/29-CONTEXT.md
@.planning/phases/29-gate-verification-results-commit/29-RESEARCH.md
@.planning/phases/29-gate-verification-results-commit/29-PATTERNS.md
@.planning/phases/29-gate-verification-results-commit/29-05-SUMMARY.md
@.planning/phases/29.1-post-run-fixes-re-freeze/29.1-VERIFICATION-BAR.md
</context>

## Artifacts this phase produces

Symbols and files **created or modified** by this plan (new symbols — do not treat as drift):

| Artifact | Kind |
|---|---|
| `.planning/phases/29-gate-verification-results-commit/29-rails-before.txt` | new evidence file |
| `.planning/phases/29-gate-verification-results-commit/29-rails-after.txt` | new evidence file |
| a new suite-level-manifest carve-out constant in `tests/unit/test_experiments_provenance.py` | new module constant |
| five new members of `SELF_DESCRIBING_JSON` (the DEGEN-01/DEGEN-02 breakdown JSONs) | new set members |
| two new `CSV_TO_RECORD` keys: `generalization_sweep_per_camera.csv`, `generalization_sweep_per_camera_band.csv` | new dict entries |
| `_discover_json_files()` gains an `_is_tracked` filter | modified existing function |
| one `fix(29):` commit on `results/rerun-freeze-02` | new git object |

## Ordering note — why this plan sits after the commit

`_is_tracked()` shells `git ls-files --error-unmatch`, which reads the **index**. While the
artifacts are merely untracked, `_discover_csv_files()` finds nothing and the CSV rails pass
vacuously. The full 8-failure set therefore only exists once plan 29-05 has staged and committed
the 227 files. Research recommended the same split for a second reason: two commits keep the
artifacts commit byte-pure and reviewable, and keep a test change out of a `results(29):` commit.

**Expected counts, stated so a correct run is not misread as a regression:**

| Point | `test_experiments_provenance.py` | `pytest tests/` |
|---|---|---|
| Before this plan (tree committed) | **8 failed** | **11 failed** (8 D1 + 3 D4) |
| After this plan | **0 failed** | **3 failed** (D4 only) |

**Three is the expected full-suite count. Zero and four are both anomalies** — D4's three
exact-equality anchor misses are ruled on in the `rerun-freeze-02` tag annotation and in
`29.1-PREPUSH-AUDIT.md` §1, and a run reporting zero would mean something silenced them.

<tasks>

<task type="auto">
  <name>Task 1: Measure the rails against the committed tree, before touching anything</name>
  <precondition>Plan 29-05's artifacts commit exists on `results/rerun-freeze-02` and `git status --porcelain` is empty, so `git ls-files --error-unmatch` resolves the artifacts as tracked.</precondition>
  <files>.planning/phases/29-gate-verification-results-commit/29-rails-before.txt</files>
  <read_first>
    - `tests/unit/_baseline_paths.py` (full) — its 64-line module docstring **predicts this phase's situation in terms**. Read it before touching any provenance test. `resolve_results_dir()` prefers the live tree only while it holds a file; `_holds_a_file` uses `rglob` because E4's records live one level down.
    - `tests/unit/test_experiments_provenance.py` lines 1-60, 78-180, 280-420 — the module's constants, discovery helpers and assertion-message idioms.
    - `.planning/phases/29-gate-verification-results-commit/29-RESEARCH.md` — § *The D1 Provenance Rails — MEASURED, not predicted*, including the eight-row diagnosis table and the D4 node ids.
    - `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-VERIFICATION-BAR.md` — the D4 diagnosis and D1's predicted return.
  </read_first>
  <action>
Measure, and write the measurement down before changing a line.

Run, capturing stdout and stderr into
`.planning/phases/29-gate-verification-results-commit/29-rails-before.txt`:

    python -m pytest tests/unit/test_experiments_provenance.py -q -p no:cacheprovider

Record: the summary line, and every failing node id verbatim. Expected: **8 failed, 279 passed,
20 skipped**, with these node ids —

1. `TestEnvironmentPresence::test_every_benchmark_record_has_environment[run_manifest.json]`
2. `TestSeedProvenance::test_every_benchmark_record_carries_a_seed[run_manifest.json]`
3. `TestCsvProvenanceMap::test_all_committed_csvs_have_a_named_record[generalization_sweep_per_camera.csv]`
4. `TestCsvProvenanceMap::test_all_committed_csvs_have_a_named_record[generalization_sweep_per_camera_band.csv]`
5. `TestCsvProvenanceMap::test_multi_seed_band_declares_its_seed_coverage[exp1_band.csv]`
6. `TestCsvProvenanceMap::test_multi_seed_band_declares_its_seed_coverage[exp1_parameter_band.csv]`
7. `TestCsvProvenanceMap::test_multi_seed_band_declares_its_seed_coverage[generalization_sweep_per_camera_band.csv]`
8. `TestSelfDescribingJson::test_schema_versionless_json_set_equals_self_describing_json`

If the measured set differs from these eight in either direction, **record the difference and
report it** — D-29-12's whole point is that the count is measured, not assumed, and a ninth failure
or a missing one is information, not noise.

Also record, in the same file:

- The outcome of `TestResolvedTreeIsObservable::test_the_resolved_tree_is_named`, confirming the
  resolved tree is **`live`**, not `archive`. This exists precisely so nobody records "the rails
  are green" from a run in which the module quietly changed subject to
  `experiments/pre_rerun_baseline/results/` and validated nothing.
- The five tree-keyed control modules' result:
  `python -m pytest tests/unit/test_baseline_paths.py tests/unit/test_expectations.py tests/unit/test_experiments_e3.py tests/unit/test_experiments_e5.py tests/unit/test_experiments_io.py -q` — expected **245 passed**. These are the modules that would reveal a repopulation problem beyond D1.
- The three D4 node ids, confirmed still failing:
  `tests/unit/test_discard_accounting.py::test_matches_frozen_anchor`,
  `tests/unit/test_optim_common.py::TestPerObservationDetailSinks::test_detail_sink_recomputed_geometry_matches_projector`,
  `tests/unit/test_pipeline.py::TestSolverConfigSeedIsInert::test_matches_pre_change_anchor`.

Change no source file in this task.
  </action>
  <verify>
    <automated>grep -qE '8 failed' .planning/phases/29-gate-verification-results-commit/29-rails-before.txt &amp;&amp; grep -q 'run_manifest.json' .planning/phases/29-gate-verification-results-commit/29-rails-before.txt &amp;&amp; grep -q 'generalization_sweep_per_camera_band.csv' .planning/phases/29-gate-verification-results-commit/29-rails-before.txt &amp;&amp; git diff --quiet -- tests/</automated>
  </verify>
  <acceptance_criteria>
    - `29-rails-before.txt` records a pytest summary of `8 failed, 279 passed, 20 skipped` (or the measured values, with any difference from that expectation called out explicitly).
    - All eight node ids listed above appear verbatim in the evidence file.
    - The file records that the resolved results tree is named `live`.
    - The file records `245 passed` for the five tree-keyed control modules.
    - The file records the three D4 node ids as still failing.
    - `git diff --quiet -- tests/` exits 0 — nothing was changed in this task.
  </acceptance_criteria>
  <done>The rail failure set is measured against the committed tree and written down node id by node id, so the repairs that follow are targeted at a measurement rather than at a prediction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair the eight assertions in place, in the module's own idioms</name>
  <precondition>Task 1 recorded the measured failure set, and the artifacts are committed so `_is_tracked()` resolves them.</precondition>
  <files>tests/unit/test_experiments_provenance.py</files>
  <behavior>
    - `run_manifest.json` is no longer swept into the benchmark-record rails, and the carve-out that excludes it names its schema owner in a comment.
    - `generalization_sweep_per_camera.csv` resolves to a named provenance record and its rail passes.
    - `generalization_sweep_per_camera_band.csv` resolves to a named provenance record whose text declares its seed span, so both the map-coverage rail and the seed-coverage rail pass from the one entry.
    - `exp1_band.csv` and `exp1_parameter_band.csv` declare the four-seed span the artifacts actually carry, and their rails pass.
    - The five DEGEN breakdown JSONs are each named in the self-describing carve-out with the record that covers them, and the set-equality rail passes.
    - A gitignored JSON in the working tree is no longer discovered by the JSON rails, matching the CSV rails' existing behaviour, while records nested one directory deep are still discovered.
    - `python -m pytest tests/unit/test_experiments_provenance.py -q` reports `0 failed`.
  </behavior>
  <read_first>
    - `tests/unit/test_experiments_provenance.py` — read in full before editing. Specifically: `SELF_DESCRIBING_JSON` (:78-99) for the per-member-comment carve-out idiom; `SEEDLESS_LEGACY_RECORDS` (:101-134) for the `# CORRECTED <date> (plan NN).` annotate-never-rewrite idiom; `CSV_TO_RECORD` (:136-176) for the parenthesised multi-line entry idiom and the embedded `seeds A-B` literal; `_seed_span()` (:293); `_discover_json_files()` (:307) and `_discover_csv_files()` (:341) side by side; `_is_tracked()` (:313-339) and its docstring; `_schema_versioned_json_files()` (:351).
    - `.planning/phases/29-gate-verification-results-commit/29-PATTERNS.md` — § `tests/unit/test_experiments_provenance.py`, Idioms A through D. These are the exact shapes the repairs must match.
    - `.planning/phases/29-gate-verification-results-commit/29-RESEARCH.md` — § *The D1 Provenance Rails*, the eight-row table's "Cheapest correct fix" column, and Pitfall 5.
    - `experiments/_run_manifest.py` around line 82 — read only, to confirm the suite-level manifest's schema owner before naming it in a comment.
    - `.planning/phases/29-gate-verification-results-commit/29-rails-before.txt` — Task 1's measurement.
  </read_first>
  <action>
Repair all eight, editing **only** `tests/unit/test_experiments_provenance.py`. Every repair
corrects an assertion that encodes an expectation which moved by design. Nothing is deselected,
xfailed, skipped, or loosened to a tolerance, and no artifact is touched.

**Failures 1 and 2 — `run_manifest.json` swept into benchmark-record rails.** `run_manifest.json`
carries `schema_version` but is **flat**: its keys are `aquacal_version_declared`,
`blas_thread_cap`, …, `git_sha`, … with no `environment` block and no `solver_config`. It is
DRIVER-02's suite-level manifest, a different schema from `assemble_benchmark_record`'s output — a
suite-level manifest describes a *run*, not a solve, so it has no seed to carry. Exclude it from
`_schema_versioned_json_files()` via a named carve-out `frozenset` beside `SELF_DESCRIBING_JSON`,
following the same per-member-comment shape, with the comment naming `experiments/_run_manifest.py`
line 82 as its schema owner. One carve-out resolves both failures.

**Failure 3 — `generalization_sweep_per_camera.csv` has no `CSV_TO_RECORD` entry.** It is a new E6
artifact. Add an entry naming `experiments/results/e6_provenance.json`, in the established
value style: a full repo-relative path plus prose naming what introduced it.

**Failures 4 and 7 — `generalization_sweep_per_camera_band.csv` has no entry.** New E6 band
artifact. Add an entry naming `experiments/results/e6_seed_band_provenance.json` **and containing
the literal seed-span string `seeds 42-47`**, because `test_multi_seed_band_declares_its_seed_coverage`
matches that substring against `_seed_span()` computed from the data — the artifact spans six seeds,
42 through 47. Omitting the span makes failure 7 replace failure 4 rather than resolving it. One
entry resolves both.

**Failures 5 and 6 — `exp1_band.csv` and `exp1_parameter_band.csv` declare a stale seed span.**
The artifacts span **four** seeds, 42 through 45; the map text still says `seeds 42-51`. This is
**Ruling A1 of 2026-08-15** (`run_experiment_suite.sh:1452`), which cut E1's band from ten seeds to
four. Update both entries' span text to `seeds 42-45`, and annotate the change inside the entry's
prose in the project's `# CORRECTED <date> (plan 29-06).` style — annotate the superseded
rationale, never delete it. Four is correct; the prose is the survivor, and it is the field whose
whole job is stating what a published number may be quoted over.

**Failure 8, part (a) — five extra schema-versionless JSONs.** Add `e1_degeneracy_breakdown.json`,
`e5_degeneracy_breakdown.json`, `e5_seed_band_degeneracy_breakdown.json`,
`e7_degeneracy_breakdown.json` and `e7_seed_band_degeneracy_breakdown.json` to
`SELF_DESCRIBING_JSON`, each with its own comment naming the record that covers it. All five are
new from DEGEN-01/DEGEN-02 and were added deliberately.

**Failure 8, part (b) — the discovery asymmetry.** `_discover_csv_files()` filters through
`_is_tracked()`; `_discover_json_files()` does not. That is the only reason a gitignored 2.1 MB
`calibration.json` reaches a rail whose stated scope is *"committed under `experiments/results/`"*.
Give the JSON helper the same `_is_tracked` filter its CSV sibling already has. **Preserve
`rglob`** — the recursive walk is deliberate, because E4's records live one level down in
`e4_cells/<cell>/benchmark.json`; change only the filter, not the walk. Extend `_is_tracked()`'s
existing docstring with the JSON case rather than writing a second rationale beside it — its
current text (*"The CSV suite documents its scope as files 'committed under
experiments/results/', but discovers them by globbing the working tree…"*) already applies word for
word.

**Do not resolve failure 8 by adding `calibration.json` to `SELF_DESCRIBING_JSON`.** It is not
committed, so it has no business in a committed-artifact carve-out; that would paper over the
asymmetry rather than fix it.

**Do not edit `tests/unit/_baseline_paths.py`.** `resolve_results_dir()` already repoints itself
from `archive` to `live` the moment the tree holds a file — that is the module's entire design, and
a change there would be a silent subject switch.

Any assertion message added or reworded matches Idiom D: name the offending filename, say what is
wrong, give the two available remedies, and carry a requirement or task id in parentheses. Keep
lines inside ruff's 88-column limit using implicit string concatenation inside parentheses, as
`CSV_TO_RECORD` already does.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_provenance.py -q -p no:cacheprovider &amp;&amp; ruff check tests/unit/test_experiments_provenance.py &amp;&amp; ruff format --check tests/unit/test_experiments_provenance.py &amp;&amp; git diff --quiet -- experiments/ tests/unit/_baseline_paths.py</automated>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: xfail, mark.skip, deselect -->
    - `python -m pytest tests/unit/test_experiments_provenance.py -q` reports `0 failed`.
    - `git diff --name-only` lists exactly one file: `tests/unit/test_experiments_provenance.py`.
    - `git diff -- tests/unit/test_experiments_provenance.py | grep -cE '^\+.*(xfail|mark\.skip|deselect)'` returns `0` — no new skip or xfail marker was introduced.
    - `git diff --quiet -- tests/unit/_baseline_paths.py` exits 0.
    - `git diff --quiet -- experiments/` exits 0 — no artifact and no frozen-tree file was touched.
    - `grep -c 'seeds 42-45' tests/unit/test_experiments_provenance.py` returns at least `2`, and `grep -c 'seeds 42-47' tests/unit/test_experiments_provenance.py` returns at least `1`. (Deliberately **not** gated on the absence of the superseded ten-seed string: the annotate-never-rewrite idiom keeps the prior span inside the entry's prose, and `test_multi_seed_band_declares_its_seed_coverage` matches the correct span as a substring of that value, so the rail passing is the real proof.)
    - `python -m pytest 'tests/unit/test_experiments_provenance.py::TestCsvProvenanceMap' -q` reports `0 failed`.
    - `grep -c 'generalization_sweep_per_camera' tests/unit/test_experiments_provenance.py` returns at least `2`.
    - `grep -c 'calibration.json' tests/unit/test_experiments_provenance.py` is unchanged from its pre-repair value — `calibration.json` was not added to any carve-out.
    - `grep -c 'rglob' tests/unit/test_experiments_provenance.py` is unchanged — the JSON walk stayed recursive.
    - The diff shows `_discover_json_files()` gaining an `_is_tracked` filter and `_is_tracked()`'s docstring extended with the JSON case.
    - All five DEGEN breakdown filenames appear in the diff as new carve-out members, each with a comment naming its covering record.
    - `ruff check` and `ruff format --check` on the file both exit 0.
  </acceptance_criteria>
  <done>All eight rails pass because eight stale assertions now state what the artifacts actually are — with no artifact edited, nothing silenced, and every superseded rationale annotated rather than deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Land the ruled full-suite count and commit the repairs separately</name>
  <precondition>Task 2 left `tests/unit/test_experiments_provenance.py` as the only modified file and the provenance module green.</precondition>
  <files>.planning/phases/29-gate-verification-results-commit/29-rails-after.txt, tests/unit/test_experiments_provenance.py</files>
  <read_first>
    - `.planning/phases/29-gate-verification-results-commit/29-rails-before.txt` — Task 1's baseline, to state the before/after delta.
    - `.planning/phases/29.1-post-run-fixes-re-freeze/29.1-PREPUSH-AUDIT.md` §1 — the D4 ruling, which is what licenses `3 failed` as the correct full-suite outcome rather than a defect.
    - `.planning/phases/29-gate-verification-results-commit/29-CONTEXT.md` — D-29-14 (the fall-through rule) and D-29-15 (three is the expected count).
  </read_first>
  <action>
Run the full suite once and commit.

    python -m pytest tests/ -q

Expect **exactly `3 failed`** — D4's three ruled exact-equality anchor misses and nothing else,
against `2407 passed, 26 skipped`. Capture the summary and the failing node ids into
`.planning/phases/29-gate-verification-results-commit/29-rails-after.txt`, alongside Task 1's
before-figures, so the delta reads `11 failed -> 3 failed` and the provenance module reads
`8 failed -> 0 failed` in one place.

**Three is the expected count. Zero and four are both anomalies.** A zero would mean the three
anchors were silenced; a four would mean something new. If the count is not three, record the
measured value and the node ids and report — do not adjust anything to reach three.

The three D4 failures are pre-existing exact-equality anchor misses (1 ULP to 1.4e-9), proven by
byte-identity of every input to `rerun-freeze-01`, and they are the first Linux evaluation of
anchors captured on Windows. They are ruled on in the `rerun-freeze-02` tag annotation and in
`29.1-PREPUSH-AUDIT.md` §1. They stay as ruled; they are not repaired here.

If any failure remains that is **neither** an assertion encoding an expectation that moved **nor**
cheaply fixable, do not fix it and do not let it block: record it under D-29-14 as a
post-submission todo naming what fails and what would fix it, and hand it to plan 29-08 to file.

Then commit, separately from plan 29-05's artifacts commit:

    git commit tests/unit/test_experiments_provenance.py -m "fix(29): ..."

A `fix(29):` subject, with a body that states: eight assertions repaired, which four idioms they
fall into, that each encodes an expectation that moved by design (Ruling A1's four-seed E1 band,
DEGEN-01/DEGEN-02's five new breakdown JSONs, DRIVER-02's flat suite-level manifest, and the
JSON/CSV discovery asymmetry), that no artifact was edited and nothing was deselected, xfailed,
skipped or loosened, and the before/after counts `8 -> 0` on the module and `11 -> 3` on the full
suite with D4's three named as ruled. Push to `origin` on `results/rerun-freeze-02`.

Do not amend plan 29-05's commit and do not open a pull request into `main`.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q 2>&amp;1 | tee /dev/stderr | grep -qE '\b3 failed\b' &amp;&amp; git log -1 --pretty=%s | grep -q '^fix(29)' &amp;&amp; test $(git show --name-only --format="" HEAD | wc -l) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -q` reports exactly `3 failed`, and the three node ids are `tests/unit/test_discard_accounting.py::test_matches_frozen_anchor`, `tests/unit/test_optim_common.py::TestPerObservationDetailSinks::test_detail_sink_recomputed_geometry_matches_projector`, and `tests/unit/test_pipeline.py::TestSolverConfigSeedIsInert::test_matches_pre_change_anchor`.
    - `29-rails-after.txt` records both deltas in one place: `8 failed -> 0 failed` on the provenance module and `11 failed -> 3 failed` on the full suite, and states that three is the expected count with zero and four both anomalies.
    - `29-rails-after.txt` records `245 passed` for the five tree-keyed control modules, unchanged.
    - `git log -1 --pretty=%s` matches `^fix(29):`.
    - `git show --name-only --format="" HEAD | wc -l` returns `1`, and that path is `tests/unit/test_experiments_provenance.py`.
    - `git log -2 --pretty=%s` shows the `fix(29):` commit strictly after the `results(29):` artifacts commit, and the artifacts commit's hash is unchanged from plan 29-05.
    - `git status --porcelain` is empty.
    - Any residual finding that is neither assertion-wrong nor cheaply fixable is written into `29-rails-after.txt` with what fails and what would fix it, for plan 29-08 to file as a post-submission todo.
  </acceptance_criteria>
  <done>The suite lands at its ruled count of three, the repairs are a separate reviewable commit, and the artifacts commit is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test assertions → published claim | These rails are the standing guarantee that every committed artifact carries a sha, a seed and a named provenance record; weakening one weakens the guarantee silently. |
| working tree (gitignored output) → committed-artifact rail | A discovery helper without a tracked-file filter lets uncommitted working-tree output enter a rail whose stated scope is committed artifacts. |
| git index → rail subject | `_is_tracked()` reads the index, and `resolve_results_dir()` reads the filesystem — both can flip what the module is validating. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-37 | Tampering (of the guarantee) | the eight assertions | high | mitigate | Each repair states what the artifacts actually are; a diff gate asserts no new skip or xfail marker was introduced, and the full-suite count is pinned at the ruled three rather than at zero. |
| T-29-38 | Tampering | `experiments/results*/` artifacts | high | mitigate | `git diff --quiet -- experiments/` asserted; D-29-08 fixes the check, never the run. |
| T-29-39 | Spoofing | rail subject (`live` vs `archive`) | high | mitigate | `TestResolvedTreeIsObservable` recorded as naming `live` before any repair, so a vacuous green against the pre-run baseline cannot be recorded as a pass. |
| T-29-40 | Information disclosure | gitignored `calibration.json` (2.1 MB) reaching a committed-artifact rail | medium | mitigate | The discovery asymmetry is fixed at the helper, not papered over by a carve-out; `rglob` preserved so nested records stay discoverable. |
| T-29-41 | Repudiation | superseded rationales | medium | mitigate | Every corrected entry annotates in the dated `# CORRECTED <date> (plan NN)` style rather than deleting the prior reasoning, matching the project's annotate-never-rewrite rule. |
| T-29-42 | Tampering | plan 29-05's artifacts commit | high | mitigate | A separate `fix(29):` commit touching exactly one file; the artifacts commit hash is asserted unchanged and amending is prohibited. |
| T-29-43 | Denial of service | none | low | accept | No network, no credential, no external service in this plan. |
</threat_model>

<verification>
- `29-rails-before.txt` records the measured 8 failures with all eight node ids, the `live`
  resolution, `245 passed` on the control modules, and the three D4 node ids.
- `python -m pytest tests/unit/test_experiments_provenance.py -q` reports `0 failed`.
- `python -m pytest tests/` reports exactly `3 failed`, all three D4 anchors.
- The diff touches only `tests/unit/test_experiments_provenance.py`; `_baseline_paths.py` and
  everything under `experiments/` unchanged; no new skip or xfail marker.
- `seeds 42-51` is gone; `seeds 42-45` appears at least twice; `seeds 42-47` at least once;
  `rglob` count unchanged; `calibration.json` not added to any carve-out.
- One `fix(29):` commit, one file, strictly after the `results(29):` commit, which is unchanged.
- `ruff check` and `ruff format --check` pass on the modified file.
</verification>

<success_criteria>
- RUN-03's test half lands at the ruled count: provenance rails green, full suite at exactly three
  D4 failures.
- Every repair fixed an assertion, not an artifact, and nothing was silenced.
- The artifacts commit stayed byte-pure and untouched.
</success_criteria>

<output>
Create `.planning/phases/29-gate-verification-results-commit/29-06-SUMMARY.md` when done.
</output>
