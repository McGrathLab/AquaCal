---
phase: 19.1-experiment-suite-consolidation
plan: 07
type: execute
wave: 5
depends_on: [19.1-04, 19.1-05, 19.1-06]
files_modified:
  - tests/synthetic/experiments.py
  - tests/synthetic/compare_refractive.py
  - docs/tutorials/01_full_pipeline.ipynb
  - docs/tutorials/02_synthetic_validation.ipynb
  - .planning/architecture.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONCERNS.md
  - experiments/README.md
  - .github/workflows/test.yml
autonomous: true
requirements: [EXP-02, EXP-03, EXP-06]

must_haves:
  truths:
    - "tests/synthetic/experiments.py is gone, and the SUMMARY carries a keep/drop verdict for all 20 of its top-level defs — 'salvage anything useful' is not auditable, a table is (D-13)"
    - "compare_refractive.py is gone; experiments/e1_refractive_comparison.py is E1's CLI entry point (D-13)"
    - "No two implementations of the same experiment remain: the export cells are deleted from BOTH notebooks, so there is exactly one producer of every committed CSV on the day the phase closes (D-12, EXP-03/SC4)"
    - "The notebook edits are narrowly scoped to the export cells only — no RIG_SIZE change, no import rewrite, no Colab-caveat removal, no re-execution; those are Phase 21's X4 (D-12)"
    - "Stale path references are swept only AFTER the moves, so the docs never describe a layout that does not exist yet (X6, RESEARCH sequencing)"
    - "experiments/README.md maps one command to each paper artifact with its expected runtime, so a SoftwareX reviewer following the reproducibility path never has to guess which script made which number (Part 4 Rule 3)"
    - "The README carries the gauge-freedom explanation, including why the reference camera's xy_position_error_mm reads exactly 0.0 — a reviewer reading the CSV will ask (D-07)"
    - "--smoke runs in CI on ubuntu-latest + Python 3.12 only, and E2's smoke reports a VISIBLE SKIPPED with a reason when the Zenodo dataset is absent (D-25)"
  artifacts:
    - path: "experiments/README.md"
      provides: "provenance table, one command per artifact, expected runtimes, gauge explanation, E7 fixed-intrinsics framing"
      contains: "gauge"
      min_lines: 90
    - path: ".github/workflows/test.yml"
      provides: "the experiments-smoke job"
      contains: "experiments-smoke"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "python -m experiments.eN --smoke"
      via: "new single-platform CI job"
      pattern: "--smoke"
    - from: "experiments/README.md"
      to: "experiments/results/*.csv"
      via: "provenance table rows"
      pattern: "exp1_parameter_errors.csv"
---

<objective>
Close out the consolidation: delete `tests/synthetic/experiments.py` with an auditable keep/drop
table covering all 20 of its defs, delete `compare_refractive.py`, delete the export cells from both
notebooks, sweep the stale path references, write `experiments/README.md`'s provenance table, and
wire `--smoke` into CI.

Purpose: EXP-03's "no two implementations" is a phase gate. Porting E1 out of notebook 02 while
leaving notebook 02's export cell in place would leave two producers of `exp{1,2,3}*.csv` on the day
the phase closes. Everything in this plan is ordered strictly after E1's comparison, per D-13/D-19.
Output: three deletions, two surgical notebook edits, four doc sweeps, a README, and a CI job.

**Precondition — check before starting.** Read
`.planning/phases/19.1-experiment-suite-consolidation/19.1-E1-REPRODUCTION.md` § Deletion gate. If
it does not say the deletions are cleared, STOP and report; do not delete anything. D-13's deletion
is ordered after E1's comparison passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**PYTHONPATH:** in a worktree, `export PYTHONPATH="$(pwd)/src"` before pytest.
**Notebook editing:** `nbsphinx_execute = "never"`, so the committed outputs are what the docs site
serves and they are per-cell. Deleting a cell is mechanical and cannot invalidate the other cells'
stored outputs. Do NOT re-execute either notebook.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-CONTEXT.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-PATTERNS.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-RESEARCH.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-04-SUMMARY.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-05-SUMMARY.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-06-SUMMARY.md

<interfaces>
<!-- Verified live 2026-07-25. -->

The 20 top-level defs of `tests/synthetic/experiments.py` (RESEARCH § Open Questions, OQ3
resolution — the SOURCE-BRIEF's "21" was off by one; this enumeration is from
`grep -n "^def "` and is authoritative). Composition: 3 runners, 13 plot helpers, 3 wide-schema CSV
writers, 1 `assemble_summary`:
   38 run_experiment_1                    (runner)
  102 _plot_focal_length_error            (plot)
  147 _plot_camera_xy_positions           (plot)
  256 _plot_camera_z_error                (plot)
  299 _plot_distortion_error              (plot)
  360 _save_parameter_errors_csv          (CSV, wide schema — the superseded one)
  400 run_experiment_2                    (runner)
  585 _plot_signed_error_vs_depth         (plot)
  630 _plot_rmse_vs_depth                 (plot)
  674 _plot_scale_factor_vs_depth         (plot)
  719 _plot_xy_heatmaps                   (plot)
  799 _plot_single_xy_heatmap             (plot)
  847 _save_depth_metrics_csv             (CSV)
  896 run_experiment_3                    (runner)
 1094 _plot_exp3_rmse_vs_depth            (plot)
 1134 _plot_exp3_focal_error_vs_depth     (plot)
 1174 _plot_exp3_z_error_vs_depth         (plot)
 1214 _plot_exp3_xy_heatmaps              (plot)
 1281 _save_exp3_depth_scaling_csv        (CSV)
 1330 assemble_summary                    (summary — the ONLY genuine keep candidate, D-13/X2)

Export cells to delete (D-12), verified live:
  docs/tutorials/02_synthetic_validation.ipynb — cell id `e3jl81minof` (40 cells total). Identify it
    by its `id` field. It builds `_mz`, `rows_exp1`, `rows_exp2`, `rows_spatial`, `rows_exp3` and
    writes four CSVs to `OUTPUT_DIR`.
  docs/tutorials/01_full_pipeline.ipynb — **this notebook's cells carry no `id` field.** The export
    cell is at 0-based index 26 (28 cells total); identify it by content, whose first lines are
    `OUTPUT_DIR.mkdir(parents=True, exist_ok=True)` / `cam_names = sorted(result.cameras.keys())` /
    `# --- Camera parameters (backs rig geometry, intrinsics, and interface plots) ---` /
    `rows_cameras = []`. It is the only cell in that notebook containing `to_csv`.

X6 stale-path locations, all verified live:
  .planning/architecture.md:256                 — describes tests/synthetic/compare_refractive.py as the runner
  .planning/codebase/STRUCTURE.md:74-75         — lists compare_refractive.py / experiment_helpers.py under tests/synthetic/
  .planning/codebase/STRUCTURE.md:167           — `pytest tests/synthetic/` or `python tests/synthetic/compare_refractive.py`
  .planning/codebase/STRUCTURE.md:356           — "Helpers: Add to tests/synthetic/experiment_helpers.py"
  .planning/codebase/CONCERNS.md:21-34          — stale evaluate_reconstruction missing-import claim; DELETE the entry
                                                  (the file it describes is being deleted wholesale, making it moot)
  .planning/phases/19-*/*.md                    — NO CHANGE. Historical phase records are not live
                                                  documentation; X6 sweeps only forward-looking docs.

CI job template — the existing `pre-commit` job in `.github/workflows/test.yml` (lines 42-57) is the
model for a single-platform narrow job. The full `test` job is a 2x3 matrix
(ubuntu/windows x 3.10/3.11/3.12) and must not be extended (D-25: one platform is sufficient, six
are waste). Use the `test` job's install step (`pip install -e ".[dev]"`), not `pre-commit`'s bare
install.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete experiments.py and compare_refractive.py with an auditable 20-def keep/drop table</name>
  <files>tests/synthetic/experiments.py, tests/synthetic/compare_refractive.py</files>
  <read_first>
    - `.planning/phases/19.1-experiment-suite-consolidation/19.1-E1-REPRODUCTION.md` § Deletion gate
      — the PRECONDITION. Do not proceed unless it says cleared.
    - `tests/synthetic/experiments.py` — run `grep -n "^def " tests/synthetic/experiments.py` and
      confirm it matches the 20-def enumeration in `<interfaces>` exactly. If the count or names
      differ, the enumeration is stale and the table must be rebuilt from the live grep.
    - `tests/synthetic/experiments.py` lines 1320-1400 — read `assemble_summary`'s FULL body. It is
      the only genuine keep candidate (D-13/X2), and RESEARCH explicitly did not read it. This is
      the one def whose verdict requires reading rather than defaulting.
    - `tests/synthetic/compare_refractive.py` — the full file (its `main()` and its
      `from tests.synthetic.experiments import ...` block at line 18).
    - `19.1-CONTEXT.md` D-13 in full.
    - `19.1-RESEARCH.md` § "The 21-def audit" and § Open Questions OQ3 resolution.
    - `19.1-RESEARCH.md` § State of the Art — the confirmation that the wide-format CSV schema feeds
      nothing downstream (grepped the figures repo; no consumer found).
  </read_first>
  <action>
First, verify the enumeration: `grep -n "^def " tests/synthetic/experiments.py` must yield exactly
the 20 names in `<interfaces>`. Record the live output in the SUMMARY.

Then read `assemble_summary`'s full body and decide its verdict on evidence, not by default. Keep it
ONLY if `experiments/e1_refractive_comparison.py` genuinely needs a console/README summary-table
builder that it does not already have. E1's own print statements and `experiments/README.md` cover
the human-readable-summary role, so the expected verdict is DROP — but state the reason from the
body you read, not from this plan. If the verdict is KEEP, port it into
`experiments/e1_refractive_comparison.py` (not into `src/`) before deleting the file, and note the
port in the SUMMARY.

Then `git rm tests/synthetic/experiments.py` and `git rm tests/synthetic/compare_refractive.py`.
E1's CLI entry point is now `python -m experiments.e1_refractive_comparison` (D-13).

The default for every other def is DROP, with these standing reasons, which the table must state
per row rather than gesture at:
  - the 3 runners are superseded by the notebook's implementation, which is itself now superseded by
    `experiments/e1_refractive_comparison.py`;
  - the 13 `_plot_*` helpers are out of scope for `experiments/` under P1 (measurement and
    presentation are separate programs; plotting belongs to `DissertationFigures`);
  - the 3 `_save_*_csv` writers emit a WIDE schema (`..._refr` / `..._nonrefr` suffix columns) that
    appears nowhere downstream, while the committed CSVs are long-format — verified by grepping the
    figures repo for a consumer of the wide schema and finding none.

Write the full 20-row keep/drop table into the SUMMARY with columns
`# | Def | Kind | Verdict | Reason`. All 20 rows are mandatory. "Salvage anything useful" is not
auditable; a table is (D-13).

Do NOT delete `tests/synthetic/experiment_helpers.py` — plan 19.1-01 already reduced it to a
re-export shim and it stays. Do NOT delete `tests/synthetic/ground_truth.py`,
`tests/synthetic/test_full_pipeline.py`, or `tests/synthetic/test_per_camera_interface.py`.
  </action>
  <verify>
    <automated>python -m pytest tests/ -m "not slow" -q</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f tests/synthetic/experiments.py` and `! test -f tests/synthetic/compare_refractive.py`.
    - `test -f tests/synthetic/experiment_helpers.py` and `test -f tests/synthetic/ground_truth.py` still pass.
    - `! grep -rn "tests\.synthetic\.experiments\b\|tests/synthetic/experiments\.py\|compare_refractive" --include=*.py --include=*.yml --include=*.toml --include=*.cfg . | grep -v "^\./\.planning/"` — no live reference remains outside `.planning/`.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 with no collection error.
    - `python -m pytest tests/synthetic/ -m "not slow" -q` exits 0.
    - The SUMMARY contains a table with exactly 20 def rows, each carrying a Verdict of KEEP or DROP and a Reason.
    - `assemble_summary`'s row states a reason derived from reading its body.
    - `ruff check . && ruff format --check .` exits 0.
  </acceptance_criteria>
  <done>Both files are deleted with `git rm`, no live reference to either remains outside `.planning/`, the full suite still collects and passes, and the SUMMARY carries a 20-row keep/drop table whose `assemble_summary` verdict is evidence-based.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the two notebook export cells and sweep the stale path references</name>
  <files>docs/tutorials/01_full_pipeline.ipynb, docs/tutorials/02_synthetic_validation.ipynb, .planning/architecture.md, .planning/codebase/STRUCTURE.md, .planning/codebase/CONCERNS.md</files>
  <read_first>
    - `docs/tutorials/02_synthetic_validation.ipynb` cell `e3jl81minof` — read it in full to confirm
      it is the export cell (it builds `_mz`, `rows_exp1`, `rows_exp2`, `rows_spatial`, `rows_exp3`
      and writes four CSVs) and that no later cell depends on any variable it defines.
    - `docs/tutorials/01_full_pipeline.ipynb` cell at 0-based index 26 — read it in full to confirm
      the content signature in `<interfaces>` and that no later cell depends on `rows_cameras`,
      `rows_residuals`, or whatever else it defines. Cells in this notebook have no `id` field, so
      identify by index AND content.
    - `19.1-CONTEXT.md` D-12 in full — especially the explicit NOT-in-scope list.
    - `.planning/architecture.md` around line 256; `.planning/codebase/STRUCTURE.md` around lines
      68-79, 160-174, 348-358; `.planning/codebase/CONCERNS.md` lines 15-36. Read the surrounding
      context, not just the cited lines, so the replacement text reads coherently.
    - `19.1-RESEARCH.md` § "X6 stale-path sweep — exact locations".
    - `docs/conf.py` — confirm `nbsphinx_execute = "never"` before touching a notebook, so you know
      the stored outputs are what the docs site serves.
  </read_first>
  <action>
**Notebook edits — export cells only.** Delete exactly one cell from each notebook, by editing the
`.ipynb` JSON programmatically (load with `json`, drop the cell from `cells`, dump with
`indent=1` and a trailing newline to match nbformat's convention). Verify the cell count drops by
exactly one per notebook and that nothing else in the JSON changed — diff the two files and confirm
the diff contains only the removed cell.

  - `02_synthetic_validation.ipynb`: remove the cell whose `id` is `e3jl81minof`. 40 cells -> 39.
  - `01_full_pipeline.ipynb`: remove the cell at 0-based index 26, after asserting its source starts
    with `OUTPUT_DIR.mkdir(parents=True, exist_ok=True)` and contains `rows_cameras`. 28 cells -> 27.
    Do not trust the index alone — assert the content signature first, because an earlier edit to the
    notebook would shift it.

Before deleting either cell, confirm no LATER cell in the same notebook references a variable the
export cell defines. If one does, report it rather than breaking the notebook's narrative.

Explicitly NOT in scope (D-12) — do not touch any of these, they are Phase 21's X4:
  - `RIG_SIZE = "small"` pin
  - the import rewrite to `aquacal.datasets.pipelines`
  - dropping the full-clone Colab caveat
  - any re-execution of either notebook, and any change to any stored output cell
Do not reformat the notebook JSON beyond what removing one cell requires. Do not touch the other
notebooks under `docs/tutorials/`.

**X6 stale-path sweep — strictly after the moves, which have all landed by now:**
  - `.planning/architecture.md` around line 256: it describes
    `tests/synthetic/compare_refractive.py` as the runner. Update to
    `experiments/e1_refractive_comparison.py`, invoked as
    `python -m experiments.e1_refractive_comparison`.
  - `.planning/codebase/STRUCTURE.md` around lines 74-75: `compare_refractive.py` is gone and
    `experiment_helpers.py` is now a re-export shim — update both entries and add the
    `experiments/` package to the structure listing with its actual contents (`__init__.py`,
    `README.md`, `_io.py`, `_render.py`, `e1_refractive_comparison.py`, `e2_real_rig.py`,
    `e4_benchmark_grid.py`, `e7_interface_ablation.py`, `results/`). Note that `benchmarks/` no
    longer exists.
  - `.planning/codebase/STRUCTURE.md` around line 167: replace
    `python tests/synthetic/compare_refractive.py` with the `python -m experiments.*` invocations.
  - `.planning/codebase/STRUCTURE.md` around line 356: "Helpers: Add to
    `tests/synthetic/experiment_helpers.py`" is now wrong — helpers go in
    `src/aquacal/datasets/pipelines.py` if they compute (P2) or `experiments/_io.py` if they do I/O
    (P3).
  - `.planning/codebase/CONCERNS.md` lines 21-34: DELETE the entry. It describes a missing-import
    concern in a file that no longer exists, so it is moot regardless of whether the claim was ever
    accurate.
  - Do NOT edit `.planning/phases/19-*/*.md` or any other phase history. Historical phase records
    are not live documentation.
  </action>
  <verify>
    <automated>python -c "
import json
a=json.load(open('docs/tutorials/01_full_pipeline.ipynb',encoding='utf-8'))
b=json.load(open('docs/tutorials/02_synthetic_validation.ipynb',encoding='utf-8'))
assert len(a['cells'])==27, len(a['cells'])
assert len(b['cells'])==39, len(b['cells'])
assert not any('to_csv' in ''.join(c.get('source',[])) for c in a['cells']), 'export cell still in nb01'
assert not any(c.get('id')=='e3jl81minof' for c in b['cells']), 'export cell still in nb02'
assert not any('rows_exp1' in ''.join(c.get('source',[])) for c in b['cells']), 'export logic still in nb02'
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/tutorials/01_full_pipeline.ipynb` has exactly 27 cells and no cell containing `to_csv`.
    - `docs/tutorials/02_synthetic_validation.ipynb` has exactly 39 cells, no cell with id `e3jl81minof`, and no cell containing `rows_exp1`.
    - `git diff --stat docs/tutorials/` shows changes confined to those two files.
    - `grep -c "RIG_SIZE = \"large\"" docs/tutorials/02_synthetic_validation.ipynb` is unchanged from the base commit — X4's pin was NOT applied.
    - `grep -c "tests.synthetic.ground_truth" docs/tutorials/02_synthetic_validation.ipynb` is unchanged — the import rewrite was NOT applied.
    - `python -c "import json,nbformat; [nbformat.validate(json.load(open(p,encoding='utf-8'))) for p in ['docs/tutorials/01_full_pipeline.ipynb','docs/tutorials/02_synthetic_validation.ipynb']]"` exits 0 — both notebooks are still valid nbformat.
    - `sphinx-build -W docs docs/_build/html` exits 0 (the merged docs build, which is where a per-plan-green build previously broke in Phase 18).
    - `! grep -rn "compare_refractive\|tests/synthetic/experiment_helpers\.py" .planning/architecture.md .planning/codebase/` — the sweep is complete.
    - `grep -c "experiments/" .planning/codebase/STRUCTURE.md` is greater than 0 — the new layout is documented.
    - `! grep -rn "benchmarks/" .planning/codebase/STRUCTURE.md`.
    - `git diff --stat .planning/phases/` is empty — no phase history was edited.
  </acceptance_criteria>
  <done>Exactly one cell is removed from each notebook, both remain valid nbformat with all other stored outputs untouched, X4's out-of-scope edits were not applied, the merged `sphinx-build -W` passes, and all four X6 locations plus the CONCERNS.md entry are swept with no phase history touched.</done>
</task>

<task type="auto">
  <name>Task 3: Write experiments/README.md and add the experiments-smoke CI job</name>
  <files>experiments/README.md, .github/workflows/test.yml</files>
  <read_first>
    - `19.1-SOURCE-BRIEF.md` Part 3 — the provenance map table. It is the table the README should
      carry, restricted to the artifacts this phase actually produced (E1, E2, E7, plus the
      relocated E4 skeleton). Note that its row "real-rig calibration JSON -> zenodo_e2e.py" is
      WRONG and corrected by D-14: the figure generator reads three CSVs, never `calibration.json`.
      The README must carry the corrected provenance.
    - `19.1-SOURCE-BRIEF.md` Part 4 rules 3 and 5.
    - `19.1-RESEARCH.md` § "Docstring text to carry into pipelines.py's gauge_correct_z keyword" —
      the pre-approved gauge wording. The README restates it (D-07 requires it in BOTH the docstring
      and the README).
    - `19.1-RESEARCH.md` § "Runtime and Sequencing Reality" — the runtime table. Replace E7's
      unmeasured 20-40 min estimate with the measured value from `19.1-05-SUMMARY.md`, and E1's/E2's
      estimates with the measured values from `19.1-06-SUMMARY.md` / `19.1-04-SUMMARY.md`.
    - `.planning/phases/19.1-experiment-suite-consolidation/19.1-05-PLAN.md`
      § `<pitfall_3_resolution>` — the paragraph the README must carry about what "fixed intrinsics"
      means in E7 and why a best-case bound is the stronger demonstration.
    - `.github/workflows/test.yml` — the full file. The `pre-commit` job (lines 42-57) is the
      single-platform template; the `test` job's install step is the one to copy.
    - `19.1-CONTEXT.md` D-02 (the layout), D-25 (the CI job's scope and E2's visible SKIPPED).
    - The three phase SUMMARYs from wave 3 and 4 for the measured runtimes and the actual emitted
      filenames.
  </read_first>
  <action>
**`experiments/README.md`** — write it fresh (no in-repo precedent for a README-as-provenance-table).
Required sections:

  1. **What this directory is** — one paragraph: paper experiment scripts, not part of the installed
     package (`pyproject.toml` scopes `packages.find` to `where=["src"]`, so `experiments/` never
     ships), invoked as `python -m experiments.eN_name`, linted but not pytest-collected (its tests
     live in `tests/unit/test_experiments_*.py`).
  2. **The uniform CLI contract** — the five flags with their defaults and semantics, including that
     `--check` and `--force` are mutually exclusive, that `--check` compares numerically at each
     script's declared `CHECK_RTOL` rather than byte-for-byte, and why (a strict byte compare across
     platforms fails on BLAS last-digit differences, and the predictable result is that the check
     gets disabled — which is the failure the determinism rule exists to prevent).
  3. **Provenance table** — one row per committed artifact with columns
     `Paper artifact | Experiment | Command | Output file | Figure generator | Runtime`. Cover every
     artifact this phase produced: the three `exp{1,2,3}` CSVs and `exp2_spatial_errors.csv` (E1),
     `camera_parameters.csv` / `reconstruction_errors.csv` / `reprojection_residuals.csv` /
     `real_rig_metrics.json` / `benchmark.json` / `calibration.json` (E2), `interface_ablation.csv` /
     the conditioning JSON+NPZ / the four trace CSVs (E7), and the per-arm/per-model
     `benchmark.json`-shaped records. Use the CORRECTED provenance from D-14 for the E2 rows: the
     figure generator reads the three CSVs, not `calibration.json`. Mark `e4_benchmark_grid.py` as a
     relocated skeleton whose grid is run in Phase 19.2 (EXP-08), so a reader does not run it
     expecting results. Give each runtime as the MEASURED value from the wave-3/4 SUMMARYs, not the
     pre-run estimate.
  4. **The gauge-freedom correction** — restate the pre-approved explanation (D-07 requires it in the
     README as well as the docstring), and state explicitly why the reference camera's
     `xy_position_error_mm` reads exactly `0.0` (it is pinned at the world origin, a geometric fact
     with zero optimizable freedom) while its `z_position_error_mm` after correction is a small
     NONZERO residual. A reviewer reading the CSV will ask.
  5. **What "fixed intrinsics" means in E7** — the paragraph from plan 19.1-05's
     `<pitfall_3_resolution>`: `intrinsics_source = ground_truth_fixed`, this is a best-case bound
     rather than the CalibMar-import scenario, and a degeneracy demonstrated under best-case
     conditions is the stronger claim because noise cannot be blamed for it.
  6. **How to read E7's output** — the failure is height/distance (`C_z_i` against `water_z_i`; only
     the sum, the absolute surface height, is physically meaningful and must be common to all
     cameras), NOT focal/standoff. `reprojection_rms_px_control` is a control, not evidence: a
     degeneracy is a flat cost valley, so RMSE stays low in both arms. The refine-ON arms are
     AquaCal's own rationale for importing intrinsics from air; they are NOT "what CalibMar
     produces" — CalibMar solves each camera in its own frame and has no such redundancy, and works
     correctly in its intended regime.
  7. **Reproducing a number** — the exact command sequence, and a note that every committed result
     has a `benchmark.json`-shaped record carrying its seed, AquaCal version, git SHA, and
     environment.

**`.github/workflows/test.yml`** — add a third job named `experiments-smoke`, alongside `test` and
`pre-commit`. Do NOT extend the `test` job's 2x3 matrix (D-25: one platform is sufficient, six are
waste). The new job: `runs-on: ubuntu-latest`, `actions/checkout@v4`,
`actions/setup-python@v5` with `python-version: '3.12'`, `pip install -e ".[dev]"` (the `test` job's
install step, not `pre-commit`'s bare install), then one `run:` step invoking, in sequence:
`python -m experiments.e1_refractive_comparison --smoke`,
`python -m experiments.e2_real_rig --smoke`,
`python -m experiments.e7_interface_ablation --smoke`.
CI has no cached Zenodo dataset, so E2's smoke will take the absent-dataset branch — it must print a
visible `SKIPPED` line with its reason and exit 0. A smoke test that silently passes without running
is the same failure the smoke-mode rule exists to prevent, so the skip must be visible in the job
log. Do not add a dataset-download step to CI.
  </action>
  <verify>
    <automated>python -c "
import pathlib, re
r = pathlib.Path('experiments/README.md').read_text(encoding='utf-8')
for tok in ['--seed','--out','--force','--smoke','--check','gauge','xy_position_error_mm','ground_truth_fixed','height/distance','reprojection_rms_px_control','CalibMar','exp1_parameter_errors.csv','real_rig_metrics.json','interface_ablation.csv','e4_benchmark_grid.py','19.2']:
    assert tok in r, tok
w = pathlib.Path('.github/workflows/test.yml').read_text(encoding='utf-8')
assert 'experiments-smoke' in w
assert w.count('--smoke') >= 3
assert 'windows-latest' not in w.split('experiments-smoke')[1]
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The `python -c` assertion block above exits 0.
    - `grep -q "gauge" experiments/README.md` succeeds (the validation contract's EXP-06 doc assertion).
    - The README's provenance table has one row per committed artifact under `experiments/results/`: `python -c "import pathlib,re; r=pathlib.Path('experiments/README.md').read_text(encoding='utf-8'); missing=[p.name for p in pathlib.Path('experiments/results').iterdir() if p.name!='.gitkeep' and p.name not in r]; assert not missing, missing"` exits 0.
    - The README states no runtime as a range copied from the pre-run estimates — each runtime traces to a measured value in a wave-3/4 SUMMARY. Verify by cross-reading the three SUMMARYs.
    - `grep -c "calibration.json" experiments/README.md` appears only as an E2 output artifact, never as the figure generator's input (D-14's correction).
    - `python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/test.yml')); j=d['jobs']; assert set(j)>={'test','pre-commit','experiments-smoke'}; s=j['experiments-smoke']; assert s['runs-on']=='ubuntu-latest'; assert 'strategy' not in s; print('ok')"` exits 0 — the new job is single-platform and un-matrixed.
    - `python -c "import yaml; d=yaml.safe_load(open('.github/workflows/test.yml')); m=d['jobs']['test']['strategy']['matrix']; assert m['os']==['ubuntu-latest','windows-latest'] and m['python-version']==['3.10','3.11','3.12']"` exits 0 — the existing matrix is unchanged.
    - Running the three smoke commands locally in sequence all exit 0, and E2's prints a line containing `SKIPPED` when the dataset cache is moved aside.
    - `pre-commit run --all-files` exits 0.
  </acceptance_criteria>
  <done>`experiments/README.md` carries all seven sections including the gauge explanation, the corrected D-14 provenance, measured runtimes, and both E7 framing paragraphs; a single-platform `experiments-smoke` job runs all three `--smoke` invocations in CI with the existing 2x3 matrix untouched, and E2's smoke skip is visible in the log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner to network | The new job runs `pip install -e ".[dev]"` and the three `--smoke` invocations. It deliberately does NOT download the Zenodo dataset, so the job has no large-artifact network dependency. |
| notebook JSON editing | Two `.ipynb` files are edited programmatically; their stored outputs are what the docs site serves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-27 | Tampering | Deleting `tests/synthetic/experiments.py` before E1's comparison passes, destroying a reference implementation while the port is still unproven | mitigate | Hard precondition: the plan's objective requires reading `19.1-E1-REPRODUCTION.md` § Deletion gate and stopping unless it says cleared. This is D-13/D-19's ordering requirement made executable. |
| T-19.1-28 | Tampering | A notebook edit that reaches beyond the export cell, silently invalidating stored outputs the docs site serves or applying Phase 21's X4 changes early | mitigate | Exact cell counts asserted (28->27, 40->39), nbformat validity asserted, `git diff --stat` confined to two files, and explicit grep assertions that `RIG_SIZE = "large"` and the `tests.synthetic.ground_truth` import are UNCHANGED. `nbsphinx_execute = "never"` means no re-execution occurs. |
| T-19.1-29 | Tampering | A merged `sphinx-build -W` failure that each individual plan's build missed — the exact defect that escaped in Phase 18 | mitigate | `sphinx-build -W docs docs/_build/html` is an acceptance criterion in this plan, run against the fully merged tree rather than a per-plan worktree. |
| T-19.1-30 | Tampering | Silently extending the CI matrix from 2x3 to something larger, or adding a 164 MB download to every CI run | mitigate | Asserted: the new job has no `strategy` key, `runs-on` is `ubuntu-latest`, the existing `test` matrix is byte-equal to its prior value, and no dataset-download step is added. E2's smoke takes the absent-dataset branch by design. |
| T-19.1-31 | Repudiation | A green CI run mistaken for a run that measured something (D-25/P7's stated concern) | mitigate | E2's smoke must print a visible `SKIPPED` line with its reason; asserted by grep in the source and by a local run with the cache moved aside. |
| T-19.1-32 | Information Disclosure | `experiments/README.md` documenting an absolute local path or a machine identity | accept | The README documents repo-relative commands only. No credential or host information is involved; hardware specs for the paper are a 19.2 concern. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | accept | The CI job runs `pip install -e ".[dev]"`, which installs only the project's already-pinned dependencies — no new package is added by this phase. RESEARCH § Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
1. `! test -f tests/synthetic/experiments.py && ! test -f tests/synthetic/compare_refractive.py && ! test -d benchmarks`.
2. `! grep -rn "tests\.synthetic\.experiments\b\|compare_refractive\|from benchmarks" --include=*.py --include=*.yml --include=*.md . | grep -v "^\./\.planning/"`.
3. Notebook cell counts are 27 and 39; both validate as nbformat; no `to_csv` or `rows_exp1` remains.
4. `sphinx-build -W docs docs/_build/html` — exits 0.
5. `! grep -rn "compare_refractive\|tests/synthetic/experiment_helpers\.py" .planning/architecture.md .planning/codebase/`.
6. `grep -q "gauge" experiments/README.md` and the README names every file under `experiments/results/`.
7. `.github/workflows/test.yml` has three jobs; `experiments-smoke` is single-platform; the `test` matrix is unchanged.
8. All three `--smoke` commands exit 0 locally; E2's prints a visible `SKIPPED` without the dataset.
9. `python -m pytest tests/ -m "not slow" -q` — exits 0.
10. `pre-commit run --all-files` — exits 0.
</verification>

<success_criteria>
- ROADMAP SC4 is TRUE: `tests/synthetic/experiments.py` is gone with its unique content salvaged
  (auditable via the 20-row table), `compare_refractive.py` has moved to `experiments/` as E1's CLI
  entry point, the Phase 19 `benchmarks/` runner has moved under `experiments/`, and no two
  implementations of the same experiment remain in the repo.
- ROADMAP SC2 is complete: `experiments/` has `_io.py`, `_render.py`, `results/`, and a README
  mapping one command to each paper artifact with its expected runtime.
- ROADMAP SC3 is complete: every script honours the five-flag contract and `--smoke` runs in CI.
- D-07's gauge explanation exists in both the library docstring and the README.
- X6 is swept after the moves, so the docs describe the layout that actually exists.
</success_criteria>

<output>
Create `.planning/phases/19.1-experiment-suite-consolidation/19.1-07-SUMMARY.md` when done.
Record: the live `grep -n "^def "` output, the full 20-row keep/drop table with a Verdict and Reason
per row, `assemble_summary`'s evidence-based verdict, the before/after cell counts for both
notebooks, and the measured runtimes now recorded in the README.
</output>
