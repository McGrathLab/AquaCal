# 19.1 Plan 04 -- E2 Dataset Findings (Assumption A3 resolution)

**Resolved:** 2026-07-27
**Task:** 19.1-04 Task 1

## Dataset resolution

- Dataset name: `real-rig` (confirmed against `src/aquacal/datasets/_manifest.py`'s registered
  name and `loader.py`'s docstring).
- Cache path (this worktree): the dataset was **already cached in the main checkout** at
  `C:\Users\tucke\PycharmProjects\AquaCal\aquacal_data` (per the orchestrator's prompt). Because
  `aquacal.datasets.download.get_cache_dir()` resolves to `Path.cwd() / "aquacal_data"` (cwd-
  relative, not configurable), a fresh worktree's `Path.cwd()` never sees the main checkout's
  cache. Rather than re-downloading the ~192 MB archive, an NTFS directory junction was created
  from the worktree root (`mklink /J aquacal_data C:\Users\tucke\PycharmProjects\AquaCal\aquacal_data`)
  so `Path.cwd() / "aquacal_data"` resolves to the same on-disk files with zero copy. Verified:
  `python -c "from aquacal.datasets import get_cache_info; print(get_cache_info())"` reports
  `cached_datasets: ['real-rig']`, `total_size_mb: 192.27`, and `load_example('real-rig')` returns
  without any network I/O (confirmed by immediate return, no download progress output).
  `python -c "from aquacal.datasets import load_example; d=load_example('real-rig'); print(d.cache_path)"`
  prints `...\aquacal_data\real-rig\real-rig` (an existing directory).
- The junction target (`aquacal_data`) is git-ignored: `aquacal_data/.gitignore` contains `*`,
  and `git check-ignore -v aquacal_data` confirms `aquacal_data/downloads/` and
  `aquacal_data/real-rig/` are both ignored. `git status --porcelain` shows no modification
  under the dataset cache directory (nothing to show -- the junction and its target are both
  untracked/ignored).

## Shipped `config.yaml` (verbatim)

```yaml
board:
  squares_x: 12
  squares_y: 9
  square_size: 0.06
  marker_size: 0.045
  dictionary: DICT_5X5_100
  legacy_pattern: false
intrinsic_board:
  squares_x: 11
  squares_y: 8
  square_size: 0.02
  marker_size: 0.015
  dictionary: DICT_4X4_100
  legacy_pattern: true
cameras:
- e3v829d
- e3v82f9
- e3v82e0
- e3v831e
- e3v832e
- e3v8334
- e3v83e9
- e3v83eb
- e3v83ee
- e3v83ef
- e3v83f0
- e3v83f1
auxiliary_cameras:
- e3v8250
fisheye_cameras:
- e3v8250
paths:
  intrinsic_videos:
    e3v8250: intrinsic/e3v8250/
    e3v829d: intrinsic/e3v829d/
    e3v82e0: intrinsic/e3v82e0/
    e3v82f9: intrinsic/e3v82f9/
    e3v831e: intrinsic/e3v831e/
    e3v832e: intrinsic/e3v832e/
    e3v8334: intrinsic/e3v8334/
    e3v83e9: intrinsic/e3v83e9/
    e3v83eb: intrinsic/e3v83eb/
    e3v83ee: intrinsic/e3v83ee/
    e3v83ef: intrinsic/e3v83ef/
    e3v83f0: intrinsic/e3v83f0/
    e3v83f1: intrinsic/e3v83f1/
  extrinsic_videos:
    e3v8250: extrinsic/e3v8250/
    e3v829d: extrinsic/e3v829d/
    e3v82e0: extrinsic/e3v82e0/
    e3v82f9: extrinsic/e3v82f9/
    e3v831e: extrinsic/e3v831e/
    e3v832e: extrinsic/e3v832e/
    e3v8334: extrinsic/e3v8334/
    e3v83e9: extrinsic/e3v83e9/
    e3v83eb: extrinsic/e3v83eb/
    e3v83ee: extrinsic/e3v83ee/
    e3v83ef: extrinsic/e3v83ef/
    e3v83f0: extrinsic/e3v83f0/
    e3v83f1: extrinsic/e3v83f1/
  output_dir: output
interface:
  n_air: 1.0
  n_water: 1.333
  normal_fixed: false
  initial_water_z:
    e3v829d: 1.0
    e3v82e0: 1.0
    e3v82f9: 1.0
    e3v831e: 1.0
    e3v832e: 1.0
    e3v8334: 1.0
    e3v83e9: 1.0
    e3v83eb: 1.0
    e3v83ee: 1.0
    e3v83ef: 1.0
    e3v83f0: 1.0
    e3v8250: 1.0
optimization:
  robust_loss: huber
  loss_scale: 3.0
  max_calibration_frames: 150
  refine_intrinsics: true
detection:
  min_corners: 8
  min_cameras: 2
  frame_step: 1
validation:
  holdout_fraction: 0.2
  save_detailed_residuals: true
```

Key fields for the VERDICT:

- **`validation.holdout_fraction: 0.2`** -- present explicitly (not relying on the 0.2 default at
  `pipeline.py:429`), and non-zero, so the validation split is non-empty.
- **`reject_outlier_frames`** does not appear anywhere in this config -- absent, so it picks up
  the `schema.py:329` default of `True` (v1.7 outlier-rejection behavior applies to this run).
- **`interface.initial_water_z`** is used (a per-camera dict, all `1.0`), not `initial_distances`.
  Behaviorally equivalent branch per RESEARCH's note.
- **`paths.output_dir: output`** -- relative to the dataset's cache path, i.e.
  `.../aquacal_data/real-rig/real-rig/output/`.
- **`save_benchmark`** does not appear under an `internals:` block in this config -- absent, so it
  picks up the `pipeline.py:437` default of `True`. The pipeline writes its own `benchmark.json`.

## `output/` presence in the shipped archive

`cache_path/output/` does **not** exist in the shipped archive (only `config.yaml`,
`extrinsic/`, `intrinsic/`, and `reference_calibration.json` are present at the dataset root).
The output directory is created fresh by a run of E2.

## Dataset contents (image-based, not video)

- 12 primary cameras + 1 auxiliary/fisheye camera (`e3v8250`) = 13 cameras total, matching
  `CLAUDE.md`'s "13-camera rig" description.
- Frames are **pre-extracted JPEG images** (`frame0000.jpg`, `frame0001.jpg`, ...), not video
  files, despite the config keys being named `intrinsic_videos`/`extrinsic_videos`.
- Frame counts (representative primary camera `e3v829d`): 60 extrinsic frames, 25 intrinsic
  frames. `optimization.max_calibration_frames: 150` has no limiting effect (60 < 150).
- This is consistent with (and slightly favorable relative to) the "48-87 min" full-rig estimate
  in `CLAUDE.md`, which was measured against video-based full-length runs; the frame counts here
  are on the smaller side of that range.

## VERDICT

**YES** -- `output_dir/spatial_measurements.csv` will be produced by this config.

Reasoning chain, confirmed against the live code in this worktree (not just RESEARCH's static
inspection):

1. `holdout_fraction: 0.2` (explicit in the shipped config, matches the default anyway) is
   `> 0` (`pipeline.py:429` reads `val.get("holdout_fraction", 0.2)`), so a non-empty validation
   split is constructed at run time.
2. The validation path (`pipeline.py` primary-camera validation block, ~line 1560) calls
   `compute_3d_distance_errors(..., include_spatial=True)` unconditionally inside
   `_evaluate_reconstruction`/the evaluation helper feeding `primary_eval.reconstruction`
   (`primary_3d`) -- there is no config flag gating `include_spatial`.
3. `pipeline.py:1568-1573` then writes `output_dir/spatial_measurements.csv` whenever
   `primary_3d.spatial is not None and len(primary_3d.spatial.positions) > 0` -- which holds
   whenever the holdout split actually triangulates at least one 3D point, the expected case for
   a healthy 13-camera rig with `min_cameras: 2`.

No fallback path is needed. Task 2 may proceed to build `reconstruction_errors.csv` around
`load_spatial_measurements(output_dir / "spatial_measurements.csv")` as RESEARCH's provenance map
specifies.

Nothing inside the dataset cache directory was modified by this inspection (`config.yaml`, the
image directories, and `reference_calibration.json` were only read).
