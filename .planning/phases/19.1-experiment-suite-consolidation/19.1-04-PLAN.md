---
phase: 19.1-experiment-suite-consolidation
plan: 04
type: execute
wave: 3
depends_on: [19.1-01, 19.1-03]
files_modified:
  - experiments/e2_real_rig.py
  - experiments/results/camera_parameters.csv
  - experiments/results/reconstruction_errors.csv
  - experiments/results/reprojection_residuals.csv
  - experiments/results/real_rig_metrics.json
  - experiments/results/benchmark.json
  - experiments/results/calibration.json
  - .planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DATASET-FINDINGS.md
  - .planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md
autonomous: true
requirements: [EXP-04]

must_haves:
  truths:
    - "Assumption A3 is resolved by inspection of the actually-downloaded dataset before any CSV writer is designed around spatial_measurements.csv existing (VALIDATION known risks, RESEARCH OQ2)"
    - "E2 goes through run_calibration_from_config, so its benchmark.json is the genuine pipeline-written article that E4's tenth grid point reuses in 19.2 (D-15, REQUIREMENTS constraint 9)"
    - "E2 emits camera_parameters.csv, reconstruction_errors.csv, and reprojection_residuals.csv with headers byte-identical to the committed baselines the figures repo reads (D-14 — schemas are FIXED)"
    - "E2 emits real_rig_metrics.json carrying every §3 real-rig number in machine-readable form, assembled from already-computed pipeline fields rather than recomputed (D-15, P2)"
    - "E2's --smoke reports a VISIBLE SKIPPED with a reason and exits 0 when the Zenodo dataset is absent, so a CI pass can never be mistaken for a run that happened (D-25, P7)"
    - "The side-by-side delta table for all nine §3 quantities is written to disk in the phase directory, so the D-16 gate has an artifact to adjudicate rather than console scrollback"
    - "The real run is launched with python -u and in the background, because without -u the log stays empty for the whole run and a stall is indistinguishable from progress (CLAUDE.md)"
  artifacts:
    - path: "experiments/e2_real_rig.py"
      provides: "the real-rig re-run: pipeline call, three CSVs, real_rig_metrics.json, delta table"
      min_lines: 220
    - path: ".planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DATASET-FINDINGS.md"
      provides: "the A3 resolution: what the shipped Zenodo config.yaml contains and whether spatial_measurements.csv is produced"
    - path: ".planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md"
      provides: "committed vs fresh vs delta for all nine §3 quantities — the D-16 gate's input"
      contains: "7,762"
    - path: "experiments/results/real_rig_metrics.json"
      provides: "the §3 prose numbers, machine-readable"
    - path: "experiments/results/camera_parameters.csv"
      provides: "the figures repo's camera-rig 3D panel input"
      contains: "camera,x_m,y_m,z_m,fx_px,fy_px,cx_px,cy_px,water_z_m,h_c_m,reprojection_rms_px"
  key_links:
    - from: "experiments/e2_real_rig.py"
      to: "aquacal.calibration.pipeline.run_calibration"
      via: "the one experiment that goes through the pipeline"
      pattern: "run_calibration"
    - from: "experiments/e2_real_rig.py"
      to: "experiments._io"
      via: "shared CLI parent and sorted CSV writer"
      pattern: "from experiments\\._io import"
    - from: "experiments/results/benchmark.json"
      to: "output_dir/benchmark.json"
      via: "copied from the pipeline's own write, never reconstructed"
      pattern: "benchmark.json"
---

<objective>
Build and run E2, the real 13-camera rig re-run against the current library (v1.7 outlier rejection
+ v1.8 intrinsics seeding), and write the side-by-side delta table the D-16 blocking gate will
adjudicate.

Purpose: REQUIREMENTS constraint 8 — twenty minutes of compute buys the earliest possible warning
that §3's real-rig numbers moved. The committed run predates both feature releases entirely, and
tutorial 01's stored run carries `e3v8250: RMS 25.991 px`, exactly the case those releases target,
so numbers moving is the expected outcome rather than the exception.
Output: `experiments/e2_real_rig.py`, six committed result artifacts, an A3 findings note, and the
delta table.

**This plan does NOT close the gate.** It produces the delta table. Plan 19.1-08 is the blocking,
non-auto-approvable human checkpoint that adjudicates it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**PYTHONPATH:** in a worktree, `export PYTHONPATH="$(pwd)/src"` before pytest or any run.
**Long runs:** every calibration invocation in this plan uses `python -u` and runs in the
background. Python block-buffers stdout when it is a pipe, so without `-u` the log is empty for the
entire 15-20 minutes and a real stall is invisible until the timeout (CLAUDE.md).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-CONTEXT.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-PATTERNS.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-RESEARCH.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-01-SUMMARY.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-03-SUMMARY.md

<interfaces>
<!-- Verified live 2026-07-25. -->

The pipeline entry point notebook 01 cell-7 uses (this is `run_calibration_from_config`'s public
wrapper — E2 is the ONE experiment that goes through the pipeline, D-15):
  from aquacal.calibration.pipeline import run_calibration
  result = run_calibration(str(config_path), verbose=True)

`config.save_benchmark: bool = True` by default (`config/schema.py:363`), so the pipeline writes
`output_dir/benchmark.json` itself. E2 COPIES that file; it does NOT construct one and does NOT
use `_io.write_direct_call_benchmark`.

**A3 pre-resolved by static inspection — the executor must still confirm against the real download,
but here is what the code says**, so Task 1 is a confirmation, not an investigation:
  - `src/aquacal/calibration/pipeline.py:1568-1573` writes `output_dir/spatial_measurements.csv`
    whenever `primary_3d.spatial is not None and len(primary_3d.spatial.positions) > 0`.
  - `src/aquacal/validation/evaluation.py:207` calls `compute_3d_distance_errors(...,
    include_spatial=True)` **unconditionally** in the validation path — there is no config flag
    gating it.
  - So the only precondition is a non-empty holdout split: `config.holdout_fraction`
    (`pipeline.py:429`, default `0.2` when the key is absent) must be > 0 and the validation frames
    must triangulate.
  Therefore A3's MEDIUM risk is most likely already discharged. Task 1 confirms
  `holdout_fraction` in the shipped `config.yaml` (or its absence, which picks up the 0.2 default)
  and records the finding.

Fields E2's `real_rig_metrics.json` assembles — every one is ALREADY COMPUTED by the pipeline
(P2: assemble, never recompute):
  result.diagnostics.reprojection_error_rms                  -> mean reprojection px
  result.diagnostics.reprojection_error_per_camera           -> dict; min/max give the range;
                                                                the auxiliary camera's key gives
                                                                the fisheye number
  cc.water_z, cc.water_z - cc.extrinsics.C[2]                -> z_w and per-camera height h_c
  {name for name, cc in result.cameras.items() if cc.is_auxiliary}  -> auxiliary set
`DistanceErrors` field names (`validation/reconstruction.py:86`): `mean` (this is MAE), `rmse`,
`percent_error`, `num_comparisons`.
`load_spatial_measurements(path)` (`validation/reconstruction.py:547`) reads the pipeline's
`spatial_measurements.csv` back as a `SpatialMeasurements`.

Pinned CSV headers — FIXED CONTRACTS, verified byte-for-byte against the committed baselines the
figures repo reads. Do not add, remove, reorder, or rename a column:
  camera_parameters.csv      camera,x_m,y_m,z_m,fx_px,fy_px,cx_px,cy_px,water_z_m,h_c_m,reprojection_rms_px
  reconstruction_errors.csv  x_m,y_m,z_m,signed_error_m,frame_idx
  reprojection_residuals.csv residual_x_px,residual_y_px,camera,is_auxiliary
There is NO `gt_water_z_m` column for the real rig — that column only appears when
`scenario is not None`, i.e. for synthetic runs. E2 is real data.

Committed baselines to diff against (READ-ONLY external repo; NEVER write into it):
  C:\Users\tucke\PycharmProjects\DissertationFigures\data\aquacal\zenodo_e2e\
    camera_parameters.csv, reconstruction_errors.csv, reprojection_residuals.csv, calibration.json

The nine §3 quantities the delta table must cover (D-16, verbatim):
  mean reprojection 0.88 px; range 0.54-2.41 px; auxiliary fisheye 15.1 px;
  inter-corner MAE 0.268 mm; RMSE 0.674 mm; 0.45% mean relative; 7,762 comparisons;
  z_w ~ 1.03 m; camera heights 1.01-1.08 m.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download the Zenodo dataset and resolve Assumption A3 before designing the CSV writer</name>
  <files>.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DATASET-FINDINGS.md</files>
  <read_first>
    - `19.1-RESEARCH.md` § "E2's exact export cell and CSV provenance" — the whole section,
      especially the `reconstruction_errors.csv` bullet explaining that it is sourced from
      `output_dir/spatial_measurements.csv` via `load_spatial_measurements`, NOT from a live
      `compute_3d_distance_errors` call (that branch is synthetic-only and needs `detections`,
      which is `None` for the Zenodo path).
    - `19.1-RESEARCH.md` § Assumptions Log row A3, and § Open Questions item 2.
    - `19.1-VALIDATION.md` § Manual-Only Verifications, the "Zenodo `spatial_measurements.csv`
      existence" row.
    - `docs/tutorials/01_full_pipeline.ipynb` — the `DATA_SOURCE = "zenodo"` cell, `cell-6`,
      `cell-7`, and `cell-21`. Read the cells, not just their outputs: cell-21 is the branch that
      reads `spatial_measurements.csv` and falls back to aggregate-only printing when absent.
    - `src/aquacal/calibration/pipeline.py` lines 420-435 (`holdout_fraction` default) and
      1560-1575 (the `spatial_measurements.csv` write).
    - `src/aquacal/datasets/loader.py` — `load_example`'s signature and what `cache_path` points at.
  </read_first>
  <action>
Download the real-rig dataset the way a reader would, via the public loader:
`python -u -c "from aquacal.datasets import load_example; d = load_example('real-rig');
print(d.cache_path)"`. It is ~164 MB and requires network on first run; run it in the background
and poll, do not block the foreground. If the exact dataset name differs, read
`src/aquacal/datasets/_manifest.py` for the registered name rather than guessing.

Then inspect, without running a calibration:
  1. The shipped `config.yaml` in the dataset — record its full contents in the findings note.
     Specifically record: the `validation`/`holdout_fraction` value (or note its absence, which
     picks up the `0.2` default at `pipeline.py:429`), whether `reject_outlier_frames` appears
     (it defaults `True` at `schema.py:329`, so a config omitting it already picks up v1.7
     behavior), whether `initial_distances` or `initial_water_z` is used, `output_dir`, and
     `save_benchmark` (defaults `True`).
  2. Whether `cache_path/output/` already exists in the shipped archive and what it contains.
  3. Whether the dataset ships images or videos, and the frame count, so the ~15-20 min estimate
     can be sanity-checked.

Write `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DATASET-FINDINGS.md` recording:
the resolved dataset name and cache path, the config contents, and an explicit VERDICT section
answering "will `output_dir/spatial_measurements.csv` be produced by this config?" with the
reasoning chain (`holdout_fraction > 0` → validation split non-empty → `evaluation.py:207` passes
`include_spatial=True` unconditionally → `pipeline.py:1568-1573` writes the CSV).

If the verdict is NO (holdout_fraction is 0 or the key is explicitly zeroed), STOP and do not
proceed to Task 2. Record the two candidate fallbacks from RESEARCH — (a) call
`compute_3d_distance_errors` live, which needs `detections` the Zenodo path does not retain, or
(b) a config change — and note that a config change must be re-justified against D-16's "confirm
every difference is deliberate" requirement, because changing the config changes what is being
compared. Surface this to the user as a decision rather than picking one.

Do NOT run the calibration in this task. Do NOT modify anything inside the dataset cache.
  </action>
  <verify>
    <automated>test -f .planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DATASET-FINDINGS.md && grep -q "VERDICT" .planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DATASET-FINDINGS.md && grep -qi "holdout_fraction" .planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DATASET-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from aquacal.datasets import load_example; d=load_example('real-rig'); print(d.cache_path)"` prints an existing directory (dataset cached locally).
    - The findings note exists and contains a `## VERDICT` section naming either YES or NO on `spatial_measurements.csv`.
    - The findings note quotes the shipped `config.yaml` verbatim, including its `holdout_fraction` value or an explicit statement that the key is absent.
    - The findings note records the camera count and frame count of the dataset.
    - `git status --porcelain` shows no modification under the dataset cache directory.
    - If the verdict is NO: no `experiments/e2_real_rig.py` was created, and the two fallbacks are written up as a user decision.
  </acceptance_criteria>
  <done>The ~164 MB dataset is cached locally, its shipped `config.yaml` is recorded verbatim, and A3 has an explicit written YES/NO verdict with its reasoning chain — obtained before any CSV-writing code was designed around the file's existence.</done>
</task>

<task type="auto">
  <name>Task 2: Write experiments/e2_real_rig.py — the port of notebook 01's export cell</name>
  <files>experiments/e2_real_rig.py</files>
  <read_first>
    - `docs/tutorials/01_full_pipeline.ipynb` cell `cell-26` — THE SOURCE BEING PORTED. Read the
      full cell source. Every column of all three CSVs is constructed there.
    - `docs/tutorials/01_full_pipeline.ipynb` cells `cell-7` (the `run_calibration` call and
      `pipeline_output_dir`) and `cell-21` (the `load_spatial_measurements` branch and the inline
      `signed_mean` / `rmse` derivation).
    - `19.1-RESEARCH.md` § "E2's exact export cell and CSV provenance" — the field-by-field
      provenance map for `real_rig_metrics.json`, including the note that the tutorial's Zenodo
      branch does NOT reconstruct MAE or `percent_error`, and the permitted 3-line inline
      derivation from the loaded per-measurement data.
    - `19.1-PATTERNS.md` § `experiments/e2_real_rig.py`.
    - `19.1-03-SUMMARY.md` — the exact `_io.py` signatures.
    - `experiments/e4_benchmark_grid.py` — the `_build_arg_parser` + `__main__` script structure to
      mirror (the shape of a script that calls a library entry point and writes results).
    - `src/aquacal/validation/reconstruction.py` lines 60-110 (`DistanceErrors` fields) and 520-570
      (`save_spatial_measurements` / `load_spatial_measurements`).
    - The three committed baselines under
      `C:\Users\tucke\PycharmProjects\DissertationFigures\data\aquacal\zenodo_e2e\` — read their
      HEADERS ONLY, to confirm the pinned column order. This repo is READ-ONLY; never write to it.
    - `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DATASET-FINDINGS.md` from Task 1.
  </read_first>
  <action>
Create `experiments/e2_real_rig.py`, invoked as `python -m experiments.e2_real_rig`.

CLI: build the parser with `parents=[build_experiment_arg_parser()]` from `experiments._io` and call
`validate_args` immediately after `parse_args()`. Declare `CHECK_RTOL` as a module constant. Declare
the three key-column tuples as module constants:
`CAMERA_PARAMS_KEY_COLUMNS = ["camera"]`,
`RECONSTRUCTION_KEY_COLUMNS = ["frame_idx", "x_m", "y_m", "z_m"]`,
`RESIDUALS_KEY_COLUMNS = ["camera", "residual_x_px", "residual_y_px"]`.
(The last two have no natural single key; sorting on these columns is what makes the row order
byte-stable across runs per D-22/P6.)

Run path: resolve the dataset via `load_example(...)` using the name Task 1 confirmed, then call
`run_calibration(str(config_path), verbose=True)` from `aquacal.calibration.pipeline`. Thread
`args.seed` into the config's seed if the config object exposes it (Part 4 Rule 1); if the config
file pins a seed, log which value won rather than silently overriding.

Emit exactly six artifacts into `args.out` via `_io.write_experiment_csv` (which sorts before
writing and honors `--force`):

  1. `camera_parameters.csv` — header EXACTLY
     `camera,x_m,y_m,z_m,fx_px,fy_px,cx_px,cy_px,water_z_m,h_c_m,reprojection_rms_px`.
     `x_m,y_m,z_m` from `cc.extrinsics.C`; `fx_px,fy_px,cx_px,cy_px` from `cc.intrinsics.K`;
     `water_z_m` from `cc.water_z`; `h_c_m` = `cc.water_z - C[2]`; `reprojection_rms_px` from
     `result.diagnostics.reprojection_error_per_camera.get(cam, nan)` — this is a genuine
     PER-CAMERA dict field, unlike E1's calibration-level scalar. Do NOT add a `gt_water_z_m`
     column; the real rig has no ground truth.
  2. `reprojection_residuals.csv` — header EXACTLY
     `residual_x_px,residual_y_px,camera,is_auxiliary`. `residual_x_px,residual_y_px` from the live
     reprojection result's `residuals`; `camera` from its `camera_labels`; `is_auxiliary` from
     `{name for name, cc in result.cameras.items() if cc.is_auxiliary}`. A fresh run always has the
     live reprojection result populated, so do not port the loaded-from-saved-calibration fallback
     branch.
  3. `reconstruction_errors.csv` — header EXACTLY `x_m,y_m,z_m,signed_error_m,frame_idx`, built
     from `load_spatial_measurements(output_dir / "spatial_measurements.csv")` per Task 1's verdict.
  4. `real_rig_metrics.json` — one JSON object carrying all nine §3 quantities as named keys with
     explicit units in the key names (e.g. `mean_reprojection_px`, `reprojection_range_px` as a
     two-element list, `auxiliary_reprojection_px` keyed by the auxiliary camera's name,
     `inter_corner_mae_mm`, `inter_corner_rmse_mm`, `mean_relative_error_pct`, `n_comparisons`,
     `water_z_m`, `camera_height_range_m` as a two-element list). Every value is ASSEMBLED from the
     already-computed fields listed in `<interfaces>`, never recomputed — except MAE and
     percent-relative, which RESEARCH explicitly permits deriving inline in three lines from the
     loaded per-measurement signed errors (`MAE = mean(abs(signed_errors))`,
     `percent = MAE / square_size * 100`), because that is exactly what
     `compute_3d_distance_errors` does internally, applied to already-saved per-point data. Add a
     `provenance` sub-object naming, for each number, which pipeline field or file it came from —
     this is the phase's actual deliverable (one number, one origin).
  5. `benchmark.json` — COPY `output_dir/benchmark.json`, which the pipeline wrote itself. Do not
     construct it, do not call `_io.write_direct_call_benchmark`, do not touch its
     `schema_version` (D-15, REQUIREMENTS #10). This is the record E4's tenth grid point reuses in
     19.2, so it must be the genuine article.
  6. `calibration.json` — COPY the run's own calibration result file (~530 KB). Rule 5's "raw
     calibration artifacts stay out" means videos and detections, not the result file (D-15).

`--smoke` (D-25 / P7): if the dataset is not already cached, print a clearly visible line
containing the literal token `SKIPPED` plus the reason (dataset absent, would require a ~164 MB
download) and `sys.exit(0)`. Do NOT download in smoke mode. When the dataset IS cached, smoke mode
must still exercise the CSV/JSON writing code paths without running the full calibration — e.g.
build the three DataFrames from a stub/minimal result and write them to a temp directory — so the
schema code is genuinely covered rather than skipped. A smoke test that silently passes without
exercising anything is the exact failure P7 exists to prevent.

`--check`: compare each freshly-built DataFrame against the corresponding on-disk committed CSV in
`args.out` via `_io.compare_experiment_csv` at `CHECK_RTOL`, print the worst offending cell per
file, write nothing, and exit nonzero on any mismatch.

Do not plot. Do not write to `DissertationFigures`. Do not edit any notebook in this plan —
deleting `cell-26` is plan 19.1-07's task, ordered after this port is confirmed working.
  </action>
  <verify>
    <automated>python -m experiments.e2_real_rig --smoke</automated>
  </verify>
  <acceptance_criteria>
    - `python -m experiments.e2_real_rig --help` exits 0 and lists all five contract flags.
    - `python -m experiments.e2_real_rig --smoke` exits 0.
    - `python -m experiments.e2_real_rig --check --force` exits nonzero with a message containing "mutually exclusive".
    - `grep -c "write_direct_call_benchmark\|assemble_benchmark_record" experiments/e2_real_rig.py` returns 0 — E2 uses the pipeline's own record.
    - `grep -c "gt_water_z_m" experiments/e2_real_rig.py` returns 0.
    - `grep -c "matplotlib\|plt\." experiments/e2_real_rig.py` returns 0.
    - `grep -c "DissertationFigures" experiments/e2_real_rig.py` returns 0.
    - `grep -c "SKIPPED" experiments/e2_real_rig.py` is at least 1.
    - The three header strings appear verbatim in the source (as the column-order lists), matching `head -1` of each committed baseline exactly.
    - `ruff check experiments/e2_real_rig.py && ruff format --check experiments/e2_real_rig.py` exits 0.
  </acceptance_criteria>
  <done>`experiments/e2_real_rig.py` exists, honors the five-flag contract, smoke-skips visibly without the dataset, constructs all three pinned CSV schemas plus `real_rig_metrics.json` with a provenance sub-object, and copies rather than reconstructs `benchmark.json`.</done>
</task>

<task type="auto">
  <name>Task 3: Run E2 for the record and write the nine-quantity delta table</name>
  <files>experiments/results/camera_parameters.csv, experiments/results/reconstruction_errors.csv, experiments/results/reprojection_residuals.csv, experiments/results/real_rig_metrics.json, experiments/results/benchmark.json, experiments/results/calibration.json, .planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md</files>
  <read_first>
    - `19.1-CONTEXT.md` D-16 in full — the nine numbers, and both reasons the gate is real rather
      than ceremonial.
    - `19.1-VALIDATION.md` § Manual-Only Verifications, the "E2's §3 delta table review" row.
    - The three committed baselines under
      `C:\Users\tucke\PycharmProjects\DissertationFigures\data\aquacal\zenodo_e2e\` — read fully
      this time (they are small) to extract the committed per-camera values for the delta table's
      camera-height and per-camera reprojection rows. READ-ONLY.
    - `CLAUDE.md` § "Always run calibration unbuffered".
    - `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DATASET-FINDINGS.md`.
  </read_first>
  <action>
Launch the run in the background, unbuffered, teeing to a log:
`python -u -m experiments.e2_real_rig --out experiments/results --force 2>&1 | tee /tmp/e2_run.log`
Budget 15-20 minutes of compute for the 13-camera rig at 200 frames. Poll the log rather than
blocking. If the log is still empty after 60 seconds, that is a real signal — `-u` guarantees
output flows, so an empty log means the process is stuck, not buffered.

When it completes, confirm all six artifacts landed in `experiments/results/` and that
`spatial_measurements.csv` was in fact produced by the pipeline (Task 1's verdict, now empirically
confirmed — record either way).

Write `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md` as a markdown
table with one row per §3 quantity and the columns
`Quantity | §3 committed value | Fresh run value | Δ | Δ% | Assessment`.
All nine rows are mandatory, in this order:
  1. mean reprojection error (committed 0.88 px)
  2. per-camera reprojection range (committed 0.54-2.41 px)
  3. auxiliary fisheye reprojection (committed 15.1 px)
  4. inter-corner MAE (committed 0.268 mm)
  5. inter-corner RMSE (committed 0.674 mm)
  6. mean relative error (committed 0.45%)
  7. number of comparisons (committed 7,762)
  8. recovered water surface height z_w (committed ~1.03 m)
  9. camera height range h_c (committed 1.01-1.08 m)
For range-valued rows, give both endpoints. For the assessment column write `UNCHANGED`,
`MOVED`, or `MOVED — mechanism: <named mechanism>`. The two mechanisms already known to be in play
are v1.7's `reject_outlier_frames` (default `True`, and tutorial 01's stored run carries
`e3v8250: RMS 25.991 px`, exactly the case it targets) and v1.8's seeded `cv2.calibrateCamera` for
Stage-1 in-air intrinsics. Name one of those, or another specific mechanism, for every moved
number. "Numerical noise" is acceptable only at the last-digit level.

Add three sections after the table:
  - `## Manuscript impact` — for each moved number, which §3 sentence would need editing. Note the
    word budget is at ~3,916 of 4,000 with captions, so a longer replacement has a real cost.
  - `## Per-camera detail` — the full committed-vs-fresh per-camera table for reprojection RMS and
    `h_c`, so a moved aggregate can be attributed to a specific camera.
  - `## Awaiting user decision` — stating explicitly that this table requires an explicit user
    response before the phase can close, per D-16, and that `workflow.auto_advance` must not
    self-approve it. Do NOT record an approval here; plan 19.1-08 owns that.

Commit the six artifacts under `experiments/results/`. They are small, they are the figure inputs,
and they are the only thing standing between a number in the PDF and a claim nobody can check
(Part 4 Rule 5). Do NOT copy anything into `DissertationFigures` — that is a manual step outside
this phase.
  </action>
  <verify>
    <automated>python -c "import json,pathlib; m=json.loads(pathlib.Path('experiments/results/real_rig_metrics.json').read_text()); req=['mean_reprojection_px','reprojection_range_px','auxiliary_reprojection_px','inter_corner_mae_mm','inter_corner_rmse_mm','mean_relative_error_pct','n_comparisons','water_z_m','camera_height_range_m','provenance']; missing=[k for k in req if k not in m]; assert not missing, missing; b=json.loads(pathlib.Path('experiments/results/benchmark.json').read_text()); assert b['schema_version']==1; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All six artifacts exist under `experiments/results/`: the three CSVs, `real_rig_metrics.json`, `benchmark.json`, `calibration.json`.
    - `head -1 experiments/results/camera_parameters.csv` equals `camera,x_m,y_m,z_m,fx_px,fy_px,cx_px,cy_px,water_z_m,h_c_m,reprojection_rms_px` exactly.
    - `head -1 experiments/results/reconstruction_errors.csv` equals `x_m,y_m,z_m,signed_error_m,frame_idx` exactly.
    - `head -1 experiments/results/reprojection_residuals.csv` equals `residual_x_px,residual_y_px,camera,is_auxiliary` exactly.
    - Each fresh CSV's header is byte-identical to `head -1` of its counterpart under `DissertationFigures/data/aquacal/zenodo_e2e/`.
    - `python -c "import json;b=json.load(open('experiments/results/benchmark.json'));assert b['schema_version']==1;assert 'memory' not in b or b.get('memory') is not None"` exits 0 (pipeline-written record, schema untouched).
    - `python -c "import json;b=json.load(open('experiments/results/benchmark.json'));t=b['timings'];assert any(v is not None for v in t.values()), t"` exits 0 — no repeat of the Phase 19 always-null stage-seconds defect.
    - `grep -c "^| " .planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md` is at least 11 (header + separator + nine quantity rows).
    - The delta table contains the literal strings `0.88`, `2.41`, `15.1`, `0.268`, `0.674`, `0.45`, `7,762` or `7762`, `1.03`, `1.08`.
    - The delta table contains an `## Awaiting user decision` section.
    - Every row's Assessment cell is `UNCHANGED` or contains the substring `mechanism:`.
    - `git status --porcelain DissertationFigures` produces nothing; no file outside this repo was written.
    - `python -m pytest tests/ -m "not slow" -q` still exits 0.
  </acceptance_criteria>
  <done>E2 ran to completion unbuffered in the background, all six artifacts are committed under `experiments/results/` with headers byte-identical to the committed baselines, `benchmark.json` is the pipeline's own schema-1 record with non-null stage timings, and the nine-row delta table exists on disk with a named mechanism for every moved number and an explicit awaiting-user-decision section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zenodo network → local cache | E2 downloads a ~164 MB frozen 2026-02 archive via the existing `aquacal.datasets.download`/`loader` code. This is pre-existing code reused, not new in this phase. |
| dataset `config.yaml` → pipeline | A YAML config from the downloaded archive drives a calibration run, including its `output_dir`. |
| CLI args → filesystem | `--out` determines where six artifacts are written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-12 | Tampering | Zenodo download integrity (a substituted or corrupted archive would silently change §3's numbers) | transfer | Out of scope for 19.1: this is `aquacal.datasets.download`'s existing responsibility, unchanged by this phase (RESEARCH § Security Domain). The relevant mitigation for THIS phase is provenance, not integrity — the delta table plus `benchmark.json`'s `git_sha`/version block record exactly which library and which archive produced each number, so a discrepancy is detectable after the fact. |
| T-19.1-13 | Tampering | The dataset's shipped `config.yaml` writing outside the intended output tree via its `output_dir` | accept | The archive is the researcher's own published dataset, downloaded from a DOI they control, and the config is inspected verbatim in Task 1 before any run. Task 1's acceptance criteria include recording `output_dir`. Adding a sandbox around a config the researcher published themselves is disproportionate. |
| T-19.1-14 | Tampering | Writing into the read-only `DissertationFigures` repo | mitigate | Explicit prohibition in both task actions, enforced by acceptance criteria: `grep -c "DissertationFigures" experiments/e2_real_rig.py` returns 0 and `git status --porcelain DissertationFigures` produces nothing. The baselines are read-only inputs. |
| T-19.1-15 | Repudiation | A §3 number changes with no record of why | mitigate | The nine-row delta table names a mechanism for every moved value, `real_rig_metrics.json` carries a `provenance` sub-object per number, and the pipeline's `benchmark.json` carries the environment/git-sha block. This is the phase's core deliverable, not a security afterthought. |
| T-19.1-16 | Tampering | `--check` overwriting the committed baseline | mitigate | Inherited from `_io.compare_experiment_csv`, which performs no writes; `--check` and `--force` are mutually exclusive at the parser level, asserted in this plan's acceptance criteria. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. RESEARCH § Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
1. `test -f .planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DATASET-FINDINGS.md` with a `## VERDICT` section.
2. `python -m experiments.e2_real_rig --smoke` — exits 0.
3. `python -m experiments.e2_real_rig --check --force` — exits nonzero, "mutually exclusive".
4. All six artifacts present under `experiments/results/`; the three CSV headers byte-identical to the committed baselines.
5. `python -c "import json;b=json.load(open('experiments/results/benchmark.json'));assert b['schema_version']==1"` — exits 0.
6. `test -f .planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md` with nine quantity rows and an `## Awaiting user decision` section.
7. `python -m pytest tests/ -m "not slow" -q` — exits 0.
8. `ruff check . && ruff format --check .` — exits 0.
</verification>

<success_criteria>
- ROADMAP SC5 (implementation half): E2's real-rig re-run against the current library emits
  `real_rig_metrics.json` and a `benchmark.json`, and every §3 real-rig number is either confirmed
  unchanged or recorded as moved with its new value and a named mechanism.
- A3 is discharged by inspection of the actual download, before the `reconstruction_errors.csv`
  writer was designed around `spatial_measurements.csv` existing.
- The delta table exists on disk. The gate itself is plan 19.1-08 and is NOT closed here.
</success_criteria>

<output>
Create `.planning/phases/19.1-experiment-suite-consolidation/19.1-04-SUMMARY.md` when done.
Record: A3's verdict, the measured wall-clock of the run, and a one-line-per-quantity summary of
which of the nine §3 numbers moved. State explicitly that the D-16 gate is still open.
</output>
