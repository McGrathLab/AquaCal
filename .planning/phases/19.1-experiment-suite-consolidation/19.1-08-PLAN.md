---
phase: 19.1-experiment-suite-consolidation
plan: 08
type: execute
wave: 6
depends_on: [19.1-04, 19.1-07]
files_modified:
  - .planning/phases/19.1-experiment-suite-consolidation/19.1-E2-GATE-RESPONSE.md
autonomous: false
requirements: [EXP-04]

must_haves:
  truths:
    - "Every §3 real-rig number is either confirmed unchanged or recorded as moved with its new value, and the confirmation is an explicit response from the user rather than an inferred pass (ROADMAP SC5, D-16)"
    - "The gate is structurally un-self-approvable: it requires an explicit user response recorded on disk, so workflow.auto_advance cannot discharge it by self-approving a labeled human-verify task (D-16)"
    - "The phase does not close until this response exists (VALIDATION sign-off: 'E2's blocking gate has an explicit user response — NOT a CI pass')"
  artifacts:
    - path: ".planning/phases/19.1-experiment-suite-consolidation/19.1-E2-GATE-RESPONSE.md"
      provides: "the user's recorded decision per moved §3 number, verbatim"
      contains: "User response"
  key_links:
    - from: ".planning/phases/19.1-experiment-suite-consolidation/19.1-E2-GATE-RESPONSE.md"
      to: ".planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md"
      via: "one response entry per delta-table row"
      pattern: "19.1-E2-DELTA-TABLE"
---

<objective>
Present E2's nine-row §3 delta table to the user and STOP until they respond. Record their response
verbatim. This is the phase's closing gate.

Purpose: D-16. Every §3 number that moved becomes a manuscript prose edit in a document whose word
budget is already at ~3,916 of 4,000 with captions, which is a decision only the user can make. And
the committed run predates v1.7 outlier rejection and v1.8 intrinsics seeding entirely — tutorial
01's stored run carries `e3v8250: RMS 25.991 px`, exactly the case those releases target — so
numbers moving is the expected outcome rather than the exception.
Output: `19.1-E2-GATE-RESPONSE.md` carrying the user's verbatim decision per number.

**This gate must not be auto-approved.** `workflow.auto_advance` self-approves labeled human-verify
tasks, and this gate exists specifically to guard against exactly that. It is satisfied only by an
explicit user response written into the response file — never by a green test suite, a green CI run,
or an inferred approval. If no response is available, the correct outcome is that the phase stays
open.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-CONTEXT.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DATASET-FINDINGS.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-04-SUMMARY.md

<interfaces>
The nine §3 quantities the delta table covers (D-16, verbatim committed values):
  mean reprojection 0.88 px
  per-camera reprojection range 0.54-2.41 px
  auxiliary fisheye 15.1 px
  inter-corner MAE 0.268 mm
  inter-corner RMSE 0.674 mm
  0.45% mean relative
  7,762 comparisons
  z_w ~ 1.03 m
  camera heights 1.01-1.08 m

The two mechanisms already known to be in play for any moved number:
  v1.7's `reject_outlier_frames` (defaults `True` at `config/schema.py:329`, so the shipped 2026-02
    config that omits the key already picks up v1.7 behavior)
  v1.8's seeded `cv2.calibrateCamera` for Stage-1 in-air intrinsics
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: BLOCKING GATE — user adjudicates E2's nine §3 numbers</name>
  <files>.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-GATE-RESPONSE.md</files>
  <read_first>
    - `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md` — the full table
      plus its `## Manuscript impact`, `## Per-camera detail`, and `## Awaiting user decision`
      sections. This is the artifact under review.
    - `.planning/phases/19.1-experiment-suite-consolidation/19.1-04-SUMMARY.md` — the run's measured
      wall-clock and the one-line-per-quantity summary.
    - `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DATASET-FINDINGS.md` — A3's
      verdict and the shipped config, in case the user asks what the fresh run was configured with.
    - `19.1-CONTEXT.md` D-16 in full — both reasons the gate is real rather than ceremonial.
    - `19.1-VALIDATION.md` § Manual-Only Verifications (first row) and § Validation Sign-Off.
  </read_first>
  <what-built>
    E2's real-rig re-run against the current library completed (plan 19.1-04), emitting
    `camera_parameters.csv`, `reconstruction_errors.csv`, `reprojection_residuals.csv`,
    `real_rig_metrics.json`, the pipeline-written `benchmark.json`, and `calibration.json` under
    `experiments/results/`. The nine-row side-by-side delta table — committed §3 value vs fresh run
    value vs Δ vs Δ%, with a named mechanism for every moved number — is written to
    `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md`, along with a
    manuscript-impact assessment and a per-camera breakdown.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-DELTA-TABLE.md`.
    2. Read the nine-row table. For each row, decide whether you accept the fresh value as the
       number that goes into the revised §3.
    3. For each row marked MOVED, read the named mechanism and the `## Manuscript impact` entry, and
       decide: accept the new number and edit the prose, or investigate further before accepting.
       The word budget is at ~3,916 of 4,000 with captions, so a longer replacement sentence has a
       real cost — that tradeoff is yours, not the executor's.
    4. Check the `## Per-camera detail` section if an aggregate moved, to see whether it is
       attributable to one camera (the auxiliary fisheye and `e3v8250` are the two to look at first).
    5. Respond with, at minimum: an overall verdict, and for every MOVED row either ACCEPT or
       INVESTIGATE. If you want anything re-run, say so — E2 is only ~15-20 minutes of compute plus
       an already-cached dataset.
  </how-to-verify>
  <action>
Present the delta table's contents to the user in the response, then STOP. Do not proceed, do not
infer approval from the absence of an objection, and do not treat a green test suite or a green CI
run as satisfying this gate.

When the user responds, write
`.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-GATE-RESPONSE.md` containing:
  - A `## User response` section quoting the user's decision VERBATIM. Do not paraphrase, summarize,
    or normalize it. If they wrote three words, three words is the record.
  - A `## Per-number disposition` table with one row per delta-table row:
    `Quantity | Delta-table assessment | User disposition | Follow-up`. Every one of the nine rows
    must have a disposition; a blank is a gate that was not actually closed.
  - A `## Consequences` section listing any follow-up the response created: prose edits owed to the
    manuscript (outside this repo), a re-run requested, or a number to carry into Phase 19.2's E4
    grid (whose tenth point reuses E2's `benchmark.json`, so a rejected E2 run invalidates that
    reuse).
  - A `## Gate` line reading `CLOSED by explicit user response on <date>` — and only after the
    response exists.

If the user asks for a re-run or rejects a number, the gate is NOT closed: record the request under
`## Consequences`, leave the `## Gate` line as `OPEN`, and report that plan 19.1-04's run needs to be
repeated. Do not write `CLOSED` to make the phase advance.
  </action>
  <verify>
    <human-check>The user has responded, and `19.1-E2-GATE-RESPONSE.md` quotes that response verbatim with a disposition recorded for all nine quantities.</human-check>
    <automated>python -c "
import pathlib, re
p = pathlib.Path('.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-GATE-RESPONSE.md')
assert p.exists(), 'gate response file missing -- the gate is not closed'
t = p.read_text(encoding='utf-8')
for s in ['## User response','## Per-number disposition','## Consequences','## Gate']:
    assert s in t, s
rows = [l for l in t.splitlines() if l.startswith('| ') and 'Quantity' not in l and set(l.strip()) - set('|- ')]
assert len(rows) >= 9, f'only {len(rows)} dispositions recorded, need 9'
assert 'CLOSED by explicit user response' in t or 'OPEN' in t
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-GATE-RESPONSE.md` exists and contains a `## User response` section with the user's words quoted verbatim.
    - The `## Per-number disposition` table has a non-empty disposition for all nine §3 quantities.
    - The `## Gate` line reads `CLOSED by explicit user response on <date>` only if a user response is actually present; otherwise it reads `OPEN` with the reason.
    - The automated assertion block above exits 0.
    - No `## Gate: CLOSED` line exists without a corresponding `## User response` section — an approval with no response is exactly what D-16 forbids.
    - If the user requested a re-run, the gate line reads `OPEN` and plan 19.1-04 is flagged for repetition in the SUMMARY.
  </acceptance_criteria>
  <resume-signal>Reply with an overall verdict plus ACCEPT or INVESTIGATE for each MOVED row (e.g. "accept all", or "accept 1-7, investigate the fisheye and z_w"). Say so explicitly if you want E2 re-run.</resume-signal>
  <done>The user has responded explicitly, their response is recorded verbatim, all nine quantities carry a disposition, and the gate line reflects the actual outcome rather than a convenient one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automation to human decision | This plan's entire purpose is a boundary: a decision that automation must not make on the user's behalf. Nothing is written to `src/`, `experiments/`, or any result file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-33 | Elevation of Privilege | `workflow.auto_advance` self-approving a labeled human-verify gate, discharging a decision reserved for the user | mitigate | The gate is structural, not label-based: it requires a `## User response` section quoting the user verbatim and a disposition for all nine quantities, both asserted programmatically. The plan is marked `autonomous: false`, the task carries `gate="blocking-human"`, and the acceptance criteria forbid a `CLOSED` line without a corresponding response section. This threat is not hypothetical — a Phase 18 figure gate caught a real backwards-arrow bug that auto-approval would have rubber-stamped. |
| T-19.1-34 | Repudiation | A moved §3 number entering the manuscript with no record of who accepted it or why | mitigate | The verbatim response plus the per-number disposition table is the record, stored in the phase directory under version control alongside the delta table it adjudicates. |
| T-19.1-35 | Tampering | Writing `CLOSED` to advance the phase when the user asked for a re-run | mitigate | Explicit instruction and acceptance criterion: a re-run request leaves the gate `OPEN` and flags plan 19.1-04 for repetition. Downstream consequence recorded too — Phase 19.2's E4 grid reuses E2's `benchmark.json` for its tenth point, so a rejected run invalidates that reuse and must be surfaced, not buried. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; no code changes at all in this plan. |
</threat_model>

<verification>
1. `.planning/phases/19.1-experiment-suite-consolidation/19.1-E2-GATE-RESPONSE.md` exists with all four required sections.
2. Nine non-empty dispositions recorded.
3. The `## Gate` line matches the actual outcome; `CLOSED` appears only alongside a `## User response` section.
4. No file outside `.planning/phases/19.1-experiment-suite-consolidation/` was modified by this plan.
</verification>

<success_criteria>
- ROADMAP SC5 is complete: every §3 real-rig number is confirmed unchanged or recorded as moved with
  its new value, and the confirmation is an explicit user decision.
- VALIDATION's sign-off item "E2's blocking gate (D-16) has an explicit user response — NOT a CI
  pass" is satisfiable by inspecting one file.
- The phase can close.
</success_criteria>

<output>
Create `.planning/phases/19.1-experiment-suite-consolidation/19.1-08-SUMMARY.md` when done.
Record: the gate's outcome (CLOSED or OPEN), the per-number dispositions, and any follow-up owed —
particularly anything that affects Phase 19.2's E4 grid, whose tenth point reuses E2's
`benchmark.json`.
</output>
