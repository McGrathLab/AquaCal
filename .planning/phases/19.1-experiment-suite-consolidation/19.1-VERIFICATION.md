---
phase: 19.1-experiment-suite-consolidation
verified: 2026-07-27T00:00:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 19.1: Experiment Suite Consolidation Verification Report

**Phase Goal:** One `experiments/` directory, one implementation of every experiment, the
shared experiment verbs importable from the installed package, and E1/E2/E7 run against the
current instrumented library.

**Verified:** 2026-07-27
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP § Phase 19.1 Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Shared experiment verbs importable from `aquacal.datasets.pipelines`; `aquacal.datasets.__all__` also exports the three generators | ✓ VERIFIED | `python -c "from aquacal.datasets import calibrate_synthetic, compute_per_camera_errors, evaluate_reconstruction, generate_camera_array, generate_real_rig_array, generate_board_trajectory, generate_dense_xy_grid"` succeeds with no sys.path hack. `src/aquacal/datasets/__init__.py:50-69` lists all seven names in `__all__`. `grep -rn "from tests" src/` returns nothing — no `src/` module imports from `tests/`. |
| 2 | `experiments/` exists outside `src/` with `_io.py`, `_render.py`, `results/`, and a README mapping commands to artifacts/runtimes | ✓ VERIFIED | `experiments/_io.py` (five-flag argparse parent, CSV writer, `--check` comparator, resumability), `experiments/_render.py` (relocated `benchmarks/aggregate.py` via preserved git history), `experiments/results/` (populated with committed CSV/JSON), `experiments/README.md` present and referenced by CI. |
| 3 | Every experiment script honours the CLI contract; `--smoke` wired into CI | ✓ VERIFIED | `experiments/_io.py:54-79` defines `--seed`, `--out`, `--force`, `--smoke`, `--check`. `.github/workflows/test.yml:59-79` — `experiments-smoke` job runs `python -m experiments.e1_refractive_comparison --smoke`, `e2_real_rig --smoke`, `e7_interface_ablation --smoke` on `ubuntu-latest`/py3.12 only. |
| 4 | `tests/synthetic/experiments.py` gone (salvage table), `compare_refractive.py` moved, `benchmarks/` runner moved under `experiments/`, no two implementations remain | ✓ VERIFIED | `tests/synthetic/experiments.py` and `tests/synthetic/compare_refractive.py` do not exist. `benchmarks/` directory does not exist. `git log --follow --oneline -- experiments/_render.py` returns 4 commits (history preserved through `git mv`, commit `afe93ea`), `experiments/e4_benchmark_grid.py` returns 2. Both notebooks' export cells (`.to_csv`) are gone — verified by parsing both `.ipynb` JSON and finding zero `.csv`-writing cells. `19.1-06-SUMMARY.md`/`19.1-07-SUMMARY.md` document the keep/drop table and deletions. |
| 5 | E2's real-rig re-run emits `real_rig_metrics.json` + `benchmark.json`; every §3 number confirmed unchanged or recorded as moved | ✓ VERIFIED | Both files exist in `experiments/results/`. `19.1-E2-GATE-RESPONSE.md` records all nine §3 quantities reproducing at 0.000% delta, cross-checked directly against the live manuscript source (not the stale PDF), with an explicit, non-auto-approved user response (`workflow.auto_advance` disabled in `556206c` prior to execution). |
| 6 | E7 reports per-camera surface-height spread, height drift, focal/standoff drift+correlation, and conditioning across all four shared/per-camera × fixed/refined arms; RMSE not the headline | ✓ VERIFIED | `experiments/results/interface_ablation.csv` has 49 lines (48 data rows = 4 arms × 12 cameras) with columns including `water_z_error_mm`, `camera_height_drift_mm`, `focal_drift_pct`, `standoff_m`, and `reprojection_rms_px_control` (name itself marks it non-headline). `interface_ablation_conditioning.json` present. Module docstring (`experiments/e7_interface_ablation.py:14-42`) explicitly labels the failure "height/distance degeneracy," never focal/standoff. |
| 7 | Ported E1 reproduces every value in the committed `exp{1,2,3}` CSVs or explains divergence; gauge-freedom correction survives with explanation | ✓ VERIFIED | `19.1-E1-REPRODUCTION.md` § Escalation: `NONE` — all headline numbers reproduce within `CHECK_RTOL`; worst divergence 8.3e-7 percentage points (last-significant-digit noise). `gauge_correct_z` keyword documented in `pipelines.py:241-257` with the reference-camera-zero rationale. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/aquacal/datasets/pipelines.py` | Houses `calibrate_synthetic`, `evaluate_reconstruction`, `compute_per_camera_errors` | ✓ VERIFIED | All three present, correct signatures, `gauge_correct_z`/`seed`/`timings_out`/`diagnostics_out` params confirmed |
| `src/aquacal/config/schema.py::PerCameraErrors` | `TypedDict` with 12 keys matching runtime dict keys | ✓ VERIFIED | `PerCameraErrors.__annotations__` == 12 keys; runtime `errors[cam_name] = {...}` dict literal in `pipelines.py:310-320` matches exactly |
| `experiments/_io.py` | I/O-only: paths, CLI parent, CSV writer, `--check` comparator | ✓ VERIFIED | Five `add_argument` calls present |
| `experiments/_render.py` | Relocated `benchmarks/aggregate.py`, history preserved | ✓ VERIFIED | `git log --follow` returns 4 commits including the two Phase 19 originals |
| `experiments/e4_benchmark_grid.py` | Relocated `sweep_runner.py` skeleton, not run | ✓ VERIFIED | `git log --follow` returns 2 commits; no grid-run artifacts in `results/` |
| `experiments/results/*.csv` | Three E2 CSVs, byte-identical headers to figure-generator expectations | ✓ VERIFIED | Headers confirmed byte-identical via `head -1` |
| `experiments/results/interface_ablation.csv` | E7's 48-row output | ✓ VERIFIED | 49 lines (1 header + 48 data) |
| `experiments/results/*.json` (benchmark records) | `schema_version: 1`, settled stage keys | ✓ VERIFIED | All 7 benchmark-shaped JSONs (`benchmark.json`, both `e1_benchmark_*`, all four `e7_benchmark_*`) have `schema_version: 1` and stage keys drawn only from `stage3_interface_optimization`/`stage3_intrinsic_pass`/`auxiliary_registration_*` |
| `19.1-E2-GATE-RESPONSE.md` | Explicit user response to blocking gate | ✓ VERIFIED | Exists, dated 2026-07-27, itemized responses to all three gate items |
| `tests/synthetic/experiments.py`, `compare_refractive.py` | Deleted | ✓ VERIFIED | Both absent |
| `benchmarks/` | Deleted | ✓ VERIFIED | Directory absent; only stale prose references remain in `experiments/e4_benchmark_grid.py` docstring and `README.md` (historical, not functional) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `experiments/e1_refractive_comparison.py` | `aquacal.datasets.pipelines` | import + call | WIRED | Imports promoted verbs, calls `calibrate_synthetic` with `timings_out`/`diagnostics_out` |
| `experiments/e2_real_rig.py` | `aquacal.io.assemble_benchmark_record` | direct call (D-09) | WIRED | `benchmark.json` present with correct schema |
| `experiments/e7_interface_ablation.py` | `optimize_interface(diagnostics_out=...)` | out-parameter | WIRED | Four `e7_benchmark_*.json` records each carry populated `stages` blocks |
| CI `experiments-smoke` job | `python -m experiments.e{1,2,7}_*` | `--smoke` flag | WIRED | `.github/workflows/test.yml:77-79` |
| `experiments/_render.py` | `experiments/results/*.csv` | CSV read | WIRED (not exercised — E4 grid deliberately not run this phase) | Confirmed skeleton only, per D-03 scope |

### Data-Flow Trace

Not applicable in the dynamic-UI sense — this phase's artifacts are CLI scripts and library
functions, not rendered components. Data flow verified directly via the committed CSV/JSON
contents (real, non-empty, matching expected schemas) and the pytest suite exercising the
promoted verbs end-to-end.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Package imports work with no sys.path hack | `python -c "from aquacal.datasets import ..."` | Success, no error | ✓ PASS |
| `PerCameraErrors` keys match runtime dict keys | `python -c "print(sorted(PerCameraErrors.__annotations__))"` vs dict literal in `pipelines.py` | 12/12 keys match | ✓ PASS |
| `experiments/` not pytest-collected | `python -m pytest --collect-only -q experiments/` | "no tests collected" | ✓ PASS |
| Full suite passes | `python -m pytest tests/ -m "not slow" -q` | 874 passed, 33 deselected, 0 failed | ✓ PASS |
| `git mv` history preserved on relocated files | `git log --follow --oneline -- experiments/_render.py` | 4 commits (>1) | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` conventional probes found, and none declared in PLAN/SUMMARY
files for this phase. Step 7c: SKIPPED (no probes declared or discovered).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| EXP-01 | 19.1-01 | Verbs importable from `aquacal.datasets.pipelines` | ✓ SATISFIED | Truth #1 |
| EXP-02 | 19.1-02, 19.1-03, 19.1-07 | `experiments/` package, CLI contract, README, CI smoke | ✓ SATISFIED | Truths #2, #3 |
| EXP-03 | 19.1-02, 19.1-07 | Deletion/relocation, no two implementations, stale-path sweep | ✓ SATISFIED | Truth #4 |
| EXP-04 | 19.1-04, 19.1-08 | E2 re-run + gate | ✓ SATISFIED | Truth #5 |
| EXP-05 | 19.1-05 | E7 four-arm ablation | ✓ SATISFIED | Truth #6 |
| EXP-06 | 19.1-06, 19.1-07 | E1 port, reproduce-or-explain | ✓ SATISFIED | Truth #7 |

REQUIREMENTS.md's traceability table (lines 219-224) still shows EXP-01..06 as "Pending" —
this is a stale-bookkeeping note, not a functional gap (the roadmap's own progress table
correctly shows 19.1 as 8/8 complete). Recommend the next housekeeping pass flip these to
"Done."

No orphaned requirements found for this phase — DATA-01a/01b are correctly scoped to Phase
21, not this phase, per both ROADMAP.md and REQUIREMENTS.md.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | none found | — | `grep` for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/"not yet implemented" across `experiments/*.py`, `pipelines.py`, `schema.py` returned zero matches |

### Recorded Deviations (confirmed documented, not failures)

| Deviation | Documented in | Verified |
|-----------|---------------|----------|
| `.pre-commit-config.yaml` — two exclusions added for `experiments/results/` (detect-secrets line 25, check-added-large-files line 38) | `19.1-04-SUMMARY.md` § Deviations | ✓ present in file, matches `.pre-commit-config.yaml:18-38` |
| `experiments/results/interface_ablation_conditioning.npz` gitignored | `19.1-05-SUMMARY.md` | ✓ present in `.gitignore:221-223` |
| `experiments/results/exp2_spatial_errors.csv` gitignored (not committed, ~11MB exceeds 1000KB gate) | `19.1-06-SUMMARY.md` § Deviations | ✓ present in `.gitignore:231`, file exists on disk |
| `timings_out` out-parameter added to `calibrate_synthetic`, outside plan 01's original scope | `19.1-06-SUMMARY.md` § Deviations | ✓ present in `pipelines.py:33` |

### Human Verification Required

None. The one blocking human-verification checkpoint this phase contained (D-16, E2's §3
delta table) already has its explicit, dated, non-auto-approved user response recorded in
`19.1-E2-GATE-RESPONSE.md`, and `workflow.auto_advance` was confirmed disabled beforehand
(`556206c`). No further human verification items were identified.

### Gaps Summary

None. All seven roadmap success criteria are verified against the live codebase, not
SUMMARY claims. The full test suite passes (874/874, 0 failures). Every specifically-flagged
claim in the verification brief was checked directly:

- No `src/` module imports from `tests/`.
- `PerCameraErrors` TypedDict's 12 declared keys equal its 12 runtime keys.
- `benchmarks/` is gone; `git log --follow` confirms history preservation for both relocated files.
- `tests/synthetic/experiments.py` and `compare_refractive.py` are deleted; both notebooks' export cells are gone.
- All three E2 CSV headers are byte-identical to the pinned schemas.
- E7's CSV has 48 data rows, an `intrinsics_source` column, and an explicitly-named control RMSE column; the module docstring labels the failure height/distance, never focal/standoff.
- Every benchmark-shaped JSON uses `schema_version: 1` and only the settled stage keys.
- `experiments/` is neither pytest-collected nor wheel-packaged.
- The full test suite passes.
- The blocking gate response exists with an explicit, dated user reply.
- All four recorded deviations are documented in their respective SUMMARY files.

---

_Verified: 2026-07-27_
_Verifier: Claude (gsd-verifier)_
