---
phase: 19.1-experiment-suite-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aquacal/datasets/pipelines.py
  - src/aquacal/datasets/__init__.py
  - src/aquacal/calibration/pipeline.py
  - src/aquacal/calibration/__init__.py
  - tests/synthetic/experiment_helpers.py
  - tests/unit/test_datasets_pipelines.py
  - tests/unit/test_datasets.py
autonomous: true
requirements: [EXP-01]

must_haves:
  truths:
    - "A pip-installed reader can run `from aquacal.datasets import calibrate_synthetic, compute_per_camera_errors, evaluate_reconstruction, generate_camera_array, generate_real_rig_array, generate_board_trajectory, generate_dense_xy_grid` with no sys.path manipulation and no import from tests/ (ROADMAP SC1, D-05, RESEARCH widen-list gap)"
    - "calibrate_synthetic accepts an explicit seed instead of hardcoding 42, so Part 4 Rule 1 (--seed threads through) is satisfiable by the experiment layer (D-06.1)"
    - "No module under src/ imports from tests.* (D-06.2)"
    - "compute_per_camera_errors returns every column the committed exp1_parameter_errors.csv needs, so experiments/ never re-derives gt_/est_ centers at the call site (D-06.3, P2)"
    - "gauge_correct_z=True subtracts the free-camera mean raw Z error from EVERY camera including the reference; it does not special-case cam0 to zero (D-07, RESEARCH Pitfall 2)"
    - "Every new keyword defaults to the pre-promotion behavior, so existing callers are bit-identical (Phases 16/17/19 zero-numerical-change discipline)"
    - "E7 can obtain per-camera water_z spread statistics from a public library function instead of duplicating five lines of untested logic (RESEARCH gap, A4)"
  artifacts:
    - path: "src/aquacal/datasets/pipelines.py"
      provides: "calibrate_synthetic, evaluate_reconstruction, compute_per_camera_errors — promoted, widened"
      exports: ["calibrate_synthetic", "evaluate_reconstruction", "compute_per_camera_errors"]
      min_lines: 260
    - path: "tests/synthetic/experiment_helpers.py"
      provides: "thin re-export shim mirroring tests/synthetic/ground_truth.py"
      contains: "from aquacal.datasets.pipelines import"
    - path: "tests/unit/test_datasets_pipelines.py"
      provides: "bit-exact determinism guard, shim test, gauge-correction test, default-unchanged test"
  key_links:
    - from: "src/aquacal/datasets/__init__.py"
      to: "src/aquacal/datasets/pipelines.py"
      via: "barrel import + __all__"
      pattern: "from aquacal\\.datasets\\.pipelines import"
    - from: "src/aquacal/datasets/pipelines.py"
      to: "aquacal.datasets.synthetic"
      via: "direct import replacing the tests.synthetic.ground_truth hack"
      pattern: "from aquacal\\.datasets\\.synthetic import"
    - from: "src/aquacal/calibration/__init__.py"
      to: "build_interface_spread_report"
      via: "barrel import + __all__"
      pattern: "build_interface_spread_report"
---

<objective>
Promote the three shared experiment verbs out of `tests/synthetic/experiment_helpers.py` into
`src/aquacal/datasets/pipelines.py` with the three mandatory signature changes (D-06), the
gauge-freedom correction as an explicit keyword (D-07), and `diagnostics_out` forwarding (D-10);
widen `aquacal.datasets.__all__`; leave the old path as a re-export shim; and promote
`_build_interface_spread_report` to public so E7 can reuse it.

Purpose: EXP-01 is a hard precedent for every other requirement in this phase — every experiment
script imports these verbs. `src/aquacal/datasets/pipelines.py` does not exist yet.
Output: A public `aquacal.datasets.pipelines` module, a widened barrel, a shim, a public
`build_interface_spread_report`, and unit tests proving zero numerical change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** this plan forks from `93cb1f37339091fd137c9e79b222b1c4c48c6131`. If your
worktree's `git merge-base HEAD 93cb1f3` is not that sha, stop and re-fork — a stale base has
bitten Phase 18 and Phase 19 both.
**PYTHONPATH:** the editable install resolves to the main checkout, not a worktree. If working in
a worktree, `export PYTHONPATH="$(pwd)/src"` before running pytest or its passing tests are
meaningless.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-CONTEXT.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-PATTERNS.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Extracted live from the codebase 2026-07-25. -->

Current (pre-promotion) signatures in `tests/synthetic/experiment_helpers.py`:
  calibrate_synthetic(scenario: SyntheticScenario, n_water: float,
                      refine_intrinsics: bool = True)
      -> tuple[CalibrationResult, DetectionResult]              # line 41
  evaluate_reconstruction(calibration: CalibrationResult, board: BoardGeometry,
                          test_detections: DetectionResult) -> DistanceErrors   # line 181
  compute_per_camera_errors(result: CalibrationResult,
                            ground_truth: SyntheticScenario)
      -> dict[str, dict[str, float]]                            # line 206

Existing out-parameters to forward (no new plumbing — D-10):
  optimize_interface(..., diagnostics_out: SolverDiagnostics | None = None)   # interface_estimation.py:147
  joint_refinement(..., diagnostics_out: SolverDiagnostics | None = None)     # refinement.py:60
  SolverDiagnostics                                                          # calibration/_observability.py:75

Function to promote (`src/aquacal/calibration/pipeline.py:731`, body already read — pure, 5-line
stats over a dict, no calibration-object dependency, one internal call site at line 1410):
  _build_interface_spread_report(distances: dict[str, float], stage: str) -> dict

Current `src/aquacal/datasets/__init__.py` exports:
  create_scenario, generate_synthetic_detections, SyntheticScenario,
  load_example, ExampleDataset, list_datasets, clear_cache, get_cache_info

Generator functions already defined in `aquacal.datasets.synthetic` but NOT yet in the barrel:
  generate_camera_array, generate_real_rig_array, generate_board_trajectory,
  generate_dense_xy_grid (synthetic.py:408), generate_real_rig_trajectory (synthetic.py:355)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/aquacal/datasets/pipelines.py with the three promoted verbs</name>
  <files>src/aquacal/datasets/pipelines.py</files>
  <read_first>
    - `tests/synthetic/experiment_helpers.py` — the FULL file. Lines 41-178 (`calibrate_synthetic`),
      181-203 (`evaluate_reconstruction`), 206-271 (`compute_per_camera_errors`) are the bodies to
      carry over. Lines 1-38 are the import block to replace. Lines 274-330 are the
      `__main__` smoke block to DROP.
    - `19.1-PATTERNS.md` § `src/aquacal/datasets/pipelines.py` — the exact import replacement, the
      widened `errors[cam_name]` dict literal, and the `gauge_correct_z` computation.
    - `19.1-RESEARCH.md` § "The gauge-freedom correction, quoted verbatim" — the notebook cell this
      keyword reproduces, AND the block titled "Docstring text to carry into pipelines.py's
      gauge_correct_z keyword", which is pre-approved wording to copy verbatim.
    - `19.1-RESEARCH.md` § Pitfall 2 — why cam0 is NOT special-cased.
    - `src/aquacal/validation/conditioning.py` lines 51-120 — the Google-docstring / `NDArray`
      house style the three promoted docstrings must match (Args/Returns blocks, one sentence of
      "why"). The pre-promotion docstrings are thinner than this bar; raise them.
    - `.claude/rules/code-style.md`, `.claude/rules/source-code.md`.
  </read_first>
  <action>
Create `src/aquacal/datasets/pipelines.py`. Module docstring: "Shared experiment pipelines over
synthetic scenarios." Carry the three function bodies over from `experiment_helpers.py`
**unchanged except for the deltas below** — this is a promotion, not a rewrite.

Import block (D-06.2): keep the existing `aquacal.*` imports verbatim
(`build_pose_graph`, `estimate_extrinsics`, `optimize_interface`, `joint_refinement`,
`CalibrationMetadata`, `CalibrationResult`, `CameraCalibration`, `DetectionResult`,
`DiagnosticsData`, `InterfaceParams`, `BoardGeometry`, `DistanceErrors`,
`compute_3d_distance_errors`). DELETE the `import sys`, `from pathlib import Path`, the
`_project_root` / `sys.path.insert` block, and the `from tests.synthetic.ground_truth import ...`
block. Replace the last with
`from aquacal.datasets.synthetic import SyntheticScenario, generate_synthetic_detections`.
Add `from aquacal.calibration._observability import SolverDiagnostics` for the type hint.

`calibrate_synthetic` signature becomes:
`calibrate_synthetic(scenario: SyntheticScenario, n_water: float, refine_intrinsics: bool = True,
seed: int = 42, diagnostics_out: dict[str, SolverDiagnostics] | None = None)
-> tuple[CalibrationResult, DetectionResult]`.
  - D-06.1: the `generate_synthetic_detections(...)` call currently at line 69 hardcodes
    `seed=42`; pass `seed=seed` instead. Default 42 preserves E1's reproduction bar (D-19).
  - D-10: when `diagnostics_out` is not None, forward
    `diagnostics_out.get("stage3_interface_optimization")` to `optimize_interface(...,
    diagnostics_out=...)` and `diagnostics_out.get("stage3_intrinsic_pass")` to
    `joint_refinement(..., diagnostics_out=...)`. These two keys are the settled Phase-18 stage
    vocabulary — do not invent experiment-local stage names. When `diagnostics_out` is None, pass
    `None` through so behavior is byte-identical to today.
  - Leave the `refine_intrinsics=False` branch (`opt_intrinsics = scenario.intrinsics`, currently
    lines 135-137) exactly as it is. Do NOT try to make it "realistically fixed" here — that
    ambiguity is E7's to resolve (plan 19.1-05), and changing it would break E1's reproduction.
    Add one docstring sentence to `refine_intrinsics` stating that `False` holds intrinsics at
    the scenario's ground-truth values, so no caller can mistake it for air-estimated intrinsics.

`evaluate_reconstruction`: carry over with the signature FROZEN byte-identical
(`calibration, board, test_detections`, in that order — RESEARCH Pitfall 4). Do not reorder, do
not rename, do not add keywords. Raise the docstring to house style only.

`compute_per_camera_errors` signature becomes:
`compute_per_camera_errors(result: CalibrationResult, ground_truth: SyntheticScenario, *,
gauge_correct_z: bool = False) -> dict[str, dict[str, float]]`.
  - D-06.3: extend each camera's returned dict with six center keys and one RMS key, assembled
    from data already in scope in the existing loop: `gt_x_m`/`gt_y_m`/`gt_z_m` from the
    ground-truth camera center, `est_x_m`/`est_y_m`/`est_z_m` from the calibrated camera center,
    and `reprojection_rms_px` from `result.diagnostics.reprojection_error_rms` (a
    calibration-level scalar, deliberately repeated on every camera's row). KEEP the existing
    `focal_length_error_pct`, `z_position_error_mm`, `xy_position_error_mm`, `k1_error`,
    `k2_error` keys — removing a documented return value is a larger surface change than adding.
  - D-07 / Pitfall 2: after the raw per-camera dict is fully built, when `gauge_correct_z=True`,
    compute `camera_names = sorted(ground_truth.intrinsics, key=lambda s: int(s.replace("cam",
    "")))`, take the mean of `z_position_error_mm` across `camera_names[1:]` (the FREE cameras,
    excluding the reference), and subtract that single scalar from `z_position_error_mm` for
    EVERY camera including `camera_names[0]`. Do NOT special-case the reference camera to zero.
    Do not touch `xy_position_error_mm` — the reference camera's `0.0` there is an unrelated
    geometric fact (it is pinned at the world origin).
  - Copy the pre-approved `gauge_correct_z` docstring wording verbatim from RESEARCH.md's
    "Docstring text to carry into pipelines.py's gauge_correct_z keyword" block, including the
    sentence explaining why the reference camera's `xy_position_error_mm` is exactly `0.0` while
    its corrected `z_position_error_mm` is a small nonzero residual.

Do not port the `if __name__ == "__main__":` block. Do not add plotting. Do not add any file I/O —
these are pure compute functions (P2/P3).
  </action>
  <verify>
    <automated>python -c "from aquacal.datasets.pipelines import calibrate_synthetic, compute_per_camera_errors, evaluate_reconstruction; import inspect; s=inspect.signature(calibrate_synthetic); assert 'seed' in s.parameters and s.parameters['seed'].default==42; assert 'diagnostics_out' in s.parameters; g=inspect.signature(compute_per_camera_errors); assert g.parameters['gauge_correct_z'].default is False; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from aquacal.datasets.pipelines import calibrate_synthetic, compute_per_camera_errors, evaluate_reconstruction"` exits 0.
    - `grep -c "sys.path\|from tests\." src/aquacal/datasets/pipelines.py` returns 0.
    - `grep -c "def calibrate_synthetic\|def evaluate_reconstruction\|def compute_per_camera_errors" src/aquacal/datasets/pipelines.py` returns 3.
    - `python -c "import inspect,aquacal.datasets.pipelines as p; print(list(inspect.signature(p.evaluate_reconstruction).parameters))"` prints exactly `['calibration', 'board', 'test_detections']`.
    - `grep -c 'gauge' src/aquacal/datasets/pipelines.py` is at least 5 (keyword, docstring paragraph, implementation).
    - `python - <<'EOF'` asserting all twelve keys are present in one camera's error dict for a
      cheap scenario: keys `focal_length_error_pct, z_position_error_mm, xy_position_error_mm,
      k1_error, k2_error, gt_x_m, gt_y_m, gt_z_m, est_x_m, est_y_m, est_z_m, reprojection_rms_px`.
    - `ruff check src/aquacal/datasets/pipelines.py && ruff format --check src/aquacal/datasets/pipelines.py` exits 0.
    - No `matplotlib`, `pandas`, `open(`, or `to_csv` appears in the file.
  </acceptance_criteria>
  <done>`aquacal.datasets.pipelines` imports cleanly, exposes the three verbs with the D-06/D-07/D-10 signatures, contains no `tests.` import and no `sys.path` manipulation, and passes Ruff.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the datasets barrel, convert experiment_helpers.py to a shim, promote build_interface_spread_report</name>
  <files>src/aquacal/datasets/__init__.py, tests/synthetic/experiment_helpers.py, src/aquacal/calibration/pipeline.py, src/aquacal/calibration/__init__.py</files>
  <read_first>
    - `src/aquacal/datasets/__init__.py` — the full current file (barrel to widen in place).
    - `src/aquacal/io/__init__.py` lines 1-45 — the `# From <module>.py (<phase>):` category-comment
      convention to mirror in `__all__`.
    - `tests/synthetic/ground_truth.py` — the full 21-line file. This IS the shim pattern, not
      merely an example of it.
    - `src/aquacal/calibration/pipeline.py` lines 731-763 (the function to rename) and line 1410
      (its single internal call site).
    - `src/aquacal/calibration/__init__.py` lines 1-40 — the `from aquacal.calibration.pipeline
      import (...)` group to extend, and the `__all__` category-comment style.
    - `19.1-PATTERNS.md` §§ `src/aquacal/datasets/__init__.py`, `tests/synthetic/experiment_helpers.py`,
      `src/aquacal/calibration/pipeline.py`.
    - `.claude/rules/source-code.md`.
  </read_first>
  <action>
**`src/aquacal/datasets/__init__.py`** — extend the existing
`from aquacal.datasets.synthetic import (...)` block with exactly four names:
`generate_board_trajectory`, `generate_camera_array`, `generate_dense_xy_grid`,
`generate_real_rig_array`. Add a new block
`from aquacal.datasets.pipelines import (calibrate_synthetic, compute_per_camera_errors,
evaluate_reconstruction)`. Extend `__all__` with all seven names, grouped under a
`# From pipelines.py (Phase 19.1 X1 promotion):` comment for the three verbs and appended to the
synthetic group for the four generators.
Do NOT add `generate_real_rig_trajectory`: RESEARCH OQ1 resolved that
`create_scenario("realistic")` already calls it internally at `synthetic.py:760-778`, so no
experiment script needs a direct import, and the notebook's import of it is vestigial.
`generate_dense_xy_grid` IS required — E1's `TEST_DEPTHS` sweep calls it directly (this is the
research gap that completes D-05's three-name list).

**`tests/synthetic/experiment_helpers.py`** — replace the entire file with a thin re-export shim
shaped exactly like `tests/synthetic/ground_truth.py`: a module docstring stating that the real
implementations now live in `aquacal.datasets.pipelines` and this path is kept for backward
compatibility, `from __future__ import annotations`, and one
`from aquacal.datasets.pipelines import (calibrate_synthetic, compute_per_camera_errors,
evaluate_reconstruction)` block with `# noqa: F401`. Nothing else — no logic, no `__main__` block,
no `sys.path` insert.

**`src/aquacal/calibration/pipeline.py`** — rename `_build_interface_spread_report` to
`build_interface_spread_report` (drop the leading underscore only; body, docstring, and return
shape unchanged — this is A4, a pure rename) and update its single call site at line 1410 inside
`run_calibration_from_config`'s `if not config.shared_interface:` branch.

**`src/aquacal/calibration/__init__.py`** — add `build_interface_spread_report` to the existing
`from aquacal.calibration.pipeline import (...)` group (alphabetically first in that group) and to
`__all__` under the pipeline category comment.
  </action>
  <verify>
    <automated>python -c "from aquacal.datasets import calibrate_synthetic, compute_per_camera_errors, evaluate_reconstruction, generate_board_trajectory, generate_camera_array, generate_dense_xy_grid, generate_real_rig_array; from aquacal.calibration import build_interface_spread_report; from tests.synthetic.experiment_helpers import calibrate_synthetic as s; import aquacal.datasets as d; assert all(n in d.__all__ for n in ['calibrate_synthetic','compute_per_camera_errors','evaluate_reconstruction','generate_board_trajectory','generate_camera_array','generate_dense_xy_grid','generate_real_rig_array']); import aquacal.calibration as c; assert 'build_interface_spread_report' in c.__all__; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The single `python -c` line above exits 0.
    - `python -c "import aquacal.datasets as d; assert 'generate_real_rig_trajectory' not in d.__all__"` exits 0.
    - `grep -c "_build_interface_spread_report" src/aquacal/calibration/pipeline.py` returns 0.
    - `wc -l tests/synthetic/experiment_helpers.py` is under 20; `grep -c "def \|sys.path" tests/synthetic/experiment_helpers.py` returns 0.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 (846 tests were green at phase start; no regression).
    - `ruff check . && ruff format --check src/ tests/` exits 0.
  </acceptance_criteria>
  <done>Seven new names resolve from `aquacal.datasets`, `build_interface_spread_report` resolves from `aquacal.calibration`, `experiment_helpers.py` is a sub-20-line shim, and the full fast suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Zero-numerical-change and export-surface tests</name>
  <files>tests/unit/test_datasets_pipelines.py, tests/unit/test_datasets.py</files>
  <behavior>
    - `test_all_exports`: every one of the seven widened `aquacal.datasets.__all__` names imports
      and is callable/typed; `generate_real_rig_trajectory` is deliberately absent.
    - `test_shim_reexports`: `tests.synthetic.experiment_helpers.calibrate_synthetic is
      aquacal.datasets.pipelines.calibrate_synthetic` (identity, not merely equality) — same for
      the other two verbs.
    - `test_default_unchanged_gauge`: `compute_per_camera_errors(result, scenario)` (default
      `gauge_correct_z=False`) returns `z_position_error_mm` values exactly equal
      (`np.testing.assert_array_equal`, rtol=0 atol=0) to the raw `(C_est_z - C_gt_z) * 1000`
      recomputed independently in the test — proving the new keyword is inert at its default.
    - `test_gauge_correction_subtracts_free_camera_mean`: with `gauge_correct_z=True`, every
      camera's `z_position_error_mm` equals its raw value minus `mean(raw over cameras[1:])`,
      INCLUDING the reference camera, and the reference camera's value is NOT exactly 0.0 (Pitfall
      2's explicit guard against a hardcoded-zero implementation).
    - `test_reference_camera_xy_error_is_exactly_zero`: the reference camera's
      `xy_position_error_mm` is exactly `0.0` under both `gauge_correct_z` values.
    - `test_widened_keys_present`: all twelve keys named in Task 1 appear for every camera.
    - `test_seed_threads_through`: `calibrate_synthetic(..., seed=7)` and `seed=7` again give
      `np.testing.assert_array_equal`-identical extrinsics and RMS; `seed=7` vs `seed=8` differ.
      Marked `@pytest.mark.slow`.
    - `test_bit_exact_repeat`: two `calibrate_synthetic` calls with identical arguments produce
      `assert_array_equal`-identical `R`, `t`, per-camera `water_z`, and RMS. Marked
      `@pytest.mark.slow`.
  </behavior>
  <read_first>
    - `tests/synthetic/test_per_camera_interface.py` lines 40-90, especially
      `test_shared_mode_end_to_end_bit_exact` — the exact `np.testing.assert_array_equal` (NOT
      `assert_allclose`) + `@pytest.mark.slow` pattern this codebase uses for
      identical-inputs-identical-numbers guards. Copy that shape.
    - `tests/unit/test_datasets.py` — the FULL existing file. Extend it in place for the export
      surface. Do NOT create `tests/unit/test_datasets_init.py`; the validation contract closed
      that gap on 2026-07-25 by confirming this file already exists and already asserts exactly
      this kind of import surface.
    - `src/aquacal/datasets/pipelines.py` as written in Task 1.
    - `19.1-VALIDATION.md` § Per-Requirement Verification Map, the four EXP-01 rows and the
      EXP-06 gauge row — they name the `-k` selectors the commands below must match
      (`default_unchanged`, `shim`, `gauge`, `all_exports`).
    - `19.1-RESEARCH.md` § Pitfall 2 for the committed reference values.
  </read_first>
  <action>
Create `tests/unit/test_datasets_pipelines.py` covering the eight behaviors above. Name the tests
so the validation contract's `-k` selectors hit: include the substrings `default_unchanged`,
`shim`, and `gauge` in the relevant test names. Extend `tests/unit/test_datasets.py` with a test
whose name contains `all_exports`.

Use the smallest scenario that exercises real code for the slow tests — `create_scenario` with a
small preset, `@pytest.mark.slow` on anything that runs an optimization, so
`pytest tests/ -m "not slow"` stays under the existing ~2-4 min budget. The gauge and
widened-keys tests can run against a cheap scenario or a hand-constructed `CalibrationResult` +
`SyntheticScenario` pair if that is faster — but if hand-constructing, the raw-error arithmetic
must still come from the real `compute_per_camera_errors`, not a reimplementation.

Use `np.testing.assert_array_equal` for every "unchanged"/"identical" assertion. Do not use
`assert_allclose` or `pytest.approx` for those — the discipline established in Phases 16/17/19 is
exact equality, and a tolerance here would hide exactly the drift these tests exist to catch.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_datasets_pipelines.py tests/unit/test_datasets.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_datasets_pipelines.py -x -q` exits 0.
    - `python -m pytest tests/unit/test_datasets_pipelines.py -k default_unchanged -x -q` collects at least 1 test and exits 0.
    - `python -m pytest tests/unit/test_datasets_pipelines.py -k shim -x -q` collects at least 1 test and exits 0.
    - `python -m pytest tests/unit/test_datasets_pipelines.py -k gauge -x -q` collects at least 2 tests and exits 0.
    - `python -m pytest tests/unit/test_datasets.py -k all_exports -x -q` collects at least 1 test and exits 0.
    - `grep -c "assert_allclose\|pytest.approx" tests/unit/test_datasets_pipelines.py` returns 0.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 and reports at least 846 passed.
    - `python -m pytest tests/unit/test_datasets_pipelines.py -m slow -q` exits 0.
  </acceptance_criteria>
  <done>All eight behaviors have passing tests, the fast suite is green with no regression from 846, and no tolerance-based assertion appears in the zero-numerical-change guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds pure compute functions to an offline library. No new network, filesystem, subprocess, deserialization, or user-input surface. All inputs are in-process Python dataclasses constructed by the caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-01 | Tampering | `build_interface_spread_report` rename changes the pipeline's per-camera-mode branch behavior | mitigate | Rename only; verified by `pytest tests/ -m "not slow"` staying green and `grep -c "_build_interface_spread_report"` returning 0 (no orphaned call site). RESEARCH A4 rates this LOW. |
| T-19.1-02 | Tampering | New default-valued keywords (`seed`, `diagnostics_out`, `gauge_correct_z`) silently alter results for existing callers | mitigate | `test_default_unchanged_gauge` + `test_bit_exact_repeat` assert exact equality (`rtol=0, atol=0`) at default values; this is the Phase 16/17/19 zero-numerical-change discipline, and the phase's whole reproduction bar (D-19) depends on it. |
| T-19.1-03 | Information Disclosure | — | accept | No secrets, credentials, PII, or network data touched. Offline scientific tooling run by one trusted researcher over version-controlled files (RESEARCH § Security Domain). |
| T-19.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this plan or this phase. RESEARCH § Package Legitimacy Audit: "Not applicable — this phase installs no new external packages." No `slopcheck` gate required. |
</threat_model>

<verification>
1. `python -m pytest tests/ -m "not slow" -q` — exits 0, at least 846 passed.
2. `python -m pytest tests/unit/test_datasets_pipelines.py -m slow -q` — exits 0.
3. `python -c "from aquacal.datasets import calibrate_synthetic, compute_per_camera_errors, evaluate_reconstruction, generate_board_trajectory, generate_camera_array, generate_dense_xy_grid, generate_real_rig_array"` — exits 0.
4. `python -c "from aquacal.calibration import build_interface_spread_report"` — exits 0.
5. `! grep -rn "sys.path\|from tests\." src/aquacal/` — no match under `src/`.
6. `ruff check . && ruff format --check src/ tests/` — exits 0.
7. `pre-commit run --all-files` — exits 0.
</verification>

<success_criteria>
- ROADMAP SC1 is TRUE: the three verbs plus the four generators are importable from
  `aquacal.datasets` by a pip-installed reader, with no `tests.` import anywhere under `src/`.
- `seed` is a real parameter (D-06.1); `gauge_correct_z` is an explicit keyword whose docstring
  carries the pre-approved explanation (D-07); the six center columns plus `reprojection_rms_px`
  are library-returned, not call-site-assembled (D-06.3).
- `diagnostics_out` forwards under the settled stage keys `stage3_interface_optimization` and
  `stage3_intrinsic_pass` (D-09/D-10) — no experiment-local stage vocabulary.
- `build_interface_spread_report` is public, so E7 reuses it rather than duplicating five lines.
- Every new keyword is provably inert at its default value.
</success_criteria>

<output>
Create `.planning/phases/19.1-experiment-suite-consolidation/19.1-01-SUMMARY.md` when done.
Record: the exact final signatures of all three verbs, the final `aquacal.datasets.__all__` list,
and confirmation that `generate_real_rig_trajectory` was deliberately excluded per RESEARCH OQ1.
</output>
