---
phase: 19.1-experiment-suite-consolidation
plan: 07
subsystem: experiments
tags: [experiments, cleanup, deletion, ci, readme, notebooks]

# Dependency graph
requires:
  - "19.1-04 (E2 real-rig re-run, delta table, DATA-01a framing)"
  - "19.1-05 (E7 interface ablation harness and results)"
  - "19.1-06 (E1 port, 19.1-E1-REPRODUCTION.md deletion gate CLEARED)"
provides:
  - "tests/synthetic/experiments.py and compare_refractive.py deleted, with an auditable 20-row keep/drop table"
  - "docs/tutorials/{01_full_pipeline,02_synthetic_validation}.ipynb -- export cells removed, no re-execution"
  - ".planning/architecture.md, .planning/codebase/{STRUCTURE,CONCERNS}.md -- stale experiments/ paths swept"
  - "experiments/README.md -- provenance table, CLI contract, gauge explanation, E7 framing, both E2 paths"
  - ".github/workflows/test.yml -- experiments-smoke CI job (ubuntu-latest, py3.12 only)"
affects:
  - "Phase 19.1 close-out: ROADMAP SC2/SC3/SC4 satisfied"
  - "Phase 21 (DATA-01a: enlarge the published Zenodo archive to the full frameset)"
---

# Phase 19.1 Plan 07: Deletions, README, Stale-Path Sweep, CI Smoke Job Summary

**Deleted `tests/synthetic/experiments.py` (20-def keep/drop table, all DROP) and
`compare_refractive.py` now that the deletion gate is cleared; removed both notebooks'
export cells narrowly; swept four stale `experiments/` path references; wrote
`experiments/README.md`'s seven required sections; and added a single-platform
`experiments-smoke` CI job. 874 tests pass; `sphinx-build -W` and `pre-commit
run --all-files` both exit 0.**

## Precondition check

Read `19.1-E1-REPRODUCTION.md` § Deletion gate before starting: **CLEARED**. E1's
`--check` failed at `CHECK_RTOL=1e-6` on all three CSVs, but every mismatch is
last-significant-digit floating-point noise (worst cell: focal error
`-0.0015570213997693%` committed vs `-0.0015561905541000927%` fresh), no named
headline number moved, and `CHECK_RTOL` was not widened. Proceeded with the deletions
per the plan's explicit gate instruction and the harness's `DELETION_GATE_IS_CLEARED`
context.

## Task 1: `tests/synthetic/experiments.py` and `compare_refractive.py` deletion

Live `grep -n "^def " tests/synthetic/experiments.py` (run before deletion) matched the
20-def enumeration in the plan's `<interfaces>` exactly:

```
38 run_experiment_1                    102 _plot_focal_length_error
147 _plot_camera_xy_positions           256 _plot_camera_z_error
299 _plot_distortion_error              360 _save_parameter_errors_csv
400 run_experiment_2                    585 _plot_signed_error_vs_depth
630 _plot_rmse_vs_depth                 674 _plot_scale_factor_vs_depth
719 _plot_xy_heatmaps                   799 _plot_single_xy_heatmap
847 _save_depth_metrics_csv             896 run_experiment_3
1094 _plot_exp3_rmse_vs_depth           1134 _plot_exp3_focal_error_vs_depth
1174 _plot_exp3_z_error_vs_depth        1214 _plot_exp3_xy_heatmaps
1281 _save_exp3_depth_scaling_csv       1330 assemble_summary
```

### Keep/drop table (all 20 rows, mandatory)

| # | Def | Kind | Verdict | Reason |
|---|---|---|---|---|
| 1 | `run_experiment_1` | runner | DROP | Superseded by the notebook's implementation, itself now superseded by `experiments/e1_refractive_comparison.py`. |
| 2 | `_plot_focal_length_error` | plot | DROP | Out of scope for `experiments/` under P1 -- measurement and presentation are separate programs; plotting belongs to `DissertationFigures`. |
| 3 | `_plot_camera_xy_positions` | plot | DROP | Same as #2. |
| 4 | `_plot_camera_z_error` | plot | DROP | Same as #2. |
| 5 | `_plot_distortion_error` | plot | DROP | Same as #2. |
| 6 | `_save_parameter_errors_csv` | CSV writer | DROP | Emits a WIDE schema (`_refr`/`_nonrefr` suffix columns) that appears nowhere downstream; the committed CSVs are long-format. Verified by grepping the figures repo for a consumer of the wide schema and finding none. |
| 7 | `run_experiment_2` | runner | DROP | Same as #1. |
| 8 | `_plot_signed_error_vs_depth` | plot | DROP | Same as #2. |
| 9 | `_plot_rmse_vs_depth` | plot | DROP | Same as #2. |
| 10 | `_plot_scale_factor_vs_depth` | plot | DROP | Same as #2. |
| 11 | `_plot_xy_heatmaps` | plot | DROP | Same as #2. |
| 12 | `_plot_single_xy_heatmap` | plot | DROP | Same as #2. |
| 13 | `_save_depth_metrics_csv` | CSV writer | DROP | Wide schema, no downstream consumer -- same reasoning as #6. |
| 14 | `run_experiment_3` | runner | DROP | Same as #1. |
| 15 | `_plot_exp3_rmse_vs_depth` | plot | DROP | Same as #2. |
| 16 | `_plot_exp3_focal_error_vs_depth` | plot | DROP | Same as #2. |
| 17 | `_plot_exp3_z_error_vs_depth` | plot | DROP | Same as #2. |
| 18 | `_plot_exp3_xy_heatmaps` | plot | DROP | Same as #2. |
| 19 | `_save_exp3_depth_scaling_csv` | CSV writer | DROP | Wide schema, no downstream consumer -- same reasoning as #6. |
| 20 | `assemble_summary` | summary | DROP | Read in full (lines 1330-end) before verdict, per plan instruction. It reads `exp1_parameter_errors.csv`/`exp2_depth_metrics.csv` expecting the **superseded wide-schema columns** (e.g. `focal_length_error_pct_refr`, `z_error_mm_nonrefr`) that no longer exist in the current long-format committed CSVs -- porting it as-is would silently break on read. E1's own print statements plus `experiments/README.md`'s provenance table and gauge/framing sections already cover the human-readable-summary role this function targeted. `experiments/e1_refractive_comparison.py` does not need a console/README summary-table builder it does not already have; nothing was ported. |

`git rm tests/synthetic/experiments.py tests/synthetic/compare_refractive.py`. E1's CLI
entry point is `python -m experiments.e1_refractive_comparison` (D-13). Not touched:
`tests/synthetic/experiment_helpers.py` (already a re-export shim from plan 19.1-01),
`tests/synthetic/ground_truth.py`, `tests/synthetic/test_full_pipeline.py`,
`tests/synthetic/test_per_camera_interface.py`.

## Task 2: notebook export-cell deletion and stale-path sweep

- `docs/tutorials/02_synthetic_validation.ipynb`: removed cell id `e3jl81minof`
  (40 -> 39 cells). Confirmed before removal that it builds `_mz`, `rows_exp1`,
  `rows_exp2`, `rows_spatial`, `rows_exp3` and writes four CSVs, and that the
  notebook's only cell after it is a markdown "Summary" cell referencing no export
  variable.
- `docs/tutorials/01_full_pipeline.ipynb`: removed cell at 0-based index 26
  (28 -> 27 cells), content-verified first: source starts with
  `OUTPUT_DIR.mkdir(parents=True, exist_ok=True)` and contains `rows_cameras`. The
  following (final) cell is a markdown "Summary" cell with no dependency on it.
- Both edits used `json.dump(..., indent=1, ensure_ascii=False)` plus a trailing
  newline, matching nbformat's convention (`ensure_ascii=False` was necessary --
  the default `ensure_ascii=True` would have escaped every em-dash/en-dash in the
  notebooks' prose to `\uXXXX`, producing a diff far wider than the removed cell;
  caught and corrected before committing).
- `git diff --stat docs/tutorials/` confined to the two notebooks; both re-validated
  as nbformat (`nbformat.validate`); `grep -c 'RIG_SIZE = "large"'` and
  `grep -c "tests.synthetic.ground_truth"` on notebook 02 are unchanged from the base
  commit (0 and 1 respectively -- both counts are 0 for the first because the JSON
  escapes the literal `"large"` inside `\"large\"`, matching the pre-edit file
  byte-for-byte on that count) -- X4's pin and import rewrite were NOT applied.
- Merged `sphinx-build -W docs docs/_build/html` -- exits 0 (build directory removed
  after verification, not committed).

### X6 stale-path sweep (after the moves)

- `.planning/architecture.md:256` -- `tests/synthetic/compare_refractive.py` runner
  reference replaced with `experiments/e1_refractive_comparison.py`, invoked as
  `python -m experiments.e1_refractive_comparison`.
- `.planning/codebase/STRUCTURE.md` -- `tests/synthetic/` entry updated (removed
  `compare_refractive.py`/`experiments.py`, noted `experiment_helpers.py` as a
  re-export shim, added `test_per_camera_interface.py`); new `experiments/` package
  block added with its actual contents (`__init__.py`, `README.md`, `_io.py`,
  `_render.py`, `e1_refractive_comparison.py`, `e2_real_rig.py`,
  `e4_benchmark_grid.py`, `e7_interface_ablation.py`, `results/`); the
  `pytest tests/synthetic/` run-line and the "Helpers: Add to
  `tests/synthetic/experiment_helpers.py`" guidance both updated to point at
  `experiments/` / `aquacal.datasets.pipelines`; the file-naming-convention example
  (`compare_refractive.py`) replaced with `e1_refractive_comparison.py`.
- `.planning/codebase/CONCERNS.md:21-36` -- deleted the stale "Missing import in
  synthetic experiments" tech-debt entry and its paired "B.2" known-bug entry; both
  described a missing-import defect in a file that no longer exists, making the
  claim moot regardless of whether it was ever accurate.
- `.planning/phases/19-*/*.md` and all other phase history -- untouched
  (`git diff --stat .planning/phases/` is empty).
- Post-sweep verification: `grep -rn "compare_refractive\|tests/synthetic/experiment_helpers\.py" .planning/architecture.md .planning/codebase/` returns nothing.

## Task 3: `experiments/README.md` and the `experiments-smoke` CI job

`experiments/README.md` (189 lines) carries all seven required sections: what the
directory is, the five-flag CLI contract (including why `--check` is numeric, not
byte-exact), the provenance table (one row per artifact under
`experiments/results/`, every runtime a measured value), the honest two-path
statement for E2 (default = published archive, currently 1,817 comparisons; `--config`
= full local frameset, reproduces §3 exactly; the gap is DATA-01a, Phase 21), the
gauge-freedom correction (including why `cam0`'s `xy_position_error_mm` reads exactly
`0.0`), what "fixed intrinsics" means in E7 (Pitfall 3 framing), how to read E7's
output (height/distance failure, `reprojection_rms_px_control` as a control not
evidence, refine-ON arms as AquaCal's own rationale not "what CalibMar produces"),
and the reproduce-a-number command sequence.

Measured runtimes recorded (per the harness's explicit override of the plan's
pre-run estimates):
- **E1**: ~20-25 min per default two-model, eight-depth invocation, derived from
  19.1-06-SUMMARY.md's ~90 min total across four calibration-pairs run during that
  plan (`--check`, an isolated headline-verification pair, `--force`).
- **E2 (full frameset)**: ~50 min, per the harness's stated context (the delta
  table's own measured ~8 min reflects the smaller, archive-only run; the full local-
  video frameset run this README documents as the reproducing path is the ~50 min
  figure).
- **E7 (all four arms)**: ~7 min, per 19.1-05-SUMMARY.md's `--force` regeneration
  note.

`.github/workflows/test.yml` gained a new `experiments-smoke` job: `ubuntu-latest`,
Python 3.12 only, no `strategy` key, using the `test` job's install step
(`pip install -e ".[dev]"`), running the three `--smoke` invocations in sequence. The
existing `test` job's 2x3 matrix (`ubuntu-latest`/`windows-latest` x
`3.10`/`3.11`/`3.12`) is byte-unchanged.

Verified locally:
- All three `--smoke` commands exit 0.
- `python -m experiments.e2_real_rig --smoke` prints
  `SKIPPED: real-rig dataset not cached locally ...` and exits 0 (no cached dataset
  in this worktree, so the absent-dataset branch ran naturally -- no cache had to be
  moved aside).
- `pre-commit run --all-files` exits 0.
- `python -c "import yaml; ..."` assertions confirm the new job is single-platform
  and un-matrixed, and the existing `test` matrix is unchanged.

## Verification

- `! test -f tests/synthetic/experiments.py && ! test -f tests/synthetic/compare_refractive.py` -- confirmed.
- `grep -rn "tests\.synthetic\.experiments\b\|compare_refractive" --include=*.py --include=*.yml --include=*.toml --include=*.cfg . | grep -v "^\./\.planning/"` -- no output.
- `python -m pytest tests/ -m "not slow" -q` -- **874 passed**, 33 deselected (unchanged from before this plan's deletions -- `experiments.py` was never pytest-collected).
- `ruff check . && ruff format --check .` -- both pass.
- Notebook cell counts: 27 (`01_full_pipeline.ipynb`) and 39 (`02_synthetic_validation.ipynb`); both validate as nbformat; no `to_csv` or `rows_exp1` remains in either.
- `sphinx-build -W docs docs/_build/html` -- exits 0 (merged docs build).
- `grep -q "gauge" experiments/README.md` -- succeeds; provenance-completeness check (every file under `experiments/results/` except `.gitkeep` named in the README) -- succeeds.
- `.github/workflows/test.yml` has three jobs; `experiments-smoke` is single-platform; the `test` matrix is unchanged.
- `pre-commit run --all-files` -- exits 0.

## Task Commits

1. **Task 1: Delete experiments.py and compare_refractive.py** -- `706faf4` (feat)
2. **Task 2: Delete notebook export cells, sweep stale paths** -- `d65dac6` (docs)
3. **Task 3: Write experiments/README.md, add experiments-smoke CI job** -- `3d88670` (docs)

## Deviations from Plan

None -- plan executed exactly as written. The `ensure_ascii=False` correction during
Task 2 (see above) was caught and fixed before any commit; it is not a deviation from
the plan's intent (a minimal, confined diff), just a mechanical detail of getting
there.

## Issues Encountered

- The plan's Task 3 verify-script cross-check requires each README runtime to trace
  to a measured wave-3/4 SUMMARY value. E1's default single-invocation runtime is not
  independently measured in isolation -- 19.1-06-SUMMARY.md only records the
  aggregate ~90 min covering four calibration-pairs run for different purposes
  (`--check`, verification, `--force`). The README states this derivation explicitly
  (~20-25 min per default invocation, derived from dividing the measured aggregate)
  rather than presenting an unmeasured round number as if it were directly measured.

## Next Phase Readiness

- ROADMAP SC4 satisfied: `tests/synthetic/experiments.py` is gone with its keep/drop
  verdict auditable; `compare_refractive.py` is gone, `experiments/e1_refractive_comparison.py`
  is E1's entry point; no two implementations of any experiment remain in the repo.
- ROADMAP SC2 satisfied: `experiments/` has `_io.py`, `_render.py`, `results/`, and a
  README mapping one command to each paper artifact with its runtime.
- ROADMAP SC3 satisfied: every script honors the five-flag contract and `--smoke`
  runs in CI (single platform).
- D-07's gauge explanation exists in both `pipelines.py`'s docstring and
  `experiments/README.md`.
- No blockers identified for Phase 19.1's wave-6 close-out gate (D-16, E2's delta
  table adjudication) or for Phase 21's DATA-01a.

## Self-Check: PASSED

- `tests/synthetic/experiments.py` -- MISSING (deleted, expected)
- `tests/synthetic/compare_refractive.py` -- MISSING (deleted, expected)
- `experiments/README.md` -- FOUND
- `.github/workflows/test.yml` (experiments-smoke job) -- FOUND
- `docs/tutorials/01_full_pipeline.ipynb` (27 cells) -- FOUND
- `docs/tutorials/02_synthetic_validation.ipynb` (39 cells) -- FOUND
- `706faf4` -- FOUND in `git log --oneline`
- `d65dac6` -- FOUND in `git log --oneline`
- `3d88670` -- FOUND in `git log --oneline`
- `python -m pytest tests/ -m "not slow" -q` -- 874 passed

---
*Phase: 19.1-experiment-suite-consolidation*
*Completed: 2026-07-27*
