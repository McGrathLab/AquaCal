# 19.1-08 — E2 Gate Response (D-16 blocking human verification)

**Responded:** 2026-07-27
**Respondent:** user (tlancaster6)
**Gate artifact reviewed:** `19.1-E2-DELTA-TABLE.md` (rebuilt version), supported by
`19.1-E2-FRAMESET-PROVENANCE.md` and `19.1-E1-REPRODUCTION.md`.

This is the explicit, recorded user response D-16 requires. It was **not** auto-approved;
`workflow.auto_advance` was disabled for this phase in `556206c` before execution began.

---

## Item 1 — §3 real-rig numbers

**Response: CONFIRMED — no manuscript prose edit required.**

All nine §3 quantities reproduce at **0.000% delta** when the current library is run against
the same frameset that produced them (262 usable frames → 210 calibration / 52 validation,
7,762 comparisons):

| # | Quantity | §3 | Fresh run | Δ% |
|---|---|---|---|---|
| 1 | Mean reprojection (mean of per-camera RMS) | 0.88 px | 0.87863 | 0.000% |
| 2 | Per-camera reprojection range | 0.54–2.41 px | 0.53938–2.40788 | 0.000% |
| 3 | Auxiliary fisheye `e3v8250` | 15.1 px | 15.13431 | 0.000% |
| 4 | Inter-corner MAE | 0.268 mm | 0.26815 | 0.000% |
| 5 | Inter-corner RMSE | 0.674 mm | 0.67407 | 0.000% |
| 6 | Mean relative error | 0.45% | 0.44692 | 0.000% |
| 7 | Number of comparisons | 7,762 | 7,762 | 0.000% |
| 8 | Recovered `z_w` | ≈1.03 m | 1.03055 | 0.000% |
| 9 | Camera height range | 1.01–1.08 m | 1.00828–1.08153 | 0.000% |

`reject_outlier_frames` (v1.7) did not fire; v1.8's seeded `cv2.calibrateCamera` left the
result bit-stable. **Phases 16–19 are numerically inert on real data** — demonstrated, not
asserted.

**Caveat raised and then RESOLVED (2026-07-27, after the initial response).**

The concern as first recorded was that `release_calibration\diagnostics.json` was *inferred*
to be §3's source rather than confirmed. On the user's prompting this was re-examined and the
concern was found to be aimed at the wrong link in the chain. Whether that file is the
original run or a later re-run is immaterial: what the zero-drift claim needs is
(a) current library + this frameset → these numbers, measured twice deterministically, and
(b) these numbers = the manuscript's §3 values. Only (b) was unverified.

**(b) has now been verified directly against the live manuscript**,
`OneDrive - Georgia Institute of Technology\Thesis\Spinoffs\papers\aquacal\main.tex`
(modified 2026-06-29 — the live source, NOT the stale `Desktop\main.pdf` export). All nine
values appear as pinned in D-16:

- line 300 — "mean \SI{0.88}{px}, individual cameras 0.54--\SI{2.41}{px}"
- line 301 — "mean absolute error of \SI{0.268}{\milli\meter} (RMSE \SI{0.674}{\milli\meter},
  0.45\% mean relative error) across 7,762 comparisons"
- line 303 — "auxiliary fisheye camera achieved \SI{15.1}{px} reprojection RMS"
- lines 69, 300–303 — the `z_w` ≈ 1.03 m and 1.01–1.08 m camera-height figures

**Independent corroboration.** Line 300 states the run used "200 calibration frames, 52
validation frames". The fresh run produced **52 validation frames** exactly, and 210
calibration frames capped to **200** by `max_calibration_frames: 200`. The manuscript's own
frame accounting therefore matches the reproduced frameset, via a source entirely independent
of `diagnostics.json`.

The caveat is withdrawn. The §3 confirmation stands on verified ground.

## Item 2 — E1 deletion gate

**Response: CLEARED** (recorded separately in `19.1-E1-REPRODUCTION.md`, commit `f193d60`).

E1's `--check` FAILed at `CHECK_RTOL=1e-6` on all three committed CSVs. Every mismatch is
last-significant-digit floating-point noise on near-zero values — worst cell a focal error of
0.0015570% vs 0.0015562%, an absolute difference of 8.3e-7 percentage points. All ten of
D-19's named headline numbers reproduce to 5+ significant figures. Both RESEARCH diagnostic
checks passed. `CHECK_RTOL` was never widened. Plan `19.1-07`'s deletions proceeded on this
basis and are complete.

## Item 3 — the archive fork

**Response: ENLARGE — proceed with both DATA-01a and DATA-01b.**

The published Zenodo archive is regenerated to carry **the frameset that produced §3**, and
the large output artifacts move into that same archive:

- **DATA-01a** — expanded `extrinsic/` image dirs (60 → 262 frames per camera, ~685 MB total;
  `intrinsic/` unchanged). Two configs ship in the archive: `config_paper.yaml` at
  `frame_step: 1` reproduces §3 exactly, and the tutorial `config.yaml` at `frame_step: 4`–`5`
  keeps tutorial 01 at today's ~8 min. `frame_step` is confirmed to apply identically to image
  directories and to video (`ImageSet.iterate_frames` honours `step`; files are `natsort`ed).
- **DATA-01b** — `calibration.json`, `reprojection_residuals.csv`,
  `reconstruction_errors.csv`, and the regenerable `exp2_spatial_errors.csv` /
  `interface_ablation_conditioning.npz` move out of git and into the archive. Split by
  function, not size: `--check` baselines and offline CI smoke need only ~20 KB of small CSVs,
  which stay versioned.

Rationale accepted: enlarging requires **no manuscript prose edits** (Item 1 shows the full
frameset reproduces §3 exactly), the archive becomes self-contained (inputs *and* reference
outputs), the repo drops from 4.1 MB to ~0.7 MB, and the
`exclude: ^experiments/results/` on `check-added-large-files` is removed — restoring the
1000 KB guard repo-wide.

Both requirements are Phase 21 work and are that phase's **critical path**, since notebook
re-execution (DATA-03) and the release cut (Phase 22) both consume the new archive, and the
manuscript cites its DOI. Due 2026-08-21.

---

## Phase disposition

**Phase 19.1 is cleared to close.** All eight plans complete; the blocking gate has an
explicit recorded user response on every live item.

### What this phase actually found

Four defects, **none of them in the library**:

| defect | location | caught by |
|---|---|---|
| Stage-2 called without refraction params | E7 harness | solver optimality 1.5e11 / `xtol` termination |
| Frameset mismatch (60 vs 262 frames) | E2 comparison | user |
| Pooled RMS compared against mean-of-per-camera | E2 metrics | reading provenance strings |
| `load_config` import error | orchestrator's `--config` addition | runtime crash |

Two produced **confident, plausible, wrong** answers before being caught: the first delta
table assigned named mechanisms and Δ% values to six invalid comparisons, and E7 initially
showed per-camera mode beating shared on a solve that had never converged. In both cases the
science numbers looked reasonable on their own; the discriminators were solver metadata and
provenance strings, not the values themselves.

That is the phase's most transferable finding, and it is why the gates earned their cost:
**a plausible delta with a plausible mechanism is not evidence that anything changed.**
