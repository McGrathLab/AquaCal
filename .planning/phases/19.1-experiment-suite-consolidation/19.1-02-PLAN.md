---
phase: 19.1-experiment-suite-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/__init__.py
  - experiments/_render.py
  - experiments/e4_benchmark_grid.py
  - experiments/results/.gitkeep
  - benchmarks/aggregate.py
  - benchmarks/sweep_runner.py
  - tests/unit/test_experiments_render.py
  - tests/unit/test_benchmarks_runner.py
autonomous: true
requirements: [EXP-02, EXP-03]

must_haves:
  truths:
    - "`experiments/` is a real importable top-level Python package invoked as `python -m experiments.eN_name`, and it never ships in the wheel because packages.find is scoped to where=['src'] (D-01)"
    - "Phase 19's LaTeX/CSV aggregator lives at experiments/_render.py with its logic unchanged, so _render.py has a real implementation rather than a placeholder (D-03)"
    - "benchmarks/sweep_runner.py lives at experiments/e4_benchmark_grid.py as a relocated skeleton that is NOT run in this phase (D-03; running the grid is 19.2/EXP-08)"
    - "The top-level benchmarks/ directory no longer exists and nothing imports from it (EXP-03)"
    - "The relocated aggregator's existing test assertions still pass under the new import path, with the sys.path.insert hack dropped because experiments/ is a real package (D-01, D-04)"
    - "experiments/ is linted by Ruff/pre-commit but never collected by pytest; its tests live in tests/unit/test_experiments_*.py (D-04)"
  artifacts:
    - path: "experiments/__init__.py"
      provides: "package marker making `python -m experiments.X` work"
    - path: "experiments/_render.py"
      provides: "CSV + LaTeX rendering, recomputes nothing"
      contains: "SUPPORTED_SCHEMA_VERSION"
    - path: "experiments/e4_benchmark_grid.py"
      provides: "relocated cameras x frames sweep skeleton, not run this phase"
      exports: ["run_sweep"]
    - path: "tests/unit/test_experiments_render.py"
      provides: "the relocated aggregator/sweep test assertions"
  key_links:
    - from: "tests/unit/test_experiments_render.py"
      to: "experiments._render"
      via: "direct package import, no sys.path manipulation"
      pattern: "from experiments\\._render import"
    - from: "experiments/e4_benchmark_grid.py"
      to: "experiments._render"
      via: "__main__ block import"
      pattern: "from experiments\\._render import"
---

<objective>
Create the `experiments/` package skeleton and relocate Phase 19's verified `benchmarks/`
deliverable into it: `aggregate.py` → `_render.py`, `sweep_runner.py` → `e4_benchmark_grid.py`,
`benchmarks/` deleted, and `tests/unit/test_benchmarks_runner.py` →
`tests/unit/test_experiments_render.py` with its `sys.path` hack dropped.

Purpose: this is the container every experiment script lands in, and the relocation is what gives
`_render.py` a real, already-tested implementation instead of a placeholder (D-03). It touches no
file that plan 19.1-01 touches, so it runs in parallel.
Output: an importable `experiments/` package with two relocated modules and a green relocated
test file; no `benchmarks/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Base commit:** fork from `93cb1f37339091fd137c9e79b222b1c4c48c6131`. Verify with
`git merge-base HEAD 93cb1f3` before starting.
**PYTHONPATH:** in a worktree, `export PYTHONPATH="$(pwd)/src"` before pytest.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-CONTEXT.md
@.planning/phases/19.1-experiment-suite-consolidation/19.1-PATTERNS.md

<interfaces>
<!-- Verified live 2026-07-25. -->

`benchmarks/aggregate.py` public surface (moves verbatim to `experiments/_render.py`):
  SUPPORTED_SCHEMA_VERSION, UnsupportedSchemaVersionError, _flatten_record, aggregate,
  write_csv, _mixed_memory_mode_values, _LATEX_SPECIALS, _latex_escape,
  write_latex_fragment, _build_arg_parser, __main__ block

`benchmarks/sweep_runner.py` public surface (moves verbatim to `experiments/e4_benchmark_grid.py`):
  run_sweep, _build_arg_parser, __main__ block.
  Imports only the public API (`aquacal.calibration.load_config`,
  `run_calibration_from_config`) — confirmed no private-module imports at `sweep_runner.py:25`.

`tests/unit/test_benchmarks_runner.py` current import block (lines 1-27) — the `sys.path.insert`
is what must NOT survive the move:
  sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
  from benchmarks.aggregate import (SUPPORTED_SCHEMA_VERSION, UnsupportedSchemaVersionError,
                                    aggregate, write_csv, write_latex_fragment)
  from benchmarks.sweep_runner import run_sweep

Fixture directory used by that test file, resolved relative to the test file's own location —
needs NO move as long as the file stays under `tests/unit/`:
  tests/unit/fixtures/benchmark_records/   (3 JSON fixtures)

`pyproject.toml:73-74` — `[tool.setuptools.packages.find] where = ["src"]`. This is why a
top-level `experiments/` package is never discovered and never ships. Do NOT add an exclude rule
"to be safe" (D-01) — it would imply the directory was at risk.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the experiments/ package and relocate benchmarks/ into it</name>
  <files>experiments/__init__.py, experiments/_render.py, experiments/e4_benchmark_grid.py, experiments/results/.gitkeep, benchmarks/aggregate.py, benchmarks/sweep_runner.py</files>
  <read_first>
    - `benchmarks/aggregate.py` — the FULL file (261 lines). This moves verbatim.
    - `benchmarks/sweep_runner.py` — the FULL file (140 lines). This moves verbatim.
    - `19.1-PATTERNS.md` §§ `experiments/_render.py`, `experiments/e4_benchmark_grid.py`,
      `experiments/__init__.py` — the relocation instructions and the exact set of lines permitted
      to change.
    - `src/aquacal/io/__init__.py` lines 1-26 — the docstring + barrel convention to scale down
      for `experiments/__init__.py`.
    - `19.1-CONTEXT.md` D-01, D-02, D-03.
    - `pyproject.toml` lines 65-90 — confirm `packages.find where = ["src"]` and the Ruff config
      (line-length 88, no exclusion for `experiments/`).
  </read_first>
  <action>
Use `git mv` for both relocations so history follows the files.

Create `experiments/__init__.py` with a one-line module docstring: "Paper experiment scripts for
AquaCal (not shipped; see experiments/README.md)." No imports, no `__all__` — this is a package
marker, not a barrel. `experiments/` is not part of the installed package (D-01), so
`.claude/rules/source-code.md`'s barrel requirement does not apply to it.

Create `experiments/results/` containing a `.gitkeep` so the committed-results directory exists
before any script writes to it.

`git mv benchmarks/aggregate.py experiments/_render.py`. Change exactly ONE thing in the moved
file: the module docstring's path reference, which currently says the module lives at the
repository root under `benchmarks/` — update it to `experiments/`. No other line changes (D-03:
"logic unchanged"). Keep `SUPPORTED_SCHEMA_VERSION` at its Phase-19 value; the `benchmark.json`
schema is settled and must not be revisited (REQUIREMENTS cross-cutting #10).

`git mv benchmarks/sweep_runner.py experiments/e4_benchmark_grid.py`. Change exactly two things:
the module docstring's path reference, and the `__main__` block's
`from benchmarks.aggregate import ...` → `from experiments._render import ...`. Do NOT run this
script in this phase — running the grid is 19.2/EXP-08 (D-03).

Delete the now-empty `benchmarks/` directory including its `__pycache__`.

Do not create `e3_*.py`, `e5_*.py`, or `e6_*.py` — those are Phase 19.2's files, and an empty stub
is worse than an absence because a stub passes `--smoke` while measuring nothing (D-02).
Do not create `_io.py` here — that is plan 19.1-03's file.
  </action>
  <verify>
    <automated>python -c "import experiments, experiments._render as r, experiments.e4_benchmark_grid as g; assert hasattr(r,'aggregate') and hasattr(r,'write_latex_fragment') and hasattr(r,'SUPPORTED_SCHEMA_VERSION') and hasattr(g,'run_sweep'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import experiments._render, experiments.e4_benchmark_grid"` exits 0 with no `sys.path` manipulation.
    - `! test -d benchmarks` — the directory is gone.
    - `test -f experiments/__init__.py && test -f experiments/_render.py && test -f experiments/e4_benchmark_grid.py && test -d experiments/results` all pass.
    - `git log --follow --oneline -- experiments/_render.py | wc -l` is greater than 1 (history followed the move; i.e. `git mv` was used, not delete+create).
    - `diff <(git show 93cb1f3:benchmarks/aggregate.py | grep -v '^#' | grep -v benchmarks) <(grep -v '^#' experiments/_render.py | grep -v experiments)` shows no differences outside the docstring path reference. Equivalent manual check acceptable; the requirement is that no functional line changed.
    - `grep -c "from benchmarks" experiments/e4_benchmark_grid.py` returns 0.
    - `grep -c "packages.find\|exclude" pyproject.toml` is unchanged from the base commit — no build-config edit was made.
    - `ruff check experiments/ && ruff format --check experiments/` exits 0.
    - `python -m pytest tests/ -m "not slow" -q --collect-only 2>&1 | grep -c "experiments/"` returns 0 — pytest never collects `experiments/`.
  </acceptance_criteria>
  <done>`experiments/` is importable, contains `__init__.py`, `_render.py`, `e4_benchmark_grid.py`, and `results/`; `benchmarks/` is deleted; both relocated files are functionally byte-identical to their sources; no build-config change was made.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate the aggregator's test file and drop its sys.path hack</name>
  <files>tests/unit/test_experiments_render.py, tests/unit/test_benchmarks_runner.py</files>
  <read_first>
    - `tests/unit/test_benchmarks_runner.py` — the FULL file. Every test body moves unchanged;
      only the import block changes.
    - `19.1-PATTERNS.md` § `tests/unit/test_experiments_render.py` — the exact old-vs-new import
      pattern, and the `FIXTURES_DIR` / `_copy_fixture` fixture pattern to keep unchanged.
    - `19.1-RESEARCH.md` § "The `benchmarks/` relocation — files touched" — the row confirming
      `tests/unit/fixtures/benchmark_records/` needs NO move because the path resolves relative to
      the test file's own location.
  </read_first>
  <action>
`git mv tests/unit/test_benchmarks_runner.py tests/unit/test_experiments_render.py`.

Change ONLY the import block:
  - Delete the `sys.path.insert(0, str(Path(__file__).resolve().parents[2]))` line and the now-unused
    `import sys` if nothing else uses it. `experiments/` is a real top-level package (D-01), and
    pytest's rootdir already includes the repo root, so no path manipulation is needed.
  - `from benchmarks.aggregate import (...)` → `from experiments._render import (...)`, same name list.
  - `from benchmarks.sweep_runner import run_sweep` → `from experiments.e4_benchmark_grid import run_sweep`.
  - Remove any now-unused `# noqa: E402` markers that existed only because the `sys.path` insert
    forced imports below top-of-file.

Keep `FIXTURES_DIR = Path(__file__).parent / "fixtures" / "benchmark_records"` and `_copy_fixture`
exactly as they are — the fixtures do not move.

Do NOT change any test body, assertion, parametrization, or test name (D-03: "logic unchanged").
If a test's docstring names `benchmarks/`, updating that string is permitted; changing what it
asserts is not.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_experiments_render.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_experiments_render.py -x -q` exits 0, with the same number of tests collected as `git show 93cb1f3:tests/unit/test_benchmarks_runner.py` defined (compare `grep -c "def test_"`).
    - `! test -f tests/unit/test_benchmarks_runner.py`.
    - `grep -c "sys.path\|noqa: E402" tests/unit/test_experiments_render.py` returns 0.
    - `grep -c "from benchmarks" tests/unit/test_experiments_render.py` returns 0.
    - `test -d tests/unit/fixtures/benchmark_records` still passes and the fixture count is unchanged.
    - `python -m pytest tests/ -m "not slow" -q` exits 0 with at least 846 passed.
    - `! grep -rn "from benchmarks\|benchmarks\.aggregate\|benchmarks\.sweep_runner" --include=*.py --include=*.yml --include=*.toml --include=*.cfg . | grep -v "^\./\.planning/"` — no live reference to the old package remains outside phase history.
  </acceptance_criteria>
  <done>The relocated test file passes with the same test count and identical assertions, imports `experiments._render` / `experiments.e4_benchmark_grid` directly with no path manipulation, and no live reference to `benchmarks` remains outside `.planning/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem ← `_render.py`'s `--root` | `aggregate()` walks a user-supplied directory tree and parses `benchmark.json` files found there. Pre-existing Phase-19 behavior, moved verbatim; inputs are repo-committed or locally-produced files, not network data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-04 | Tampering | Relocation silently changes aggregator behavior (a verified-complete Phase 19 deliverable) | mitigate | Verbatim `git mv` with only docstring path references and one import line permitted to change; proven by the relocated test file passing with an unchanged test count and unchanged assertions. |
| T-19.1-05 | Tampering | `experiments/` accidentally ships in the wheel, exposing unreviewed scripts to installers | accept | Verified impossible without a config change: `pyproject.toml:73-74` scopes `packages.find` to `where=["src"]`. Acceptance criteria assert no build-config edit was made. |
| T-19.1-06 | Tampering | `_render.py` parsing an untrusted `benchmark.json` | accept | Inputs are repo-committed or locally-generated files under version control, not arbitrary external data (RESEARCH § Security Domain). `json.load` is appropriate at this trust level; no schema-validation library warranted. The existing `UnsupportedSchemaVersionError` already refuses unexpected schema versions and names the offending file. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. RESEARCH § Package Legitimacy Audit: not applicable — this phase installs no new external packages. |
</threat_model>

<verification>
1. `python -c "import experiments._render, experiments.e4_benchmark_grid"` — exits 0.
2. `python -m pytest tests/unit/test_experiments_render.py -x -q` — exits 0.
3. `python -m pytest tests/ -m "not slow" -q` — exits 0, at least 846 passed.
4. `! test -d benchmarks && ! test -f tests/unit/test_benchmarks_runner.py`.
5. `! grep -rn "from benchmarks\|benchmarks\.aggregate\|benchmarks\.sweep_runner" --include=*.py --include=*.yml . | grep -v "^\./\.planning/"`.
6. `ruff check . && ruff format --check .` — exits 0.
7. `pre-commit run --all-files` — exits 0 (confirms `experiments/` is linted, D-04).
</verification>

<success_criteria>
- ROADMAP SC2 (partial): `experiments/` exists outside `src/` with `_render.py` (reads CSV/JSON,
  recomputes nothing) and a `results/` directory.
- ROADMAP SC4 (partial): the Phase 19 `benchmarks/` runner has moved under `experiments/`.
- `python -m experiments.e4_benchmark_grid --help` works; the grid itself is NOT run (D-03).
- The relocated test file proves the move was behavior-preserving.
</success_criteria>

<output>
Create `.planning/phases/19.1-experiment-suite-consolidation/19.1-02-SUMMARY.md` when done.
Record: the exact diff surface of both relocations (which lines changed and why), and the test
count before/after the test-file move.
</output>
